# Polish — Prose Lens — addendum.md — 2026-09-09 (third update)

- **Lens:** prose (bmad-review, run after the structure lens; its one applied finding — the "Reviewer Gate" heading case — was honored; no CUT or MERGE passages)
- **Target:** `addendum.md` only; attention on Source Inputs bullet 4, the "Options Considered — Third 2026-09-09 Update" section, its "Reviewer Gate" subsection, and the two second-update bullets amended today
- **Style guide:** Microsoft Writing Style Guide (baseline), US spelling
- **Voice noted and preserved:** clinical decision-record voice; terse "Rejected; …" / "Rejected in favor of …" / "… was rejected: …" openers; backticked code identifiers, plain register identifiers (OQ, FR, SM, A, AD, finding codes); PRD-defined terms capitalized (Conversation, Party, Provider, Platform, Spine); spaced em dashes; straight quotes for quoted rule text; `..` range notation in the reviewer-gate code list, mirroring the reconcile file's own Inputs line.
- **Acceptance rule:** a finding is applied only if it changes no recorded decision or rejected alternative — no bullet dropped, no rationale shortened to lose its reason, no identifier changed.

## Findings

| Pass | Original Text | Revised Text | Changes |
| --- | --- | --- | --- |
| prose | The prior rule let a row added after the `RQ-1` evaluation was scheduled block only once the Release PM acknowledged it, so the gate failed open on silence. | Under the prior rule, a row added after the scheduling of the `RQ-1` evaluation blocked only once the Release PM acknowledged it, so the gate failed open on silence. | Garden-path sentence: "let a row … was scheduled block" stacks three verbs and reads as if the row was scheduled. Restated with the same subject, condition, and consequence. **Applied.** |
| prose | Rejected alternatives from the decisions taken while applying the 2026-09-09T11:01Z validation report, whose PRD-side outcomes are FR amendments, the new OQ-31, and assumptions A-22 through A-26. | Rejected alternatives from the decisions taken while applying the 2026-09-09T11:01Z validation report. Those decisions' PRD-side outcomes are FR amendments, the new OQ-31, and assumptions A-22 through A-26. | "whose" attaches to "validation report" but the outcomes belong to the decisions; sentence split with the antecedent named. **Applied.** |
| prose | …was rejected; more than 5 distinct Conversations in 30 days is one wide inspection needing Platform Operator approval… | …was rejected; inspecting more than 5 distinct Conversations in 30 days is one wide inspection needing Platform Operator approval… | Missing subject: the count alone is not an inspection; the act of inspecting is. Threshold and consequence unchanged. **Applied.** |
| prose | Rejected; the three drafted carried lows landed (UJ-1 wording, …). | Rejected; the three low-severity findings drafted for carrying landed (UJ-1 wording, …). | "carried lows" is run-internal shorthand (as "non-co-located mediums" was in the previous run); expanded to what it denotes. **Applied.** |
| prose | …D-H-1..3, D-M-1..2, D-L-1..2 and the co-located mediums and lows). | …D-H-1..3, D-M-1..2, D-L-1..2, and the co-located medium- and low-severity findings). | Serial comma per the style guide; same shorthand expansion as the row above. The `..` ranges are kept — they mirror the reconcile file's Inputs line the sentence cites. **Applied.** |
| prose | …the grace remains only for changes that tighten, and "the version in force"… | …the grace remains only for changes that tighten protection, and "the version in force"… | "tighten" had no object; "protection" is the object the same sentence gives "loosen". **Applied.** |
| prose | …unapprovable (FR-15, FR-27); the marker was named `SafetyFailed` here and renamed `RestrictedContent`… | …unapprovable (FR-15, FR-27). The marker was named `SafetyFailed` here and renamed `RestrictedContent`… | Sentence split: the second semicolon chained a naming history onto a three-clause rejection rationale. **Applied.** |
| prose | …a caller-supplied version is never stored as given and a stale expected version is a typed rejection (FR-4). / …seam 1 returns a roster version and an absence counts only on a read… | …never stored as given, and a stale expected version… / …returns a roster version, and an absence counts only… | Comma before "and" joining two independent clauses, matching the file's practice elsewhere ("…as a mirror entry, and the branch ends…"). **Applied (2 locations).** |
| prose | Rejected for the discontinuity it produced at five Parties; | Rejected for the discontinuity it produced at 5 Parties; | Numeral consistency: the same bullet's heading and the rest of the file write "5 Parties", "5 distinct Conversations". **Applied.** |
| prose | Consider: expand the reviewer-gate code ranges ("H-1..H-8", "R-M-1..3", …) to the "through" form the file uses for OQ and A ranges? | — | The `..` form mirrors the Inputs line of the cited reconcile section; keeping it lets a reader match the two lists by eye. **Rejected.** |
| prose | Consider: "Automatic mode stays in V1 scope" (§Reviewer Gate, R-H-1 bullet) → "Automatic Response Mode stays in V1 scope"? | — | The bullet's bold title already carries the glossary term in full, and `prd.md` itself uses the "Automatic mode" shorthand once; no casing issue. **Rejected (no comprehension gain).** |
| prose | Consider: **Keeping "the more restrictive of two policies".** — period inside the closing quote per US style? | — | The quotation is verbatim rule text; moving the period inside would present it as part of the quoted rule. **Rejected.** |
| prose | Consider: "the Conversation Agent State, rate limits, and Eligible Approver resolution precede context measurement…" (FR-8 order bullet) → name the first item as a check? | — | The bare noun matches how FR-8 names its steps; adding "check" risks misnaming the step. **Rejected.** |
| prose | Lines 1–55 (title, authority statement, Source Inputs bullets 1–3, Discovery Research Notes, first and second Options Considered sections including today's two amended bullets) | — | Re-read; no communication issue found. Bullets amended today read cleanly with their new closing sentences. |

Applied: 9 rows (10 edits). Rejected: 4. Clean: the remainder of the file.

## Constraints honored

No bullet dropped, no rejected-alternative rationale shortened, no identifier or `prd.md` cross-reference changed; spaced em dashes and US spelling kept; heading case changed only where the structure pass applied it ("Reviewer Gate"); the three Options Considered sections were not merged. `prd.md` untouched.
