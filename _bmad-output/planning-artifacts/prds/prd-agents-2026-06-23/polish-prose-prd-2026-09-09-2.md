# Polish — Prose Lens (bmad-review) — `prd.md`, second 2026-09-09 update

- **Target:** `_bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md`
- **Scope:** same 2026-09-09 material as the structure pass (see `polish-structure-prd-2026-09-09-2.md`); ran after it, on the restructured text.
- **Style guide:** Microsoft Writing Style Guide baseline; author choices preserved — spaced em dashes, US spelling, Title Case headings with capitalized "And"/"Of", code-styled typed identifiers, defined terms capitalized (Agent, Party, Conversation, Approver, Provider), "post hoc" adverbial / "post-hoc" adjectival.
- **Voice noted and preserved:** declarative normative register ("The system …", "A … does not satisfy this requirement"), negative-test sentences that name what fails the requirement, parenthetical FR cross-references at clause end.
- **Constraints honored:** no requirement meaning, typed identifier, number, or ID changed; every `[ASSUMPTION A-n]` tag left in place; FR-18 table cells and §13 rows untouched except one word in OQ-24.

## Findings

| Pass | Original Text | Revised Text | Changes |
| --- | --- | --- | --- |
| prose | FR-28 trigger sub-bullet: "A trigger review, which the Release Operator convenes and records within one business day of the condition — a review not convened in that time is recorded as `TriggerReviewOverdue` on the FR-30 surface — mandatory when, over a rolling seven-day window, any of these rates exceeds its threshold: …" | "A trigger review is mandatory when, over a rolling seven-day window, any of these rates exceeds its threshold: … . The Release Operator convenes and records the review within one business day of the condition; a review not convened in that time is recorded as `TriggerReviewOverdue` on the FR-30 surface." | Applied. The sentence had no main verb; the convening deadline is now its own sentence |
| prose | FR-28 kill-switch bullet: "Pulling it disables Agent Calls tenant-wide, lets proposals … be rejected or abandoned but not approved, edited, or regenerated, holds `Approved` proposals …, lets a `PostingPending` attempt …, pauses the `PostingFailed` retry-window clock …, leaves `ExpiresAt` running — … — never system-abandons anything, and deletes no proposal or Audit Evidence (FR-18)." | Six sentences: disables calls; awaiting proposals; `Approved` held and `PostingPending` completes; retry clock paused; `ExpiresAt` keeps running with `ExpiredWhileSuspended`; never system-abandons, deletes nothing (FR-18). | Applied. One 100-word chain of seven verbs split; every effect retained in order |
| prose | FR-7 re-check: "only after two consecutive passes, at least one cadence apart, each return a roster whose …" | "… each of which returns a roster whose …" | Applied. Subject-verb agreement |
| prose | FR-2 clear rule: "A clear is refused with a typed reason while a mirror entry is sent and unacknowledged, and cancels every outbox entry of a lower `BlockVersion`." | "A clear is refused with a typed reason while a mirror entry is sent and unacknowledged; a clear cancels every outbox entry of a lower `BlockVersion`." | Applied. The original could be read as the refused clear doing the cancelling |
| prose | FR-18 lookup bullet: "… through the seam-2 existence read, except for a `PostingFailed` proposal whose failure was a pre-post re-validation that never attempted a post, for which the lookup is skipped." | "… through the seam-2 existence read. The lookup is skipped for a `PostingFailed` proposal whose failure was a pre-post re-validation that never attempted a post." | Applied. Exception moved out of a 50-word sentence |
| prose | FR-18 lookup bullet: "… is governed by Conversations retention; for a retry exit only a typed absence permits the retry." | "… is governed by Conversations retention. For a retry exit, only a typed absence permits the retry." | Applied. The retry rule was hanging off the abandon-exit aside |
| prose | FR-18 pre-post re-validation: "… passes the then-current Content Safety Policy; a failure moves the proposal to `PostingFailed` …" | "… passes the then-current Content Safety Policy. A failure moves the proposal to `PostingFailed` …" | Applied. 90-word sentence split at the outcome |
| prose | FR-27 cache bullet: "… and Content Safety Policy version: publishing a policy version invalidates …, and an edited or deleted message simply no longer matches its hash …" | "… and Content Safety Policy version. Publishing a policy version invalidates …, and an edited or deleted message no longer matches its hash …" | Applied. Key list separated from its invalidation consequences; "simply" dropped |
| prose | §8 seam 5: "… which every Agent Call, the FR-7 resolution, and the FR-18 pre-post re-validation depend on; the existence and accessibility reads answer with …" | "… depend on. The existence and accessibility reads answer with …" | Applied. 110-word sentence split; `[ASSUMPTION A-15]` stays at its end |
| prose | FR-28 vocabulary bullet: "are additionally additive `AgentLaunchReadinessBlocker` values" | "are also additive `AgentLaunchReadinessBlocker` values" | Applied |
| prose | FR-32 concurrent bound: "— no implicit default, Platform Operator or Release Operator sets it, the Tenant Agent Administrator may only lower it —" | "— no implicit default; the Platform Operator or Release Operator sets it; the Tenant Agent Administrator may only lower it —" | Applied. Three clauses were comma-spliced inside the dashes |
| prose | §3 Platform Operator: "initial caps and rate limits and cap overrides" | "initial caps, rate limits, and cap overrides" | Applied. Parallel list |
| prose | FR-1: "Tenant Agent Administrators then activate and configure it:" | "Tenant Agent Administrators then configure and activate it:" | Applied. Order now matches §4.1 and UJ-1 (configuration precedes activation) |
| prose | §0: "… and the `RQ-1` gate (FR-28), and §8 reports the current dependency status without softening it." | "… and the `RQ-1` gate (FR-28); §8 reports the current dependency status without softening it." | Applied. Double "and" |
| prose | §12 preamble: "… are confirmed or retuned by Product before enablement (A-13)." | "… before enablement." | Applied. The tag `[ASSUMPTION A-13]` two clauses earlier already names the row |
| prose | OQ-24: "Every unretired `A-n` and `ARCH-A-n` row blocks whatever its owner." | "… row blocks `RQ-1` whatever its owner." | Applied. Missing object; matches FR-28 item 9 and §8.1 |
| prose | FR-18 bullet "until one of the following occurs: an Eligible Approver abandons it; the Tenant Agent Administrator abandons it …; an audited administrative retry succeeds; or the FR-7 re-check … abandons it …" | Consider: no change | Checked for parallelism; the four items are already parallel subject-verb clauses |
| prose | FR-33 row "Inspect unposted proposal versions …" — 60-word cell with a nested dash aside | Consider: split the cell at "(FR-24);"? | Not applied: table cells are compact by design and the caller asked not to reshape FR-33 rows beyond readability; readable as is |
| prose | "any current Facilitator" (OQ-25), "Facilitators in scope" (OQ-30) versus "Conversation Facilitator" elsewhere | Consider: expand to "Conversation Facilitator"? | Not applied: §13 rows, shorthand is unambiguous after first use, and the glossary defines the full term |

16 fixes applied; 3 items considered and left as is. No further minor fixes withheld.

## Inconsistencies noticed, not fixed (would change meaning)

1. FR-2 "A removal performed directly in Hexalith.Conversations … records `ExternallyRemoved` and sets the block itself" and §8 "detects an external removal and sets the Agents-owned block" versus the FR-2 membership sub-bullet, which records `ExternallyRemoved` as a state distinct from `Blocked` (rubric mechanical note, still open).
2. `MembershipRejected` is named once (FR-2 membership-failure bullet) and appears in no FR-18 row, FR-25 count, or SM-C4 class.
3. FR-18 says a system-abandoned proposal "is excluded from the SM-3 denominator" unconditionally; §12 SM-3 keeps a system abandonment from `Approved` or `PostingFailed` in the denominator as decided.
4. The FR-18 "retry budget exhausted" exit list names four exits; the transition table also allows `PostingFailed → Abandoned (RemovedInConversations)` and `PostingFailed → Posted (LateConfirmed)`.
5. Glossary order: "Proposed Agent Reply" is filed after "Provider" (unchanged 2026-09-09 material; out of scope).
