# Polish — Prose Lens (bmad-review) — `prd.md`, third 2026-09-09 update

- **Target:** `_bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md`
- **Scope:** the whole PRD, run after the structure pass on the restructured text (see `polish-structure-prd-2026-09-09-3.md`). 1,081 lines, 30,239 words before this pass; 1,081 lines, 30,244 words after it.
- **Style guide:** Microsoft Writing Style Guide baseline; author choices preserved — spaced em dashes, US spelling, Title Case headings with capitalized "And"/"Of", code-styled typed identifiers, defined terms capitalized per §3, "post hoc" adverbial / "post-hoc" adjectival, "at-least-once" adjectival / "at least once" adverbial, "fail-closed" adjectival / "fails closed" verbal.
- **Voice noted and preserved:** declarative normative register ("The system …", "A … does not satisfy this requirement"), negative-test sentences that name what fails the requirement, parenthetical FR cross-references at clause end, labelled sub-bullets ("Setting:", "Enablement:").
- **Constraints honored:** no requirement meaning, typed identifier, number, threshold, or ID changed; every `[ASSUMPTION A-n]` tag left in place; FR-18 and FR-33 table cells and §13 rows untouched; YAML frontmatter untouched. Every fix is a sentence split, a punctuation fix, a term-casing fix against §3, or a one-word grammar fix.

## Findings

| Pass | Original Text | Revised Text | Changes |
| --- | --- | --- | --- |
| prose | §1: "invocation rules, response policy, and traceable evidence"; UJ-3: "posted into the source Conversation as `hexa`"; §3 Agent Response: "for a source Conversation"; UJ-4: "and query audit evidence" | "Response Policy"; "Source Conversation" (both); "Audit Evidence" | Applied. Four lowercase uses of §3 terms; every other use in the document is cased. No other lowercase variant of a glossary term was found |
| prose | FR-1: "… never by Party type (§8) [ASSUMPTION A-27]; that identity is immutable for the life of the tenant." | "… [ASSUMPTION A-27]. That identity is immutable for the life of the tenant." | Applied. 75-word sentence split at the semicolon; A-27 stays at the end of the identity clause |
| prose | FR-2: "are on the FR-23 deprecate-and-reject register, a link or replace naming any Party is a typed rejection, and the link command's provisioning leg is re-homed …" | "… register; a link or replace naming any Party is a typed rejection; and the link command's provisioning leg is re-homed …" | Applied. Three independent clauses after a colon were comma-joined |
| prose | FR-2 external-removal branch: "and the last mirror, block or re-admission, is confirmed: record `ExternallyRemoved`" | "and the last mirror (block or re-admission) is confirmed: record `ExternallyRemoved`" | Applied. The comma-set gloss read as a fourth list item in a sentence that already carries two dash asides; parentheses match the FR-2 "(the participant read is unavailable)" style |
| prose | FR-2 and FR-9: "creates neither Provider work, a Proposed Agent Reply, nor a Conversation Message" | "creates no Provider work, Proposed Agent Reply, or Conversation Message" | Applied in both places. "neither … nor" takes two alternatives; the three-item form is nonstandard |
| prose | FR-2 Mirroring: "… while the block itself stays in force; the Tenant Agent Administrator's action on `MirrorRefused` is to re-set the block …" and "A seam answer that `hexa` is already absent confirms the mirror, and a block set from `NeverJoined` is confirmed …" | "… stays in force. The Tenant Agent Administrator's action on `MirrorRefused` is …" and "… confirms the mirror. A block set from `NeverJoined` is confirmed …" | Applied. A 95-word sentence split at the point where the subject changes; the two unrelated confirmation rules split |
| prose | FR-2 Clearing: "… so that a later absence can be told from the mirror's late effect; the Conversation Agent State records `Joined` only at …" | "… late effect. The Conversation Agent State records `Joined` only at …" | Applied. 80-word sentence split |
| prose | FR-5: "… or within a declared tightening grace (FR-4), otherwise the typed reason `DataHandlingAcceptanceLapsed`, which FR-8 step 4 also returns … because the tenant controls the acceptance; and its `CapabilityVersion` is not regressed. Failing any element blocks activation with a typed reason." | "… or within a declared tightening grace (FR-4); and its `CapabilityVersion` is not regressed. Failing any element blocks activation with a typed reason; a failure of the data-handling element carries the typed reason `DataHandlingAcceptanceLapsed`, which FR-8 step 4 also returns … because the tenant controls the acceptance." | Applied. A 45-word aside sat inside a semicolon-separated eligibility list, so "and its `CapabilityVersion`" read as part of the aside; the aside is now its own clause after the outcome sentence. Same reason, same counting rule, same cross-references |
| prose | FR-7 re-check: "must report fresh and available, fresh meaning the projection's recorded position is not older than …" | "must report fresh and available, where fresh means that the projection's recorded position is not older than …" | Applied. Dangling absolute phrase |
| prose | FR-8: "… is measured by the NFR-9 fast pre-Provider gate; only steps 9 and 10 are excluded from it — … — and a step-10 rejection is reported …" | "… gate. Only steps 9 and 10 are excluded from it — … — and a step-10 rejection is reported …" | Applied. Split before the exclusion rule |
| prose | FR-11 first sub-bullet: "the generation success instant is the approval instant, and the pre-post re-validation, `PostingPending` …, and the `Abandoned` exits apply on the FR-18 terms." | "… is the approval instant. The pre-post re-validation, `PostingPending` …, and the `Abandoned` exits apply on the FR-18 terms." | Applied. The two equivalences and the seven-item list of FR-18 mechanisms were one sentence |
| prose | FR-18 stored deadline: "… or to `PostingFailed` if the lookup finds no posted message; an unavailable lookup leaves it `PostingPending` and the lookup is repeated …" | "… no posted message. An unavailable lookup leaves it `PostingPending`, and the lookup is repeated …" | Applied. 100-word sentence split at the unavailable case |
| prose | FR-18 `PostingWindowElapsed`: "is system-abandoned with reason `PostingWindowElapsed` once, for `PostingFailed`, the `MessageId` lookup finds no posted message; it counts as decided for SM-3 (§12)." | "is system-abandoned with reason `PostingWindowElapsed` — for `PostingFailed`, once the `MessageId` lookup finds no posted message — and counts as decided for SM-3 (§12)." | Applied. "once, for `PostingFailed`, the" was a three-comma parse; the `PostingFailed`-only condition is now a dash aside, matching the table row |
| prose | FR-28 item 9: "… on the same terms as an item 6 exclusion; its revisit date is no more than 30 calendar days after the deferral; a row whose retirement condition names Security is never deferrable; and a deferral whose revisit date passes …" | "… on the same terms as an item 6 exclusion. Its revisit date is no more than 30 calendar days after the deferral; a row … is never deferrable; and a deferral whose revisit date passes …" | Applied. 110-word sentence split between the deferral's recording rules and its bounds |
| prose | FR-28 kill switch: "… without a recorded review decision. Its trigger conditions are:" | "… The switch's trigger conditions are:" | Applied. "Its" sat nine sentences after its antecedent |
| prose | FR-28 trigger review: "… in UTC — of the condition; a review not convened in that time is recorded as `TriggerReviewOverdue` …" | "… of the condition. A review not convened in that time is recorded as `TriggerReviewOverdue` …" | Applied. 85-word sentence split at the overdue rule |
| prose | FR-28 trigger review: "Excluded are only rejections at a rate limit set by the Platform Operator or Release Operator and `DataHandlingAcceptanceLapsed` rejections (FR-5) — a rejection at a limit or cap the Tenant Agent Administrator lowered counts, is attributed …" | "… `DataHandlingAcceptanceLapsed` rejections (FR-5). A rejection at a limit or cap the Tenant Agent Administrator lowered counts, is attributed …" | Applied. The dash opened a 120-word aside that never closed |
| prose | FR-34 Identity: "… provisioned by the Release Operator from that record (FR-33); on every check the runtime verifies … on a mismatch; a missing value, an unsigned engine, or a mismatch leaves the blocker standing …" | "… (FR-33). On every check the runtime verifies … on a mismatch. A missing value, an unsigned engine, or a mismatch leaves the blocker standing …" | Applied. One 150-word sentence split into three at its semicolons |
| prose | §8 seam 1: "— by id, never by Party type until `EXT-PARTIES-1` lands (A-27) [ASSUMPTION A-21], and restricts adding …" | "— by id, never by Party type until `EXT-PARTIES-1` lands (A-27) [ASSUMPTION A-21] — and restricts adding …" | Applied. The dash aside closed with a comma; A-21 stays inside it |
| prose | §8.1: "… unless deferred under FR-28 item 9 (FR-28), the `UnretiredAssumption` blocker names the row, its table, and the evaluated index version, and the Spine's index is authoritative …" | "… (FR-28); the `UnretiredAssumption` blocker names the row, its table, and the evaluated index version; and the Spine's index is authoritative …" | Applied. Three independent clauses, one with an internal list, were comma-joined |
| prose | §3 head "Provider/model selection" is the one lowercase glossary head | Consider: "Provider/Model Selection"? | Not applied: every use in the body is lowercase "Provider/model selection" and the §4.2 heading is the only cased form; recasing the head alone would make the head disagree with its uses |
| prose | "any current Facilitator" (OQ-16, OQ-25), "Facilitators in scope" (OQ-30), "a Facilitator" (FR-2, FR-27) versus "Conversation Facilitator" | Consider: expand? | Not applied, as on 2026-09-09-2: shorthand after first use, glossary defines the full term, and the §13 rows are not to be reshaped |
| prose | FR-33 row "Inspect unposted proposal versions …" — 60-word cell with a nested dash aside | Consider: split the cell at "(FR-24);"? | Not applied: table cell, prior decision stands |
| prose | SM-C3 "if Provider governance, secret safety, and per-Agent selection are not robust" | Consider: replace "robust"? | Not applied: the adjective is the metric's criterion, not filler; no other residual adjective or adverb (simply, just, clearly, very, merely, in order to) was found |

21 fixes applied (25 replacements; the neither/nor fix and the casing fix cover several places); 4 items considered and left as is. No further minor fixes withheld.

## Inconsistencies noticed, not fixed (would change meaning)

1. `MembershipRejected` at acceptance is named in FR-2, FR-25, and SM-C4, but appears in no FR-8 step-10 wording — step 10 says only "the roster-dependent join (FR-2)"; a reader has to open FR-2 to learn the two typed reasons that step can return.
2. FR-18 table row `Approved`, `PostingFailed` → `Abandoned` (`RemovedInConversations`) and the FR-2 membership-failure bullet agree; the FR-12 preamble lists "the Agents-owned block or `ExternallyRemoved` record (FR-2)" while FR-11's first bullet says "`Blocked` or `ExternallyRemoved`" — same rule, two spellings of the state pair (one is the record, one the state name).
3. §3 "Compliance Inspector — One of the six FR-33 roles" and §3 "Platform Operator — The one platform-scoped FR-33 role" count six roles; FR-33's preamble names six and then adds the Conversation Facilitator as a Conversations role and the Release PM as a party — consistent, but SM-4's exemption list refers to "the two Release PM `RQ-1` rows" as rows of a non-role, which a reader could take as a seventh role.
