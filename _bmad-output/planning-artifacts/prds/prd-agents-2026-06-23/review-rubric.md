# PRD Quality Review — Hexalith Agents

- **PRD:** `/home/administrator/projects/hexalith/agents/_bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md` (frontmatter `status: final`, `updated: 2026-09-09`; 1000 lines, file time 10:08)
- **Addendum:** `addendum.md` (non-normative; both "Options Considered" sections agree with §13 and record the rejected alternatives behind OQ-24..OQ-30; nothing in it contradicts the PRD)
- **Rubric:** `/home/administrator/projects/hexalith/agents/.claude/skills/bmad-prd/assets/prd-validation-checklist.md`
- **Reviewed:** 2026-09-09, after the doc-standards polish (structure + prose) and the four by-hand seam fixes that followed the second same-day update (`ExternallyRemoved` wording in FR-2/§8, SM-3 exclusion qualified in FR-18, `PostingFailed` exit list completed, `MembershipRejected` surfaced in FR-25)
- **Baseline:** `review-rubric-2026-09-09-update-gate-2.md` was read to classify its findings as closed, still open, or replaced; every judgment below is on the current text. Cross-document facts were re-verified against `external-dependency-register.md` (file time 12:08), `launch-readiness-register.md` (12:49), `ARCHITECTURE-SPINE.md` (12:49, and still changing while this review ran — Spine facts are cited by content, not line), `epics.md` (12:44, uncommitted edits), `sprint-change-proposal-2026-09-09-2.md` (approved), and `src/Hexalith.Agents.Contracts`. All four planning documents were edited after the PRD's last write, so the baseline's cross-document conclusions were not trusted.
- **Calibration:** launch-level stakes; chain-top (feeds UX, Architecture Spine, epics/stories); brownfield.

## Overall verdict

**Good on its own page; overtaken on the pages it points at.** The polish and by-hand fixes closed nine of the baseline's thirteen mediums without opening a new prose-versus-table seam of the same kind: the FR-18 table now has every exit the prose promises, the `MessageId` lookup has an authoritative-absent answer, `ExternallyRemoved` is one state with a named clearing authority, FR-34 clears on an attestation rather than a phrase, the compatibility command is exempt from the `Available` rule, and tenant acceptance is a tenant act. What is at risk is everything the PRD asserts about other documents: since 10:08 the approved correct-course proposal returned `EXT-HOST-1` to `Uncommitted` (so the §0 status line names the wrong exception), the Spine absorbed all three "open Spine defects" §8.1 lists and grew to twelve `ARCH-A` rows, and the register already carries the two seams §8 calls "pending" — none of it landed in the PRD, and the proposal does not list `prd.md` under `amends_if_approved`, so nothing is scheduled to land it. Inside the FR text one new decidability seam appeared (`ExpiredWhileSuspended` has two definitions that move the SM-3/SM-C5 denominators), and the four Product-deferred mediums (FR-26, FR-31, Agent Call contract, p99 at n = 30) are open for the third run in a row.

## Decision-readiness — adequate

Decisions are still decisions. §13 holds thirty rows; OQ-24..OQ-30 each say what was chosen and the addendum says what was rejected and why (freezing `ExpiresAt`, a `Suspended` lifecycle state, a host-asserted availability signal, Platform-recorded tenant acceptance, letting `Approved` proposals post through a suspension). Trade-offs still name the cost: OQ-10 and FR-7's last consequence (line 259) accept `hexa` being unavailable rather than silent; OQ-27 says "safety wins over 'complete on their own terms'". The `RQ-1` gate is one numbered list (FR-28 lines 679–689), `InsufficientEvidence` resolves to NOT READY (line 703), and item 9 now blocks on every unretired row "whatever its owner" (line 688) — the baseline's Security-exemption medium is closed.

What drops the dimension from *strong* is that the two sentences a decision-maker reads first are no longer true. §0 line 16 says "all critical external dependencies except `EXT-HOST-1` are `Uncommitted` — eight of the nine", and §8 line 814 says "only `EXT-HOST-1` is `Committed`". The register (`updated: 2026-09-09`, `authority: sprint-change-proposal-2026-09-09-2.md`) records `AcceptedStatus` `Uncommitted` on all nine entries and says `EXT-HOST-1`'s "earlier target/date/command are historical only because the required artifact now includes the production `EXT-PROTECTION-1` binding and FR-34 attestation port"; the approved proposal states "`EXT-HOST-1` becomes `Uncommitted` immediately when its expanded artifact is recorded." The PRD's own §8 change rule predicted this — FR-34's attestation port is a required-artifact change — and the PRD still reports the pre-change status. A second, smaller authority gap: FR-28 gives a "Release PM" two `RQ-1` powers, but no role or glossary entry says who that is.

### Findings

- **[high]** The §0 status line and §8 report `EXT-HOST-1` as `Committed`; the register, under an approved proposal that does not amend the PRD, holds all nine entries `Uncommitted` (§0 line 16 "all critical external dependencies except `EXT-HOST-1` are `Uncommitted` — eight of the nine register entries"; §8 line 814 "only `EXT-HOST-1` is `Committed`"; `external-dependency-register.md` lines 94 and 212 "All nine dependency records are `Uncommitted`"; `sprint-change-proposal-2026-09-09-2.md` line 450, and its `amends_if_approved` list, which names the two registers, the Spine, and `epics.md` but not `prd.md`) — the exception §0 singles out is the one entry the PRD's own FR-34 change re-opened, and the §0 landing rule (line 18) gives a reader no signal that an amendment is pending because none is. This is the second time today the §0 count has drifted from the register; a count that must be re-typed at every register change is the wrong kind of sentence for a status line. *Fix:* Replace both counts with the register-keyed form — "at this revision every §8 register entry is `Uncommitted` (the `EXT-HOST-1` commitment was returned to `Uncommitted` when FR-34 added the attestation port to its required artifact); the register is authoritative for current status" — and add the one sentence the baseline asked for: that FR-34's attestation port changed the `EXT-HOST-1` artifact and, under the §8 change rule, its status.
- **[medium]** "Release PM" holds two `RQ-1` decision powers but is not one of the six FR-33 roles and has no glossary entry (§ FR-28 item 6, line 685 "a dependency may be excluded only by a recorded Release PM decision naming the FR consequences the run will not exercise"; item 9, line 688 "blocks only once the Release PM records an acknowledgement of it"; §8.1 line 838; FR-33 line 453 "Every authorization rule in this PRD resolves to one of six Agents roles"; "Release PM" ×14 in the PRD, defined nowhere in the PRD, the two registers, or the Spine) — the Release Operator glossary entry (line 114) covers the `RQ-1` decision but not profile exclusions or late-row acknowledgements, so a story writer cannot say which principal the FR-30 surface accepts these two records from, and the FR-33 matrix has no row for either act. *Fix:* Add a glossary entry — "Release PM: the planning-time name of the Release Operator role; the two names denote the same role" — and two FR-33 rows (exclude a dependency from the qualification profile; acknowledge a late `ARCH-A` row), or rename the fourteen uses to Release Operator where they are authorization rules and leave "Release PM" for §13 ownership only.

## Substance over theater — strong

Re-verified on the current text. Four personas, each still driving decisions: Nora's UJ-1 carries the FR-1 provisioning split (line 50); Anika's UJ-3 carries the four-moment Eligible Approver rule inline (line 66); Omar's UJ-4 edge case is the FR-33 typed-denial rule. The Compliance Inspector has no UJ but earns its glossary entry through FR-24's computed subject set and OQ-30. The Vision (§1) is not swappable: "attributable to a durable Party identity rather than anonymous system output" and the explicit list of what `hexa` does not do (line 26) are this product's bet. NFRs carry numbers or product-specific invariants (NFR-8 names the fail-closed point; NFR-9 has four latency pairs; NFR-11 "RPO 0" and the 15-minute exercise; NFR-14 three UI thresholds). FR-34 remains the opposite of NFR theater: the no-op default, a five-step attestation, and a wrapper-that-lies test (line 730). SM-6 is still admitted as a requirement check in place.

No findings.

## Strategic coherence — strong

The thesis (§1 line 24, "AI assistance must become part of the conversation model without weakening the model's existing identity and governance guarantees") is served by every feature group, and the 2026-09-09 material tightens the bet rather than widening scope: FR-34 exists so no content-bearing path runs before protection is real; FR-4's data-handling record and §6.2's exclusion of tenant credentials keep Provider governance platform-owned; FR-31 makes the Conversation untrusted input. SM-7 is the falsifier ("the metric that could falsify the §1 thesis", line 948); SM-2 is secondary; every primary is counter-balanced (SM-C1..SM-C5); OQ-23 is honest that Automatic-mode tenants are covered by SM-C4 and the posting-failure rate only. MVP kind is problem-solving with two deliberate platform seams (Approved Bounded Context Behavior, the `ProposedAgentReplyState` contract), and §6.1/§6.2 match.

No findings.

## Done-ness clarity — adequate

The FR set is testable where it was already testable and has not regressed: FR-18's table (lines 417–433) enumerates fifteen transitions with actor and guard and the prose exits now all appear in it; FR-2 (lines 151–170) decides the membership step per state and per read outcome; FR-7 (lines 252–257) separates authoritative from unavailable answers with two-pass confirmation; FR-28 (line 700) gives the trigger review a tuple denominator, a scaled minimum sample, and a defined posting-failure numerator; FR-4 (line 199) names the four data-handling fields and a 30-day grace. Every typed name recurs consistently (`Suspended`, `ReadmitPending`, `RemovedInConversations`, `LateConfirmed`, `ResolutionUnavailable`, `ResolutionEmptyPending`, `PayloadProtectionUnavailable`, `DependencyNotAvailable`) and each is either shipped (`Draft`, `ProposedAgentReplyState`, `MembershipUnavailable`, `MembershipRejected`, `ContextUnavailable` — all present in `src/`) or declared additive under FR-23 (none of the others is in `src/`, which is correct for a declaration).

What holds the dimension at *adequate*: one new two-definition seam on a term that moves the primary launch-health denominators, and the four decidability mediums Product deferred at both earlier runs, which are open again here.

### Findings

- **[medium]** `ExpiredWhileSuspended` has two definitions, and they move the SM-3 and SM-C5 denominators differently (§ FR-18 table row 424 "recorded with reason `ExpiredWhileSuspended` when the awaiting window overlapped a `Suspended` period"; FR-28 line 698 "a proposal that expires during the suspension is recorded `ExpiredWhileSuspended`"; SM-3 line 947 and SM-C5 line 961 exclude such proposals; OQ-27 "so the switch cannot manufacture its own launch-health miss") — under the row-424 reading a one-minute pull removes from the SM-3 and SM-C5 denominators every proposal that was awaiting at that minute and later expired on its own, however long after release; under the FR-28 reading only proposals whose `ExpiresAt` passed while the switch was pulled leave. OQ-27's stated purpose is to stop the switch manufacturing a miss; the overlap reading lets it manufacture a pass. The Spine (AD-12, "expiry during suspension records `ExpiredWhileSuspended`") follows FR-28. *Fix:* Reword row 424 to "Stored `ExpiresAt` passed; recorded with reason `ExpiredWhileSuspended` when it passed while the tenant was `Suspended`", matching FR-28.
- **[medium]** "More restrictive of two policies" is still not computable — *deferred item, still open* (§ FR-26 line 644 "each retry, approval-time check, and pre-post check uses the more restrictive of the initial attempt's policy and the then-current active policy"; line 641 "unless a stricter mode-specific policy is configured"; FR-33 row 468 "a tenant may only add restrictions through a stricter mode-specific policy"; and line 640, which says the same re-checks "always evaluate the then-current active policy", the consistency reviewer's L-I3) — two policy versions with different category sets are not totally ordered, so an implementer must invent "must pass both", and line 640 and line 644 give two different rules for the same re-check. *Fix:* "the content must pass every applicable policy version — the initial attempt's and the then-current active policy, and a mode-specific policy in addition to, never instead of, the platform policy"; delete "more restrictive" and reconcile line 640 to it.
- **[medium]** FR-31 blocks the reporting UJ-2 exists for, and drifts from OQ-9 — *deferred item, still open* (§ FR-31 line 653 "asserts a decision, commitment, or approval on a Party's behalf"; OQ-9 line 979 "asserts a decision on a Party's behalf"; UJ-2 line 56 "needs help interpreting the prior discussion") — a summary that says "Anika agreed to ship on Friday" reports a commitment on a Party's behalf and is blocked under FR-31 as written; the category is also wider in the FR than in the binding decision. *Fix:* Narrow both to first-person impersonation and content that presents a decision, commitment, or approval as the Party's own act when the Conversation does not record it; permit reporting a Party's recorded position, and align OQ-9.
- **[medium]** The Agent Call still has no public state contract — *deferred item, still open* (§ FR-8 lines 267–286; FR-25 line 600 "recent Agent Call outcomes"; §10 line 899 "Agent Call status"; `src/Hexalith.Agents.Contracts/AgentInteraction/AgentInteractionStatus.cs` with 29 values including 16 `Proposal*` duplicates of the FR-18 states; `src/Hexalith.Agents.UI/Components/Shared/AgentCallStatus.cs` with 14) — FR-18 adopts `ProposedAgentReplyState` by name and the shipped code has two different call-status enumerations; nothing in the PRD says which one is public, which members are terminal, or whether the `Proposal*` duplicates are a second source of truth for proposal state. *Fix:* An FR-8 consequence naming the public contract, its terminal members, and placing the `Proposal*` duplicates on the FR-23 deprecate-and-reject register.
- **[medium]** p99 over 30 executions is the sample maximum — *deferred item, still open* (§ NFR-9 line 791 "p99 ≤ 120 seconds ... Each gate requires at least 30 production-like executions"; FR-28 item 3, line 682, and line 693; OQ-5; §11 line 928 requires the measurement contract to name "the numerator and denominator or percentile method") — at n = 30 the 99th percentile is the single slowest run, so the p99 gate is a max gate with a percentile label and one outlier fails `RQ-1`. *Fix:* Gate on p95 at n ≥ 30 and report p99 as launch-health, or require n ≥ 300 for the p99 gates, and name the percentile method in NFR-9.
- **[low]** The posting-failure rate counts every automatic post twice as written (§ FR-28 line 700 "automatic posts count as one approved and one failed on the same terms") — the intent is one denominator entry per automatic post and one numerator entry only if it entered `PostingFailed`; the sentence says both. *Fix:* "an automatic post counts as one entry into `Approved` and, if it entered `PostingFailed` at least once, one into the numerator".
- **[low]** The `Approved → Abandoned` row requires a `MessageId` lookup for a proposal that never attempted a post, and the unavailable-lookup outcome is defined for `PostingFailed` only (§ FR-18 row 429 guard "the `MessageId` lookup finds no posted message" from `Approved`; line 440 "If the lookup is unavailable, the exit is refused with a typed reason and the proposal stays `PostingFailed`"; line 440 also skips the lookup only for a `PostingFailed` proposal that never posted) — the Spine (AD-5) already says the proposal "stays `Approved`" on an unavailable lookup; the PRD does not. *Fix:* Add to line 440: "For the `Approved` origin the lookup is skipped, because no post was attempted; a removal detected against an `Approved` proposal abandons it directly."
- **[low]** SM-C4's "exactly" list has no class for FR-8 step 2 and step 3 rejections (§ SM-C4 line 957 "Blocked calls, whose reasons are exactly: ..."; line 960 names the separately reported classes; FR-8 steps 2–3; FR-4 line 193 "cannot be activated or called until reconfigured"; line 199 "after which the model cannot be called for that tenant") — a call rejected because the Agent is `Disabled`, the Provider/model is disabled, or the data-handling grace has lapsed is not blocked, failed, unavailable, or in the separately reported list, so the trigger review cannot place it; the grace lapse in particular is a structural unavailability the counter-metric exists to see. *Fix:* Add "Agent lifecycle and Provider/model eligibility (FR-8 steps 2–3)" to the separately reported list, and the data-handling grace lapse to the blocked-call reasons.
- **[low]** Residual adjective-only phrasing — *deferred item, still open* (§ FR-4 line 193 "A documented migration state may allow temporary read-only inspection"; FR-20 line 510 "at a level sufficient to explain"; FR-25 line 606 "support launch monitoring of adoption and approval workflow metrics") — unchanged since the first 2026-09-09 run. *Fix:* Name the migration state or delete the sentence; FR-20: "records the role basis, the FR-33 row, and the typed denial reason"; FR-25: "exposes the SM-2, SM-3, SM-7, SM-C4, and SM-C5 inputs defined in §12".
- **[low]** The tenant-role calling restriction has no configuration home (§ FR-33 row 462 "unless the Tenant Agent Administrator restricts calling to a tenant role ... the Agent call permission that FR-8 and FR-20 enforce" `[ASSUMPTION A-11]`; FR-33 row 461 "Configure `hexa`" lists ten settings without it; §10 line 896 lists the same set; glossary Agent Administrator line 83 likewise) — the permission is enforced at FR-8 step 1 but no row grants the act of setting it and no surface exposes it. *Fix:* Add "calling restriction (A-11)" to the Configure `hexa` row and to §10 Agent administration.

## Scope honesty — adequate

Omissions are explicit and placed where an implementer reads them: §6.2 excludes tenant-supplied credentials with the reason; FR-18 line 435 says the kill switch "is never a system-abandonment reason"; FR-34 line 731 says what is *not* protected content so `hexa` can be configured early; OQ-23 names the uncovered cohort; §0 separates requirements authority from build readiness. The 2026-09-09 update was honest about its Spine divergences (§8.1 lines 840–844) — the problem now is that those sentences have outlived the divergences (see Downstream usability).

Open-items density: 21 `[ASSUMPTION]` rows (A-21 added since the baseline), 3 *Deferred* register rows (OQ-15, OQ-18 due 2026-10-15, OQ-23), 0 `[NOTE FOR PM]`, and — as of the register — nine of nine critical dependencies `Uncommitted` with every compatibility command `TBD`. Fourteen of the 21 rows are wholly or partly Product/Architecture/Governance-owned and block `RQ-1`. For a `status: final` PRD this is honest only because §0 says no story is `ready-for-dev`; the density has grown at every revision this month.

### Findings

- **[low]** Thirteen assumptions retire on a condition with no date — *deferred item, still open, one row larger* (§ §8.1 A-1..A-4, A-9..A-12, A-15, A-16, A-18, A-21 retire on a register commitment whose `TargetIntegrationDate` is `TBD` on every entry, or on "Product confirms the FR-33 rows"; A-5..A-8, A-13, A-17, A-19, A-20 say "before enablement", which is a date only once enablement is scheduled) — A-9..A-12 are one Product decision and could carry one due date the way A-14 does. *Fix:* One due date for the FR-33 confirmations; for the seam rows, cite the register's `TargetIntegrationDate` field rather than restating a milestone.
- **[low]** §8 says two seams are "pending register extension"; the register already carries both (§ §8 seam 2, line 818 "The existence read is not yet in the register's seam-2 text; adding it is a pending register extension (correct-course, 2026-09-09)"; seam 4, line 820 "The event-feed alternative is a pending register extension"; `external-dependency-register.md` line 57 lists "an existence read by `MessageId` returning the message, typed absence, typed `ConversationDeleted`, typed `PrincipalRemovedFromConversation`, or unavailable" and "a Conversations-side tenant event feed", with line 65 recording the 2026-09-09 scope extension) — stale in the safe direction, but a reader who checks will find the PRD behind the register on the seam whose absence the PRD says blocks `Available`. *Fix:* Delete both "pending" sentences; keep "`EXT-CONV-AI-1` cannot reach `Available` on a compatibility command that does not exercise the existence read".

## Downstream usability — adequate

Chain-top obligations on the PRD's own page are met: every ID is unique and every reference resolves (FR-1..FR-34, NFR-1..NFR-14, OQ-1..OQ-30, SM-1..SM-7, SM-C1..SM-C5, UJ-1..UJ-4, A-1..A-21, nine `EXT-*-1` entries — all verified); the glossary carries the code names (`AgentInteraction`, `ProviderCatalog`), the five-state Conversation Agent State, and an `RQ-1` entry that cites FR-28 rather than restating it; `epics.md` (edited 12:44) references every one of FR-1..FR-34, so the downstream inventory is current. Brownfield references that name code are accurate: `ProposedAgentReplyState` (11 values incl. `Unknown`), `AgentLifecycleStatus` (`Unknown | Draft | Active | Disabled`), `AgentLaunchReadinessBlocker` (8 shipped values; the three PRD additions absent, as declared), `AgentSetupWriteStatus.Submitted = 0`, `ApproverPolicySourceKind.Caller`/`ConversationOwner`, `CostControlPosture.ReportingOnlyMonitoring`/`AcceptedLaunchRisk`, `ContentSafetyFailureHandling.BlockWithAuditableOverride`, `AgentInteractionContextMode` (`Full`, `Bounded`; `Blocked` additive as declared). The launch readiness register now defines `UnretiredAssumption`, `DependencyNotAvailable`, `OpenDecision`, and `InsufficientEvidence` — the baseline's vocabulary low is closed.

What drops the dimension from *strong* is the §8.1 reconciliation paragraph, which stories will cite when the PRD and Spine disagree. It names three open Spine defects; the Spine (`updated: 2026-09-09`, file time 12:49) has absorbed all three, and it now diverges from the PRD in a place the paragraph does not list. A story writer following §8.1 today would "correct" a Spine that already agrees and miss the one that does not.

### Findings

- **[medium]** The §8.1 Spine-defect list is stale in both directions (§ §8.1 lines 840–844 "three are open at this revision, owned by Architecture: AD-5 forbids the system abandonment from `Approved` and `PostingFailed` ... AD-22 names the Tenant Agent Administrator as the sole inspection second party and gives export and hold release none ... AD-12 states the Release Operator trigger without the review, minimum sample, and `InsufficientEvidence` rule"; OQ-26 "a deliberate divergence from Spine AD-5"; OQ-27 "a Spine AD-12 correction owned by Architecture"; `ARCHITECTURE-SPINE.md` AD-5 now reads "System abandonment ... now also fires from `Approved` and `PostingFailed` on a detected removal or block"; AD-12 now carries "trigger review is mandatory ... below that sample the trigger is `InsufficientEvidence` ... `TriggerReviewOverdue` ... An `Approved` proposal stays `Approved` without starting a post"; AD-22 now carries the computed subject set, "`ExportRequest` requires this same second-party approval before any content read, never post hoc; `LegalHoldRelease` requires the audited approval of a second Compliance Inspector or the Platform Operator") — meanwhile the Spine's AD-13 rule states "Eligible Approver resolution runs in every response mode, not Confirmation mode only — an Automatic-mode Agent with zero configured Approvers is rejected up front with `NoEligibleApprover`", which contradicts FR-8 step 8 ("where Confirmation Response Mode applies", line 284) and FR-7, and is listed nowhere. Under the PRD's own rule the PRD governs, but the paragraph that tells a story writer where the two disagree is wrong on all four counts. *Fix:* Replace lines 840–844 with the current state: AD-5, AD-12, AD-22 reconciled on 2026-09-09; one open divergence — AD-13's Automatic-mode Eligible Approver requirement versus FR-8 step 8 — either adopted into FR-8 as a Product decision (it is a real safety choice: an Automatic-mode tenant with no Approver has no human who could ever be resolved) or recorded as a Spine defect. Drop the "for Architecture to correct" clauses from OQ-26 and OQ-27.
- **[low]** §8.1 states a Spine row count that the Spine at the same `updated` date does not have — *reopened* (§ §8.1 line 836 "at that date the table holds `ARCH-A-1` through `ARCH-A-10`, with `ARCH-A-5` retired"; the Spine table now holds `ARCH-A-1` through `ARCH-A-12`, `ARCH-A-5` retired) — the range is declared open so nothing breaks, but this is the second same-day drift of a count the PRD does not need to carry. *Fix:* "the table current at that date" — no number.
- **[low]** Process gates remain inside FR-21 — *deferred item, still open* (§ FR-21 lines 521–523 story-governance consequences; line 525 "No runtime, test, or qualification path may execute ..."; line 526) — consequences 521–523 and 525 are rules for the register owner and testers, not system behavior a story can implement; the circularity the baseline flagged is closed by the last sentence of line 525, which is the only part of it that belongs in an FR. *Fix:* Move 521–523 and the execution rule to §8; keep the `DependencyNotAvailable` typed outcome and the `PayloadProtectionUnavailable` path in FR-21.

## Shape fit — adequate

The shape fits a multi-stakeholder B2B governance capability with real UX: UJs are load-bearing, SMs are user-facing where the thesis needs them (SM-7) and operational where the gate needs them (SM-4, SM-5, SM-6), brownfield adoptions are explicit (FR-18, FR-3's `Draft`, FR-9's additive `Blocked`, FR-23's deprecate-and-reject register). The strain is unchanged: release-process and ownership content interleaved with product behavior — FR-21 consequences 521–523 and 525, FR-28's register mechanics (line 697), the FR-34 ownership sentence (now in the description at line 723, out of the consequences, which is the right place), NFR-11/NFR-12 drills. §0 declares "normative evidence authority" as a deliberate choice, so this is a recorded shape cost rather than a defect; a story writer must still sort behavior from procedure, and the FR-21 low above is the one concrete instance worth moving.

No findings beyond the FR-21 low recorded under Downstream usability.

## Mechanical notes

- **ID continuity.** FR-1..FR-34, NFR-1..NFR-14, OQ-1..OQ-30, SM-1..SM-7, SM-C1..SM-C5, UJ-1..UJ-4, A-1..A-21: all defined, all unique, every reference resolves. FR headings remain out of numeric order by design (FR-33 after FR-18; FR-29 after FR-23; FR-30 after FR-25; FR-31 after FR-26; FR-32 and FR-34 after FR-28); downstream tooling must key on IDs. Spine rows cited by number: `ARCH-A-1`, `ARCH-A-5`, `ARCH-A-6`, `ARCH-A-10` (all exist); AD-2, AD-5, AD-7, AD-12, AD-22, AD-30 (all exist).
- **Assumptions Index roundtrip.** Passes. 35 `[ASSUMPTION` occurrences: 31 keyed inline tags mapping onto A-1..A-21 (A-3 ×3, A-9 ×3, A-10 ×3, A-13 ×3, A-4 ×2, A-17 ×2), three explanatory `A-n` mentions (§0 line 14, FR-33 line 453, §8.1 line 834), one generic `[ASSUMPTION]` in OQ-21 (line 991). Every index row's "Where" column names a location that carries the tag (A-21 → §8 line 817); no inline tag lacks a row.
- **§0 versus §8 versus register.** Inconsistent — see the Decision-readiness high. Internally §0 and §8 agree with each other (eight of nine `Uncommitted`, `EXT-HOST-1` `Committed`, none `Available`); both disagree with the register.
- **§8.1 versus Spine.** Row count stale (ten stated, twelve present); three-defect list stale (all three absorbed); one live divergence unlisted (AD-13 Automatic-mode Eligible Approver). The Spine changed between two reads during this review (12:44 and 12:49 file times; AD-5 moved from line 135 to 141) — a PRD sentence that pins a Spine line count or row count will be wrong within the hour on a day like this.
- **Frontmatter.** `status: final`, `updated: 2026-09-09`; matches the body and the §13 preamble.
- **Callout tags.** Zero `[NOTE FOR PM]` and zero `[NON-GOAL for MVP]` inline; the *Deferred* rows, §5, §6.2, FR-27's OQ-18 sentence, and §12's OQ-23 sentence do the equivalent work.
- **UJ protagonists.** Nora, Milan, Anika, Omar, each with inline context; no floating UJs.
- **Glossary drift (low, unchanged).** "Agent response(s)" lowercase ×4 (§2.2 line 41, UJ-2 line 60, FR-19 line 498, §6.2 line 776) versus "Agent Response" ×16; "Agent call" ×3 and "Agent calling" ×1 versus "Agent Call" ×24; glossary heading "Provider/Model Selection" versus "Provider/model selection" ×7; "eligible Conversations" lowercase ×3 in SM-2/OQ-11 versus the glossary term ×3. New: "Release PM" ×14 with no entry (see the Decision-readiness medium).
- **Typed-name consistency.** `MembershipUnavailable` is now classified (FR-25 line 610, SM-C4 line 959 — baseline low closed), but FR-2 line 160 still says it and `MembershipRejected` "remain posting-failure reasons for that re-validation only" three lines after line 157 uses `MembershipUnavailable` as an acceptance rejection; reword to "at re-validation these two are posting-failure reasons; at acceptance `MembershipUnavailable` is a call rejection". `DeletionDeferredByHold` still appears only in §9 (line 883); FR-30 and §10's legal-hold bullet should expose it. SM-C4 line 960 says "with rate-limit rejections excluded" where FR-28 line 700 excludes only limits set by the Platform Operator or Release Operator — align SM-C4 to FR-28.
- **FR-2 `Joined` detection when no mirror ever existed.** Line 154's condition "the last mirror is confirmed, and no clear is newer than that mirror" has no truth value for a Conversation that was never blocked; state that the condition holds vacuously when no mirror exists, or every never-blocked Conversation's external removal is undetectable.
- **A-9 row versus FR-33.** FR-33 row 465 tags the `ExternallyRemoved` clear ("Tenant Agent Administrator or any current Conversation Facilitator") with A-9, but the A-9 index row (line 858) and OQ-16 describe only the block clear; add the `ExternallyRemoved` clause to A-9 so the index matches the tagged text (OQ-25 already has it).
- **Brownfield references verified in `src/`.** Every enum the PRD names by member exists as stated (see Downstream usability). `MembershipUnavailable`, `MembershipRejected`, and `ContextUnavailable` are shipped names reused by the PRD; every other new typed name is absent from `src/`, consistent with declaration.
- **Required sections.** Present for a launch-level, chain-top, brownfield PRD: purpose with status, vision, users and journeys, glossary, features with FRs and testable consequences, non-goals, MVP scope, NFRs, dependencies with Assumptions Index, governance, API surface, evidence, success metrics with counter-metrics, decision register.

## Baseline disposition

Disposition of every finding in `review-rubric-2026-09-09-update-gate-2.md`, judged on the current text:

- Security-owned `ARCH-A` exemption in `RQ-1` item 9 (medium) — **closed** (FR-28 line 688 "whatever its owner"; §8.1 line 836).
- Register vocabulary asserted but absent (low) — **closed** (launch readiness register now carries `UnretiredAssumption` ×4, `DependencyNotAvailable` ×5, `OpenDecision` ×4, `InsufficientEvidence` ×28).
- `Approved` cannot "complete on their own terms" while `Suspended` (medium) — **closed** (FR-28 line 698, FR-3 line 176, table row 425; OQ-27 records the choice).
- No system row for `PostingFailed → Abandoned(SourceConversationUnavailable)` (medium) — **closed** (row 430; FR-7 line 252 covers `PostingFailed`).
- `MessageId` lookup lacks an authoritative-absent outcome (medium) — **closed** (line 440 classifies all four answers).
- `ExternallyRemoved`/`Blocked` overlap and clearing authority (medium) — **closed** (lines 154, 165, FR-33 row 465); residual: A-9 row and OQ-16 not updated (mechanical note).
- FR-34 host-supplied availability signal undefined (medium) — **closed** (attestation, line 727; register `EXT-HOST-1` artifact names the port).
- `Available` circular for the compatibility command (medium) — **closed** (line 525 last sentence).
- Platform-recorded tenant acceptance (medium) — **closed** (FR-33 row 459; FR-4 line 199).
- FR-26 "more restrictive" (medium, deferred) — **open**.
- FR-31 commitment category / OQ-9 drift (medium, deferred) — **open**.
- Agent Call public state contract (medium, deferred) — **open**.
- p99 at n = 30 (medium, deferred) — **open**.
- SM-C4 tuple share denominator and "on the same terms" (low) — **replaced**: the denominator is now defined (line 700); the automatic-post sentence remains ambiguous (new low).
- `MembershipUnavailable` classification (low) — **closed** (FR-25 line 610; SM-C4 third class); the "for that re-validation only" remnant in FR-2 line 160 is a mechanical note.
- Residual adjectives in FR-4/FR-20/FR-25 (low, deferred) — **open**.
- Twelve undated assumptions (low) — **open**, now thirteen.
- §8.1 stale Spine row count (low) — **replaced**: the count was updated to ten and is stale again at twelve (new low).
- Process gates in FR-21 (low, deferred) — **open**; the circularity part is closed.
- Spine AD-5 divergence unrecorded in the PRD (low) — **closed** (§8.1 line 842; OQ-26) — and now superseded: the Spine has adopted the rule, so the PRD's record is stale (new medium).
- FR-34 ownership RACI inside consequences (low) — **closed** (moved to the FR-34 description, line 723).
- Mechanical: Release Operator glossary "pulls or releases ... as the recorded decision" — **closed** (line 114). `MembershipRejected` without a home — **closed** (FR-25 line 609). `DeletionDeferredByHold` only in §9 — **open**. FR-18 row for human abandonment lacking the `Disabled`/`Suspended` clause — **closed** (row 423). "Exactly five states" versus "set the Agents-owned block" — **closed** (line 154). Eligible Conversation rationale in the glossary — **open**. Glossary drift — **open**, unchanged.

## Deferred items revisited

Each item the second 2026-09-09 update deferred to "owner Product, revisit at the next validate run", with its verdict on the current text:

1. **FR-26 "more restrictive of two policies" decidability** — *still open, medium.* Line 644 unchanged; line 640 gives a second, different rule for the same re-checks; FR-33 row 468 and FR-26 line 641 use "stricter" for mode-specific policies. Fix stated under Done-ness.
2. **FR-31 assertion category versus OQ-9** — *still open, medium.* FR-31 line 653 "decision, commitment, or approval"; OQ-9 line 979 "decision" only; UJ-2's reporting use case is blocked as written.
3. **Public Agent Call state contract** — *still open, medium.* No FR names it; two shipped enumerations (29 and 14 values) compete; `Proposal*` duplicates are not on the FR-23 register.
4. **p99 at n = 30** — *still open, medium.* NFR-9, FR-28 item 3, OQ-5 unchanged; §11 requires a percentile method the PRD never names.
5. **Undated assumptions** — *still open, low.* Thirteen rows retire on `TBD`-dated register events or "Product confirms"; A-14 remains the only dated row.
6. **Process gates inside FR-21** — *still open, low.* Lines 521–523 and 525 unchanged in kind; the circularity sub-issue is resolved by the compatibility-command sentence.
7. **Residual adjectives** — *still open, low.* FR-4 line 193, FR-20 line 510, FR-25 line 606 verbatim as at the baseline.
8. **Glossary drift** — *still open, low.* Counts unchanged (lowercase "Agent response" ×4, "Agent call" ×3, "Agent calling" ×1, "Provider/Model Selection" ×1, lowercase "eligible Conversations" ×3); one new term without an entry ("Release PM").
9. **Eligible Conversation rationale placement** — *still open, low.* The glossary entry (line 104) still carries the SM-2/SM-C4 measurement rationale ("Eligibility measures reach, so SM-2 remains an adoption metric rather than an acceptance rate" and the A-4 sentence); it belongs under SM-2 in §12 with the glossary reduced to the definition.
