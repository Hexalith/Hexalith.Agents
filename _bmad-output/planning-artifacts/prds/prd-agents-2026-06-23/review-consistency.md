# Consistency Review — PRD Hexalith Agents (third 2026-09-09 update)

- **PRD:** `_bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md` (`status: final`, `updated: 2026-09-09`; mtime 2026-09-09 16:08:07 +0200; 1016 lines) with `addendum.md` (mtime 16:09:01; 83 lines)
- **Reviewed:** 2026-09-09, after the third update
- **Artifacts checked as on disk (`stat -c '%y'`):**
  - `architecture/architecture-agents-2026-06-23-2/ARCHITECTURE-SPINE.md` — `updated: 2026-09-09`, `architecture_assumption_index_version: 3`, `ARCH-A-INDEX-3`, binds `PRD FR-1..FR-34`, `NFR-1..NFR-14`, `OQ-1..OQ-30`; mtime **15:42:23** (older than the PRD; it predates the third update)
  - `external-dependency-register.md` — `updated: 2026-09-09`; mtime **13:28:06**
  - `launch-readiness-register.md` — `updated: 2026-09-09`; mtime **12:49:11**
  - `epics.md` — no `updated:`; mtime **12:44:27**
  - `sprint-change-proposal-2026-09-09-2.md` — mtime 11:24:15 (consulted for prior CX-17 only)
- **Prior review:** `review-consistency-2026-09-09-validate-3.md` (CI-1..CI-18, CX-1..CX-19), read after this pass; dispositions in the last section.

Line numbers refer to each file as read at review time.

## Verdict

**Internally consistent with four medium defects; cross-artifact not consistent — the PRD is now pointer-only and its dependency-status claims are true on disk, but it undercounts its own divergence from the Spine, and two of its "register vocabulary" codes do not exist in the register.**

Pass 1: the glossary, FR-18 table, FR-8 step numbering (every "step n" citation resolves to the reordered list), §8.1 tags, §13 rows, SM definitions, and §6 scope all agree; typed-name spelling is uniform (`ExternallyRemoved`, `MirrorRefused`, `PostingWindowElapsed`, `DeferredAssumption`, `SafetyFailed`, `ContextUnavailable`, `SuspensionReviewOverdue` each spelled one way everywhere; the only lowercase variants are descriptive prose lists at 68, 419, 552, 772). All eighteen prior internal findings are resolved. Four new medium and six new low internal findings remain, the most consequential being that FR-3 claims to retire Spine `ARCH-A-11` by PRD text alone, which §8.1 and FR-28 forbid.

Pass 2: the PRD's claims about the registers (all nine entries `Uncommitted`; `EXT-HOST-1` returned to `Uncommitted` on 2026-09-09; the open Story 5.3 Product approval; seams 1–6; nine-field schema) are true on disk, and no row counts or line numbers remain. Three claims are false or unsupported on disk: (1) `SuspensionReviewOverdue` and `DeferredAssumption` are called "register vocabulary" but the launch readiness register's closed blocker vocabulary does not contain them; (2) §8.1 states "one divergence is open" and "one PRD-originated amendment is pending" in the Spine, whereas the Spine (written at 15:42, before the PRD's 16:08 third update) diverges on at least nine further rules; (3) §8.1 points at "the Spine's own reconciliation ledger", which does not exist. The Spine still carries the AD-13 every-mode Eligible Approver rule (prior CX-1) and the `DataHandlingVersion` gap (prior CX-6), and now also cites a stale "FR-8 step 4" for rate limits.

Counts — internal: 0 critical / 0 high / 4 medium / 6 low. Cross-artifact: 0 critical / 4 high / 8 medium / 6 low. Prior findings: 24 resolved / 13 still open / 0 regressed.

## Part A — Internal (CI-n)

### Critical

None.

### High

None.

### Medium

- **[medium] CI-1 — FR-3 says PRD text retires Spine `ARCH-A-11`; §8.1 and FR-28 say only a recorded confirmation and a readiness observation naming the row can, and the Spine row is still open** (FR-3 179 ↔ §8.1 852, FR-28 708; Spine 785). FR-3: "this is the Product confirmation that retires Spine `ARCH-A-11`." §8.1: "An `ARCH-A` row whose retirement condition names a party other than Architecture — Product … — is retired only by a recorded confirmation from that party, never by an Architecture edit alone"; FR-28: "An assumption or decision is retired for `RQ-1` purposes by a recorded readiness observation naming the row, made in the same change that edits the keyed text (§8.1); the runtime never parses this document." Spine row 785 is unretired ("Product confirms this rule … TBD"). A PRD sentence is neither the recorded confirmation nor the readiness observation, and the Spine has not recorded it. *Fix (PRD FR-3 179):* "this is the Product rule on which Spine `ARCH-A-11` is retired once Product's confirmation is recorded and the Spine index retires the row (§8.1)."
- **[medium] CI-2 — A lapsed `DataHandlingVersion` acceptance is both a blocked-call reason and a step-4 eligibility rejection that SM-C4 puts in no class** (SM-C4 972 ↔ SM-C4 975, FR-8 284, FR-5 212). SM-C4: "Blocked calls, whose reasons are exactly: … a lapsed `DataHandlingVersion` acceptance (FR-4), …". Same metric, three lines later: "Authorization denials, Agent lifecycle and Provider/model eligibility rejections (FR-8 steps 2 and 4), … are in none of the three classes and are reported separately." FR-5 makes "its current `DataHandlingVersion` accepted for this tenant" an element of the step-4 Provider/model eligibility set, so the same rejection is counted twice one way and excluded the other. *Fix (PRD SM-C4 975):* "Agent lifecycle and Provider/model eligibility rejections (FR-8 steps 2 and 4) — except the lapsed `DataHandlingVersion` acceptance, which is the one step-4 rejection counted as a blocked call — …".
- **[medium] CI-3 — FR-8 says only step 9 is outside the NFR-9 fast gate, but step 10 runs after the classifier call** (FR-8 292 ↔ FR-8 289–290, NFR-9 803, FR-27 681). FR-8: "Steps 1 through 8 require no classifier call, so every rejection they produce … is measured by the NFR-9 fast pre-Provider gate; only step 9 is excluded from it (FR-27)." NFR-9: the 2-second gate covers "authorization, policy, budget, and context rejections that require no content safety classifier call." A step-10 membership rejection (`RemovedInConversations`, `MembershipUnavailable`, `MembershipRejected`) is produced after step 9's scan, so its elapsed time includes the classifier call; "only step 9 is excluded" makes step 10 a gate input the NFR-9 definition cannot measure. *Fix (PRD FR-8 292):* "only steps 9 and 10 are excluded from it — step 9 because it is the classifier call, step 10 because it follows it (FR-27); a step-10 rejection is reported with the safety-scan latency series."
- **[medium] CI-4 — `SuspensionReviewOverdue` needs a "recorded review" that an SM-4 pull never has** (FR-28 709 ↔ FR-28 709–710, FR-33 485). FR-28: "A pull older than 7 days without a recorded review is recorded as `SuspensionReviewOverdue`." The same paragraph gives an SM-4 pull no review at all: "release of a pull recorded on an SM-4 event requires the Platform Operator's recorded containment finding, and release of a review pull requires a recorded review decision"; FR-33 485 repeats it. Read literally, every SM-4 pull becomes `SuspensionReviewOverdue` on day 8 whatever the Platform Operator does. *Fix (PRD FR-28 709):* "A pull older than 7 days without a recorded review decision — for an SM-4 pull, without a recorded containment finding or a recorded decision to keep it pulled — is recorded as `SuspensionReviewOverdue`."

### Low

- **[low] CI-5 — OQ-17's status cell was not updated for the third-update amendment the preamble records** (§13 980 ↔ OQ-17 1002). Preamble: "Row OQ-31 records the decision taken in the third 2026-09-09 update, which … amended OQ-3, OQ-5, OQ-6, OQ-9, OQ-16, OQ-17, OQ-22, …". OQ-17 row text carries the amendment ("(§8; stated in FR-21 until 2026-09-09)") but its status reads "Resolved 2026-09-08" only. *Fix (PRD OQ-17 status):* "Resolved 2026-09-08; amended 2026-09-09".
- **[low] CI-6 — A-23's "Where" names FR-33, which carries no `[ASSUMPTION A-23]` tag** (§8.1 882 ↔ FR-33 488; tags at 601 only). Row: "Where: FR-24, FR-33". FR-33 488 restates the aggregation rule untagged ("an Inspector with an unreviewed inspection cannot open another post-hoc inspection"). *Fix (PRD):* either tag FR-33 488 "`[ASSUMPTION A-23]`" or set the row's Where to "FR-24".
- **[low] CI-7 — FR-11 applies the FR-18 exits "unchanged" to an automatic post, including an Eligible-Approver abandon that cannot exist in Automatic Response Mode** (FR-11 334 ↔ FR-8 286, FR-18 441/449). FR-11: "`PostingFailed` with the same retry bound, … and the same exits apply unchanged." FR-8 step 6 resolves Eligible Approvers "where Confirmation Response Mode applies" only, so an automatic post has no resolved Approver set; FR-18 449's first exit ("an Eligible Approver abandons it") is unreachable for it. Not a contradiction in outcome (the Tenant Agent Administrator and system exits remain), but the AD-13 divergence (CX-2) turns on exactly this point. *Fix (PRD FR-11 334):* "and the same exits apply, except that no Eligible Approver exists for an automatic post, so its human exit is the Tenant Agent Administrator row alone."
- **[low] CI-8 — SM-4's exempt-row list omits the register-recorded half of the readiness row** (SM-4 954 ↔ FR-33 479, FR-28 701). SM-4 requires "at least one recorded denial" for "every FR-33 row that is a callable operation on the §10 public surface" and exempts named human-process rows. Row 479 combines a public-surface operation (enable production-like generation) with "record the `RQ-1` READY or NOT READY decision in the launch readiness register", which FR-28 701 says is "evaluated and recorded in that register by the Release Operator, not in the Agent aggregate". *Fix (PRD SM-4 954):* add "the `RQ-1` recording within the readiness row" to the exempt list.
- **[low] CI-9 — Acceptance-time `MembershipRejected` has no SM-C4 class and no FR-25 counter** (FR-2 156, 162 ↔ SM-C4 971–975, FR-25 618–621). FR-2 produces `MembershipRejected` at acceptance when a re-add under a standing admission is refused (156) and when the A-21 AI-type read fails (162). SM-C4 lists it in no class; FR-25 621 counts unavailable calls by three reasons and names `MembershipRejected` only as a posting-failure reason (620). *Fix (PRD SM-C4 975):* add "`MembershipRejected` at acceptance (FR-2)" to the separately-reported set, and FR-25 621 accordingly.
- **[low] CI-10 — Addendum restates a count that the PRD no longer matches** (`addendum.md` 60 ↔ prd.md). Addendum: "Folding the fourteen uses [of Release PM] into the Release Operator role was rejected"; the PRD now carries nineteen. Not a contradiction of a rule; a stale-prone number. *Fix (addendum 60):* "Folding every use of 'Release PM' into …".

### Checked clean (internal)

- FR-8 step citations: 283 (step 10 = membership), 292 (steps 1–8 / step 9), 403 (steps 2–10), 727 (step 5 = rate limits and concurrency), 854 (step 6 = Eligible Approver), 975 (steps 2 and 4 = lifecycle, eligibility) all resolve to the reordered list at 280–290; FR-28 703 (reserve after context measurement, before Provider) and FR-7 253 (resolution before Provider work) agree with steps 7–8 and 6.
- FR-18 table ↔ prose: every prose exit from `PostingFailed` (449) has a row (438–443); `PostingWindowElapsed` row 440 ↔ 448 ↔ OQ-3 ↔ SM-3 962 ("counts as decided"); human abandon row 441 ↔ FR-33 489 ↔ 449; administrative retry row 443 ↔ FR-33 490; `Approved`-origin abandon skips the lookup (438 guard "for `PostingFailed`" ↔ 450); `ExpiredWhileSuspended` 433 ↔ FR-28 709 ↔ OQ-27; kill-switch guards 428–434 ↔ FR-3 179 ↔ FR-28 709; four system reasons 445 ↔ table.
- FR-25 counters ↔ SM-C4 classes ↔ FR-9 309 / FR-7 263 / FR-2 163: blocked, failed, unavailable, separately-reported sets agree apart from CI-2 and CI-9.
- FR-33 rows ↔ FR-1, FR-2 165–168, FR-4, FR-7, FR-18, FR-24, FR-26, FR-28 (items 6, 9; SM-4; trigger review; pull/release), FR-30, FR-32, FR-34, §3 role entries: every action an FR names has a row with the same actor; "six roles" (93, 457, 463) and "Platform Operator is the one platform-scoped role" (109, 500) hold; Release PM is defined (115) and confined to register-recorded rows (480–481).
- §8.1: every inline `[ASSUMPTION A-n]` tag (100, 104, 158, 165, 202, 236, 256, 303, 402, 449, 468, 469, 472, 474, 475, 495, 601 ×2, 703, 711 ×2, 712, 730, 738, 831 ×3, 832, 833, 834, 835, 836, 839, 949, 962, 963, 1003) resolves to a row A-1..A-26 whose "Where" names it (CI-6 excepted); A-26 keys A-9..A-12 in-table.
- §13 OQ-1..OQ-31 ↔ FR text: OQ-3/FR-18, OQ-5/NFR-9, OQ-6/FR-28, OQ-9/FR-26–27–31, OQ-10/FR-9, OQ-11/§12, OQ-14/FR-7, OQ-16/FR-2, OQ-18/FR-27, OQ-20/FR-6, OQ-21/FR-24, OQ-22/FR-28, OQ-23/FR-28 item 10, OQ-24/FR-28–34, OQ-25/FR-2, OQ-27/FR-28, OQ-28/FR-1–33, OQ-29/FR-4, OQ-30/FR-24, OQ-31/FR-34–§9 all state the same rule; OQ-26's "on the same terms" claim about AD-5 is a cross-artifact matter (CX-6).
- SM / SM-C ↔ FR-28 launch-health rules: review cadence (702, 713, 949, 960), trigger thresholds (711 ↔ A-17 ↔ SM-C4 975), posting-failure cohort (711 ↔ FR-11 334), `InsufficientEvidence` (714 ↔ NFR-14 ↔ §11 943), SM-3/SM-C5 denominators (962, 976 ↔ 709).
- §6.1 ↔ FR-18 (ten states, 772), §6.1 ↔ FR-1 (provisioning, 763), §6.2 ↔ FR-4/FR-7/§9; §5 non-goals ↔ FR-9/NFR-8/§6.2.
- Glossary ↔ FR text: Conversation Agent State (97 ↔ FR-2), Eligible Approver (103 ↔ FR-7 251–255), Approver Policy (90 ↔ FR-7 236), Agent Administrator (83 ↔ FR-33 471, 476, 469), Platform Operator (109 ↔ FR-33), Release Operator (114 ↔ FR-33 476–485), Release PM (115 ↔ 480–481), Compliance Inspector (93 ↔ 492–495), Accepted Agent Call (81 ↔ FR-8, NFR-9, SM-2), Safe Context Budget (118 ↔ FR-9 303), Conversation Facilitator (100 ↔ FR-7 246, OQ-14).
- `addendum.md` ↔ `prd.md`: every rejected alternative maps to a landed rule (lines 59–82 ↔ FR-28 item 9/§8.1, §3 Release PM, FR-2 A-22, FR-18 `PostingWindowElapsed`, FR-4 loosening/tightening, FR-34 secrets-custodied value, FR-24 A-23, FR-8 reorder, FR-2 `MirrorRefused`, OQ-31, FR-13/EXT-CONV-UI-1, FR-7 human-only, FR-15 `SafetyFailed`, FR-26 "every applicable version", FR-18 lookup skip from `Approved`, FR-31, NFR-9/OQ-5, FR-9 `ContextUnavailable`, FR-4 pricing version, A-17 sample, A-26, §8 gate rules, §8 Story 5.3); addendum 49–50 now describe AD-5/AD-12 as corrected, matching §8.1. No contradiction.

## Part B — Cross-artifact (CX-n)

Each finding names both quotes, file + line + mtime, and which side changes.

### Critical

None.

### High

- **[high] CX-1 — PRD calls `SuspensionReviewOverdue` and `DeferredAssumption` "register vocabulary"; the register's closed blocker vocabulary has neither** (FR-28 701, FR-30 632, FR-33 481, §8.1 852 ↔ `launch-readiness-register.md` 38–56, 70–81, mtime 12:49:11; Spine AD-17 231, mtime 15:42:23). PRD FR-28: "`GateOutOfScope`, `TriggerReviewOverdue`, `SuspensionReviewOverdue`, and `DeferredAssumption` are register vocabulary that the FR-30 surface mirrors." Register: "The following support-safe codes are the closed cross-gate vocabulary for V1 … a consumer may not invent a peer code. An unknown code fails closed." — the table lists `GateRecordMissing`, `EvidenceNotRecorded`, `DependencyUncommitted`, `DependencyNotAvailable`, `InsufficientEvidence`, `UnretiredAssumption`, `OpenDecision`, `GateOutOfScope`, `TriggerReviewOverdue`, `PayloadProtectionUnavailable`, `ProhibitedCostControlPosture`; the emitter table 72–81 has no row for either new code; the register also has no record kind for a Release PM deferral or a kill-switch review deadline. Spine AD-17 231 emits the same closed set. Under the register's own rule, a FR-30 surface emitting `SuspensionReviewOverdue` fails closed. *Fix — register:* add `SuspensionReviewOverdue` (emitted by `LR-PRODUCT-METRICS` post-enablement, "a kill-switch pull older than 7 days without a recorded review decision or containment finding") and `DeferredAssumption` (emitted by `RQ-1` evaluation, "a late-added `ARCH-A` row deferred by a recorded Release PM decision naming row, owner, unexercised FR consequences, and revisit date") to the vocabulary and emitter tables, and a record form for the two Release PM decisions FR-33 480–481 place in the register; Spine AD-17 231 likewise. *PRD side:* date the claim until the register lands — "are register vocabulary (extension dated 2026-09-09, pending in the register)".
- **[high] CX-2 — Spine AD-13 still requires Eligible Approver resolution in every response mode, its acceptance order omits the Conversation Agent State step and places resolution after safety, and AD-21 cites "FR-8 step 4" for rate limits; the PRD dates only the first of these** (FR-8 280–290, FR-7 253, FR-32 727, §8.1 854 ↔ Spine AD-13 201, AD-21 255, diagram 398, mtime 15:42:23). Spine AD-13: "Acceptance follows the FR-8 order (authorization, lifecycle, provider eligibility, rate limits, context measurement, reservation, pre-Provider safety, Eligible Approver resolution, membership) … Eligible Approver resolution runs in every response mode, not Confirmation mode only — an Automatic-mode Agent with zero configured Approvers is rejected up front with `NoEligibleApprover`". PRD FR-8: "3. Conversation Agent State … 4. Provider/model eligibility … 5. Rate limits and the per-Party concurrent bound … 6. Eligible Approver resolution where Confirmation Response Mode applies (FR-7). 7. Conversation Context Policy … 8. Cost reservation … 9. Pre-Provider Content Safety Policy … 10. Agent membership". Spine AD-21: "one atomic `BudgetLedger` `AdmitCall` command at FR-8 step 4 enforces the per-Party and per-Conversation windows and the `MaxConcurrentNonterminalInteractionsPerParty`"; PRD FR-32: "enforced at FR-8 step 5". A developer following AD-13 rejects every Automatic-mode call on a tenant with no Approver Policy (which FR-1 permits) and resolves Approvers after spending the classifier call the PRD orders before it; AD-21's "step 4" now points at Provider/model eligibility. *Fix — Spine:* AD-13 201 and diagram 398: "(authorization, lifecycle, Conversation Agent State, provider eligibility, rate limits, Eligible Approver resolution where Confirmation Response Mode applies, context measurement, reservation, pre-Provider safety, membership)" and delete the every-mode sentence; AD-21 255: "at FR-8 step 5". *PRD side (§8.1 854):* extend the dated sentence: "Spine AD-13 requires Eligible Approver resolution in every response mode and lists the acceptance steps in the pre-reorder sequence, and AD-21 cites the pre-reorder step number; FR-8 governs".
- **[high] CX-3 — §8.1 asserts exactly one open Spine divergence and one pending amendment, and points at a Spine "reconciliation ledger" that does not exist** (§8.1 854, OQ-26 1011 ↔ Spine, mtime 15:42:23: no section, heading, or sentence containing "ledger", "defect", or "divergence" outside the sources list at 34–46). PRD: "Open Spine defects are tracked in the Spine's own reconciliation ledger, not restated here … One divergence is open at this revision … Spine AD-13 … One PRD-originated amendment, dated 2026-09-09, is pending in the Spine: FR-18 now allows a human abandon from `Approved` and a `PostingWindowElapsed` system abandonment … This PRD states … no list of its open defects beyond the dated sentences above". On disk the Spine, written 26 minutes before the PRD's third update, also diverges on: the AD-13 order and AD-21 step (CX-2), the AD-5 `MessageId` lookup from `Approved` (CX-6), the AD-12 small-tenant sample (CX-7), AD-22 inspection escalation and aggregation (CX-8), AD-14 attestation cadence/pin/compare value (CX-9), AD-7 re-admission ungated by A-22 (CX-11), the third-update typed names (CX-12), and `Draft` (CX-13). The "one/one" count is a restated fact that was already false when written, and the pointer resolves to nothing. *Fix — PRD (§8.1 854):* replace "One divergence is open … One PRD-originated amendment … is pending" with "The third 2026-09-09 update was applied after the Spine's last edit (`updated: 2026-09-09`, `ARCH-A-INDEX-3`); every FR amended in that update is a PRD-originated amendment pending in the Spine until its index version increments, and the dated divergences named below are examples, not the set" and replace "the Spine's own reconciliation ledger" with "the Spine's next update report" (or drop the pointer). *Fix — Spine:* add a dated reconciliation ledger section, or absorb CX-2, CX-6 to CX-13 in one update that increments `architecture_assumption_index_version`.
- **[high] CX-4 — The data-handling record and `DataHandlingVersion` acceptance gate are still absent from Spine AD-2/AD-10 and from the register's Provider Readiness Contract (prior CX-6, still open)** (FR-4 202, FR-5 212, FR-24 607, FR-33 468–469, OQ-29 ↔ Spine AD-10 181, AD-2 129, mtime 15:42:23 — `DataHandlingVersion` 0 hits; `launch-readiness-register.md` 209–211, mtime 12:49:11). PRD FR-5: activation requires "its data-handling record is complete and its current `DataHandlingVersion` accepted for this tenant (FR-4)"; FR-4: "A change that loosens any field … blocks the model for the tenant immediately with a typed reason". Spine AD-10 hard gates: "missing, stale, unconfigured, unpriced, invalid-limit, failed, disabled, not-enabled, regressed, currency-mismatched …, unknown, or indeterminate state is `Blocked`" — no data-handling gate; register 211: "freshness, platform enabled state, tenant enablement, configured/text-generation state, current pricing, secret resolution, Provider health, non-regressed capability version, and valid positive limits" — no data-handling reason code. A readiness result built from either reports `Callable` for a model whose terms the tenant never accepted. *Fix — Spine AD-10 / AD-2 and register Provider Readiness Contract* (add the four-field record, `DataHandlingVersion`, tenant acceptance held on `TenantProviderEnablement`, loosening-blocks / tightening-grace, and reason codes such as `DataHandlingUnrecorded`, `DataHandlingUnaccepted`). PRD-side change: none.

### Medium

- **[medium] CX-5 — Register `EXT-CONV-AI-1` seam 1 does not carry the A-22 admission restriction the PRD says retires with that entry, nor the typed already-absent removal answer (prior CX-9)** (§8 831, §8.1 881 ↔ `external-dependency-register.md` 57, 60, mtime 13:28:06). PRD: "Conversations … restricts adding an `AiAgent` participant to `ParticipantRole.Facilitator` or the Conversation's administrative role, which is what lets FR-2 read presence again as re-admission [ASSUMPTION A-22]"; A-22 retires when "`EXT-CONV-AI-1` reaches `Committed` with that rule". Register seam (1): "limited for Agents to stable AI Party identity, `ParticipantType.AiAgent`, and `ParticipantRole.Member` … plus a participant-state read for the AI participant and a participant removal limited to the AI participant"; its assumption list reads "(PRD §8.1 assumptions A-1 through A-4, A-15, A-16, A-21)" — no A-22, no admission-authority rule, no typed already-absent answer. As written, `Committed` could not retire A-22, and FR-2 158's presence-based re-admission would rest on nothing. *Fix — register seam (1), contract 60, assumption list:* add "adding an `AiAgent` participant is restricted to `ParticipantRole.Facilitator` or the Conversation's administrative role (A-22)" and "removal answers a typed already-absent as a confirmed no-op". PRD-side change: none.
- **[medium] CX-6 — Spine AD-5 runs the `MessageId` lookup before the `Approved` → `Abandoned` transition; FR-18 skips it, and OQ-26 says the two are "on the same terms"** (FR-18 438, 450, OQ-26 1011, `addendum.md` 73 ↔ Spine AD-5 149, mtime 15:42:23). Spine: "before the system's `Approved` -> `Abandoned` (`RemovedInConversations`) transition … the system looks the approved version's `MessageId` up … An unavailable lookup refuses the exit and the proposal stays in its pre-check state — … `Approved` for the `Approved`-origin system-abandon check". PRD: "The lookup is skipped for any proposal for which no post was ever attempted — … every `Approved` proposal, since posting has not started — so a removal detected against an `Approved` proposal abandons it directly"; OQ-26: "The system abandonment from `Approved` and `PostingFailed` on a detected removal is stated by FR-18 and by Spine AD-5 … on the same terms". The addendum records the AD-5 form as rejected on 2026-09-09, but neither §8.1 nor OQ-26 dates the divergence. *Fix — Spine AD-5:* skip the lookup for `Approved` (no post attempted). *PRD side:* OQ-26 "on the same terms" → "on the same terms except that AD-5 still performs the `MessageId` lookup from `Approved`, which FR-18 skips (pending in the Spine, 2026-09-09)"; add the same clause to §8.1 854.
- **[medium] CX-7 — Spine AD-12's small-tenant minimum sample is the flat "20 calls" the PRD replaced** (FR-28 711, A-17 876, `addendum.md` 78 ↔ Spine AD-12 195, mtime 15:42:23). Spine: "a tenant with fewer than 5 calling Parties uses every Party that called and 20 calls." PRD: "for a tenant with fewer than 5 calling Parties it is every Party that called and 10 Agent Calls per calling Party, never fewer than 20 nor more than 50". A three-Party tenant needs 30 calls under the PRD and 20 under the Spine. *Fix — Spine AD-12 195:* "every Party that called and 10 Agent Calls per calling Party, bounded to 20–50 (PRD A-17)". PRD-side change: none.
- **[medium] CX-8 — Spine AD-22 defines no escalation for an unreviewed inspection and no aggregation bound; FR-24 defines both** (FR-24 601, FR-33 488, A-23 882 ↔ Spine AD-22 261, mtime 15:42:23). Spine: "An inspection not reviewed within its post-hoc window is recorded as unreviewed on the `audit-evidence` projection — … the unreviewed flag persists as a visible governance signal … no further automatic escalation is defined for V1." PRD: "an inspection not reviewed within that window is recorded as unreviewed on the audit surface and is a compliance finding reported at the next launch-health review. Post-hoc inspections by one Inspector that touch more than 5 distinct Conversations in a rolling 30-day window are one wide inspection: the sixth requires prior Platform Operator approval [ASSUMPTION A-23]. An Inspector with an unreviewed inspection cannot open another post-hoc inspection until it is reviewed or the Platform Operator records a disposition." *Fix — Spine AD-22:* replace "no further automatic escalation is defined for V1" with the FR-24 block-next-inspection rule, the launch-health finding, and the A-23 aggregation bound and rate threshold. PRD-side change: none.
- **[medium] CX-9 — FR-34's attestation cadence, engine-identity pin, and Release-Operator-owned compare value are absent from Spine AD-14 and from the `EXT-SECRETS-1` register artifact** (FR-34 738, FR-27 679, A-24 883 ↔ Spine AD-14 209, mtime 15:42:23; `external-dependency-register.md` 147, mtime 13:28:06). PRD: "The attestation runs at startup, on every readiness evaluation, on a bounded cadence — hourly by default [ASSUMPTION A-24] — and on any host composition change, and the attested engine identity is pinned: every seal and unseal call verifies the engine instance against the pin … The committed engine identity and version are … provisioned to the runtime as a Release-Operator-owned configuration value custodied through `EXT-SECRETS-1`". Spine AD-14: "`EXT-HOST-1` binds the `EXT-PROTECTION-1` engine at startup and on every readiness evaluation, records a canary protect/unprotect result plus engine identity and version" — no cadence, no pin, no compare value, and still no DEK-destroy/`Erased` step (prior CX-16). Register `EXT-SECRETS-1` `RequiredArtifact`: "Secret resolution, rotation, denial, and leak-evidence contract for Provider and export/deletion operations" — it names neither the per-tenant HMAC `DigestKey` (FR-27; Spine AD-22/AD-29) nor the FR-34 compare value nor the per-tenant KEK (Spine AD-22), so a `Committed` `EXT-SECRETS-1` would not custody what FR-27, FR-34, and AD-22 route through it. *Fix — Spine AD-14 209* (cadence A-24, pin on every seal/unseal, compare value from `EXT-SECRETS-1`, DEK-destroy and `Erased` replay steps); *register `EXT-SECRETS-1` artifact and contract* (add the versioned `DigestKey`, per-tenant KEK operations, and the Release-Operator-owned protection-target value). PRD-side change: none.
- **[medium] CX-10 — PRD lists a fourth `EXT-CONV-UI-1` artifact (the Conversation-level status entry) that the register's "Three artifact kinds" does not carry** (§8 838, FR-13 367, `addendum.md` 69 ↔ `external-dependency-register.md` 73, mtime 13:28:06; Spine AD-31 317). PRD: "tracked as `EXT-CONV-UI-1`: a versioned Conversation action contribution and registration contract …, rendering of the AI-generated and human-edited markers …, a callability read …, and a Conversation-level status entry showing the pending-proposal state to authorized Approvers (FR-13); the register's `RequiredArtifact` text governs the exact artifact kinds." Register: "Three artifact kinds. (1) … action contribution … (2) A per-message decoration slot … (3) `GetCallabilityAsync(tenant, conversation)`". Spine AD-31: "The Conversation status entry is rendered through the same decoration slot from the same provenance accessor; no third seam exists." The PRD hedges, but it still states a fourth kind as tracked; the addendum says the extension "is listed for correct-course", which the PRD sentence does not date. *Fix — register `EXT-CONV-UI-1` artifact:* add the status entry (as a use of kind 2 if AD-31's reading stands). *PRD side (§8 838):* "and a Conversation-level status entry … (a register extension dated 2026-09-09, pending; the register's `RequiredArtifact` text governs)".
- **[medium] CX-11 — Spine AD-7 re-admits `hexa` from `ExternallyRemoved` on presence without the A-22 gate, and AD-2's first paragraph still contradicts FR-2 on `MirrorPending`, `ReadmitPending`, and `BlockVersion` (prior CX-8, still open)** (FR-2 158, 166–168 ↔ Spine AD-7 163, AD-2 129 vs 131, mtime 15:42:23; the Spine cites no `A-22`..`A-26`). Spine AD-7: "`ExternallyRemoved` plus absent rejects and plus present records `Joined`". PRD: "Until A-22 is retired, presence again does not re-admit: the state stays `ExternallyRemoved`, the call is rejected on the same terms as `Blocked`, and only the FR-33 clear row re-admits." Spine AD-2 129: "a `MirrorPending` flag set whenever a block, clear, or removal record is written … `ReadmitPending` is entered on clear and holds until the Conversations mirror confirms re-admission … `BlockVersion` — incremented on every set or clear"; PRD FR-2: `ExternallyRemoved` has "no mirror because there is nothing to remove", a clear "cancels every outbox entry of a lower `BlockVersion`" and "`hexa` re-joins at the membership step of the next Agent Call", and only "setting it … increments the `BlockVersion`". Also AD-7 163 "`NeverJoined` causes no Conversations call" versus FR-2 167 "a block set from `NeverJoined` is confirmed without a seam call when the participant read confirms `hexa` absent, and is otherwise mirrored". *Fix — Spine:* AD-7 163 gate presence re-admission on A-22 and mirror a `NeverJoined` block when the read shows `hexa` present; AD-2 129 replace the `ConversationAgentState` clause with the 131 wording. PRD-side change: none.
- **[medium] CX-12 — Third-update typed names and rules exist only in the PRD: `MirrorRefused`, `PostingWindowElapsed`, `SafetyFailed`, `DeferredAssumption`, `SuspensionReviewOverdue`, human-only Approvers, the A-25 override bound, and the pricing-version rejection** (FR-2 167, FR-18 440/448, FR-15 391, FR-28 699/709, FR-7 249, FR-32 730, FR-4 198 ↔ Spine, mtime 15:42:23: 0 hits each; AD-8 169, AD-20 249, AD-21 255; epics.md, mtime 12:44:27: 0 hits each). Spine AD-21: "A cap override is audited, scoped, justified, Platform-Operator-only, carries a numeric ceiling and an expiry, and lapses at the earlier of the two" — PRD adds "bounded to at most 25% of the monthly cap and 7 days per override; a second override for the same tenant in the same month requires Product's recorded approval [ASSUMPTION A-25]". Spine AD-8 admits any predefined `PartyId` — PRD FR-7: "an organization or AI Party named as a predefined Party is a typed configuration rejection". Spine AD-20 marks a version on approval-time failure only — PRD FR-15 scans at edit time and stores a `SafetyFailed` marker. Spine AD-15 217's additive public-value list stops at the second update. These are the PRD-originated amendments CX-3 asks §8.1 to acknowledge collectively. *Fix — Spine* (absorb in one update: AD-7/AD-15 `MirrorRefused`; AD-5/AD-15 `PostingWindowElapsed` and human abandon from `Approved`; AD-20/AD-15 `SafetyFailed`; AD-17 the two register codes; AD-8 human-only; AD-21 A-25 and the server-assigned pricing version); *epics* (Stories 6.6, 7.2, 7.5, 7.7, 8.4, 8.7 own the corresponding members). PRD-side change: none beyond CX-3.

### Low

- **[low] CX-13 — The `Draft` lifecycle state exists only in the PRD** (FR-3 174, FR-22 552 ↔ Spine 0 hits, epics 0 hits; prior CX-11 still open for epics). Spine AD-2/AD-30 speak of "configure, activate, disable"; AD-10/AD-15 expose `AgentReadinessStatus` growth states but no `Draft`. *Fix — Spine AD-2 or AD-15:* name the public lifecycle enum `Draft`, `Active`, `Disabled` (+ `Unknown` sentinel); epics FR3 row and Stories 5.2/5.7.
- **[low] CX-14 — Spine frontmatter binds `PRD OQ-1..OQ-30`; the PRD now has OQ-31** (§13 1016 ↔ Spine 15, mtime 15:42:23). *Fix — Spine 15:* `OQ-1..OQ-31`.
- **[low] CX-15 — Spine AD-21 still carries the two settlement instants the PRD reconciled (prior CI-5 residue)** (FR-28 703, OQ-6 991 ↔ Spine AD-21 255). Spine: "an `Indeterminate` outcome holds the reservation for a configured period … and then settles `ChargedAtMaximum` … A reservation past its deadline with no authoritative outcome becomes `Unreconciled`, stays counted, and is settled only by an audited `TenantBudgetUpdate` operator command or at period close". PRD: "An `Indeterminate` reservation whose hold deadline passes without an operator settlement becomes `Unreconciled`: it stays counted against the cap and is settled only by an audited operator command or at period close at the estimated maximum". *Fix — Spine AD-21 255:* delete "and then settles `ChargedAtMaximum`" from the first sentence.
- **[low] CX-16 — Epics FR28 row states p99 gates at 30 executions; the PRD gates p99 only at 300** (NFR-9 803, OQ-5 990 ↔ `epics.md` 92, mtime 12:44:27). Epics: "runtime latency gates use their exact p95/p99 thresholds with at least 30 production-like executions". PRD: "the p99 targets are reported as launch-health values and become `RQ-1` gates only once a gate has at least 300 executions". *Fix — epics 92 (and Story 8.5 2913 if it gates p99).*
- **[low] CX-17 — Register vocabulary still describes `PayloadProtectionUnavailable` in host-report terms and `LR-PARTY-IDENTITY` still names "identity-link changes" (prior CX-14, CX-18, still open)** (FR-34 738, FR-2 149 ↔ `launch-readiness-register.md` 53, 100, mtime 12:49:11). Register 53: "The host has not reported the production payload-protection engine required by FR-34 as available"; PRD: "A readiness policy that … clears it on a host-asserted signal alone … does not satisfy this requirement". Register 100: "Invalidated by Party state, identity contract, or Agent identity-link changes"; PRD: the link is deprecate-and-reject. *Fix — register 53, 100.* PRD-side change: none.
- **[low] CX-18 — Remaining restated facts in the PRD that can go stale** (PRD-side, all currently true): the registers' and Spine's frontmatter dates quoted as "(`updated: 2026-09-09`)" at §0 16, §8.1 850 and 854, OQ-26 1011, OQ-27 1012; "six seams" (830) and "nine-field commitment record" (838) restating register structure; the nine `EXT-*` IDs enumerated at 824 ("initial register scope" hedges it); "No consuming story is `ready-for-dev`" (16), a sprint-status fact; addendum 60 "fourteen uses" (CI-10). None is a row count or line number, so the pointer-only rule is met; the frontmatter dates are the ones most likely to be false after the next Spine or register edit. *Fix (PRD, optional):* replace each "(`updated: 2026-09-09`)" with "as read on 2026-09-09" so the sentence stays true when the other file's frontmatter advances; keep "six"/"nine" only with "as the register states".

### Checked clean (cross-artifact)

- `external-dependency-register.md` (13:28:06): §0 16 and §8 828 ("no entry `Committed` or `Available`"; `EXT-HOST-1` returned to `Uncommitted` when its artifact gained the protection binding and attestation port) match 94–97 and 212; nine entries match §8 824; `Available`/`Committed` semantics (41, 45, 47) match §8 824 and FR-21 535; seam (2) existence-read answers, seam (4) count-or-feed, seam (5) typed `ConversationDeleted`/`PrincipalRemovedFromConversation` and current edit/delete state, seam (6) deletion signal match §8 832–836; seam (3) matches A-3; A-21 present in seam (1); `EXT-PROTECTION-1` contract 178 covers the five FR-34 attestation steps and identity/version equality; the Known Consumer Non-Conformance section matches §8 828 ("open Product approval instead of a dated record"); consuming-story numbers all exist in epics Epics 5–8.
- `launch-readiness-register.md` (12:49:11): `RQ-1` NOT-READY rule 17–19 matches FR-28 690–701; `UnretiredAssumption` names row, table, and index version (49, 75) as FR-28 item 9 requires; emitter rows for `DependencyNotAvailable`, `OpenDecision`, `ProhibitedCostControlPosture`, `PayloadProtectionUnavailable` (with `DependencyNotAvailable` for `EXT-PROTECTION-1`), `GateOutOfScope`, `TriggerReviewOverdue`, `InsufficientEvidence` match FR-28/FR-30/FR-34; `LR-PRODUCT-METRICS` 114/174/315 and `product-metrics` 288 carry the FR-28 gate/launch-health split; SM-7 contract 184 matches SM-7 963; two-miss rule and `product-metrics` trigger source 188 match FR-28 712 and AD-12; `AuthorizedProducer` for Platform gates (120–137) matches FR-33 479; the Platform-scope assumption sentence 139 matches Spine ARCH-A-3 and names Product and the Release PM; blocker vocabulary is register-owned, as FR-28 701 states, apart from CX-1.
- `ARCHITECTURE-SPINE.md` (15:42:23): `ARCH-A-INDEX-3` and `architecture_assumption_index_version: 3` as §8.1 850 cites; ARCH-A-5 retired, ARCH-A-6 owner "Architecture + Product" as FR-28 703; Architecture-owned rows carry `TBD` dates and self-report blocking, matching FR-28 item 9 (requirement unmet, not contradicted — prior CX-19); AD-2/AD-30 `ProvisionHexa` create-only under `Platform` matches FR-1/FR-33 470/OQ-28; AD-7 Party link/replace retired to the FR-23 register (FR-2 149, FR-23 567); AD-7 clear rule matches FR-2 168/FR-33 475; AD-5 `PostingPending` uninterruptible, 3-attempt/15-minute bound (A-7), `LateConfirmed`, administrative retry needing no read access; AD-12 `Approved` waits, retry clock paused with union `PausedDuration` (FR-18 451), `ExpiredWhileSuspended`, `Suspended` exposed distinctly, trigger review and four thresholds, `InsufficientEvidence`, `TriggerReviewOverdue`; AD-20 "never weaker" pair rule matches FR-26 655; AD-21 `NotInvoked`, `Unreconciled`, currency, no-default caps, Tenant Agent Administrator lower-only; AD-22 computed subject set, Platform Operator or second Inspector, 30-day anti-collusion, export prior approval, hold-release second approver, Platform Operator deletion with Inspector approval (A-12); AD-28 SM-2 denominator from a Conversations feed (A-4); AD-30 forged-extension test named for SM-4 954; AD-31 matches `EXT-CONV-UI-1` kinds (1)–(3).
- `epics.md` (12:44:27): "exactly 33 stories" (28, 32) matches 33 Story headings in Epics 5–8 (10 + 8 + 7 + 8), and the PRD restates no count; FR1, FR7, FR18, FR21, FR26, FR29–FR34 rows and the OQ-24..OQ-30 map match the PRD rules they cite; Story 5.3 keeps the Branch A/B decision open as §8 828 states; Story 7.4/7.5/7.7 `MessageId` lookup answers match FR-18 450; Story 6.6 five states and clear rule match FR-2. Rows FR4, FR5, FR12, FR22, FR25, FR27 remain pre-2026-09-09 summaries (prior CX-10, still open) and no story carries the `DataHandlingVersion` acceptance or grace (prior CX-12, still open; 1361 is the only data-handling AC).

## Prior findings status

Prior review: `review-consistency-2026-09-09-validate-3.md` (against prd.md at 10:08, 1000 lines).

| Prior ID | Finding | Status | Evidence (current file/line) |
| --- | --- | --- | --- |
| CI-1 | Safety re-check stated two ways ("then-current" vs "more restrictive") | **Resolved** | FR-26 651/655 "never weaker than it" / "pass every applicable policy version"; FR-17 414, FR-18 430/452, FR-27 678, OQ-9 994 |
| CI-2 | Party link/replace missing from FR-23 register list | **Resolved** | FR-23 567 |
| CI-3 | Administrative retry guard lacked budget-exhausted condition | **Resolved** | FR-18 row 443 |
| CI-4 | `ExpiredWhileSuspended` on overlap vs at expiry | **Resolved** | FR-18 row 433 "when `ExpiresAt` passed while the tenant was `Suspended`" |
| CI-5 | Two settlement instants for `Indeterminate` | **Resolved** in PRD (FR-28 703, OQ-6 991); Spine residue → CX-15 | |
| CI-6 | `NoEligibleApprover` abandonment over `PostingFailed` | **Resolved** | FR-7 259 |
| CI-7 | `ContextUnavailable` never produced | **Resolved** | FR-9 309 |
| CI-8 | Retry clock while `Disabled` unstated | **Resolved** (FR-3 179, FR-18 451); the retirement claim it added → CI-1 (new) | |
| CI-9 | Release PM / Product unmapped by FR-33 | **Resolved** | FR-33 463, §3 115, rows 480–481 |
| CI-10 | `Approved`-origin lookup guard | **Resolved** | FR-18 438 guard, 450 (lookup skipped from `Approved`); Spine now diverges → CX-6 |
| CI-11 | A-9 row lags clear rule | **Resolved** | §8.1 868 |
| CI-12 | §6.1 "or linking" | **Resolved** | §6.1 763 |
| CI-13 | FR-12 omits `ExternallyRemoved` | **Resolved** | FR-12 342 |
| CI-14 | FR-22 "failed-call" as proposal state | **Resolved** | FR-22 552 |
| CI-15 | OQ-22 narrows owners | **Resolved** | OQ-22 1007 "whatever its owner" |
| CI-16 | `GateOutOfScope`/`TriggerReviewOverdue` unclassified | **Resolved** (FR-28 701); the sentence now also names two codes the register lacks → CX-1 (new) | |
| CI-17 | Calling restriction absent from configuration lists | **Resolved** | §3 83, FR-33 471, §10 911 |
| CI-18 | Glossary omits lowering caps | **Resolved** | §3 83 |
| CX-1 | Spine AD-13 Eligible Approver in every mode | **Still open** (Spine AD-13 201, diagram 398 unchanged); PRD now dates it (§8.1 854) → CX-2 (new, widened) | |
| CX-2 | PRD said `EXT-HOST-1` `Committed` / "eight of the nine" | **Resolved** | §0 16, §8 828 |
| CX-3 | Seam-2 read / seam-4 feed "pending register extension" | **Resolved** | §8 832, 834 |
| CX-4 | PRD misstated Spine assumption range | **Resolved** | §8.1 850 (no count; cites index version) |
| CX-5 | AD-5/AD-12/AD-22 listed as open defects | **Resolved** | §8.1 854, OQ-26 1011, OQ-27 1012, addendum 49–50 |
| CX-6 | `DataHandlingVersion` absent from Spine AD-10 / register readiness contract | **Still open** → CX-4 (new) | Spine 0 hits; register 209–211 |
| CX-7 | PRD asserted a non-conformance record exists | **Resolved** | §8 828 |
| CX-8 | Spine AD-2 first paragraph self-contradiction | **Still open** → CX-11 (new) | Spine 129 vs 131 |
| CX-9 | Register seam 1 lacks typed already-absent | **Still open** → CX-5 (new) | Register 57, 60 |
| CX-10 | Epics inventory rows FR4/5/12/22/25/27 stale | **Still open** | epics 44, 46, 60, 80, 86, 90 |
| CX-11 | Epics lack `Draft`, `MembershipUnavailable`, `MembershipRejected` | **Still open** | epics 0 hits each |
| CX-12 | Epics lack `DataHandlingVersion` acceptance/grace ACs | **Still open** | epics 1361, 1396 only |
| CX-13 | Spine AD-17 narrows assumption owners | **Still open** | Spine 229 "Product, Architecture, or Governance" |
| CX-14 | Register `PayloadProtectionUnavailable` host-report wording | **Still open** → CX-17 (new) | Register 53 |
| CX-15 | Epics `AiAgent`/`AIAgent`, projection ids | **Still open** | epics 144, 160 |
| CX-16 | Spine AD-14 attestation omits DEK-destroy/`Erased` | **Still open** → CX-9 (new) | Spine 209 |
| CX-17 | Sprint proposal "29-story" vs epics 33 | **Resolved** (the phrase "29-story" no longer appears in `sprint-change-proposal-2026-09-09-2.md`, mtime 11:24; not re-read in depth) | |
| CX-18 | `LR-PARTY-IDENTITY` "identity-link changes" | **Still open** → CX-17 (new) | Register 100 |
| CX-19 | ARCH-A target dates `TBD` | **Still open** (unmet and self-reported, not contradicted) | Spine 775–786 |

Tally: 24 resolved (18 internal + CX-2, CX-3, CX-4, CX-5, CX-7, CX-17), 13 still open (CX-1, CX-6, CX-8, CX-9, CX-10, CX-11, CX-12, CX-13, CX-14, CX-15, CX-16, CX-18, CX-19), 0 regressed. Every still-open item is on the Spine, register, or epics side; the PRD's text about each is now correct or hedged, except where CX-3 (new) asks it to stop claiming a single open divergence.
