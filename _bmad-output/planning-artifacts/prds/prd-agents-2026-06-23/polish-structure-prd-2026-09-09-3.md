# Polish — Structure Lens (bmad-review) — `prd.md`, third 2026-09-09 update

- **Target:** `_bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md` (1,035 lines, 30,191 words before this pass; 1,081 lines, 30,239 words after the structure pass — the growth is list scaffolding and four sub-bullet labels, not new content).
- **Scope:** the whole launch-level PRD as it stands after the third 2026-09-09 update, weighted toward the material that update and its reviewer gate touched (FR-1, FR-2, FR-4, FR-5, FR-8, FR-11, FR-18, FR-24, FR-28, FR-34, §8, §8.1, §12, §13).
- **Constraints honored:** no requirement meaning, typed identifier, number, ID, threshold, table row, or section order changed; no sentence merged; every `[ASSUMPTION A-n]` tag left in place; YAML frontmatter untouched. Every applied item is shape-only: a paragraph or run-on bullet split into sub-bullets, or one glossary entry moved.
- **Purpose/audience read:** this document exists to help product, UX, architecture, implementation, QA, and release-readiness readers agree on and build the governed V1 `hexa` capability, with the launch gate and dependency status stated up front.
- **Structure model:** Strategic/Context (Pyramid). The document conforms: §0 leads with status and the readiness verdict, §4 groups features by area, §4 groups are MECE, and evidence (§11, §12) and decisions (§13) follow rather than lead. No model-fit finding. The remaining shape problem is local: several rule sets are single 200–400-word bullets that a reader must re-read to find one rule.

## Findings

| Pass | Original Text | Revised Text | Changes |
| --- | --- | --- | --- |
| structure | §3 — "Proposed Agent Reply" filed between "Provider" and "Provider/model selection" | MOVE: entry now precedes "Provider" | Applied. Alphabetical order restored; text unchanged (0 words) |
| structure | FR-4 data-handling record bullet — one 330-word bullet covering the record fields, enablement precondition, tenant acceptance, version-change lapse and tightening declaration, loosening definition, and `[ASSUMPTION A-10]` | CONDENSE (shape only): lead kept (record fields and `DataHandlingVersion`), then four sub-bullets — Enablement, Acceptance, Version change, Loosening — in the FR-2 "Setting/Mirroring/Clearing" style | Applied. A-10 stays at the end of the Loosening item; every sentence verbatim except the initial capital after each label (+5 words of labels) |
| structure | FR-8 `AgentInteractionStatus` bullet — one 230-word bullet: member list, terminal set, step-to-status mapping, and the non-duplication rule with the nine deprecated members | CONDENSE (shape only): lead keeps the member list and "this PRD states no member count"; three sub-bullets — terminal members; step mapping; proposal-state non-duplication | Applied. The terminal set §12 and FR-11 refer to is now findable on one line; 0 words changed |
| structure | FR-11 posting-record bullet — one 230-word bullet: record definition, FR-18 mechanisms applied, Eligible Approver substitution, terminal status and slot release, FR-28 counting, Spine AD-5 divergence | CONDENSE (shape only): lead sentence ending at "(FR-8, FR-18):" then five sub-bullets in the original order | Applied. The AD-5 divergence §8.1 cites is now its own line; 0 words changed |
| structure | FR-18 retry-budget bullet — the five "until one of the following occurs" exits run inline in one 90-word sentence | Sub-bullet the five exits as a list; "Two further exits come from the table" and "therefore always has an exit" kept as the closing lines | Applied. The prior pass rejected condensing this list to a table reference (it is the budget-exhausted subset); listing it changes nothing and makes the count of five visible |
| structure | FR-24 compliance-inspection sub-bullet — one 300-word sub-bullet: scope and second party, computed subject set, second-party selection, mutual-approval and wide-inspection rules, post-hoc window `[ASSUMPTION A-19]`, aggregation bound and unreviewed-inspection block `[ASSUMPTION A-23]`, rate threshold `[ASSUMPTION A-23]` and audit surface | CONDENSE (shape only): lead sentence ends at "outside the case's subject set:"; six third-level items in the original order; "Inspection fails closed outside these paths" unchanged | Applied. A-19 and both A-23 tags sit in their own items; 0 words changed |
| structure | FR-28 cost-cap bullet — one 250-word bullet: hard boundaries, reservation timing, `NotInvoked`, no-usage release and `Indeterminate` hold `[ASSUMPTION A-8]`, retry eligibility, `Unreconciled` and period close, missing pricing | CONDENSE (shape only): lead keeps the boundary sentence; four sub-bullets — reservation and `NotInvoked`; release, hold, retry eligibility; `Unreconciled` and period close; missing pricing | Applied. OQ-6 cites these rules by name and can now point at a line each; 0 words changed |
| structure | FR-28 trigger-review sub-bullet — one 420-word sub-bullet: the four rates, convening deadline and `TriggerReviewOverdue`, review validity, distinct-Party shares and convening floor, exclusions and acknowledgement closures, posting-failure cohort, minimum sample and `InsufficientEvidence`, the Release Operator's pull | CONDENSE (shape only): the rate list stays as the sub-bullet lead; seven third-level items in the original order; the SM-3/SM-7 trigger and the "Launch-health reviews run" closing line unchanged | Applied. This is the third nesting level the prior pass declined for the kill-switch effects; here the parent is a single 420-word item, so the level is justified. Both `[ASSUMPTION A-17]` tags stay where they were; 0 words changed |
| structure | FR-34 attestation bullet — one 330-word bullet whose two parts, Identity and Liveness, are introduced by "has two parts." and run on inside the paragraph | CONDENSE (shape only): "has two parts:" then two sub-bullets, Identity and Liveness; "The attestation passes only when both parts succeed" and the negative test follow as the closing lines | Applied. The A-24 tag stays in the lead; the Security-qualification test sentence in the next bullet is untouched; 0 words changed |
| structure | §13 preamble — one 230-word paragraph of amendment history (six "Rows OQ-… record decisions taken in …" sentences plus the OQ-12 clarification) followed by two general rules | Amendment history rendered as a seven-item list, sentences verbatim; "The Evidence Level taxonomy is normative in §11. Changes apply to future calls and proposals unless a row states otherwise." kept as the closing paragraph | Applied. Not a condensation (the 2026-09-08 and 2026-09-09 decisions to keep this history stand); a future update appends one item instead of one sentence; 0 words changed |
| structure | §6.1 In Scope list restates §4 feature headings | CUT | **Rejected.** The MVP scope list is the pyramid's headline grouping for a reader who never opens §4; the §4 headings carry consequences the list does not |
| structure | FR-16 regeneration-ceiling default and range restated in FR-32 | MERGE into one location | **Rejected.** A-6 explicitly records the restatement; each FR keys its own tests to the value. Cross-referenced restatement, not redundancy |
| structure | NFR-9 latency thresholds restated in OQ-5 | MERGE | **Rejected.** OQ-5 is the decision record; FR-28 already forbids restating the thresholds there and cites NFR-9. Dropping either would drop a testable location |
| structure | §3 "Agent Administrator" and "Tenant Agent Administrator" — two entries for one role | MERGE | **Rejected.** The pointer entry is one line and serves lookup under the FR-33 name; merging would drop the alias a reader searching by that name needs |
| structure | §8 "Register gate rules (moved here from FR-21)" — historical parenthetical | CUT the parenthetical | **Rejected.** OQ-17 cross-references the move ("stated in FR-21 until 2026-09-09"); cutting it orphans that reference |
| structure | §8 seam 1 — one 200-word numbered item with a long `[ASSUMPTION A-1: …]` bracket | Split into sub-items | Not applied. PRESERVE: the seven seams are a reference list read item by item, and the A-1 bracket's contents are the register-owned name list; splitting would separate the names from the tag |
| structure | FR IDs are non-sequential within §4 (FR-33 under 4.7, FR-29 under 4.8, FR-30 under 4.9, FR-31 and FR-32/FR-34 under 4.10) | Renumber or reorder | Not applied. PRESERVE: §0 declares "globally stable" IDs; grouping by feature area is the pyramid's grouping rule and the IDs are cited from downstream artifacts |
| structure | §3 glossary placed before §4 rather than as an appendix | MOVE to the end | Not applied. PRESERVE: human readers need the terms before FR-1; the document is dependency-first by design |
| structure | FR-18 `MessageId` lookup bullet (200 words) | Sub-bullet | Not applied. PRESERVE: the prior prose pass split it into short sentences that read as one argument (lookup, skip rule, present, classification, exits); a list would separate the classification from its two exit rules |

## Summary

- Recommendations: 19 (10 applied, 5 rejected, 4 preserved/not applied).
- Estimated reduction if all applied items are accepted: 0 words; the applied set added 48 words (+0.2%) of sub-bullet labels and list scaffolding, and 46 lines.
- No length target was provided.
- Comprehension trade-offs: none. Every applied change adds scannability to a human-read rule set and removes nothing; two changes add a third bullet level (FR-24 compliance inspection, FR-28 trigger review), each under a parent that was itself a single 300–420-word item.
