# Validation Report — Hexalith Agents

- **PRD:** `/home/administrator/projects/hexalith/agents/_bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md`
- **Rubric:** `/home/administrator/projects/hexalith/agents/.claude/skills/bmad-prd/assets/prd-validation-checklist.md`
- **Run at:** 2026-09-09T11:01:49Z
- **Grade:** Poor
- **Reviewers:** rubric, adversarial-general, consistency, implementation-drift

## Overall verdict
**Good on its own page; overtaken on the pages it points at.** The polish and by-hand fixes closed nine of the baseline's thirteen mediums without opening a new prose-versus-table seam of the same kind: the FR-18 table now has every exit the prose promises, the `MessageId` lookup has an authoritative-absent answer, `ExternallyRemoved` is one state with a named clearing authority, FR-34 clears on an attestation rather than a phrase, the compatibility command is exempt from the `Available` rule, and tenant acceptance is a tenant act. What is at risk is everything the PRD asserts about other documents: since 10:08 the approved correct-course proposal returned `EXT-HOST-1` to `Uncommitted` (so the §0 status line names the wrong exception), the Spine absorbed all three "open Spine defects" §8.1 lists and grew to twelve `ARCH-A` rows, and the register already carries the two seams §8 calls "pending" — none of it landed in the PRD, and the proposal does not list `prd.md` under `amends_if_approved`, so nothing is scheduled to land it. Inside the FR text one new decidability seam appeared (`ExpiredWhileSuspended` has two definitions that move the SM-3/SM-C5 denominators), and the four Product-deferred mediums (FR-26, FR-31, Agent Call contract, p99 at n = 30) are open for the third run in a row.

The three extra reviewers shift the picture from "re-sync" to "not signable". The adversarial pass would still not sign off: every Critical and High from the last gate is closed the way it said it would be, but the H-6 fix introduced one fail-open in the launch gate itself — a late `ARCH-A` row blocks `RQ-1` only if the Release PM chooses to acknowledge it (AC-1) — and nine Highs of a lower class remain (a state-machine branch that never self-heals, an enforcement register that omits the commands it names, an `Approved` state with no human exit and no clock, an automatic-mode posting path with no lifecycle, a 30-day data-handling grace that ships content under unaccepted terms, an attestation compared against a value the same operator supplies, a scope-splittable compliance inspection, an auto-re-admission that trusts any re-adder, and cross-document statements false on disk). The consistency pass finds the PRD internally sound (0 critical / 0 high / 10 medium / 8 low) but not consistent with its artifacts: five statements about the registers and the Spine are now false, one new critical defect sits in the Spine (AD-13 requires Eligible Approver resolution in Automatic Response Mode, contradicting FR-1, FR-7, FR-8 step 8 and UJ-2), and the `DataHandlingVersion` enablement gate is absent from Spine AD-10 and the register. The implementation-drift pass reports that the code has not moved since the baseline while the PRD widened the distance: six Criticals (prohibited cost postures still accepted at readiness recording, no FR-34 blocker or attestation, no segregation of duties on approval, no approval-time or edit-time safety scan, no pre-Provider scan, no cost-cap or rate-limit readiness blockers) and nine Highs, all shipped logic that a host binding the live ports would execute unchanged.

Grade composition: on the rubric alone this PRD would grade **Good** (five adequate, two strong, one high, no critical). It grades **Poor** because the run as a whole carries eight criticals. Two are in the PRD text or its immediate cross-document claims: AC-1 (`RQ-1` fails open on a late `ARCH-A` row — a PRD fix) and CX-1 (Spine AD-13 contradicts FR-8 Automatic mode — a Spine-targeted fix, no PRD change). The remaining six (DC-1..DC-6) target the codebase, not the PRD, and are masked today only by the fail-closed `Deferred*` defaults in `Program.cs`; they become live the moment a host binds `EXT-PROVIDER-1` or `EXT-CONV-AI-1`.

The dominant theme is that the PRD's cross-document claims went stale because the correct-course run edited the two registers, the Spine, and `epics.md` after the PRD's 10:08 write, and the approved proposal does not list `prd.md` under `amends_if_approved`, so no amendment is scheduled to re-sync it. The Spine and the launch-readiness register were also being edited concurrently while the reviewers ran (Spine mtimes 12:44, 12:49, 12:52; register 12:49), so every cross-artifact finding is against the on-disk text at the mtime the reviewer recorded, and a count or line number the PRD pins on those documents will be wrong within the hour on a day like this.

## Dimension verdicts
- Decision-readiness — adequate
- Substance over theater — strong
- Strategic coherence — strong
- Done-ness clarity — adequate
- Scope honesty — adequate
- Downstream usability — adequate
- Shape fit — adequate

## Findings by severity
### Critical (8)
**[Adversarial]** — AC-1 `RQ-1` fails open on a late `ARCH-A` row: the party that wants to launch decides whether a known unretired assumption blocks (§ FR-28 item 9 line 688; §8.1 line 838)
The headline rule is "No unretired assumption: every §8.1 `A-n` row and every `ARCH-A-n` row in the Architecture Spine table is retired, whatever its owner". The same item then says "An `ARCH-A` row added after the `RQ-1` evaluation is scheduled blocks only once the Release PM records an acknowledgement of it". Architecture discovers a real assumption two days before the scheduled evaluation and adds the row. The Release PM — whose deliverable is the launch — does nothing; the row does not block; `RQ-1` records READY over a table that contains an unretired row, contradicting the first sentence of the same item and §0's "Every unretired assumption ... blocks `RQ-1`". The prior H-6 fix asked for a guard against Architecture blocking indefinitely; what landed is a guard against Architecture blocking at all once a date is on the calendar, and the "target retirement date" every Architecture-owned row must carry has no consequence when it passes (the Spine's rows read "TBD — target milestone", which is not a date). The gate this whole document hinges on is gameable by inaction.
Fix: Invert the default: "An `ARCH-A` row added after the evaluation is scheduled blocks `RQ-1` unless the Release PM records a deferral naming the row, its owner, the FR consequences not exercised, and a revisit date; the deferral is itself visible on the FR-30 surface as `DeferredAssumption`." State that a passed target retirement date on an Architecture-owned row escalates the row to Product for a keep-or-retire decision and is reported at the launch-health review. Keep the anti-indefinite-blocking intent by bounding Architecture, not by unbounding the Release PM.

**[Consistency-cross-artifact]** — CX-1 Spine AD-13 requires Eligible Approver resolution, and a `NoEligibleApprover` rejection, in Automatic Response Mode (§ FR-1 132, §4.3 215, FR-7 259, FR-8 284, UJ-2 55–61 ↔ ARCHITECTURE-SPINE.md AD-13 197, diagram 398)
Spine: "Eligible Approver resolution runs in every response mode, not Confirmation mode only — an Automatic-mode Agent with zero configured Approvers is rejected up front with `NoEligibleApprover` before any Provider call". PRD FR-8 step 8: "Eligible Approver resolution where Confirmation Response Mode applies (FR-7)"; FR-1: "Response Policy, and Approver Policy where confirmation is enabled"; §4.3: "When confirmation is enabled, all approval authority comes from the Agent's Approver Policy"; FR-7 259 scopes the `NoEligibleApprover` rejection to a Confirmation Response Mode Conversation; UJ-2 has Milan receive an automatic reply with no Approver in the story. A developer following AD-13 rejects every Automatic-mode call on a tenant that never configured an Approver Policy, which FR-1 permits, and SM-C4 would report the calls as blocked. The Spine's own sequence diagram (398 "EligibleApprover resolution (both modes)") repeats it.
Fix: Spine AD-13 197 and diagram 398: "Eligible Approver resolution runs only where Confirmation Response Mode applies (PRD FR-8 step 8)". PRD-side change: none.

**[Drift]** — DC-1 Readiness recording still accepts `ReportingOnlyMonitoring` and `AcceptedLaunchRisk` (§ FR-28 line 695, FR-23 line 557, NFR-10 line 792, OQ-6 ↔ src/Hexalith.Agents/Agent/AgentAggregate.cs:1009-1012; AgentLaunchReadinessPolicy.cs:73-76; AgentLaunchReadinessBlocker.cs:22-46; CostControlPosture.cs:33-36)
PRD says the two postures are "rejected at readiness recording with a typed rejection, and surfaced as the additive `ProhibitedCostControlPosture` blocker wherever a previously recorded posture still carries them … Rejecting only an absent posture does not satisfy this requirement"; code rejects only `CostControlPosture.Unknown`, the readiness policy emits `MissingCostControlPosture` only for `Unknown`, the blocker enum has no `ProhibitedCostControlPosture` (0 whole-word hits), and neither value carries `[Obsolete]`; `LaunchReadinessPresentationTests.cs:53-54` still renders them as ordinary postures. Kind: forbidden-shipped. (Baseline debt item 1, unchanged.)
Fix: code — reject both at `RecordAgentLaunchReadiness`, add the blocker, and have `ComputeLaunchReadinessBlockers` emit it for a stored posture.

**[Drift]** — DC-2 FR-34 has no blocker, no attestation, and no fail-closed path; production-like generation can be enabled against the no-op default (§ FR-34 lines 723-731, FR-21 line 526, FR-28 item 6 line 685, §0 line 16 ↔ AgentLaunchReadinessPolicy.cs:44-80; AgentLaunchReadinessBlocker.cs:22-46; AgentAggregate.cs:637-660; AgentInteractionGateOrchestrator.cs:346-356; Program.cs:118-120)
PRD says "Launch readiness emits the additive blocker `PayloadProtectionUnavailable` … until a protection attestation passes, and production-like generation cannot be enabled while that blocker stands … A readiness policy that lacks the blocker, that clears it on a host-asserted signal alone, or that can be satisfied against the no-op default does not satisfy this requirement", and §0 states as current fact that "live content-bearing execution stays disabled under FR-34"; code has 0 whole-word hits for `PayloadProtectionUnavailable`, `DependencyNotAvailable`, `PayloadProtection`, or `IEventPayloadProtectionService`, the readiness policy computes eight blockers none of which concerns protection, the call-time gate tests only `ProductionLikeGenerationEnabled`, and content-bearing execution is blocked solely because `DeferredAgentGenerationProvider` returns `Unavailable` — the exact "host that binds a live Provider adapter before the protection engine" case FR-34 line 728 requires to fail closed would run. Kind: gap on a readiness fail-closed path (and §0's "stays disabled under FR-34" describes a mechanism that does not exist). (Replaces the baseline High.)
Fix: code — additive blocker, host-supplied attestation port, `PayloadProtectionUnavailable` outcome on every FR-21 content-bearing path; PRD — reword §0 to "stays disabled because no Provider adapter is bound; FR-34 is the required mechanism" until the blocker ships.

**[Drift]** — DC-3 Approval enforces no segregation of duties: the approving Party is never compared with the caller or the last editor (§ FR-7 lines 247-251, §3 "Eligible Approver" line 103, FR-17 ↔ AgentInteractionProposalApprovalOrchestrator.cs:76-77,114-126; ApproverPolicyVerdict.cs:26; AgentInteractionProposalApprovalRequest.cs:26-39)
PRD says "At approval time, the approving Party must itself be an Eligible Approver for the version being approved, so a caller can never approve their own Agent Call and no Party can approve a version it last edited" and "Every proposal action in FR-7 tests this one predicate"; code resolves authorization as `_approverPolicyResolver.ResolveAsync(request.TenantId, policy, ct)` and `ApproverPolicyVerdict.Evaluate(policy, resolution)` — a verdict computed from the tenant-level policy and its source outcomes only; the request carries `ApproverPartyId` but no caller identity, no version editor, and no Conversation roster, and the same shape is used by the edit, regeneration, rejection, and abandonment orchestrators. Any Party for whom the policy is resolvable can approve any version, including one it edited or one answering its own call. Kind: gap (governance rule on the approval path; masked today only because `DeferredApproverPolicyResolver` fails closed).
Fix: code — carry caller and last-editor identities on the request, resolve the Conversation-scoped Eligible Approver set, and refuse when the approver is the caller or the editor of the selected version.

**[Drift]** — DC-4 No approval-time or pre-post safety re-check, and a human-edited version is never scanned (§ FR-17 line 405, FR-27 lines 658, 665-667, FR-18 line 442, OQ-9 ↔ AgentInteractionProposalApprovalOrchestrator.cs:133-171 (`ApproveAndPostAsync`); AgentInteractionProposalEditOrchestrator.cs:53-54)
PRD says "The exact version being approved passes the then-current active policy at approval time. A human-edited version is scanned on the same terms as generated output; no edit path can place unscanned content into a Conversation" and the pre-post re-validation checks "that the approved version still passes the then-current Content Safety Policy"; code's approval path is read version → read Party → ensure membership → append, with no `IContentSafetyEvaluator` call, and the edit orchestrator's only dependencies are `IApproverPolicyResolver` and `IAgentCommandDispatcher` (it records a `ContentSafetyPolicyVersion` at :161 without evaluating anything). An edited version is therefore posted with content no scan has ever seen. Kind: gap (safety path).
Fix: code — scan at edit time and re-scan the exact selected version at approval and immediately before append, under the then-current policy.

**[Drift]** — DC-5 No pre-Provider scan of the caller prompt and Conversation Context (§ FR-27 lines 658, 666, 668-670, FR-8 step 7 line 283, OQ-9 ↔ AgentInteractionGenerationOrchestrator.cs:134-175; AgentInteractionProposalRegenerationOrchestrator.cs:200-245)
PRD says the system "applies Content Safety Policy to the prompt and authorized Conversation Context before Provider invocation" with a per-Conversation verdict cache keyed by an HMAC content hash; code's generation order is (1) re-read content, (2) read budget, (3) invoke Provider, (4) scan the generated output; regeneration follows the same order; there is no cache, no hash, and 0 hits for `HMAC`/`ContentHash`. Unscanned Conversation content leaves the platform on every call. Kind: gap (safety path; FR-8 step 7 is absent from the gate).
Fix: code — add the pre-Provider scan (prompt + not-yet-scanned history) before `InvokeProviderAsync`, with the keyed-hash cache as a follow-on.

**[Drift]** — DC-6 Production-like enablement has no cost-cap, rate-limit, or concurrency blockers, and no reservation step exists (§ FR-28 lines 692, 696, FR-32 lines 711, 715-716, FR-8 steps 4 and 6 lines 280-282 ↔ AgentLaunchReadinessPolicy.cs:44-80; AgentInteractionGateCheck.cs:23-53; AgentInteractionGateOrchestrator.cs:105-124)
PRD says "Per-tenant cost caps must be configured under FR-32 before production-like generation is enabled; an unconfigured cap is a readiness blocker, not a default" and "an unconfigured rate limit blocks production-like generation on the same terms"; code has 0 whole-word hits for `CostCap`, `RateLimit`, `Reservation`, `KillSwitch`, or `NotInvoked`, the readiness policy's eight blockers say nothing about caps, and the ten gate checks contain no rate-limit, concurrency, or reservation step. `EnableProductionLikeGeneration` therefore succeeds with unlimited spend. Kind: gap (readiness gate).
Fix: code — cap/rate-limit configuration, the corresponding readiness blockers, and the FR-8 step-4/step-6 gate checks.

### High (24)
**[Adversarial]** — AH-1 FR-2 says the Party link and replace commands are on the FR-23 deprecate-and-reject register; FR-23's enumerated register does not contain them (§ FR-2 line 148; FR-23 line 557; §3 line 90; OQ-25 line 995)
FR-2: "the shipped Party link and replace commands (Spine AD-7) are on the FR-23 deprecate-and-reject register, and a link or replace naming any Party is a typed rejection". FR-23's register is closed-form ("is:") and lists only the two `CostControlPosture` values, `ContentSafetyFailureHandling.BlockWithAuditableOverride`, and `ApproverPolicySourceKind.Caller`. A lazy implementer works from the FR-23 list — that is what the list is for — and leaves the link/replace commands live; the prior C-2 attack (a tenant role re-points `hexa` at a human Party) is back through the door the fix said it locked.
Fix: Add to the FR-23 register sentence: "and the Agent Party link and replace commands (FR-2, Spine AD-7; no principal may re-point `hexa`'s Party identity)". Add an FR-23 consequence: "A public command or value that any FR names as deprecate-and-reject is on this register; the register and those FRs are reconciled in the same change."

**[Adversarial]** — AH-2 The `Joined` + absent + "clear is newer" branch names no call outcome and never self-heals, and the predicate is undefined for a Conversation that was never blocked (§ FR-2 lines 154, 165; OQ-16 line 986)
"An absence observed while a mirror is unconfirmed or a clear is newer is the mirror's late effect, not an external removal; the state is left as it is." The PRD anticipates the late effect and then stops. Is the call accepted with `hexa` absent from the roster — in which case Provider budget is spent and the pre-post re-validation finds the same "late effect" and either posts as a non-member or abandons `RemovedInConversations` — or rejected, with which typed reason? Nothing moves the state: the last mirror stays confirmed, the clear stays newer, so every subsequent call to that Conversation lands in the same branch forever. `hexa` is silently, permanently unavailable there, with no state, no marker, and no blocked-call reason. Worse, in the ordinary case — a Conversation that was never blocked — there is no mirror at all, so "the last mirror is confirmed" is undefined; an implementer who evaluates `lastMirror is null` as not confirmed never detects an external removal in any never-blocked Conversation, which is the case the state exists for.
Fix: Add a branch: "`Joined`, absent, and a clear is newer than the last mirror, or no mirror exists: the absence is not a removal; re-add `hexa` idempotently (the clear or the original join is the standing admission), stay `Joined`, and accept; if the add is refused, reject with `MembershipRejected`." State explicitly: "A Conversation with no mirror history satisfies 'the last mirror is confirmed'." Add both as negative tests, replacing the two history sentences at line 158.

**[Adversarial]** — AH-3 `ExternallyRemoved` re-admits `hexa` on the say-so of whoever can add a participant in Conversations, and the PRD records no assumption about who that is (§ FR-2 lines 156, 165; §8 seam 1 line 817; §8.1 A-1, A-21)
"`ExternallyRemoved` and the read confirms `hexa` is present again: record `Joined` and accept, because re-adding `hexa` in Conversations is the removal authority's re-admission." The clause assumes the re-adder is the remover. Seam 1 says nothing about which Conversations principals may add an `AiAgent` participant, and no `A-n` row records the assumption. A Facilitator removes `hexa`; an ordinary Participant who dislikes the decision adds it back through the Conversations participant UI; the next call re-admits without any Agents-side authority. A removal performed in Conversations therefore has weaker protection than a block set in Agents, although both are "a rejecting state on the same terms". A hostile tenant gets a re-admission path that bypasses FR-33's clear row.
Fix: Either (a) record `A-22`: "Conversations restricts adding an `AiAgent` participant to `ParticipantRole.Facilitator` (or the Conversation's administrative role)", owned by the Conversations Maintainer, retired when `EXT-CONV-AI-1` reaches `Committed` with that rule, and state that until it is retired `ExternallyRemoved` + present-again moves to `ReadmitPending`, not `Joined`; or (b) drop the auto re-admission and require the FR-33 clear in every case. State which.

**[Adversarial]** — AH-4 `Approved` has no human exit and no clock, `PostingFailed` never expires, and the administrative retry has no age bound: approved content can sit outside every retention clock forever or be posted months late into a Conversation that has moved on (§ FR-18 table lines 425–433; FR-18 line 438; FR-3 line 176; FR-33 line 478; §9 line 881; deferred M-12)
"Approval freezes expiry: `Approved`, `PostingPending`, and `PostingFailed` proposals never expire." The only rows leaving `Approved` are system rows. No Approver, Administrator, or Platform Operator can abandon an `Approved` proposal. A Tenant Agent Administrator who disables `hexa` and never re-enables it leaves every `Approved` proposal — approved tenant content under an interaction key — waiting indefinitely, non-terminal, outside §9's "365 days after the interaction reaches a terminal state", with no operator able to end it except by blocking `hexa` in each Conversation. `PostingFailed` has exits but none is time-bound: "always has an exit" is not "always exits". And the audited administrative retry may run a year after approval — the re-validation checks safety, membership, and lifecycle, not whether the Conversation has moved on, and the Approver is never consulted.
Fix: Add a row "`Approved`, `PostingFailed` → `Abandoned` — Eligible Approver; Tenant Agent Administrator (audited) — the `MessageId` lookup finds no posted message (skipped when no post was attempted); permitted while `Disabled` or `Suspended`" (the Spine's "never a human abandon from `Approved`" is then a Spine defect to log). Add a post-decision staleness bound: "A proposal that has been `Approved` or `PostingFailed` for longer than the proposal's original expiry duration, excluding paused time, is system-abandoned with reason `PostingWindowElapsed`, counted as decided for SM-3." Make the administrative retry re-validate against that bound.

**[Adversarial]** — AH-5 Automatic-mode posting has no lifecycle: no failure state, no retry bound, no `MessageId` lookup, no exit — yet the trigger review counts automatic posts "as one approved and one failed on the same terms" (§ FR-11 lines 322–330; FR-12 line 334; FR-28 line 700; NFR-11 line 793; FR-25 line 609; Spine AD-7)
FR-18's machine is "A Proposed Agent Reply occupies exactly one of the states"; FR-11 says only "the system posts successful generated content". What happens when the append's acknowledgement is lost in Automatic Response Mode? The PRD gives three retries, a 15-minute window, a `MessageId` existence read, `LateConfirmed`, and typed exits to a proposal; an automatic post is not a proposal. A lazy implementer either retries until the Conversation answers (bounded only by A-2's uncommitted idempotency) or drops the generated, paid-for content with no status, no evidence of the failure reason, and no exit. The Spine has already invented "automatic-mode `PostingFailed`" to fill the gap — a proposal outcome absent from FR-18. FR-28's posting-failure rate presumes the state exists.
Fix: State in FR-11: "An automatic post follows the FR-18 posting rows from `Approved` onward — the generated version is the approved version, the generation success instant is the approval instant — including pre-post re-validation, `PostingPending`, `PostingFailed` with the same retry bound, the `MessageId` lookup, `LateConfirmed`, and the same exits; a `PostingFailed` automatic post is abandonable by the Tenant Agent Administrator on the FR-33 row." Expose the state on FR-25 as the Agent Call's posting outcome.

**[Adversarial]** — AH-6 A data-handling change runs a 30-day grace during which tenant content leaves under terms the tenant has not accepted, and "the version in force" is ambiguous in the evidence (§ FR-4 line 199; FR-5 line 209; FR-24 line 596; FR-33 line 459; A-10 line 859)
"until re-acceptance, calls continue under the previously accepted version for 30 days, after which the model cannot be called for that tenant". A data-handling record documents what the Provider does; changing it to "training-use: not opted out" or a new processing region describes a change that has already happened at the Provider. "Calls continue under the previously accepted version" is a fiction: the content goes to the Provider under the new terms while FR-24 "records the Provider data-handling record version in force" — the accepted one (misleading) or the current one (unaccepted)? The PRD does not say. The grace is a fail-open written to avoid an outage on a Platform Operator's catalogue edit.
Fix: "A `DataHandlingVersion` change that loosens any field (longer retention, training use enabled, new or broader region, weaker contractual reference) blocks the model for the tenant immediately until the Tenant Agent Administrator accepts the new version; a change that only tightens continues without re-acceptance and notifies. Audit Evidence records both the current `DataHandlingVersion` and the version the tenant accepted; they differ only in the tightening case." Retire the 30-day grace or limit it to tightening changes.

**[Adversarial]** — AH-7 The FR-34 attestation compares the engine against "the register's committed target", but the runtime "never parses this document" — so the committed identity reaches the runtime through the same host the Platform Operator controls (§ FR-34 line 727; FR-28 line 697; FR-33 line 474; §8 line 825)
"it passes only when every step succeeds and the engine reports an identity and version equal to the register's committed `EXT-PROTECTION-1` target". Where does the runtime get the target? Host configuration, which "the Platform Operator binds ... in the host (`EXT-HOST-1`)". The principal that binds the engine also supplies the value it is compared to; a wrapper that seals with a reversible transform, reports the committed identity string, and passes the canary round-trip clears the blocker. The self-test proves the engine can seal, unseal, and erase something; it does not prove the engine is the committed one, which is what the identity clause exists for.
Fix: "The committed engine identity and version are recorded in the launch readiness register by the Release Operator and provisioned to the runtime as a Release-Operator-owned configuration value through `EXT-SECRETS-1`, separate from host composition; a missing value, or a value that differs from the register, leaves the blocker standing. The attestation records which principal supplied each of the two values." Add to the FR-34 test: a host that binds an engine reporting the committed identity while the Release-Operator value is absent must still block.

**[Adversarial]** — AH-8 Compliance inspection can be scope-split: N single-Conversation post-hoc inspections cover a tenant with no prior approval, one colleague rubber-stamps them all, and an unreviewed inspection has no consequence (§ FR-24 line 590; FR-33 line 476; OQ-30 line 1000; deferred gate L-6)
"any inspection wider than one Conversation requires Platform Operator approval. Post-hoc review by such a second party is permitted only for an inspection scoped to a single proposal or a single Conversation". Inspector B opens 200 inspections, each scoped to one Conversation, each post-hoc; content is read on the spot. Inspector A reviews them within 7 days — the 30-day rule is directional, so A approving all of B's is allowed as long as B approves none of A's. The Platform Operator sees "the inspection rate", a number with no threshold and no obligation. If A declines to review, the inspection is "recorded as unreviewed" — and B opens the next one; the Spine confirms "already-granted read access is not retroactively revoked". Unilateral tenant-wide content access, one Conversation at a time, remains available to any single Inspector.
Fix: "Post-hoc inspections by one Inspector touching more than 5 distinct Conversations in a rolling 30-day window are one wide inspection: the sixth requires prior Platform Operator approval. An Inspector with an unreviewed inspection cannot open another post-hoc inspection until it is reviewed or the Platform Operator records a disposition. The inspection rate carries a threshold (A-n) above which the Platform Operator's review is mandatory at the next launch-health review."

**[Adversarial]** — AH-9 The PRD makes statements about the Spine and the register that are false on disk today: the three "open Spine defects" are already corrected, the seam-2 existence read and the seam-4 feed are already in the register, and `EXT-HOST-1` is `Uncommitted`, not `Committed` (§ §8.1 lines 840–844; §8 lines 818, 820; §0 line 16; §8 line 814)
Spine AD-5 already reads "`PostingPending` is uninterruptible ... now also fires from `Approved` and `PostingFailed` on a detected removal ... runs only after the `MessageId` lookup"; AD-12 already carries the trigger review, minimum sample, `InsufficientEvidence`, `TriggerReviewOverdue`; AD-22 already carries the computed subject set. The register's seam-2 line already has the existence read by `MessageId` with its typed answers. `EXT-HOST-1`'s `AcceptedStatus` is `Uncommitted`. So the "requirements authority" tells an implementer that AD-5 is a defect (it is correct), that the register lacks a read (it has it), and that one dependency is committed (none is). The prior H-6(a) was the same class of error — a cross-artifact claim false the day it was written — and the fix added more such claims with no closure rule.
Fix: Replace the enumerated defect list with a pointer: "Open Spine defects are tracked in the Spine's own reconciliation ledger (or `.memlog.md`), not restated here." Replace the two "pending register extension" sentences with "the register's seam text governs". Rewrite §0 and §8 status lines as "at this revision no register entry is `Committed` or `Available`; the register is authoritative for current status", so the PRD cannot go stale on a number it does not own.

**[Consistency-cross-artifact]** — CX-2 The PRD reports `EXT-HOST-1` as `Committed`; the register has returned it to `Uncommitted` (§ §0 16, §8 814 ↔ external-dependency-register.md 94, 97, 212)
PRD §0: "all critical external dependencies except `EXT-HOST-1` are `Uncommitted` — eight of the nine register entries (§8)"; §8 814: "At the time of this revision, only `EXT-HOST-1` is `Committed`". Register EXT-HOST-1 `AcceptedStatus` `Uncommitted`, with "Historical prior commitment only … The expanded protection binding and attestation artifact has not been re-accepted"; Current Blocking Summary: "All nine dependency records are `Uncommitted`". A reader of §0 would treat Stories 5.1 and 5.6 as unblocked by `EXT-HOST-1`.
Fix: PRD §0 16: "all nine critical external dependencies are `Uncommitted` (§8) — `EXT-HOST-1` returned to `Uncommitted` on 2026-09-09 when its required artifact gained the FR-34 attestation port — and none is `Available`"; §8 814: "At the time of this revision every entry is `Uncommitted`; `EXT-HOST-1`'s earlier target, date, and command are historical only (register)".

**[Consistency-cross-artifact]** — CX-3 The PRD says the seam-2 existence read and the seam-4 event feed are pending register extensions; both have landed (§ §8 818, 820 ↔ external-dependency-register.md 57, 60, 65)
PRD seam 2: "The existence read is not yet in the register's seam-2 text; adding it is a pending register extension (correct-course, 2026-09-09)"; seam 4: "The event-feed alternative is a pending register extension". Register seam (2) carries "an existence read by `MessageId` returning the message, typed absence, typed `ConversationDeleted`, typed `PrincipalRemovedFromConversation`, or unavailable … and record `LateConfirmed`"; seam (4) carries the Conversations-side tenant event feed; compatibility contract 60 names both; note 65: "Scope extended 2026-09-09".
Fix: PRD §8 818: replace the pending sentence with "The register's seam-2 text and compatibility contract carry the existence read (extended 2026-09-09); `EXT-CONV-AI-1` cannot reach `Available` on a command that does not exercise it"; 820: delete the pending sentence or replace with "(register seam 4, extended 2026-09-09)".

**[Consistency-cross-artifact]** — CX-4 The PRD misstates the Spine's assumption range and index (§ §8.1 836, FR-28 688 ↔ ARCHITECTURE-SPINE.md 11, 765, 763–786)
PRD: "at that date the table holds `ARCH-A-1` through `ARCH-A-10`, with `ARCH-A-5` retired". Spine at the same `updated:` date: `architecture_assumption_index_version: 3`, `ARCH-A-INDEX-3`, table `ARCH-A-1` through `ARCH-A-12` (`ARCH-A-11` disabled-Agent retry pause, owner Architecture + Product; `ARCH-A-12` `DigestKey` rotation, owner Architecture + Security). The "range is open" clause keeps FR-28 item 9 correct, but the count is false for the second time today, and `ARCH-A-11`'s retirement condition names Product, so it is a Product-blocking row the PRD does not acknowledge.
Fix: PRD §8.1 836: cite the index identifier instead of a count — "(frontmatter `updated: 2026-09-09`, `ARCH-A-INDEX-3`; the Spine's index is authoritative for the row set at any moment)" — and add `ARCH-A-11` to the FR-18/FR-3 text per CI-8.

**[Consistency-cross-artifact]** — CX-5 The PRD lists AD-5, AD-22, and AD-12 as open Spine defects; the Spine has corrected all three (§ §8.1 840–844, OQ-26 996, OQ-27 997, A-7 856, addendum.md 48–49 ↔ ARCHITECTURE-SPINE.md AD-5 145, AD-7 157, AD-12 189–191, AD-22 257, AD-30 301, Deferred table 778–786)
PRD §8.1: "three are open at this revision, owned by Architecture: AD-5 forbids the system abandonment from `Approved` and `PostingFailed` …; AD-22 names the Tenant Agent Administrator as the sole inspection second party …; AD-12 states the Release Operator trigger without the review, minimum sample, and `InsufficientEvidence` rule". Spine AD-5 now fires system abandonment from `Approved` and `PostingFailed` after the `MessageId` lookup; AD-22 computes the subject set and requires prior second-party approval for `ExportRequest` and `LegalHoldRelease`; AD-12 carries the mandatory trigger review, minimum sample, `InsufficientEvidence`, and `TriggerReviewOverdue`. OQ-26, OQ-27, A-7 and the addendum repeat the stale state. The Spine's `.memlog.md` explicitly flags "prd.md section 8.1 still names AD-5, AD-12, and AD-22 as open 'Spine defects' — all three are now corrected".
Fix: PRD §8.1 840–844: "none is open at this revision; the three recorded on 2026-09-09 (AD-5, AD-12, AD-22) were corrected by the Spine the same day"; OQ-26 and OQ-27: change "for Architecture to correct" / "owned by Architecture" to "corrected in the Spine on 2026-09-09"; addendum 48–49 likewise.

**[Consistency-cross-artifact]** — CX-6 The data-handling record and `DataHandlingVersion` enablement gate exist only in the PRD and epics Story 5.3; Spine AD-10 and the register's Provider Readiness Contract do not know them (§ FR-4 199, FR-5 209, FR-24 596, FR-33 458–459, OQ-29 999 ↔ ARCHITECTURE-SPINE.md AD-10 177, AD-2 125; launch-readiness-register.md 209–211; epics.md 1361–1364)
PRD FR-4: "A model whose data-handling record is absent or incomplete cannot be enabled for any tenant … The Tenant Agent Administrator accepts a named `DataHandlingVersion` for the tenant before the first activation that selects the model, and again when the version changes"; FR-5 makes acceptance an activation-eligibility element. Spine AD-10's capability floor has no data-handling record and no tenant acceptance; `DataHandlingVersion` has zero hits in the Spine. The register's Provider Readiness Contract reason codes and hard-gate list carry no data-handling or acceptance gate. A developer following AD-10 and the register builds a readiness result that reports `Callable` for a model whose Conversation Context leaves the platform under unaccepted terms.
Fix: Spine AD-10: add the data-handling record (four fields, `DataHandlingVersion`, contractual reference) to the capability floor and "data-handling record complete and current version accepted for the tenant (30-day grace after change)" to the hard gates; AD-2 `TenantProviderEnablement`: hold the accepted `DataHandlingVersion` per entry. Register Provider Readiness Contract: add reason codes (e.g. `DataHandlingUnrecorded`, `DataHandlingUnaccepted`). PRD-side change: none.

**[Rubric: Decision-readiness]** — The §0 status line and §8 report `EXT-HOST-1` as `Committed`; the register, under an approved proposal that does not amend the PRD, holds all nine entries `Uncommitted` (§ §0 line 16; §8 line 814; external-dependency-register.md lines 94, 212; sprint-change-proposal-2026-09-09-2.md line 450 and its amends_if_approved list)
§0 line 16 says "all critical external dependencies except `EXT-HOST-1` are `Uncommitted` — eight of the nine register entries"; §8 line 814 says "only `EXT-HOST-1` is `Committed`". The register says "All nine dependency records are `Uncommitted`", and the approved proposal names the two registers, the Spine, and `epics.md` under `amends_if_approved` but not `prd.md`. The exception §0 singles out is the one entry the PRD's own FR-34 change re-opened, and the §0 landing rule (line 18) gives a reader no signal that an amendment is pending because none is. This is the second time today the §0 count has drifted from the register; a count that must be re-typed at every register change is the wrong kind of sentence for a status line.
Fix: Replace both counts with the register-keyed form — "at this revision every §8 register entry is `Uncommitted` (the `EXT-HOST-1` commitment was returned to `Uncommitted` when FR-34 added the attestation port to its required artifact); the register is authoritative for current status" — and add the one sentence the baseline asked for: that FR-34's attestation port changed the `EXT-HOST-1` artifact and, under the §8 change rule, its status.

**[Drift]** — DH-1 Configuration still accepts `ContentSafetyFailureHandling.BlockWithAuditableOverride` and `ApproverPolicySourceKind.Caller`, and the UI offers `Caller` (§ FR-23 line 557, FR-7 line 233, FR-27 line 665, OQ-14 ↔ AgentAggregate.cs:852-855, :751-757; ContentSafetyFailureHandling.cs:26; ApproverPolicySourceKind.cs:24; ApproverPolicy.razor:110-116)
PRD says each register value "is rejected server-side with a typed rejection wherever it is presented" and "The caller source is retired"; code rejects only `Unknown` for failure handling, accepts `Caller` on the same branch as `ConversationOwner`, and the page still lists "The four V1 approver-source kinds (AD-8)" with `Caller` first; `AgentApproverPolicyTests.cs:85-96` and `AgentContentSafetyPolicyContractsTests.cs:37-72` lock the acceptance in. Kind: forbidden-shipped. (Baseline debt item 2, unchanged.)
Fix: code.

**[Drift]** — DH-2 Party link/replace commands still accepted; provisioning creates no AI-type Party; a tenant Agent Administrator can create `hexa` (§ FR-1 lines 132, 136, FR-2 lines 147-148, FR-33 row line 460, OQ-25, OQ-28 ↔ AgentAggregate.cs:306-336, :355-380, :106-115, :676-678; AgentPartyIdentityOrchestrator.cs:139; AgentsFrontComposerRegistration.cs:23)
PRD says "the shipped Party link and replace commands (Spine AD-7) are on the FR-23 deprecate-and-reject register, and a link or replace naming any Party is a typed rejection", "Provisioning also creates `hexa`'s Party identity … as an AI-type Party", and "a provision attempted by any tenant role is a typed denial"; code accepts both commands whenever the envelope carries a `Valid` Parties verdict and re-points `PartyId`, has 0 hits for `ProvisionAgent` and no Party-creation call, and gates `CreateAgent` on `actor:agentsAdmin`, which the server populates from the tenant `Agents.Administrator` policy; no Platform Operator role exists anywhere in `src/`. Kind: forbidden-shipped (link/replace) and drift (provisioning authority).
Fix: code — `[Obsolete]` plus typed rejection on both handlers, a Platform-scoped provision command that creates the Party, and denial of `CreateAgent` to tenant roles.

**[Drift]** — DH-3 `ProposalDetail.razor` still treats `PostingFailed` as terminal with no retry or abandon affordance (§ FR-18 lines 410, 439, FR-22 line 542 ↔ ProposalDetail.razor:245-251, 293-295; ProposalDetailTests.cs:126-131)
PRD says `PostingFailed` is "posting failed; bounded retry available", the terminal set is exactly `Posted`, `Rejected`, `Abandoned`, `Expired`, and "every surface uses this one terminal set"; code keeps `PostingFailed` in `_terminalStates`, excludes it from `_retryableStates`, and the test `Terminal_proposal_routes_to_start_new_call_and_offers_no_act_on` pins that behaviour. Kind: drift. (Baseline debt item 3, unchanged.)
Fix: code.

**[Drift]** — DH-4 `PostingFailed` has no abandon exit and an unbounded retry through re-approval, which also re-enters an in-flight `PostingPending` (§ FR-18 table lines 415, 429-433, 439-440, A-7 ↔ AgentInteractionAggregate.cs:504-509, :621-623; AgentInteractionProposalApprovalOrchestrator.cs:66, 103-109, 133-171; AgentProposalApprovalPolicy.cs:21-32)
PRD says "`PostingPending` is uninterruptible: nothing leaves it except the posting attempt's own conclusion", allows `PostingFailed → Abandoned` by an Eligible Approver or the Tenant Agent Administrator, bounds retry to "at most 3 attempts over 15 minutes", and requires the `MessageId` existence read before every retry and every exit; code rejects abandon from `PostingFailed` with `ProposalNotPending`, implements retry as a fresh `ApproveProposedAgentReply` that re-runs membership and append with no attempt counter or window, accepts that same command while the state is `PostingPending` (starting a second attempt under the same idempotency key), and has 0 hits for `LateConfirmed` or any existence read. A `PostingFailed` proposal whose Conversation is gone is therefore stranded. Kind: drift.
Fix: code — dedicated retry command with budget, abandon from `PostingFailed`, reject approve/retry while `PostingPending`, `MessageId` lookup on exit.

**[Drift]** — DH-5 Membership is established only at posting time; no pre-Provider membership step and no Conversation Agent State (§ FR-2 lines 151-160, 162-167, FR-8 step 9 line 285, FR-11 line 326, OQ-16, OQ-25 ↔ AgentInteractionGateOrchestrator.cs:105-124; AgentInteractionGateCheck.cs:23-53; AgentInteractionPostingOrchestrator.cs:148-151; ConversationClientResponsePoster.cs:54-95)
PRD says "Membership is the last pre-Provider step of an Accepted Agent Call" and "Verifying membership only at posting time, after Provider work, does not satisfy this requirement"; code's ten gate checks contain no `Membership` check, `EnsureAiAgentParticipantAsync` is called only from the two posting paths, it reads presence and returns `SeamUnavailable` when absent (no join, no `AddParticipantAsync`), and 0 hits exist for `ConversationAgentState`, `NeverJoined`, `ExternallyRemoved`, `ReadmitPending`, `MirrorPending`, `BlockVersion`, or `RemovedInConversations`, so no block, mirror, clear, or external-removal detection exists. Kind: forbidden-shipped (posting-time-only verification) and gap (state machine).
Fix: code.

**[Drift]** — DH-6 Expiry is enforced only by the timer command, never on approve/edit/regenerate, and the default expiry reader records no `ExpiresAt` (§ FR-18 lines 437-438, OQ-3 ↔ AgentInteractionAggregate.cs:364-430, 429-480, 482-550, :650-680; AgentProposalExpiryPolicy.cs:12, 47; DeferredProposalExpiryPolicyReader.cs:12, 21; AgentInteractionProposalOrchestrator.cs:55)
PRD says a proposal whose stored `ExpiresAt` has passed "is treated as `Expired` on every read and every command, regardless of timer delivery" and "The default expiry duration is 24 hours"; code's edit, regeneration, and approval handlers and orchestrators never consult `ExpiresAt`, so an expired-but-not-yet-timed proposal can be approved and posted, and the registered expiry reader "returns no expiry configured", so proposals are created with no `ExpiresAt` and never expire. Kind: drift (command-time enforcement) and gap (default).
Fix: code.

**[Drift]** — DH-7 Safe Context Budget subtracts only the reserved output allowance (§ FR-9 line 296, §3 "Safe Context Budget" line 117, A-5 ↔ AgentInteractionContextPolicy.cs:77)
PRD says the budget is the input limit "minus the reserved output allowance, the Agent Instructions, the caller prompt and any system framing the request adds, and a configured safety margin … A budget that subtracts only the output allowance does not satisfy this requirement"; code computes `int availableContextBudget = m.ContextWindowTokenLimit - m.ReservedOutputTokenCount;` and no instruction, prompt, framing, or margin term exists (0 hits for `SafetyMargin`/`MarginPercent`). Kind: forbidden-shipped (the PRD names the shipped formula as non-conformant).
Fix: code.

**[Drift]** — DH-8 Indeterminate Provider outcomes are mapped to `AdapterFailure`/`GenerationError`, and the retry budget is handed to the adapter unconditionally (§ FR-12 line 342, FR-4 line 197, FR-28 line 692, NFR-11 line 793 ↔ AgentOutputGenerationResult.cs:17-47; AgentInteractionGenerationOrchestrator.cs:155-159, 266-270; AgentInteractionProposalRegenerationOrchestrator.cs:357, 408-419; AgentGenerationProviderRequest.cs:20, 29; ProviderModelTimeoutPolicy.cs:9-10)
PRD says an undeterminable attempt "resolves to a typed `Indeterminate` terminal outcome … and is never silently retried. Mapping an indeterminate outcome to an adapter or generation error does not satisfy this requirement", and a retry budget "never authorizes re-invocation after an `Indeterminate` outcome"; code catches every adapter exception (including one thrown after the request left the process) as `AdapterFailure`, maps an unknown outcome to `AdapterFailure`, and passes `MaxRetries` into the adapter with no confirmed-no-usage condition. Kind: forbidden-shipped (mapping) and gap (`Indeterminate`).
Fix: code.

**[Drift]** — DH-9 Lifecycle and tenant suspension are not re-validated on approve/edit/regenerate, and no kill switch or `Suspended` status exists (§ FR-3 line 176, FR-18 table guards lines 419-421, 425, 433, line 442, FR-28 line 698, FR-25 line 611 ↔ AgentInteractionProposalApprovalOrchestrator.cs:133-171; edit and regeneration orchestrators; 0 hits for `KillSwitch`/`Suspended`)
PRD says "While the Agent is `Disabled` or its tenant is `Suspended`, proposals awaiting a decision accept reject and abandon only; approval, edit, and regeneration are rejected with a typed reason" and the pre-post re-validation checks "that the Agent is `Active`, its tenant not `Suspended`"; code's approval path checks the selected version, the Party, membership, and append only — a Disabled Agent's proposals can still be approved and posted. Kind: gap (governance guard on a shipped path).
Fix: code.

### Medium (46)
**[Adversarial]** — AM-1 The `ExpiredWhileSuspended` overlap rule lets a brief pull launder SM-3 and SM-C5 (§ FR-18 row line 424; FR-28 line 698; §12 lines 947, 961; OQ-27)
"recorded with reason `ExpiredWhileSuspended` when the awaiting window overlapped a `Suspended` period" and such proposals "leave the SM-3 and SM-C5 denominators". A one-minute pull each day overlaps every awaiting proposal's window; every one of them that later expires — hours or days after release — is excluded. The rule written "so the switch cannot manufacture its own launch-health miss" now lets it manufacture a pass.
Fix: "`ExpiredWhileSuspended` applies only when `ExpiresAt` fell inside a `Suspended` period, or when the remaining awaiting time at pull was shorter than the suspension's duration." Report the count of `ExpiredWhileSuspended` at each launch-health review next to the switch's pull history.

**[Adversarial]** — AM-2 Release is weaker than pull: either role releases an SM-4 pull with a justification string, no concurrence, no review, no bound (§ FR-33 lines 470–473; FR-28 line 698; §3 line 114)
A pull on a security event needs the Platform Operator's recorded confirmation; a pull on operational rates needs a convened review. Release: "Platform Operator or Release Operator, with audited justification". A Release Operator under pressure releases the Platform Operator's SM-4 pull ten minutes later; nothing requires the confirming party's concurrence or a recorded finding that the event is contained. Conversely, a self-confirmed SM-4 pull has no duration bound and no escalation, so one Platform Operator can suspend a tenant indefinitely.
Fix: "Release of a pull recorded on an SM-4 event requires the Platform Operator's recorded containment finding; release of a review pull requires a recorded review decision. A pull older than 7 days without a recorded review is `SuspensionReviewOverdue` on the FR-30 surface."

**[Adversarial]** — AM-3 Trigger-review episode semantics are undefined, and the Provider-or-generation failure share has no denominator (§ FR-28 line 700; A-17)
"A trigger review is mandatory when, over a rolling seven-day window, any of these rates exceeds its threshold". A rolling window exceeds continuously. Is a review owed per evaluation (daily reviews, then `TriggerReviewOverdue` noise) or per episode (one "continue" decision silences a condition that persists for months)? The Release Operator decides, alone, in the direction of fewer reviews. "The Provider-or-generation failure share above 20%" — share of Agent Calls, of Accepted Agent Calls, of distinct tuples? The other three rates say; this one does not.
Fix: "A review decision is valid for 7 days or until a rate rises by a further 10 points, whichever first; a persisting condition then requires a new review." State the failure-share denominator as Accepted Agent Calls that reached Provider invocation in the window.

**[Adversarial]** — AM-4 A tenant can force the Release Operator into mandatory reviews on demand (§ FR-28 line 700; FR-33 line 466; FR-32 line 707)
"a rejection at a limit the Tenant Agent Administrator lowered counts", "cost caps" is a blocked-call reason, and "for a tenant with fewer than 5 calling Parties it is every Party that called and 20 Agent Calls". A Tenant Agent Administrator lowers the rate limit to one call per day, four Parties make 20 calls, blocked share reaches 100%, and the Release Operator must convene and record a review within one business day or be recorded `TriggerReviewOverdue` — repeatable weekly across every tenant that wants attention.
Fix: Count tenant-caused rejections in the FR-25 surface and the tenant's own launch-health view, but let the review for a condition composed only of rejections at tenant-lowered limits or caps be a recorded acknowledgement rather than a convened review; a condition with any platform-caused reason keeps the convening bound.

**[Adversarial]** — AM-5 FR-8 still runs the paid classifier and the cost reservation before two free, local rejections, so a blocked Conversation burns classifier budget and the NFR-9 fast gate cannot see the checks behind the scan (§ FR-8 lines 276–286; FR-27 line 670; NFR-9 line 791; deferred M-6)
The order is context (5), reservation (6), safety scan (7), Eligible Approver resolution (8), membership (9). The Conversation Agent State "is authoritative the moment it is written" — a local read, free, and decisive. It is checked ninth, after the context load, the reservation, and the classifier call. A Participant in a blocked Conversation spends a classifier call per attempt up to the rate limit, and the `RemovedInConversations` and `NoEligibleApprover` rejections that NFR-9 counts as "require no content safety classifier call" occur after the classifier call, so their p95 ≤ 2 s gate is unmeasurable.
Fix: Split step 9: "2a. Conversation Agent State (`Blocked`, `ExternallyRemoved`) — local read" immediately after lifecycle; move Eligible Approver resolution (8) before the reservation (6); leave the roster-dependent join last.

**[Adversarial]** — AM-6 SM-4's completeness rule demands a recorded denial for FR-33 rows that are not callable operations, so the gate is permanently `InsufficientEvidence` or the telemetry is staged (§ §12 line 939; FR-33 rows 470, 471, 474)
"SM-4 is `InsufficientEvidence` unless every FR-33 row has at least one recorded denial in qualification telemetry". "Bind the production payload-protection engine in the host composition" is a deployment act; "Confirm an SM-4 event" and "Convene and record the kill-switch trigger review" are human process rows. No runtime can record a denial of them. A tester who needs READY records a synthetic denial, and the security gate's evidence is now partly staged.
Fix: "The SM-4 completeness rule applies to every FR-33 row that is a callable operation on the §10 public surface; the PRD lists the exempt rows (bind engine, confirm SM-4 event, convene review, Security approval), whose evidence is the recorded artefact each row names."

**[Adversarial]** — AM-7 A permanently refused mirror makes a block unclearable and `MirrorPending` eternal; whether the Agents Service Principal may remove a participant is not in seam 1 (§ FR-2 lines 164–165; §8 seam 1 line 817; A-1)
"the mirror is retried until it succeeds" and "A clear is refused with a typed reason while a mirror entry is sent and unacknowledged". Seam 1 does not commit that Conversations authorizes the Agents Service Principal to remove a participant. If Conversations refuses (`MembershipRejected` is a named outcome for exactly this principal), the entry is re-sent forever, every clear finds an entry "sent and unacknowledged", and the Facilitator who set the block can never clear it. The block still works, but the clear row in FR-33 is dead and `MirrorPending` is not a transient state.
Fix: "A typed permanent refusal confirms the mirror as `MirrorRefused`, visible under FR-25 and counted as an unavailable-call signal; only a transient failure is retried, each attempt with a timeout, and a clear is refused only while an attempt is in flight." Add the removal authorization to A-1's text.

**[Adversarial]** — AM-8 The computed subject set makes the Tenant Agent Administrator branch dead in single-Administrator tenants, and the PRD and Spine now differ on Administrator turnover (§ FR-24 line 590; FR-33 line 476; Spine AD-22)
"plus the Tenant Agent Administrator whose configuration version was in force for any call in scope. The second party is the Tenant Agent Administrator when outside that set". In the common tenant with one Administrator, every call ran under that Administrator's configuration, so the Administrator is always a subject and the "common case" second party is always the Platform Operator or a second Inspector — which, with the 30-day anti-reciprocity rule and two Inspectors, means the Platform Operator alone after the first pair. Fail-closed, but the design intent is defeated silently. The Spine resolves it with "the current Administrator when not one of the historical principals recorded in the subject set"; the PRD's text does not say "principal" or "historical".
Fix: Adopt the Spine's wording: "the subject set holds the specific Administrator principal(s) whose configuration version was in force; the second party is the current Tenant Agent Administrator when that principal is not in the set". State the consequence for single-Administrator tenants explicitly.

**[Adversarial]** — AM-9 Agent Instructions are declared not protected content, retained for the life of the tenant plus 365 days, exempt from erasure and approved deletion, and FR-21 still calls "the content fields of Audit Evidence" content-bearing (§ FR-34 line 731; §9 lines 881–882; FR-1 line 139; FR-21 line 526; FR-31 line 648)
"Agent Instructions and configuration audit are not protected content ... so `hexa` can be configured before the engine is `Available`". Tenants will write instructions like "never mention the pending acquisition of X" or paste customer lists. FR-1 records "prior value ... and new value" of every instruction change; §9 keeps them "for the life of the tenant plus 365 days"; approved deletion "erases or redacts protected sensitive payloads" — instructions survive it. A tenant that offboards cannot have its instructions erased for a year. Separately, FR-21 line 526 lists "the content fields of Audit Evidence" as content-bearing while FR-34 says configuration audit — which carries instruction values — is not; an implementer picks one.
Fix: Either protect instructions under an Agent-level key with erasure on tenant offboarding, or keep them unprotected and add to §9: "Agent Instructions and their audit history are erased on tenant offboarding or on approved deletion naming the Agent; this exemption from interaction-key protection is a Governance decision (OQ-n) with owner and revisit date." Reconcile FR-21's phrase to "the content fields of interaction Audit Evidence".

**[Adversarial]** — AM-10 Any Participant with write access can switch `hexa` off in a Conversation by posting one policy-failing message, the block is attributed to the caller, and the offending message and its author are not recorded (§ FR-27 line 669; FR-26 line 642; OQ-18; SM-C4 line 957)
"A Conversation whose scanned history fails the active policy stays blocked for generation until that history has been re-evaluated under a policy it passes ... a Participant may edit or delete the message there". Paste a string that looks like a credential, and `hexa` is unavailable in that Conversation for everyone; each attempt records a Content Safety Policy blocked call against the caller. The Facilitator cannot see which message failed or who posted it, because the audit outcome records the verdict, not the message hash or author. OQ-18 defers the remedy; the weaponisation is not deferred.
Fix: "A history verdict failure records, as operator-visible Audit Evidence, the keyed hash, author Party, and timestamp of each failing message so a Facilitator can act in Conversations; FR-25 attributes the block to the Conversation, not to the calling Party, for SM-C4 tuple counting."

**[Adversarial]** — AM-11 FR-13's "Conversation status entry" is still not an `EXT-CONV-UI-1` artifact, so an `Available` entry does not deliver FR-13 (§ FR-13 line 359; §8 line 824; register EXT-CONV-UI-1 RequiredArtifact; deferred M-13)
"V1 uses in-product pending-proposal visibility ... including a pending count, a queue, and a Conversation status entry." The register's three artifact kinds are the action contribution, the per-message decoration slot, and `GetCallabilityAsync`. A Conversation-level status entry is a Conversations-owned surface with no seam; the register can reach `Available` and FR-13 remains unbuildable, invisible to FR-21's gate.
Fix: Either add the status entry to `EXT-CONV-UI-1` (a fourth artifact kind, correct-course) and say so in §8, or move the entry to the Agents admin surface and delete "Conversation" from FR-13.

**[Adversarial]** — AM-12 Segregation of duties is Party-granular and a predefined Party may be an organization, so the "second Approver" can be the caller acting for the organization (§ §3 lines 103, 108; FR-7 lines 248–251; deferred M-1)
"Party - A stable Hexalith.Parties identity representing a human, organization, or AI participant"; the Eligible Approver predicate excludes "the caller" and "the Party that last edited". A policy naming two predefined Parties passes configuration; one is an organization Party; the human who calls `hexa` is the same human who acts for the organization Party; the organization Party is not the caller, so the human approves their own call under a different identity. No rule restricts predefined Parties or Facilitators to human Parties.
Fix: "Every Approver Policy source resolves to human Parties only; an organization or AI Party named as a predefined Party is a typed configuration rejection, and a Facilitator that resolves to a non-human Party contributes no Approver." Record who acts for an organization Party as out of V1.

**[Adversarial]** — AM-13 Unsafe human edits are stored, inspectable, and retained unscanned until an approval is attempted (§ FR-15 lines 379–382; FR-27 line 667; FR-10 line 312; deferred M-2)
"The exact version being approved passes the then-current active policy at approval time." Nothing scans at edit time. An Approver can store any text as a proposal version — CSAM-category text, credentials, cross-tenant data — as a normal `Edited` version: retained 365 days, visible to every Eligible Approver and to compliance inspection, never flagged, never routed to FR-10's "separate, non-approvable failure record". The proposal store becomes a durable dump for always-blocked content.
Fix: "An edit is scanned before it is recorded; a version that fails the always-blocked categories is rejected with a typed reason and not stored; a version that fails a restricted category is stored with a `SafetyFailed` marker, is not approvable, and is visible on FR-25." Keep the approval-time re-scan for policy drift.

**[Adversarial]** — AM-14 "Fresh and available" for the FR-7 secondary inputs has no staleness bound, so a projection that is a day behind is "fresh" if the implementer says so (§ FR-7 line 253; FR-29 line 569; FR-33 line 489)
"Every secondary input to the Eligible Approver predicate — the tenant-role projection and the per-Party access check — must report fresh and available, or the pass is unavailable as a whole." FR-29 exposes "the projection version and freshness they were served at" but no requirement says what freshness is acceptable. A projection whose last update predates the roster read by 24 hours resolves a role holder who was removed yesterday; the second consecutive empty pass then abandons on the same stale evidence.
Fix: "Fresh means the projection's recorded position is not older than one re-check cadence (A-20) at the moment of the pass; an older position is unavailable."

**[Adversarial]** — AM-15 The FR-34 attestation runs at startup and on readiness evaluation only, so an engine replaced between evaluations runs every content path unattested (§ FR-34 lines 727–728)
"The attestation runs at startup and on every readiness evaluation". A host reconfiguration (or a dependency-injection scope that resolves a different protector after a module reload) swaps the engine mid-run; the blocker was cleared at 09:00 and nothing re-evaluates until an operator opens the readiness surface. "Every content-bearing runtime path ... fails closed ... while the blocker stands" — and the blocker does not stand.
Fix: "The attested engine identity is pinned at attestation; every seal/unseal call verifies the engine instance identity against the pin and fails closed on mismatch; the attestation re-runs on a bounded cadence (default hourly) and on any host composition change."

**[Consistency-internal]** — CI-1 Safety re-check policy stated two ways (L-I3 verdict: open, real) (§ FR-26 640 ↔ FR-26 644; FR-17 405, FR-18 421/442, FR-27 667, OQ-9 979)
Line 640: the approval-time and pre-post re-checks "always evaluate the then-current active policy"; FR-17, FR-18 rows 421 and 442, FR-27 667, and OQ-9 say "then-current" too. Line 644 of the same FR: "each retry, approval-time check, and pre-post check uses the more restrictive of the initial attempt's policy and the then-current active policy. A re-check can therefore only tighten the outcome, never loosen it." When the platform policy is loosened between attempt and approval the two rules give different answers. Spine AD-20 implements 644 with `LastLoosensVersion` ordering, so the downstream authority has already picked the "never weaker" reading.
Fix: PRD. Keep 644 as the rule; reword 640 to "always evaluate the then-current active policy, never weaker than the attempt's snapshot policy (below)", and add "(never weaker than the attempt's snapshot policy, FR-26)" after "then-current" at 405, 421, 442, 667, and in OQ-9. This closes baseline L-I3 rather than deferring it again.

**[Consistency-internal]** — CI-2 Party link/replace commands are "on the FR-23 deprecate-and-reject register" but FR-23's register enumeration omits them (§ FR-2 148 ↔ FR-23 557; OQ-25 995)
FR-2: "the shipped Party link and replace commands (Spine AD-7) are on the FR-23 deprecate-and-reject register, and a link or replace naming any Party is a typed rejection". FR-23's closed register lists the two `CostControlPosture` values, `ContentSafetyFailureHandling.BlockWithAuditableOverride`, and `ApproverPolicySourceKind.Caller` only. A consumer reading the closed FR-23 list treats link/replace as accepted.
Fix: PRD FR-23 557: add "and the Agent Party link and replace commands (FR-2; provisioning is the only identity path)".

**[Consistency-internal]** — CI-3 Administrative retry of a `PostingFailed` proposal: table allows it at any time, FR-33 only after the budget is exhausted (§ FR-18 433 ↔ FR-33 478, FR-18 439)
Table row 433 names the Tenant Agent Administrator (audited administrative retry) with no budget condition on the administrative actor. FR-33 478: "Retry a `PostingFailed` proposal administratively after its retry budget is exhausted (FR-18)"; 439 lists the administrative retry among the exits "When the retry budget is exhausted".
Fix: PRD table row 433 guard: add "administrative retry only after the automatic budget is exhausted (FR-33)".

**[Consistency-internal]** — CI-4 `ExpiredWhileSuspended` is recorded on two different conditions (§ FR-18 424 ↔ FR-28 698, OQ-27 997)
Table row 424: "when the awaiting window overlapped a `Suspended` period" (any overlap, even if released before expiry). FR-28: "a proposal that expires during the suspension is recorded `ExpiredWhileSuspended`"; OQ-27 same; Spine AD-12 "expiry during suspension records `ExpiredWhileSuspended`". The overlap reading removes more proposals from the SM-3 and SM-C5 denominators than the expiry-instant reading.
Fix: PRD row 424: "when `ExpiresAt` passed while the tenant was `Suspended`".

**[Consistency-internal]** — CI-5 Two settlement instants for a held `Indeterminate` reservation (§ FR-28 692, both sentences; OQ-6 976)
"a reservation whose outcome is `Indeterminate` is held for a bounded period — default 24 hours … — and then conservatively settled at the estimated maximum" versus, four sentences later, "A reservation past its hold deadline with no authoritative outcome becomes `Unreconciled`: it stays counted against the cap and is settled only by an audited operator command or at period close at the estimated maximum". FR-12 342 makes every undeterminable outcome resolve to the typed `Indeterminate`, so "no authoritative outcome" past the hold deadline is either unreachable or the same case settled at a different time. Spine AD-21 carries the same two sentences.
Fix: PRD FR-28 692: state which case becomes `Unreconciled` (recommended: an `Indeterminate` reservation whose hold deadline passes without an operator settlement becomes `Unreconciled` and is settled at period close; delete "and then conservatively settled at the estimated maximum" from the first sentence and align OQ-6), then mirror in Spine AD-21 / ARCH-A-6.

**[Consistency-internal]** — CI-6 The FR-7 re-check's `NoEligibleApprover` abandonment is stated over `PostingFailed` proposals but FR-18 has no such transition (§ FR-7 252/255 ↔ FR-18 table 429–431, 435, 439)
FR-7 252: "A scheduled re-check runs over every awaiting proposal and every `PostingFailed` proposal"; 255: "The re-check moves a proposal to `Abandoned` with reason `NoEligibleApprover` only after two consecutive passes". FR-18 415: "any other (from, to) pair is rejected"; the only system rows out of `PostingFailed` into `Abandoned` are 429 (`RemovedInConversations`) and 430 (`SourceConversationUnavailable`).
Fix: PRD FR-7 255: "moves an awaiting proposal to `Abandoned` …"; add "a `PostingFailed` proposal receives the `ResolutionEmptyPending` marker only" so the marker rule and the table agree.

**[Consistency-internal]** — CI-7 `ContextUnavailable` is counted but never produced (§ FR-25 610, SM-C4 959 ↔ FR-9 301, 302)
FR-25 and SM-C4 count unavailable calls by reason including `ContextUnavailable`. FR-9 302 only says "If the complete Conversation Context cannot be loaded or sent safely, the Agent Call fails closed" with no typed reason, and 301 records the context mode as `Blocked` "with a typed block reason" — so an unavailable Conversations read is indistinguishable from a policy block, which SM-C4 counts as a blocked call. Spine AD-15 and epics Story 6.2/6.8 already carry `ContextUnavailable` as a public value.
Fix: PRD FR-9 302: "… fails closed with the typed reason `ContextUnavailable`, which FR-25 and SM-C4 count as an unavailable call, not a blocked one; the recorded context mode is `Blocked` with that reason".

**[Consistency-internal]** — CI-8 `PostingFailed` retry clock while the Agent is `Disabled` is unstated, and the Spine has had to assume it (§ FR-3 176, FR-18 433/441 ↔ Spine AD-12 189, ARCH-A-11)
FR-18 441 pauses the retry-window clock "While the tenant kill switch is pulled"; row 433 blocks retries while the Agent is not `Active`; FR-3 176 says what a `Disabled` Agent's awaiting and `Approved` proposals do but nothing about `PostingFailed`. With a 15-minute window (A-7) a disable/re-enable cycle silently exhausts the budget. Spine AD-12 now states the pause under `[ASSUMPTION ARCH-A-11]` and its table row asks "Product confirms this rule".
Fix: PRD FR-18 441 and FR-3 176: "The same pause and suspension apply while the Agent is `Disabled`" — and record the confirmation so ARCH-A-11 can retire.

**[Consistency-internal]** — CI-9 FR-28 assigns recorded decisions to "Release PM" and "Product", neither of which FR-33 maps (§ FR-28 685, 688, 701 ↔ FR-33 453)
FR-33: "Every authorization rule in this PRD resolves to one of six Agents roles at a stated scope". FR-28 item 6: "a dependency may be excluded only by a recorded Release PM decision …, and that exclusion is itself an `RQ-1` blocker until Product accepts it"; item 9: "blocks only once the Release PM records an acknowledgement"; 701: "Product records a disable-or-continue decision". §8.1 840 says a rule granting a role an operation absent from FR-33 is "a PRD amendment pending".
Fix: PRD FR-33: either add a note that Release PM and Product are planning parties whose decisions are recorded by the Release Operator in the launch readiness register (and say so at the three FR-28 sites), or add matrix rows for the three decisions.

**[Consistency-internal]** — CI-10 The `Approved`-origin abandonment guard names a `MessageId` lookup the lookup rule never performs from `Approved` (§ FR-18 429 ↔ FR-18 440)
Row 429: "`Approved`, `PostingFailed` → `Abandoned` (`RemovedInConversations`) | System | Removal or block detected (FR-2), and the `MessageId` lookup finds no posted message". 440 scopes the lookup to "Before every retry attempt and before any exit from `PostingFailed`" and skips it when no post was attempted; it says nothing about an unavailable lookup against an `Approved` proposal. Spine AD-5 had to fill the gap.
Fix: PRD FR-18 440: extend the lookup rule to "and before the system's `Approved` → `Abandoned` (`RemovedInConversations`) transition; an unavailable answer leaves the proposal in `Approved`".

**[Consistency-cross-artifact]** — CX-7 The PRD asserts a non-conformance record exists for work completed against an `Uncommitted` entry; the register records an open Product approval and neither branch (§ §8 814, FR-21 523, OQ-17 987 ↔ external-dependency-register.md 185–194)
PRD §8: "any story already completed against an `Uncommitted` entry carries a non-conformance record in the register"; OQ-17 says the same. Register "Known Consumer Non-Conformance": "Until Product selects one evidence-backed branch, neither disposition is recorded … This open approval condition is blocking provenance, not a fabricated non-conformance finding". Baseline M-X8 is therefore replaced rather than closed: the section now exists, the record does not.
Fix: PRD §8 814: "the register carries an open Product ruling on whether Story 5.3 consumed `EXT-PROVIDER-1` (Branch A records the non-conformance; Branch B records none)". Register: Product selects the branch (proposal §6 open item).

**[Consistency-cross-artifact]** — CX-8 Spine AD-2's first paragraph contradicts FR-2 (and the Spine's own correction paragraph) on what sets `MirrorPending`, when `ReadmitPending` exits, and when `BlockVersion` increments (§ FR-2 154, 163–165 ↔ ARCHITECTURE-SPINE.md AD-2 125 vs 127, AD-7 159)
AD-2 125: "a `MirrorPending` flag set whenever a block, clear, or removal record is written … `ReadmitPending` is entered on clear and holds until the Conversations mirror confirms re-admission … `BlockVersion` — incremented on every set or clear". PRD: `ExternallyRemoved` is recorded "with no mirror because there is nothing to remove"; clearing "records `ReadmitPending`, and `hexa` re-joins at the membership step of the next Agent Call that reaches it"; only setting increments `BlockVersion`. The Spine's own correction paragraph (127) and AD-7 159 state the PRD rule and declare themselves normative, so the Spine is self-contradictory on the page a developer reads first.
Fix: Spine AD-2 125: replace the `ConversationAgentState` clause with the 127 wording. PRD-side change: none.

**[Consistency-cross-artifact]** — CX-9 Register seam 1 lacks the typed "already absent" removal answer that FR-2's mirror confirmation depends on (§ §8 817, FR-2 164 ↔ external-dependency-register.md 57, 60)
PRD seam 1: "a participant removal for the FR-2 block, the removal answering typed 'already absent' as a confirmed no-op"; FR-2: "A seam answer that `hexa` is already absent confirms the mirror". Register seam (1) names a participant removal limited to the AI participant with no typed already-absent answer; contract 60 names "AI-participant removal" only. Spine AD-7 relies on it.
Fix: Register seam (1) and contract 60: "… a participant removal limited to the AI participant that answers a typed already-absent as a confirmed no-op". PRD-side change: none.

**[Consistency-cross-artifact]** — CX-10 Epics requirements-inventory rows outside the correct-course rewrite set still summarise the pre-2026-09-09 FRs (§ FR-4 186–201, FR-5 209, FR-12 334, FR-22 542, FR-25 604–612, FR-27 658–670 ↔ epics.md 44, 46, 60, 80, 86, 90)
FR4 row: "Authorized administrators can configure the Global Providers Aggregate" (PRD: the Platform Operator; pricing, limits, retry budget, data-handling record, `CapabilityVersion` absent). FR5 row omits the selection-eligibility set incl. tenant enablement and `DataHandlingVersion` acceptance. FR12 row omits the kill switch and the block/`ExternallyRemoved`. FR22 row lacks `Draft`, `Suspended`, and invalid configuration. FR25 row omits every second-update exposure. FR27 row omits the approval-time and pre-post scan and the verdict cache. The proposal scoped the rewrite to FR1/7/18/21/26, so this is residual drift, not a missed action; §0 18 still makes the epic set "the single executable backlog".
Fix: epics inventory rows FR4, FR5, FR12, FR22, FR25, FR27 from PRD 186–201, 209, 334, 542, 604–612, 658–670. PRD-side change: none.

**[Consistency-cross-artifact]** — CX-11 Epics never name the `Draft` lifecycle state or the `MembershipUnavailable` / `MembershipRejected` typed reasons (§ FR-3 171, FR-22 542, FR-2 157/160, FR-25 609–610 ↔ epics.md (0 hits for each))
PRD FR-3: "The public lifecycle states are `Draft` (provisioned, not yet valid for activation), `Active`, and `Disabled`"; FR-2: an unavailable participant read rejects with `MembershipUnavailable`, and both names "remain posting-failure reasons"; FR-25 exposes them. Epics UX-DR20/UX-DR25 and Stories 5.2/5.7 speak of `active`/disabled only; Story 6.6 says "membership or posting is unavailable" with no typed name, and Story 6.8 does not list them. Spine AD-15 and the sprint proposal's debt table carry them.
Fix: epics: add `Draft` to the FR3 row, UX-DR25, and Story 5.2/5.7 lifecycle ACs; add `MembershipUnavailable` (acceptance rejection and posting-failure reason) and `MembershipRejected` to Story 6.6 or 6.8 owned members. PRD-side change: none.

**[Consistency-cross-artifact]** — CX-12 Epics Story 5.3 owns `DataHandlingVersion` but no story carries the enablement block, the Tenant Agent Administrator acceptance, or the 30-day grace (§ FR-4 199, FR-5 209, FR-33 458–459, OQ-29 ↔ epics.md 1361–1364, 351, 361)
Story 5.3 AC: "the catalog owns and advances `DataHandlingVersion` with the governed capability/pricing record". Nothing in 5.3, 5.5, or 5.7 states "a model whose record is absent or incomplete cannot be enabled for any tenant", the TAA acceptance before first activation and on change, the 30-day continuation, or the FR-24 "record version in force" evidence per Provider attempt.
Fix: epics: add the enablement-gate AC to Story 5.3, the acceptance/grace AC to Story 5.7 (activation), and the per-attempt evidence clause to Story 6.4; cite OQ-29 in each. PRD-side change: none.

**[Rubric: Decision-readiness]** — "Release PM" holds two `RQ-1` decision powers but is not one of the six FR-33 roles and has no glossary entry (§ FR-28 items 6 and 9, lines 685, 688; §8.1 line 838; FR-33 line 453; "Release PM" ×14)
FR-28 item 6: "a dependency may be excluded only by a recorded Release PM decision naming the FR consequences the run will not exercise"; item 9: "blocks only once the Release PM records an acknowledgement of it"; FR-33: "Every authorization rule in this PRD resolves to one of six Agents roles". "Release PM" is defined nowhere in the PRD, the two registers, or the Spine. The Release Operator glossary entry (line 114) covers the `RQ-1` decision but not profile exclusions or late-row acknowledgements, so a story writer cannot say which principal the FR-30 surface accepts these two records from, and the FR-33 matrix has no row for either act.
Fix: Add a glossary entry — "Release PM: the planning-time name of the Release Operator role; the two names denote the same role" — and two FR-33 rows (exclude a dependency from the qualification profile; acknowledge a late `ARCH-A` row), or rename the fourteen uses to Release Operator where they are authorization rules and leave "Release PM" for §13 ownership only.

**[Rubric: Done-ness clarity]** — `ExpiredWhileSuspended` has two definitions, and they move the SM-3 and SM-C5 denominators differently (§ FR-18 table row 424; FR-28 line 698; SM-3 line 947; SM-C5 line 961; OQ-27)
Row 424: "recorded with reason `ExpiredWhileSuspended` when the awaiting window overlapped a `Suspended` period"; FR-28 line 698: "a proposal that expires during the suspension is recorded `ExpiredWhileSuspended`". Under the row-424 reading a one-minute pull removes from the SM-3 and SM-C5 denominators every proposal that was awaiting at that minute and later expired on its own, however long after release; under the FR-28 reading only proposals whose `ExpiresAt` passed while the switch was pulled leave. OQ-27's stated purpose is to stop the switch manufacturing a miss; the overlap reading lets it manufacture a pass. The Spine (AD-12) follows FR-28.
Fix: Reword row 424 to "Stored `ExpiresAt` passed; recorded with reason `ExpiredWhileSuspended` when it passed while the tenant was `Suspended`", matching FR-28.

**[Rubric: Done-ness clarity]** — "More restrictive of two policies" is still not computable — deferred item, still open (§ FR-26 lines 640, 641, 644; FR-33 row 468)
Line 644: "each retry, approval-time check, and pre-post check uses the more restrictive of the initial attempt's policy and the then-current active policy"; line 640 says the same re-checks "always evaluate the then-current active policy" (the consistency reviewer's L-I3). Two policy versions with different category sets are not totally ordered, so an implementer must invent "must pass both", and line 640 and line 644 give two different rules for the same re-check.
Fix: "the content must pass every applicable policy version — the initial attempt's and the then-current active policy, and a mode-specific policy in addition to, never instead of, the platform policy"; delete "more restrictive" and reconcile line 640 to it.

**[Rubric: Done-ness clarity]** — FR-31 blocks the reporting UJ-2 exists for, and drifts from OQ-9 — deferred item, still open (§ FR-31 line 653; OQ-9 line 979; UJ-2 line 56)
FR-31: "asserts a decision, commitment, or approval on a Party's behalf"; OQ-9: "asserts a decision on a Party's behalf"; UJ-2: "needs help interpreting the prior discussion". A summary that says "Anika agreed to ship on Friday" reports a commitment on a Party's behalf and is blocked under FR-31 as written; the category is also wider in the FR than in the binding decision.
Fix: Narrow both to first-person impersonation and content that presents a decision, commitment, or approval as the Party's own act when the Conversation does not record it; permit reporting a Party's recorded position, and align OQ-9.

**[Rubric: Done-ness clarity]** — The Agent Call still has no public state contract — deferred item, still open (§ FR-8 lines 267–286; FR-25 line 600; §10 line 899; src/Hexalith.Agents.Contracts/AgentInteraction/AgentInteractionStatus.cs (29 values); src/Hexalith.Agents.UI/Components/Shared/AgentCallStatus.cs (14))
FR-18 adopts `ProposedAgentReplyState` by name and the shipped code has two different call-status enumerations, the first with 16 `Proposal*` duplicates of the FR-18 states; nothing in the PRD says which one is public, which members are terminal, or whether the `Proposal*` duplicates are a second source of truth for proposal state.
Fix: An FR-8 consequence naming the public contract, its terminal members, and placing the `Proposal*` duplicates on the FR-23 deprecate-and-reject register.

**[Rubric: Done-ness clarity]** — p99 over 30 executions is the sample maximum — deferred item, still open (§ NFR-9 line 791; FR-28 item 3 line 682 and line 693; OQ-5; §11 line 928)
NFR-9: "p99 ≤ 120 seconds ... Each gate requires at least 30 production-like executions"; §11 requires the measurement contract to name "the numerator and denominator or percentile method". At n = 30 the 99th percentile is the single slowest run, so the p99 gate is a max gate with a percentile label and one outlier fails `RQ-1`.
Fix: Gate on p95 at n ≥ 30 and report p99 as launch-health, or require n ≥ 300 for the p99 gates, and name the percentile method in NFR-9.

**[Rubric: Downstream usability]** — The §8.1 Spine-defect list is stale in both directions (§ §8.1 lines 840–844; OQ-26; OQ-27; ARCHITECTURE-SPINE.md AD-5, AD-12, AD-13, AD-22)
§8.1 names three open Spine defects owned by Architecture (AD-5 forbids the system abandonment from `Approved` and `PostingFailed`; AD-22 names the Tenant Agent Administrator as the sole inspection second party; AD-12 states the Release Operator trigger without the review, minimum sample, and `InsufficientEvidence` rule). The Spine has absorbed all three. Meanwhile the Spine's AD-13 rule states "Eligible Approver resolution runs in every response mode, not Confirmation mode only — an Automatic-mode Agent with zero configured Approvers is rejected up front with `NoEligibleApprover`", which contradicts FR-8 step 8 ("where Confirmation Response Mode applies", line 284) and FR-7, and is listed nowhere. Under the PRD's own rule the PRD governs, but the paragraph that tells a story writer where the two disagree is wrong on all four counts.
Fix: Replace lines 840–844 with the current state: AD-5, AD-12, AD-22 reconciled on 2026-09-09; one open divergence — AD-13's Automatic-mode Eligible Approver requirement versus FR-8 step 8 — either adopted into FR-8 as a Product decision (it is a real safety choice: an Automatic-mode tenant with no Approver has no human who could ever be resolved) or recorded as a Spine defect. Drop the "for Architecture to correct" clauses from OQ-26 and OQ-27.

**[Drift]** — DM-1 Baseline additive members still missing (§ FR-9 line 301, FR-7 lines 249, 254, FR-2 lines 154-160, FR-8 line 286, FR-28 lines 690, 695, FR-3 line 171 ↔ AgentInteractionContextMode.cs:17-26; AgentInteractionGateCheck.cs:23-53; AgentLaunchReadinessBlocker.cs:22-46)
PRD declares these additive public values; whole-word grep finds none of `AgentInteractionContextMode.Blocked`, `NoEligibleApprover`, `RemovedInConversations`, `SourceConversationUnavailable`, `NotInvoked`, a `Membership` gate check, `ProhibitedCostControlPosture`, `PayloadProtectionUnavailable`, or a `Suspended` status value. Kind: gap (additive). (Baseline debt item 4; `Indeterminate` moved to DH-8, the budget terms to DH-7.)
Fix: code.

**[Drift]** — DM-2 Second-update vocabulary has no code footprint (§ FR-7 lines 252-257, FR-18 lines 424, 432, 440, FR-25 lines 608-610, FR-28 lines 690, 700, FR-30 line 621, §8 seams 2 and 5 ↔ 0 whole-word hits in src/ + test/; AgentLaunchReadinessView.cs:27-33)
`ExpiredWhileSuspended`, `ResolutionEmptyPending`, `ResolutionUnavailable`, `ApproverResolutionUnavailable`, `LateConfirmed`, `MirrorPending`, `BlockVersion`, `DataHandlingVersion`, `GateOutOfScope`, `TriggerReviewOverdue`, `DependencyNotAvailable`, `ConversationDeleted`, `PrincipalRemovedFromConversation`, `Unreconciled`, `DeletionDeferredByHold`; the scheduled FR-7 re-check job itself does not exist; the FR-30 surface has no field for `GateOutOfScope` or `TriggerReviewOverdue`. The update report lists these as expected code gaps; none has been started. Kind: gap (additive).
Fix: code.

**[Drift]** — DM-3 `ContextUnavailable` is a blocked-call reason that also absorbs authorization denials (§ FR-25 line 610, SM-C4 lines 956-960 ↔ AgentInteractionContextBlockReason.cs:11, 22; AgentInteractionContextPolicy.cs:60-64; AgentInteractionStatus.cs:39)
PRD classifies `ContextUnavailable` as an unavailable call, "neither blocked nor failed calls for SM-C4", and reports authorization denials separately; code records it as `ContextBlocked(ContextUnavailable)` and the enum comment says it "covers both the unauthorized and the unavailable load outcomes". Under the shipped classification an outage or a denial inflates the SM-C4 blocked-call share that feeds the kill-switch trigger review. Kind: drift (classification).
Fix: code — split unauthorized from unavailable and report the latter as an unavailable call; or PRD — acknowledge the coarse reason as transitional.

**[Drift]** — DM-4 Provider data-handling record, `DataHandlingVersion`, tenant acceptance, and per-tenant Provider enablement are absent from the catalog and from activation eligibility (§ FR-4 line 199, FR-5 line 209, FR-33 rows lines 458-459, FR-24 line 596, OQ-29 ↔ ProviderCatalogAggregate.cs; AgentActivationProviderRevalidation.cs; 0 hits for `DataHandling`, `EnabledForTenant`, `TenantEnablement`)
PRD says "A model whose data-handling record is absent or incomplete cannot be enabled for any tenant" and activation validates "its data-handling record is complete and its current `DataHandlingVersion` accepted for this tenant"; code has neither the record nor a tenant enablement dimension, so activation succeeds without them. Kind: gap (additive; governance on data leaving the platform).
Fix: code.

**[Drift]** — DM-5 Approver Policy structural rule at configuration time and Conversation-scoped resolution at call time are absent (§ FR-7 lines 245, 248-249 ↔ AgentAggregate.cs:729-790; AgentInteractionGateOrchestrator.cs:242-264; IApproverPolicyResolver.cs:29)
PRD says configuration "rejects a policy that names no Conversation-dependent source … and names fewer than two predefined Parties" and call time "resolves the Eligible Approver set for the proposal before any Provider work and rejects the call with the typed reason `NoEligibleApprover` when it is empty"; code validates only shape and duplicates, and the gate's `ResolveApproverPolicyAsync` calls `ResolveAsync(tenantId, policy, ct)` with no Conversation identifier, so it can only judge whether the sources resolve tenant-wide. Kind: gap.
Fix: code.

**[Drift]** — DM-6 No regeneration ceiling (§ FR-16 line 393, FR-32 line 717, A-6 ↔ AgentInteractionProposalRegenerationOrchestrator.cs; AgentInteractionAggregate.cs:429-480; 0 hits for `RegenerationCeiling`)
PRD says "exceeding it is a typed rejection. The ceiling defaults to 3 and is configurable per Agent from 1 through 10"; code counts nothing and regenerates without bound. Kind: gap (cost bound).
Fix: code.

**[Drift]** — DM-7 Pricing version: a reused version is accepted, and a caller-supplied version is silently replaced (§ FR-4 line 195 ↔ ProviderCatalogAggregate.cs:492-499, 505-516)
PRD says "a pricing version that starts at 1 on create and strictly increases … a decreased or reused pricing version is a typed rejection, not a silent acceptance"; code rejects only `PricingVersion < current` (and `0` is always accepted as "unset"), then `AssignPricing` discards the supplied value and assigns `current + 1` only when a unit price or currency changed — so a request that reuses the current version with changed prices is accepted and re-versioned, and a request that supplies `current + 5` is accepted as `current + 1`. The server-assigned semantics are internally sound; the PRD's "reused … typed rejection" clause is what does not hold. Kind: drift (partial). (Replaces the baseline's pricing-version item.)
Fix: PRD — say the version is server-assigned, bumps on any unit-price or currency change, and that only a supplied version below the stored one is rejected; or code — reject an equal supplied version when units change.

**[Drift]** — DM-8 FR-25 counters and markers have no read-model fields (§ FR-25 lines 607-612, SM-C4 line 960 ↔ AgentStatusView.cs:52-71)
PRD says status "exposes the per-tenant count of Agent Calls blocked, by each SM-C4 blocked-call reason, together with the count of system-abandoned proposals by reason", per-proposal markers, and cost-cap consumption; the shipped status view carries lifecycle, configuration versions, and boolean presence flags only. Kind: gap (additive; the kill-switch trigger review in FR-28 consumes these counts).
Fix: code.

### Low (40)
**[Adversarial]** — AL-1 A-9's text omits the `ExternallyRemoved` clearing authority that FR-33 cites it for (§ A-9 line 858; FR-33 line 465; FR-2 line 165)
A-9 says "a block is cleared only by the setting authority or the Tenant Agent Administrator"; FR-33's clear row adds "any current Conversation Facilitator" for `ExternallyRemoved` under the same key. Product confirming A-9 as worded does not confirm the Facilitator clear.
Fix: Extend A-9's text to both rules.

**[Adversarial]** — AL-2 Minimum-sample discontinuity at five Parties (§ FR-28 line 700)
A 4-Party tenant is evaluated at 20 calls; a 5-Party tenant needs 50. A 5-Party tenant at 40 calls, all failing, is `InsufficientEvidence`; a 4-Party tenant at 20 calls triggers.
Fix: Scale linearly (10 calls per calling Party, minimum 20, maximum 50).

**[Adversarial]** — AL-3 Posting-failure rate can exceed 100% and miss cross-window failures (§ FR-28 line 700)
Numerator: "entered `PostingFailed` at least once in the window"; denominator: "entered `Approved` in it". A proposal approved last window and failing this one is in the numerator only.
Fix: Use one cohort: proposals that entered `Approved` in the window, with failures attributed to that cohort whenever they occur within the retry bound.

**[Adversarial]** — AL-4 "Business day" is undefined (§ FR-28 line 700; FR-33 line 471)
Timezone and calendar are the Release Operator's choice.
Fix: "One business day = 24 hours excluding Saturday and Sunday in UTC."

**[Adversarial]** — AL-5 The cost-cap override "is itself bounded" with no bound (§ FR-32 line 719; FR-33 line 467)
"records actor, justification, scope, and expiry, and is itself bounded" names no maximum amount or duration; the Platform Operator can override a cap by any amount for any period.
Fix: "bounded to at most 25% of the monthly cap and 7 days per override; a second override in the same month requires Product's recorded approval."

**[Adversarial]** — AL-6 FR-7's two-pass `NoEligibleApprover` abandonment is not scoped to awaiting states while the sweep covers `PostingFailed`; the table restricts it (§ FR-7 lines 252, 255; FR-18 rows 423, 430–431)
An implementer following FR-7 abandons a `PostingFailed` proposal for an empty roster; one following the table does not.
Fix: Add "for awaiting proposals" to FR-7's `NoEligibleApprover` bullet.

**[Adversarial]** — AL-7 Regeneration re-runs FR-9 and FR-27 but not the FR-8 Eligible Approver resolution or membership step (§ FR-16 line 394; FR-8 steps 8–9)
A regeneration can invoke the Provider for a Conversation `hexa` was removed from before the removal is observed.
Fix: "A regeneration re-runs FR-8 steps 2 through 9."

**[Adversarial]** — AL-8 A block "set from `NeverJoined` is confirmed without a seam call" although `hexa` may already be present through a direct add in Conversations (§ FR-2 line 164)
The mirror is confirmed while `hexa` sits in the roster; "removal is visible to Participants" is false for that Conversation.
Fix: "confirmed without a seam call when the participant read confirms `hexa` absent; otherwise mirrored."

**[Adversarial]** — AL-9 Policy publication and HMAC secret rotation invalidate every cached verdict tenant- or platform-wide with no rescan budget (§ FR-27 line 668; FR-26 line 640)
The next call in every Conversation re-scans its full history; NFR-9's confirmation latency and the classifier budget absorb the spike with no stated bound.
Fix: State a background re-scan on publication with a per-tenant concurrency bound, and that a call during the re-scan waits or fails closed with `ContextUnavailable`, never posts under a stale verdict.

**[Adversarial]** — AL-10 A caller rejected `NoEligibleApprover` still gets no remediation hint (§ FR-33 line 479; FR-7 line 259; deferred M-10)
"a caller sees the outcome of their own Agent Calls only"; the outcome is a reason code.
Fix: Return a disclosure-category-safe hint: "no Facilitator in this Conversation" or "the configured Approvers are not Participants here".

**[Adversarial]** — AL-11 FR-2 line 158's two negative sentences are history, not requirement (§ FR-2 line 158; deferred gate L-7)
"a step that treats absence after a cleared block as a fresh removal does not satisfy it either" names the pre-fix bug.
Fix: Replace both with the AH-2 negative tests; move the history to the memlog.

**[Adversarial]** — AL-12 Carried lows still open from the validate signal (§ deferred gate L-10; FR-16 line 394; FR-27 line 668; NFR-9 line 791; SM-3 line 947; FR-33 line 453; FR-26 line 634; NFR-13 line 795; UJ-1 line 50)
L-1 (the regenerating Approver is not "the caller"), L-4 (tautological reused-verdict version record), L-5 (approval-to-post includes a classifier call with no separate budget), L-6 (SM-3 "the Agent's configured expiry" — at creation or measurement), L-7 ("Security approval" has no actor or record), L-9 (NFR-13 "required context" untestable), L-11 (carried predecessor lows), plus gate L-2 (UJ-1 "enables" vs FR-3 "activate"). Validate L-8 (retention of failure records and configuration audit) is closed by §9 line 881.
Fix: None stated as a single change; each carried low keeps the fix recorded at the run that raised it.

**[Adversarial]** — AL-13 A-21 can be retired by relocating the check to Agents with no FR requiring Agents to perform it (§ A-21 line 870; FR-2)
"or the register records that Agents must verify it before membership" retires the row while no FR-2 consequence lands.
Fix: Add the conditional consequence to FR-2 now: "Where the register assigns the check to Agents, the membership step reads the Party type from Parties and fails closed on a non-AI Party."

**[Consistency-internal]** — CI-11 A-9 index row lags the FR-2/FR-33 clear rule it keys (§ §8.1 858 ↔ FR-2 165, FR-33 465)
Row: "a block is cleared only by the setting authority or the Tenant Agent Administrator"; the tagged text adds "An `ExternallyRemoved` record is cleared by the Tenant Agent Administrator or by any current Conversation Facilitator". Baseline M-I2 asked for the row to be extended; the body was, the row was not.
Fix: PRD A-9 row: append "; an `ExternallyRemoved` record by the Tenant Agent Administrator or any current Facilitator".

**[Consistency-internal]** — CI-12 §6.1 still lists "Agent Party identity provisioning or linking through Hexalith.Parties" (§ §6.1 752 ↔ FR-1 132, FR-2 148)
Linking is retired to deprecate-and-reject.
Fix: PRD §6.1: "Agent Party identity provisioning through Hexalith.Parties at tenant enablement".

**[Consistency-internal]** — CI-13 FR-12 names the Agents-owned block but not `ExternallyRemoved`, which FR-11 names (§ FR-12 334 ↔ FR-11 326)
FR-11: "does not post while the Conversation is `Blocked` or `ExternallyRemoved`"; FR-12: "the tenant kill switch, the Agents-owned block (FR-2), …".
Fix: PRD FR-12 334: "the Agents-owned block or `ExternallyRemoved` record (FR-2)".

**[Consistency-internal]** — CI-14 FR-22 lists "failed-call" among "proposal states" (§ FR-22 542 ↔ FR-18 410)
"the awaiting-decision, posting-failed, failed-call, and expired proposal states"; a failed call is an Agent Call outcome (FR-10), not a `ProposedAgentReplyState`.
Fix: PRD FR-22: "the awaiting-decision, posting-failed, and expired proposal states and the failed-call outcome".

**[Consistency-internal]** — CI-15 OQ-22 narrows the assumption owners that block `RQ-1` (§ OQ-22 992 ↔ FR-28 688, OQ-24 994, §8.1 836)
OQ-22: "Unretired Product, Architecture, or Governance assumptions in §8.1 … block `RQ-1`"; FR-28 item 9 and OQ-24: "whatever its owner" (A-1, A-2, A-15, A-16, A-18, A-21 are owned by the Conversations or EventStore Maintainer).
Fix: PRD OQ-22: "Every unretired §8.1 assumption, whatever its owner, and …".

**[Consistency-internal]** — CI-16 `GateOutOfScope` and `TriggerReviewOverdue` are classified neither as register vocabulary nor as `AgentLaunchReadinessBlocker` values (§ FR-28 690 ↔ FR-30 621, FR-28 700)
690 classifies four codes as register vocabulary and two as additive blocker values; 621 and 700 say the other two are "recorded … on the FR-30 surface". The register (lines 51–52, 79–80) treats both as `BlockerCode` values.
Fix: PRD FR-28 690: add "`GateOutOfScope` and `TriggerReviewOverdue` are register vocabulary that the FR-30 surface mirrors".

**[Consistency-internal]** — CI-17 The tenant-role calling restriction (A-11) is a Tenant Agent Administrator setting that no configuration list carries (§ FR-33 462 ↔ FR-33 461, §3 83, §10 896)
462 grants "unless the Tenant Agent Administrator restricts calling to a tenant role"; the "Configure `hexa`" row 461, the glossary Agent Administrator entry, and §10 Agent administration omit it.
Fix: PRD: add "calling restriction (A-11)" to 461, 83, and 896.

**[Consistency-internal]** — CI-18 Glossary Agent Administrator omits lowering caps and limits (§ §3 83 ↔ FR-33 466, FR-32 707)
The glossary entry does not carry the FR-33 row 466 / FR-32 grant.
Fix: PRD §3 83: append "and lowering, never raising, the tenant's cost caps, rate limits, and concurrency bound".

**[Consistency-cross-artifact]** — CX-13 Spine AD-17 narrows the `UnretiredAssumption` owner set (§ FR-28 688, OQ-24 ↔ ARCHITECTURE-SPINE.md AD-17 225)
AD-17: "for every unretired Product, Architecture, or Governance assumption" against the PRD's "whatever its owner". The register emitter row 75 says "whatever its owner" and AD-17 defers to the register, so the narrowing is textual.
Fix: Spine AD-17: "whatever its owner". (PRD OQ-22 has the same wording — CI-15.)

**[Consistency-cross-artifact]** — CX-14 Register vocabulary row describes `PayloadProtectionUnavailable` in host-report terms the PRD forbids as sufficient (§ FR-34 727 ↔ launch-readiness-register.md 53 vs 78)
Register 53: "The host has not reported the production payload-protection engine required by FR-34 as available"; PRD: a readiness policy that "clears it on a host-asserted signal alone … does not satisfy this requirement". The emitter row 78 has the attestation wording.
Fix: register 53: "The FR-34 seal/unseal/erase identity-and-version attestation against the committed `EXT-PROTECTION-1` engine has not passed".

**[Consistency-cross-artifact]** — CX-15 Epics vocabulary drift on `AiAgent` spelling and projection ids (§ epics.md 144, 160 ↔ A-1; register 270–272; Spine AD-17 225)
"`ParticipantType.AiAgent`/`AIAgent`" (144) against A-1 "`AiAgent` is the spelling this PRD uses"; projection inventory "`agent-setup-readiness`, `provider-capability-pricing`" (160) against the register's ratified shipped ids `agent-setup`, `provider-catalog`, `tenant-provider-enablement`.
Fix: epics 144, 160.

**[Consistency-cross-artifact]** — CX-16 Spine AD-14's attestation summary omits the DEK-destroy and `Erased` replay steps (§ FR-34 727 ↔ ARCHITECTURE-SPINE.md 205)
Spine 205: "records a canary protect/unprotect result plus engine identity and version"; the register's `EXT-HOST-1` artifact (89) has the full sequence.
Fix: Spine 205: "seal, plaintext-absence, unseal, DEK destruction, `Erased` replay, identity and version".

**[Consistency-cross-artifact]** — CX-17 The sprint change proposal preserves a "29-story structure" while `epics.md` states "exactly 33 stories" (§ sprint-change-proposal-2026-09-09-2.md frontmatter 39, §2 79 ↔ epics.md 32/34)
Stories 5.9, 5.10, 6.8, 7.7 were added by the earlier same-day follow-through proposal; the second proposal's debt table assigns to 5.5/5.4/8.4 what Stories 5.9/5.10 now own. The PRD asserts no story count, and epics reconciles it.
Fix: none required for the PRD; the proposal could carry a one-line note that 33 is current.

**[Consistency-cross-artifact]** — CX-18 Register `LR-PARTY-IDENTITY` is "Invalidated by … Agent identity-link changes"; linking is retired (§ launch-readiness-register.md 100 ↔ FR-2 148)
The invalidation trigger names a command the PRD has retired to deprecate-and-reject.
Fix: register 100: "Party identity or provisioning changes".

**[Consistency-cross-artifact]** — CX-19 FR-28 item 9 and §8.1 838 require that "every Architecture-owned row carries a target retirement date"; every Architecture-owned open Spine row reads "TBD — target milestone" (§ FR-28 688, §8.1 838 ↔ ARCHITECTURE-SPINE.md ARCH-A table 763–786)
The Spine table now has the `TargetRetirementDate` column but every Architecture-owned open row is `TBD`, and the Spine says so ("Architecture-owned open rows remain `TBD` until their co-owners approve a date and therefore block `RQ-1`"). Not a contradiction — the requirement is unmet and self-reported.
Fix: none in text; Architecture co-owners supply dates (proposal §6).

**[Rubric: Done-ness clarity]** — The posting-failure rate counts every automatic post twice as written (§ FR-28 line 700)
"automatic posts count as one approved and one failed on the same terms" — the intent is one denominator entry per automatic post and one numerator entry only if it entered `PostingFailed`; the sentence says both.
Fix: "an automatic post counts as one entry into `Approved` and, if it entered `PostingFailed` at least once, one into the numerator".

**[Rubric: Done-ness clarity]** — The `Approved → Abandoned` row requires a `MessageId` lookup for a proposal that never attempted a post, and the unavailable-lookup outcome is defined for `PostingFailed` only (§ FR-18 row 429; FR-18 line 440)
Row 429's guard is "the `MessageId` lookup finds no posted message" from `Approved`; line 440 defines the unavailable-lookup outcome ("the exit is refused with a typed reason and the proposal stays `PostingFailed`") and skips the lookup only for a `PostingFailed` proposal that never posted. The Spine (AD-5) already says the proposal "stays `Approved`" on an unavailable lookup; the PRD does not.
Fix: Add to line 440: "For the `Approved` origin the lookup is skipped, because no post was attempted; a removal detected against an `Approved` proposal abandons it directly."

**[Rubric: Done-ness clarity]** — SM-C4's "exactly" list has no class for FR-8 step 2 and step 3 rejections (§ SM-C4 lines 957, 960; FR-8 steps 2–3; FR-4 lines 193, 199)
A call rejected because the Agent is `Disabled`, the Provider/model is disabled, or the data-handling grace has lapsed is not blocked, failed, unavailable, or in the separately reported list, so the trigger review cannot place it; the grace lapse in particular is a structural unavailability the counter-metric exists to see.
Fix: Add "Agent lifecycle and Provider/model eligibility (FR-8 steps 2–3)" to the separately reported list, and the data-handling grace lapse to the blocked-call reasons.

**[Rubric: Done-ness clarity]** — Residual adjective-only phrasing — deferred item, still open (§ FR-4 line 193; FR-20 line 510; FR-25 line 606)
"A documented migration state may allow temporary read-only inspection"; "at a level sufficient to explain"; "support launch monitoring of adoption and approval workflow metrics" — unchanged since the first 2026-09-09 run.
Fix: Name the migration state or delete the sentence; FR-20: "records the role basis, the FR-33 row, and the typed denial reason"; FR-25: "exposes the SM-2, SM-3, SM-7, SM-C4, and SM-C5 inputs defined in §12".

**[Rubric: Done-ness clarity]** — The tenant-role calling restriction has no configuration home (§ FR-33 rows 461, 462 [ASSUMPTION A-11]; §10 line 896; glossary line 83)
Row 462 grants "unless the Tenant Agent Administrator restricts calling to a tenant role"; the Configure `hexa` row lists ten settings without it, §10 lists the same set, and the glossary Agent Administrator entry likewise. The permission is enforced at FR-8 step 1 but no row grants the act of setting it and no surface exposes it.
Fix: Add "calling restriction (A-11)" to the Configure `hexa` row and to §10 Agent administration.

**[Rubric: Scope honesty]** — Thirteen assumptions retire on a condition with no date — deferred item, still open, one row larger (§ §8.1 A-1..A-4, A-9..A-12, A-15, A-16, A-18, A-21; A-5..A-8, A-13, A-17, A-19, A-20)
Thirteen rows retire on a register commitment whose `TargetIntegrationDate` is `TBD` on every entry, or on "Product confirms the FR-33 rows"; eight more say "before enablement", which is a date only once enablement is scheduled. A-9..A-12 are one Product decision and could carry one due date the way A-14 does.
Fix: One due date for the FR-33 confirmations; for the seam rows, cite the register's `TargetIntegrationDate` field rather than restating a milestone.

**[Rubric: Scope honesty]** — §8 says two seams are "pending register extension"; the register already carries both (§ §8 seam 2 line 818; seam 4 line 820; external-dependency-register.md lines 57, 65)
Line 818: "The existence read is not yet in the register's seam-2 text; adding it is a pending register extension (correct-course, 2026-09-09)"; line 820: "The event-feed alternative is a pending register extension". The register line 57 lists the existence read by `MessageId` with its typed answers and the Conversations-side tenant event feed, with line 65 recording the 2026-09-09 scope extension. Stale in the safe direction, but a reader who checks will find the PRD behind the register on the seam whose absence the PRD says blocks `Available`.
Fix: Delete both "pending" sentences; keep "`EXT-CONV-AI-1` cannot reach `Available` on a compatibility command that does not exercise the existence read".

**[Rubric: Downstream usability]** — §8.1 states a Spine row count that the Spine at the same `updated` date does not have — reopened (§ §8.1 line 836; ARCHITECTURE-SPINE.md ARCH-A table)
"at that date the table holds `ARCH-A-1` through `ARCH-A-10`, with `ARCH-A-5` retired"; the Spine table now holds `ARCH-A-1` through `ARCH-A-12`, `ARCH-A-5` retired. The range is declared open so nothing breaks, but this is the second same-day drift of a count the PRD does not need to carry.
Fix: "the table current at that date" — no number.

**[Rubric: Downstream usability]** — Process gates remain inside FR-21 — deferred item, still open (§ FR-21 lines 521–523, 525, 526)
Consequences 521–523 and 525 are rules for the register owner and testers, not system behavior a story can implement; the circularity the baseline flagged is closed by the last sentence of line 525, which is the only part of it that belongs in an FR.
Fix: Move 521–523 and the execution rule to §8; keep the `DependencyNotAvailable` typed outcome and the `PayloadProtectionUnavailable` path in FR-21.

**[Drift]** — DL-1 A-1 should be narrowed: `ParticipantType.AiAgent` is the compiled Conversations spelling (§ §8 seam 1 line 817, A-1 row line 850 ↔ ConversationClientResponsePoster.cs:85-86, 88-92; ApproverPolicySourceKind.cs:13)
PRD keeps `ParticipantType.AiAgent` inside assumption A-1 ("the register owns the final names; `AiAgent` is the spelling this PRD uses"); code compiles `p.ParticipantType == ParticipantType.AiAgent` against the Conversations contract in use, while the comment at :88-92 records that `IConversationClient.AddParticipantAsync` is not exposed and `ParticipantRole.Member` appears nowhere. Verdict on the deferred item: narrow A-1 to `AddParticipantAsync`, the participants route, and `ParticipantRole.Member`; retire the `AiAgent` half. Kind: PRD-side naming.
Fix: PRD.

**[Drift]** — DL-2 `AgentSetupWriteStatus.Submitted = 0` remains, as the PRD records (§ FR-23 line 558, FR-29 line 567 ↔ AgentSetupWriteResult.cs:8-11)
PRD already names this as "non-conformant and is corrected additively before the first tenant is enabled"; code unchanged. Kind: acknowledged gap, no drift.
Fix: code, before enablement; nothing for the PRD.

**[Drift]** — DL-3 FR-26 "more restrictive of the initial attempt's policy and the then-current active policy" has no code, and no retry path exists to apply it (§ FR-26 line 644 ↔ no policy-merge symbol; retry absent per DH-4)
PRD deferred the wording itself (update report, "Deferred"); reported for completeness. Kind: gap.
Fix: code, together with DH-4 and DC-4.

## Deferred items revisited
- FR-26 "more restrictive of two policies" decidability — still open, medium (rubric; consistency CI-1 gives L-I3 its verdict: real rule stated two ways, PRD-side fix; drift DL-3 notes no code and no retry path to apply it).
- FR-31 assertion category versus OQ-9 — still open, medium (rubric).
- Public Agent Call state contract — still open, medium (rubric): two shipped enumerations (29 and 14 values) compete; `Proposal*` duplicates are not on the FR-23 register.
- p99 at n = 30 — still open, medium (rubric): §11 requires a percentile method the PRD never names.
- Undated assumptions — still open, low (rubric): thirteen rows retire on `TBD`-dated register events or "Product confirms"; A-14 remains the only dated row.
- Process gates inside FR-21 — still open, low (rubric); the circularity sub-issue is resolved by the compatibility-command sentence.
- Residual adjectives (FR-4 193, FR-20 510, FR-25 606) — still open, low (rubric), verbatim as at the baseline.
- Glossary drift — still open, low (rubric): counts unchanged; one new term without an entry ("Release PM").
- Eligible Conversation rationale placement — still open, low (rubric): the glossary entry still carries the SM-2/SM-C4 measurement rationale that belongs under SM-2 in §12.
- M-1 organization Parties as Approvers — still open, medium (adversarial AM-12).
- M-2 scan at edit time — still open, medium (adversarial AM-13): unsafe edits are stored as ordinary versions.
- M-6 FR-8 classifier ordering — still open, medium (adversarial AM-5): sharper now, the `Blocked` state is a free local read checked ninth.
- M-10 `NoEligibleApprover` remediation hint — still open, low (adversarial AL-10).
- M-12 staleness bound on `Approved` — still open, upgraded to high (adversarial AH-4): `Approved`/`PostingFailed` never expire, `Approved` has no human exit, administrative retry has no age bound.
- M-13 status entry in `EXT-CONV-UI-1` — still open, medium (adversarial AM-11): register artifact kinds unchanged.
- Gate L-5 SM-3 text on post-decision system abandonment — resolved (adversarial; line 947 and FR-18 line 435).
- Gate L-6 unreviewed-inspection consequence — still open, folded into high (adversarial AH-8).
- Gate L-7 FR-2 negative-test history sentences — still open, low (adversarial AL-11).
- Gate L-10 carried validate lows — mostly still open, low (adversarial AL-12); validate L-8 resolved by §9 line 881.
- L-I3 "then-current" versus "more restrictive" — verdict given: open, real, medium, PRD-side fix (consistency CI-1).
- Narrowing A-1 (`ParticipantType.AiAgent` already compiled against) — verdict given: narrow it; retire the `AiAgent` half, keep `AddParticipantAsync`, the participants route, and `ParticipantRole.Member` (drift DL-1, low).

## Correct-course follow-ups
Source: `update-report-2026-09-09-2.md`, "For correct-course", checked by the consistency reviewer against the registers, Spine, epics, and update report on disk.

- Landed: 27 of 29 document follow-ups. Launch-readiness register: `RQ-1` no longer gated on SM-1..SM-6, SM-7 contract, 8-code blocker vocabulary, `LR-PRODUCT-METRICS`/`product-metrics` split. Dependency register: seam-2 `MessageId` existence read, seam-4 event feed, seam-5 typed answers, seam-1 AI-type verification (A-21), `DependencyNotAvailable` semantics, `EXT-CONV-AI-1` consumers, `EXT-HOST-1` attestation port (entry returned to `Uncommitted`), `EXT-PROTECTION-1` contract. Spine: frontmatter binds FR-1..FR-34/OQ-1..OQ-30; AD-5, AD-22, AD-12 corrected; AD-7 retires link/replace; AD-14 names `PayloadProtectionUnavailable`; AD-15 rate limits; AD-2 five states (with the self-contradiction in CX-8). Epics: FR1/7/18/21/26 rows rewritten, FR29–FR34 rows and map, Story 5.4 caller drop, Story 6.6 state machine, Story 8.1 second approver, Stories 7.4/7.5/7.7 `MessageId` lookup and `LateConfirmed`, UX-DR45. Update report: proposal-amendment trail.
- Partly landed, by design: item 9 (non-conformance record — section present, disposition open pending Product's Branch A/B ruling; CX-7) and item 21 (ARCH-A `TargetRetirementDate` column added, values `TBD` pending co-owners; CX-4, CX-19).
- Not landed outright: none. Item 30 (code side) was outside the consistency reviewer's artifact set; the drift reviewer confirms every entry on it is still open.
- PRD text now stale on 6 of the 27 landed items: seam-2 read and seam-4 feed said to be "pending" (CX-3); non-conformance record asserted to exist (CX-7); `EXT-HOST-1` said to be `Committed` (CX-2); AD-5, AD-22, AD-12 said to be open defects (CX-5); ARCH-A count said to be ten (CX-4). Seam 1 still lacks the typed already-absent answer (CX-9).

## Mechanical notes
- **ID continuity.** FR-1..FR-34, NFR-1..NFR-14, OQ-1..OQ-30, SM-1..SM-7, SM-C1..SM-C5, UJ-1..UJ-4, A-1..A-21: all defined, all unique, every reference resolves. FR headings remain out of numeric order by design (FR-33 after FR-18; FR-29 after FR-23; FR-30 after FR-25; FR-31 after FR-26; FR-32 and FR-34 after FR-28); downstream tooling must key on IDs. Spine rows cited by number: `ARCH-A-1`, `ARCH-A-5`, `ARCH-A-6`, `ARCH-A-10` (all exist); AD-2, AD-5, AD-7, AD-12, AD-22, AD-30 (all exist).
- **Assumptions Index roundtrip.** Passes. 35 `[ASSUMPTION` occurrences: 31 keyed inline tags mapping onto A-1..A-21 (A-3 ×3, A-9 ×3, A-10 ×3, A-13 ×3, A-4 ×2, A-17 ×2), three explanatory `A-n` mentions (§0 line 14, FR-33 line 453, §8.1 line 834), one generic `[ASSUMPTION]` in OQ-21 (line 991). Every index row's "Where" column names a location that carries the tag (A-21 → §8 line 817); no inline tag lacks a row.
- **§0 versus §8 versus register.** Inconsistent — see the Decision-readiness high. Internally §0 and §8 agree with each other (eight of nine `Uncommitted`, `EXT-HOST-1` `Committed`, none `Available`); both disagree with the register.
- **§8.1 versus Spine.** Row count stale (ten stated, twelve present); three-defect list stale (all three absorbed); one live divergence unlisted (AD-13 Automatic-mode Eligible Approver). The Spine changed between two reads during this review (12:44 and 12:49 file times; AD-5 moved from line 135 to 141) — a PRD sentence that pins a Spine line count or row count will be wrong within the hour on a day like this.
- **Frontmatter.** `status: final`, `updated: 2026-09-09`; matches the body and the §13 preamble.
- **Callout tags.** Zero `[NOTE FOR PM]` and zero `[NON-GOAL for MVP]` inline; the Deferred rows, §5, §6.2, FR-27's OQ-18 sentence, and §12's OQ-23 sentence do the equivalent work.
- **UJ protagonists.** Nora, Milan, Anika, Omar, each with inline context; no floating UJs.
- **Glossary drift (low, unchanged).** "Agent response(s)" lowercase ×4 (§2.2 line 41, UJ-2 line 60, FR-19 line 498, §6.2 line 776) versus "Agent Response" ×16; "Agent call" ×3 and "Agent calling" ×1 versus "Agent Call" ×24; glossary heading "Provider/Model Selection" versus "Provider/model selection" ×7; "eligible Conversations" lowercase ×3 in SM-2/OQ-11 versus the glossary term ×3. New: "Release PM" ×14 with no entry (see the Decision-readiness medium).
- **Typed-name consistency.** `MembershipUnavailable` is now classified (FR-25 line 610, SM-C4 line 959 — baseline low closed), but FR-2 line 160 still says it and `MembershipRejected` "remain posting-failure reasons for that re-validation only" three lines after line 157 uses `MembershipUnavailable` as an acceptance rejection; reword to "at re-validation these two are posting-failure reasons; at acceptance `MembershipUnavailable` is a call rejection". `DeletionDeferredByHold` still appears only in §9 (line 883); FR-30 and §10's legal-hold bullet should expose it. SM-C4 line 960 says "with rate-limit rejections excluded" where FR-28 line 700 excludes only limits set by the Platform Operator or Release Operator — align SM-C4 to FR-28.
- **FR-2 `Joined` detection when no mirror ever existed.** Line 154's condition "the last mirror is confirmed, and no clear is newer than that mirror" has no truth value for a Conversation that was never blocked; state that the condition holds vacuously when no mirror exists, or every never-blocked Conversation's external removal is undetectable.
- **A-9 row versus FR-33.** FR-33 row 465 tags the `ExternallyRemoved` clear ("Tenant Agent Administrator or any current Conversation Facilitator") with A-9, but the A-9 index row (line 858) and OQ-16 describe only the block clear; add the `ExternallyRemoved` clause to A-9 so the index matches the tagged text (OQ-25 already has it).
- **Brownfield references verified in `src/`.** Every enum the PRD names by member exists as stated (see Downstream usability). `MembershipUnavailable`, `MembershipRejected`, and `ContextUnavailable` are shipped names reused by the PRD; every other new typed name is absent from `src/`, consistent with declaration.
- **Required sections.** Present for a launch-level, chain-top, brownfield PRD: purpose with status, vision, users and journeys, glossary, features with FRs and testable consequences, non-goals, MVP scope, NFRs, dependencies with Assumptions Index, governance, API surface, evidence, success metrics with counter-metrics, decision register.
- **Cross-artifact vocabulary drift (consistency reviewer, low, not covered above).** Spine AD-7 157 calls `ExternallyRemoved` a "system-set `ExternallyRemoved` block" where the PRD treats it as a state distinct from the Agents-owned block; epics FR9 row says "full-or-blocked" against the PRD's three context modes (`Full`, `Bounded`, `Blocked`); UJ-3 line 68 says "posting" for `PostingPending` (prose, acceptable); `ContextUnavailable` is used by FR-25 and SM-C4 but defined nowhere in FR-9 (CI-7); epics 144 spells `AIAgent` beside `AiAgent` and its projection inventory (160) uses ids the register does not ratify (CX-15).

## Reviewer files
- `review-rubric.md`
- `review-adversarial-general.md`
- `review-consistency.md`
- `review-implementation-drift.md`
