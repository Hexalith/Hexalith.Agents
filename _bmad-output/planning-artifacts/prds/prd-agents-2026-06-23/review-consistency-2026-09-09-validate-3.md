# Consistency Review — PRD Hexalith Agents

- **PRD:** `_bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md` (`status: final`, `updated: 2026-09-09`, file 2026-09-09 10:08, 1000 lines) with `addendum.md` (10:04)
- **Reviewed:** 2026-09-09 (afternoon, after the correct-course run)
- **Artifacts checked (frontmatter `updated:` / file mtime):**
  - `external-dependency-register.md` — `updated: 2026-09-09`, `authority: sprint-change-proposal-2026-09-09-2.md`; mtime 12:08
  - `launch-readiness-register.md` — `updated: 2026-09-09`, `authority: sprint-change-proposal-2026-09-09-2.md`; mtime 12:49 (changed during this review: line 108 gained the `DeletionRequest` symmetry)
  - `architecture/architecture-agents-2026-06-23-2/ARCHITECTURE-SPINE.md` — `updated: 2026-09-09`, `architecture_assumption_index_version: 3`, binds `PRD FR-1..FR-34`, `NFR-1..NFR-14`, `OQ-1..OQ-30`; mtime 12:52 (changed during this review: round-4 update landed `ARCH-A-11`, `ARCH-A-12`, `ARCH-A-INDEX-3`, the AD-7 clear rule, AD-12 disabled-Agent retry pause, the corrected EventStore gitlink). Findings below are against the 12:52 text.
  - `epics.md` — no `updated:` field (frontmatter carries `stepsCompleted` / `inputDocuments` only; `inputDocuments` names `sprint-change-proposal-2026-09-09-2.md`); mtime 12:44, uncommitted working-tree edits to Stories 8.2 and 8.8
  - `sprint-change-proposal-2026-09-09-2.md` — `updated: 2026-09-09`, `status: approved`, `execution_status: approved-awaiting-implementation`; mtime 11:24
- **Baseline:** `review-consistency-2026-09-09-update-gate-2.md` (internal 0C/1H/2M/7L; cross-artifact 1C/3H/9M/4L; 17 prior dispositions)
- **Follow-up list checked:** `update-report-2026-09-09-2.md`, section "For correct-course"

Line numbers refer to each file as read at review time.

## Verdict

**Not consistent as it stands — the PRD needs a small factual re-sync, not a requirements change.** Internally the PRD holds together (no critical or high internal finding; every ID series is contiguous, every cross-reference and `[ASSUMPTION A-n]` tag round-trips, the FR-18 table and its prose now agree, and every baseline internal finding except the deferred L-I3 is closed). Cross-artifact, the correct-course run landed essentially everything the previous review asked for, and that is precisely the problem: five statements the PRD makes about the registers and the Spine — that `EXT-HOST-1` is `Committed` ("eight of the nine"), that the seam-2 existence read and seam-4 event feed are "pending register extension[s]", that the Spine table holds `ARCH-A-1` through `ARCH-A-10`, and that AD-5, AD-12, and AD-22 are open Spine defects — are now false. One new critical cross-artifact defect exists in the Spine (AD-13 requires Eligible Approver resolution in Automatic Response Mode, which would make Automatic mode unusable for an Agent with no Approver Policy, contradicting FR-1, FR-7, FR-8 step 8, and UJ-2), and one high (the `DataHandlingVersion` / data-handling-record enablement gate of FR-4/FR-5/OQ-29 is absent from Spine AD-10 and from the register's Provider Readiness Contract). L-I3 gets its verdict: it is a real rule stated two ways, Medium, with the fix on the PRD side.

Counts — internal: 0 critical / 0 high / 10 medium / 8 low. Cross-artifact: 1 critical / 5 high / 6 medium / 7 low.

## Part A — Internal

### Critical

None.

### High

None.

### Medium

- **[medium]** CI-1 — Safety re-check policy stated two ways (L-I3 verdict: open, real) (FR-26 640 ↔ FR-26 644; FR-17 405, FR-18 421/442, FR-27 667, OQ-9 979) — Line 640: "the approval-time and pre-post re-checks required by FR-27 … always evaluate the then-current active policy"; FR-17 405, FR-18 table row 421 and 442, FR-27 667, and OQ-9 say "then-current" too. Line 644 of the same FR: "each retry, approval-time check, and pre-post check uses the more restrictive of the initial attempt's policy and the then-current active policy. A re-check can therefore only tighten the outcome, never loosen it." When the platform policy is loosened between attempt and approval the two rules give different answers (640 admits content the snapshot policy would have blocked; 644 blocks it). Spine AD-20 245 implements 644 ("evaluate under the current pair when it is at least as restrictive as the snapshot pair and under the snapshot pair exactly otherwise", with `LastLoosensVersion` ordering), so the downstream authority has already picked the "never weaker" reading. *Fix:* PRD. Keep 644 as the rule; reword 640 to "always evaluate the then-current active policy, never weaker than the attempt's snapshot policy (below)", and add "(never weaker than the attempt's snapshot policy, FR-26)" after "then-current" at 405, 421, 442, 667, and in OQ-9. This closes baseline L-I3 rather than deferring it again.
- **[medium]** CI-2 — Party link/replace commands are "on the FR-23 deprecate-and-reject register" but FR-23's register enumeration omits them (FR-2 148 ↔ FR-23 557) — FR-2: "the shipped Party link and replace commands (Spine AD-7) are on the FR-23 deprecate-and-reject register, and a link or replace naming any Party is a typed rejection"; OQ-25 995 repeats it. FR-23: "The V1 deprecate-and-reject register is: `CostControlPosture.ReportingOnlyMonitoring` and `CostControlPosture.AcceptedLaunchRisk` …, `ContentSafetyFailureHandling.BlockWithAuditableOverride` …, and `ApproverPolicySourceKind.Caller` … and is listed here so that no consumer treats it as accepted behavior." A consumer reading the closed FR-23 list treats link/replace as accepted. *Fix:* PRD FR-23 557: add "and the Agent Party link and replace commands (FR-2; provisioning is the only identity path)".
- **[medium]** CI-3 — Administrative retry of a `PostingFailed` proposal: table allows it at any time, FR-33 only after the budget is exhausted (FR-18 433 ↔ FR-33 478, FR-18 439) — Table row 433: "`PostingFailed` → `PostingPending` | System (automatic retry within budget); Tenant Agent Administrator (audited administrative retry) | Failure reason is not a safety verdict; tenant not `Suspended` and Agent `Active`; the `MessageId` lookup finds no posted message; full pre-post re-validation re-run" — no budget condition on the administrative actor. FR-33 478: "Retry a `PostingFailed` proposal administratively after its retry budget is exhausted (FR-18)"; 439 lists the administrative retry among the exits "When the retry budget is exhausted". *Fix:* PRD table row 433 guard: add "administrative retry only after the automatic budget is exhausted (FR-33)".
- **[medium]** CI-4 — `ExpiredWhileSuspended` is recorded on two different conditions (FR-18 424 ↔ FR-28 698, OQ-27 997) — Table row 424: "recorded with reason `ExpiredWhileSuspended` when the awaiting window overlapped a `Suspended` period" (any overlap, even if released before expiry). FR-28: "a proposal that expires during the suspension is recorded `ExpiredWhileSuspended`"; OQ-27 same; Spine AD-12 189 "expiry during suspension records `ExpiredWhileSuspended`". The overlap reading removes more proposals from the SM-3 and SM-C5 denominators than the expiry-instant reading. *Fix:* PRD row 424: "when `ExpiresAt` passed while the tenant was `Suspended`".
- **[medium]** CI-5 — Two settlement instants for a held `Indeterminate` reservation (FR-28 692, both sentences; OQ-6 976) — "a reservation whose outcome is `Indeterminate` is held for a bounded period — default 24 hours … — and then conservatively settled at the estimated maximum" versus, four sentences later, "A reservation past its hold deadline with no authoritative outcome becomes `Unreconciled`: it stays counted against the cap and is settled only by an audited operator command or at period close at the estimated maximum". FR-12 342 makes every undeterminable outcome resolve to the typed `Indeterminate`, so "no authoritative outcome" past the hold deadline is either unreachable or is the same case settled at a different time (hold deadline vs period close). Spine AD-21 251 carries the same two sentences. *Fix:* PRD FR-28 692: state which case becomes `Unreconciled` (recommended: an `Indeterminate` reservation whose hold deadline passes without an operator settlement becomes `Unreconciled` and is settled at period close; delete "and then conservatively settled at the estimated maximum" from the first sentence and align OQ-6), then mirror in Spine AD-21 / ARCH-A-6.
- **[medium]** CI-6 — The FR-7 re-check's `NoEligibleApprover` abandonment is stated over `PostingFailed` proposals but FR-18 has no such transition (FR-7 252/255 ↔ FR-18 table 429–431, 435, 439) — FR-7 252: "A scheduled re-check runs over every awaiting proposal and every `PostingFailed` proposal"; 255: "The re-check moves a proposal to `Abandoned` with reason `NoEligibleApprover` only after two consecutive passes". FR-18 415: "any other (from, to) pair is rejected"; the only system rows out of `PostingFailed` into `Abandoned` are 429 (`RemovedInConversations`) and 430 (`SourceConversationUnavailable`); 439 lists the re-check exit from `PostingFailed` as the `ConversationDeleted`/`PrincipalRemovedFromConversation` answer only. *Fix:* PRD FR-7 255: "moves an awaiting proposal to `Abandoned` …"; add "a `PostingFailed` proposal receives the `ResolutionEmptyPending` marker only" so the marker rule and the table agree.
- **[medium]** CI-7 — `ContextUnavailable` is counted but never produced (FR-25 610, SM-C4 959 ↔ FR-9 302, FR-9 301) — FR-25: "the per-tenant count of unavailable calls by reason (`MembershipUnavailable`, `ApproverResolutionUnavailable`, `ContextUnavailable`)"; SM-C4 lists the same three. FR-9 302 only says "If the complete Conversation Context cannot be loaded or sent safely, the Agent Call fails closed" with no typed reason, and 301 records the context mode as `Blocked` "with a typed block reason" — so an unavailable Conversations read is indistinguishable from a policy block, which SM-C4 counts as a blocked call. Spine AD-15 213 and epics Story 6.2/6.8 already carry `ContextUnavailable` as a public value. *Fix:* PRD FR-9 302: "… fails closed with the typed reason `ContextUnavailable`, which FR-25 and SM-C4 count as an unavailable call, not a blocked one; the recorded context mode is `Blocked` with that reason".
- **[medium]** CI-8 — `PostingFailed` retry clock while the Agent is `Disabled` is unstated, and the Spine has had to assume it (FR-3 176, FR-18 433/441 ↔ Spine AD-12 189, ARCH-A-11) — FR-18 441 pauses the retry-window clock "While the tenant kill switch is pulled"; row 433 blocks retries while the Agent is not `Active`; FR-3 176 says what a `Disabled` Agent's awaiting and `Approved` proposals do but nothing about `PostingFailed`. With a 15-minute window (A-7) a disable/re-enable cycle silently exhausts the budget. Spine AD-12 now states "a `PostingFailed` automatic or administrative retry is suspended with its retry-window clock paused while the Agent is disabled … [ASSUMPTION ARCH-A-11]" and its table row asks "Product confirms this rule". *Fix:* PRD FR-18 441 and FR-3 176: "The same pause and suspension apply while the Agent is `Disabled`" — and record the confirmation so ARCH-A-11 can retire.
- **[medium]** CI-9 — FR-28 assigns recorded decisions to "Release PM" and "Product", neither of which FR-33 maps (FR-28 685, 688, 701 ↔ FR-33 453) — FR-33: "Every authorization rule in this PRD resolves to one of six Agents roles at a stated scope". FR-28 item 6: "a dependency may be excluded only by a recorded Release PM decision …, and that exclusion is itself an `RQ-1` blocker until Product accepts it"; item 9: "blocks only once the Release PM records an acknowledgement"; 701: "Product records a disable-or-continue decision". §8.1 840 says a rule granting a role an operation absent from FR-33 is "a PRD amendment pending". *Fix:* PRD FR-33: either add a note that Release PM and Product are planning parties whose decisions are recorded by the Release Operator in the launch readiness register (and say so at the three FR-28 sites), or add matrix rows for the three decisions.
- **[medium]** CI-10 — The `Approved`-origin abandonment guard names a `MessageId` lookup the lookup rule never performs from `Approved` (FR-18 429 ↔ FR-18 440) — Row 429: "`Approved`, `PostingFailed` → `Abandoned` (`RemovedInConversations`) | System | Removal or block detected (FR-2), and the `MessageId` lookup finds no posted message". 440 scopes the lookup to "Before every retry attempt and before any exit from `PostingFailed`" and skips it when no post was attempted; it says nothing about an unavailable lookup against an `Approved` proposal. Spine AD-5 145 had to fill the gap ("an unavailable lookup refuses the exit and the proposal stays in its pre-check state — … `Approved` for the `Approved`-origin system-abandon check"). *Fix:* PRD FR-18 440: extend the lookup rule to "and before the system's `Approved` → `Abandoned` (`RemovedInConversations`) transition; an unavailable answer leaves the proposal in `Approved`".

### Low

- **[low]** CI-11 — A-9 index row lags the FR-2/FR-33 clear rule it keys (§8.1 858 ↔ FR-2 165, FR-33 465) — Row: "a block is cleared only by the setting authority or the Tenant Agent Administrator"; the tagged text adds "An `ExternallyRemoved` record is cleared by the Tenant Agent Administrator or by any current Conversation Facilitator". Baseline M-I2 asked for the row to be extended; the body was, the row was not. *Fix:* PRD A-9 row: append "; an `ExternallyRemoved` record by the Tenant Agent Administrator or any current Facilitator".
- **[low]** CI-12 — §6.1 still lists "Agent Party identity provisioning or linking through Hexalith.Parties" (§6.1 752 ↔ FR-1 132, FR-2 148) — linking is retired to deprecate-and-reject. *Fix:* PRD §6.1: "Agent Party identity provisioning through Hexalith.Parties at tenant enablement".
- **[low]** CI-13 — FR-12 names the Agents-owned block but not `ExternallyRemoved`, which FR-11 names (FR-12 334 ↔ FR-11 326) — FR-11: "does not post while the Conversation is `Blocked` or `ExternallyRemoved`"; FR-12: "the tenant kill switch, the Agents-owned block (FR-2), …". *Fix:* PRD FR-12 334: "the Agents-owned block or `ExternallyRemoved` record (FR-2)".
- **[low]** CI-14 — FR-22 lists "failed-call" among "proposal states" (FR-22 542 ↔ FR-18 410) — "the awaiting-decision, posting-failed, failed-call, and expired proposal states"; a failed call is an Agent Call outcome (FR-10), not a `ProposedAgentReplyState`. *Fix:* PRD FR-22: "the awaiting-decision, posting-failed, and expired proposal states and the failed-call outcome".
- **[low]** CI-15 — OQ-22 narrows the assumption owners that block `RQ-1` (OQ-22 992 ↔ FR-28 688, OQ-24 994, §8.1 836) — OQ-22: "Unretired Product, Architecture, or Governance assumptions in §8.1 … block `RQ-1`"; FR-28 item 9 and OQ-24: "whatever its owner" (A-1, A-2, A-15, A-16, A-18, A-21 are owned by the Conversations or EventStore Maintainer). *Fix:* PRD OQ-22: "Every unretired §8.1 assumption, whatever its owner, and …".
- **[low]** CI-16 — `GateOutOfScope` and `TriggerReviewOverdue` are classified neither as register vocabulary nor as `AgentLaunchReadinessBlocker` values (FR-28 690 ↔ FR-30 621, FR-28 700) — 690 classifies four codes as register vocabulary and two as additive blocker values; 621 and 700 say the other two are "recorded … on the FR-30 surface". The register (lines 51–52, 79–80) treats both as `BlockerCode` values. *Fix:* PRD FR-28 690: add "`GateOutOfScope` and `TriggerReviewOverdue` are register vocabulary that the FR-30 surface mirrors".
- **[low]** CI-17 — The tenant-role calling restriction (A-11) is a Tenant Agent Administrator setting that no configuration list carries (FR-33 462 ↔ FR-33 461, §3 83, §10 896) — 462 grants "unless the Tenant Agent Administrator restricts calling to a tenant role"; the "Configure `hexa`" row 461, the glossary Agent Administrator entry, and §10 Agent administration omit it. *Fix:* PRD: add "calling restriction (A-11)" to 461, 83, and 896.
- **[low]** CI-18 — Glossary Agent Administrator omits lowering caps and limits (§3 83 ↔ FR-33 466, FR-32 707). *Fix:* PRD §3 83: append "and lowering, never raising, the tenant's cost caps, rate limits, and concurrency bound".

### Checked clean (internal)

- FR-1..FR-34, NFR-1..NFR-14, OQ-1..OQ-30, SM-1..SM-7, SM-C1..SM-C5, UJ-1..UJ-4, A-1..A-21: each defined once, each series contiguous (FR-33 is placed at 451 ahead of FR-19, FR-29 after FR-23, FR-30 after FR-25, FR-31/FR-32/FR-34 inside §4.10 — deliberate grouping, every reference resolves). §13 has exactly 30 rows.
- Every inline `[ASSUMPTION A-n]` tag (100, 104, 162, 199, 233, 252, 296, 393, 458, 459, 462, 464, 465, 483, 590, 692, 700, 701, 817, 818, 819, 820, 821, 822, 825, 934, 947, 948, 988) maps to the "Where" cell of its §8.1 row, and every "Where" cell names only tagged locations; A-6 "(restated in FR-32)" and A-13 "(restated in OQ-11)" are accurate.
- §0 16 ↔ §8 812/814 ↔ FR-21 522/525 ↔ FR-28 685 ↔ FR-34: one `Uncommitted`/`Committed`/`Available` vocabulary; the two §0/§8 counts agree with each other (they disagree with the register — CX-2).
- FR-18 transition table ↔ prose: every prose exit from `PostingFailed` (439) has a row (429, 430, 431, 432, 433); `Rejected`/`Abandoned` rows carry "permitted while `Disabled` or `Suspended`" (422, 423, 431) as FR-3 176 and FR-28 698 require; row 431 carries the FR-33 "gone or inaccessible" condition; `PostingPending` uninterruptibility (415) agrees with FR-2 167 and FR-3 176. Baseline H-I1, L-I2 closed.
- FR-2 state machine ↔ §3 97, OQ-16 986 (now "`Blocked` may be set from any state"), OQ-25 995: one five-state vocabulary, detection only from `Joined` with confirmed mirror and no newer clear, re-admission from `ReadmitPending` or from `ExternallyRemoved` on presence, clear authority evaluated at clear time. Baseline M-I2, L-I4 closed.
- FR-8 nine-step order ↔ §3 "Accepted Agent Call" ↔ FR-28 692 (reserve after measurement) ↔ FR-2 151 (membership last) ↔ FR-27 670/NFR-9 (scan outside the 2-second gate).
- SM-C4 ↔ FR-25 607/610 ↔ §3 104: blocked / failed / unavailable / reported-separately classes agree. Baseline M-I1 closed.
- Kill switch: FR-3 171/176, FR-18 435/441, FR-25 611, FR-28 698–703, FR-33 471–473, OQ-27: one behaviour.
- FR-33 ↔ FR-1/2/4/5/7/18/24/26/28/30/32/34, §3 role entries, UJ-1 49: roles, scopes, and grants agree; "Platform-scoped gate observations are recorded by the Platform Operator" is present (baseline M-X9 closed); glossary entries and UJ-1 aligned (baseline L-I6 closed); "two rows" wording resolved by naming the block and clear rows (baseline L-I1 closed).
- §13 preamble ↔ status cells: every amendment date named in the preamble appears on the row and vice versa, including OQ-12 "clarified 2026-09-08" (baseline L-I5 closed).
- §12 preamble cadence "30 and 60 days … and monthly thereafter" everywhere (baseline L-I7 closed). §6 MVP ↔ §5 non-goals ↔ §4 features ↔ OQ-13/OQ-20/OQ-29: consistent.
- Addendum "Options Considered" ↔ §13: every rejected alternative maps to a row or FR (SM-3/`RQ-1` → OQ-19/OQ-22; status label → §0; Safe Context Budget, `Blocked` mode → FR-9; wire name → OQ-14; membership seam, removal notification → §8/FR-2; disclosure category → FR-7; `ExpiresAt` under kill switch, `Suspended` status → OQ-27/FR-3; provisioning → OQ-28; seam 4 feed, A-4 → §8; data-handling → OQ-29; cadence → A-20; `Approved → Abandoned` → OQ-26; attestation → FR-34; small-tenant sample → A-17). The two addendum lines that say AD-5 / AD-12 are "recorded in §8.1 as a Spine defect" / "listed for Architecture to correct" are consistent with the PRD but stale against the Spine (CX-5).

## Part B — Cross-artifact

### Critical

- **[critical]** CX-1 — Spine AD-13 requires Eligible Approver resolution, and a `NoEligibleApprover` rejection, in Automatic Response Mode (FR-1 132, §4.3 215, FR-7 259, FR-8 284, UJ-2 55–61 ↔ `ARCHITECTURE-SPINE.md` AD-13 197) — Spine: "Eligible Approver resolution runs in every response mode, not Confirmation mode only — an Automatic-mode Agent with zero configured Approvers is rejected up front with `NoEligibleApprover` before any Provider call". PRD FR-8 step 8: "Eligible Approver resolution where Confirmation Response Mode applies (FR-7)"; FR-1: "Response Policy, and Approver Policy where confirmation is enabled"; §4.3: "When confirmation is enabled, all approval authority comes from the Agent's Approver Policy"; FR-7 259: "`hexa` is unavailable, with a typed `NoEligibleApprover` rejection, in a Confirmation Response Mode Conversation whose Participants yield no Eligible Approver"; UJ-2 has Milan receive an automatic reply with no Approver in the story. A developer following AD-13 rejects every Automatic-mode call on a tenant that never configured an Approver Policy, which FR-1 permits, and SM-C4 would report the calls as blocked. The Spine's own sequence diagram (398 "EligibleApprover resolution (both modes)") repeats it. *Fix:* Spine AD-13 197 and diagram 398: "Eligible Approver resolution runs only where Confirmation Response Mode applies (PRD FR-8 step 8)". PRD-side change: none.

### High

- **[high]** CX-2 — The PRD reports `EXT-HOST-1` as `Committed`; the register has returned it to `Uncommitted` (§0 16, §8 814 ↔ `external-dependency-register.md` 94, 97, 212) — PRD §0: "all critical external dependencies except `EXT-HOST-1` are `Uncommitted` — eight of the nine register entries (§8)"; §8 814: "At the time of this revision, only `EXT-HOST-1` is `Committed`; the remaining entries are `Uncommitted`." Register EXT-HOST-1 `AcceptedStatus` | `Uncommitted`, with "*Historical prior commitment only: target `a66cdf34…`, integration date `2026-09-30`, and `./eng/verify-agents-host.sh` covered the earlier clean-checkout host artifact. The expanded protection binding and attestation artifact has not been re-accepted*"; Current Blocking Summary: "All nine dependency records are `Uncommitted`". The PRD's §0 says build readiness is owned by the register and that "§8 reports the current dependency status without softening it"; a reader of §0 would treat Stories 5.1 and 5.6 as unblocked by `EXT-HOST-1`. *Fix:* PRD §0 16: "all nine critical external dependencies are `Uncommitted` (§8) — `EXT-HOST-1` returned to `Uncommitted` on 2026-09-09 when its required artifact gained the FR-34 attestation port — and none is `Available`"; §8 814: "At the time of this revision every entry is `Uncommitted`; `EXT-HOST-1`'s earlier target, date, and command are historical only (register)".
- **[high]** CX-3 — The PRD says the seam-2 existence read and the seam-4 event feed are pending register extensions; both have landed (§8 818, 820 ↔ `external-dependency-register.md` 57, 60, 65) — PRD seam 2: "The existence read is not yet in the register's seam-2 text; adding it is a pending register extension (correct-course, 2026-09-09), and `EXT-CONV-AI-1` cannot reach `Available` on a compatibility command that does not exercise it"; seam 4: "The event-feed alternative is a pending register extension (correct-course, 2026-09-09)." Register seam (2): "plus an existence read by `MessageId` returning the message, typed absence, typed `ConversationDeleted`, typed `PrincipalRemovedFromConversation`, or unavailable … and record `LateConfirmed`"; seam (4): "supplied either as an active-Conversation count per tenant/window or as a Conversations-side tenant event feed from which Agents computes the same count, never from Agent Calls (A-4)"; compatibility contract 60 names both; note 65: "*Scope extended 2026-09-09 …*". *Fix:* PRD §8 818: replace the pending sentence with "The register's seam-2 text and compatibility contract carry the existence read (extended 2026-09-09); `EXT-CONV-AI-1` cannot reach `Available` on a command that does not exercise it"; 820: delete the pending sentence or replace with "(register seam 4, extended 2026-09-09)".
- **[high]** CX-4 — The PRD misstates the Spine's assumption range and index (§8.1 836, FR-28 688 ↔ `ARCHITECTURE-SPINE.md` 11, 765, 763–786) — PRD: "(frontmatter `updated: 2026-09-09`; at that date the table holds `ARCH-A-1` through `ARCH-A-10`, with `ARCH-A-5` retired)". Spine at the same `updated:` date: `architecture_assumption_index_version: 3`, "**Architecture Assumption Index:** `ARCH-A-INDEX-3`", table `ARCH-A-1` through `ARCH-A-12` (`ARCH-A-5` retired; `ARCH-A-11` disabled-Agent retry pause, owner Architecture + Product; `ARCH-A-12` `DigestKey` rotation, owner Architecture + Security). The "range is open" clause keeps FR-28 item 9 correct, but the count is false for the second time today (baseline M-X4 was closed at 10, then reopened by the Spine's same-day edits), and `ARCH-A-11`'s retirement condition names Product, so it is a Product-blocking row the PRD does not acknowledge. *Fix:* PRD §8.1 836: cite the index identifier instead of a count — "(frontmatter `updated: 2026-09-09`, `ARCH-A-INDEX-3`; the Spine's index is authoritative for the row set at any moment)" — and add `ARCH-A-11` to the FR-18/FR-3 text per CI-8.
- **[high]** CX-5 — The PRD lists AD-5, AD-22, and AD-12 as open Spine defects; the Spine has corrected all three (§8.1 840–844, OQ-26 996, OQ-27 997, A-7 856, `addendum.md` 48–49 ↔ `ARCHITECTURE-SPINE.md` AD-5 145, AD-7 157, AD-12 189–191, AD-22 257, AD-30 301, Deferred table 778–786) — PRD §8.1: "three are open at this revision, owned by Architecture: AD-5 forbids the system abandonment from `Approved` and `PostingFailed` on a detected removal that FR-18 requires …; AD-22 names the Tenant Agent Administrator as the sole inspection second party and gives export and hold release none …; AD-12 states the Release Operator trigger without the review, minimum sample, and `InsufficientEvidence` rule". Spine AD-5: "System abandonment … now also fires from `Approved` and `PostingFailed` on a detected removal or block … only after the `MessageId` lookup below finds no posted message"; AD-22: "The second party is computed, not declared … the Platform Operator or a second Compliance Inspector … `ExportRequest` requires this same second-party approval before any content read, never post hoc; `LegalHoldRelease` requires the audited approval of a second Compliance Inspector or the Platform Operator"; AD-12: "A Release Operator trigger review is mandatory … using the PRD numerator/denominator exclusions and minimum sample; below that sample the trigger is `InsufficientEvidence` … an overdue review is `TriggerReviewOverdue`"; the "Two-person rule on legal-hold release" Deferred row is gone. OQ-26 ("a deliberate divergence from Spine AD-5, recorded in §8.1 as a Spine defect for Architecture to correct"), OQ-27 ("a Spine AD-12 correction owned by Architecture"), A-7 ("the Spine fixes the bound as non-configurable (AD-5)" — still true, AD-21 251) and the addendum repeat the stale state. The Spine's `.memlog.md` explicitly flags "prd.md section 8.1 still names AD-5, AD-12, and AD-22 as open 'Spine defects' — all three are now corrected". *Fix:* PRD §8.1 840–844: "none is open at this revision; the three recorded on 2026-09-09 (AD-5, AD-12, AD-22) were corrected by the Spine the same day"; OQ-26 and OQ-27: change "for Architecture to correct" / "owned by Architecture" to "corrected in the Spine on 2026-09-09"; addendum 48–49 likewise.
- **[high]** CX-6 — The data-handling record and `DataHandlingVersion` enablement gate exist only in the PRD and epics Story 5.3; Spine AD-10 and the register's Provider Readiness Contract do not know them (FR-4 199, FR-5 209, FR-24 596, FR-33 458–459, OQ-29 999 ↔ `ARCHITECTURE-SPINE.md` AD-10 177, AD-2 125; `launch-readiness-register.md` 209–211; `epics.md` 1361–1364) — PRD FR-4: "A model whose data-handling record is absent or incomplete cannot be enabled for any tenant … The Tenant Agent Administrator accepts a named `DataHandlingVersion` for the tenant before the first activation that selects the model, and again when the version changes; until re-acceptance, calls continue under the previously accepted version for 30 days, after which the model cannot be called"; FR-5 makes "its data-handling record is complete and its current `DataHandlingVersion` accepted for this tenant" an activation-eligibility element. Spine AD-10 capability floor: "`ProviderId`, `ModelId`, display label, enabled state, `SecretReference` and configured state, text-generation capability, context-window and max-output token limits, timeout policy, an optional per-model retry budget, optional safe capability flags including `ProcessingRegion`, input/output pricing … and `CapabilityVersion`" — no data-handling record, no tenant acceptance; `DataHandlingVersion` has zero hits in the Spine. Register Provider Readiness Contract reason codes (`Unconfigured`, `Unpriced`, `InvalidLimits`, …) and the hard-gate list at 211 ("freshness, platform enabled state, tenant enablement, configured/text-generation state, current pricing, secret resolution, Provider health, non-regressed capability version, and valid positive limits") carry no data-handling or acceptance gate. Epics 5.3 owns "the catalog owns and advances `DataHandlingVersion`" but has no AC for the enablement block, the acceptance, or the 30-day grace (see CX-12). A developer following AD-10 and the register builds a readiness result that reports `Callable` for a model whose Conversation Context leaves the platform under unaccepted terms. *Fix:* Spine AD-10: add the data-handling record (four fields, `DataHandlingVersion`, contractual reference) to the capability floor and "data-handling record complete and current version accepted for the tenant (30-day grace after change)" to the hard gates; AD-2 `TenantProviderEnablement`: hold the accepted `DataHandlingVersion` per entry. Register Provider Readiness Contract: add reason codes (e.g. `DataHandlingUnrecorded`, `DataHandlingUnaccepted`) to the additive list and to the hard-gate sentence. PRD-side change: none.

### Medium

- **[medium]** CX-7 — The PRD asserts a non-conformance record exists for work completed against an `Uncommitted` entry; the register records an open Product approval and neither branch (§8 814, FR-21 523, OQ-17 987 ↔ `external-dependency-register.md` 185–194) — PRD §8: "any story already completed against an `Uncommitted` entry carries a non-conformance record in the register"; OQ-17: "Work completed against an `Uncommitted` entry is recorded as non-conformant in the dependency register". Register "Known Consumer Non-Conformance": "Until Product selects one evidence-backed branch, neither disposition is recorded … This open approval condition is blocking provenance, not a fabricated non-conformance finding". Baseline M-X8 is therefore replaced rather than closed: the section now exists, the record does not. *Fix:* PRD §8 814: "the register carries an open Product ruling on whether Story 5.3 consumed `EXT-PROVIDER-1` (Branch A records the non-conformance; Branch B records none)". Register: Product selects the branch (proposal §6 open item).
- **[medium]** CX-8 — Spine AD-2's first paragraph contradicts FR-2 (and the Spine's own correction paragraph) on what sets `MirrorPending`, when `ReadmitPending` exits, and when `BlockVersion` increments (FR-2 154, 163–165 ↔ `ARCHITECTURE-SPINE.md` AD-2 125 vs 127, AD-7 159) — AD-2 125: "a `MirrorPending` flag set whenever a block, clear, or removal record is written and cleared only when the Conversations-side mirror confirms it … `ReadmitPending` is entered on clear and holds until the Conversations mirror confirms re-admission … `BlockVersion` — incremented on every set or clear". PRD: `ExternallyRemoved` is recorded "with no mirror because there is nothing to remove"; clearing "records `ReadmitPending`, and `hexa` re-joins at the membership step of the next Agent Call that reaches it, never at the moment of clearing" (no re-admission mirror exists); "setting it records `Blocked` and increments the `BlockVersion`" (clear cancels lower-version entries, no increment). The Spine's own "Second-update membership correction" (127) and AD-7 159 state the PRD rule and declare themselves "the normative state contract where the earlier AD-2 shorthand differs", so the Spine is self-contradictory on the page a developer reads first. *Fix:* Spine AD-2 125: replace the `ConversationAgentState` clause with the 127 wording. PRD-side change: none.
- **[medium]** CX-9 — Register seam 1 lacks the typed "already absent" removal answer that FR-2's mirror confirmation depends on (§8 817, FR-2 164 ↔ `external-dependency-register.md` 57, 60) — PRD seam 1: "a participant removal for the FR-2 block, the removal answering typed 'already absent' as a confirmed no-op"; FR-2: "A seam answer that `hexa` is already absent confirms the mirror". Register seam (1): "plus a participant-state read for the AI participant and a participant removal limited to the AI participant for the PRD FR-2 removal block" — no typed already-absent answer; the compatibility contract 60 names "AI-participant removal" only. Spine AD-7 159 relies on it ("an already-absent participant confirms the entry"). *Fix:* Register seam (1) and contract 60: "… a participant removal limited to the AI participant that answers a typed already-absent as a confirmed no-op". PRD-side change: none.
- **[medium]** CX-10 — Epics requirements-inventory rows outside the correct-course rewrite set still summarise the pre-2026-09-09 FRs (FR-4 186–201, FR-5 209, FR-12 334, FR-22 542, FR-25 604–612, FR-27 658–670 ↔ `epics.md` 44, 46, 60, 80, 86, 90) — FR4 row: "Authorized administrators can configure the Global Providers Aggregate" (PRD: the Platform Operator; pricing metadata, capability limits, retry budget, data-handling record, `CapabilityVersion` concurrency absent). FR5 row: "selected provider/model state is validated before activation" (PRD: the complete selection-eligibility set incl. tenant enablement and `DataHandlingVersion` acceptance). FR12 row omits the tenant kill switch and the Agents-owned block/`ExternallyRemoved`. FR22 row: "distinguish active, disabled, invalid, pending proposal, failed call, and expired proposal states" (PRD: `Draft`, `Active`, `Disabled`, `Suspended`, invalid configuration, awaiting-decision, posting-failed, failed-call, expired). FR25 row omits every second-update exposure (`Suspended`, markers, `LateConfirmed`, unavailable-call counts, reserved-vs-settled). FR27 row omits the approval-time and pre-post scan of the exact version and the verdict cache. The proposal scoped the rewrite to FR1/7/18/21/26, so this is residual drift, not a missed action; §0 18 still makes the epic set "the single executable backlog". *Fix:* epics inventory rows FR4, FR5, FR12, FR22, FR25, FR27 from PRD 186–201, 209, 334, 542, 604–612, 658–670. PRD-side change: none.
- **[medium]** CX-11 — Epics never name the `Draft` lifecycle state or the `MembershipUnavailable` / `MembershipRejected` typed reasons (FR-3 171, FR-22 542, FR-2 157/160, FR-25 609–610 ↔ `epics.md`: `Draft` 0 hits, `MembershipUnavailable` 0 hits, `MembershipRejected` 0 hits) — PRD FR-3: "The public lifecycle states are `Draft` (provisioned, not yet valid for activation), `Active`, and `Disabled`"; FR-2: an unavailable participant read rejects with `MembershipUnavailable`, and both names "remain posting-failure reasons"; FR-25 exposes them. Epics UX-DR20/UX-DR25 and Stories 5.2/5.7 speak of `active`/disabled only; Story 6.6 2114 says "membership or posting is unavailable … records pending, safe failure, or success" with no typed name, and Story 6.8 (which owns the missing runtime members) does not list them. Spine AD-15 213 and the sprint proposal's debt table ("6.6 owns the Membership gate") carry them. *Fix:* epics: add `Draft` to the FR3 row, UX-DR25, and Story 5.2/5.7 lifecycle ACs; add `MembershipUnavailable` (acceptance rejection and posting-failure reason) and `MembershipRejected` to Story 6.6 or 6.8 owned members. PRD-side change: none.
- **[medium]** CX-12 — Epics Story 5.3 owns `DataHandlingVersion` but no story carries the enablement block, the Tenant Agent Administrator acceptance, or the 30-day grace (FR-4 199, FR-5 209, FR-33 458–459, OQ-29 ↔ `epics.md` 1361–1364, 351, 361; `DataHandlingVersion` 4 hits, "accept" absent near them) — Story 5.3 AC: "the catalog owns and advances `DataHandlingVersion` with the governed capability/pricing record And replay, migration, API/client, UI, and evidence surfaces preserve that version". Nothing in 5.3, 5.5, or 5.7 states "a model whose record is absent or incomplete cannot be enabled for any tenant", the TAA acceptance before first activation and on change, the 30-day continuation, or the FR-24 "record version in force" evidence per Provider attempt. *Fix:* epics: add the enablement-gate AC to Story 5.3, the acceptance/grace AC to Story 5.7 (activation), and the per-attempt evidence clause to Story 6.4; cite OQ-29 in each. PRD-side change: none.

### Low

- **[low]** CX-13 — Spine AD-17 narrows the `UnretiredAssumption` owner set (FR-28 688 "whatever its owner", OQ-24 ↔ `ARCHITECTURE-SPINE.md` AD-17 225 "for every unretired Product, Architecture, or Governance assumption"). The register emitter row 75 says "whatever its owner" and AD-17 defers to the register, so the narrowing is textual. *Fix:* Spine AD-17: "whatever its owner". (PRD OQ-22 has the same wording — CI-15.)
- **[low]** CX-14 — Register vocabulary row describes `PayloadProtectionUnavailable` in host-report terms the PRD forbids as sufficient (FR-34 727 ↔ `launch-readiness-register.md` 53 vs 78) — Register 53: "The host has not reported the production payload-protection engine required by FR-34 as available"; PRD: "A readiness policy that … clears it on a host-asserted signal alone … does not satisfy this requirement". The emitter row 78 has the attestation wording. *Fix:* register 53: "The FR-34 seal/unseal/erase identity-and-version attestation against the committed `EXT-PROTECTION-1` engine has not passed".
- **[low]** CX-15 — Epics vocabulary drift: "`ParticipantType.AiAgent`/`AIAgent`" (144) against A-1 "`AiAgent` is the spelling this PRD uses"; projection inventory "`agent-setup-readiness`, `provider-capability-pricing`" (160) against the register's ratified shipped ids `agent-setup`, `provider-catalog`, `tenant-provider-enablement` (270–272) and Spine AD-17 225. *Fix:* epics 144, 160.
- **[low]** CX-16 — Spine AD-14's attestation summary omits the DEK-destroy and `Erased` replay steps (FR-34 727 ↔ `ARCHITECTURE-SPINE.md` 205 "records a canary protect/unprotect result plus engine identity and version"); the register's `EXT-HOST-1` artifact (89) has the full sequence. *Fix:* Spine 205: "seal, plaintext-absence, unseal, DEK destruction, `Erased` replay, identity and version".
- **[low]** CX-17 — The sprint change proposal preserves "active Epics 5 through 8 and their 29-story structure" and says "No epic or story is added" (frontmatter 39, §2 79) while `epics.md` 32/34 states "exactly 33 stories" with 5.9, 5.10, 6.8, 7.7 added by the earlier same-day `sprint-change-proposal-2026-09-09-prd-validation-follow-through.md`; the proposal's debt table assigns to 5.5/5.4/8.4 what Stories 5.9/5.10 now own. The PRD asserts no story count, and epics reconciles it ("the second proposal's superseded 29-story count is historical"). *Fix:* none required for the PRD; the proposal could carry a one-line note that 33 is current.
- **[low]** CX-18 — Register `LR-PARTY-IDENTITY` is "Invalidated by … Agent identity-link changes" (100); linking is retired (FR-2 148). *Fix:* register 100: "Party identity or provisioning changes".
- **[low]** CX-19 — FR-28 item 9 and §8.1 838 require that "every Architecture-owned row carries a target retirement date"; the Spine table now has the `TargetRetirementDate` column but every Architecture-owned open row reads "TBD — target milestone: …" and the Spine says so ("Architecture-owned open rows remain `TBD` until their co-owners approve a date and therefore block `RQ-1`"). Not a contradiction — the requirement is unmet and self-reported. *Fix:* none in text; Architecture co-owners supply dates (proposal §6).

### Checked clean (cross-artifact)

- `launch-readiness-register.md`: blocker vocabulary `DependencyNotAvailable`, `UnretiredAssumption`, `OpenDecision`, `ProhibitedCostControlPosture`, `PayloadProtectionUnavailable`, `GateOutOfScope`, `TriggerReviewOverdue`, `InsufficientEvidence` present (42–56, 72–81); `RQ-1` NOT-READY rule (19) matches FR-28 690; `LR-PRODUCT-METRICS` (114) and `product-metrics` (288) split as FR-28 items 2 / 691; SM-7 contract (184) matches SM-7 948; `ReleaseQualificationGateSet` metric slice (174) matches FR-28 items 2–3; Platform gates' `AuthorizedProducer` (120–137) matches FR-33 469; kill-switch trigger source and two-miss rule (188) match FR-28 700–701.
- `external-dependency-register.md`: nine entries as §8 812; `Available` semantics (41, 45, 47) match §8 812 and FR-21 525; `EXT-CONV-AI-1` seams 1–6 match §8 817–822 apart from CX-9; A-1, A-2, A-3, A-4, A-15, A-16, A-18, A-21 wording matches; `EXT-PROTECTION-1` compatibility contract covers the FR-34 attestation steps; consumers list is a superset of the proposal's six and matches every epics story that declares the seam.
- `ARCHITECTURE-SPINE.md`: frontmatter binds FR-1..FR-34 / OQ-1..OQ-30 (13–15); AD-5 / AD-7 carry `PostingPending` uninterruptibility, `MessageId` lookup, `LateConfirmed`, `Approved`/`PostingFailed` → `Abandoned`(`RemovedInConversations`), acceptance-time `MembershipUnavailable`; AD-7 clear rule matches FR-2 165 / FR-33 465 (Facilitator cannot clear an Administrator block; `ExternallyRemoved` by TAA or any current Facilitator); AD-12 trigger review, four rates, tuple denominator, scaled minimum sample, `Approved` waits, retry clock pause, `ExpiredWhileSuspended`, `Suspended` exposed; AD-14 names `PayloadProtectionUnavailable`; AD-15 counters include rate limits and every second-update public value; AD-17 emits the six codes; AD-20 matches FR-27 (and the "never weaker" half of FR-26); AD-21 matches FR-28/FR-32 incl. the `MaxConcurrentNonterminalInteractionsPerParty` bound now in FR-32 716; AD-22 matches FR-24/FR-30/FR-33/OQ-30; AD-30 grants `Platform` the `LegalHoldRelease` approver role and confines `ProvisionHexa`; AD-31 matches `EXT-CONV-UI-1`; ARCH-A-6 owner "Architecture + Product" as FR-28 692 states.
- `epics.md`: FR1/FR7/FR18/FR21/FR26 rows, FR29–FR34 rows and coverage-map entries, OQ-24..OQ-30 map, Story 5.4 caller drop with negative AC, Story 6.6 five-state machine and clearing rule, Story 8.1 second-approver step, Stories 7.4/7.5 (and 7.7) `MessageId` lookup and `LateConfirmed`, Story 8.2 prior approval and Story 8.8 computed subject set (uncommitted working tree), UX-DR45 gate/launch-health split, `EXT-CONV-AI-1` declared by 6.1, 6.2, 6.6, 7.1, 7.4, 8.5 — all present and consistent with the PRD text they cite. Story 8.5 AC 2918 matches SM-1..SM-7 and OQ-11. `RQ-1` section 3120 matches FR-28.
- `sprint-change-proposal-2026-09-09-2.md`: `preserves: prd.md without amendment`; does not list `prd.md` under `amends_if_approved`, so the §0 landing rule is not triggered; its §4 OLD/NEW blocks match the register, Spine, and epics text now on disk (with the 29/33 residue in CX-17).

## Baseline disposition

| Prior finding | Disposition | Evidence |
| --- | --- | --- |
| H-I1 `PostingFailed` → `Abandoned` (`SourceConversationUnavailable`) had no row | **Closed** | Row 430; FR-7 252 "every awaiting proposal and every `PostingFailed` proposal" |
| M-I1 `MembershipUnavailable` double use; SM-C4 unclassified outcomes | **Closed** | FR-2 160 "for that re-validation only"; FR-25 610; SM-C4 959–960 |
| M-I2 who clears a block set on external removal | **Closed** (row residue → CI-11) | FR-2 165, FR-33 465 "cleared by the Tenant Agent Administrator or by any current Conversation Facilitator" |
| L-I1 "one row" vs two Facilitator rows | **Closed** | FR-33 453 "the block and clear rows" |
| L-I2 table guards missing prose conditions | **Closed** | Rows 423, 431 |
| L-I3 "then-current" vs "more restrictive" | **Open — verdict Medium, PRD-side fix** | CI-1 |
| L-I4 OQ-16 diagram narrower than FR-2 | **Closed** | OQ-16 986 "`Blocked` may be set from any state" |
| L-I5 §13 amendment marking | **Closed** | OQ-16/OQ-18 cells; preamble names OQ-12 "clarified" |
| L-I6 glossary / UJ-1 lag FR-33 | **Closed** (residue → CI-17, CI-18) | §3 83, 109; UJ-1 49 |
| L-I7 launch-health cadence | **Closed** | §12 934 |
| C-X1 register gates `RQ-1` on SM-1..SM-6, no SM-7 | **Closed** | Register 114, 174, 184, 288 |
| H-X1 hold release / export second party in Spine and epics | **Closed** | Spine AD-22 257, AD-30 301, Deferred table; epics 8.1 2680–2683, 8.2 (working tree) |
| H-X2 inspection second party in Spine | **Closed** | Spine AD-22 257 |
| H-X3 epics inventory stale | **Closed** (residue outside the rewrite set → CX-10, CX-11) | epics 38, 50, 72, 78, 88, 94–104, 272–352 |
| M-X1 Spine AD-5/AD-7 removal after approval | **Closed** | Spine AD-5 145, AD-7 157 |
| M-X2 block clearing authority / five states in Spine and epics | **Closed** (replaced by CX-8 for AD-2's first paragraph) | Spine AD-7 157/159, AD-2 127; epics 6.6 2091–2107 |
| M-X3 `MembershipUnavailable` at acceptance in Spine | **Closed** | Spine AD-7 159; PRD FR-2 160 |
| M-X4 PRD misstates Spine assumption range | **Closed then reopened** → CX-4 | PRD 836 says 1–10; Spine now 1–12, index 3 |
| M-X5 Spine binds and vocabulary lag | **Closed** | Spine 13–15, AD-14 205, AD-15 213, AD-17 227 |
| M-X6 kill-switch trigger in Spine | **Closed** | Spine AD-12 189–191 |
| M-X7 register consumers | **Closed** | Register 63 |
| M-X8 non-conformance record | **Replaced** → CX-7 | Register 185–194 open Product approval |
| M-X9 Platform-scoped readiness observations | **Closed** | FR-33 469 |
| L-X1 AD-15 counters omit rate limits | **Closed** | Spine AD-15 211 |
| L-X2 concurrency bound absent from PRD | **Closed** | FR-32 716, FR-28 item 8 |
| L-X3 `MessageId` read / `LateConfirmed` in register, Spine, epics | **Closed** (PRD text now stale → CX-3) | Register 57; Spine AD-5; epics 7.4/7.5/7.7 |
| L-X4 UX-DR45 | **Closed** | epics 260 |
| Part 0 #17 update-report trail (F-UPD-1) | **Closed** | `update-report-2026-09-09.md` "Proposal-amendment trail" |

Tally: 26 closed (one with residue), 1 open with verdict (L-I3), 2 replaced (M-X4 → CX-4, M-X8 → CX-7).

## Correct-course follow-ups landed / not landed

Source: `update-report-2026-09-09-2.md`, "For correct-course".

| # | Follow-up | Target | Landed? | Evidence | PRD text still correct? |
| --- | --- | --- | --- | --- | --- |
| 1 | Stop gating `RQ-1` on SM-1..SM-6 attainment; add SM-7 contract (C-X1) | LRR | Yes | 114, 174, 184, 288, 315 | Yes |
| 2 | Blocker vocabulary: 8 codes (F-LRR-1, F-LRR-3) | LRR | Yes | 19, 42–56, 72–81 | Yes (CI-16 minor) |
| 3 | `LR-PRODUCT-METRICS` / `product-metrics` split (F-LRR-2) | LRR | Yes | 114, 288 | Yes |
| 4 | Seam 2 `MessageId` existence read with typed answers (F-REG-1) | EDR | Yes | 57, 60 | **No** — §8 818 says "pending register extension" (CX-3) |
| 5 | Seam 4 event-feed alternative | EDR | Yes | 57, 60 | **No** — §8 820 says pending (CX-3) |
| 6 | Seam 5 typed existence/access answers (A-15) | EDR | Yes | 57 | Yes |
| 7 | Seam 1 AI-type Party verification (A-21) | EDR | Yes | 57 | Yes (typed already-absent still missing — CX-9) |
| 8 | `DependencyNotAvailable` semantics (F-REG-2) | EDR | Yes | 47 | Yes |
| 9 | Non-Conformance Records section for Story 5.3 (F-REG-3) | EDR | Partly — section present, disposition open pending Product | 185–194 | **No** — §8 814 / OQ-17 assert the record exists (CX-7) |
| 10 | `EXT-CONV-AI-1` consumers 6.1, 6.2, 6.6, 7.1, 7.4, 8.5 (F-REG-4) | EDR | Yes (superset) | 63 | Yes |
| 11 | `EXT-HOST-1` artifact gains attestation port; returns to `Uncommitted` | EDR | Yes | 89–97, 212 | **No** — §0 16, §8 814 say `Committed` / "eight of the nine" (CX-2) |
| 12 | `EXT-PROTECTION-1` command must exercise seal/unseal/erase/hold-pin/identity | EDR | Yes (contract; command `TBD` by design) | 178 | Yes |
| 13 | Spine frontmatter binds FR-1..FR-34, OQ-1..OQ-30 (F-SPINE-1) | Spine | Yes | 13–15 | Yes |
| 14 | AD-5: system abandon from `Approved`/`PostingFailed`, `MessageId` lookup, uninterruptible `PostingPending` (F-SPINE-2) | Spine | Yes | AD-5 145, AD-7 157 | **No** — §8.1 842, OQ-26, A-7, addendum 48 call AD-5 an open defect (CX-5) |
| 15 | AD-22: computed subject set, hold-release approver, export prior approval (F-SPINE-3) | Spine | Yes | AD-22 257, AD-30 301, Deferred table | **No** — §8.1 843 (CX-5) |
| 16 | AD-12: trigger review, minimum sample, `InsufficientEvidence`, `Approved` waits (F-SPINE-4) | Spine | Yes | AD-12 189–191 | **No** — §8.1 844, OQ-27, addendum 49 (CX-5) |
| 17 | AD-7 retire Party link/replace | Spine | Yes | AD-7 157 | Yes |
| 18 | AD-14 name `PayloadProtectionUnavailable` | Spine | Yes | 203, 205 | Yes |
| 19 | AD-15 rate limits in blocked-call counters | Spine | Yes | 211 | Yes |
| 20 | AD-2 `ConversationAgentState` five states, `BlockVersion`, `MirrorPending` | Spine | Yes (with self-contradiction — CX-8) | 125, 127 | Yes |
| 21 | ARCH-A table: Architecture-owned rows carry a target retirement date | Spine | Partly — column added, values `TBD` pending co-owners | 763–786 | Partly — §8.1 836 count stale (CX-4); date requirement unmet (CX-19) |
| 22 | Epics inventory rows FR1, FR7, FR18, FR21, FR26 rewritten (F-EPICS-1) | Epics | Yes | 38, 50, 72, 78, 88 | Yes |
| 23 | Epics FR29–FR34 rows and map; OQ-24..OQ-30 references | Epics | Yes | 94–104, 330–352 | Yes |
| 24 | Story 5.4 AC drops "caller" | Epics | Yes | 1430, 1435–1438 | Yes |
| 25 | Story 6.6 state machine and clearing rule | Epics | Yes | 2089–2107 | Yes |
| 26 | Story 8.1 second-approver step | Epics | Yes | 2680–2683 | Yes |
| 27 | Stories 7.4/7.5 `MessageId` lookup and `LateConfirmed` | Epics | Yes (plus 7.7) | 2446–2449, 2495–2498, 2610–2618 | Yes |
| 28 | UX-DR45 launch-readiness content | Epics | Yes | 260 | Yes |
| 29 | `update-report-2026-09-09.md` proposal-amendment trail (F-UPD-1) | Update report | Yes | "Proposal-amendment trail" paragraph | Yes |
| 30 | Code side (readiness accepts prohibited postures; `Caller`/`BlockWithAuditableOverride`; `ProposalDetail.razor`; additive members; FR-34 paths) | Source | Not checked (out of this review's artifact set); epics assign it to Stories 5.9, 5.10, 6.8, 7.7 and the debt table | — | n/a |

Landed: 27 of 29 document follow-ups (items 9 and 21 partly, by design: they wait on Product's Branch A/B ruling and on co-owner dates). Not landed: 0 outright. PRD text now stale on 6 of the 27 landed items (CX-2, CX-3, CX-4, CX-5, CX-7).

## Vocabulary tables

Each table lists the canonical spelling, where the PRD defines it, where it is used, and any drift found.

**Conversation Agent State (FR-2, §3 97)**

| Value | Defined | Used | Drift |
| --- | --- | --- | --- |
| `NeverJoined` | §3 97, FR-2 152 | OQ-16, OQ-25, FR-2 164; Spine AD-2/AD-7; epics 6.6 | none |
| `Joined` | §3, FR-2 152–156 | same | none |
| `ExternallyRemoved` | §3, FR-2 154, 156, 165, 167 | FR-11 326, FR-33 465, OQ-16, OQ-25; Spine; epics 6.6 | FR-12 334 omits it (CI-13); Spine AD-7 157 calls it a "system-set `ExternallyRemoved` block" |
| `Blocked` (state) | §3, FR-2 155, 163 | FR-11, OQ-16; Spine; epics | none — distinct from context mode `Blocked` (FR-9 301) and readiness `Blocked` (register 211) by context |
| `ReadmitPending` | §3, FR-2 165 | FR-33 465, OQ-16, OQ-25; Spine AD-2 127, AD-7 159; epics 6.6 | Spine AD-2 125 says it exits on mirror confirmation (CX-8) |
| `BlockVersion` | §3, FR-2 163–165 | FR-33 (implicit), OQ-25; Spine; epics 6.6 | Spine AD-2 125 "incremented on every set or clear" (CX-8) |
| `MirrorPending` | §3, FR-2 164 | FR-25 608, OQ-16, OQ-25; Spine; epics 6.6 | Spine AD-2 125 sets it on clear and removal record too (CX-8) |

**Proposal lifecycle (`ProposedAgentReplyState`, FR-18 410)**

| Value | Class | Used consistently in | Drift |
| --- | --- | --- | --- |
| `Pending`, `Edited`, `Regenerated` | awaiting decision | FR-3, FR-7, FR-18 table, SM-3, OQ-3; Spine AD-5; epics FR18 row, 7.7 | none |
| `Approved`, `PostingPending`, `PostingFailed` | after decision, non-terminal | FR-2, FR-3, FR-18, FR-28, FR-33, OQ-3, OQ-26, OQ-27; Spine; epics | UJ-3 68 "posting" for `PostingPending` (prose, acceptable); §6.1 761 lowercase list complete |
| `Posted`, `Rejected`, `Abandoned`, `Expired` | terminal | same | none |
| `Unknown` | FR-23 sentinel, never recorded | FR-18, FR-9 301, FR-12 342 | none |

**FR-18 transition rows ↔ prose** — 15 rows; every row has a prose statement (415, 435–442, FR-2 167, FR-3 176, FR-7 252–258, FR-28 698) and every prose exit has a row, except: row 433 admin-retry timing (CI-3), row 424 expiry condition (CI-4), FR-7 255 over `PostingFailed` (CI-6), row 429 `Approved`-origin lookup (CI-10).

**Proposal markers and flags (FR-7, FR-18, FR-25)**

| Value | Defined | Used | Drift |
| --- | --- | --- | --- |
| `ResolutionEmptyPending` | FR-7 255 | FR-25 608, OQ-26; Spine AD-15; epics 7.1 | none |
| `ResolutionUnavailable` | FR-7 256 | FR-25 608, SM-C4, OQ-26; Spine; epics 7.1 | none |
| `LateConfirmed` | FR-18 440 | FR-18 432/439, FR-25 608, OQ-26; register seam 2; Spine AD-5; epics 7.4/7.5/7.7 | none |
| `ExpiredWhileSuspended` | FR-18 424 | FR-28 698, SM-3, SM-C5, OQ-27; Spine AD-12; epics 7.6/7.7/8.5 | two recording conditions (CI-4) |

**Typed reasons and outcomes**

| Value | Kind | Defined | Used | Drift |
| --- | --- | --- | --- | --- |
| `NoEligibleApprover` | call rejection; system abandon reason | FR-7 249, 255 | FR-18 435, FR-25, SM-C4, OQ-26; Spine AD-8/AD-13/AD-15; epics 7.1 | Spine AD-13 applies it in Automatic mode (CX-1); FR-7 255 scope (CI-6) |
| `ApproverResolutionUnavailable` | call rejection (unavailable) | FR-7 257 | FR-25 610, SM-C4; Spine AD-15; epics 7.1 | none |
| `SourceConversationUnavailable` | system abandon reason | FR-7 254 | FR-18 430/435, OQ-26; Spine AD-5; epics 7.5 | none |
| `RemovedInConversations` | call rejection; system abandon reason | FR-2 154–155, 160, 167 | FR-18 429/435/442, SM-C4; Spine; epics 6.6 | none |
| `MembershipUnavailable` | call rejection (unavailable); posting-failure reason | FR-2 157, 160 | FR-25 609–610, SM-C4; Spine AD-7/AD-15 | absent from epics (CX-11) |
| `MembershipRejected` | posting-failure reason | FR-2 160 | FR-25 609; Spine AD-7 | absent from epics (CX-11) |
| `ContextUnavailable` | call rejection (unavailable) | — (never defined) | FR-25 610, SM-C4; Spine AD-15; epics 6.2/6.8 | undefined in FR-9 (CI-7) |
| `NotInvoked` | reservation release reason | FR-8 286, FR-28 692 | OQ-6; Spine AD-13/AD-21; epics 6.4 | none |
| `Indeterminate` | Provider attempt outcome | FR-12 342 | FR-4 197, FR-28 692, NFR-11, OQ-6; Spine; epics 6.4 | settlement timing (CI-5) |
| `Unreconciled` | reservation state | FR-28 692 | Spine AD-21, ARCH-A-6 | overlaps `Indeterminate` hold settlement (CI-5) |
| `DependencyNotAvailable` | FR-21 typed outcome; `RQ-1` blocker | FR-21 525 | FR-25 610, FR-28 685/690, FR-34 729, SM-C4, OQ-24; register 47, 74; Spine AD-17; epics FR21 row | none |
| `PayloadProtectionUnavailable` | FR-21 typed outcome; `AgentLaunchReadinessBlocker`; `RQ-1` blocker | FR-21 526, FR-34 727 | FR-25, FR-28, FR-30, SM-C4, OQ-24; register 53, 78; Spine AD-14; epics FR21/FR34 rows | register 53 wording (CX-14) |

**Launch-readiness blockers (FR-28 690, FR-30 621)** — `UnretiredAssumption`, `DependencyNotAvailable`, `OpenDecision`, `InsufficientEvidence` (register vocabulary); `PayloadProtectionUnavailable`, `ProhibitedCostControlPosture` (also `AgentLaunchReadinessBlocker`); `GateOutOfScope`, `TriggerReviewOverdue` (FR-30 surface; unclassified in 690 — CI-16). Register 42–56 and Spine AD-17 227 carry all eight; epics carry all eight across the FR21/FR30/FR34 rows and Stories 5.5/5.9/8.7.

**Context mode (FR-9 301)** — `Full`, `Bounded`, `Blocked`; `Unknown` never recorded. Spine AD-11 enum `Unknown`, `Full`, `Bounded`, `Blocked`; epics FR9 row says "full-or-blocked" (Low, noted in CX-10 family).

**Lifecycle and status (FR-3 171)** — lifecycle `Draft`, `Active`, `Disabled` (+ `Unknown` sentinel); tenant status `Suspended` (FR-25 611). Spine AD-12 exposes `Suspended`; epics lack `Draft` (CX-11).

**Cost-control postures (FR-28 686, NFR-10)** — accepted `Quotas`, `Budgets`, `ProviderModelLimits`; rejected `ReportingOnlyMonitoring`, `AcceptedLaunchRisk` (FR-23 register). Consistent in OQ-6, register 54/77, Spine AD-21, epics 5.9.

**FR-23 deprecate-and-reject register (557)** — `CostControlPosture.ReportingOnlyMonitoring`, `CostControlPosture.AcceptedLaunchRisk`, `ContentSafetyFailureHandling.BlockWithAuditableOverride`, `ApproverPolicySourceKind.Caller`; FR-2 148 and OQ-25 add the Party link/replace commands, which 557 omits (CI-2). Spine AD-7 and epics 5.2/5.4/5.10/8.4 carry all six.

**Dependency statuses (§8 812)** — `Uncommitted`, `Committed`, `Available`; register 37–41 identical. Current register state: all nine `Uncommitted`; PRD §0/§8 say eight (CX-2).

**Roles (FR-33 453)** — Platform Operator (platform-scoped), Tenant Agent Administrator (= glossary "Agent Administrator"), Approver, Conversation Participant, Compliance Inspector, Release Operator; Conversation Facilitator is a Conversations role used as an authority; Security approval is a condition. Spine AD-30 maps the six one-to-one to FrontComposer policies; epics FR33 row lists the six. Non-role decision parties named by the PRD: Release PM, Product (CI-9), Conversations Maintainer, EventStore Maintainer, Platform Maintainer, Agents Runtime Maintainer (owners in §8.1 / FR-34 — planning owners, not runtime roles).

**Approver Policy sources (FR-7 233)** — Conversation Facilitator (wire `ApproverPolicySourceKind.ConversationOwner`), predefined Parties, tenant roles; `Caller` retired. Consistent in §3, OQ-14, Spine AD-8, epics UX-DR5 and Story 5.4.

**Conversations seam typed answers (§8 821, FR-7 253)** — `ConversationDeleted`, `PrincipalRemovedFromConversation`; register seams 2 and 5; Spine AD-5; epics 6.6, 7.4, 7.5, 7.7. Consistent. Seam 1 typed already-absent: PRD only (CX-9).
