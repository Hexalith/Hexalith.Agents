---
title: Hexalith Agents Launch Readiness Register
status: active
created: 2026-08-02
updated: 2026-09-13
project: agents
authority: sprint-change-proposal-2026-09-09-2.md
release_gate: RQ-1
---

# Launch Readiness Register

## Authority And Evaluation Rule

This register is the authoritative machine-testable inventory for Hexalith Agents callability and release qualification. Host configuration, UI state, narrative evidence, or a passing deterministic calculator fixture cannot substitute for a current register record.

`RQ-1` returns READY only when every required record for the evaluated `TenantScope` and `EnvironmentProfile` exists, is fresh, is `Pass`, meets its `RequiredEvidenceLevel`, references qualifying evidence, and uses the current configuration or measurement contract. A missing, stale, blocked, or insufficient record makes the result NOT READY. `RQ-1` is a release gate, not a development story.

`RQ-1` additionally records NOT READY when any `UnretiredAssumption`, `DeferredAssumption`, `OpenDecision`, `DependencyNotAvailable`, `ProhibitedCostControlPosture`, `PayloadProtectionUnavailable`, or applicable overdue-review blocker stands. These blockers implement the single PRD FR-28 input list; they do not add an independent gate or metric.

## Normative Record Schema

| Field | Contract |
| --- | --- |
| `GateId` | Stable identifier from the minimum inventory; never reused for another control. |
| `TenantScope` | Exact tenant or explicitly authorized cohort to which the evidence applies. |
| `EnvironmentProfile` | Versioned environment profile, including `production-like` when used for Levels 4/5 or `RQ-1`. |
| `State` | `Pass`, `Block`, `InsufficientEvidence`, or `Stale`. |
| `Owner` | Named role or person accountable for the gate and evidence source. |
| `SourceVersion` | Immutable configuration, component, policy, fixture, or measurement-contract version observed. |
| `ObservedAt` | UTC instant at which the authoritative observation completed. |
| `ValidUntil` | UTC exclusive upper bound chosen by this gate's contract. No global implicit freshness duration exists. |
| `RequiredEvidenceLevel` | Minimum normative PRD Evidence Level required for the evaluated profile. |
| `EvidenceReference` | Immutable, access-controlled reference to the evidence manifest or result. |
| `ConfigurationOrMeasurementContract` | Versioned configuration values or metric definition, including authoritative timestamps, calculation, sample/window/cohort, late/missing-data, and invalidation rules where measured. |
| `BlockerCode` | Stable support-safe code; never raw content, secrets, exceptions, or Provider payloads. |

### Blocker Code Vocabulary

The following support-safe codes are the closed cross-gate vocabulary for V1. A gate-specific contract may name a narrower reason underneath one of these codes, but a consumer may not invent a peer code. An unknown code fails closed.

| `BlockerCode` | Meaning and use |
| --- | --- |
| `GateRecordMissing` | No record exists for a required gate, scope, and profile. |
| `EvidenceNotRecorded` | The gate has no accepted evidence observation yet. |
| `DependencyUncommitted` | A dependency needed to begin compatibility work has not reached `Committed`. |
| `DependencyNotAvailable` | A dependency consumed by the evaluated qualification profile has not reached `Available`, or its exact compatibility command does not pass. |
| `InsufficientEvidence` | Required samples, timestamps, live evidence, manifest fields, or evidence level are absent. |
| `UnretiredAssumption` | An unretired PRD `A-n` or Architecture `ARCH-A-n` row blocks each evaluation in its recorded affected scope; an `RQ-1` blocker names the row, table, affected evaluation, and evaluated index version. A-28 is `AutomaticModeEnablementEligibility`-only. |
| `OpenDecision` | A deferred PRD decision or versioned Spine `OD-*` record whose contract requires resolution before the evaluated enablement, operation, or qualification has not landed. |
| `GateOutOfScope` | A demanded gate or evidence requirement is not derivable from the authoritative FR-28 input list and is void for `RQ-1` until reconciled. |
| `TriggerReviewOverdue` | A required FR-28 kill-switch trigger review was not recorded within one business day. |
| `SuspensionReviewOverdue` | An SM-4 pull lacks its containment review after two business days, or a review pull is older than 7 days without a recorded review decision. |
| `DeferredAssumption` | The Release PM deferred a late-added Architecture assumption past a scheduled `RQ-1`; the deferral itself blocks until Product accepts it. |
| `PayloadProtectionUnavailable` | The host has not reported the production payload-protection engine required by FR-34 as available; this is also an additive `AgentLaunchReadinessBlocker`. |
| `ProhibitedCostControlPosture` | A recorded posture is `ReportingOnlyMonitoring` or `AcceptedLaunchRisk`; this is also an additive `AgentLaunchReadinessBlocker` and is never collapsed into a missing-posture code. |

`UnretiredAssumption`, `DeferredAssumption`, `DependencyNotAvailable`, `OpenDecision`, and `InsufficientEvidence` are register vocabulary evaluated by `RQ-1`, not Agent aggregate members. `PayloadProtectionUnavailable` and `ProhibitedCostControlPosture` additionally cross the runtime readiness boundary because FR-30 and FR-34 require enablement to fail closed before a document/register lookup.

## State, Freshness, And Invalidation

At an evaluation instant `T`, a record can contribute `Pass` only when `ObservedAt <= T < ValidUntil`, all required fields are populated, the evidence satisfies `RequiredEvidenceLevel`, and `SourceVersion` plus `ConfigurationOrMeasurementContract` still match the deployed/evaluated system.

| Condition | Effective state | Required blocker behavior |
| --- | --- | --- |
| Authoritative control and evidence meet the current contract | `Pass` | `BlockerCode` is empty. |
| The control proves a launch-blocking failure | `Block` | A stable safe reason is required. |
| Required timestamps, samples, live evidence, manifest fields, or evidence level are absent | `InsufficientEvidence` | The missing evidence class is named safely. |
| `T >= ValidUntil`, source/configuration/policy/fixture version changed, or the evidence contract was superseded | `Stale` | Re-observation under the current source and contract is required. |
| No record exists for a required gate/scope/profile | implicit `Block` | Evaluator emits `GateRecordMissing`; absence is never degraded or passing. |

The following emitter contracts are additive to `EvidenceNotRecorded`, `DependencyUncommitted`, and `GateRecordMissing`:

| `BlockerCode` | Emitted by | Contract |
| --- | --- | --- |
| `DependencyNotAvailable` | `RQ-1` evaluation | Emit one code per `EXT-*` record in the qualification profile that is not `Available`, or whose compatibility command did not pass against the exact target; name the record. |
| `UnretiredAssumption` | Scope named by the assumption; `RQ-1` only when included | Emit one code per unretired PRD §8.1 `A-n` or Spine `ARCH-A-n` row whose affected scope includes the evaluation; name the row, table, affected evaluation, and index version, whatever its owner. A-28 emits only for `AutomaticModeEnablementEligibility`. |
| `OpenDecision` | `RQ-1` and applicable operation evaluation | Emit one code per open, missing, malformed, pending, or unapproved materialized `ArchitectureDecisionRecord` whose effective-or-pending `AffectedEvaluations` intersects the gate, dependency, story authorization, Automatic-mode enablement, operation, or qualification being evaluated. This includes `OD-PRD-OQ18-HISTORICAL-SAFETY-1`, `OD-PRD-OQ23-AUTOMATIC-RETRACTION-1`, and `OD-PRD-OQ31-INSTRUCTION-PROTECTION-1`; name the decision id/version and never parse the PRD or Spine at runtime. |
| `ProhibitedCostControlPosture` | `RQ-1` evaluation | Emit when the recorded posture is `ReportingOnlyMonitoring` or `AcceptedLaunchRisk`. |
| `PayloadProtectionUnavailable` | `RQ-1` evaluation, alongside `DependencyNotAvailable` for `EXT-PROTECTION-1` | Emit when the host cannot produce the FR-34 seal/unseal/erase identity-and-version attestation against the committed engine. |
| `GateOutOfScope` | `RQ-1` evaluation | Emit when a register gate or evidence demand is not derivable from FR-28's single input list; name the discrepant gate and remain NOT READY until reconciled. |
| `TriggerReviewOverdue` | `LR-PRODUCT-METRICS` post-enablement trigger-review evaluation | Emit when a mandatory FR-28 trigger review was not convened and recorded within one business day; report it on the FR-30 surface and never treat it as an `RQ-1` metric input. |
| `SuspensionReviewOverdue` | `TenantGovernancePolicy` suspension-review evaluation | Emit when an SM-4 pull has no recorded containment review after two business days, or a review pull is older than 7 days without a recorded review decision; report it on FR-30 and block release or qualification while it stands. |
| `DeferredAssumption` | `RQ-1` evaluation | Emit when the Release PM records a deferral for an `ARCH-A` row added after the scheduled evaluation; name the row, owner, unexercised FR consequences, and revisit date. The record remains a blocker until Product accepts it; expiry without retirement or a new Product-accepted deferral returns the row to `UnretiredAssumption`. Security-owned retirement conditions are never deferrable. |
| `InsufficientEvidence` | Any GateId; specifically `LR-PRODUCT-METRICS` for an unmet metric sample | Emit when required timestamps, samples, live evidence, manifest fields, or evidence level are absent; `RQ-1` propagates NOT READY and names the gate or metric. |

`ObservedAt` and `ValidUntil` are supplied by each gate's authoritative source. Wall-clock observations from different systems are never subtracted to produce browser interaction durations. A corrected evidence record is appended or republished with a new `SourceVersion` or evidence reference; previous evidence remains auditable.

## Open Decision Records

The fixed reserved-system `ArchitectureDecisionCatalog` stream is required before decision-dependent evaluation. Its immutable effective catalog enumerates every stable literal `DecisionId`, minimum/current contract version, and baseline `AffectedEvaluations`; absence or invalid effective root/predecessor authorization emits the existing blocker `OpenDecision` with safe detail `CatalogAbsentOrInvalid`. A successor is `PendingCatalog` and cannot activate until every newly required record exists at the exact minimum contract; effective/pending catalog entries remain unioned until the explicit activation append, and removing/narrowing an entry requires predecessor authorization. The catalog materializes every Spine `OD-*` item and every binding Deferred PRD decision that can block a runtime operation, dependency activation, delivery authorization, Automatic-mode enablement, or `RQ-1`, so a listed but wholly absent decision stream still emits `OpenDecision` without parsing planning documents. Each record is immutable by (`DecisionId`, `DecisionVersion`) and contains contract state (`PendingContract` or `EffectiveContract`), outcome state (`Open` or `Approved`), nonempty `Owner`/`RequiredApprovers`, root/predecessor governance authorization, outcome `ApprovalEvidence`, `AffectedEvaluations` (closed references to GateIds, `EXT-*` records, operation families/variants, story-authorization checks, `AutomaticModeEnablementEligibility`, or `RQ-1`), and a versioned decision contract. `Approved` is valid only when every required role's externally authenticated approval evidence binds the exact effective version and complete contract; the decision contract states any required human-identity separation rather than the register inventing it. Missing fields, unknown state, pending contract, absent required approval, or an older version when the catalog names a newer one is `OpenDecision`; no consumer may infer a choice from an adapter default or planning prose.

| `DecisionId` | `DecisionVersion` | `State` | `Owner` / required approvers | `AffectedEvaluations` | Decision contract / approval evidence |
| --- | --- | --- | --- | --- | --- |
| `OD-HOLD-DELETION-PRECEDENCE-1` | 1 | `Open` | Product + Governance + Security | `RQ-1`; `StoryAuthorization:8.1`; `StoryAuthorization:8.3`; `GovernanceProtection:RecordLegalHoldScopeGuardRegistration`; `GovernanceProtection:HoldPrepare` and its branch/recovery rows; `GovernanceProtection:AuthorizeDeletionDestructionStartBarrier`; `GovernanceProtection:CommitDeletionDestructionStartBarrierEffect`; `GovernanceProtection:AuthorizeDeletionContainmentBatch`; `GovernanceProtection:CommitDeletionContainmentBatchEffect`; `GovernanceProtection:AuthorizeDeletionBatchDispatch`; `GovernanceProtection:CommitDeletionBatchDispatchEffect`; `GovernanceProtection:ReserveAndConsumeDeletionBatchEffect`; `GovernanceProtection:DeletionPurgeRecovery`; `GovernanceProtection:AuthorizeDeletionCompletionBarrier`; `GovernanceProtection:CommitDeletionCompletionBarrierEffect`, each only when its exact pre-seal or post-start hold-overlap branch consumes the decision | Must choose the Product/public outcome and recovery transition both for a contender first registered after `DeletionArmed` but before `DestructionStarted` and for a `PostStartHoldContenderObserved` fact after sealing but before completion. The mechanical ordering is fixed at `GovernanceScopeGuard(TenantId)` through seal/dispatch/completion and, for an already-dispatched batch, at the protection-owner consumption reservation. While Open/missing/stale/mismatched, the exact affected branch blocks; a deletion-allowed outcome must be explicitly applied to each later batch/purge/completion step, while a hold-allowed outcome grants no destructive authority. Ordinary no-contender deletion, no-armed hold, pre-arm deferral, already-reserved/consumed immutable vectors, and mandatory uncommitted-export cleanup remain evaluable. Architecture records no Product outcome or approval evidence. |
| `OD-HOLD-PREPARE-CANCELLATION-1` | 1 | `Open` | Product + Governance + Security | `GovernanceProtection:HoldPrepareBranchDecision` only for `Abort`, `GovernanceProtection:HoldPrepareUnwindRecovery`, `StoryAuthorization:8.1` cancellation branch; not `RQ-1` | Must define whether a still-preparing legal hold can be cancelled before Active, who may authorize it, and the user-visible outcome. While Open, recovery may Resume the original valid hold intent but no dependency failure, timeout, operator retry, or workflow may select Abort or unpin. |
| `OD-OPERATOR-DELETION-CANCELLATION-1` | 1 | `Open` | Product + Governance + Security | `GovernanceProtection:DeletionPrepareBranchDecision` only for operator `Abort`, `GovernanceProtection:DeletionPrepareUnwindRecovery`, the conditional scope-fence-removal/cut-release variants, and Story 8.3 operator-cancellation branch; not `RQ-1` | Must decide whether an already approved operator-origin deletion may cancel or end from a closed permanent-failure class; who requests/approves it; and its public/audit outcome. While Open, operator deletion Resume/retries, no Abort/unwind/fence removal/cut release executes, and all installed fences/cuts remain restrictive. If approval permits Abort, its contract must require cleanup-complete evidence, exact guard removal and owner-cut release before final `DeletionAborted`; if it forbids Abort those variants stay inapplicable. |
| `OD-OPERATOR-DELETION-NONTERMINAL-DISPOSITION-1` | 1 | `Open` | Product + Governance | `GovernanceProtection:ConvergeInteraction` only for an operator deletion containing an awaiting or Approved proposal/nonterminal interaction, `StoryAuthorization:8.3` for that branch; not `RQ-1` | Must choose either a deletion-specific terminal status/reason plus proposal, open-lease, audit, SM-3 and SM-C4 treatment, or require ordinary legal terminality before the operator deletion can proceed. While Open, the admission fence and Closing remain restrictive but the affected owner cannot reach Effective; no Workflow invents rejection, human abandonment, `SourceConversationUnavailable`, or a new public status. Conversation-approved source deletion uses its existing Conversation-deleted transitions and is unaffected. |
| `OD-EXPORT-LIFECYCLE-1` | 1 | `Open` | Product + Governance + Security + Platform Maintainer | `EXT-EXPORT-STORE-1`, `RQ-1`, `ExportRequest`, `ExportDownload`, `GovernanceProtection:HoldPrepare`, `GovernanceProtection:HoldPrepareBranchDecision`, `GovernanceProtection:HoldPrepareResumeRecovery`, `GovernanceProtection:HoldPrepareUnwindRecovery`, `GovernanceProtection:HoldRelease`, `GovernanceProtection:HoldReleaseRecovery` when committed artifacts overlap; `GovernanceProtection:ExportPrepare`, `GovernanceProtection:ExportPrepareBranchDecision`, `GovernanceProtection:ExportPrepareResumeRecovery`, `GovernanceProtection:ExportCommitDecision`, `GovernanceProtection:ExportCommitRecovery`; `GovernanceProtection:DeletionPrepare` only when its fence-owned accepted set contains an export artifact or lifecycle-covered copy | Must fix exact lifetime, later-hold treatment, restore treatment, and storage provider. The common audit/protection gate, deletion-request recording, deletion-inventory acceptance, export-free deletion, and mandatory cleanup of a preparation that wrote nothing or partial output remain evaluable. An export-bearing deletion cannot initially prepare until the exact decision/store binding is recorded; that prepare phase-pins it, so branch decision, destruction, purge, and completion use only the recorded version/store plus direct outcome evidence and never re-enter a later mutable decision union. No approval evidence recorded. |
| `OD-RATE-CONCURRENCY-CONSUMPTION-1` | 1 | `Open` | Product + Architecture | `AgentCallAcceptance:RateAndOpenAdmission`, `StoryAuthorization:6.4` only; not `RQ-1` | Must choose whether a concurrency-rejected original call consumes each rolling-rate window, then fix one strict owner order or common decision and the exact status/FR-25 metric attribution. While Open, evaluation blocks before either rate or open ledger is contacted; no approval evidence recorded. |
| `OD-DAPR-SECURITY-1` | 1 | `Open` | Security + Builds Maintainer | `RQ-1`, `ARCH-A-15` retirement, `StoryAuthorization:6.1`, Dapr Workflow adoption | Must select an atomic parent-authoritative Dapr package-family/gitlink upgrade to `1.18.7+` with compatibility evidence or a bounded Security exception for `1.18.5`; neither the parent-modified but internally clean Builds checkout at `cf52f74` nor prose is approval evidence. |
| `OD-SPRINT-5.1-5.2-1` | 1 | `Open` | Delivery owner + Platform Maintainer | `StoryAuthorization:5.1/5.2-dependent-work` only; not a runtime operation or `RQ-1` input | Must either reconcile the tracker to current dependency/evidence authority or prove historical completion and record the changed-dependency exception plus replacement verifier. It changes no architecture or dependency commitment; no approval evidence recorded. |
| `OD-PRD-OQ18-HISTORICAL-SAFETY-1` | 1 | `Open` | Product + Security | `RQ-1` | Materializes PRD OQ-18 without choosing its outcome. Until approved, V1 keeps the current fail-closed whole-history rule and no per-message redaction/exclusion is inferred. |
| `OD-PRD-OQ23-AUTOMATIC-RETRACTION-1` | 1 | `Open` | Product + Conversations Maintainer | `AutomaticModeEnablementEligibility` only; not global `RQ-1` or Confirmation-mode enablement | Materializes PRD OQ-23's three options without selecting one. The first Automatic-mode tenant cannot be enabled while Open; other modes and global qualification retain the PRD scope. |
| `OD-PRD-OQ31-INSTRUCTION-PROTECTION-1` | 1 | `Open` | Governance + Product | `RQ-1`, `StoryAuthorization:5.8`, `StoryAuthorization:8.3` | Materializes PRD OQ-31 without choosing Agent-level key placement. Until approved, production enablement and the protection/deletion stories cannot claim a complete Agent Instructions/offboarding inventory. |
| `OD-LEGACY-PLAINTEXT-DISPOSITION-1` | 1 | `Open` | Product + Governance + Security + EventStore Maintainer | `InteractionDirectoryMigration:LegacyPlaintextDisposition`, `StoryAuthorization:5.8`, `StoryAuthorization:6.1`, `StoryAuthorization:8.3`, and `RQ-1` only when the frozen deployment inventory contains legacy plaintext | Must choose an explicitly authorized EventStore/snapshot/cache/backup migration-or-eradication contract with any required immutable-history exception, a governed unsupported/decommission posture, or another compliant outcome. While Open, or until a selected outcome has an architecture-amended authorize/effect/result/recovery procedure and all-copy/restore/replay receipts, legacy plaintext remains `PayloadProtectionUnavailable`; exact-empty and already-protected inventories remain evaluable. No outcome or approval evidence is recorded. |
| `OD-INITIAL-OUTPUT-SAFETY-STATUS-1` | 1 | `Open` | Product + Security | `ProviderInvocation:AuthorizeUnderEffectLease`, `ProviderInvocation:RecordOutputSafetyFailureUnderEffectLease`, `StoryAuthorization:6.1`, `StoryAuthorization:6.3`, and `RQ-1`; explicitly not `ProviderInvocation:RecordNotAuthorizedUnderEffectLease` | Must choose the one public terminal `AgentInteractionStatus` and reason/metric/open-lease mapping for an initial generated output denied or unavailable at output safety: `GenerationFailed` or `SafetyFailed`. Both discard output and create no proposal, message, or failure-content record. The generic Provider lease may commit while this authority is Open, but the next interaction-owned branch must record `ProviderInvocationNotAuthorized` and settle all pre-invocation obligations without Provider work; after approval `ProviderInvocationAuthorized` phase-pins the complete mapping and recovery never re-reads a successor. Architecture records no default. |
| `OD-GOVERNANCE-CLASS-RANGE-SCOPE-1` | 1 | `Open` | Product + Governance + Security | `GovernanceScopeV1:ClassUtcRange`, class/range `LegalHold`, `AuditExport`, `DeletionRequest`, StoryAuthorization:8.1/8.2/8.3 for that variant, and `RQ-1` | Must choose the closed `ContentClass` vocabulary/classification owner, authoritative UTC instant, inclusive/exclusive range boundaries, and public scope meaning. Until approved, the class/range variant is rejected before fence/cut/inventory work; exact-interaction scope and the PRD-fixed source-deletion exact-Conversation scope remain evaluable. No implementation may classify or choose time semantics locally. |
| `OD-HUMAN-EXACT-CONVERSATION-SCOPE-1` | 1 | `Open` | Product + Governance + Security | human-origin `GovernanceScopeV1:ExactConversation` for `LegalHold`, `AuditExport`, and operator `DeletionRequest`, StoryAuthorization:8.1/8.2/8.3 for those variants, and `RQ-1` | Must choose point-in-time versus prospective membership, authoritative cut instant, later-derived-content treatment, fence lifetime/removal or explicit permanent-ban outcome, and public/audit wording. While Open, human-origin exact-Conversation scope rejects before fence/cut/inventory work; exact-interaction scope remains evaluable. `ConversationApprovedDeletion` retains its PRD-fixed source-deletion permanent cut and is unaffected. |
| `OD-RELEASE-RECORDER-SCOPE-1` | 1 | `Open` | Product + Release PM + Governance | `RQ-1`, `StoryAuthorization:5.5`, `ArchitectureDecision:PublishCatalog`, `ArchitectureDecision:ActivateCatalog`, `ArchitectureDecision:PublishContract`, `ArchitectureDecision:RecordApproval`; explicitly not `ArchitectureDecision:BootstrapRecorderScope` | Must identify the authoritative tenant/source role assignment for a Release Operator recording Platform-scoped decision packages, or define a separately governed recorder role. Until an independently signed Product + Release PM + Governance resolution is recorded through the one target-limited bootstrap variant, no tenant's Release Operator and no Platform principal may record or activate the catalog, a contract, or approval evidence. The bootstrap records that external choice only and is not an outcome source. |

`ArchitectureDecisionCatalog` plus one `ArchitectureDecisionRecord` stream per listed id are the durable owners; runtime never parses this table or the PRD. The Release Operator recorder publishes only exact externally signed packages verified through `EXT-SECRETS-1` public anchors/trust profile; signing keys and role-assignment authority remain outside Agents/platform-host custody, and the recorder/key custodian cannot mint approver evidence. The recorder's stable actor id must differ from every owner/approval-evidence actor regardless of dual roles. `OD-RELEASE-RECORDER-SCOPE-1` remains Open, so no recording or catalog-activation principal is currently authorized. Genesis requires the independent root-governance manifest and nonempty quorum. A successor catalog/contract remains pending until predecessor governance and required records/approvals are complete; reduced quorums, removed catalog entries, or narrowed evaluations require explicit predecessor authorization. `ArchitectureDecision:ActivateCatalog` records one immutable exact required-record revision/digest manifest on the catalog stream and is the only transition from `PendingCatalog` to effective. Until that activation, previous blockers and effective/pending unions remain. Those unions govern initial work and branch selection; after a durable prepare disposition, human hold-release decision, export commit decision, or guard-owned `DestructionStarted`, dedicated recovery uses only the effective versions recorded by that branch and direct outcome evidence, so later catalog/decision successors cannot strand it. History is never overwritten. `EXT-EXPORT-STORE-1` cannot reach `Available` unless its accepted compatibility evidence names the approved `OD-EXPORT-LIFECYCLE-1` version implemented by the deployed target. A deletion with no overlapping hold may authorize then atomically commit `DestructionSealed` at the EventStore guard; an armed deletion with later hold contention cannot authorize that barrier unless the authorization names the approved `OD-HOLD-DELETION-PRECEDENCE-1` version and outcome.

`ArchitectureDecision:BootstrapRecorderScope` is the sole self-bootstrap exception. It can append only the independently signed exact Product + Release PM + Governance resolution to the fixed `OD-RELEASE-RECORDER-SCOPE-1` stream after the submitting Release Operator is freshly proven by the proposed authority source. Its root package binds source/version, recorder actor, contract/catalog digests, nonempty approvals, separation from approvers/custodian, revocation/expiry evidence, and expected no-stream/current revision. Exact replay/lost acknowledgement reads that one stream; changed evidence conflicts. It cannot publish or activate a catalog or touch another decision, and every ordinary decision command remains blocked until this record is effective. `ExportPreparing` likewise records the exact lifecycle decision version and store target that phase-pin that export before any artifact write: commit/recovery must equal those values, a successor governs only a new prepare, and switching versions requires cleanup-complete Abort then a new preparation rather than relabelling bytes.

**Initial catalog commitment:** The recorder-scope bootstrap record contains exactly one `InitialCatalogVersion` and `InitialCatalogDigest`. When no effective catalog exists, `ArchitectureDecision:PublishCatalog` accepts only that exact version/digest and atomically marks the commitment consumed in the catalog publication evidence; a different catalog, even under the same recorder source or root signer, is rejected. After the first catalog becomes effective, every successor uses only the ordinary effective-predecessor authorization. The bootstrap digest is therefore enforceable authority, not informational evidence.

For `OD-HOLD-DELETION-PRECEDENCE-1`, the catalog's closed baseline affected set is the complete decision row above, including its post-start containment, dispatch, protection reservation, purge, completion, and StoryAuthorization evaluations. An initial catalog omitting any of them, or a successor attempting to narrow them without explicit predecessor-governance authorization, is invalid. This materializes scope only; it does not create a Product outcome.

### Record Authority And Supersession

The EventStore `LaunchReadinessGate` aggregate is the sole readiness-record writer for each logical key (`GateId`, `TenantScope`, `EnvironmentProfile`). Evidence producers, the platform host, UI, and qualification runners submit server-trusted commands; none can assign `State` or update the projection directly. Each accepted immutable observation has a deterministic `ObservationId = H(observation, GateId, TenantScope, EnvironmentProfile, SourceVersion, EvidenceReference, ObservedAt)` computed by the submitting orchestrator with the shared Agents identity canonicalizer (architecture AD-29) and recomputed by the aggregate, which rejects a mismatch; re-observation with an unchanged `SourceVersion` is legal and produces a new revision. EventStore optimistic concurrency serializes each observation at a stream revision exposed as `RegistryRevision`. `TenantScope` is the closed grammar `tenant:<TenantId>` or `platform`; each `GateId` declares a `ScopeKind` below, and evaluation for a tenant reads `platform` records for `Platform` gates and `tenant:<TenantId>` records for `Tenant` gates; no other cohort syntax exists in V1. `EnvironmentProfile` is `<name>@<ProfileVersion>`; a profile version change invalidates every record of the old profile. Each `GateId` binds an `AuthorizedProducer` role; a submission whose principal lacks it is rejected before append, and a `GateId` outside the minimum inventory is rejected at submission.

The `launch-readiness` projection selects each gate observation at the greatest committed stream revision, never the greatest `ObservedAt`. For decisions it first selects the effective/pending catalog union, emits the existing `OpenDecision` code with safe detail `CatalogAbsentOrInvalid` if that fixed stream/effective authorization is absent, and emits `OpenDecision` naming every catalog-listed missing record. It selects each record's greatest `EffectiveContract` plus latest event revision and separately retains every higher `PendingContract`; a pending candidate never supersedes an effective predecessor. Effective/pending catalog and affected-evaluation unions remain active until exact predecessor/root authorization, required records, candidate approval, and activation are durable. An incomplete, invalid, stale, open, pending, or blocking selection fails closed and never falls back to an older Pass/approval or drops a predecessor blocker. For recovery already phase-pinned by an immutable prepare disposition, human hold-release decision, export commit decision, or destruction event, it selects the recorded effective decision versions and direct outcomes only; later successors cannot add a blocker to that branch. `RQ-1` and initial operation evaluators read all required gate/catalog/decision keys from one projection checkpoint; if the checkpoint changes during evaluation, they retry against one new checkpoint or return NOT READY/blocked. Exact duplicate commands are idempotent; conflicting payloads are rejected and audited.

## Minimum Gate Inventory

The following IDs and meanings are normative and closed for V1: implementations may not add, remove, merge away, or weaken entries; a stricter control is expressed as stricter evidence under an existing GateId.

| `GateId` | Governing contract and invalidation trigger |
| --- | --- |
| `LR-TOPOLOGY` | Platform-owned production-like composition, exact component versions, replicated durable security-audit spool, reset/seed/capture/failure-injection procedures, and no conditional skips. Invalidated by host, spool, fixture, component, or procedure changes. |
| `LR-EVENTSTORE` | Live EventStore command/event/projection path, business-state durability, and replay correctness. Invalidated by EventStore target, state-store, schema, subscription, or projection changes. |
| `LR-TENANT-ACCESS` | Fresh tenant access and focused cross-tenant denial for every affected public/runtime path, including trusted-envelope replay-before-target-idempotency and caller-independent content-free denial routing through the durable security recorder. Invalidated by authorization policy, tenant projection, identity, replay/recorder/spool, or role mapping changes. |
| `LR-PARTY-IDENTITY` | Unique active Agent Party identity plus current posting eligibility, authoritative human/liveness classification for Approvers, and versioned historical Party-to-`AuthenticatedHumanActorId` binding. Invalidated by Party state, classification, actor-binding history, identity contract, or Agent identity-link changes. |
| `LR-CONVERSATION-CONTEXT` | Fresh complete authorized Source Conversation read, no truncation/summary/windowing, and exact current budget validation. Invalidated by Conversations read contract, context policy, Provider/model selection, capability version, or tokenizer changes. |
| `LR-CONVERSATIONS-MEMBERSHIP-POSTING` | `EXT-CONV-AI-1` membership, posting, and durable approved-deletion outbox/feed with stable logical signal, ordered backfill, retry-until-Agents-acknowledgement, poison blocking, append idempotency, typed conflicts, attribution, and cross-tenant denial. Invalidated by Conversations target, feed checkpoint/retention, acknowledgement, or compatibility-contract changes. |
| `LR-PROVIDER` | Public Provider readiness result, selected adapter, current pricing/capabilities/limits, deterministic attempt, safe failure, data-handling acceptance/grace state, and usage evidence. Invalidated by Provider/Agent Framework adapter target, catalog entry, secret state, pricing, limits, capability version, tenant acceptance or decline, or data-handling grace expiry. |
| `LR-TOKENIZER` | Selected Provider/model tokenizer measures the exact prepared request and blocks unsupported/missing measurement. Invalidated by Provider/model, tokenizer, template, serialization, or context-policy changes. |
| `LR-SAFETY` | Fresh versioned prompt/context and output safety decisions, always-blocked/restricted policy, no override, and no-weaker retry. Safety-epoch evidence freezes one EventStore position, deterministically enumerates active Agents tenants from provision events with count/hash/high-water, enumerates each caught-up existing-Conversation cohort from content-free `safety-verdict-directory` keys with count/hash/high-water, proves `ProvisionHexa` pending/active-epoch initialization, and conditionally initializes every previously unindexed Conversation through a complete `EXT-CONV-AI-1` read/scan before callability. Invalidated by policy, digest key, rescan profile, directory/index, or adapter target/configuration changes. |
| `LR-SECRETS` | Resolution, rotation, denial, and no-leak evidence through the platform host. Invalidated by secret reference, store, host, access policy, or rotation-procedure changes. |
| `LR-COST` | Current pricing, numeric monthly tenant budget, numeric per-call cap, 80% warning, 100% block, atomic reservation, reconciliation, and retry reuse. Invalidated by pricing, currency/unit, budget, cap, reservation policy, or Provider/model changes. |
| `LR-AUDIT-PROTECTION-DELETION` | Common payload protection, authorized audit/change/security evidence including durable-spool drain, retention, legacy-directory bootstrap/readiness plus EventStore namespace write-fence installation/revocation, target-key-alias interaction permits and creation/start outboxes, Conversation-owned reserved/committed/settled effect leases, Closing/Effective deletion manifests, origin-discriminated deletion audit, the shared hold/export/deletion fence with fence-owned deletion inventory acceptance, durable export-key delivery evidence, cryptographic erasure/redaction, named projection purge, and safe tombstone mechanics. Operation-specific export-store availability and versioned open decisions are evaluated separately through `DependencyNotAvailable`/`OpenDecision`, so their absence cannot make unrelated proposal, policy, or export-free deletion operations fail this common gate. The posted-copy preservation/purge gap remains separately fail-closed for `RQ-1` as `UnretiredAssumption` `ARCH-A-10`; it is not a hidden qualification-only subcondition of this otherwise atomic gate. Invalidated by protection, retention, directory migration/permit/lease/barrier, key-delivery, audit recorder/spool, fence, deletion, or projection-inventory changes. |
| `LR-RECOVERY` | NFR-11 production-like recovery with EventStore RPO 0, no duplicate effects, all terminal decisions preserved, and processing restored within 15 minutes across directory migration/write-fence, interaction permits/create/start outboxes, effect leases/results/settlements, Conversations deletion feed, immutable owner/ordinal cycles, interactions, rate/open/budget decisions, safety epochs, proposal indexes, every deletion fence ordinal/ledger partition/content binding, legal-hold scope-guard registration/release, candidate/accepted inventory acknowledgements, atomic destruction seal, manifest-batch consumption/revocation/exact per-target outcome lookup, export-key delivery, dedicated governance recovery, trusted-envelope replay, security spool, and architecture decisions. Invalidated by workflow, EventStore, Provider attempt, timer, ledger, posting, Conversations feed, interaction directory/migration/lease/guard/barrier, export/protection/secrets adapter, fence, replay/recorder retention, host/spool, or recovery-procedure changes. |
| `LR-CAPACITY-FAIRNESS` | NFR-12 versioned numeric tenant/system concurrency and queue limits, positive admission-preparation timeout, queue-or-reject behavior before Provider invocation, fairness policy, and cost-cap coexistence. Invalidated by profile, admission-preparation timeout, scheduler/admission, Provider, or cost-policy changes. |
| `LR-UI-CONFORMANCE` | NFR-13 WCAG 2.2 AA behavior, whole-string English/French key parity, FrontComposer/Fluent V5 use, and restrictive-viewport blocking for high-impact actions. Invalidated by UI component, localization, route, interaction, or supported-viewport changes. |
| `LR-RUNTIME-PERFORMANCE` | NFR-9 accepted-call/post/proposal, approval/post, and fast pre-Provider rejection percentile gates using at least 30 production-like executions each. Invalidated by runtime path, profile, timestamp source, percentile, cohort, or threshold changes. |
| `LR-UI-PERFORMANCE` | NFR-14 browser-monotonic page usability, authoritative pending acknowledgement, and terminal render/live-region announcement gates using at least 30 production-like executions each. Invalidated by UI path, browser profile, instrumentation seam, threshold, percentile, or sample rules. |
| `LR-PRODUCT-METRICS` | Versioned calculation of the pre-enablement gate metrics SM-1, SM-4, SM-5, and SM-6 and their attainment on the qualification cohort (gate inputs), plus versioned calculation and reporting of the launch-health metrics SM-2, SM-3, SM-7 and counter-metrics SM-C1..SM-C5, which are never `RQ-1` inputs and are reviewed at 30 and 60 days after enablement and monthly thereafter; deterministic fixtures prove formulas only. Invalidated by source event, formula, threshold, cohort, late-data, or insufficiency rules. |

### Gate Scope Kinds And Authorized Producers

| `GateId` | `ScopeKind` | `AuthorizedProducer` (architecture AD-30 principal kind) |
| --- | --- | --- |
| `LR-TOPOLOGY` | Platform | `Platform` |
| `LR-EVENTSTORE` | Platform | `Platform` |
| `LR-TENANT-ACCESS` | Tenant | `User` holding Release Operator, or `Platform` |
| `LR-PARTY-IDENTITY` | Tenant | `User` holding Release Operator, or `Platform` |
| `LR-CONVERSATION-CONTEXT` | Tenant | `User` holding Release Operator, or `Platform` |
| `LR-CONVERSATIONS-MEMBERSHIP-POSTING` | Tenant | `User` holding Release Operator, or `Platform` |
| `LR-PROVIDER` | Tenant | `User` holding Release Operator, or `Platform` |
| `LR-TOKENIZER` | Tenant | `User` holding Release Operator, or `Platform` |
| `LR-SAFETY` | Tenant | `User` holding Release Operator, or `Platform` |
| `LR-SECRETS` | Platform | `Platform` |
| `LR-COST` | Tenant | `User` holding Release Operator, or `Platform` |
| `LR-AUDIT-PROTECTION-DELETION` | Tenant | `User` holding Release Operator, or `Platform` |
| `LR-RECOVERY` | Platform | `Platform` |
| `LR-CAPACITY-FAIRNESS` | Platform | `Platform` |
| `LR-UI-CONFORMANCE` | Platform | `Platform` |
| `LR-RUNTIME-PERFORMANCE` | Platform | `Platform` |
| `LR-UI-PERFORMANCE` | Platform | `Platform` |
| `LR-PRODUCT-METRICS` | Tenant | `User` holding Release Operator, or `Platform` |

The `Platform` assignment of `LR-UI-CONFORMANCE`, `LR-RUNTIME-PERFORMANCE`, and `LR-UI-PERFORMANCE` is an architecture assumption (spine AD-17, 2026-09-09) confirmed or retuned by Product and the Release PM before enablement.

### Gate Sets And Non-Circular Evaluation

The `QualificationExecutionGateSet` contains `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-PARTY-IDENTITY`, `LR-CONVERSATION-CONTEXT`, `LR-CONVERSATIONS-MEMBERSHIP-POSTING`, `LR-PROVIDER`, `LR-TOKENIZER`, `LR-SAFETY`, `LR-SECRETS`, `LR-COST`, `LR-AUDIT-PROTECTION-DELETION`, and `LR-CAPACITY-FAIRNESS`. It permits only explicitly authorized controlled production-like executions used to collect evidence, and only when every external seam executed by that operation is `Available` and its compatibility command passes against the deployed target. `Committed` permits development/contract work but never execution of the seam. This set does not authorize production enablement.

Every controlled execution re-evaluates the applicable operation subset from `OperationGateMatrixVersion = 7`. Versions 1 through 6 remain immutable history; version 7 carries forward every v6 row and adds stable pre-seal destruction identity plus replacement-key compromise arbitration at protection-owner activation. Every public command and every workflow activity declares exactly one operation family and concrete operation variant in its contract; a command with no declaration fails contract tests. This matrix is the single contract consumed by API, BFF, UI, workflow, and the `launch-readiness` projection. A missing family or variant, unknown matrix version, missing or ambiguous target scope, missing record, or GateId outside the minimum inventory blocks with a safe code; consumers may not maintain local subsets.

| Operation family | Required GateIds |
| --- | --- |
| `ProviderCatalogMutation` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-SECRETS`, `LR-COST`, `LR-AUDIT-PROTECTION-DELETION` |
| `AgentSetupMutation` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-PARTY-IDENTITY`, `LR-AUDIT-PROTECTION-DELETION` |
| `AgentActivation` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-PARTY-IDENTITY`, `LR-PROVIDER`, `LR-TOKENIZER`, `LR-SAFETY`, `LR-SECRETS`, `LR-COST`, `LR-AUDIT-PROTECTION-DELETION`, `LR-CAPACITY-FAIRNESS` |
| `AgentCallAcceptance` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-PARTY-IDENTITY`, `LR-CONVERSATION-CONTEXT`, `LR-PROVIDER`, `LR-TOKENIZER`, `LR-SAFETY`, `LR-SECRETS`, `LR-COST`, `LR-AUDIT-PROTECTION-DELETION`, `LR-CAPACITY-FAIRNESS` |
| `ProviderInvocation` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-PARTY-IDENTITY`, `LR-CONVERSATION-CONTEXT`, `LR-PROVIDER`, `LR-TOKENIZER`, `LR-SAFETY`, `LR-SECRETS`, `LR-COST`, `LR-AUDIT-PROTECTION-DELETION`, `LR-CAPACITY-FAIRNESS` |
| `ConversationPosting` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-PARTY-IDENTITY`, `LR-CONVERSATIONS-MEMBERSHIP-POSTING`, `LR-SAFETY`, `LR-AUDIT-PROTECTION-DELETION` |
| `ProposalResolution` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION` |
| `PolicyPublication` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION` |
| `TenantBudgetUpdate` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-COST`, `LR-AUDIT-PROTECTION-DELETION` |
| `LegalHold` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION` |
| `ExportRequest` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-SECRETS`, `LR-AUDIT-PROTECTION-DELETION` |
| `DeletionRequest` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-SECRETS`, `LR-AUDIT-PROTECTION-DELETION` |
| `ReadinessInspection` | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS` |
| `ProposalEdit` (v2) | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION` |
| `ProposalRegeneration` (v2) | same set as `ProviderInvocation` |
| `SystemTimer` (v2) | `LR-EVENTSTORE`; used by expiry, retention-due, reservation-deadline, and queue-expiry activities, which need no human authorization but must not append when EventStore readiness is blocked |
| `LegalHoldRelease` (v2) | same set as `LegalHold` |
| `ExportDownload` (v2) | same set as `ExportRequest` |
| `AuditInspection` (v2) | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION`; covers posted-provenance and compliance inspection |
| `TenantKillSwitch` (v2) | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION` |
| `ReadinessObservation` (v2) | `LR-EVENTSTORE`; the family every readiness-evidence submission declares, accepted only from the gate's `AuthorizedProducer` principal kind |
| `TenantProviderEnablement` (v2) | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-PROVIDER`, `LR-AUDIT-PROTECTION-DELETION` |
| `DataHandlingAcceptance` (v3) | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION` |

#### OperationGateMatrixVersion 7 — Stable Destruction Identity And Replacement-Key Arbitration

Every variant evaluates the scope below. A Tenant row requires a concrete `tenant:<TenantId>`; `tenant:system` is invalid. A Platform row reads only Platform-scoped gate records and never evaluates `LR-TENANT-ACCESS` or another Tenant-only gate. Version 7 carries forward version 6's rule that no subcondition is subtracted from an aggregate readiness record: a bootstrap variant omits the entire circular GateId and instead evaluates the closed, typed `DirectPreconditions` listed below from authoritative owners at expected revisions. A precondition result contains `PreconditionId`, `Pass|Block|Unavailable`, owner source/version/revision, and a safe reason; missing, duplicate, unknown, stale, ambiguous, or unavailable results block. The accepted command records the exact result set. No code-local exception or private subcondition vocabulary is valid.

Security processing has one closed order. After cryptographic/time verification, `TrustedEnvelopeAdmission:RegisterFirstSeen` performs its intrinsic conditional create/exact-existing decision before a target command exists; only an exact replay-record match proceeds. The target command then runs AD-29 idempotency before every mutable readiness or direct-precondition read: an exact replay returns the original authoritative evaluation and outcome without re-running the operation, while the same tuple with a different fingerprint is a conflict. Every ordinary command row follows that order. Replay admission, content-free security recording, and deletion-capability compromise registration are the only named pre-command exceptions and have their own exact-id semantics; no implementation may consult target idempotency before nonce registration. Only a new logical command evaluates current matrix inputs, except that a dedicated recorded-branch recovery variant evaluates only the frozen/direct inputs its row names and never selects a new outcome.

| Operation family / concrete variant | EvaluationScope | Required GateIds in v7 | DirectPreconditions |
| --- | --- | --- | --- |
| `ProviderCatalogMutation:CreateOrRepairEntry` | Platform | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-SECRETS` | `PlatformOperatorAuthorized`, `CatalogTargetIdentityValid`, `ExpectedCatalogRevisionMatches` |
| `ProviderCatalogMutation:ChangeExistingEntry` | Platform | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-SECRETS` | `PlatformOperatorAuthorized`, `CatalogEntryExists`, `ExpectedCatalogRevisionMatches` |
| `PolicyPublication:PlatformSafety` | Platform | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-SECRETS` | `PlatformOperatorAuthorized`, `SecurityApprovalReferenceValid`, `ExpectedPolicyRevisionMatches` |
| `PolicyPublication:TenantGovernance` | Tenant | v3 set unchanged | none |
| `AgentSetupMutation:ProvisionHexa` | Tenant | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION` (omit circular `LR-PARTY-IDENTITY`) | `PartiesDependencyAvailable`, `TenantExists`, `HexaProvisioningStateCompatible` (`Absent` or `ExistingExact`; `ExistingConflict` blocks), `DeterministicPartyProvisionAuthorized`, `ExpectedAgentRevisionMatches`; `ExistingExact` returns the original outcome without another Party or Agent write |
| `AgentSetupMutation:ConfigureExisting` | Tenant | v3 set unchanged | none |
| `TenantBudgetUpdate:InitializeOrRepair` | Tenant | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION` (omit circular `LR-COST`) | `BudgetCurrencyValid`, `NumericLimitsWithinPolicyRange`, `ExpectedGovernanceRevisionMatches` |
| `TenantBudgetUpdate:ChangeExisting` | Tenant | v3 set unchanged | none |
| `TenantProviderEnablement:EnableOrRepair` | Tenant | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION` (omit circular `LR-PROVIDER`) | `CatalogEntryExists`, `CatalogEntryPlatformEnabled`, `DataHandlingRecordComplete`, `ExpectedEnablementRevisionMatches` |
| `TenantProviderEnablement:Disable` | Tenant | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION` (omit `LR-PROVIDER` so a broken Provider can be disabled) | `TenantEnablementEntryExists`, `ExpectedEnablementRevisionMatches` |
| `DataHandlingAcceptance:AcceptOrDecline` | Tenant | v3 set unchanged | `CatalogEntryExists`, `CurrentDataHandlingVersionMatches`, `ExpectedEnablementRevisionMatches` |
| `TenantKillSwitch:ConfirmSm4Incident` | Tenant | none; this containment command attempts only the target `TenantGovernancePolicy` append directly | `TargetTenantValid`, `FreshPlatformOperatorAuthorized`, `SecurityObservationIdAndSourceRevisionExact`, `SecurityEventClassificationCrossTenantOrUnauthorizedAction`, `Sm4IncidentIdValid`, `ContentFreeEvidenceDigestValid`, `ExpectedGovernanceRevisionMatches`, `IdempotencyFingerprintValid`; appends `Sm4IncidentConfirmed` and no switch transition |
| `TenantKillSwitch:RecordTriggerReviewDecision` | Tenant | v3 set unchanged | `FreshReleaseOperatorAuthorized`, `ProductMetricsContractWindowAndCohortExact`, `TriggerThresholdAndMinimumSampleEvaluated`, `DecisionEvidenceValid`, `ExpectedGovernanceRevisionMatches`; appends `TriggerReviewDecisionRecorded` and no switch transition |
| `TenantKillSwitch:PullContainment` | Tenant | none; this containment command attempts only the target `TenantGovernancePolicy` append directly | `TargetTenantValid`, `FreshPlatformOperatorAuthorized`, `RecordedUnconsumedSm4IncidentConfirmationExact`, `ExpectedGovernanceRevisionMatches`, `IdempotencyFingerprintValid`; appends `TenantKillSwitchPulled` binding incident id, target tenant, `CrossTenant` or `UnauthorizedAction` classification, confirming actor, evidence digest, source observation revision, and owner revision |
| `TenantKillSwitch:PullReviewed` | Tenant | v3 set unchanged | `FreshReleaseOperatorAuthorized`, `RecordedCurrentTriggerReviewDecisionExact`, `ExpectedGovernanceRevisionMatches`, `IdempotencyFingerprintValid`; appends `TenantKillSwitchPulled` binding the exact review decision and owner revision |
| `TenantKillSwitch:ReleaseReviewed` | Tenant | v3 set unchanged | `RecordedContainmentOrReviewDecisionValid`, `ExpectedGovernanceRevisionMatches` |
| `TrustedEnvelopeAdmission:RegisterFirstSeen` | Platform; target must be reserved-`system` replay stream (`Issuer`, `DeliveryNonce`) | none; this non-public pre-command security primitive attempts only its target-limited conditional EventStore append and reads no readiness record | `CryptographicEnvelopeVerified`, `SecretsProfileVersionMatches`, `RegistrarCapabilityValid`, `ReplayTargetExact`, `ExpectedReplayRevisionNoStreamOrExistingExact` |
| `SecurityObservation:SpoolAndRecord` | Platform | none; execution kind is the non-public pre-command security-recorder capability, routing is computed as authenticated actor/workflow tenant or reserved `system`, and no readiness record is read | `SecurityRecorderCapabilityValid`, `SecurityObservationIdValid`, `RoutingTenantComputedNotCallerSupplied`, `ContentFreeSchemaValid`, `DurableSpoolAppendOrExactExisting`, `EventStoreAppendOrPendingRetry`; it can mutate only the computed SecurityEventLog day stream |
| `DeletionCapabilityCompromiseAdmission:Register` | none; tenant-targeted non-public registrar, no Agents principal | none; `IDeletionCapabilityCompromiseRegistrar` is composed only into the trusted `EXT-SECRETS-1` emergency-revocation subscriber and reads no readiness record | `AuthenticatedSecretsIssuerAudienceTrustProfileAndPerTenantDeletionCapabilityKeyFamilyExact`, `EmergencyRevocationVersionRevisionAndEventIdentityExact`, `RegistrarCapabilityValid`, `ProtectionTargetTenantKeyVersionAndExpectedBlockIdentityExact`, `ProtectionOwnerBlockOrExistingExactBeforeGuardMirror`, `GovernanceScopeGuardTargetAndExpectedRevisionExact`, `CrossTenantReplayAndChangedPayloadRejected`; installs the tenant/key-version block at `EXT-PROTECTION-1`, then appends only `DeletionBatchCapabilityKeyCompromiseObserved` with the authenticated protection receipt through the pure guard handler. Exact lookup recovers either acknowledgement; unavailable/unknown remains restrictive. The capability cannot sign, issue, re-attest, dispatch, consume, or reach another protection target, stream, event, operation, API, Workflow, target handler, or general dispatcher |
| `ArchitectureDecision:BootstrapRecorderScope` | Platform | none; sole self-bootstrap exception, reads/requires no catalog or prior effective decision, and targets only the versioned-contract literal reserved-system `OD-RELEASE-RECORDER-SCOPE-1` stream without planning-document parse | `IndependentProductReleasePmGovernancePackageValid`, `ProposedRecorderAuthoritySourceIdentityAndVersionExact`, `SubmittingReleaseOperatorFreshlyAuthorizedByProposedSource`, `RecorderActorDistinctFromEveryApprovalActorAndCustodian`, `RootAuthorizationAndNonemptyApprovalsValid`, `RecorderScopeContractAndDigestExact`, `InitialCatalogVersionAndDigestExact`, `PackageExpiryAndRevocationValid`, `ExpectedDecisionRevisionNoStreamOrCurrentExact`, `IdempotencyFingerprintValid`; one append records only the externally decided effective recorder-scope contract/outcome and exact initial-catalog commitment, exact replay/lost acknowledgement reads that stream, changed evidence conflicts, and no catalog or other decision mutation is authorized |
| `ArchitectureDecision:PublishContract` | Platform | none; blocked while `OD-RELEASE-RECORDER-SCOPE-1` is Open and otherwise attempts only the catalog-listed reserved-system decision stream append | `ResolvedRecorderAuthoritySourceAndRoleRevisionMatch`, `ReleaseOperatorRecorderAuthorized`, `EffectiveOrPendingCatalogEntryExact`, `IndependentDecisionTrustProfileValid`, `RootOrEffectivePredecessorGovernanceAuthorizationValid`, `NonemptyRequiredApprovers`, `DecisionVersionMonotonic`, `AffectedEvaluationsClosed`, `ReducedQuorumOrNarrowedAffectedSetExplicitlyAuthorized`, `ExpectedDecisionRevisionMatches`; appends `PendingContract` and cannot supersede the predecessor |
| `ArchitectureDecision:PublishCatalog` | Platform | none; blocked while `OD-RELEASE-RECORDER-SCOPE-1` is Open and otherwise attempts only the catalog stream append | `ResolvedRecorderAuthoritySourceAndRoleRevisionMatch`, `ReleaseOperatorRecorderAuthorized`, `IndependentDecisionTrustProfileValid`, (`NoEffectiveCatalogAndProposedVersionDigestEqualUnconsumedBootstrapInitialCatalogCommitment` or `EffectivePredecessorCatalogAuthorizationValid`), `CatalogVersionMonotonic`, `StableDecisionIdsValid`, `RequiredVersionsAndBaselineAffectedEvaluationsClosed`, `RemovedOrNarrowedEntryExplicitlyAuthorized`, `ExpectedCatalogRevisionMatches`; the first append consumes the exact bootstrap commitment, successors use ordinary predecessor authority, a mismatch changes no state, and every success appends `PendingCatalog` only and can never activate it |
| `ArchitectureDecision:ActivateCatalog` | Platform | none; blocked while `OD-RELEASE-RECORDER-SCOPE-1` is Open and otherwise attempts only the catalog stream append | `ResolvedRecorderAuthoritySourceAndRoleRevisionMatch`, `ReleaseOperatorRecorderAuthorized`, `PendingCatalogVersionAndDigestExact`, `RootOrEffectivePredecessorCatalogAuthorizationValid`, `RequiredDecisionRecordRevisionDigestManifestComplete`, `EveryRequiredRecordContractAtLeastCatalogMinimum`, `NoUnlistedOrConflictingManifestMember`, `ExpectedCatalogRevisionMatches`, `IdempotencyFingerprintValid`; appends `CatalogActivated` with the immutable manifest and makes that catalog effective, exact lost acknowledgement resolves by reading the catalog stream, and a changed manifest/version conflicts |
| `ArchitectureDecision:RecordApproval` | Platform | none; the Release Operator recorder never counts as an owner or approver, including when dual-hatted | `ResolvedRecorderAuthoritySourceAndRoleRevisionMatch`, `ReleaseOperatorRecorderAuthorized`, `IndependentDecisionTrustProfileValid`, `SignedDecisionAuthorityManifestValid`, `ContractEffectiveOrPendingExact`, `ContractDigestAndVersionMatch`, `EveryRequiredApproverEvidenceValidAndDistinctAsRequired`, `RecorderActorDistinctFromEveryApprovalActor`, `RecorderAndCustodianCannotMintApproval`, `ExpectedDecisionRevisionMatches`; `ContractActivated` requires predecessor/root authorization plus complete candidate approval |
| `AutomaticModeEnablementEligibility` | Tenant | v3 `AgentActivation` set unchanged | `ApprovedPrdOq23DecisionVersionMatches`; Open/missing/malformed blocks only first Automatic-mode tenant enablement and never global `RQ-1` or Confirmation mode |
| `AgentCallAcceptance:RateAndOpenAdmission` | Tenant | v3 set unchanged | `ApprovedRateConcurrencyDecisionVersionMatches`, `AdmissionPreparationTimeoutPositiveAndStrictlyLessThanBothFrozenRollingWindows`, `InteractionDirectoryMigrationReadyAndWriteFenceEpochExact`, `NoMatchingDeletionScopeAdmissionFenceAtEventStore`, `NoMatchingRequestScopedOwnerClosingOrEffectiveCut`, `InteractionPermitAndExpectedInteractionRevisionExact`; atomically appends `RateAdmissionAuthorized` plus `OpenInteractionLeaseAuthorized` before either external owner is contacted, and only those exact revisions may start the two-owner prepare protocol. Results, commit/abort decisions, and acknowledgements are separate exact-revision steps and cannot authorize another admission |
| `InteractionDirectoryMigration:Start` | Tenant | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-SECRETS` | `FreshPlatformOperatorAuthorizedForTargetTenant`, `ExactHostEventStoreProtectionSecretsAndAuditSpoolTargetsBound`, `DeterministicMigrationIdAndCutoverContractVersionExact`, `WorkflowInstanceAbsentOrExistingExact`, `IdempotencyFingerprintValid`; starts only the target-limited `InteractionDirectoryMigration` Workflow and grants no plaintext read, data disposition, deletion, or cross-tenant authority |
| `InteractionDirectoryMigration:BeginQuiescedCutover` | Tenant | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-SECRETS`; explicitly omits the circular `LR-AUDIT-PROTECTION-DELETION` gate but no unrelated gate | `RecordedMigrationWorkflowPrincipalTenantCutoverAndInstanceExact`, `CallAndLegacyInteractionWritersQuiescedAcrossExactTopologyCohort`, `ProtectionEngineAvailableAtExactTargetAndAttestationValid`, `AuditSpoolAvailableAndDrainContractExact`, `FiniteTenantEventStoreCheckpointAndLegacyStreamCountHashHighWaterExact`, `ExpectedMigrationRevisionNoStreamOrCurrentExact`, `IdempotencyFingerprintValid`; appends the cutover manifest and proposed migration epoch, records the actual registry checkpoint for retained gates, and authorizes no deletion or resumed intake |
| `InteractionDirectoryMigration:InstallEventStoreWriteFence` | Tenant | none; recorded-cutover bootstrap effect uses direct target evidence | `RecordedMigrationWorkflowCutoverManifestAndEpochExact`, `EventStoreNamespaceGuardTargetAndContractExact`, `LegacyDirectWriteCredentialOrEpochRevocationAuthorized`, `DirectoryPermitAndCommittedEffectCapabilityGrammarExact`, `ExpectedMigrationRevisionMatches`, `IdempotencyFingerprintValid`; installs a tenant namespace guard requiring the current migration epoch plus exact directory permit or committed-effect capability for every later AgentInteraction/GenerationFailureRecord create or content-bearing append, revokes the legacy capability, and records only the authenticated installation/revocation receipt; unknown/unavailable remains pending |
| `InteractionDirectoryMigration:BackfillLegacyMember` | Tenant | none; recorded-cutover recovery reads exact EventStore owners | `RecordedMigrationWorkflowCheckpointManifestFenceEpochAndMemberExact`, `LegacyInteractionOrFailureStreamRevisionAndSourceConversationExact`, `ContentFreeMigratedFromMemberAndCurrentOwnerOutcomeExact`, `ConversationAgentStateExpectedRevisionMatches`, `IdempotencyFingerprintValid`; exact retry is a no-op and changed/cross-tenant/wrong-stream membership conflicts |
| `InteractionDirectoryMigration:CompleteCutover` | Tenant | none; recorded-cutover completion reads exact owners, write-fence receipt, and topology acknowledgements | `EveryManifestMemberBackfilledAtExactRevision`, `EveryLegacySensitiveFieldProtectedUnderAcceptedEngineKeyContractOrInventoryExactlyEmpty`, `EventStoreNamespaceWriteFenceInstalledAndLegacyCapabilityRevokedAtExactEpoch`, `TwoSuccessiveIdenticalPostFenceLegacyInventoryHighWaterPassesOrIndependentlySignedExactEmptyCheckpointManifest`, `NoAcceptedPostFenceLegacyWrite`, `ExpectedMigrationRevisionMatches`, `IdempotencyFingerprintValid`; any legacy plaintext emits `PayloadProtectionUnavailable` under `OD-LEGACY-PLAINTEXT-DISPOSITION-1`, while protected/exact-empty completion appends `DirectoryReady`, resumes only directory-first/epoch-guarded intake, and authorizes Conversation deletion inventory |
| `InteractionDirectoryMigration:ContainRejectedOrAcceptedLateWrite` | Tenant | none; containment append targets only the current migration owner | `RecordedMigrationFenceEpochAndReadyOrPendingRevisionExact`, (`RejectedStaleLegacyWriteAttemptReceiptExact` or `AcceptedPostFenceLegacyWriteIntegrityIncidentExact`), `ExpectedMigrationRevisionMatches`, `IdempotencyFingerprintValid`; a rejected attempt records containment evidence without weakening readiness, while any accepted post-fence write appends `DirectoryInvalidated`, blocks calls/deletion, and requires fence repair plus a complete new reconciliation—such an acceptance can never coexist with passing write-fence evidence |
| `InteractionDirectoryMigration:BeginRepairEpochTransition` | Tenant | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-SECRETS`; repair remains independent of the invalidated audit/protection/deletion readiness record | `RecordedDirectoryInvalidatedIncidentAcceptedStreamAndRevisionExact`, `RecordedMigrationWorkflowPrincipalTenantCutoverAndInstanceExact`, `AllCallLegacyAndDirectoryWritersQuiescedAcrossExactTopologyCohort`, `CurrentOldEpochAndGuardReceiptExact`, `SuccessorFenceOrdinalAssignedOnce`, `ExpectedMigrationRevisionMatches`, `IdempotencyFingerprintValid`; appends only the immutable old/successor epoch plan and directory-repair-fence authorization identity, freezes no directory cohort, and authorizes no new intake or business effect |
| `InteractionDirectoryMigration:InstallDirectoryRepairFence` | Tenant | none; committed repair authorization permits one EventStore directory/effect-authorization namespace guard effect | `RecordedRepairEpochPlanAndAuthorizationRevisionExact`, `EventStoreDirectoryAndEffectAuthorizationNamespaceGuardTargetAndContractExact`, `CurrentDirectoryAppendCapabilityEpochExact`, `EveryActiveDeletionAdmissionFenceOrdinalOwnerCycleContinuousContentBindingHoldContenderDestructionBarrierAndBatchIdentityPreserved`, `RepairFenceIdAndIdempotencyExact`; atomically revokes the old capability for every new permit, creation/workflow-start/User-action-intent outbox, lease acquire/commit, and rate/open/Budget/capacity phase-authorization append, returns the directory/effect-authorization checkpoint, and changes no Agents aggregate; a pre-boundary winner is before the checkpoint, a post-boundary loser appends nothing, and unknown/lost acknowledgement requires exact lookup |
| `InteractionDirectoryMigration:RecordDirectoryRepairFence` | Tenant | none; result records only the exact authorized EventStore outcome | `RecordedRepairFenceAuthorizationRevisionExact`, `AuthenticatedDirectoryRepairFenceEpochCheckpointAndRevocationReceiptExact`, `EventStoreExactOutcomeLookupResolved`, `ExpectedMigrationRevisionMatches`, `IdempotencyFingerprintValid`; appends the receipt and permits no cohort freeze from an unknown result |
| `InteractionDirectoryMigration:FreezeRepairCohort` | Tenant | none; post-fence enumeration reads exact directory/effect owners at the recorded checkpoint | `RecordedDirectoryRepairFenceReceiptAndCheckpointExact`, `FiniteConversationDirectoryPermitCreationWorkflowStartUserActionIntentOutboxEffectLeaseAndRateOpenBudgetCapacityAuthorizationResultAcknowledgementCountHashHighWatersAtCheckpointExact`, `EveryPreFenceWinnerIncludedAndNoPostFenceDirectoryOrPhaseAuthorizationAppend`, `EveryActiveDeletionAdmissionFenceOrdinalViolationGapChainOwnerCycleContinuousContentBindingHoldContenderBarrierContainmentObservedGuardRevisionAuthorizationStaleResultSealBatchIdCapabilityKeyAndConsumedOrRevokedStateExact`, `ExpectedMigrationRevisionMatches`, `IdempotencyFingerprintValid`; appends the immutable repair cohort/bridge manifest only after the boundary, including reserved-intent cancellation/consumption, committed-intent target-result/outbox-ack/lease-settlement, and authorized-unleased-phase result/disposition/acknowledgement obligations |
| `InteractionDirectoryMigration:InstallOldEpochRepairBridge` | Tenant | none; recorded repair authorization uses direct EventStore guard evidence | `RecordedRepairCohortOldSuccessorEpochPlanAndDirectoryFenceExact`, `EventStoreNamespaceGuardTargetAndContractExact`, `OldEpochCapabilityGrammarRestrictedToFrozenPendingCreationWorkflowStartUserActionIntentOutboxesCommittedLeasesAndRecordedUnleasedPhaseAuthorizations`, ``, `NewPermitReservationCommitPhaseAuthorizationAndUnlistedTargetDenied`, `ExpectedMigrationRevisionMatches`, `IdempotencyFingerprintValid`; installs the finite bridge and records its authenticated receipt; unknown/unavailable remains pending and drain cannot begin without the exact receipt |
| `InteractionDirectoryMigration:DrainOldEpochRepairMember` | Tenant | none; frozen-cohort recovery uses only exact owner state, direct target outcomes, and the installed bridge | `RecordedRepairCohortOldEpochBridgeAndMemberExact`, `WorkflowCapabilityAndTargetExact`, (`PendingCreationOutboxTargetOutcomeExact` or `CommittedEffectLeaseTargetOutcomeSettlementAndSettledAcknowledgementExact` or `RecordedUnleasedPhaseAuthorizationResultDispositionAndAcknowledgementExact` or `ReservedLeaseStillUncommittedAndConversationAgentStateExpectedRevisionMatches` or `WorkflowStartOutboxWithoutCommittedStartLeaseAndSuppressionTargetRevisionExact` or `UserActionIntentOutboxOutcomeOrCancelledReservationExact`), `IdempotencyFingerprintValid`; the bridge delivers an exact creation outbox, permits a workflow start only for its already-committed lease, drives an already-authorized rate/open/Budget/capacity phase only to its exact result/disposition/acknowledgement, consumes an uncommitted start through `StartSuppressedByMigrationRepair`, consumes a User-action intent only through its exact committed result or reservation cancellation, changes every other reserved member to `CancelledBeforeCommit`, and never cancels/rebinds a committed member or starts a new phase; unknown remains pending and exact replay/lost acknowledgement reads the named owners |
| `InteractionDirectoryMigration:InstallSuccessorWriteFence` | Tenant | none; recorded repair transition uses direct EventStore guard evidence | `EveryFrozenOldEpochCreationWorkflowStartUserActionIntentOutboxLeaseAndUnleasedPhaseAuthorizationMemberOutcomeOrCancellationAcknowledgedExact`, `RecordedDirectoryRepairFenceCheckpointAndEveryDirectoryEffectAuthorizationHighWaterStillExact`, `NoPostFencePermitLeaseAcquisitionCommitOrPhaseAuthorizationExists`, `SuccessorMigrationWriteEpochIdAndFenceOrdinalExact`, `EventStoreNamespaceGuardTargetAndContractExact`, ``, `OldEpochRepairFenceAndBridgeRevocationAuthorized`, `ExpectedMigrationRevisionMatches`, `IdempotencyFingerprintValid`; atomically makes only the successor epoch current, preserves every active deletion/hold guard, compare-token, and batch guard-issued and referenced protection-owner outcome fact, and revokes the old epoch/repair fence/bridge, so no stale capability survives or is rebound |
| `InteractionDirectoryMigration:CompleteRepair` | Tenant | none; repair completion reads exact migration/directory/effect owners, successor-fence receipt, and topology acknowledgements | `AcceptedLateStreamRevisionBackfilledExact`, `EveryLegacyAndRepairCohortMemberIncludingUnleasedPhaseAuthorizationsReconciledAtExactRevision`, `EveryLegacySensitiveFieldProtectedUnderAcceptedEngineKeyContractOrInventoryExactlyEmpty`, `SuccessorWriteFenceInstalledAndOldEpochDirectoryRepairFenceBridgeRevokedExact`, ``, `TwoSuccessiveIdenticalSuccessorPostFenceDirectoryAndEffectAuthorizationInventoryHighWaterPasses`, `NoAcceptedWriteOrPhaseAuthorizationOutsideCurrentEpochAndExactCapabilityGrammar`, `ExpectedMigrationRevisionMatches`, `IdempotencyFingerprintValid`; appends `DirectoryReady` and resumes only successor-epoch directory-first intake; unknown, legacy plaintext, unresolved member, stale append, or unpreserved guard evidence remains invalidated |
| `AgentCallAcceptance:RegisterInteractionPermit` | Tenant | v3 set unchanged | `PreCreationAuthorizationReadinessConversationAccessAndProtectionGatesPassed`, `NoMembershipReadOrMutationPerformed`, `InteractionDirectoryMigrationReadyAndWriteFenceEpochExact`, `NoMatchingDeletionScopeAdmissionFenceAtEventStore`, `ConversationAgentStateExpectedRevisionMatches`, `InteractionIdLogicalCommandFingerprintAndProtectedPayloadEnvelopeExact`, `TargetInteractionProtectionKeyAliasExactAndTenantBound`, `SourceConversationRevisionExact`, `CreationOutboxTargetLimitedDurableAndCarriesCurrentWriteCapability`, `IdempotencyFingerprintValid`; conditionally appends the permit plus protected target-limited creation outbox; exact retry returns it, and the migration or deletion admission guard rejects a losing append before interaction/workflow/provider content |
| `ConversationEffectLease:Acquire` (`WorkflowStart`, `ApproverResolution`, `ContextRead`, `ConversationMembership`, `ProviderInvocation`, `ProposalMutation`, or `ConversationPosting`) | Tenant | the exact GateId set of the bound downstream operation family/variant in the v3 table; the lease records that closed mapping | `InteractionDirectoryMigrationReadyAndWriteFenceEpochExact`, `NoMatchingDeletionScopeAdmissionFenceAtEventStore`, `InteractionPermitAndTargetOwnerRevisionExact`, `ConversationAgentStateExpectedRevisionMatches`, `NoMatchingRequestScopedOwnerClosingOrEffectiveCut`, `LeaseKindOperationAttemptTargetStepAndIdempotencyIdentityExact`, `DownstreamAuthorizationNotAlreadyRecordedOrExistingExact`, `ClosedContentFreeOwnerAndPhasePinnedControlPlaneEvidenceExact`, `NoProtectedContentOrTargetDependencyReadOrEffectBegun`; appends one deterministic `Reserved` lease/high-water entry and authorizes no effect |
| `ProposalMutation:ReserveUserActionIntent` | Tenant | exact v3 gate set of the edit, regeneration, or resolution variant being requested | `OriginalAuthenticatedUserPrincipalCurrentRoleAndHumanActorBindingExact`, `TargetInteractionConversationOperationVersionAndExpectedRevisionsExact`, `LogicalCommandClientIdempotencyAndPayloadFingerprintExact`, `TargetInteractionProtectionKeyAliasExact`, `EditedBytesSealedUnderTargetDekOrContentStructurallyAbsentForVariant`, `ConversationAgentStateExpectedRevisionMatches`, `NoMatchingDeletionScopeAdmissionFenceAtEventStore`, `NoMatchingRequestScopedOwnerClosingOrEffectiveCut`, `InteractionDirectoryMigrationReadyAndWriteFenceEpochExact`; atomically appends the immutable target-limited intent/outbox plus its `Reserved` lease and authorizes no effect |
| `ConversationEffectLease:CommitToEffect` | Tenant | none; same-owner transition uses the reserved lease's phase-pinned gate evidence | `RecordedReservedLeaseIdentityAcquiringRevisionAndDeterministicTargetStepExact`, `ExactDownstreamPrincipalStillAuthorized`, `EveryClosedContentFreeOwnerLocalPreCommitCheckExact`, `NoProtectedContentTargetMutationSafetyRosterConversationOrOtherDependencyReadOrExternalEffectBegun`, `ConversationAgentStateExpectedRevisionMatches`, `NoMatchingDeletionScopeAdmissionFenceAtEventStore`, `NoMatchingRequestScopedOwnerClosingOrEffectiveCut`, `InteractionDirectoryMigrationReadyAndWriteFenceEpochExact`, `IdempotencyFingerprintValid`; conditionally appends `CommittedToEffect`, and only this revision authorizes the exact phase/result; the admission fence and Closing serialize against it |
| `AgentCallAcceptance:StartWorkflowUnderEffectLease` | Tenant | none; uses the committed `WorkflowStart` lease and owner state | `CommittedWorkflowStartLeaseAndCommitRevisionExact`, `CreationOutboxAndInteractionFirstRevisionExact`, `WorkflowInstanceAbsentOrExistingExact`, `ExpectedInteractionRevisionMatches`; only the exact instance may start, and its first durable checkpoint appends the exact lease `Settled` revision; `CancelledBeforeCommit` consumes the start outbox only through `StartSuppressedByDeletion` |
| `AgentCallAcceptance:ResolveApproversUnderEffectLease` | Tenant | none; Product-fixed Confirmation-mode step 6 uses the committed `ApproverResolution` lease | `CommittedApproverResolutionLeaseAndCommitRevisionExactBeforeConversationsOrPartiesRead`, `TargetInteractionApproverPolicyAndSourceConversationExact`, `ConversationsRosterAccessAndPartiesHumanLivenessBindingResultBoundToLease`, `ResolvedSetOrTypedUnavailableOrEmptyResultAndExpectedInteractionRevisionMatch`; appends the exact step-6 result, settles before Context begins, and is absent in Automatic mode; scheduled proposal rechecks use their own deterministic lease operation identity and the same result/settlement rule |
| `AgentCallAcceptance:ContextAndSafetyUnderEffectLease` | Tenant | none; uses the committed `ContextRead` lease and phase-pinned gate evidence | `CommittedContextReadLeaseAndCommitRevisionExact`, `TargetInteractionAndPreparedAdmissionRevisionsMatch`, `ContextTokenAndSafetyResultBoundToLease`, `ExpectedInteractionRevisionMatches`; result or failure records no unbound content and must settle the lease |
| `AgentCallAcceptance:MembershipUnderEffectLease` | Tenant | none; Product-fixed FR-8 step 10 uses the committed `ConversationMembership` lease | `StepsOneThroughNineSucceededAtExactRevisions`, `CommittedConversationMembershipLeaseAndCommitRevisionExact`, `ProvisionedAgentPartyAndConversationTargetExact`, `RosterReadOrIdempotentJoinResultBoundToLease`, `ExpectedInteractionAndDirectoryRevisionsMatch`; a failure terminalizes before acceptance, and result settlement precedes `AgentCallAccepted` |
| `AttemptBudget:AuthorizeReservation` | Tenant | the v3 Budget/Provider-attempt gate set | `PreparedAttemptDescriptorPricingPeriodAmountAndExpectedInteractionRevisionExact`, `InteractionDirectoryMigrationReadyAndWriteFenceEpochExact`, `NoMatchingDeletionScopeAdmissionFenceAtEventStore`, `NoMatchingRequestScopedOwnerClosingOrEffectiveCut`, `IdempotencyFingerprintValid`; appends `BudgetReservationAuthorized`, whose exact revision is the sole capability to contact `BudgetLedger`, and authorizes no other phase |
| `AttemptBudget:ReserveAndRecord` | Tenant | none; uses only the recorded reservation authorization | `RecordedBudgetReservationAuthorizationRevisionDescriptorPricingPeriodAndAmountExact`, `BudgetLedgerExpectedRevisionAndOutcomeExact`, `ExpectedInteractionRevisionMatches`, `IdempotencyFingerprintValid`; reserves or records the typed negative outcome once, appends the exact acknowledgement/result, and cannot create another reservation authorization |
| `CapacityAdmission:Authorize` | Tenant | the v3 Provider/capacity gate set | `AcceptedAttemptReservationRateOpenAndExpectedInteractionRevisionExact`, `InteractionDirectoryMigrationReadyAndWriteFenceEpochExact`, `NoMatchingDeletionScopeAdmissionFenceAtEventStore`, `NoMatchingRequestScopedOwnerClosingOrEffectiveCut`, `IdempotencyFingerprintValid`; appends `CapacityAdmissionAuthorized`, whose exact revision is the sole capability to acquire or queue, and authorizes no Provider work |
| `CapacityAdmission:AcquireOrQueueAndRecord` | Tenant | none; uses only the recorded capacity authorization | `RecordedCapacityAdmissionAuthorizationRevisionAttemptAdmissionIdentityExact`, `AllocatorExpectedRevisionAndOutcomeOrExactLookupExact`, `ExpectedInteractionRevisionMatches`, `IdempotencyFingerprintValid`; calls the allocator once by the exact authorization, appends `CapacityAdmissionRecorded(Admitted|Queued|Rejected)` with identity/fence where applicable, and cannot start another capacity phase |
| `ProviderInvocation:AuthorizeUnderEffectLease` | Tenant | none; uses the already-committed generic `ProviderInvocation` lease | `CommittedProviderInvocationLeaseAndCommitRevisionExact`, `PreparedDescriptorReservationCapacityFenceAndExpectedInteractionRevisionMatch`, `CapacityAdmissionRecordedAtExactRevision`, `ApprovedInitialOutputSafetyStatusDecisionRevisionContractDigestSelectedStatusReasonMetricOpenLeaseMappingAndCatalogActivationExact`, `ProviderAuthorizationBindsCommitRevisionAndOutputSafetyContractBeforeBeginInvocation`, `NoExistingProviderAuthorizationBranch`, `IdempotencyFingerprintValid`; appends `ProviderInvocationAuthorized` with the immutable output-safety binding, and effect/result/lookup may use only this branch, lease, and binding until settlement; a later successor cannot relabel it |
| `ProviderInvocation:RecordNotAuthorizedUnderEffectLease` | Tenant | none; uses the already-committed generic `ProviderInvocation` lease and records no Provider effect | `CommittedProviderInvocationLeaseAndCommitRevisionExact`, `OutputStatusAuthorityObservedOpenMissingMalformedOrUnavailableAtExactCatalogAndDecisionRevisions`, `NoExistingProviderAuthorizationBranch`, `PreparedReservationAndCapacityIdentityExact`, `AuthenticatedAllocatorLookupProvesNoBeginOrInvocationActive`, `ExpectedInteractionRevisionMatches`, `IdempotencyFingerprintValid`; appends content-free `ProviderInvocationNotAuthorized` plus `BudgetDispositionDecided(NotInvokedRelease)` and `PreProviderCapacityDispositionDecided(CancelNoInvocation)`, then only those exact revisions may release/acknowledge the existing Budget/capacity identities, record the negative target result, and settle the lease. It never calls Provider, invents no-use, or re-reads a successor after this branch records |
| `ProviderInvocation:RecordGenerationFailureUnderEffectLease` | Tenant | none; Provider error/timeout remains inside the committed `ProviderInvocation` lease | `ProviderFailureOrTimeoutOutcomeExact`, `CommittedProviderInvocationLeaseAndCommitRevisionExact`, `FailureReasonProviderEvidenceRevisionAndExpectedInteractionRevisionMatch`, `ProposalVersionIdProposalKindSealedOutputEnvelopeAndLiveGenerationFailureRecordIdentityStructurallyAbsent`, `IdempotencyFingerprintValid`; appends one content-free `GenerationFailed` result on `AgentInteraction`, creates no proposal/message/failure-content stream, and drives Budget/capacity/Provider-lease settlement from that exact revision |
| `ProviderInvocation:RecordOutputSafetyFailureUnderEffectLease` | Tenant | none; uses only the status contract phase-pinned by `ProviderInvocation:AuthorizeUnderEffectLease` | `RecordedProviderAuthorizationOutputSafetyDecisionRevisionContractDigestSelectedMappingAndCatalogActivationExact`, `OutputSafetyDeniedOrUnavailableAtExactVersion`, `CommittedProviderInvocationLeaseAndCommitRevisionExact`, `PhasePinnedSelectedStatusReasonMetricOpenLeaseMappingAndExpectedInteractionRevisionMatch`, `ProposalVersionIdProposalKindSealedOutputEnvelopeAndLiveGenerationFailureRecordIdentityStructurallyAbsent`, `IdempotencyFingerprintValid`; appends only the phase-pinned content-free `GenerationFailed` or `SafetyFailed`, creates no proposal/message/failure-content stream, and drives settlement from that exact revision; a later catalog/decision successor cannot relabel or strand the attempt |
| `ProviderInvocation:RecordGeneratedOutputUnderEffectLease` | Tenant | none; allowed generated output uses its own committed `ProposalMutation` lease under the existing ProviderInvocation gate set | `OutputSafetyAllowedAtExactVersion`, `CommittedGeneratedProposalMutationLeaseAndCommitRevisionExact`, `ProposalVersionIdSealedEnvelopeKindAndExpectedInteractionRevisionMatch`, `ProviderOutcomeRevisionExact`, `IdempotencyFingerprintValid`; appends exactly `RecordGeneratedVersion`, settles after target acknowledgement, and if Closing cancels before commit the returned content is discarded without persistence |
| `ProposalEdit:ApplyUnderEffectLease`, `ProposalRegeneration:ApplyUnderEffectLease`, `ProposalResolution:ApplyUnderEffectLease` | Tenant | none; each uses its committed `ProposalMutation` lease and durable User-action intent | `CommittedProposalMutationLeaseIntentOutboxAndCommitRevisionExactBeforeAnyProtectedVersionOrApprovalSafetyRead`, `OriginalUserRequestOrRecoverCommittedUserProposalMutationWorkflowExact`, `OperationFamilyVariantOriginalActorVersionPayloadDigestAndExpectedInteractionRevisionMatch`, `ActionSpecificCurrentAuthorizationSafetyOrOtherDependencyResultBoundToLeaseWhereRequired`, `MutationOrTypedFailureResultBoundToLease`, `IdempotencyFingerprintValid`; changed family/actor/version/target conflicts, recovery records the original human plus Workflow executor without impersonation, and every positive or negative result acknowledges the intent and must settle the lease |
| `ProposalMutation:RecoverCommittedUserAction` | Tenant | none; non-revocable committed-intent recovery uses exact owner state and direct current authorization/dependency evidence | `ExistingInteractionWorkflowInstanceAndTargetExact`, `CommittedUserActionIntentOutboxLeaseAndMissingTargetAcknowledgementExact`, `OriginalSignedHumanIdentityRoleBindingOperationVersionPayloadDigestAndIdempotencyExact`, `CurrentAuthorizationPartiesConversationSafetyLifecycleAndTargetResultsBoundToLease`, `ExpectedInteractionAndDirectoryRevisionsMatch`; may dispatch only the intent's exact action or typed negative result, may never originate/change an action or satisfy second-party authority, and exact result acknowledgement consumes the outbox before settlement |
| `ConversationPosting:BeginUnderEffectLease` | Tenant | none; uses the committed `ConversationPosting` lease and phase-pinned gate evidence | `CommittedConversationPostingLeaseAndCommitRevisionExactBeforePrePostSafetyOrConversationRead`, `ApprovedVersionPostingAttemptMessageIdDeadlineAndExpectedInteractionRevisionMatch`, `FullPrePostValidationAndTypedResultBoundToLeaseExact`, `BeginPostingBindsCommitRevisionBeforeConversationAppendWhenValidationPasses`; a validation failure records its state-legal no-post result, while a begun post remains committed through external result or authoritative MessageId lookup and settlement |
| `ConversationEffectLease:SettleOrRecover` | Tenant | none; recorded-lease recovery reads no mutable readiness record | `RecordedCommittedLeaseIdentityCommitRevisionAndDeterministicTargetStepExact`, `TargetInteractionAuthorizationAndResultRevisionExact`, `ExternalOutcomeLookupExactWhereEffectMayHaveBegun`, `BudgetCapacityAndPostingSettlementExactWhereApplicable`, `ConversationAgentStateExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends `Settled` only from exact outcome/settlement evidence, unknown/unavailable remains `CommittedToEffect`, and exact lost acknowledgement reads the directory; a cancelled reservation cannot act or become settled as if an effect occurred |
| `GovernanceProtection:AuthorizeLegalHoldScopeGuardRegistration` | Tenant | same set as `LegalHold`; authorizes no hold effect | `RecordedHoldIntentAndInitiatingHumanIdentityExact`, `CanonicalGovernanceScopeV1SchemaPredicateDigestAndMembershipProofExact`, `ClassUtcRangeApprovedDecisionVersionMatchesWhenApplicable`, `ApprovedHumanExactConversationScopeDecisionVersionMatchesWhenApplicable`, `ExpectedLegalHoldRevisionMatches`, `IdempotencyFingerprintValid`; appends the target-limited guard-registration authorization before any `ProtectionFence` prepare/freeze. Human exact-Conversation and class/range reject while their decisions are Open |
| `GovernanceProtection:RegisterLegalHoldScopeGuardEffect` | Tenant | none; exact authorization permits one tenant EventStore scope-guard operation | `RecordedLegalHoldScopeGuardRegistrationAuthorizationRevisionExact`, `TenantGovernanceScopeGuardTargetCanonicalPredicateAndExpectedRevisionExact`, `HoldRequestIdentityScopeAndActorBindingExact`, `IdempotencyFingerprintValid`; serialized with start, batch-status, containment-issue, and completion commits. Before seal it conditionally records `HoldContenderRegistered`. After seal but before completion it conditionally appends `PostStartHoldContenderObserved(..., DestructionAlreadySealed)` with the exact seal and new guard revision; after completion it appends `DeletionAlreadyCompleted`. Every branch is a durable authenticated guard fact, never a read-only absence. Exact retry/lost acknowledgement reads the same result |
| `GovernanceProtection:RecordLegalHoldScopeGuardRegistration` | Tenant | none; records only the authenticated guard result | `RecordedLegalHoldScopeGuardRegistrationAuthorizationRevisionExact`, (`AuthenticatedHoldContenderRegisteredReceiptExact` or `AuthenticatedPostStartHoldContenderObservedDestructionAlreadySealedReceiptExact` or `AuthenticatedDeletionAlreadyCompletedReceiptExact`), `EventStoreExactOutcomeLookupResolved`, `ProtectionFenceAndLegalHoldExpectedRevisionsMatch`, `IdempotencyFingerprintValid`; mirrors the result. A registered or post-start contender is restrictive immediately even if this mirror is lost; an armed overlap and every still-preventable post-start destructive step remain pending while `OD-HOLD-DELETION-PRECEDENCE-1` is Open/missing/mismatched. No row infers the Product outcome |
| `GovernanceProtection:HoldPrepare` | Tenant | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-SECRETS`, `LR-AUDIT-PROTECTION-DELETION` | `RecordedHoldIntentAndHoldContenderRegisteredReceiptExact`, `CanonicalGovernanceScopeV1SchemaPredicateDigestAndMembershipProofExact`, `ClassUtcRangeApprovedDecisionVersionMatchesWhenApplicable`, `ApprovedHumanExactConversationScopeDecisionVersionMatchesWhenApplicable`, `FrozenInteractionAndCommittedArtifactSetFromFenceIndexExact`, `FenceTokenAndExpectedRevisionsMatch`, `InitiatingHumanIdentityMatches`, `NoArmedDeletionOrApprovedHoldDeletionDecisionVersionAndOutcomeMatch`, `NoCommittedArtifactOrApprovedExportLifecycleDecisionAndExactStoreTargetAvailable`; a registered armed overlap with Open/missing/mismatched precedence decision advances neither branch, while ordinary no-armed hold continues |
| `GovernanceProtection:HoldPrepareBranchDecision` | Tenant | none; records no external effect and reads no mutable readiness record | For `Resume`: `OriginalHoldPrepareIntentStillValidAtExactRevision`, `NoAuthorizedCancellationOrAbortFact`, `FrozenSetTokenAndPolicyDecisionVersionsMatch`. For `Abort`: `ApprovedHoldPrepareCancellationDecisionVersionMatches`, `RecordedAuthorizedHoldPrepareCancellationFactValid`; a pin/store/protection failure never satisfies Abort. Both require `PrepareStillReversible`, `DispositionMatchesSourceFact`, `ResumeOrAbortMutuallyExclusive`, and `ExpectedOwnerRevisionMatches`. |
| `GovernanceProtection:HoldPrepareResumeRecovery` | Tenant | none; recorded-branch recovery reads no mutable readiness record | `RecordedPrepareRecoveryDispositionResumeRevisionMatches`, `RecordedHoldPrepareDecisionAndPolicyVersionsMatch`, `WorkflowCapabilityAndTargetExact`, `FenceTokenAndFrozenInteractionArtifactSetMatch`, `RequiredProtectionSecretsAndConditionalExportStoreDirectlyAvailable`, `ExistingDekAndArtifactPinOutcomesLookupExact`; may only resume/acknowledge the recorded prepare |
| `GovernanceProtection:HoldPrepareUnwindRecovery` | Tenant | none; recorded-branch recovery reads no mutable readiness record | `RecordedPrepareRecoveryDispositionAbortRevisionMatches`, `WorkflowCapabilityAndTargetExact`, `FenceTokenAndFrozenInteractionArtifactSetMatch`, `RequiredProtectionSecretsAndConditionalExportStoreDirectlyAvailable`, `ExistingDekAndArtifactUnpinOutcomesLookupExact`; may only unwind the recorded prepare |
| `GovernanceProtection:HoldRelease` | Tenant | same set as `GovernanceProtection:HoldPrepare` | `RecordedHumanReleaseAndSecondPartyApprovalValid`, `FrozenInteractionAndCommittedArtifactSetExact`, `ApprovedExportLifecycleDecisionAndExactStoreTargetIfArtifactsOverlap`, `FenceTokenAndExpectedRevisionsMatch` |
| `GovernanceProtection:HoldReleaseRecovery` | Tenant | none; recorded-branch recovery reads no mutable readiness record | `RecordedHumanReleaseDecisionAndRevisionMatch`, `RecordedEffectiveExportLifecycleDecisionVersionAndExactStoreTargetMatchIfArtifactsOverlap`, `WorkflowCapabilityAndTargetExact`, `FenceTokenAndFrozenInteractionArtifactSetMatch`, `RequiredProtectionSecretsAndConditionalExportStoreDirectlyAvailable`, `ExistingDekAndArtifactUnpinOutcomesLookupExact` |
| `GovernanceProtection:AuthorizeLegalHoldScopeGuardRelease` | Tenant | none; authorizes no guard effect | `RecordedHumanReleaseDecisionAndEveryDekArtifactUnpinAcknowledgementExact`, `ProtectionFenceHoldReleasedAtExpectedRevision`, `RecordedHoldContenderGuardRevisionAndCanonicalPredicateExact`, `ExpectedLegalHoldRevisionMatches`, `IdempotencyFingerprintValid`; appends one target-limited guard-release authorization only after no held member remains |
| `GovernanceProtection:ReleaseLegalHoldScopeGuardEffect` | Tenant | none; exact release authorization permits one tenant scope-guard operation | `RecordedLegalHoldScopeGuardReleaseAuthorizationRevisionExact`, `OriginalHoldContenderIdentityGuardRevisionAndScopeExact`, `ProtectionFenceReleaseReceiptExact`, `NoRemainingPinOrActiveHoldFact`, `GuardExpectedRevisionMatches`, `IdempotencyFingerprintValid`; conditionally records `HoldContenderReleased`, exact retry/lookup returns the same receipt, and deletion remains blocked while result is unknown |
| `GovernanceProtection:RecordLegalHoldScopeGuardReleased` | Tenant | none; records only the exact guard result | `RecordedLegalHoldScopeGuardReleaseAuthorizationRevisionExact`, `AuthenticatedHoldContenderReleasedReceiptAndExactLookupResolved`, `ProtectionFenceAndLegalHoldExpectedRevisionsMatch`, `IdempotencyFingerprintValid`; mirrors final release and authorizes no other hold/deletion outcome |
| `GovernanceProtection:ExportPrepare` | Tenant | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-SECRETS`, `LR-AUDIT-PROTECTION-DELETION` | `RecordedExportIntentAndSecondPartyApprovalValid`, `CanonicalGovernanceScopeV1SchemaPredicateDigestAndMembershipProofExact`, `ClassUtcRangeApprovedDecisionVersionMatchesWhenApplicable`, `ApprovedHumanExactConversationScopeDecisionVersionMatchesWhenApplicable`, `ExportStoreAvailableAtExactTarget`, `ApprovedExportLifecycleDecisionVersionMatches`, `FrozenResourceSetExact`, `PartialOutputInventoryExact`, `FenceTokenAndExpectedRevisionsMatch`; human-origin `ExactConversation` rejects before fence/freeze while `OD-HUMAN-EXACT-CONVERSATION-SCOPE-1` is Open |
| `GovernanceProtection:ExportPrepareBranchDecision` | Tenant | none; records no external effect and reads no mutable readiness record | For `Resume`: `OriginalExportPrepareIntentStillValidAtExactRevision`, `NoAuthorizedCancellationOrAbortFact`. For `Abort`: `RecordedAuthorizedExportCancellationOrProfileMappedClosedFailureFactValid`. Both require `PrepareStillReversibleAndNoKeyDelivered`, `DispositionMatchesSourceFact`, `ResumeOrAbortMutuallyExclusive`, `FenceTokenFrozenSetInventoryLifecycleVersionMatch`, and `ExpectedOwnerRevisionMatches`. |
| `GovernanceProtection:ExportPrepareResumeRecovery` | Tenant | none; recorded-branch recovery reads no mutable readiness record | `RecordedPrepareRecoveryDispositionResumeRevisionMatches`, `RecordedExportPrepareDecisionAndLifecycleVersionMatch`, `WorkflowCapabilityAndTargetExact`, `FenceTokenFrozenSetAndPartialInventoryMatch`, `RequiredStoreProtectionAndSecretsDirectlyAvailable`, `ExistingArtifactManifestIndexKeyOutcomesLookupExact`; may only resume/acknowledge the recorded export |
| `GovernanceProtection:ExportAbortCleanupRecovery` | Tenant | none; mandatory safe-state recovery is independent of hold/deletion precedence and reads no mutable readiness record | `RecordedPrepareRecoveryDispositionAbortRevisionMatches`, `RecordedExportAbortIntentValid`, `WorkflowCapabilityAndTargetExact`, `PartialOutputInventoryAndFenceTokenMatch`, `RequiredStoreAndSecretsDirectlyAvailable`, `EveryWrittenOutputPurgedOrKeyDestroyedWithReceiptOrProvenNeverWritten`; an unknown lookup retains `ExportCleanupPending` |
| `GovernanceProtection:ExportCommitDecision` | Tenant | none; this single-owner decision reads no mutable readiness record after export prepare | `RecordedExportPrepareDispositionResumeOrCompletedRevisionMatches`, `ArtifactManifestAndContentFreeIndexAcknowledgementsExact`, `ExportKeyNotDelivered`, `LifecycleDecisionVersionStoreTargetAndContractExactlyEqualExportPreparing`, `NoArtifactRelabelledAcrossDecisionSuccessor`, `PartialOutputInventoryAndFenceTokenMatch`, `ExpectedProtectionFenceRevisionMatches`, `IdempotencyFingerprintValid`; appends `ExportCommitDecided` under the prepare-pinned lifecycle values and advances the committed-export index high-water in the same `ProtectionFence` append; a changed version/target conflicts and switching requires cleanup-complete Abort plus a new prepare |
| `GovernanceProtection:ExportCommitRecovery` | Tenant | none; recorded-branch completion reads no mutable readiness record | `RecordedProtectionFenceExportCommitDecisionRevisionAndLifecycleVersionMatch`, `WorkflowCapabilityAndTargetExact`, `AuditExportCommitAcknowledgementMissingOrExact`, `ExpectedAuditExportRevisionMatches`, `ArtifactManifestIndexAcknowledgementsExact`, `ExportKeyNotDeliveredBeforeAuditExportAcknowledgement`, `FenceTokenAndFrozenSetMatch`; idempotently appends the secondary `AuditExport` acknowledgement, resolves lost acknowledgement by exact stream lookup, and only then makes `ExportDownload:AuthorizeKeyDelivery` eligible; deletion reads the fence decision/high-water even if this acknowledgement is pending |
| `ExportDownload:AuthorizeKeyDelivery` | Tenant | v3 `ExportDownload` set unchanged | `ComplianceInspectorCurrentlyAuthorizedAndActorBindingExact`, `RecordedExportCommitAcknowledgementRevisionExact`, `ArtifactAvailableAndExclusiveExpiryNotReached`, `PurposeAudienceAndRequesterExact`, `LifecycleStoreAndSecretsContractVersionsEqualExportPreparing`, `DeterministicDeliveryIdValid`, `ExpectedAuditExportRevisionMatches`, `IdempotencyFingerprintValid`; appends content-free `ExportKeyDeliveryAuthorized` and no key material |
| `ExportDownload:DeliverKeyEffect` | Tenant | none; uses only the recorded authorization and direct custodian contract | `RecordedExportKeyDeliveryAuthorizationRevisionAndDeliveryIdExact`, `SignedOneTimeCustodianCapabilityExact`, `RequesterEndpointBoundToAuthenticatedActor`, `CurrentRequesterAuthorityAndExclusiveExpiryPassUnlessOutcomeAlreadyDelivered`, `CustodianContractVersionMatches`, `DeliveryOutcomeAbsentOrExistingExact`; custodian delivery is idempotent by the complete identity, rejects expiry/revocation before release, sends key bytes directly to the principal, and returns only `Delivered(ObservedAt)`, `NotDelivered(Reason)`, or `Unknown/Unavailable` |
| `ExportDownload:RecordOrRecoverKeyDelivery` | Tenant | none; recorded-delivery recovery reads no readiness record but directly revalidates authority/expiry before an unstarted retry | `RecordedDeliveryAuthorizationAndExpectedAuditExportRevisionMatch`, `AuthenticatedCustodianOutcomeLookupByDeliveryIdExact`, `CurrentRequesterAuthorityAndExclusiveExpiryPassIfOutcomeNotDelivered`, `LifecycleStoreAndContractVersionsEqualRecordedAuthorization`, `IdempotencyFingerprintValid`; Delivered is immutable even if observed after later expiry/revocation, NotDelivered may retry the same id only while current authority/expiry pass, expiry/revocation records terminal non-delivery requiring a new audited request, and unknown/unavailable remains pending without inferred success or a second id |
| `ConversationDeletionPropagation:Intake` | Tenant | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-AUDIT-PROTECTION-DELETION` | `TargetLimitedWorkflowPrincipalDerivedFromAuthenticatedSource`, `ConversationsDependencyAvailableAtExactTarget`, `AuthenticatedConversationsDeletionSourceIdentityAndContractVersionMatch`, `StableSignalIdOneToOneWithApprovedSourceDecision`, `TenantConversationSourceRevisionAndApprovalReferenceExact`, `ApprovedPrdOq31DecisionVersionMatches`, `DeterministicDeletionRequestIdMatches`, `ExpectedProtectedDeletionRevisionNoStreamOrExistingExact`, `IdempotencyFingerprintValid`; appends only the `ConversationApprovedDeletion` origin plus its workflow-start outbox to the signal-derived `ProtectedDeletion`, exact replay/lost acknowledgement returns `DeletionSignalAccepted` with the target revision, changed fields conflict and remain unacknowledged; no later variant is legal before that origin/start revision |
| `ConversationDeletionPropagation:AuthorizeDeletionScopeAdmissionFence`, `GovernanceProtection:AuthorizeDeletionScopeAdmissionFence` | Tenant | none; recorded-origin step reads exact directory/migration owners and no mutable readiness record | `RecordedOriginSpecificDeletionRequestWorkflowAndStartRevisionExact`, `CanonicalGovernanceScopeV1SchemaPredicateDigestAndTenantExact`, `ClassUtcRangeApprovedDecisionVersionMatchesWhenApplicable`, `ApprovedHumanExactConversationScopeDecisionVersionMatchesForOperatorOriginWhenApplicable`, `InteractionDirectoryMigrationReadyEpochGuardAndDirectoryEffectAuthorizationNamespaceHighWatersExact`, (`InitialAdmissionFenceBranch(NoPriorAdmissionFenceForRequestAndOrdinalOne, NoContainmentReceiptExpected, NoPriorFenceOwnerCycleOrViolationReceiptExists)` or `SuccessorAdmissionFenceBranch(RecordedPreDestructionAdmissionFenceViolationContainedAndNextOrdinalAssignedOnce, ContainmentReceiptProvesEitherNoGlobalCutCandidateTokenExistedOrEachExactExistingArtifactWasInvalidated, EveryPriorFenceOwnerCycleAndViolationReceiptRetainedExact)`), `ExpectedProtectedDeletionRevisionMatches`, `IdempotencyFingerprintValid`; appends `DeletionScopeAdmissionFenceAuthorized(AdmissionFenceOrdinal)` before candidate enumeration. Only that revision may authorize the matching EventStore effect; the explicit initial branch requires no containment evidence; successor ordinal is the one assigned prior + 1, concurrent/lost containment reuses it, and no prior evidence is erased/relabelled. Human-origin `ExactConversation` rejects while its decision is Open, while source deletion retains its fixed semantics |
| `ConversationDeletionPropagation:InstallDeletionScopeAdmissionFenceEffect`, `GovernanceProtection:InstallDeletionScopeAdmissionFenceEffect` | Tenant | none; committed authorization permits one target-limited EventStore directory/effect-authorization guard effect | `RecordedDeletionScopeAdmissionFenceAuthorizationRevisionAndOrdinalExact`, `EventStoreCanonicalScopePredicateTargetEpochGuardDirectoryAndEffectAuthorizationHighWatersExact`, `FenceIdOrdinalAndIdempotencyIdentityExact`; conditionally installs the scope guard that rejects every later matching `RegisterInteractionPermit`, `ReserveUserActionIntent`, lease `Acquire`/`CommitToEffect`, `RateAdmissionAuthorized`, `OpenInteractionLeaseAuthorized`, `BudgetReservationAuthorized`, or `CapacityAdmissionAuthorized` append under old, bridged, current, restored, or successor authority. A pre-fence append wins before the returned checkpoint, a post-fence loser appends nothing, and unknown/lost acknowledgement requires exact lookup |
| `ConversationDeletionPropagation:RecordDeletionScopeAdmissionFence`, `GovernanceProtection:RecordDeletionScopeAdmissionFence` | Tenant | none; result records only the exact authorized EventStore outcome | `RecordedDeletionScopeAdmissionFenceAuthorizationRevisionAndOrdinalExact`, `AuthenticatedInstalledFenceIdOrdinalCanonicalPredicateEpochGuardDirectoryEffectAuthorizationInstallCheckpointAndReceiptExact`, `OrdinalPartitionInitializedWithNoAcceptedViolationAtInstall`, `EveryPriorOrdinalReceiptStillVisible`, `EventStoreExactOutcomeLookupResolved`, `ExpectedProtectedDeletionRevisionMatches`, `IdempotencyFingerprintValid`; appends the immutable admission-fence receipt and authorizes the finite owner-cohort freeze, while changed or unknown results cannot advance |
| `ConversationDeletionPropagation:InstallBarrierClosing`, `GovernanceProtection:InstallBarrierClosing` | Tenant | none; admission-fence-bound workflow step reads exact directory owners | `RecordedDeletionScopeAdmissionFenceReceiptOrdinalAndDirectoryEffectAuthorizationCheckpointExact`, `OriginSpecificWorkflowCapabilityAndTargetExact`, `FiniteConversationAgentStateOwnerCohortCountHashAndHighWaterExactForCanonicalScope`, `ConversationAgentStateExpectedRevisionMatches`, (`NoCutCycleForOrdinal` or `ExistingSameOrdinalClosingExact`), `EveryPriorOrdinalEffectiveOrClosingReceiptImmutableAndEveryStillRestrictiveObligationCarriedForward`, `EveryNewlyVisibleOrViolatingWinnerIncluded`, `EveryPermitPendingCreationWorkflowStartOrUserActionIntentOutboxReservedOrCommittedEffectLeaseAndRateOpenBudgetCapacityPostingAuthorizationDecisionResultAndAcknowledgementThroughHighWatersInManifest`, `IdempotencyFingerprintValid`; at one owner revision it appends separately keyed `DeletionEffectCut(..., AdmissionFenceOrdinal, Closing)`, serializes against the guarded operations, changes manifested `Reserved` leases to `CancelledBeforeCommit` and consumes their intents, retains committed/authorized work as must-settle, and authorizes no new work or destruction. An earlier ordinal's Effective fact is never reopened or overwritten |
| `ConversationDeletionPropagation:ConvergeInteraction`, `GovernanceProtection:ConvergeInteraction` | Tenant | none; Closing-manifest-bound recovery reads no mutable readiness record | `RecordedAdmissionFenceClosingOwnerManifestAndRevisionMatch`, `OriginSpecificWorkflowCapabilityAndTargetExact`, `InteractionPermitCreationWorkflowStartAndUserActionIntentOutboxIdentitiesExact`, `CommittedEffectLeaseIdentityCommitRevisionAndTargetStepExactWherePresent`, `TargetInteractionExpectedRevisionMatches`, `CurrentStateSpecificOutcomeExact`, `RequiredExternalOutcomeLookupDirectlyAvailableAtExactTargetWhenEffectMayHaveBegun`, `ApprovedOperatorNonterminalDispositionDecisionVersionAndOutcomeMatchesWhenOperatorOriginAndNoOrdinaryTerminalTransitionExists`; may deliver an already-permitted creation, suppress a cancelled start, record an intent-bound User-action result, or complete a committed lease's exact result. Conversation origin may apply only its existing Conversation-deleted terminal transition. Operator origin may terminalize awaiting/`Approved` or otherwise nonterminal work only under the exact approved `OD-OPERATOR-DELETION-NONTERMINAL-DISPOSITION-1` outcome; while Open that owner remains Closing. Provider convergence persists no returned content but records only authenticated usage/no-use/Indeterminate; posting stays committed through AD-5 `MessageId` lookup. It cannot invent rejection, abandonment, `SourceConversationUnavailable`, a new public status, another outcome, cancellation of a committed lease, or an outside-manifest target |
| `ConversationDeletionPropagation:DecideManifestedPreProviderCapacityCancellation`, `GovernanceProtection:DecideManifestedPreProviderCapacityCancellation` | Tenant | none; exact Closing-manifest decision reads allocator and interaction owners | `RecordedClosingManifestQueuedOrAdmittedCapacityIdentityExact`, `AuthenticatedAllocatorOutcomeLookupExact`, `NoProviderInvocationAuthorizedBeginOrActiveOutcomeAtExactInteractionRevision`, `CapacityAdmissionOrQueueStillCancelableExact`, `ExpectedInteractionRevisionMatches`, `IdempotencyFingerprintValid`; appends the mutually exclusive `PreProviderCapacityDispositionDecided(CancelNoInvocation)` and cannot infer no-use from absence or choose an invocation outcome |
| `ConversationDeletionPropagation:ReleaseManifestedPreProviderCapacity`, `GovernanceProtection:ReleaseManifestedPreProviderCapacity` | Tenant | none; exact disposition-bound allocator recovery reads no mutable readiness record | `RecordedPreProviderCapacityCancelNoInvocationDispositionRevisionExact`, `OriginalQueueOrAdmissionIdentityAndAllocatorExpectedRevisionMatch`, `NoProviderAuthorizationOrInvocationOutcomeExists`, `IdempotencyFingerprintValid`; may cancel/release and acknowledge only that existing queue/admission, and forbids acquire, enqueue, invoke, identity change, or outcome invention |
| `ConversationDeletionPropagation:SettleManifestedRateAdmission`, `GovernanceProtection:SettleManifestedRateAdmission` | Tenant | none; exact Closing-manifest decision recovery reads no mutable readiness record | `RecordedClosingManifestInteractionRateAdmissionAndBothScopePreparationsExact`, `ExistingRateAdmissionCommitOrAbortDecisionAndRevisionExact`, `PartyAndConversationRateLedgerExpectedRevisionsMatch`, `IdempotencyFingerprintValid`; may only drive both existing preparations to the recorded decision and acknowledge them |
| `ConversationDeletionPropagation:SettleManifestedOpenInteraction`, `GovernanceProtection:SettleManifestedOpenInteraction` | Tenant | none; exact Closing-manifest decision recovery reads no mutable readiness record | `RecordedClosingManifestInteractionAndCallerPreparationOrLeaseExact`, `ExistingAcceptanceAbortTerminalOrReleaseDecisionAndRevisionExact`, `OpenInteractionLedgerExpectedRevisionMatches`, `IdempotencyFingerprintValid`; may only drive the existing caller state to its recorded decision |
| `ConversationDeletionPropagation:ReleaseManifestedPreProviderBudget`, `GovernanceProtection:ReleaseManifestedPreProviderBudget` | Tenant | none; exact Closing-manifest decision recovery reads no mutable readiness record | `RecordedClosingManifestInteractionReservationAndOriginalPeriodExact`, `ExistingBudgetDispositionDecidedNotInvokedReleaseAndRevisionExact`, `NoProviderInvocationAuthorizedForReservation`, `BudgetLedgerExpectedRevisionMatches`, `IdempotencyFingerprintValid`; may only release and acknowledge the existing reservation |
| `ConversationDeletionPropagation:SettleManifestedProviderBudget`, `GovernanceProtection:SettleManifestedProviderBudget` | Tenant | none; exact Closing-manifest/outcome-bound settlement reads no mutable readiness record | `RecordedClosingManifestCommittedProviderLeaseAndInteractionExact`, `ExistingBudgetReservationOriginalPeriodAndInvocationDispositionExact`, `AuthenticatedProviderUsageNoUseOrIndeterminateOutcomeRevisionExact`, `ExpectedBudgetLedgerRevisionMatches`, `IdempotencyFingerprintValid`; may only settle the existing amount under its recorded disposition |
| `ConversationDeletionPropagation:ReleaseManifestedCapacity`, `GovernanceProtection:ReleaseManifestedCapacity` | Tenant | none; exact Closing-manifest/outcome-bound release reads no mutable readiness record | `RecordedClosingManifestCommittedProviderLeaseInteractionAttemptAndAdmissionExact`, `AuthenticatedProviderOutcomeAndBudgetSettlementRevisionExact`, `ExistingCapacityAdmissionFenceAndExpectedRevisionMatch`, `IdempotencyFingerprintValid`; may only release the existing admission after the committed Provider result |
| `ConversationDeletionPropagation:AcknowledgeDirectoryOutcome`, `GovernanceProtection:AcknowledgeDirectoryOutcome` | Tenant | none; Closing-manifest-bound recovery reads no mutable readiness record | `RecordedAdmissionFenceClosingOwnerManifestAndRevisionMatch`, `OriginSpecificWorkflowCapabilityAndTargetExact`, `PermitOutboxInteractionFailureOrCommittedEffectOutcomeRevisionExact`, `RateOpenBudgetCapacityAndPostingSettlementExactWhereApplicable`, `ConversationAgentStateExpectedRevisionMatches`, `MemberOfFrozenOwnerManifest`, `IdempotencyFingerprintValid`; advances only that exact acknowledgement/high-water, with exact lost-ack lookup |
| `ConversationDeletionPropagation:ActivateBarrierEffective`, `GovernanceProtection:ActivateBarrierEffective` | Tenant | none; recorded same-ordinal Closing branch reads exact manifested outcomes | `RecordedAdmissionFenceOrdinalClosingOwnerManifestAndRevisionMatch`, `EveryCarriedForwardAndCurrentManifestPermitCreationWorkflowStartAndUserActionIntentOutboxOwnerOutcomeAcknowledgedExact`, `EveryManifestReservedLeaseCancelledBeforeCommit`, `EveryManifestCommittedLeaseOutcomeSettlementAndSettledRevisionExact`, `EveryApplicableRateOpenPreProviderBudgetProviderBudgetPreProviderCapacityProviderCapacityAndPostingDecisionAcknowledgedExact`, `NoPostAdmissionFenceMatchingDirectoryOrPhaseAuthorizationAppendExists`, `PostingPendingCountZeroAfterOwnOutcomeLookups`, `ConversationAgentStateExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends immutable request/scope/ordinal `DeletionEffectCutEffective` for that owner and authorizes no destruction by itself; an Effective fact never transitions backward |
| `ConversationDeletionPropagation:RecordDeletionScopeEffectCutEffective`, `GovernanceProtection:RecordDeletionScopeEffectCutEffective` | Tenant | none; global cut record reads exact same-ordinal owner and admission-fence evidence only | `RecordedAdmissionFenceIdOrdinalReceiptCanonicalPredicateInstallCheckpointAndFiniteOwnerCohortExact`, `EveryOwnerSameOrdinalDeletionEffectCutEffectiveRevisionAndManifestHashExact`, `EveryPriorOrdinalCutAndViolationReceiptRetainedExact`, `AuthenticatedCurrentOrdinalZeroAcceptedViolationsSinceInstallAtVerifiedHighWater`, `NoMissingUnknownOrChangedOwner`, `ExpectedProtectedDeletionRevisionMatches`, `IdempotencyFingerprintValid`; appends the global `DeletionScopeEffectCutEffective` receipt binding the fence id/ordinal/install checkpoint/zero-since-install verified high-water, after which a reversible candidate may be built |
| `GovernanceProtection:BuildDeletionInventoryCandidate` | Tenant | same set as v3 `DeletionRequest`; builds no destructive authority | `RecordedOperatorDeletionRequestDistinctInspectorApprovalAndWorkflowStartRevisionExact`, `GovernanceProtectionWorkflowPrincipalTargetTenantAndStableRequestIdExact`, `CanonicalGovernanceScopeV1PredicateDigestAndCandidateOrdinalExact`, `ApprovedPrdOq31DecisionVersionMatches`, `RecordedGlobalDeletionScopeEffectCutEffectiveReceiptAdmissionFenceOrdinalAndZeroViolationCheckpointExact`, `AuthenticatedCurrentOrdinalZeroAcceptedViolationsSinceInstallProofStillExact`, `CompleteInteractionFailureExportCopyProjectionWorkflowAndFenceIndexCandidateExact`, `EveryNewlyDiscoveredResourceAuthoritativelyProvenInsidePredicate`, `ExpectedProtectedDeletionRevisionMatches`, `IdempotencyFingerprintValid`; appends or supersedes a reversible candidate and never expands scope or authorizes preparation |
| `ConversationDeletionPropagation:FreezeDeletionSet` | Tenant | none; recorded global-cut branch builds a reversible candidate from exact owners | `RecordedGlobalDeletionScopeEffectCutEffectiveReceiptCanonicalPredicateAdmissionFenceOrdinalAndZeroViolationCheckpointExact`, `AuthenticatedCurrentOrdinalZeroAcceptedViolationsSinceInstallProofStillExact`, `InteractionDirectoryMigrationReadyAndAdmissionFenceStillExact`, `EveryOwnerManifestOutcomeAcknowledgedExact`, `CompleteInteractionFailureExportCopyProjectionWorkflowAndFenceIndexCandidateExact`, `ExpectedProtectedDeletionRevisionMatches`, `IdempotencyFingerprintValid`; appends or supersedes a reversible candidate and authorizes no preparation |
| `ConversationDeletionPropagation:AcceptDeletionInventory`, `GovernanceProtection:AcceptDeletionInventory` | Tenant | none; fence-owned expected-revision decision reads exact current protection, migration, and admission-violation owners | `RecordedOriginCompatibleDeletionCandidateRevisionExact`, `OriginSpecificWorkflowPrincipalAndStartRevisionExact`, `CanonicalGovernanceScopeV1SchemaPredicateDigestAndOperatorApprovalStillExact`, `RecordedGlobalDeletionScopeEffectCutEffectiveReceiptAdmissionFenceOrdinalAndZeroViolationCheckpointExact`, `AuthenticatedCurrentOrdinalZeroAcceptedViolationsSinceInstallProofStillExact`, `InteractionDirectoryMigrationReadyEpochAndGuardReceiptCurrentForAcceptedScope`, `AuthoritativeInteractionNamespaceRevisionAndHighWaterExact`, `CurrentHoldExportPrepareCommitCleanupCommittedIndexAndCopyRevisionsExact`, `CandidateEqualsCompleteCurrentResourceSet`, `NoUnresolvableMember`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; atomically appends the immutable accepted set plus deletion token, canonical predicate, effect-cut/admission-fence/zero-since-install proof, migration guard receipt, and namespace high-water; a competing owner or accepted admission violation requires invalidation and a successor higher-ordinal cut/candidate under the same request and predicate |
| `ConversationDeletionPropagation:AcknowledgeAcceptedDeletionInventory`, `GovernanceProtection:AcknowledgeAcceptedDeletionInventory` | Tenant | none; fence-decision acknowledgement reads no mutable readiness record | `RecordedProtectionFenceAcceptedDeletionInventoryDecisionRevisionExact`, `OriginSpecificWorkflowPrincipalAndStartRevisionExact`, `OperatorOriginRequesterInspectorAndApprovedScopeStillExactWhenApplicable`, `CandidateHashOriginAndDeletionRequestIdMatch`, `ExpectedProtectedDeletionRevisionMatches`, `IdempotencyFingerprintValid`; appends only the accepted fence set/token/revision to `ProtectedDeletion`, and lost acknowledgement resolves by exact fence lookup |
| `ConversationDeletionPropagation:AuthorizeDeletionScopeWriteFence`, `GovernanceProtection:AuthorizeDeletionScopeWriteFence` | Tenant | none; authorization reads the exact accepted owners and admission-violation ledger and records no external effect | `RecordedAcceptedDeletionInventoryAndAcknowledgementExact`, `RecordedGlobalEffectCutAdmissionFenceIdOrdinalReceiptAndZeroViolationCheckpointExact`, `AuthenticatedCurrentOrdinalZeroAcceptedViolationsSinceInstallProofStillExact`, `OriginSpecificWorkflowPrincipalAndStartRevisionExact`, `CanonicalGovernanceScopeV1SchemaPredicateDigestAndAcceptedTokenExact`, `MigrationReadyEpochGuardAndNamespaceHighWaterExact`, `NoDeletionPreparationOrDestructionStarted`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends `DeletionScopeWriteFenceAuthorized`, and only that revision may authorize the exact EventStore effect |
| `ConversationDeletionPropagation:InstallDeletionScopeWriteFenceEffect`, `GovernanceProtection:InstallDeletionScopeWriteFenceEffect` | Tenant | none; committed authorization permits one target-limited EventStore content guard effect | `RecordedDeletionScopeWriteFenceAuthorizationRevisionExact`, `EventStoreCanonicalScopePredicateAcceptedTokenCurrentOrdinalEpochGuardAndNamespaceHighWaterExact`, `NoExistingContentGuardForRequestPredicate`, `DeletionScopeAdmissionFenceStillInstalledExact`, `InteractionDirectoryMigrationStillReadyAtAuthorizedEpochAndGuardReceipt`, `AuthoritativeNamespaceRevisionStillMatches`, `FenceIdAndIdempotencyIdentityExact`; conditionally installs the persistent `(DeletionRequestId, ScopePredicateDigest)` content guard plus its first ordinal/global-cut/token binding and rejects every later in-scope interaction/failure create or content append under any writer epoch; unknown/unavailable requires exact lookup |
| `ConversationDeletionPropagation:RecordDeletionScopeWriteFence`, `GovernanceProtection:RecordDeletionScopeWriteFence` | Tenant | none; result records only the exact authorized EventStore outcome | `RecordedDeletionScopeWriteFenceAuthorizationRevisionExact`, `AuthenticatedInstalledFenceIdScopeEpochGuardNamespaceFirstOrdinalTokenBindingAndReceiptExact`, `EventStoreExactOutcomeLookupResolved`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends the continuous-install receipt and first binding; changed result conflicts |
| `ConversationDeletionPropagation:AuthorizeDeletionScopeWriteFenceSuccessorBinding`, `GovernanceProtection:AuthorizeDeletionScopeWriteFenceSuccessorBinding` | Tenant | none; authorizes no install or content effect | `RecordedSuccessorAcceptedInventoryAndCurrentGlobalCutOrdinalZeroProofExact`, `OriginalContentGuardContinuousInstallReceiptRequestPredicateExact`, `LatestActuallyInstalledOrdinalTokenBindingExact`, `CompleteGapFreeOrderedInterveningInvalidatedOrTokenlessOrdinalContainmentReceiptChainThroughCurrentPredecessorExact`, `NoDestructionSealedAndNoFenceRemoval`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends the target-limited successor-binding authorization; a newer violation makes it obsolete rather than requiring an invalid intermediate binding, and it cannot change scope/fence identity |
| `ConversationDeletionPropagation:BindDeletionScopeWriteFenceSuccessorEffect`, `GovernanceProtection:BindDeletionScopeWriteFenceSuccessorEffect` | Tenant | none; exact authorization permits one EventStore content-guard append | `RecordedContentFenceSuccessorBindingAuthorizationRevisionExact`, `SameRequestPredicateAndContinuousOriginalFenceReceiptExact`, `LatestActuallyInstalledBindingAndCompleteGapFreeOrderedContainmentChainExact`, `AtomicCurrentAdmissionOrdinalGlobalCutAcceptedTokenAndZeroSinceInstallProofExact`, `NoRemovalGapOrDestructionSealed`, `GuardExpectedRevisionMatches`, `IdempotencyFingerprintValid`; atomically appends the new ordinal/global-cut/token binding to the existing guard without remove/reinstall when the authorized ordinal is still current. If a later violation won, it returns authenticated terminal `ObsoleteBindingAuthorization(CurrentOrdinal, ContainmentChainHighWater)` without a guard append; exact lookup resolves either result, and changed scope/token or a gapped chain conflicts |
| `ConversationDeletionPropagation:RecordDeletionScopeWriteFenceSuccessorBinding`, `GovernanceProtection:RecordDeletionScopeWriteFenceSuccessorBinding` | Tenant | none; records only the authenticated guard result | `RecordedContentFenceSuccessorBindingAuthorizationRevisionExact`, (`AuthenticatedContinuousFenceSuccessorBindingReceiptAndExactLookupResolved` or `AuthenticatedObsoleteBindingAuthorizationCurrentOrdinalAndContainmentChainHighWaterExact`), `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends the exact terminal result; only a binding receipt for the current ordinal permits prepare/barrier, while an obsolete result permits a new authorization from the latest installed binding plus the extended gap-free chain |
| `ConversationDeletionPropagation:ContainDeletionScopeAdmissionFenceViolation`, `GovernanceProtection:ContainDeletionScopeAdmissionFenceViolation` | Tenant | none; admission-guard integrity recovery reads direct EventStore and authoritative owner evidence | `RecordedAdmissionFenceIdAndInstallReceiptExact`, `ServerIssuedAdmissionGuardEvaluationReceiptAcceptedAtOrdinalAcceptedAtGuardHighWaterAndCallerPresentedOrdinalDiagnosticExact`, `ViolationRecordedInThePartitionCurrentAtAcceptanceExact`, `ViolationPermitIntentLeaseOrPhaseAuthorizationIdentityAndRevisionExact`, `ViolationAuthoritativeMembershipProofUnderCanonicalPredicateExact`, `SecurityObservationRecordedOrDurablySpooledExact`, (`NoDestructionSealedAndNoCurrentGlobalCutCandidateOrAcceptedTokenExistsAtExactRevisions` or `NoDestructionSealedAndEachExistingGlobalCutCandidateAcceptedTokenInvalidatedAtExactRevision` or `DestructionSealedAndDeletionIntegrityCompromisedRecordedAtExpectedRevision`), `NextAdmissionFenceOrdinalAssignedOnceFromTheContaminatedCurrentPartitionOrExistingExact`, `IdempotencyFingerprintValid`; append acceptance and successor-ordinal installation serialize, so caller-supplied stale ordinal never selects a partition. Before sealing it preserves every restrictive fence/cycle, invalidates only artifacts that exist, and assigns exactly one successor. When the content guard exists, the receipt is the next ordered gap link and makes an in-flight binding authorization terminal-obsolete. Delayed detection follows the stored acceptance receipt; missing/unverifiable acceptance-time evidence records `AdmissionIntegrityUnattributable`, invalidates current clean evidence, and blocks rather than guessing. After sealing any accepted matching append records compromise and blocks further destruction/completion |
| `ConversationDeletionPropagation:SupersedeAcceptedInventoryBeforePrepare`, `GovernanceProtection:SupersedeAcceptedInventoryBeforePrepare` | Tenant | none; conflict recovery reads exact fence/namespace outcomes only | `RecordedScopeFenceInstallConflictOrVerifiedNotInstalledExact`, `NoDeletionPreparationArmedOrDestructionStarted`, `AcceptedSetAndTokenStillExact`, `CurrentMigrationOrNamespaceWinnerRevisionExact`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; marks the immutable old token `SupersededBeforePrepare` and permits a successor candidate under the same request and already-approved scope, never scope expansion |
| `ConversationDeletionPropagation:ContainDeletionScopeWriteFenceViolation`, `GovernanceProtection:ContainDeletionScopeWriteFenceViolation` | Tenant | none; recorded-scope containment reads direct owner and EventStore violation evidence | `RecordedContinuousDeletionContentGuardCurrentBindingAndViolationReceiptExact`, `ViolationResourceAuthoritativeMembershipProofUnderCanonicalPredicateExact`, `CompleteProtectionTargetTenantInteractionAndAliasExact`, (`NoDestructionSealedAndSupersessionRequired` or `DestructionSealedAndTargetCoverageLookupExact`), `ProtectionFenceExpectedRevisionMatches`, `SecurityObservationRecordedOrDurablySpooledExact`, `IdempotencyFingerprintValid`; before sealing it permits only refreeze. After sealing it serializes on `ProtectionFence` by the complete protection target. If that target is in the accepted manifest or an existing containment batch, it appends non-authorizing `ViolationCoveredByDeletionBatch` linking the resource/receipt to the original batch/target and allocates no ordinal; a consumed original carries exact `AlreadyDestroyedByBatch`, while every newly observed copy still needs purge evidence. Only an uncovered target receives the next monotonic `ContainmentOrdinal` and immutable singleton manifest. Concurrent same-target observations converge; different uncovered targets remain distinct. Unknown, malformed, outside-scope, cross-tenant, or unresolvable proof blocks |
| `GovernanceProtection:AuthorizeDeletionContainmentBatch` | Tenant | none; authorizes no destructive effect | `RecordedDestructionSealedReceiptAndSuccessfulProtectionFenceMirrorExact`, `RecordedUncoveredPostSealTargetViolationReceiptContainmentOrdinalAndImmutableSingletonSortedTargetManifestDigestExact`, `ProtectionFenceTargetCoverageIndexProvesNoPriorAcceptedOrContainmentBatchForTarget`, `ContinuousContentGuardRequestPredicateCurrentBindingAndViolationCheckpointExact`, `ObservedGovernanceScopeGuardRevisionCompareTokenExact`, `NoPostSealAdmissionIntegrityCompromiseAtObservedLedgerHighWater`, (`NoOverlappingPostStartHoldContender` or `ExactEffectivePostStartDeletionAllowedDecisionVersionOutcomeAndHoldSetForThisOrdinal`), (`NoPriorContainmentBatchAuthorizationForOrdinal` or `RecordedPriorContainmentBatchAuthorizationStaleAtExactGuardRevision`), `ContainmentBatchAuthorizationAttemptOrdinalAssignedOnceOrExistingExact`, `DeterministicDeletionDestructionBatchIdValid`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends only a target-limited authorization for the same containment ordinal, batch id, frozen singleton manifest, and any required post-start disposition. Open/missing/mismatched/hold-allowed evidence rejects. Exact retry reuses it; changed target/digest conflicts; a new attempt follows only a recorded stale result |
| `GovernanceProtection:AuthorizeDeletionBatchCapabilitySigning` | Tenant | none; authorizes no secret use except one immutable accepted or containment batch signing attempt | (`RecordedCurrentBarrierAuthorizationBatchShapeExact` or `RecordedCurrentContainmentBatchAuthorizationExact`), `DeletionBatchCapabilityV1CanonicalFieldsIssuerAudienceTenantRequestStableDestructionSealIdBatchKindOrdinalIdManifestGuardStreamAttestationOrdinalSigningAttemptOrdinalIntendedIssuedGuardRevisionAndKeyVersionExact`, `CurrentNonRevokedPerTenantDeletionBatchCapabilitySigningKeyVersionAndPublicAnchorProfileExact`, `AttestationOrdinalAssignedOnceOrExistingExact`, `SigningAttemptOrdinalAssignedOnceOrExistingExact`, (`NoPriorSigningAttemptForAttestation` or `PriorSignedAttemptRecordedObsoleteUnissuedAtExactStaleGuardRevision`), `NoActiveAttestationForThisBatchAndOrdinal`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends one target-limited authorization. A pre-issue retry retains stable seal id/batch/manifest/attestation ordinal, increments signing-attempt ordinal, and binds the returned current guard revision; a signature is not destructive authority until the exact guard issue records it active |
| `GovernanceProtection:SignDeletionBatchCapabilityEffect` | Tenant | none; exact authorization permits one `EXT-SECRETS-1` signing operation | `RecordedDeletionBatchCapabilitySigningAuthorizationRevisionCanonicalDigestSigningAttemptOrdinalAndIntendedIssuedGuardRevisionExact`, `CommittedPerTenantDeletionBatchCapabilitySigningKeyFamilyIssuerAudienceAlgorithmAndKeyVersionExact`, `SigningRequestIdentityIncludesStableDestructionSealIdBatchManifestAttestationSigningAttemptIntendedGuardRevisionKeyVersionAndAudienceExact`, `IdempotencyFingerprintValid`; returns or exactly looks up one detached ES256 JWS over RFC 8785 canonical bytes. It exposes no private key and cannot sign changed fields, another attempt/revision, audience, or tenant |
| `GovernanceProtection:RecordDeletionBatchCapabilitySigned` | Tenant | none; records only the exact signer outcome | `RecordedSigningAuthorizationRevisionCanonicalDigestSigningAttemptIntendedGuardRevisionKeyVersionAndAnchorExact`, `AuthenticatedSignerOutcomeOrExactLookupResolved`, `JwsHeaderPayloadSignatureAndTrustProfileExact`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; records that attempt for the corresponding guard issue commit; unknown/unavailable remains pending and cannot be replaced or marked obsolete |
| `GovernanceProtection:RecordSignedDeletionBatchCapabilityObsoleteUnissued` | Tenant | none; terminalizes one signed pre-issue attempt and grants no authority | `RecordedSignedCapabilityAttemptStableDestructionSealIdBatchManifestAttestationSigningAttemptAndIntendedIssuedGuardRevisionExact`, (`AuthenticatedBarrierAuthorizationStaleCurrentGuardRevisionExact` or `AuthenticatedContainmentBatchAuthorizationStaleCurrentGuardRevisionExact`), `GovernanceScopeGuardExactLookupProvesNoBatchAttestationIssuedFromThisSigningRequestAtAnyRevision`, `SignerOutcomeDigestExact`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends `SignedAttestationObsoleteUnissued` once. Exact retry reuses it; changed proof conflicts. Only after this record may a successor signing attempt keep the same stable seal id, batch id, manifest, and attestation ordinal, increment `SigningAttemptOrdinal`, and bind the returned current guard revision/current healthy key. Unknown issue outcome remains pending; this row is never emergency re-attestation |
| `GovernanceProtection:CommitDeletionContainmentBatchEffect` | Tenant | none; exact authorization permits one `GovernanceScopeGuard(TenantId)` operation | `RecordedContainmentBatchAuthorizationRevisionAttemptOrdinalObservedGuardRevisionAndBindingsExact`, `RecordedDeletionBatchCapabilitySignedForExactStableDestructionSealIdBatchManifestIntendedIssuedGuardRevisionAttestationOrdinalAndSigningAttemptOrdinal`, `GovernanceScopeGuardExpectedRevisionEqualsAuthorizationObservedRevision`, `AtomicDestructionSealStillExact`, `AtomicContinuousContentGuardViolationReceiptContainmentOrdinalAndSingletonManifestExact`, `AtomicProtectionTargetStillUncovered`, `AtomicNoPostSealAdmissionIntegrityCompromise`, (`AtomicNoOverlappingPostStartHoldContender` or `ExactEffectivePostStartDeletionAllowedDispositionAppliedForThisBatchInThisCommit`), `NoContainmentBatchIdentityAlreadyIssuedForOrdinal`, `DeterministicDeletionDestructionBatchIdCapabilityKeyVersionAndAttestationExact`; conditionally appends at the observed guard revision. If unchanged, it records `ContainmentBatchIssued` plus the stable destruction-seal id, one active attestation, and exact signing request identity. The authenticated success result returns `CommittedIssuedGuardRevision` equal to the assigned event revision; that result revision is excluded from the stable seal id, batch id, signing request id, and signed bytes. It then returns the deterministic non-expiring, revocable, single-use capability. Any intervening guard/hold/coverage/capability mutation returns authenticated `ContainmentBatchAuthorizationStale(CurrentGuardRevision)` and no capability; exact lookup returns the same issued-or-stale result and cannot mint a second identity |
| `GovernanceProtection:RecordDeletionContainmentBatchIssued` | Tenant | none; records only an authenticated successful guard result | `RecordedContainmentBatchAuthorizationRevisionAndAttemptOrdinalExact`, `AuthenticatedContainmentBatchIssuedReceiptBatchIdManifestDigestCapabilityKeyVersionAttestationOrdinalAnchorAndGuardRevisionExact`, `AppliedPostStartHoldDispositionExactWhenRequired`, `GovernanceScopeGuardExactOutcomeLookupResolved`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; mirrors the immutable issued batch; only its current guard-active attestation plus dispatch permits `ReserveAndConsumeDeletionBatchEffect` |
| `GovernanceProtection:RecordDeletionContainmentBatchIssuanceStale` | Tenant | none; records only an authenticated stale guard result | `RecordedContainmentBatchAuthorizationRevisionAndAttemptOrdinalExact`, `AuthenticatedContainmentBatchAuthorizationStaleCurrentGuardRevisionExact`, `NoContainmentBatchIssuedForOrdinalByExactGuardLookup`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends the stale outcome and permits reauthorization of the same containment ordinal, batch id, and manifest at the returned current guard revision; it grants no protection effect |
| `GovernanceProtection:AuthorizeDeletionBatchCapabilityReattestation` | Tenant | none; security recovery changes no logical batch authority | `RecordedDeletionBatchCapabilityKeyCompromiseObservedExact`, `AuthenticatedProtectionOwnerConsumptionBlockedByCapabilityKeyCompromiseForExactBatchOldAttestationKeyVersionAndRevocationRevision`, `GuardIssuedBatchIdentityManifestAndOldAttestationExact`, `HealthyCurrentPerTenantSigningKeyVersionAndAnchorExact`, `NextAttestationOrdinalAssignedOnce`, `NoOtherActiveAttestation`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; authorizes signing for the same immutable batch/manifest only. A prior unconsumed lookup, `BatchBlockPending`, `ConsumptionReserved`, consumed vector, or unknown owner status cannot authorize replacement |
| `GovernanceProtection:RecordDeletionBatchCapabilityKeyCompromise` | Tenant; execution kind is the non-public `IDeletionCapabilityCompromiseRegistrar`, not an Agents principal | none; mirrors only the exact registrar outcome after its protection-owner block | `AuthenticatedSecretsIssuerAudiencePerTenantKeyFamilyVersionEmergencyRevocationAndTrustProfileRevisionExact`, `RegistrarDeliveryReplayIdentityExact`, `ProtectionOwnerKeyVersionBlockReceiptAndEveryKnownBatchOutcomeExact`, `GovernanceScopeGuardExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends `DeletionBatchCapabilityKeyCompromiseObserved` and the finite issued-batch reconciliation set. It blocks new issue/dispatch/purge/completion; an already-dispatched batch remains `BatchBlockPending` until its exact protection state is blocked/reserved/terminal. No human Platform or Workflow identity can invoke this row |
| `GovernanceProtection:CommitDeletionBatchCapabilityReattestationEffect` | Tenant | none; exact signed replacement permits one guard operation while protection stays blocked | `RecordedReattestationAuthorizationAndNewSignerOutcomeExact`, `GovernanceScopeGuardExpectedRevisionAndOldActiveAttestationExact`, `SameBatchIdManifestIssuerAudienceAndSealExact`, `OldKeyVersionEmergencyRevoked`, `AuthenticatedProtectionOwnerConsumptionBlockedByExactCompromiseReceipt`, `IdempotencyFingerprintValid`; atomically marks the old attestation revoked and new attestation active on the guard. It cannot unblock protection or consume. Unknown/mismatched protection state remains blocked; no second batch or concurrently active credential is created |
| `GovernanceProtection:RecordDeletionBatchCapabilityReattested` | Tenant | none; records only the exact guard result | `RecordedReattestationAuthorizationAndSignerOutcomeExact`, `AuthenticatedGuardReattestedOutcomeExactLookupResolved`, `AuthenticatedProtectionOwnerStillBlockedBySameCompromiseReceipt`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; mirrors credential replacement, grants no new target authority, and requires a successor dispatch plus protection-owner activation before consumption |
| `GovernanceProtection:DeletionPrepare` | Tenant | `LR-TOPOLOGY`, `LR-EVENTSTORE`, `LR-TENANT-ACCESS`, `LR-SECRETS`, `LR-AUDIT-PROTECTION-DELETION` | (`RecordedOperatorDeletionRequestAndComplianceApprovalValid` or `RecordedConversationApprovedDeletionOriginExact`), `ApprovedPrdOq31DecisionVersionMatches`, `FenceOwnedAcceptedDeletionInventoryAcknowledgementCanonicalPredicateAndGlobalEffectCutExact`, `RecordedCurrentAdmissionFenceAndContinuousContentGuardCurrentOrdinalTokenBindingReceiptsExact`, `AuthenticatedCurrentOrdinalZeroAcceptedViolationsSinceInstallProofStillExact`, `NoDeletionIntegrityCompromised`, (`NoLifecycleCoveredExportOrCopyInAcceptedSetAtRecordedInventoryRevisions` or (`ApprovedExportLifecycleDecisionVersionMatches` and `ExportStoreAvailableAtExactTargetAndContract`)), `FenceTokenAndExpectedRevisionsMatch`, `NoOverlappingRegisteredHoldContenderOrExportCleanupPending`; export-bearing preparation phase-pins the exact lifecycle/store values before any reversible effect |
| `GovernanceProtection:DeletionDeferByHold` | Tenant | none; content-free pre-arm deferral reads only recorded origin and fence owners | `RecordedDeletionOriginAndRequestRevisionExact`, `FenceOwnedAcceptedDeletionInventoryAndAcknowledgementExact`, `ActiveOrPreparingPreArmHoldIdsAndRevisionsExact`, `DeletionNotArmedAndNoIrreversibleEffect`, `FenceExpectedRevisionMatches`, `DeterministicReconsiderationIdValid`, `IdempotencyFingerprintValid`; appends `DeletionDeferredByHold` with the whole accepted set and successor-fence reconsideration identity, performs no preparation/destruction, and remains legal while ancillary gates or armed-contention policy are unavailable/Open |
| `GovernanceProtection:DeletionPrepareBranchDecision` | Tenant | none; records no external effect and reads no mutable readiness record | For `Resume` on either origin: `OriginalDeletionPrepareIntentStillValidAtExactRevision`, `NoAuthorizedCancellationOrAbortFact`. For operator `Abort` only: `ApprovedOperatorDeletionCancellationDecisionVersionAndOutcomeMatch`, `RecordedAuthorizedOperatorDeletionCancellationFactValid`. `ConversationApprovedDeletion` never has Abort; while `OD-OPERATOR-DELETION-CANCELLATION-1` is Open the operator Abort branch is also inapplicable. Both legal dispositions bind the frozen set, canonical predicate, every admission-fence ordinal, content-fence id, current global cut receipt, lifecycle values, and expected owner revision. |
| `GovernanceProtection:DeletionPrepareResumeRecovery` | Tenant | none; reversible recovery reads no mutable readiness record | `RecordedPrepareRecoveryDispositionResumeRevisionMatches`, `RecordedDeletionPrepareDecisionPolicyLifecycleAndStoreVersionsMatch`, `WorkflowCapabilityAndTargetExact`, `FenceTokenAndFrozenSetMatch`, (`RecordedNoLifecycleCoveredCopyProofExact` or `PhasePinnedExportStoreProtectionAndSecretsDirectlyAvailable`), `ExistingPrepareOutcomesLookupExact`; may only resume/acknowledge the recorded branch |
| `GovernanceProtection:DeletionPrepareUnwindRecovery` | Tenant | none; approved operator-Abort cleanup reads no mutable readiness record | `RecordedOperatorApprovedDeletionOriginExact`, `ApprovedOperatorDeletionCancellationDecisionVersionAndOutcomeMatch`, `RecordedPrepareRecoveryDispositionAbortRevisionMatches`, `WorkflowCapabilityAndTargetExact`, `FenceTokenCanonicalPredicateEveryAdmissionOrdinalContentFenceAndOwnerOrdinalCycleMatch`, `ExistingPrepareExportAndProtectionUnwindOutcomesLookupExact`, `EveryReversibleExternalOutputPurgedOrProvenAbsent`; may only complete cleanup and cannot remove a fence/cycle or finalize Abort |
| `GovernanceProtection:AuthorizeOperatorDeletionFenceRemoval` | Tenant | none; available only when the approved cancellation decision permits Abort | `RecordedCleanupCompleteAbortDispositionAndDecisionVersionExact`, `NoDestructionSealed`, `EveryAdmissionFenceIdOrdinalPlusWriteFenceCanonicalPredicateAndMigrationEpochExact`, `EveryOwnerOrdinalCutCycleStillRestrictiveExact`, `ExpectedProtectionFenceRevisionMatches`, `IdempotencyFingerprintValid`; appends `OperatorDeletionAbortAuthorized` and fixes the removal manifest. This authorization competes with destruction-start authorization at the `ProtectionFence` revision, and only it may authorize exact guard removal |
| `GovernanceProtection:RemoveOperatorDeletionFencesEffect` | Tenant | none; committed cancellation authorization permits one target-limited EventStore effect | `RecordedOperatorDeletionAbortAuthorizationRevisionAndRemovalManifestExact`, `ApprovedCancellationDecisionStillExact`, `CleanupCompleteEvidenceExact`, `EveryAdmissionFenceOrdinalAndWriteFenceIdCanonicalPredicateAndStartRevisionExact`, `NoDestructionSealed`, `RemovalIdempotencyIdentityExact`; atomically removes every admission-fence ordinal plus the continuous content guard and all ordinal/token bindings and returns one authenticated receipt; the guard rejects a later destruction-start barrier. Unknown/lost acknowledgement requires exact lookup and no owner cycle is released first |
| `GovernanceProtection:RecordOperatorDeletionScopeFencesRemoved` | Tenant | none; result records only the exact authorized EventStore outcome | `RecordedOperatorDeletionAbortAuthorizationRevisionAndRemovalManifestExact`, `AuthenticatedEveryAdmissionOrdinalAndWriteFenceRemovalReceiptExact`, `EventStoreExactOutcomeLookupResolved`, `ExpectedProtectionFenceRevisionMatches`, `IdempotencyFingerprintValid`; appends the immutable complete removal receipt |
| `GovernanceProtection:ReleaseOperatorDeletionEffectCut` | Tenant | none; removal-receipt-bound owner transition reads no mutable readiness record | `RecordedCompleteOperatorFenceRemovalReceiptExact`, `OwnerAndAdmissionOrdinalCutCycleManifestExact`, `ConversationAgentStateExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends `DeletionEffectCutReleasedAfterApprovedAbort` for exactly that owner/ordinal cycle and authorizes no replay of cancelled work or new deletion effect |
| `GovernanceProtection:RecordOperatorDeletionAborted` | Tenant | none; final approved cancellation reads exact cleanup/removal/release receipts only | `RecordedApprovedCancellationDecisionAndAbortDispositionExact`, `CleanupCompleteAndEveryFenceOrdinalRemovalReceiptExact`, `EveryOwnerOrdinalCutCycleReleasedAfterApprovedAbortExact`, `NoDestructionSealed`, `ExpectedProtectedDeletionRevisionMatches`, `IdempotencyFingerprintValid`; appends final `DeletionAborted`; while the decision is Open or forbids Abort, every removal/release/finalization row rejects |
| `GovernanceProtection:AuthorizeDeletionDestructionStartBarrier` | Tenant | none; gate-free recorded branch authorizes no destructive effect | `RecordedDeletionPrepareDispositionResumeAndArmedRevisionMatch`, `FenceOwnedAcceptedSetTokenCanonicalPredicateGlobalEffectCutCurrentAdmissionFenceOrdinalAndPreparePinnedDecisionPolicyLifecycleStoreVersionsExact`, `AcceptedInventoryAssignedStableDestructionSealIdFromTokenPredicateAndManifestDigestExact`, `InteractionDirectoryMigrationStillReadyAtAcceptedEpochAndGuardReceipt`, `CurrentAdmissionFenceAndContinuousContentGuardCurrentBindingReceiptsExact`, `AuthenticatedCurrentOrdinalZeroAcceptedViolationsSinceInstallProofStillExact`, `ContentGuardViolationFreeThroughAuthenticatedCheckpointExact`, `SortedAcceptedTenantInteractionTargetProtectionKeyAliasManifestAndDigestExact`, `NoDeletionIntegrityCompromised`, (`RecordedNoLifecycleCoveredCopyProofExact` or `PhasePinnedExportStoreProtectionAndSecretsDirectlyAvailable`), (`NoOverlappingRegisteredHoldContenderAtAuthorization` or `ApprovedHoldDeletionDecisionVersionAndOutcomeMatchRecordedContention`), `NoOverlappingExportCleanupPending`, `ObservedGovernanceScopeGuardRevisionCompareTokenAndActiveContenderManifestExact`, (`NoPriorBarrierAuthorizationForAcceptedToken` or `RecordedPriorBarrierAuthorizationStaleAtExactGuardRevision`), `BarrierAuthorizationOrdinalAssignedOnceOrExistingExact`, `FenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends a target-limited barrier authorization binding the stable destruction-seal id, checkpoints, key-target manifest, hold disposition if any, exact observed guard revision/compare token, and authorization ordinal. It competes with operator removal and does not start destruction; exact retry reuses the authorization, while a new attempt is legal only after an authenticated stale result is recorded |
| `GovernanceProtection:CommitDeletionDestructionStartBarrierEffect` | Tenant | none; exact authorization permits one `GovernanceScopeGuard(TenantId)` operation | `RecordedDeletionDestructionStartBarrierAuthorizationRevisionOrdinalBindingsAndObservedGuardRevisionCompareTokenExact`, `RecordedDeletionBatchCapabilitySignedForExactAcceptedStableDestructionSealIdBatchManifestIntendedIssuedGuardRevisionAttestationOrdinalAndSigningAttemptOrdinal`, `CurrentAdmissionFenceOrdinalAndInstallReceiptExact`, `AtomicCurrentOrdinalZeroAcceptedViolationsSinceInstallAtVerifiedHighWater`, `AtomicContinuousContentGuardCurrentBindingAndCleanCheckpointExact`, `MigrationReadyEpochGuardAndEveryFenceReceiptStillExact`, `NoFenceRemovalAuthorizedOrEffective`, `NoDeletionIntegrityCompromisedOrUnattributableAdmission`, (`NoOverlappingRegisteredHoldContenderAtGuardRevision` or `ExactEffectiveDeletionAllowedHoldDispositionConsumedInThisCommit`), `SortedAcceptedKeyTargetManifestDigestExact`, `GovernanceScopeGuardExpectedRevisionEqualsAuthorizationObservedRevision`, `BarrierBatchAndAttestationIdempotencyIdentityExact`; conditionally appends at that exact guard revision and is serialized with every matching admission append and overlapping hold registration/release. Any intervening guard mutation returns authenticated `BarrierAuthorizationStale(CurrentGuardRevision)` and no seal. It atomically commits `DestructionSealed`, the stable `DestructionSealId`, accepted-batch identity, the one active `DeletionBatchCapabilityV1` attestation, and its exact signing request identity. The authenticated success result returns `CommittedDestructionSealGuardRevision` equal to the assigned event revision. That actual revision is result evidence and is excluded from seal id, batch id, signing request id, and signed bytes. That instant is `DestructionStarted`; the capability is non-expiring and its logical batch has no renewal/second-batch branch. Lost acknowledgement/restore uses exact guard lookup |
| `GovernanceProtection:RecordDeletionDestructionStartBarrierStale` | Tenant | none; records only an authenticated stale guard result | `RecordedDeletionDestructionStartBarrierAuthorizationRevisionOrdinalAndObservedGuardRevisionExact`, `AuthenticatedBarrierAuthorizationStaleCurrentGuardRevisionExact`, `NoDestructionSealOrAcceptedBatchIssuedByExactGuardLookup`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends the stale result and permits a new barrier authorization ordinal bound to the returned current guard revision only after any contender has exact approved disposition or release evidence; it grants no destruction effect |
| `GovernanceProtection:RecordDeletionDestructionStarted` | Tenant | none; records only an authenticated guard result | `RecordedDeletionDestructionStartBarrierAuthorizationRevisionOrdinalAndObservedGuardRevisionExact`, `AuthenticatedDestructionSealedReceiptManifestDigestBatchCapabilityKeyVersionAttestationOrdinalAnchorAndExactOutcomeLookupResolved`, `BatchNotKnownConsumedOrRevokedAndNoDeletionIntegrityCompromised`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; mirrors only the exact successful seal/batch/attestation receipt and phase-pinned values on `ProtectionFence`; changed, unavailable, revoked, or compromised result remains pending |
| `GovernanceProtection:AuthorizeDeletionBatchDispatch` | Tenant | none; authorizes no protection effect | `RecordedGuardIssuedAcceptedOrContainmentBatchAndProtectionFenceMirrorExact`, `BatchManifestAndCurrentActiveCapabilityAttestationExact`, `ObservedGovernanceScopeGuardRevisionCompareTokenExact`, `NoDeletionIntegrityOrCapabilityKeyCompromise`, (`NoOverlappingPostStartHoldContender` or `ExactEffectiveDeletionAllowedPostStartDispositionForThisBatch`), (`NoPriorDispatchAuthorization` or `RecordedPriorDispatchAuthorizationStaleOrInvalidatedByReattestationExact`), `DispatchAuthorizationAttemptOrdinalAssignedOnceOrExistingExact`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends a target-limited dispatch authorization. Open/missing/mismatched/hold-allowed post-start evidence blocks |
| `GovernanceProtection:CommitDeletionBatchDispatchEffect` | Tenant | none; exact authorization permits one guard operation | `RecordedDeletionBatchDispatchAuthorizationRevisionAttemptOrdinalBatchManifestAttestationDispositionAndObservedGuardRevisionExact`, `GovernanceScopeGuardExpectedRevisionEqualsAuthorizationObservedRevision`, `AtomicBatchManifestAndActiveAttestationStillIssuedUnrevoked`, `AtomicNoAdmissionIntegrityOrCapabilityKeyCompromise`, (`AtomicNoOverlappingPostStartHoldContender` or `ExactEffectiveDeletionAllowedPostStartDispositionAppliedForThisBatchInThisCommit`), `NoPriorDispatchReceiptForBatchOrExistingExact`, `IdempotencyFingerprintValid`; conditionally appends `DeletionBatchDispatchAuthorized`. An earlier hold or any intervening guard mutation returns authenticated `DeletionBatchDispatchAuthorizationStale(CurrentGuardRevision)` and no dispatch. A winning commit is uninterruptible only against a later hold on that batch. Admission-integrity and key-compromise cancellation still race `ReserveAndConsumeDeletionBatchEffect` at the protection owner |
| `GovernanceProtection:RecordDeletionBatchDispatchIssuedOrStale` | Tenant | none; records only the exact guard result | `RecordedDispatchAuthorizationRevisionAndAttemptOrdinalExact`, (`AuthenticatedDeletionBatchDispatchAuthorizedReceiptExact` or `AuthenticatedDeletionBatchDispatchAuthorizationStaleCurrentGuardRevisionExact`), `GovernanceScopeGuardExactOutcomeLookupResolved`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; only the current successful receipt permits protection. Stale must be recorded before retry; re-attestation invalidates an unconsumed old-attestation dispatch and requires a successor dispatch authorization |
| `GovernanceProtection:ReserveAndConsumeDeletionBatchEffect` | Tenant | none; exact guard-dispatched batch permits one protection-owner operation | `RecordedDeletionBatchDispatchAuthorizedAndProtectionFenceMirrorExact`, `DeterministicBatchIdManifestDigestAndSortedTenantInteractionTargetProtectionKeyAliasTargetsExact`, `DeletionBatchCapabilityV1JwsCanonicalBytesIssuerAudienceAnchorKeyVersionAttestationSigningAttemptAndIntendedIssueRevisionExact`, `LinearizableGovernanceScopeGuardExactLookupProvesSameBatchManifestActiveAttestationAndDispatchReceipt`, `ProtectionOwnerCurrentStateUnconsumedForThatExactAttestationDispatchAndNoTenantKeyVersionOrDeletionRequestBlock`, (`NoOverlappingPostStartHoldContenderBeforeDispatch` or `ExactDeletionAllowedPostStartDispositionBoundToDispatch`), `IdempotencyFingerprintValid`; signature-only, issue-only, or a guard read followed by an unconditional write is forbidden. At `EXT-PROTECTION-1`, one conditional local transition races every block/revocation and changes `Unconsumed` to `ConsumptionReserved(AttestationOrdinal, DispatchReceiptId)`; that is the irreversible-effect instant for admission/key compromise. The same owner operation performs all-or-none physical destruction and reaches terminal `Consumed(Vector)`, while recovery of `ConsumptionReserved` finishes or returns that exact outcome and never re-authorizes. A block winner returns `ConsumptionBlocked`; reserved/consumed/revoked return their immutable result. A later hold ordered after dispatch cannot cancel this batch but remains restrictive elsewhere. Unavailable lookup/owner, changed target, or unknown state blocks; exact prior-target coverage returns authenticated `AlreadyDestroyedByBatch` only for that complete target identity |
| `GovernanceProtection:RecordOrRecoverDeletionDekManifest` | Tenant | none; records only exact protection-owner outcome | `RecordedAcceptedOrGuardIssuedContainmentBatchIdentityAndManifestDigestExact`, (`AuthenticatedAllOrNoneBatchOutcomeOrExactLookupResolved` or `AuthenticatedAlreadyDestroyedByBatchOriginalBatchAndPerTargetReceiptExact`), `EveryPerTargetReceiptIdentityAndOrderExact`, `ProtectionFenceTargetCoverageIndexAndResourceLinksExact`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; records the vector/coverage once and never infers partial success. Same-target resources use their original batch receipt; a new containment batch exists only for a previously uncovered target |
| `GovernanceProtection:BlockDeletionBatchConsumption` | Tenant | none; integrity-compromise recovery targets only the protection owner | `RecordedPostSealAdmissionIntegrityCompromiseAndExactDeletionRequestBatchSetAtGuardRevision`, `ProtectionTargetAndExpectedBatchOrRequestBlockRevisionExact`, `IdempotencyFingerprintValid`; atomically installs a deletion-request/batch block that also rejects later registration of an affected dispatched batch and races the same local transition as `ReserveAndConsumeDeletionBatchEffect`. It returns exactly `ConsumptionBlockedByAdmissionIntegrity`, `ConsumptionReserved`, `Consumed(Vector)`, or existing `RevokedBeforeConsumption`; the result is mirrored as the authoritative cancel-versus-consume order. Any result blocks new batches/purge/completion, reserved/consumed destruction cannot be recalled, and unknown/lost acknowledgement uses exact protection lookup and remains pending |
| `GovernanceProtection:ActivateReattestedDeletionBatch` | Tenant | none; reopens only one protection-owner compromise block after guard replacement and successor dispatch | `AuthenticatedProtectionOwnerConsumptionBlockedByCapabilityKeyCompromiseReceiptExact`, `GuardReattestedSameBatchManifestNextAttestationHealthyKeyAndOldAttestationRevokedExact`, `SuccessorDeletionBatchDispatchAuthorizedForThatNewAttestationExact`, `ProtectionOwnerExpectedBlockedRevisionMatches`, `ReplacementCapabilityKeyVersionAttestationOrdinalSuccessorDispatchReceiptExpectedBlockedBatchRevisionAndExpectedTenantKeyBlockSetRevisionBoundInActivationIdentity`, `NoTenantKeyVersionBlockForReplacementAttestationAtExpectedProtectionOwnerRevision`, `NoAdmissionIntegrityBlockOrTerminalReservationConsumption`, `IdempotencyFingerprintValid`; in one protection-owner transaction conditionally changes the same batch from the exact capability-key block to `Unconsumed` under only the new attestation/dispatch. If the replacement-key block exists or wins the same compare, it returns `ActivationBlockedByReplacementKeyCompromise(NewKeyVersion, NewRevocationRevision, NewBlockReceiptId)` and conditionally retains or changes the batch to the exact new-key `ConsumptionBlocked(CapabilityKeyCompromise, NewRevocationRevision)` state; activation never clears that tenant-key block. A stale/missing dispatch, different compromise receipt, old attestation, admission block, reserved/consumed vector, stale key-block-set revision, or unknown owner state cannot reopen. Exact lookup by `DeletionBatchReattestationActivationId` recovers the identical activated-or-new-key-blocked result and no second batch or target authority is created |
| `GovernanceProtection:DeletionPurgeRecovery` | Tenant | none; irreversible recorded-branch recovery reads no mutable readiness record | `DestructionSealedReceiptDecisionPolicyLifecycleStoreScopeFenceAndVerificationRevisionsMatch`, `WorkflowCapabilityAndTargetExact`, `AcceptedResourceSetAndAppendOnlyContentViolationTargetCoverageManifestExact`, `EveryAcceptedAndContainmentManifestBatchOutcomeVectorOrAlreadyDestroyedCoverageExact`, `NoDeletionIntegrityCompromisedOrUnattributableAdmission`, `NoPostStartAdmissionViolationOrUnresolvedAdmissionEvidence`, (`NoOverlappingPostStartHoldContender` or `ExactEffectiveDeletionAllowedPostStartDispositionAppliedForPurge`), (`RecordedNoLifecycleCoveredCopyProofExact` or `PhasePinnedExportStoreProtectionAndSecretsDirectlyAvailable`), `PriorPurgeOutcomesLookupExact`, `EveryObservedContentScopeFenceViolationCoveredAndCopyPurgedOrCompletionBlockedExact`; may only finish the recorded branch; unknown batch/export-copy/ledger/hold evidence remains pending |
| `GovernanceProtection:AuthorizeDeletionCompletionBarrier` | Tenant | none; authorizes no terminal result or external effect | `DestructionSealedReceiptDecisionPolicyLifecycleStoreCanonicalPredicateEveryFenceOrdinalAndContentBindingExact`, `WorkflowCapabilityAndTargetExact`, `EveryServerAssignedAcceptanceOrdinalPartitionAndCurrentZeroHighWaterExact`, `EveryGuardIssuedBatchAndActiveOrTerminalAttestationAccountedExact`, `ProtectionFenceTargetCoverageMapsEveryViolationResourceToExactConsumedPerTargetVectorOrAlreadyDestroyedReceipt`, `AllCopyKeyIndexProjectionWorkflowAndBackupReceiptsExact`, (`RecordedNoLifecycleCoveredCopyProofExact` or `RecordedPreparePinnedExportLifecycleStoreAuthorityExact`), `NoDeletionIntegrityCompromisedOrUnattributableAdmission`, `NoAcceptedOrUnresolvedAdmissionOrContentViolation`, (`NoOverlappingPostStartHoldContender` or `ExactEffectiveDeletionAllowedPostStartDispositionForCompletion`), `NoOverlappingHoldContenderOrExportPreparingOrCleanupPending`, `ObservedGovernanceScopeGuardRevisionCompareTokenExact`, (`NoPriorCompletionAuthorization` or `RecordedPriorCompletionAuthorizationStaleAtExactGuardRevision`), `CompletionAuthorizationAttemptOrdinalAssignedOnceOrExistingExact`, `FenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; freezes the complete completion manifest/digest and observed guard revision, but does not complete deletion |
| `GovernanceProtection:CommitDeletionCompletionBarrierEffect` | Tenant | none; exact authorization permits one `GovernanceScopeGuard(TenantId)` terminal operation | `RecordedDeletionCompletionBarrierAuthorizationRevisionAttemptOrdinalManifestFenceCoverageDigestAndObservedGuardRevisionExact`, `GovernanceScopeGuardExpectedRevisionEqualsAuthorizationObservedRevision`, `AtomicCurrentAcceptanceOrdinalPartitionsZeroProofsAndContentBindingCheckpointExact`, `AtomicEveryGuardIssuedBatchTargetAndActiveOrTerminalAttestationRepresentedInCompletionManifest`, `ImmutableFenceOwnedCoverageDigestMapsEveryViolationResourceToThoseTargetsAndAuthenticatedProtectionVectors`, `AuthenticatedCopyReceiptsReferencedByAuthorizationCoverEveryTargetAndResource`, `AtomicNoIntegrityCompromiseUnattributableAdmissionOrUncontainedViolation`, (`AtomicNoOverlappingPostStartHoldContender` or `ExactEffectiveDeletionAllowedPostStartDispositionAppliedForCompletionInThisCommit`), `NoExportPreparingOrCleanupPending`, `CompletionIdempotencyIdentityExact`; conditionally appends at the observed guard revision and serializes with every admission/content write, hold/release, containment issue, batch dispatch, and capability-status mutation. Any intervening guard mutation returns authenticated `DeletionCompletionAuthorizationStale(CurrentGuardRevision)` and no completion; a concurrent fence coverage change makes the authorization lose at its fence revision. If unchanged, it records immutable `DeletionCompletionSealed`; every later matching admission/content write rejects. Exact lookup returns only the same stale-or-sealed result |
| `GovernanceProtection:RecordDeletionCompletionBarrierStale` | Tenant | none; records only an authenticated stale guard result | `RecordedCompletionAuthorizationRevisionAttemptOrdinalAndObservedGuardRevisionExact`, `AuthenticatedDeletionCompletionAuthorizationStaleCurrentGuardRevisionExact`, `NoDeletionCompletionSealByExactGuardLookup`, `ProtectionFenceExpectedRevisionMatches`, `IdempotencyFingerprintValid`; appends the stale result and permits reauthorization only after the winning guard mutation is fully reconciled |
| `GovernanceProtection:RecordDeletionCompleted` | Tenant | none; records only the authenticated terminal guard result | `RecordedCompletionAuthorizationRevisionAttemptOrdinalAndManifestExact`, `AuthenticatedDeletionCompletionSealedReceiptGuardRevisionAndExactOutcomeLookupResolved`, `ProtectedDeletionAndProtectionFenceExpectedRevisionsMatch`, `IdempotencyFingerprintValid`; mirrors final completion. A direct clean-read completion or direct `ProtectedDeletion` append is forbidden |
| `ReadinessObservation:ObserveEventStoreBootstrapOrRepair` | Platform; target must be `LR-EVENTSTORE` | none; this command is the first/repair write to the EventStore-backed registry | `PlatformProducerAuthorized`, `EnvironmentAndEventStoreTargetIdentityExact`, `SignedAppendReadReplayManifestValid`, `ObservationIdValid`, `ExpectedGateRevisionMatches` (`NoStream` or exact current revision) |
| `ReadinessObservation:ObserveOtherGate` | target GateId's declared scope | `LR-EVENTSTORE` | `AuthorizedProducerMatchesTargetGate`, `EvidenceManifestSchemaValid`, `ObservationIdValid`, `ExpectedGateRevisionMatches` |
| every other family/variant | Tenant | v3 set unchanged | none |

Contract fixtures exercise first use in an empty tenant and repair from each missing, disabled, stale, or incompatible direct precondition. Every ordinary bootstrap fixture also carries at least two simultaneous failures: the omitted circular gate and one unrelated required gate. It proves the circular gate is not read, the unrelated failure still blocks, all direct preconditions are recorded, Platform variants are tenant-independent, and a missing or ambiguous target fails closed. Dedicated fixtures prove the EventStore observation can create `NoStream`, replace a stale/blocking observation, cannot target another GateId, and records no pass when its own append fails; kill-switch fixtures prove a confirmed SM-4 or recorded Release-Operator review pull still appends when topology, tenant-access, or audit readiness is blocked, while missing/stale/cross-tenant incident evidence and every release remain blocked; provisioning fixtures prove exact replay returns the first identity and divergent state fails. Replay fixtures prove nonce registration precedes even an existing target idempotency result, first use, concurrent create, later byte-identical duplicate, exact lost-ack recovery, cross-tenant/changed-field rejection, unavailable-ledger failure, and denial to every public, human, Workflow, general-dispatcher, or wrong-stream caller. Security-recorder fixtures prove closed routing, durable spool/idempotency, outage recovery, and ACL confinement. Decision fixtures prove Release-Operator recording, independent root/predecessor governance, nonempty and non-self-weakening quorum/affected sets, effective-versus-pending projection, union blockers, concurrent publish/approve/activate, lost-ack recovery, materialized PRD decision scope, and story-only/Automatic-only exclusion from global `RQ-1`. Governance fixtures exercise every initial, prepare-branch-decision, and dedicated resume/unwind/cleanup recovery row before and after its disposition and first/last acknowledgement, including concurrent resume/abort, current readiness becoming blocked after export cleanup or `DestructionStarted`, later holds over committed artifacts, and holds racing armed deletion: recovery must still record receipts and converge using only the one frozen branch/direct evidence. Carried-forward matrix-v6 deletion fixtures retained by v7 additionally race an ordinal-N caller before/at/after N+1 installation and delayed detection; post-start hold observation before/at/after accepted/containment dispatch with Open, missing, deletion-allowed, and hold-allowed outcomes; same-alias distinct resources before/after original issue and consumption; signing rotation/compromise/revocation/re-attestation against consume; and completion authorization against every admission/content/hold/release/coverage/containment/capability mutation, lost acknowledgement, and restore. An unrecognized workflow activity has no fallback row and blocks.

The matrix is additive and immutable per version. The readiness decision exposes matrix version 7, concrete variant, evaluation scope, `RegistryCheckpoint` (`Read(RegistryRevision)` or `NotRead`), applicable GateIds, direct-precondition results with their owner revisions, and safe blockers so every surface renders the same outcome. Version 7 retains v6 and adds stable pre-seal destruction identity plus protection-owner replacement-key block arbitration during re-attestation activation; it does not change any Product-owned outcome. Only a row whose required GateId set is explicitly `none` may record `NotRead`; it never invents revision zero.

Matrix-v7 deletion fixtures retain those races and additionally force two or more consecutive signed-obsolete accepted-batch attempts across guard mutations while the stable seal/batch ids remain byte-identical and the eventual committed seal revision is recorded separately. They race compromise of each replacement key before re-attestation, before successor dispatch, before/at activation, and after activation but before reservation; every activation returns one exact activated-or-new-key-blocked outcome, and the latter is eligible for another same-batch re-attestation. They prove one protection-owner order, no activation can clear a global tenant-key block, no post-block attestation can consume, no false re-attestation from a read or pending block, and recovery of every blocked/reserved/terminal state and activation identity across crash, lost acknowledgement, migration, and restore. Separate fixtures force guard issue stale before signing, after signer effect, after signer-result recording, and after lost acknowledgement; only exact no-issue proof permits `SignedAttestationObsoleteUnissued`, the successor signing request changes attempt/revision deterministically while seal/batch identity does not, and emergency re-attestation never substitutes for this pre-issue path. Registrar fixtures prove issuer/audience/tenant/key-version/revocation binding, first/replay/lost-ack behavior, protection-before-guard-mirror ordering, cross-tenant rejection, and unavailability to every public, human, Workflow, target-handler, and general-dispatcher caller.

The `ReleaseQualificationGateSet` contains all 18 minimum GateIds. Only current `Pass` records for the complete set plus `Available` consumed external dependencies allow `RQ-1` READY and production enablement. Its metric slice is exactly pre-enablement SM-1, SM-4, SM-5, and SM-6 through `LR-PRODUCT-METRICS`, NFR-9 through `LR-RUNTIME-PERFORMANCE`, and NFR-14 through `LR-UI-PERFORMANCE`; SM-5 supplies the 100% audit-completeness threshold. SM-2, SM-3, and SM-7 are never `RQ-1` inputs. The remaining GateIds enforce the non-metric safety, context, dependency, recovery, capacity, accessibility, payload-protection, and governance inputs that FR-28 also names. `LR-RECOVERY`, `LR-UI-CONFORMANCE`, `LR-RUNTIME-PERFORMANCE`, `LR-UI-PERFORMANCE`, and `LR-PRODUCT-METRICS` therefore qualify release without circularly blocking the controlled executions that produce their evidence.

## Launch Health Reviews

Launch health is recorded separately from `RQ-1`. It consumes real post-enablement SM-2, SM-3, and SM-7 results at 30 days, 60 days, and monthly thereafter. It never changes which inputs produced an earlier READY/NOT READY decision and it cannot be used to fill a missing pre-enablement gate.

| Metric | Launch-health contract |
| --- | --- |
| SM-2 | Eligible Conversation adoption over the PRD rolling window and cohort, using the `EXT-CONV-AI-1` active-Conversation count or Conversations-side event-feed seam; never an Agent Call denominator. |
| SM-3 | Human proposal-decision timeliness and the associated posting-failure/audit-completeness reporting defined by the PRD. Audit completeness remains independently required at 100% before enablement through SM-5. |
| SM-7 | Numerator: distinct proposals edited, regenerated, or rejected before a human decision in the window. Denominator: distinct proposals reaching a human decision in the window. Attainment band: 10% through 60%, inclusive. Classification: launch health only, reviewed at 30 days, 60 days, and monthly thereafter. Missing authoritative decision/version events, incomplete window/cohort data, an empty denominator, or unmet contract-specific sample rules returns `InsufficientEvidence`; it never passes and never gates `RQ-1`. |

SM-C1..SM-C5 are calculated and reported with the launch-health family at the same cadence. They are counter-metrics, never `RQ-1` inputs, and cannot be omitted merely because the primary launch-health metrics are available.

Missing samples or an ineligible cohort produce `InsufficientEvidence` in the launch-health review and never silently pass. Two consecutive SM-3 or SM-7 misses require the recorded Product disable-or-continue decision and feed the FR-28 kill-switch trigger review. `product-metrics` is the only trigger source.

## Live-Seam Matrix

The matrix is the home of the AD-17 bind-and-test atomicity rule (approved sprint change proposal of 2026-08-04). Every row maps one seam to its current authority, its `BindingStatus` (`Deferred`, `DeferredOutOfV1`, or `Live`), and the `Hexalith.Agents.IntegrationTests` lane that must pass in the same change that flips it to `Live`. Story 5.1 owns the verifier that enforces the matrix; Story 5.6 creates the project and covers the projection and query seams that Stories 5.2 and 5.3 bound before it existed.

| Seam | Authority | `BindingStatus` | Required integration test |
| --- | --- | --- | --- |
| EventStore dispatch and setup projections (`agent-setup`, `provider-catalog`, `tenant-provider-enablement`) | AD-2, AD-3, AD-15, AD-17 | `Live` for shipped setup/catalog source; platform catalog migration and tenant enablement are `Deferred` (reopened Story 5.3; coverage owed by 5.6) | `SetupProjectionLiveTests`, `ProviderCatalogMigrationLiveTests`, `TenantProviderEnablementProjectionLiveTests` |
| Readiness observations and `launch-readiness` | AD-17 | `Deferred` (Story 5.5/5.6) | `LaunchReadinessProjectionLiveTests` |
| Trusted-envelope replay, content-free denial recording, and architecture-decision materialization | AD-2, AD-3, AD-17, AD-30 | `Deferred` (Stories 5.4, 5.5, 5.6) | `TrustedEnvelopeReplayAndOrderingLiveTests`, `SecurityAuditSpoolRecoveryLiveTests`, `ArchitectureDecisionProjectionLiveTests` |
| Dapr Workflow durable owner and restart recovery | AD-18, AD-23, AD-27 | `Deferred` (Story 6.1) | `InteractionWorkflowRecoveryLiveTests` |
| Provider invocation, reservation, admission | AD-13, AD-21, AD-24 | `Deferred` (Story 6.4, 6.5) | `ProviderAttemptProtocolLiveTests` |
| Conversations membership and posting | AD-6, AD-7 | `Deferred` (Story 6.6) | `ConversationPostingLiveTests` |
| Conversation interaction-directory migration and EventStore write fence, target-key-alias permits, creation/start outboxes, reserved/committed effect leases, durable deletion feed/acknowledgement, immutable per-admission-ordinal Closing/Effective cycles, convergence, candidate/refreeze, fence acceptance, and atomic destruction-start barrier | AD-2, AD-6, AD-7, AD-12, AD-22, AD-29, AD-30 | `Deferred` (Stories 6.1 and 8.3) | `ConversationInteractionDirectoryMigrationAndEffectLeaseLiveTests`, `ConversationDeletionPropagationFailureInjectionTests`, `DeletionDestructionStartBarrierRaceTests` |
| Content safety adapter and distributed verdict rescan | AD-20 | `Deferred` (Stories 6.3, 8.4) | `ContentSafetyDecisionLiveTests`, `SafetyVerdictRescanRecoveryLiveTests` |
| Parties human/liveness classification and historical Party-to-actor binding for Approvers | AD-8, AD-22, AD-30 | `Deferred` (Story 5.4) | `HumanApproverResolutionLiveTests`, `HistoricalHumanActorBindingLiveTests` |
| EventStore payload protection, erased replay, and reference-only workflow state | AD-22, AD-27 | `Deferred` (Story 5.8) | `PayloadProtectionLiveTests`, `WorkflowContentSweepTests` |
| Shared protection fence plus tenant hold/deletion scope guard, protected export store, continuous deletion content guard, manifest-batch key destruction, and deletion completion | AD-22 | `Deferred` (Stories 8.1 to 8.3) | `LegalHoldDeletionScopeGuardFailureInjectionTests`, `ExportDeletionFenceRaceTests`, `AuditExportManifestAndStoreLiveTests`, `ProtectedDeletionManifestBatchAllCopiesLiveTests` |
| Governed audit inspection | AD-22, AD-30 | `Deferred` (Story 8.8) | `AuditInspectionLiveTests` |
| Tools, MCP, A2A, remote agents | AD-19 | `DeferredOutOfV1` | none until a separately approved scope |

## Provider Readiness Contract

The public `ProviderReadinessResult` contains exactly `OperationalState`, `Callability`, `ReasonCode`, `CapabilityVersion`, `CurrentDataHandlingVersion`, `InForceDataHandlingVersion`, optional exclusive `GraceExpiresAt`, `ObservedAt`, exclusive `ValidUntil`, and the discriminated `Freshness` value (architecture AD-17). `GraceExpiresAt` is present only during an unexpired declared-tightening grace and later unaccepted versions never extend it. The catalog is platform-scoped: `CapabilityVersion` is a non-reusable unsigned monotonic sequence per (`ProviderId`, `ModelId`) entry in the `ProviderCatalog` aggregate stored under the reserved EventStore tenant `system`; comparisons across keys are undefined. A tenant reads the platform entry joined with its `TenantProviderEnablement`; a by-key query for an entry not enabled for the caller's tenant, or absent, returns the not-found response, and `EntryMissing` is reported only for the Agent's own currently-selected entry or an in-flight interaction's previously-snapshotted entry inside `AgentReadinessStatus`; terminal historical interactions use immutable snapshot and authorized Audit Evidence instead. `ProviderReadinessReasonCode` is versioned and additive: `Unknown = 0`, `None`, `NonBlockingOperationalWarning`, `DependencyUnavailable`, `EntryMissing`, `Stale`, `Disabled`, `Unconfigured`, `Unpriced`, `InvalidLimits`, `SecretUnavailable`, `CapabilityVersionRegressed`, `AdapterUnavailable`, `ProviderHealthFailed`, `Indeterminate`, `CurrencyMismatch`, `DataHandlingAcceptanceLapsed`, and (added 2026-09-09) `PlatformNotReady`, the code a tenant-facing surface reports in place of any platform-only blocker (`Unconfigured`, `Unpriced`, `InvalidLimits`, `SecretUnavailable`, `AdapterUnavailable`, `ProviderHealthFailed`) whose detail is disclosed to the Platform Operator only.

Only (`Ready`, `Callable`, `None`), (`Degraded`, `Callable`, `NonBlockingOperationalWarning`), and (`Blocked`, `Blocked`, a defined blocker code) are valid. `Degraded` is callable only when `EXT-PROVIDER-1` is `Available` and verified and every hard gate passes: freshness, platform enabled state, tenant enablement, configured/text-generation state, current pricing, secret resolution, Provider health, non-regressed capability version, valid positive limits, a complete data-handling record, and current tenant acceptance or an unexpired declared-tightening grace. Unknown codes and missing, stale, disabled, unconfigured, unpriced, invalid, unavailable, failed, regressed, acceptance-lapsed, or indeterminate inputs are `Blocked`.

## NFR-11 Recovery Evidence Contract

`LR-RECOVERY` uses a production-like failure-injection exercise identified by `RecoveryExerciseId`. Immediately before injection it freezes the cohort of every durable nonterminal interaction eligible to run and the current interaction-directory migration/cutover and every pending interaction permit/creation or workflow-start outbox/directory acknowledgement/effect lease, Conversations deletion-feed item/Agents acknowledgement, Conversation-deletion Closing/Effective barrier manifests/workflow, rate/open/budget decision, safety-epoch activation, proposal-index checkpoint, hold/export/deletion fence token, export-key delivery authorization/pending outcome, governance-protection workflow, unexpired trusted-envelope replay record, pending security-audit spool record, and current architecture-decision projection position. Items created after injection or already terminal/settled/acknowledged before the freeze instant are excluded and cannot improve the result.

One injected monotonic fixture clock records `RecoveryClockOriginId`, `RecoveryStartedTick` when the fault makes processing unavailable, and `RecoveryVerifiedTick` when every frozen member is accounted for, authoritative EventStore state and required projections are current, and terminal outcomes are observable. `RecoveryVerifiedTick - RecoveryStartedTick` must be at most 15 minutes. UTC `ObservedAt` and `ValidUntil` govern record freshness only; they are never subtracted for recovery duration.

After restart, current safety, authorization, cost, and capacity gates are re-evaluated before any new effect. A still-reversible governance preparation may not choose resume versus unwind from mutable reads. `Resume` comes only from the original still-valid prepare intent and unchanged recorded values. Export `Abort` comes only from its recorded authorized cancellation or phase-pinned closed failure mapping. Hold `Abort` additionally requires approved `OD-HOLD-PREPARE-CANCELLATION-1`. Conversation deletion has no Abort. Operator deletion also has no Abort while `OD-OPERATOR-DELETION-CANCELLATION-1` is Open or forbids it; if approved, only its named cancellation evidence, cleanup-complete receipts, exact removal of every request admission-fence ordinal plus the continuous content guard and all ordinal/token bindings, release of every owner/ordinal cut cycle, and final recorded outcome may advance before `DestructionSealed`. Dependency failure never invents deletion cancellation. `ExportPreparing` phase-pins lifecycle/store values, and any already-recorded prepare, disposition, release, commit, cleanup, or guard-owned destruction decision remains bound to its exact recovery row/direct evidence.

Deletion recovery for either origin replays its predicate; every admission ordinal and immutable ledger partition; explicit pre-cut/post-cut containment receipt and one successor assignment, including repeated pre-binding recuts, the gap-free invalidation chain, and terminal obsolete binding authorizations; each separately keyed owner/ordinal cycle; and each global cut before candidate. It recovers candidate/acceptance, initial content-guard install or continuous successor binding, preparation, exact hold-contender state, guard-revision-bound barrier authorization, authenticated stale result/reauthorization, atomic scope-guard seal, seal lookup/mirror, non-expiring capability-key verification across restore, manifest-batch consumption or exact lookup, and per-target vectors. Legal-hold recovery likewise recovers guard registration before prepare and guard release only after every unpin/fence receipt. Migration repair freezes/drains the pre-boundary directory/effect cohort and preserves every admission ordinal/violation chain, owner cycle, content binding, hold contender, barrier/containment compare authorization and stale result, seal, and batch identity/capability-key/guard-issued state plus referenced protection-owner consumed-or-revoked outcome. After sealing, recovery accounts for the accepted set plus each ProtectionFence-serialized singleton containment ordinal and its guard-owned authorization/effect/issued-or-stale result before protection consumption; an integrity failure races revocation against unconsumed batches and always blocks completion. The qualification manifest inventories every identity, decision, high-water, fence, cycle, contender, binding/gap chain, guard compare token, authorization/stale outcome, capability-key version, batch state, vector, receipt, and terminal outcome; failure injection surrounds each authorization/effect/result, repeated recut/binding-obsolescence boundary, admission/hold/guard-compare/seal boundary, containment allocation/issuance, non-expiring restore, batch consumption/revocation, and first/last acknowledgement. The run passes only with RPO 0, no duplicate/partial effects or changed decisions, drained security records, and every manifested/accepted/contained member accounted for.

## NFR-12 Capacity And Fairness Profile

Every `LR-CAPACITY-FAIRNESS` record's `ConfigurationOrMeasurementContract` contains a versioned environment profile with these mandatory values:

```text
ProfileVersion
AdmissionPreparationTimeout
PerTenantConcurrencyLimit
SystemConcurrencyLimit
PerTenantQueueDepthLimit
SystemQueueDepthLimit
OverflowBehavior = Queue | Reject
FairnessPolicyKind = WeightedRoundRobinV1
TenantWeight[tenant] = integer 1..100
```

All limits and `AdmissionPreparationTimeout` are positive and test-visible. For each admission the profile is valid only when the timeout is strictly shorter than both frozen rolling-rate windows, so its derived `PreparationDeadline` is strictly earlier than both `PartyResetAt` and `ConversationResetAt`; equality, overflow, or a longer timeout blocks before either ledger. The timeout is the sole source from which AD-13 and AD-21 derive their stored open-lease and two-scope rate-preparation deadlines once from the interaction command's AD-28 `EvaluatedAt`; both ledgers receive the same exact deadline and `ProfileVersion`, never derive from receipt time, and reject a mismatch without state change. Reaching either reset boundary before both preparation acknowledgements drives the same interaction-owned abort decision even if the separate preparation deadline has not elapsed. One platform-composed shared capacity allocator is the sole owner of tenant/system leases and queue order across replicas; process-local counters or queues are forbidden. A linearizable admission keyed by deterministic `AttemptId` atomically acquires both scopes and returns admitted with `AdmissionId` plus monotonically increasing `AdmissionFence`, queued with durable `QueueId`, or rejected before `ProviderInvocationAuthorized`. The authorization fact binds the fence. Immediately before Provider transport, a linearizable `BeginInvocation(AttemptId, AdmissionId, AdmissionFence)` must verify the unexpired current fence and transition `Admitted` to `InvocationActive`; reclamation or terminalization increments/invalidates the fence, so stale authorization can never invoke. Within a tenant, order is allocator sequence then `AttemptId`. Across eligible nonempty tenants, a persisted rotating cursor grants exactly `TenantWeight` slots per complete cycle of `sum(active TenantWeight)`, giving complete-cycle share `TenantWeight / sum(active TenantWeight)`. Retries retain their original `QueueId`/position and receive no additional weight.

Queue cancellation and expiry are durable terminal admission results and permit no Provider call. Leases are reclaimed only after a recorded terminal attempt or after expiry beyond the Provider timeout plus authoritative Provider outcome lookup proving no active invocation. The versioned test saturates at least two tenants across complete cycles and proves exact shares, no starvation, replica/crash/cancel/expiry recovery, and no duplicate admission while tenant/system concurrency, queue, and cost caps remain unbreached. Missing values, hidden host-only values, unbounded queues, process-local state, or insufficient evidence block the gate.

## NFR-13 UI Conformance Contract

`LR-UI-CONFORMANCE` evidence covers every interactive V1 route and high-impact state through the same public contracts used by API clients. The versioned browser/component suite proves WCAG 2.2 AA behavior, keyboard/focus order, semantic labels and live regions, whole-string localization, English/French key parity, and FrontComposer plus Fluent UI Blazor V5 inheritance. At the most restrictive supported viewport, all ten matrix-v7 lock-bearing families — `ProposalResolution`, `PolicyPublication`, `TenantBudgetUpdate`, `LegalHold`, `ExportRequest`, `DeletionRequest`, `ProviderCatalogMutation`, `AgentSetupMutation`, `AgentActivation`, and `DataHandlingAcceptance` — remain blocked whenever required decision context cannot be presented safely; this includes both accept and decline for data-handling acceptance and the matrix-defined related operations grouped under those families. Missing routes, locale keys, viewport evidence, or conditional skips produce `InsufficientEvidence`.

## NFR-14 Browser Monotonic Timing Contract

Browser durations use one injected monotonic clock and one clock-origin identifier per page lifecycle. They never subtract browser time from server or projection wall-clock timestamps.

| Gate | Start seam | End seam | Threshold |
| --- | --- | --- | --- |
| Page usability | `NavigationStartedTick` captured when navigation begins | `PageUsableTick` captured after the authorized route renders a non-loading state and its required primary action/status is operable | p95 <= 2.5 seconds |
| Authoritative pending acknowledgement | `CommandSubmittedTick` captured immediately before dispatch | `AuthoritativePendingRenderedTick` captured after a server/EventStore-accepted pending identity and projection/version reference is received and the pending state is rendered | p95 <= 500 milliseconds |
| Terminal render and announcement | `AuthoritativeTerminalReceivedTick` captured when the client receives the authoritative terminal projection/version | both `TerminalRenderedTick` and `LiveRegionAnnouncedTick`; the sample duration uses the later tick | p95 <= 2 seconds |

Every versioned sample has common fields `SampleId`, `QualificationSessionId`, `ExecutionId`, `SampleKind`, `ClockOriginId`, route/operation family, safe trace reference, authoritative projection ID/version where applicable, viewport/profile, locale, and outcome. `SampleId` is deterministically derived from (`QualificationSessionId`, `ExecutionId`, `SampleKind`). An exact duplicate is an idempotent no-op; a conflicting duplicate is rejected and makes the evidence insufficient.

| `SampleKind` (`SampleId = H(sample, TenantScope, QualificationSessionId, ExecutionId, SampleKind)` per architecture AD-29) | Required ticks | Forbidden ticks |
| --- | --- | --- |
| `PageUsability` | `NavigationStartedTick`, `PageUsableTick` | pending and terminal ticks |
| `AuthoritativePending` | `CommandSubmittedTick`, `AuthoritativePendingRenderedTick` | page-usability and terminal ticks |
| `TerminalRenderAnnouncement` | `AuthoritativeTerminalReceivedTick`, `TerminalRenderedTick`, `LiveRegionAnnouncedTick` | page-usability and pending ticks |

`LiveRegionAnnouncedTick` is captured after render commit when the localized terminal-text mutation is observable in its `aria-live` or `role=status` node; it proves announcement-ready DOM state, not speech completion. Only samples from an authenticated versioned qualification session issued by `EXT-TOPOLOGY-1` enter `browser-ui-metrics`. The platform ingress validates kind-specific ticks, monotonic order, clock origin, session, and safe trace/projection against server evidence. Raw prompt, context, generated/proposal content, secrets, Provider payloads, and Party PII are forbidden. Unattested operational telemetry cannot qualify. Each kind requires at least 30 qualifying production-like executions. Missing/forbidden ticks, mixed origins, absent authoritative references/live-region mutation, failed correlation, conflicting duplicates, or too few samples yields `InsufficientEvidence`.

## Explicit Projection Inventory

These logical projection IDs are authoritative for readiness, deletion scope, and evidence manifests. Implementations bind storage/type names to these IDs; a story cannot replace the inventory with “all affected projections.”

| Projection ID | Owned truth exposed |
| --- | --- |
| `agent-setup` | Agent identity/configuration/lifecycle, kill-switch state, and gate-derived callability (shipped id, ratified 2026-09-09). |
| `provider-catalog` | Platform Provider/model capability, readiness, pricing, secret-configured state, freshness, and effective version (shipped id, ratified 2026-09-09). |
| `tenant-provider-enablement` | Per-tenant enabled entries joined with the platform catalog for tenant-facing readiness. |
| `agent-interaction-status` | Current interaction state, safe failure class, attempt/posting outcome, and authoritative version. |
| `conversation-interaction-directory` | Content-free view of `InteractionDirectoryMigration` readiness/write-fence epoch/integrity invalidation, `ConversationAgentState` permits, creation/workflow-start/terminal acknowledgement revisions, reserved/committed/settled/cancelled effect-lease identities and high-waters, and permanent Closing/Effective deletion barrier manifests; the aggregate streams, not this rebuildable view, are intake/effect-cutover/deletion-completeness truth. |
| `proposal-detail` | Current proposal state, selected version, expiry, and authorized detail metadata. |
| `proposal-version-history` | Immutable generated, edited, and regenerated version history. |
| `pending-proposal-queue` | Authorized pending proposals ordered for action. |
| `pending-proposal-count` | Authorized pending counts for navigation and Conversation status. |
| `audit-evidence` | Tenant-scoped safe evidence graph linking calls, decisions, versions, and final posts. |
| `rate-limit-usage` | Immutable per-Party and per-Conversation rolling-window consumption and `ResetAt`. |
| `open-interaction-leases` | Original-caller concurrency leases held through overall interaction/posting terminality. |
| `budget-reservation-usage` | Monthly monetary reservations, usage reconciliation, settlement, and warning/block state in the original budget period. |
| `safety-verdict-status` | Tenant invalidation epoch, bounded rescan state, per-Conversation high-water, and safe `RescanPending` status; never content. |
| `safety-verdict-directory` | Content-free keys for existing `SafetyVerdictIndex` Conversations plus source EventStore position; supplies caught-up deterministic count/hash/high-water enumeration for safety-epoch activation and never carries message content or verdict payload. |
| `retention` | Sensitive-content retention deadlines and expiry execution state. |
| `legal-hold` | Active/released legal holds and their protected scope. |
| `export` | Authorized export requests, manifests, expiry, and audit status. |
| `export-artifact-index` | Content-free interaction-to-export-artifact lookup used by hold and deletion frozen-set resolution. |
| `export-artifact-store` | Not a projection: immutable encrypted bytes under `EXT-EXPORT-STORE-1`; a named hold/deletion/purge-receipt scope item. |
| `deletion` | Requested/confirmed payload protection, closed `OperatorApprovedDeletion` versus `ConversationApprovedDeletion` origin with only origin-valid safe authority evidence, each named purge outcome, and safe tombstone completion. |
| `launch-readiness` | Current gate records and aggregate callability/READY blockers by scope/profile. |
| `runtime-metrics` | NFR-9 runtime latency source observations and qualification results. |
| `browser-ui-metrics` | NFR-14 browser-monotonic samples and qualification results. |
| `workflow-execution-state` | Not a projection: the Dapr Workflow state-store scope for terminal interaction instances, carrying references only (architecture AD-27); a named purge scope item whose confirmation deletion completion requires. |
| `product-metrics` | SM-1/SM-4/SM-5/SM-6 gate calculations, SM-2/SM-3/SM-7 launch-health rolling-window/cohort calculations, SM-C1..SM-C5, and insufficiency state. |

Deletion completion names and confirms `provider-catalog` only when it contains protected content, `workflow-execution-state`, `agent-interaction-status`, `proposal-detail`, `proposal-version-history`, `pending-proposal-queue`, `pending-proposal-count`, `audit-evidence`, `retention`, `legal-hold`, `export`, `export-artifact-index`, every matching `export-artifact-store` object, `deletion`, and any content-bearing metric projection identified by its current projection contract. `agent-setup`, content-free `conversation-interaction-directory` migration/permit/outbox/effect-lease/barrier/tombstone evidence, `tenant-provider-enablement`, `rate-limit-usage`, `open-interaction-leases`, `budget-reservation-usage`, `safety-verdict-status`, content-free `safety-verdict-directory`, `launch-readiness`, and non-content metric records retain only support-safe references required by policy.

## Initial Gate Records

No qualifying evidence was supplied when this register was created. The initial records therefore block readiness without inventing observations or validity periods.

| GateId | TenantScope | EnvironmentProfile | State | Owner | SourceVersion | ObservedAt | ValidUntil | RequiredEvidenceLevel | EvidenceReference | ConfigurationOrMeasurementContract | BlockerCode |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `LR-TOPOLOGY` | `TBD` | `production-like` | `InsufficientEvidence` | Platform Maintainer | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | `EXT-TOPOLOGY-1`; exact fixture/profile contract required | `EvidenceNotRecorded` |
| `LR-EVENTSTORE` | `TBD` | `production-like` | `InsufficientEvidence` | `TBD` | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | RPO 0, command/event/projection/replay contract required | `EvidenceNotRecorded` |
| `LR-TENANT-ACCESS` | `TBD` | `production-like` | `InsufficientEvidence` | `TBD` | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | Fresh role and `AuthenticatedHumanActorId` resolution; versioned historical Party-to-actor binding with mismatch/gap/ambiguity denial; canonical trusted-envelope HMAC with immutable logical-command id, issuer-wide delivery nonce, replay registration before target idempotency, expiry/skew/audience/replay-retention/rotation/emergency-revocation; caller-independent actor/system denial routing through the durable content-free security-audit spool and ACL-confined recorder; human second-party; and focused cross-tenant denial contract required | `EvidenceNotRecorded` |
| `LR-PARTY-IDENTITY` | `TBD` | `production-like` | `InsufficientEvidence` | `TBD` | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | Agent Party identity/posting eligibility plus Approver human/liveness classification and versioned historical Party-to-actor binding contract required | `EvidenceNotRecorded` |
| `LR-CONVERSATION-CONTEXT` | `TBD` | `production-like` | `InsufficientEvidence` | `TBD` | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | Complete authorized context and exact budget contract required | `EvidenceNotRecorded` |
| `LR-CONVERSATIONS-MEMBERSHIP-POSTING` | `TBD` | `production-like` | `InsufficientEvidence` | Conversations Maintainer | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | `EXT-CONV-AI-1` compatibility contract required | `DependencyUncommitted` |
| `LR-PROVIDER` | `TBD` | `production-like` | `InsufficientEvidence` | Agents Runtime Maintainer | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | `EXT-PROVIDER-1` and public readiness contract required | `DependencyUncommitted` |
| `LR-TOKENIZER` | `TBD` | `production-like` | `InsufficientEvidence` | Agents Runtime Maintainer | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | `EXT-TOKEN-1` exact measurement contract required | `DependencyUncommitted` |
| `LR-SAFETY` | `TBD` | `production-like` | `InsufficientEvidence` | Security Engineering | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | `EXT-SAFETY-1` conjunctive snapshot-plus-current decisions and durable tenant rescan profile (positive worker/batch/lease/call-wait bounds, cross-replica recovery, exact `RescanPending`) required | `DependencyUncommitted` |
| `LR-SECRETS` | `TBD` | `production-like` | `InsufficientEvidence` | Platform Maintainer | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | `EXT-SECRETS-1` resolution/rotation/denial/no-leak contract required | `DependencyUncommitted` |
| `LR-COST` | `TBD` | `production-like` | `InsufficientEvidence` | Agents Runtime Maintainer | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | Distinct rolling rate-consumption, original-caller open-interaction lease, and monthly monetary reservation/reconciliation lifetimes plus rollover/recovery contract required | `EvidenceNotRecorded` |
| `LR-AUDIT-PROTECTION-DELETION` | `TBD` | `production-like` | `InsufficientEvidence` | Security Engineering | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | Common payload protection/audit including durable security-spool drain, legacy-directory bootstrap and EventStore write-fence cutover, target-key-alias permit/create/start directory, reserved/committed/settled effect leases, Conversations deletion source delivery/acknowledgement, Closing/Effective barrier manifests, origin-discriminated audit, shared hold/export/deletion fence and fence-owned deletion-inventory acceptance, durable export-key delivery, cryptographic erasure, projection purge, and safe-tombstone failure-injection evidence required; operation-scoped `OD-*` approvals and export-store availability for export-bearing sets are separate blockers and are not prerequisites for this common record or export-free deletion | `EvidenceNotRecorded` |
| `LR-RECOVERY` | `TBD` | `production-like` | `InsufficientEvidence` | Test Architect | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | NFR-11 recovery contract including frozen/direct recorded-branch governance completion, trusted replay, security-spool drain, and architecture-decision projection in this register | `EvidenceNotRecorded` |
| `LR-CAPACITY-FAIRNESS` | `TBD` | `production-like` | `InsufficientEvidence` | Test Architect | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | NFR-12 profile and fairness contract in this register | `EvidenceNotRecorded` |
| `LR-UI-CONFORMANCE` | `TBD` | `production-like` | `InsufficientEvidence` | UX Designer | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | NFR-13 conformance contract in this register | `EvidenceNotRecorded` |
| `LR-RUNTIME-PERFORMANCE` | `TBD` | `production-like` | `InsufficientEvidence` | Test Architect | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | NFR-9 measurement contract required | `EvidenceNotRecorded` |
| `LR-UI-PERFORMANCE` | `TBD` | `production-like` | `InsufficientEvidence` | Test Architect | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | NFR-14 browser timing contract in this register | `EvidenceNotRecorded` |
| `LR-PRODUCT-METRICS` | `TBD` | `production-like` | `InsufficientEvidence` | Test Architect | `TBD` | `TBD` | `TBD` | Levels 4 and 5 | `TBD` | Versioned SM-1/SM-4/SM-5/SM-6 qualification-cohort gate contracts and attainment, including 100% SM-5 audit completeness; plus versioned SM-2/SM-3/SM-7 launch-health rolling-window/cohort contracts and SM-C1..SM-C5 reporting at 30 days, 60 days, and monthly thereafter; launch health and counter-metrics are excluded from `RQ-1` | `EvidenceNotRecorded` |

## Current Release Decision

`RQ-1`: **NOT READY**. Every minimum gate is `InsufficientEvidence`, critical external dependencies are `Uncommitted`, and no production-like tenant scope, source versions, authoritative timestamps, validity periods, or qualifying Levels 4/5 evidence references have been accepted.
