---
title: Hexalith Agents External Dependency Register
status: active
created: 2026-08-02
updated: 2026-09-13
project: agents
authority: sprint-change-proposal-2026-09-09-2.md
---

# External Dependency Register

## Authority And Entry Gate

This register is the authoritative delivery contract for critical Hexalith Agents dependencies delegated by the PRD and the approved Sprint Change Proposal. A consuming story cannot move to `ready-for-dev` unless every dependency it names is `Committed` or `Available` and has no `TBD` target, integration date, or executable verification command.

Unknown values remain `TBD` and the record remains `Uncommitted`. They are blockers, not inferred commitments. A repository path, package version, branch, or local working-tree state is not a commitment unless its owner accepts it in this register.

## Record Schema

`ID` is the stable dependency key. Each record then contains the nine commitment fields required by PRD §8.

| Field | Contract |
| --- | --- |
| `ID` | Stable `EXT-*` identifier; never reused for a different compatibility seam. |
| `Owner` | Named role or person accountable for the artifact and commitment. |
| `Repository` | Owning repository, not the consuming checkout path unless they are the same authority. |
| `RequiredArtifact` | Public package, API, host composition, adapter, fixture, or other deliverable the consumer requires. |
| `TargetVersionOrCommit` | Exact package version or immutable commit. A branch, range, or `latest` is not accepted. |
| `TargetIntegrationDate` | Accepted date by which the target and compatibility evidence will be consumable. |
| `CompatibilityContractAndVerificationCommand` | Stable behavioral contract plus an executable command that proves the target against the consumer. Narrative-only verification is insufficient. |
| `RequiredEvidenceLevel` | Normative PRD Evidence Level required from the dependency. |
| `AcceptedStatus` | `Uncommitted`, `Committed`, or `Available`, using the semantics below. |
| `ConsumingStories` | Exact replacement stories or release gate blocked by this record. |

### Accepted Status Semantics

| Status | Meaning |
| --- | --- |
| `Uncommitted` | One or more commitment fields are unknown or have not been accepted. Every consumer is blocked from `ready-for-dev`. |
| `Committed` | Owner, repository, artifact, immutable target, integration date, compatibility contract and executable command, evidence level, and consumers are accepted. The artifact may still be in delivery. |
| `Available` | The committed target is installed/consumable and its executable compatibility command passes with live Level 4 component evidence. Where `RequiredEvidenceLevel` also names Level 5, cross-system attainment is completed by the consuming launch-readiness gate and `RQ-1`; it is not a prerequisite for beginning that same qualification run. |

Changing an owner, artifact, target, compatibility behavior, verification command, or required evidence level invalidates prior acceptance and returns the record to `Uncommitted` until the changed commitment is accepted. A failed compatibility command makes the dependency unavailable and blocks consumers even if the record was previously `Available`.

`Committed` permits story readiness and contract/package work only. The register owner's compatibility command against the exact committed target is the sole permitted execution of a `Committed` seam, and only to establish `Available`; it does not authorize any consuming runtime, test, or qualification path. No other runtime, test, or qualification path may execute the seam until the record is `Available` and that command has passed against the exact target used by the execution.

A runtime, test, or qualification path that would execute a seam whose record is not `Available` fails closed with the typed outcome `DependencyNotAvailable` naming the record (PRD FR-21). `RQ-1` records the same code for every record in its qualification profile that is not `Available` (PRD FR-28).

## Critical Dependency Records

### EXT-CONV-AI-1 — Conversations AI Membership And Posting

| Commitment field | Value |
| --- | --- |
| `Owner` | Conversations Maintainer |
| `Repository` | `Hexalith.Conversations` |
| `RequiredArtifact` | Six core V1 seams. (1) Membership: public `IConversationClient.AddParticipantAsync` and `POST /api/v1/conversations/{conversationId}/participants`, limited for Agents to stable AI Party identity, `ParticipantType.AiAgent`, and `ParticipantRole.Member`; exact retries are idempotent no-ops, conflicting type/role is a typed conflict, and cross-tenant or general participant management is denied; plus a participant-state read for the AI participant and a participant removal limited to the AI participant for the PRD FR-2 removal block. Before accepting membership, Conversations verifies the immutable provisioned Party by id and additionally verifies AI type only when `EXT-PARTIES-1` commits Branch A; Branch B accepts its approved Organization type by id; the record owner supplies the final member and typed failure names (A-21). (2) Posting: append a Conversation Message as the `AiAgent` participant with an Agents-supplied deterministic `MessageId` and idempotency key, persisted verbatim or rejected (a differing returned id is a typed incompatibility that blocks posting), and message metadata carrying the Agent Call trace reference and provenance flags (AI-generated; human-edited and by which Party), so restart or replay cannot duplicate a post; plus an existence read by `MessageId` returning the message, typed absence, typed `ConversationDeleted`, typed `PrincipalRemovedFromConversation`, or unavailable, so Agents can check before retry or abandonment after a lost acknowledgement and record `LateConfirmed` rather than contradict an already-posted message. (3) Facilitator resolution: `ParticipantRole.Facilitator` on the participant read model. (4) The Eligible Conversation denominator is supplied either as an active-Conversation count per tenant/window or as a Conversations-side tenant event feed from which Agents computes the same count, never from Agent Calls (A-4). (5) Tenant-scoped reads under the Agents Service Principal of complete Conversation content, the Participant roster with roles, and Conversation existence/accessibility; they return typed `ConversationDeleted` and `PrincipalRemovedFromConversation` distinctly from transient denial or error, and content contains the messages a Participant would currently see, including current edit/delete state (A-15). (6) A Conversations-owned durable outbox or ordered tenant/source feed entry created atomically with the approved Conversation-deletion decision. It carries one immutable `ConversationDeletionSignalId` bound one-to-one to that logical approval, `TenantId`, `ConversationId`, stable source stream/revision, approval reference, source-contract version, and an independent delivery-attempt id. Reconnect/backfill is ordered from a durable checkpoint; the source retains and retries the same logical entry through Agents outage, typed refusal, target-version rollover, and lost acknowledgement until authenticated `DeletionSignalAccepted(SignalId, SourceRevision, ProtectedDeletionRevision)` advances the checkpoint. Retransmission never mints a new signal id/source revision; changed fields at the same id are a quarantined compatibility conflict that is not skipped or acknowledged, blocks seam readiness, and exposes only safe poison metadata. Source-revision and Agents acknowledgement lookup resolve both sides of lost delivery. The authenticated source identity and exact target are verified before Agents dispatch; the signal triggers PRD FR-30 deletion of derived Agent content and never grants a human Agents role, local cancellation authority, or scope beyond that source Conversation. Final member and typed failure names are owned by this record (PRD §8.1 assumptions A-1 through A-4, A-15, A-16, A-21). |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: typed idempotent membership with participant-state read, AI-participant removal, and Hexalith.Parties identity-by-id verification plus AI-type verification only under committed Branch A; idempotent posting with trace/provenance metadata and a typed `MessageId` existence read covering message, absence, deletion, principal removal, and unavailable; Facilitator role exposure; active-Conversation count or equivalent Conversations-side tenant event feed; tenant-scoped current content/roster/existence/accessibility reads that distinguish deletion/removal from transient denial/error; and atomic durable approved-deletion outbox/feed delivery with stable logical signal/source revision, ordered checkpoint/backfill, retry until authenticated Agents acknowledgement, target-version rollover, conflict quarantine/no-skip, acknowledgement/source lookup, local-cancellation denial, and focused cross-tenant denial. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | 5.4, 6.1, 6.2, 6.3, 6.6, 6.8, 7.1–7.5, 7.7, 8.3, 8.5, 8.8; `RQ-1` |

*Scope extended 2026-09-09 by the PRD validation reconciliation and follow-through proposal: the posting existence read, Facilitator resolution, active-count/event-feed denominator, and tenant-scoped content/roster/existence/accessibility consumers are explicit. Story 8.3 was added as the delivery owner for seam 6 Conversation-deletion propagation on 2026-09-12. The record was already `Uncommitted`; the extension changes no status and blocks every newly listed consumer from `ready-for-dev` until accepted.*

*Deletion-delivery clarification 2026-09-12: seam 6 now requires source-atomic durable publication, stable logical identity, ordered backfill, retry through typed refusal/outage, authenticated Agents acknowledgement, and poison-entry blocking. These are reliability mechanics for the already-required A-16 trigger, not a new Product deletion or cancellation choice. The record remains `Uncommitted` until its owner accepts all fields.*

*Core-scope correction 2026-09-12: the optional PRD-requested message-retraction signal moved to `EXT-CONV-RETRACTION-1`, so its unresolved status cannot block commitment of these six core seams. This record remains `Uncommitted` on its own incomplete fields.*

*Party-type compatibility correction 2026-09-12: every membership check always verifies the immutable provisioned Party by id. AI-type verification applies only when `EXT-PARTIES-1` selects Branch A and is `Available`; under Branch B the Product/Parties-approved Organization-typed identity remains valid by id and must not be rejected or replaced on type alone. An `Uncommitted` record supports no launch execution under either branch.*

### EXT-CONV-RETRACTION-1 — Optional AI Message Retraction Signal

| Commitment field | Value |
| --- | --- |
| `Owner` | Conversations Maintainer |
| `Repository` | `Hexalith.Conversations` |
| `RequiredArtifact` | A typed message-retraction signal reporting when a Conversation Message posted as the AI participant is retracted, deleted, or human-flagged, with the acting Party and authoritative instant required by the optional OQ-23 automatic-post metric [ASSUMPTION A-28]. |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: tenant-scoped, idempotent retraction/deletion/human-flag notification for AI-authored messages with cross-tenant denial and safe typed unavailability. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | None until OQ-23 selects the automatic-retraction branch; `RQ-1` only if that branch enters the qualification profile |

*This record is deliberately independent of `EXT-CONV-AI-1`; its optional unresolved seam cannot block the six core V1 Conversations seams.*

### EXT-CONV-UI-1 — Conversation Action Contribution For Call hexa

| Commitment field | Value |
| --- | --- |
| `Owner` | `TBD` |
| `Repository` | `Hexalith.Conversations` |
| `RequiredArtifact` | Four artifact kinds plus a focus-return contract. (1) A versioned Conversation action contribution and registration contract allowing Agents to contribute the Conversation-owned **Call hexa** action into a Conversation surface, with tenant-scoped authorization and typed failure when the action cannot be registered; Conversations owns the trigger and Agents owns the self-contained `ConversationAgentCallPanel` dialog body. (2) A per-message decoration slot keyed by `MessageId` through which Agents renders AI-generated and human-edited provenance wherever a message's provenance is disclosed; an Agents-side provenance accessor is the slot's sole data source, while posting metadata remains owned by `EXT-CONV-AI-1`. (3) `GetCallabilityAsync(tenant, conversation)` so the trigger can reflect Agents callability before the dialog opens. (4) An Agents-owned persistent status region contributed beside the trigger, with a lifetime matching the Conversation view, for validation, submitted, authoritative-pending, and terminal caller outcomes and for pending-proposal state visible to authorized Approvers after the dialog closes. The panel owns no live-region node; all its announcements route through this persistent region. The seam also provides a focus-return contract to the Conversations-owned trigger or, when it no longer exists, a deterministic successor. Conversations never references Agents packages (architecture AD-31). |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: authorized, tenant-scoped action contribution with typed registration failure, a `MessageId`-keyed decoration slot with one Agents provenance source, a callability gateway, a persistent status region that is the sole live-region owner and survives dialog close with validation/submission/caller/Approver disclosure tests, deterministic focus return, and no cross-tenant exposure. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | 6.7; `RQ-1` |

*Added 2026-09-08 by the PRD update applying the approved 2026-08-03 sprint change proposal (PRD §8, AM-5); provenance-marker rendering added 2026-09-09 (PRD finding H4); decoration slot, callability gateway, and consuming Story 6.7 bound 2026-09-09 by the architecture update (AD-31). Commitment fields remain `TBD`, so under PRD FR-21 every consuming story stays blocked from `ready-for-dev` until an owner accepts them.*

*Required artifact amended 2026-09-12 from the final UX reconciliation: the persistent status region and focus-return contract are explicit, and the provenance accessor is the sole decoration source. The material change keeps the record `Uncommitted` pending Conversations owner acceptance.*

### EXT-PARTIES-1 — Agent Party Identity Contract

| Commitment field | Value |
| --- | --- |
| `Owner` | Parties Maintainer |
| `Repository` | `Hexalith.Parties` |
| `RequiredArtifact` | One owner-accepted V1 branch. **Branch A:** a public AI Party type and an authorization contract permitting only the Agents Service Principal to create `hexa`'s deterministic provisioned Party under that type. **Branch B:** Parties Maintainer and Product formally accept the existing Organization-typed provisioned Party as the V1 launch contract, verified everywhere by its immutable deterministic id and never by type. Both branches require create-only AD-2 provisioning, idempotent exact retries, typed conflict for divergent identity/type requests, mandatory tenant scope, no tenant-role creation or replacement, and no Party PII in public results. The record must name the selected branch; absence of that decision is `Uncommitted`. Independently of the selected identity branch, Parties exposes a tenant-scoped authoritative resolution by `PartyId` returning stable Party classification (`Human`, `Organization`, or `AI`), active/liveness state, evidence position and observed instant, plus typed missing, ambiguous, stale, denied, and unavailable outcomes. For every human Party, the same seam also returns one stable non-PII `AuthenticatedHumanActorId`, `HumanActorBindingVersion`, validity interval, and queryable binding history sufficient to reproduce the identity in force at an action; it returns typed missing, ambiguous, stale, and overlapping-history outcomes. AD-8 uses both classification and binding at predefined-approver configuration and every runtime approver resolution; non-human Parties can never be Approvers, and a human command carrying a Party must bind to the same authenticated actor id. |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: for the selected branch, create and resolve exactly one deterministic tenant-scoped provisioned Party for `hexa` under the Agents Service Principal; verify immutable identity by id on every path and additionally verify AI type only for Branch A; reject tenant-role creation, cross-tenant access, divergent replay, and replacement; return typed safe outcomes without Party PII; resolve approver candidates to fresh authoritative human-type and active/liveness evidence; resolve each human Party to its stable `AuthenticatedHumanActorId` and binding version; reproduce the binding in force for a historical instant; and reject missing, stale, ambiguous, overlapping, or actor-mismatched bindings with typed fail-closed outcomes. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | `RQ-1`; Stories 5.2, 5.4, 5.5, 6.1, 6.6, 7.1–7.5, 8.1–8.4, and 8.8 directly or conditionally consume identity or human Party/liveness/binding evidence. Story 6.1 directly verifies the caller and provisioned Agent Party and executes FR-8 membership step 10; Story 5.5's consumption remains conditional on the Product-selected recorder scope; Story 8.4 consumes the Release-Operator review branch. Branch-B-compatible identity-by-id portions may be developed where applicable, but no consumer may cite that work or any human actor-binding seam as launch evidence while this record is `Uncommitted` |

*Added 2026-09-12 to land the dependency requested by PRD A-27. Its branch, immutable target, date, and verification command remain incomplete, so `RQ-1` stays blocked; adding the record does not retire A-27 or turn transitional development behavior into launch evidence.*

### EXT-HOST-1 — Platform-Owned Agents Host Composition

| Commitment field | Value |
| --- | --- |
| `Owner` | Platform Maintainer |
| `Repository` | `Hexalith.Platform` |
| `RequiredArtifact` | Platform-owned host composition for the Agents DomainService and UI with EventStore, Conversations, Parties, Tenants, Provider and safety adapters, Dapr Workflow, secrets, health, identity, telemetry, and a replicated durable content-free `security-audit-spool`. The spool accepts a deterministic `SecurityObservationId` before a classified Agents denial returns, retains pending records across EventStore/replay outages, exposes exact-id outcome lookup/acknowledgement, and grants its drain worker an ACL restricted to the computed `SecurityEventLog(RoutingTenantId, UtcDay)` stream and event; if the spool itself is unavailable, ingress constructs no Agents command and platform health blocks tenant-access/audit readiness. The host additionally binds the production `EXT-PROTECTION-1` engine and exposes the FR-34 attestation port used at startup and every readiness evaluation. The port lets Agents seal a canary, prove persisted bytes contain no plaintext, unseal it, destroy its DEK, replay `Erased`, and obtain engine identity/version for comparison with the committed protection target; the host fails closed against both the no-op default and a self-reporting wrapper around it. For Story 6.1 it also composes the target-limited directory-migration Workflow and EventStore namespace ports. Installing a tenant migration epoch revokes the legacy direct-write credential/epoch, and every later interaction/failure create or content append requires the current epoch plus an exact directory permit or committed-effect capability. Every `ConversationAgentState` permit, creation/workflow-start/User-action-intent outbox, lease acquire/commit, and interaction-owned rate/open/Budget/capacity phase-authorization append is likewise conditional on the current directory capability at EventStore. Repair first atomically installs a directory-repair fence that revokes all those new-directory/effect-authorization operations and returns an authenticated checkpoint; only the post-fence finite permit/outbox/lease/phase-authorization cohort may enter the old-epoch bridge, whose exact pending/committed/authorized work drains before successor activation revokes the old epoch, repair fence, and bridge. For Story 8.3 the same port evaluates canonical `GovernanceScopeV1` and its predicate digest from authoritative tenant stream identity, immutable first-event/directory-permit `SourceConversationId`, and permit/action/lease/phase-authorization owner facts—never caller classification. Before either deletion origin builds a candidate, it conditionally installs a persistent ordinal scope admission fence that rejects later matching permit, User-action-intent, lease-acquire/commit, and rate/open/Budget/capacity phase-authorization appends; after the finite same-ordinal owner cycles become Effective it conditionally installs and verifies the separate content write fence that rejects later matching interaction/failure creates or content appends. The admission violation ledger is one append-only request ledger partitioned by fence ordinal: prior violations remain visible, while only the current installed ordinal can attest `ZeroAcceptedViolationsSinceInstall`. A pre-cut violation uses an explicit no-global-cut/candidate/token receipt; a later violation invalidates only exact present artifacts. Both assign one successor ordinal and require separately keyed owner cycles carrying all prior obligations. The content guard is stable by request/predicate and continuously installed: after one or more recuts, a successor-binding authorize/effect/result operation starts from the latest installed binding and appends the new ordinal/global-cut/token only after proving a complete gap-free ordered chain of intervening invalidated/tokenless ordinal receipts and the current zero proof; an overtaken authorization returns terminal obsolete without a guard append. The port also owns the tenant `GovernanceScopeGuard`: every authorized legal-hold request conditionally registers its canonical scope there before fence preparation and remains until an exact fully acknowledged release removes it. Hold registration and `CommitDeletionDestructionStartBarrierEffect` serialize on that owner. The `ProtectionFence` barrier authorization persists the exact observed `GovernanceScopeGuard` revision/compare token. Conditional append at that revision makes the seal operation serialize with admission writes and hold registration/release; an intervening mutation returns authenticated stale and must be mirrored before revision-bound reauthorization. The seal operation verifies both ledgers/current content binding/migration receipts, rejects any overlapping contender that won after authorization unless it consumes the exact effective deletion-allowed hold disposition, commits immutable `DestructionSealed`, and returns one deterministic non-expiring, revocable, single-use `DestroyDekManifest` capability bound to the sorted accepted key-target manifest; it has no `ExpiresAt` or renewal branch, and its capability-key version remains verifiable through outage, migration, restore, and the full retention of the batch identity and terminal outcome, including exact retry/lookup after consumption or revocation. No direct clean read, stale no-hold authorization, or unrelated append is authority. Each fence/guard/barrier exposes authenticated installation, exact-outcome lookup, verification/high-water, and complete append-only evidence; every admission ordinal/violation gap chain, owner cycle, content binding, hold contender/release, barrier/containment compare authorization and stale result, destruction seal, and batch identity/capability-key/guard-issued state plus referenced protection-owner blocked, reserved, re-attested, consumed, or revoked outcome survives migration successors. The protection engine consumes each manifest batch atomically as all targets or none, returns an ordered per-target irreversible receipt vector, and exposes exact batch lookup; each first previously uncovered complete protection target from an authoritative post-seal content violation is ProtectionFence-serialized into one monotonic ordinal and immutable singleton target manifest, while later same-target resources receive coverage links only, then follows `AuthorizeDeletionContainmentBatch`, guard-revision-conditional `CommitDeletionContainmentBatchEffect`, exact lookup, and `RecordDeletionContainmentBatchIssued` or `RecordDeletionContainmentBatchIssuanceStale` before its distinct batch may be consumed; exact duplicates reuse the ordinal/result and concurrent distinct targets receive distinct ordinals. An accepted admission after sealing invokes `BlockDeletionBatchConsumption` to race `ReserveAndConsumeDeletionBatch` at the protection owner, records `DeletionIntegrityCompromised`, blocks new batches, and can never be treated as resource-only content containment or successful completion. Missing, malformed, unknown-version, cross-tenant, or unverifiable scope membership rejects a write and prevents a clean verification result. The artifact replaces module-owned AppHost, Aspire, and ServiceDefaults ownership. |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: clean-checkout composition through the platform host without module-owned hosting infrastructure, including the durable security-audit spool, production protection-engine binding, and complete FR-34 attestation. The target-limited migration contract enforces the current epoch on interaction, directory, and effect-authorization writes; provides atomic directory-repair-fence install/checkpoint/lookup; freezes only the post-boundary cohort including protected User-action intents and rate/open/Budget/capacity authorizations; restricts the bridge to manifest-listed pending, committed, or already-authorized work; preserves every deletion fence ordinal/violation gap chain, owner-cycle receipt, continuous content binding, guard compare authorization/stale result, destruction seal, and batch capability/state; and atomically revokes old epoch/repair fence/bridge at successor activation. The deletion contract implements canonical exact-interaction and source-deletion exact-Conversation predicates; ordinal admission-fence authorize/effect/result; explicit pre-cut and post-cut violation containment with one successor ordinal; immutable separately keyed owner cycles and current zero proof; one continuously installed predicate content guard whose successor binding uses the latest installed binding plus a gap-free invalidation chain and returns terminal obsolete when overtaken; tenant-scope-guard revision/compare-token hold registration/release serialized with the destruction seal and authenticated stale reauthorization; an exact effective armed-contention disposition only when required; guard-owned atomic start-barrier authorize/effect/result; deterministic non-expiring, revocable, all-or-none manifest-batch destruction with per-target vectors, exact lookup and revocation race; guard-owned singleton containment-batch authorize/effect/result with ordinal allocation and lost-issuance lookup; migration preservation; post-start integrity blocking; and content containment with distinct batches. Tests race every directory/phase-authorization append and target write around migration-repair, admission-fence, and destruction-barrier boundaries; cover violations before the first cut and after content-fence installation; prove one successor ordinal, carried obligations, immutable Effective, repeated recut gap chains/obsolete binding outcomes, continuous content binding, and current zero; race hold registration/release before/at/after guard-revision-bound barrier authorization/seal while Open precedence stalls both and stale compare results require reauthorization; prove the guard commit—not a prior read—is first irreversible authority; and prove an exact multi-key manifest batch is consumed once, all targets or none, with non-expiring restore, lost-result lookup, revocation, explicit singleton containment issuance/lookup, and prior violations preserved. Human-origin exact-Conversation tests remain blocked pending `OD-HUMAN-EXACT-CONVERSATION-SCOPE-1`; `ClassUtcRange` tests remain blocked pending `OD-GOVERNANCE-CLASS-RANGE-SCOPE-1`. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | 5.1, 5.4, 5.6, 6.1, 8.1, 8.3; `RQ-1` |

**Required v21 contract extension:** `EXT-HOST-1` must make the EventStore append guard assign and persist `AcceptedAtAdmissionFenceOrdinal`/`AcceptedAtGuardHighWater` at the accepted write's own linearization, serialize that assignment with successor installation, and treat missing acceptance evidence as integrity-unattributable. `GovernanceScopeGuard` must durably record post-start hold attempts, active/applied Product disposition versions, target-alias coverage, capability-key compromise/attestation state, batch-dispatch state, and the final completion barrier. The host composes the target-limited capability authorize/sign/result and same-batch re-attestation steps with `EXT-SECRETS-1`; each protection effect additionally requires `AuthorizeDeletionBatchDispatch` → guard-conditional `CommitDeletionBatchDispatchEffect` → issued-or-stale mirror, so a hold or compromise is ordered before a still-preventable effect or after its uninterruptible dispatch authorization. `EXT-PROTECTION-1` must validate that exact dispatch/attestation state online. The host also supplies `AuthorizeDeletionCompletionBarrier` → conditional `CommitDeletionCompletionBarrierEffect` → stale-or-sealed mirror with exact lookup. Its migration/restore bridge preserves all these facts. Compatibility evidence must race ordinal attribution, post-start hold versus every still-preventable destructive step, same-alias coverage, key compromise/re-attestation versus dispatch/consume, and completion versus every guard mutation; an Open Product outcome or unavailable owner stays blocked.

**Required v23 host contract extension:** The host composes `OperationGateMatrixVersion 7` and the protection-owner protocol without treating a guard lookup plus later write as atomic. After dispatch, it routes every post-seal admission-integrity cancellation to `BlockDeletionBatchConsumption` and every destructive call to `ReserveAndConsumeDeletionBatch`; both conditionally change the same `EXT-PROTECTION-1` state, whose `ConsumptionReserved` transition is the irreversible instant for those reasons. It exposes exact lookup/mirroring for block-pending, blocked, reserved, consumed, and revoked outcomes. For key compromise, it supplies `IDeletionCapabilityCompromiseRegistrar` only to the authenticated `EXT-SECRETS-1` revocation subscriber; the registrar installs the per-tenant/key-version protection block before recording the exact guard fact, carries no Agents principal, and has no public/Workflow/general-dispatcher path. The host also composes `SignedAttestationObsoleteUnissued` and deterministic successor signing attempts that retain one stable destruction-seal/batch identity while intended revisions change and actual successful issue revision is recorded only in the result. Guard re-attestation/successor dispatch is followed by protection-owner `ActivateReattestedDeletionBatch`, which atomically compares the replacement key's block-set revision and returns one exact activated-or-new-key-blocked result. Migration/restore preserves every attempt, block, receipt, owner state, and referenced guard revision. Compatibility races every before/at/after boundary and fails closed on unknown results; a later hold remains intentionally ordered at dispatch and no Product outcome is selected.

*Historical prior commitment only: target `a66cdf346e521ad147f442b686f301f0f59c525c`, integration date `2026-09-30`, and `./eng/verify-agents-host.sh` covered the earlier clean-checkout host artifact. The expanded protection binding/attestation and migration Workflow/EventStore write-fence composition have not been re-accepted by the Platform Maintainer, so those values are not current commitment fields.*

### EXT-PROVIDER-1 — Provider Generation Adapter

| Commitment field | Value |
| --- | --- |
| `Owner` | Agents Runtime Maintainer |
| `Repository` | `TBD` |
| `RequiredArtifact` | Selected Provider adapter and any selected Provider or Agent Framework SDK, with safe availability, error, timeout, usage, pricing, capability-version, deterministic idempotent invocation, and outcome lookup by `AttemptId`. Provider SDK types and raw errors do not cross the Agents public boundary. |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: the adapter implements the public Provider readiness and prepared-attempt contracts without owning orchestration or domain state, and crash recovery can resolve/reuse an `AttemptId` without duplicate Provider work. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | 5.5, 6.4, 7.3; `RQ-1` (narrowed 2026-09-09 by the architecture update: Story 5.3 published catalog truth adapter-free and is not a consumer) |

### EXT-SAFETY-1 — Versioned Content Safety Adapter

| Commitment field | Value |
| --- | --- |
| `Owner` | Security Engineering |
| `Repository` | `TBD` |
| `RequiredArtifact` | Versioned prompt, complete-context, and generated-output safety adapter with safe reason codes, always-blocked/restricted-category enforcement, no Approver override, independent evaluation by exact requested platform/tenant versions, and conjunctive snapshot-plus-current behavior for retry, regeneration, approval, and pre-post. |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: fresh deterministic safety decisions before Provider and before proposal/posting side effects, including exact-version repeatability, two-version conjunctive outcomes, optional formally-equivalent dominance proof, and fail-closed missing, stale, unversioned, and indeterminate outcomes. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | 6.3, 6.8, 7.3, 7.4, 7.7; `RQ-1` |

### EXT-TOKEN-1 — Provider/Model Tokenizer

| Commitment field | Value |
| --- | --- |
| `Owner` | Agents Runtime Maintainer |
| `Repository` | `TBD` |
| `RequiredArtifact` | Provider/model-specific tokenizer that measures the exact prepared prompt and complete authorized Conversation Context against the selected model budget. |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: deterministic token counts for the exact prepared request; missing or unsupported Provider/model tokenization fails closed before Provider invocation. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | 6.2, 6.8, 7.3; `RQ-1` |

### EXT-SECRETS-1 — Platform Secret Resolution

| Commitment field | Value |
| --- | --- |
| `Owner` | Platform Maintainer |
| `Repository` | `TBD` |
| `RequiredArtifact` | Versioned secret custody for Provider credentials, per-tenant KEKs and DigestKeys, tenant migration-epoch/directory-effect capability signing keys with exact revocation state, the reserved-system security-observation digest key, per-export envelope keys, manifest-signing keys/public trust anchors, trusted-envelope HMAC keys, and decision-authority verification anchors/trust profile. Decision-approval signing keys remain outside Agents and platform-host custody under the independently governed issuer named by that profile; the profile binds issuer, stable actor/authority-role sources for Product, Governance, Security, Architecture, and named maintainers, key-use purpose, freshness/expiry/revocation, and forbids the Release Operator recorder or platform key custodian from requesting or minting approval evidence. The trusted-envelope profile fixes issuer/audience, numeric maximum tag lifetime, clock skew, rotation overlap, command retry/recovery horizon, and `ReplayRecordRetention` no shorter than the greater of tag lifetime plus skew plus overlap and that recovery horizon, consumed by the Agents-owned reserved-`system` `TrustedEnvelopeReplay(Issuer, DeliveryNonce)` aggregate whose record binds the authenticated target tenant; it also fixes active/revoked key state, emergency revocation, retained digest-key versions, decision-manifest verification, wrap/unwrap/pin/unpin/destroy operations with irreversible receipts, safe rotation/invalidation signals, and a principal-bound `DeliverExportKey(DeliveryId, ExportId, CommitAcknowledgementRevision, RequesterActorId, Purpose, Audience, ExclusiveExpiry, LifecycleVersion, StoreTarget, ContractVersion)` operation whose key bytes are delivered directly to that authenticated principal. Delivery is idempotent by the complete identity and exposes authenticated exact outcome lookup as `Delivered(ObservedAt)`, `NotDelivered(Reason)`, or `Unknown/Unavailable`; the custodian rejects cross-tenant, changed-identity, expired, or revoked authority before release. Only opaque references, versions, public anchors, policy bounds, and configured/not-configured state cross Agents boundaries. |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: platform-hosted versioned resolution and custody; AES-256-GCM export-key wrapping; RFC 7515/7518 ES256 manifest signing and offline public-anchor verification; HMAC-SHA-256 trusted-envelope issuance/constant-time verification over AD-30 canonical bytes; target-tenant migration epoch/directory-effect capability issuance and legacy-epoch revocation accepted by the EventStore append guard; system-keyed HMAC of untrusted security-observation fields; independently issued signed decision-governance/authority manifests binding exact contract/version, effective predecessor or root policy, nonempty required approvers, affected evaluations, stable actor/role evidence, freshness, and revocation, with tests proving neither recorder nor custodian can sign/request approval; immutable `LogicalCommandId`, issuer-wide unique per-delivery `DeliveryNonce`, byte-identical replay routed only to domain idempotency, changed-field or cross-tenant nonce reuse rejected in the reserved-system replay namespace, bounded expiry/skew/overlap with retained replay records across the complete bound, emergency-revocation redispatch under a new key and nonce, rotation-triggered safety-verdict invalidation, denied access, destruction receipts, and principal-bound export-key delivery with deterministic identity, idempotent direct release, exact outcome lookup, exclusive-expiry/current-authority rejection before release, immutable Delivered outcome after release, and lost-ack recovery; tests cross expiry and actor revocation immediately before/after release, lookup outage, duplicate retry, cross-tenant substitution, changed purpose/audience/version, and stale/disconnected/restored migration writers while proving secret values do not enter events, projections, responses, logs, traces, or evidence. The live test also proves the non-public replay registrar credential can conditionally append only the exact replay stream/event and is unavailable to public, Workflow, and target-handler paths. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | 5.4, 5.5, 5.6, 5.8, 6.1, 6.3, 6.4, 7.3, 8.1, 8.2, 8.3, 8.4; `RQ-1` |

**Required current destruction-capability signing profile:** `EXT-SECRETS-1` alone custodies the private `DeletionBatchCapabilitySigningKey` family. It signs `DeletionBatchCapabilityV1` as a detached ES256 JWS over RFC 8785 canonical JSON whose closed fields are `Issuer`, committed `EXT-PROTECTION-1` `Audience`, `TenantId`, `DeletionRequestId`, `DestructionSealId`, `BatchKind`, `BatchOrdinal`, `BatchId`, `ManifestDigest`, `GuardStreamId`, `IntendedIssuedGuardRevision`, `AttestationOrdinal`, `SigningAttemptOrdinal`, and `CapabilityKeyVersion`; the actual successful `CommittedIssuedGuardRevision` (`CommittedDestructionSealGuardRevision` for the accepted batch) is recorded separately as result evidence and is excluded from seal id, batch id, signing request id, and signed bytes; the seam publishes the recorded public-anchor id/version and never exposes private material. A target-limited recorded authorization precedes signing, deterministic request lookup resolves loss, and only an exact guard-recorded active attestation is consumable. Routine rotation chooses the current non-revoked version for new issuance and retains every old public verifier through the full referenced batch/outcome lifetime. Emergency compromise publishes an authenticated revocation that immediately blocks unconsumed old-key effects, lets the guard enumerate affected batches, and permits only expected-revision re-attestation of an exactly unconsumed immutable batch under a healthy key: same batch/manifest, next attestation ordinal, old attestation revoked, one active credential. It is not logical capability renewal. Tests cover canonical-byte/header substitution, wrong issuer/audience/tenant, stale/revoked key, lost signing result, routine overlap, compromise before/at/after consume, incomplete enumeration, restore, and no secret leakage.

**Required v23 signing and compromise extension:** The key family is per tenant. Signed bytes contain the once-assigned stable `DestructionSealId`, `IntendedIssuedGuardRevision`, and `SigningAttemptOrdinal`; `DeletionBatchCapabilitySigningRequestId` binds all three with batch, manifest, attestation, key version, and audience. If conditional issue loses, only exact guard no-issue proof permits terminal `SignedAttestationObsoleteUnissued`; a successor pre-issue attempt retains stable seal id/batch/manifest/attestation ordinal, increments signing attempt, and binds the returned current guard revision/current healthy key. Two or more consecutive stale attempts change only signing-attempt ordinal, intended revision, and—if rotation requires—the current healthy key; the actual successful issue revision remains separate. Unknown signer/issue outcome cannot retry. Emergency re-attestation remains post-issue only and requires an authenticated protection-owner `ConsumptionBlocked(CapabilityKeyCompromise)` receipt. The authenticated emergency-revocation envelope binds issuer, audience, tenant, key family/version, revocation revision, trust-profile revision, event identity, and signature digest for the non-public registrar; duplicates/lost acknowledgement use exact lookup and cross-tenant or changed evidence rejects. The registrar cannot request a signature, mint an attestation, or act as a human Platform/Workflow principal.

### EXT-EXPORT-STORE-1 — Protected Export Artifact Store

| Commitment field | Value |
| --- | --- |
| `Owner` | Platform Maintainer + Governance |
| `Repository` | `TBD` |
| `RequiredArtifact` | Platform-composed implementation of the Agents-owned immutable export-artifact port keyed by (`TenantId`, `ExportId`, `ManifestVersion`), with authenticated encrypted bytes, content-free interaction-to-export lookup, authenticated download, expiry/purge, physical purge receipts, backup/restore treatment, and legal-hold/deletion coordination through the AD-22 `ProtectionFence`. Export preparation and a frozen partial-output inventory precede every content write; an overlapping earlier deletion rejects export; and a winning export token blocks deletion arm/completion until cleanup-complete abort or the fence records the sole `ExportCommitDecided` event and advances its committed-export index high-water in that same append. The secondary `AuditExport` commit acknowledgement is idempotent, gates export-key delivery, and never controls whether later deletion sees the committed copy. For every lifecycle-covered artifact, manifest, index entry, wrapped key, provider TTL, backup, and restored copy, the port exposes idempotent `Pin`, `Unpin`, and exact outcome lookup bound to the complete object identity, `HoldId`, fence token/revision, and approved lifecycle `DecisionVersion`. Every physical expiry, purge, key destruction, autonomous provider-TTL action, backup expiry, and restore cleanup obtains a current linearizable fence authorization or an equivalently linearizable store reservation; missing, stale, unavailable, or lost control evidence retains bytes and keys and cannot report expiry, purge, unpin, or absence. `ExportCleanupPending` remains fenced until every partial member is proven never written or physically purged/key-destroyed with a durable receipt. Provider technology is deliberately unselected. This record cannot reach `Available` unless its accepted evidence names the approved `OD-EXPORT-LIFECYCLE-1` `DecisionVersion` that the deployed store enforces. |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: immutable-write conflict, tenant isolation, AES-256-GCM artifact confidentiality/integrity, index-complete interaction lookup with fence-token prepare/cleanup-pending/cleanup-complete-abort and lost-ack recovery; one `ProtectionFence` `ExportCommitDecided` append atomically advances its committed-export high-water after exact artifact/manifest/index acknowledgements, followed by an idempotent `AuditExport` acknowledgement that alone makes durable key-delivery authorization eligible; deletion sees the fence commit even if that acknowledgement is absent. Also required: exact approved `OD-EXPORT-LIFECYCLE-1` `DecisionVersion` enforcement, deletion/export race serialization, later-hold and restore behavior, offline-verifiable signed manifest, idempotent artifact/key/index `Pin`/`Unpin` plus exact outcome lookup, idempotent physical purge/index-removal/key-destruction receipts, and deletion completion across every committed or partial lifecycle-covered copy. Tests cover crashes before the fence decision, after it but before secondary acknowledgement, lost acknowledgement, and concurrent fence revisions; they also linearize hold intent immediately before, at, and after expiry and inject lost pin/unpin acknowledgement, fence/store-control outage, autonomous lifecycle worker, provider TTL, backup expiry, and restore cleanup. No physical action succeeds without the exact current fence/store reservation, and unknown/unavailable evidence retains bytes and keys. The command must fail if the policy record is open, missing, malformed, or differs from the deployed store binding. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | 8.2 and `RQ-1` unconditionally; 8.1 and 8.3 only when their exact frozen sets contain a committed export artifact or lifecycle-covered copy |

*No export runtime path may execute, and `RQ-1` remains blocked, until this record and the Product/Governance/Security export-lifecycle policy in the spine are both resolved. An export-free hold/deletion branch neither executes this seam nor fabricates its decision and remains eligible under its other gates.*

*Prepare/commit version boundary: the accepted store target and lifecycle contract version recorded by `ExportPreparing` phase-pin that export before its first byte. Every artifact, manifest, index acknowledgement, fence commit, recovery lookup, and key delivery must match them. A later decision or target successor applies only to a new prepare; it cannot relabel existing bytes, and switching requires cleanup-complete Abort followed by a new preparation.*

### EXT-TOPOLOGY-1 — Production-Like Qualification Fixture

| Commitment field | Value |
| --- | --- |
| `Owner` | Platform Maintainer |
| `Repository` | `TBD` |
| `RequiredArtifact` | Versioned production-like fixture with component versions, reset/seed procedure, evidence capture, failure injection, browser execution, authenticated qualification sessions, and safe trace/projection correlation through the platform evidence ingress. |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: reproducible Level 5 topology and browser/runtime evidence collection without hidden manual setup or conditional skips. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | 5.6, 6.1, 6.5, 8.5, 8.6, 8.7, 8.8; `RQ-1` |

### EXT-PROTECTION-1 — EventStore Payload-Protection Engine

| Commitment field | Value |
| --- | --- |
| `Owner` | EventStore Maintainer |
| `Repository` | `Hexalith.EventStore` |
| `RequiredArtifact` | A production payload-protection engine behind the existing EventStore hooks, applied field-level to a `ProtectedContent` envelope so every other event field stays plaintext and a destroyed-key field unprotects as the typed value `Erased` without breaking replay: one data-encryption key per `AgentInteraction`, plus an explicit provider-neutral `TargetProtectionKeyAlias` that lets a target-limited creation-outbox `ProtectedContent` field owned by `ConversationAgentState` select that exact interaction DEK without deriving it from the directory aggregate identity, wrapped by a per-tenant key-encryption key custodied through `EXT-SECRETS-1`. The custodian exposes `WrapDek`, `UnwrapDek`, `PinDek`, `UnpinDek`, per-key `DestroyDek`, and an atomic `DestroyDekManifest(BatchId, ManifestDigest, SortedTenantInteractionAliasTargets, GuardCapability)` operation. The batch identity and signed capability are deterministic and target-bound. The capability is non-expiring and revocable, carries its signing-key version, and that version remains verifiable for the full retention of the batch identity and terminal outcome, including exact retry/lookup after consumption or revocation; there is no expiry or renewal path and restore reuses the same identity. Consumption is single-use and all-or-none, returns an ordered irreversible per-target receipt vector, exact retry/lookup returns that vector, a changed target/digest conflicts, and `BlockDeletionBatchConsumption` races `ReserveAndConsumeDeletionBatch` at the same protection owner so exactly one wins. Each first previously uncovered complete protection target `(TenantId, AgentInteractionId, TargetProtectionKeyAlias)` uses one guard-issued batch for its ProtectionFence-assigned monotonic containment ordinal and immutable singleton target manifest; later violation resources for that same target receive coverage links only, no ProtectionFence event alone is a guard capability, and distinct uncovered targets receive distinct ordinals/batches. Hold pinning rejects both per-key and manifest destruction; sealed envelopes remain sealed on broker/read models, `PayloadUnprotectionOutcome` is unreadable after erasure, and snapshot/replay caches inherit the DEK (AD-22, AD-27). The shipped no-op default is not acceptable for content-bearing workflows (AD-14). |
| `TargetVersionOrCommit` | `TBD` |
| `TargetIntegrationDate` | `TBD` |
| `CompatibilityContractAndVerificationCommand` | Contract: an owner-supplied executable command must prove at minimum (1) seal with plaintext-absence proof, (2) unseal with payload equality, (3) irreversible erase with typed `Erased` replay, (4) hold pin rejecting erase until authorized unpin, and (5) engine identity/version reporting equal to the exact committed target, with per-tenant isolation and restore-cannot-revive behavior. It must seal the directory creation-outbox field through the target alias and exercise a multi-interaction `DestroyDekManifest`: deterministic single-use batch identity, exact sorted target binding, all targets destroyed or none, ordered per-key receipts, byte-identical retry and lost-result lookup, changed-manifest conflict, duplicate-consumption denial with exact prior result, hold-pin rejection, and a protection-owner `BlockDeletionBatchConsumption`-versus-`ReserveAndConsumeDeletionBatch` race with exactly one winner, plus capability non-expiry across delayed dispatch/restore and signing-key verification until terminal batch state. A containment batch is accepted only with the exact guard-issued capability for its ProtectionFence-assigned singleton ordinal/manifest; response loss uses exact issuance and protection lookup, exact duplicates reuse it, concurrent distinct targets remain distinct, and containment ordinals cannot reuse the accepted-set batch. Replay must yield typed `Erased` from both source outbox and target events. Negative fixtures cover cross-tenant alias substitution, changed payload/manifest, partial-provider failure, snapshot/cache replay, backup, restore, and stale or revoked guard capability. Command: `TBD`. |
| `RequiredEvidenceLevel` | Levels 4 and 5 |
| `AcceptedStatus` | `Uncommitted` |
| `ConsumingStories` | 5.6, 5.8, 6.1–6.4, 6.8, 7.1–7.4, 7.7, 8.1–8.3, 8.8; `RQ-1` |

**Required v21 manifest-destruction extension:** The engine verifies the complete `DeletionBatchCapabilityV1` JWS against the committed anchor/audience and performs a linearizable exact `GovernanceScopeGuard` lookup at consumption; signature-only or batch-issue-only acceptance is forbidden. The lookup must confirm the same batch/manifest, one active unrevoked attestation, and an exact `DeletionBatchDispatchAuthorized` receipt whose conditional guard commit ordered every earlier post-start hold or compromise before dispatch. Open/missing/mismatched/hold-allowed evidence that preceded dispatch, a stale dispatch, unavailable lookup, or compromised/replaced attestation performs no destruction. A later hold ordered after successful dispatch cannot cancel the possibly begun batch but remains restrictive for other batches, purge, and completion. Emergency key revocation blocks the old attestation before consume; same-batch re-attestation is accepted only after the guard atomically makes it the sole active credential and records a successor dispatch authorization. The protection owner indexes terminal destruction by complete `(TenantId, AgentInteractionId, TargetProtectionKeyAlias)`: a later different BatchId for the exact already-destroyed target returns authenticated `AlreadyDestroyedByBatch(OriginalBatchId, OriginalPerTargetReceipt)`, while changed/cross-tenant targets conflict. Same-alias distinct-resource tests before/after original consumption, hold/dispatch/consume ordering, online-lookup races, compromise/re-attestation, lost acknowledgement, and restore are mandatory.

**Required v23 protection-owner extension:** The engine owns the only post-dispatch cancel-versus-consume state machine. It atomically orders `BlockDeletionBatchConsumption` (admission-integrity or registrar-authenticated per-tenant/key-version compromise) against `ReserveAndConsumeDeletionBatch`; only `Unconsumed` may become `ConsumptionReserved`, and that reservation is the irreversible instant for those cancellation reasons. The owner returns authenticated blocked/reserved/consumed/revoked outcomes and exact lookup after loss; a pending/unknown block performs no new effect. `ConsumptionReserved` recovery completes the same all-or-none batch or returns its terminal vector, never a new authorization. Re-attestation begins only from the exact capability-compromise block, remains blocked while the guard replaces the attestation and issues a successor dispatch, and reopens only when `ActivateReattestedDeletionBatch` conditionally consumes those receipts at the same owner while atomically requiring absence of a replacement-key block at the activation identity's expected tenant-key block-set revision. If that key block exists or wins the compare, activation returns `ActivationBlockedByReplacementKeyCompromise(NewKeyVersion, NewRevocationRevision, NewBlockReceiptId)` and leaves/places the batch in that exact new-key `ConsumptionBlocked` state for another same-batch re-attestation; it never clears a global tenant-key block. Admission-integrity blocks never reopen. The target index is complete `(TenantId, AgentInteractionId, TargetProtectionKeyAlias)`: later violation resources use coverage links and never receive another ordinal/batch; `AlreadyDestroyedByBatch` is exact-target defense in depth, not successor authority. Fixtures cover registration arriving before/at/after reserve; a second compromise before replacement, before dispatch, before/at activation, and after activation before reserve; block delivery/mirror loss; exact activation lookup; no false replacement; restore; and tenant/key/attestation/dispatch/revision substitution.

*Added 2026-09-09 by the architecture update: the 2026-09-09 verified-current review found that the EventStore checkout carries only the protection hooks and a no-op default, so the AD-22 key hierarchy has no owner without this record.*

## Known Consumer Non-Conformance

### Story 5.3 / EXT-PROVIDER-1 Historical Consumption — Resolved Branch B

`None: Story 5.3 executed no EXT-PROVIDER-1 seam (Product ruling 2026-09-10)`.

Product approved Branch B on 2026-09-10 from the inspected Story 5.3 specification, implementation history, verification scope, and fail-closed deferred provider. Story 5.3 published Provider catalog truth through its EventStore, API/client, and UI paths; substituted `IAgentGenerationProvider` test ports and the deferred provider returning `Unavailable` did not execute a live external Provider adapter or real Provider transport. No Story 5.3 / `EXT-PROVIDER-1` non-conformance record is therefore created under PRD FR-21.

This historical ruling does not commit `EXT-PROVIDER-1`, advance its status, or change its current consumers.

### NC-5.3-PLATFORM-CATALOG-SCOPE — Story 5.3 Platform Catalog Migration

| Field | Value |
| --- | --- |
| `Status` | Open |
| `Owner` | Agents Runtime Maintainer |
| `AffectedStory` | 5.3 — Govern Provider Models And Pricing Through Live Operations |
| `Finding` | The shipped implementation uses tenant-scoped Provider catalog streams and read models. It does not yet implement AD-2's platform `system` catalog plus tenant-scoped `TenantProviderEnablement` split. |
| `Disposition` | Retain the shipped implementation as migration source; migrate idempotently into platform catalog and tenant-enablement streams with `MigratedFrom`; freeze legacy streams/read models without rewriting or deleting history. |
| `ClosureEvidence` | Revised Story 5.3 verification passes platform/tenant aggregate, migration/replay, projection, authorization, and cross-tenant non-disclosure tests, with Story 5.6 live-seam evidence where assigned. |
| `DependencyEffect` | This record does not make `EXT-PROVIDER-1` a Story 5.3 consumer, does not alter any dependency status, and cannot be cited as current architecture or release conformance. |

The record closes only when its named evidence is accepted and this table is amended in the same change. A completed historical Story 5.3 specification or the existing tenant-scoped implementation does not close it.

## Current Blocking Summary

All twelve dependency records are `Uncommitted`; every consumer remains blocked from `ready-for-dev` until the fields required for `Committed` are accepted. `EXT-HOST-1`'s earlier target/date/command are historical only because the required artifact now includes the production `EXT-PROTECTION-1` binding and FR-34 attestation port. `RQ-1` additionally requires every dependency in its qualification profile to be `Available` with the exact compatibility command passing against the target used by the run.
