# Spine Pair Review — Hexalith Agents

Reviewer: rubric walker. Run date: 2026-09-09. Inputs: `DESIGN.md` and `EXPERIENCE.md` (both `status: final`, updated 2026-09-08), `.memlog.md`, `reconcile-validation-2026-09-08.md`, the archived prior rubric pass (`.working/validation-2026-09-08-pre-update/review-rubric.md`), `design-md-spec.md`, the five calibration examples, every `sources:` path in both frontmatters (15 + 18, all resolve), and — for name verification — `src/Hexalith.Agents.Contracts` enums, `src/Hexalith.Agents.UI/Composition/AgentsFrontComposerRegistration.cs`, `src/Hexalith.Agents.UI/Resources/AgentsResources*.resx`, the FrontComposer submodule at `053b2008`, and the pinned `Microsoft.FluentUI.AspNetCore.Components 5.0.0-rc.5-26219.1` XML docs. `imports/` is empty; `mockups/` and `wireframes/` do not exist.

## Overall verdict

The pair is a consumable contract: the 22 component names are identical across the DESIGN frontmatter, the DESIGN `###` sections, and the EXPERIENCE Component Patterns table; every `{path.to.token}` in both files resolves; all 33 source paths resolve; UJ-1..UJ-4 are carried verbatim with protagonist, numbered steps, climax, and failure paths; and 26 of the 28 prior rubric findings are verifiably closed in the text. What keeps it from *strong* is one high finding — the `FcFluentIcons` inventory in DESIGN.md is stale against its own listed source, so six status glyphs the spine tells downstream to *request* already exist, and under the spine's own "no glyph until the request lands" rule three badge components would ship text-only — plus six medium gaps a story-dev would otherwise decide alone (a v4 `Appearance.Accent` on the confirm button, a nav-glyph reuse that contradicts the spine's rule, no binding of detail routes to `FcAggregateDetailState` where the shell's `Stale` collides with the spine's reserved `Stale`, the `nearing expiry` threshold committed in the reconcile but not carried, regeneration-ceiling numbers pointing at an FR that has none, and no surface action for disabling `hexa`). No critical findings.

## 1. Flow coverage — adequate

Checked: PRD §2.3 journeys UJ-1..UJ-4 (titles compared character-for-character against `prd.md:43,51,59,67`), FR-1..FR-32 including FR-29..FR-32 (`prd.md:438,481,512,554`), NFR-9..NFR-14, addendum, reconcile-brief, sprint change proposal, and the readiness register gate inventory. Each of UJ-1..UJ-4 in `EXPERIENCE.md § Key Flows` (lines 413–480) has a named protagonist, numbered steps, a bolded **Climax**, a Resolution, and at least one failure path; the two UX-derived flows (UX-J5, UX-J6) are labelled as such and grounded in named FRs/NFRs/stories. PRD edge cases are carried: UJ-1 "no Provider/model" (line 430), UJ-2 "lacks permission" (447), UJ-3 "expires" and "loses read access → existence only" (466), UJ-4 "typed denial before side effect" (480). FR→surface: FR-1..FR-32 each land on an IA row, component row, state table, or flow step; FR-29 stages (State Patterns 182–188), FR-30 operations (`audit-governance-panel`, `cost-control-editor`, `content-safety-policy-editor`), FR-31 (Conversation Integration Seam line 91), FR-32 (`cost-control-editor`, `agent-config-form`) are all delivered.

### Findings
- **medium** FR-3 "activate, disable, and inspect `hexa`" has no disable action on any surface. The `hexa` configuration IA row says only "Configure `hexa` and activate it" (`EXPERIENCE.md:51`); `agent-config-form` names Activation as `AgentActivation` and "other writes" as `AgentSetupMutation` (`EXPERIENCE.md:152`, `DESIGN.md:283`) but never names a Disable control, its family, or its confirmation content; the readiness state `disabled` exists (`EXPERIENCE.md:212`) with no way to reach it. FR-1 "create or enable" is likewise unplaced: the overview assumes `hexa` already exists (`EXPERIENCE.md:417`). *Fix:* add Disable/Enable to the `agent-config-form` action rail as a named high-risk family (`AgentActivation` covers both directions, or say so), give `high-impact-confirmation` its content line (in-flight proposals stay proposals, calls rejected before Provider invocation), and state once whether `hexa` is pre-provisioned per tenant or has a "not yet created" state on the overview.
- **medium** The regeneration ceiling is described as "documented default and valid range from FR-32, future proposals only" (`EXPERIENCE.md:152`), but PRD FR-32 says only "a documented default and a valid range" (`prd.md:566`) and FR-16 gives no numbers either. The reference dangles; the proposal-expiry sibling on the same row carries its numbers (24 h, 1 h–30 d). *Fix:* either commit UX-side numbers flagged for Product sign-off, or mark the field "default/range: deferred (owner: Product)" in the row and add it to `reconcile-validation-2026-09-08.md § 8`.
- **low** `AgentCallOperationStatus.Requested` (`src/Hexalith.Agents.Contracts/Operations/AgentCallOperationStatus.cs`) has no row in § Agent call (`EXPERIENCE.md:228–244`): `submitted` is "Local only" and `authoritative pending` is "Accepted identity plus projection reference", so which UX state `Requested` renders as is unstated. *Fix:* one clause: "`Requested` renders as `authoritative pending`."

## 2. Token completeness — adequate

Checked: every frontmatter key in `DESIGN.md` (8 colors, 5 typography roles, `rounded` note, 6 spacing steps, 22 components) against every `{…}` reference in both spines. `DESIGN.md` uses 19 distinct references (`colors.*` ×8, `spacing.1/2/3/4/6/8`, `typography.body/label/heading/caption/mono`), 116 occurrences; `EXPERIENCE.md` uses 7 (`colors.status-*`), 21 occurrences; all resolve by name. Brace placeholders `{count}`, `{expiresAt}`, `{dependency}`, `{party}`, `{n}`, `{AgentInteractionId}` are localization/route placeholders with no dotted path and will not collide with a `{path.to.token}` resolver.

**Inheritance judgment.** Colors are `note:` objects naming Fluent `BadgeColor` roles instead of hex. `design-md-spec.md § UI-system inheritance` allows referencing a system's tokens by name; all eight names (`Brand`, `Danger`, `Important`, `Informative`, `Severe`, `Subtle`, `Success`, `Warning`) exist in the pinned rc.5 `BadgeColor` enum; `BadgeAppearance.Tint`/`Filled` exist; the pin is named (`DESIGN.md:166`); AA targets (4.5:1 text, 3:1 non-text/focus, Light/Dark/forced-colors) are stated with a re-verification rule and a per-route proof lane (`DESIGN.md:205`). The inheritance is complete for badges. It is incomplete where `{colors.brand-accent}` is bound to non-badge chrome (see the first finding).

### Findings
- **medium** `FluentButton` `Appearance.Accent` (`DESIGN.md:152`, `DESIGN.md:347`) does not exist in Fluent UI Blazor v5: at the pinned rc.5, `FluentButton.Appearance` is typed `ButtonAppearance` {`Default`, `Outline`, `Primary`, `Subtle`, `Transparent`}; `Appearance.Accent` belongs to a different (legacy) enum. This is the confirm button of every one of the nine high-risk families, and it contradicts the spine's own Don't ("Reference controls that do not exist in Fluent v5", `DESIGN.md:367`). Related: `brand-accent` inherits "`BadgeColor.Brand` / FrontComposer theme accent" (`DESIGN.md:39`), but its two uses are a button (`ButtonAppearance.Primary`) and `FluentRadioGroup` selection chrome, neither of which consumes `BadgeColor`. *Fix:* replace with `ButtonAppearance.Primary`; reword the `brand-accent` note to "Fluent theme brand tokens as rendered by `ButtonAppearance.Primary` and radio selection; `BadgeColor.Brand` for badges only".
- **low** `components.agent-readiness-badge.color` lists four roles (`DESIGN.md:62`) but the section also renders lifecycle `active` as a `{colors.status-subtle}` chip (`DESIGN.md:267`). *Fix:* add `{colors.status-subtle}` to the list.
- **low** `rounded` is a single `note` rather than a scale object, and `colors.*` are note objects rather than hex strings; both deviate from the spec's literal types and neither is resolver-flattenable. Same convention as the Tenants precedent that `DESIGN.md` lists as a source; note only.

## 3. Component coverage — strong

Checked: component names extracted from `DESIGN.md` frontmatter `components:` (22), `DESIGN.md ###` headings (22), and `EXPERIENCE.md § Component Patterns` bold row names (22); the three sorted sets are identical, matching the reconcile file's canonical 22 exactly. Every backticked kebab name in either file outside the 22 is an ARIA attribute, a CSS logical property, a projection name (`agent-setup-readiness`, `launch-readiness`), or a sprint state. Every component has a visual section with role bindings and variants and a behavioral row with real rules plus a props/events/variants cell. Fluent and FrontComposer primitives named in prose (`FluentAccordion`, `FluentDataGrid`, `FcAggregateListPage`, `FcStatusFilterChips`, …) are inherited primitives, correctly not given rows; all cited names exist in the pinned Fluent package or the FrontComposer submodule.

### Findings
- **low** The seam names an exported `ConversationAgentCallPanel` (`EXPERIENCE.md:81`) while the component is `conversation-agent-call` (`DESIGN.md:105/305`, `EXPERIENCE.md:158`); nothing says they are the same artifact, and the code has exactly one file mentioning the panel. *Fix:* one clause in the `conversation-agent-call` row: "shipped as `ConversationAgentCallPanel`".

## 4. State coverage — adequate

Checked: walked all 14 IA surfaces (`EXPERIENCE.md:48–64`) against empty, cold-load, catching-up, focus, error, offline, permission-denied, pending, stale, and concurrency. Coverage is deep where the domain is: nine readiness states, four provider triples, fifteen call states, ten lifecycle states plus eight display-only outcomes, four launch-evidence states, four audit-availability states, the projection catch-up contract with numbers, the nine-family pending-lock table, `connection lost` (line 310), the single `not available` denial state (308), and the route-heading focus rule (345). Grids get loading/empty/filtered-empty/error/permission-denied/`catching up` generically (308).

### Findings
- **medium** Every route renders `FcAggregateListPage` or `FcAggregateDetailPage` (`EXPERIENCE.md:345`), but the spine never binds its states to the shell's `FcAggregateDetailState` {`Loading`, `Ready`, `Stale`, `Degraded`, `Unauthorized`, `NotFound`, `Unavailable`} (`FcAggregateDetailPage.razor.cs:23`). Three consequences a story-dev must resolve alone: the spine's one `not available` state with "identical copy, status class, and timing" (308) spans three distinct shell states; the shell's `Stale` collides with the spine's reserved `Stale` = evidence past `ValidUntil` (198, 203); and `catching up` has no shell-state home (`Degraded`? `Stale`?). *Fix:* a five-row mapping under § List, detail, and deep-link surfaces: `Loading` → cold load (`LoadingContent`); `Unauthorized`/`NotFound`/`Unavailable` → the single `not available` content; shell `Stale` is not used, `catching up` renders in `Ready` with the freshness badge; `Degraded` → dependency unavailable.
- **medium** `nearing expiry` has no start threshold in either spine: it is a Warning role (`DESIGN.md:197`), a chip on `proposal-state-badge` (`DESIGN.md:275`), a rendering rule on `proposal-queue-grid` (`EXPERIENCE.md:155`), and a once-per-proposal announcement (341), but nothing says *when* it begins. The reconcile committed "10 % of the configured window or 1 h before `ExpiresAt`, whichever is smaller" (`reconcile-validation-2026-09-08.md:64`) and `.memlog.md:34` records it; the spines did not carry it. *Fix:* add the threshold to the `proposal-state-badge` row or a `nearing expiry` row in § Proposal lifecycle.

## 5. Visual reference coverage — strong

Checked: `imports/` is empty; `mockups/` and `wireframes/` do not exist; `.working/` holds only archived validation runs. No orphans and no unspecific references. The spine-only decision is stated once (`EXPERIENCE.md:42`, "By decision, no mockups or wireframes were produced; the spines are the sole reference", backed by `.memlog.md:38` for all 14 surfaces) and spines-win-on-conflict once (`EXPERIENCE.md:30`). No findings.

## 6. Bloat & overspecification — adequate

Checked: pixel values (only the 4 px spacing rhythm — fine), source restatement, prose-vs-table, sections no consumer reads, decorative narrative. `DESIGN.md` is tight (28 KB) and its editorial voice is tied to decisions. `EXPERIENCE.md` prose is declarative; the NFR-14 duplication is gone (one owned statement at line 163, references at `DESIGN.md:327` and `EXPERIENCE.md:336`), UX Closure Decisions is a one-line pointer (30), and non-goals appear once (40).

### Findings
- **low** Component Patterns cells run to 150–200 words of unbroken prose (`EXPERIENCE.md:154` provider-catalog-grid, `156` proposal-editor, `161` content-safety-policy-editor, `168` high-impact-confirmation). The content is real rules, so this is extraction friction, not bloat; `.memlog.md:40` records the column schema as left by decision. Note only. *Fix (optional):* a `### name` subsection with a bullet list per high-density component.
- **low** Two rules are stated in both spines or repeatedly: "Production enablement is a separate indicator, Success only when `RQ-1` records READY" appears at `EXPERIENCE.md:148, 215, 492` and `DESIGN.md:267`; "Accordion titles are h2; in-panel sections h3" at `DESIGN.md:240` and `EXPERIENCE.md:346`. *Fix:* keep one statement per spine and reference it.

## 7. Inheritance discipline — adequate

Checked: all 15 `DESIGN.md` and 18 `EXPERIENCE.md` source paths resolve (including the four Hexalith.Tenants and four FrontComposer files and the shipped `AgentsFrontComposerRegistration.cs`). UJ-1..UJ-4 titles verbatim. Glossary against code and PRD: `ProposedAgentReplyState` 10/10 match the contract enum and the reconcile §1 list; `AuditAvailabilityStatus` 4/4; `ApproverPolicySourceKind` 4/4 (`ConversationOwner` labelled Conversation Facilitator per PRD FR-7/OQ-14, zero "owner" hits outside the sentences forbidding it); `AgentReadinessStatus` 6/6 existing plus the three the spine says must be added; `AgentCallOperationStatus` 8 existing (see §1 for `Requested`) plus five to add; freshness vocabulary (`Stale` / `catching up` / `awaiting projection`) identical across both spines and reconcile §1; nine high-risk families 9/9 in `launch-readiness-register.md`; `RegistryRevision`, `EnvironmentProfile`, `LiveRegionAnnouncedTick`, `LR-UI-CONFORMANCE`, `RQ-1`, `§ NFR-14 Browser Monotonic Timing Contract` all resolve in the register; `EXT-CONV-UI-1`, `EXT-SECRETS-1` resolve in the dependency register; the four policy constants match the code string for string; every story number cited (4.1, 5.2–5.7, 6.1–6.7, 7.1–7.6, 8.1–8.7) exists in `epics.md`; the resx pair exists at 599/599 keys and every key follows `Agents.<Surface>.<Item>` as the spine asserts. Component names identical across all sections. EXPERIENCE token references resolve to DESIGN tokens by name.

### Findings
- **high** The `FcFluentIcons` inventory is stale against the listed source. `DESIGN.md:168` says the curated set is "Apps, BuildingPeople, ChevronRight, DevMode, DeveloperBoard, Navigation, People, PersonBoard, Search, Settings" and the role→glyph table (`DESIGN.md:211–219`) marks six of seven status roles as "FrontComposer request: checkmark circle / clock or progress ring / warning triangle / prohibited circle / dismiss circle / question circle". But `references/Hexalith.FrontComposer/src/Hexalith.FrontComposer.Shell/Components/Icons/FcFluentIcons.cs:66–90` already exposes `CheckmarkCircle16`, `ArrowSync16`, `Warning16`, `SubtractCircle16`, `DismissCircle16`, `QuestionCircle16`, `InfoCircle16` (plus `Checkmark16`, `Star16`, `Edit16`, `Eye16`, `Key16`, `Copy16`); only the `TryCreate` string vocabulary is limited to the ten Size20 nav glyphs, and `DevMode`/`DeveloperBoard` are one glyph (`DevMode20` renders `DeveloperBoard`). Under the spine's own rule — "Until a request lands, the nav entry … renders without a glyph" (187) — `agent-readiness-badge`, `provider-status-badge`, and `proposal-state-badge` would ship text-only for six roles, and the no-color-only promise (207: "role plus one glyph plus visible whole-string text") degrades for glyphs that exist today. The `{colors.status-subtle}` row also binds a Size20 nav glyph (`Regular.Size20.Navigation`) beside Size16 status glyphs. *Fix:* rewrite the role→glyph table to the factory methods (`Success` → `CheckmarkCircle16`, `Informative` → `ArrowSync16` (decide, or request a clock), `Warning` → `Warning16`, `Severe` → `SubtractCircle16`, `Danger` → `DismissCircle16`, `Important` → `QuestionCircle16`, `Subtle` → `InfoCircle16` or none); restate the curated inventory as it is in the source (ten Size20 nav glyphs by `TryCreate` name, thirteen Size16 status/action glyphs by method); keep only genuinely missing glyphs as requests.
- **medium** Nav glyph reuse contradicts the spine's rule: `DESIGN.md:179` assigns `Regular.Size20.Search` to both "Operational status nav" and "Audit evidence nav", while `DESIGN.md:187` says an entry "never reuses another entry's glyph". The shipped registration does the same (`AgentsFrontComposerRegistration.cs:171,184`), so the spine is describing drift as if it were the decision. *Fix:* per the rule, one of the two renders without a glyph until the "history glyph for audit" request lands; say which.
- **low** PRD FR-18 enumerates seven states (`Pending`, `Approved`, `Posted`, `PostFailed`, `Rejected`, `Abandoned`, `Expired`, `prd.md:355`); the spine's ten follow the contract enum, adding `Edited`, `Regenerated`, `PostingPending`. The spine notes only the `PostFailed`/`PostingFailed` alias (`EXPERIENCE.md:248`, `DESIGN.md:275`). A consumer reading PRD and spine side by side finds three states the PRD does not name. *Fix:* one sentence: "PRD FR-18 names seven; `Edited`, `Regenerated`, `PostingPending` are contract states the PRD folds into `Pending`/`Approved`"; raise PRD/contract alignment with Product.
- **low** `AgentsResourcesParityTests` is named as the enforced parity gate (`EXPERIENCE.md:134`, `.memlog.md:33`) but no such test exists under `tests/` (grep: 0). The spine reads as if it exists. *Fix:* "owned by Story 5.7 (to be built)" or the correct story.

## 8. Shape fit — strong

Checked: `DESIGN.md` carries all eight canonical sections in canonical order (Brand & Style 162 → Colors 191 → Typography 223 → Layout & Spacing 231 → Elevation & Depth 253 → Shapes 257 → Components 261 → Do's and Don'ts 353). `EXPERIENCE.md` carries all eight required defaults (Foundation 32, Information Architecture 44, Voice and Tone 93, Component Patterns 142, State Patterns 171, Interaction Primitives 321, Accessibility Floor 332, Key Flows 409) and both required-when-applicable sections, each triggered: Inspiration & Anti-patterns (369; addendum names Slack AI, M365 Copilot, Zoom AI Companion, Rovo; three rejects) and Responsive & Platform (379; four breakpoints, 320 px reflow). Nothing dropped. Invented sections: Conversation Integration Seam (74) earns its place — it is the only home for the `EXT-CONV-UI-1` binding, the membership/removal rules, and the FR-31 rule, and it blocks Story 6.7; FrontComposer Readiness (392) is a pure cross-index but short and every row points at an owning section. No findings.

## Mechanical notes

- **Frontmatter:** both spines carry `name`, `description`, `status: final`, `created`, `updated: 2026-09-08`, `sources`; all 33 paths resolve. `DESIGN.md` lists `./reconcile-validation-2026-09-08.md` as a source (a planning artifact in the same folder) — acceptable, but it is a decision record, not a product source.
- **Cross-refs:** all `{colors.*}`, `{spacing.*}`, `{typography.*}` resolve (DESIGN 19 distinct / EXPERIENCE 7 distinct); `FC-LYT`/`FC-TBL`/`FC-A11Y`/`FC-L10N`, `FcAggregateListPage`, `FcAggregateDetailPage`, `FcFilterEmptyState`, `FcFilterResetButton`, `FcExpandInRowDetail`, `FcStatusFilterChips`, `FcStatusIcon`, `IBadgeCountService`, `FcPendingCommandSummary`, `FcDestructiveConfirmationDialog`, `HeadingTabIndex`, `PageTitle`, `HFC1050`–`HFC1055` all resolve in the FrontComposer submodule; `RegistryRevision` and `EnvironmentProfile` resolve only in the readiness register (0 hits in `src/`) — consistent with the spine's "contracts that must grow".
- **Fluent v5 rc.5:** every cited component type exists (`FluentRadioGroup`, `FluentRadio`, `FluentNumberInput`, `FluentSelect`, `FluentTextArea`, `FluentDialog`, `FluentAccordion`, `FluentAccordionItem`, `FluentDataGrid`, `FluentMessageBar`, `FluentBadge`, `FluentButton`); `BadgeColor` 8/8; `BadgeAppearance.Tint`/`Filled` present. The one invalid reference is `FluentButton Appearance.Accent` (§2).
- **Code drift the spine supersedes (documented, not a spine defect):** `AgentsFrontComposerRegistration.cs` still registers `/agents/conversation-call` (Order 4, `ChevronRight`), lists nine entries, and gates Launch readiness under `Agents.Administrator` with the `Settings` glyph; the spine records the harness rule (68), twelve entries (48–62), `Agents.Operator` for Launch readiness (60), and "none" as today's glyph (183).
- **Reconcile §3 commitments not carried into the spines:** the `nearing expiry` threshold (§4 finding) and "pricing version … increases strictly" (`reconcile:85`; spine has "pinned column" only, `DESIGN.md:291`). The second is server-side and low impact.
- **Name consistency:** `Conversation Facilitator`, `PostingFailed`, `catching up`/`Stale`/`awaiting projection`, `Approver Policy` (PRD casing), `Global Providers Aggregate` / `ProviderCatalog` / Provider catalog (named together at `EXPERIENCE.md:46`) are consistent throughout; the only casing/name splits found are `DevMode`/`DeveloperBoard` (§7) and `ConversationAgentCallPanel`/`conversation-agent-call` (§3).
- **Mermaid:** none used. The single ```` ```text ```` fence (`EXPERIENCE.md:181–186`) is closed.
- **Visual references:** none exist; spine-only and spines-win each stated once (§5).

### Regression check

Prior rubric findings (`.working/validation-2026-09-08-pre-update/review-rubric.md`) against the dispositions in `reconcile-validation-2026-09-08.md § 4` and the current spines.

| # | Prior finding (section · severity) | Disposition claimed | Observed | Citation |
|---|---|---|---|---|
| 1 | UJ-5 not a PRD journey (§1 medium; §7 medium) | A — retitled UX-J5, grounded | resolved | `EXPERIENCE.md:482–484` |
| 2 | FR-18 expiry configuration has no surface (§1 medium) | A — `agent-config-form` field, UJ-1 step | resolved | `EXPERIENCE.md:152, 423`; `DESIGN.md:283` |
| 3 | FR-7 disclosure category not carried (§1 low) | A — per category in builder, history, audit | resolved | `EXPERIENCE.md:153, 157, 167` |
| 4 | Version comparison uncommitted (§1 low) | A — no diff view in V1 | resolved | `EXPERIENCE.md:157` |
| 5 | No pinned Fluent package (§2 medium) | A — pin in Brand & Style | resolved | `DESIGN.md:166` |
| 6 | No contrast target or badge appearance (§2 medium) | A — AA targets, Tint/Filled | resolved | `DESIGN.md:63, 68, 73, 205, 207` |
| 7 | Icon half unspecified (§2 medium) | A — role→glyph table with `FcFluentIcons` names and requests | **partially resolved** — table exists, but six of seven roles are "requests" for glyphs the source already exposes (`FcFluentIcons.cs:66–90`); see §7 high | `DESIGN.md:168, 211–219` |
| 8 | Pipe-separated colour lists not resolver-flattenable (§2 low) | no change by decision | unchanged (not claimed fixed) | `DESIGN.md:62, 67, 72` |
| 9 | High-impact confirmation / pending indicator unnamed; rules under `audit-governance-panel` (§3 high) | A — two components added; paragraphs moved | resolved | `DESIGN.md:149–159, 345–351, 329–331`; `EXPERIENCE.md:168–169` |
| 10 | `conversation-agent-call` dialog vs panel (§3 low) | A — `FluentDialog` | resolved | `DESIGN.md:106, 307`; `EXPERIENCE.md:158` |
| 11 | `proposal-notification` has no anatomy (§3 low) | A — nav-entry count, domain-named link | resolved | `DESIGN.md:335`; `EXPERIENCE.md:165` |
| 12 | No offline / connection-lost state (§4 medium) | A — `connection lost` row | resolved | `EXPERIENCE.md:310` |
| 13 | No concurrent-edit / stale-detail treatment (§4 medium) | A — `stale proposal` | resolved | `EXPERIENCE.md:274` |
| 14 | Call hexa hidden vs disabled when not callable (§4 low) | A — visible, `aria-disabled`, safe blocker | resolved | `EXPERIENCE.md:87`; `DESIGN.md:307` |
| 15 | Prompt surface states unspecified (§4 low) | A — prompt required, no client limit | resolved | `EXPERIENCE.md:158` |
| 16 | Spine-only decision not recorded (§5 low) | A — Foundation clause + memlog | resolved | `EXPERIENCE.md:42`; `.memlog.md:38` |
| 17 | NFR-14 restated in six places (§6 medium) | A — one owned statement, references elsewhere, section removed | resolved | `EXPERIENCE.md:163, 336`; `DESIGN.md:327`; no Browser Performance Evidence section |
| 18 | UX Closure Decisions is a changelog (§6 low) | A — reduced to a pointer | resolved | `EXPERIENCE.md:30` |
| 19 | Non-goals restated twice (§6 low) | A — once in Foundation | resolved | `EXPERIENCE.md:40`; § Inspiration 369–377 |
| 20 | "Conversation owner" vs AD-8 Facilitator (§7 medium) | A — Facilitator everywhere | resolved | `EXPERIENCE.md:123, 153`; `DESIGN.md:287, 362` |
| 21 | `pending approval` as an eleventh state (§7 low) | A — dropped as a state | resolved — survives only as link copy `{count} proposals pending approval` | `EXPERIENCE.md:114, 165`; `DESIGN.md:335` |
| 22 | `Global Providers Aggregate` absent (§7 low) | A — three names given together | resolved | `EXPERIENCE.md:46` |
| 23 | Casing drift (`approved`, `approver policy`, `Level 4–5`) (§7 low) | A — normalised to PRD forms | resolved — 0 hits for the lowercase/`Level 4` forms | `EXPERIENCE.md:422, 454` |
| 24 | Browser Performance Evidence adds no decision (§8 low) | A — removed | resolved | section absent |
| 25 | EXPERIENCE frontmatter lacks `description` (§8 low) | A — added | resolved | `EXPERIENCE.md:3` |
| 26 | Accordion / high-impact rules misplaced under `audit-governance-panel` (mechanical) | A — moved | resolved | `DESIGN.md:240` (Layout & Spacing); `DESIGN.md:345` |
| 27 | "Candidate entries" vs "final" (mechanical) | A | resolved — 0 hits for "Candidate" | `EXPERIENCE.md:46, 72` |
| 28 | Hedge "can be refined by architecture" (mechanical) | A | resolved — 0 hits | — |

Totals: **26 resolved · 1 partially resolved (#7) · 0 not resolved · 1 unchanged by decision (#8)**.

## Finding totals

| Severity | Count |
|---|---|
| critical | 0 |
| high | 1 |
| medium | 6 |
| low | 8 |
