# Implementation Readiness Review — Hexalith Agents

Reviewer lens: the story-dev agent that must build any story in Epics 5–8 from `DESIGN.md` + `EXPERIENCE.md` (both final, `updated: 2026-09-09`) plus the named sources, with `src/Hexalith.Agents.UI` as the starting codebase and no one to ask. Verified against: the shipped UI (registration, 11 pages, 22 shared components, `Contracts` enums, `Resources/*.resx`, `test/Hexalith.Agents.UI.Tests`), FrontComposer `src/` and its four `_bmad-output/contracts/` constraint sets, Fluent UI Blazor v5 through the `fluent-ui-blazor` MCP (server documents `5.0.0.26180`; the pin is `5.0.0-rc.5-26219.1`), `epics.md` Stories 5.1–8.7, `ARCHITECTURE-SPINE.md`, `launch-readiness-register.md`, `external-dependency-register.md`, `hexalith-ux-instructions.md`, and `.working/validation-2026-09-09-pre-update/review-implementation-readiness.md`. Date: 2026-09-09.

## Overall verdict

The 2026-09-09 Update landed the corrections that mattered most to a build, and it landed them *accurately*: I re-checked the icon claim against `FcFluentIcons.cs` and it is now exact to the character — twelve `TryCreate` names, thirteen no-arg 16 px factories unreachable through it, all seven status roles bound to methods that exist — and the `FcAggregateDetailPage` heading claim, the `ButtonAppearance.Primary` correction, the `DisabledFocusable` binding, the nearing-expiry threshold, and every one of the eleven listed shipped-code divergences check out against the code today. Nothing is critical. What still stops a dev is no longer *vocabulary* but *seams and completeness*: three of the four surfaces the spine calls mandatory on its grids write into a shell Fluxor snapshot that the spine's own replacement filter controls do not use, so the mandated Reset button clears state nobody applied; the register's `EXT-CONV-UI-1` still describes a narrower, incompatible seam than the one the spine widened it to, and `epics.md` Story 6.7 still says there is no seam at all; the `ProviderReadinessResult` by-Provider/model query the Provider-disable confirmation depends on is named at Story 5.5 but absent from Story 5.5's acceptance criteria, from Architecture, and from the ship-first story that needs it. And the § Shipped-code corrections table — the Update's best new idea — is two rows short and two rows too narrow: the shipped UI ships **no CSS at all** behind ~115 BEM class names, the two `Agents.Surface.NotAvailable.*` keys the spine makes mandatory **do not exist** while the per-cause keys it forbids do, and the `role="status"` and `Disabled` rows each name a handful of sites where the divergence is repo-wide. PRD FR-18's seven-versus-ten misalignment blocks nothing today. `sprint-status.yaml` still stops at Epic 5.

## Conversations seam (`EXT-CONV-UI-1`)

- **[high]** The register's `EXT-CONV-UI-1` describes a seam the spine no longer wants. `EXPERIENCE.md § Conversation Integration Seam` requires **two artifact kinds** (an action contribution *and* a per-message decoration slot keyed by `MessageId`), an **Agents-side provenance accessor** keyed by `MessageId`, and **`GetCallabilityAsync(tenant, conversation)`** on the gateway. `external-dependency-register.md § EXT-CONV-UI-1` `RequiredArtifact` carries only the action contribution plus "rendering of the AI-generated and human-edited provenance markers **carried in message metadata**" — which is `EXT-CONV-AI-1`'s seam (2) posting metadata, i.e. Conversations rendering from its own data. Those are two incompatible designs for the same marker, and neither the decoration slot nor `GetCallabilityAsync` appears anywhere in the register. A Conversations maintainer accepting the register's fields would deliver something Story 6.7 cannot consume. *Fix:* amend the register's `RequiredArtifact` to the spine's three additions, or — if the metadata-carried design is preferred — say so in `EXPERIENCE.md § Conversation Integration Seam` and delete the Agents-side accessor from the `agent-response-marker` row. Do not leave both standing.
- **[high]** `epics.md:1841` still reads "**External:** No new external seam; the story consumes only the completed automatic path," and the register's `ConsumingStories` for `EXT-CONV-UI-1` is still `TBD`, while `EXPERIENCE.md § Conversation Integration Seam` (Consuming story row) blocks 6.7 from `ready-for-dev` under FR-21. Two of the three documents a sprint planner reads say 6.7 is ready. Correctly deferred with a named owner in `reconcile-validation-2026-09-09.md § 4`, but the contradiction is still live and it is a two-line edit. *Fix:* land it before Epic 6 planning; until then the spine's own rule is the only thing holding the gate.
- **[medium]** Harness removal is a spine rule with no acceptance criterion to derive. `EXPERIENCE.md § Information Architecture` (rules) says "Story 6.7 removes it: the route is unregistered and the page deleted before 6.7 closes". Story 6.7's ACs (`epics.md:1846-1876`) say only that "alternate invocation entries are absent"; its `NegativeEvidence` does name `AlternateInvocationGuardTests`. The two artifacts are `src/Hexalith.Agents.UI/Composition/AgentsFrontComposerRegistration.cs` (the `Order: 4` `"Conversation call"` entry) and `src/Hexalith.Agents.UI/Components/Pages/ConversationCall.razor`. *Fix:* name both in the spine's rule so the AC writes itself.

## Grid seams (FC-TBL, Fluxor, and the third grid)

- **[high]** The mandated `FcFilterResetButton` cannot reset the filters the spine actually applies. `FcFilterResetButton.razor.cs:52` dispatches `FiltersResetAction(ViewKey)`; `FilterEffects.cs:140-146` translates that into `ClearGridStateAction(viewKey)`, which removes that view's entry from the shell's `DataGridNavigationState.ViewStates`. `FcStatusFilterChips.razor.cs` writes into the same snapshot's reserved `__status` key. But the 2026-09-09 revision replaced the chips with "Agents-owned `aria-pressed` toggle buttons inside a named `role="group"`" (`proposal-queue-grid` row), and `EXPERIENCE.md § Grid rules` still lists `FcFilterResetButton` as mandatory while saying only that each component "takes one [`ViewKey`]" and that "view-key mismatches fail closed". Nobody is told to read `ViewStates[ViewKey].Filters` back and apply it, or to dispatch `ColumnFilterChangedAction`/`StatusFilterToggledAction` from the Agents toggles. Built as written, both grids get a Reset button that clears a snapshot nothing reads while the applied filters survive — and the mandated filtered-empty state is unrecoverable. *Fix:* choose one and say it in § Grid rules — either the Agents filter state *is* the shell snapshot (read `ViewStates[ViewKey]`, dispatch the shell's filter actions from the toggles) or `FcFilterResetButton` comes off the mandatory list and Agents owns the reset control.
- **[medium]** "The mandatory FC-TBL component set" misattributes an Agents decision to FrontComposer. `references/Hexalith.FrontComposer/_bmad-output/contracts/fc-tbl-table-api-contract-2026-06-04.md` defines a *frozen 14-type public surface* (adding `ColumnDescriptor`, `ColumnVisibilityContext`, `FcColumnFilterCell`, `FcColumnPrioritizer`, `FcExpandedRowHiddenBanner`, `FcFilterSummary`, `FcMaxItemsCapNotice`, `FcNewItemIndicator`, `FcProjectionGlobalSearch`, `FcSlowQueryNotice`) plus generated-grid envelope rules — `data-fc-datagrid`, `Virtualize=true`, density-bound `ItemSize`, `OverscanCount=3`, stable item keys, priority-then-declaration column order, `MaxVisibleColumns` default 10, reserved column keys `__status`/`__search`/`__hidden`, and a detail panel outside the virtualized grid with an always-present `role="region"`. It does not define a four-component usage mandate. `EXPERIENCE.md § Grid rules` (Mandatory components), § FrontComposer Readiness (FC-TBL row), and `DESIGN.md § Layout & Spacing` ("the mandatory FC-TBL component set") all present the four-name list as FC-TBL's. A dev who opens the contract to check will not find it, and will not know whether the envelope rules bind a hand-authored grid. *Fix:* relabel the four as an Agents-owned set, and state which FC-TBL envelope rules (virtualization, `ItemSize`, reserved keys, the `role="region"` detail panel) carry over to a hand-authored `FluentDataGrid`.
- **[medium]** `FcExpandInRowDetail` has three `[EditorRequired]` parameters the spine never supplies. `FcExpandInRowDetail.razor.cs:40-71` requires `ViewKey`, `HasExpanded`, `ChildContent`, and **`DetailPanelAriaLabel`** (all `[EditorRequired]` except the optional `SuppressedAnnouncement`/`PanelId`), and injects `IExpandInRowJSModule`. `EXPERIENCE.md § Grid rules` assigns the `ViewKey` and § Responsive & Platform says secondary columns move into the component; nothing names the localized panel label or where `HasExpanded` comes from on a hand-authored grid. *Fix:* add `DetailPanelAriaLabel` to the localization inventory (it is a per-grid whole string) and name `HasExpanded`'s source in § Grid rules.
- **[medium]** Launch readiness is a third `FluentDataGrid` governed by nothing. `DESIGN.md § launch-readiness-panel` mandates a FullWidth `FluentDataGrid` of gate records, but `EXPERIENCE.md § Grid rules` opens with "These govern **both** Agents grids" and `DESIGN.md § Layout & Spacing` says "The mandatory FC-TBL component set therefore applies to the Provider catalog and Proposal queue grids only". So the gate grid has no wrapper (`FcAggregateListPage`?), no `ViewKey`, no `ItemKey`, no distinguished loading/empty/error/`catching up` states, and no 320 CSS px surviving-column set — while every other grid rule exists precisely because those things were needed. Shipped code builds it as a raw `<table>` (`LaunchReadiness.razor:100-127`). *Fix:* bring it under § Grid rules with its own surviving-column set, or state explicitly that it is a plain read-only grid and give it the 320 px column list.

## § Shipped-code corrections: completeness and scope

All eleven listed divergences are still real and still mapped. Verified today: polling `250 ms`/`5 s` at `AgentConfiguration.razor:222-223` and `200 ms`/`8 s` at `ProviderCatalog.razor:233-234`; the harness at `Order: 4` in `AgentsFrontComposerRegistration.cs`; nine `AddNavEntry` calls (Order 0–8) against the spine's twelve; Launch readiness gated `AgentsAdministratorPolicy`; queue default sort `GridSort.ByDescending(CreatedAt)` at `ProposalQueue.razor:173-174`; `role="listbox"` at `ProposalVersionHistory.razor:14`; no `FluentDialog` anywhere in the project, so confirmations are inline; `_currency = "USD"` at `ProviderCatalog.razor:253,309,328` and `PricingVersion: 0` at `:597`; five badges carrying `role="status"`; `Disabled` on `ProposalRegenerator.razor`; and `test/Hexalith.Agents.UI.Tests/LocalizationResourceTests.cs`. Two rows are too narrow and two divergences are missing.

- **[high]** **Twelfth divergence, unlisted: the shipped UI has no CSS.** `src/Hexalith.Agents.UI` contains no `.css`, no `.razor.css`, no `wwwroot`, no `<style>` block, and no inline `style=` attribute, while its Razor carries roughly 115 distinct BEM class values (`agents-launch-readiness__metrics-table`, `proposal-version-history__marker--approved`, `conversation-agent-call-panel__status`, a bare `class="mono"`, …). Every layout instruction in `DESIGN.md § Layout & Spacing` — the `{spacing.1}`…`{spacing.8}` rhythm, `scroll-padding` equal to sticky heights, logical `inline-start`/`inline-end` rails for RTL, reserved stable space for badges and pending indicators — and `{typography.mono}` for identifiers therefore has no delivery mechanism, no owning story, and no row in this table. This is the single largest unowned build decision in the pair. *Fix:* add a row naming the delivery decision — Fluent component parameters (`FluentStack` `*Gap`/`Orientation`, `FluentText` `Size`/`Weight`, `Margin`/`Padding`) and Fluent 2 tokens per `references/Hexalith.AI.Tools/hexalith-ux-instructions.md § No theme redefinition`, versus one Agents stylesheet for layout the design system does not own — with an absorbing story (8.6 is the conformance lane; the classes are authored in 5.x/7.x). The positive: **zero** hits for `--type-ramp-*`, `--neutral-*`, `--accent-*`, `--neutral-fill-*`, or `--palette-*` across all of `src/`, so there is no legacy-token backlog to allowlist.
- **[high]** **Twelfth divergence, unlisted: the two `not available` keys do not exist and the keys they replace do.** `EXPERIENCE.md § Voice and Tone` requires the single `not available` state to use "exactly two keys, `Agents.Surface.NotAvailable.Title` and `Agents.Surface.NotAvailable.Message`" bound identically to `Unauthorized`, `NotFound`, and `Unavailable`, and calls per-cause copy "a conformance failure, because differing copy is itself a disclosure channel". `src/Hexalith.Agents.UI/Resources/AgentsResources.resx` (and `.fr.resx`) carry **no `Agents.Surface.NotAvailable.*` key at all**, and do carry exactly the per-cause set the rule forbids: `Agents.Surface.PermissionDenied.Title/.Message`, `Empty.*`, `FilteredEmpty.*`, `Error.*`, `Unavailable.*`, `Stale.*`, `Degraded.*`. Worse, the same section ends "`AgentsResources.fr.resx` is the enforced source; on conflict the resource file wins and this table is updated" — read literally, the shipped per-cause copy wins over the rule that forbids it. *Fix:* add a row to § Shipped-code corrections for "per-cause `not available` copy (`Agents.Surface.PermissionDenied.*` et al.)" with an absorbing story (8.6, alongside the parity-gate rename), and narrow the enforced-source sentence to *wording of existing keys*, not key inventory.
- **[high]** Two listed rows are scoped smaller than the divergence, so their absorbing story is undersized.
  - "Per-badge `role="status"` on five badge components → 8.6" understates it by three times. There are 15 `role="status"` sites across 13 files: five badges (`AgentReadinessBadge.razor:9`, `ProviderStatusBadge.razor:10`, `ProposedAgentReplyStateBadge.razor:10`, `AgentCallStatusBadge.razor:10`, `AuditAvailabilityBadge.razor:13`), **three panels** (`OperationalStatusPanel.razor:15`, `AuditEvidencePanel.razor:17`, `LaunchReadinessPanel.razor:12`), **three inline policy rows** (`ApproverPolicy.razor:48,70,93`), and four page/action nodes (`ProposalEditor.razor:53`, `ProposalRegenerator.razor:40`, `ProposalDetail.razor:47`, `LaunchReadiness.razor:142`). `EXPERIENCE.md § Accessibility Floor` already covers all of them ("no badge, panel, or row component carries `role="status"` of its own") — the correction row does not, so a Story 8.6 dev reading the table removes five and leaves ten.
  - "`Disabled` on the regenerate control → 7.3": `DisabledFocusable` has **zero** occurrences anywhere in `src/`. Every blocked-but-explicable control across eleven pages diverges from § Interaction Primitives, not one control in one story.
  *Fix:* restate both rows as classes of divergence with their site counts, or split them into per-story rows (5.2/5.3/5.7/7.2/7.3/8.6) so each story pays its own share.
- **[medium]** Two superseded shipped types are named in behavioral rows but have no absorbing story. The `agent-readiness-badge` row says "The built `AgentReadiness.MapState` (`Callable = Active && no blockers`) is superseded and replaced" and the `provider-status-badge` row supersedes the shipped provider state, but neither appears in § Shipped-code corrections. The concrete artifacts are `src/Hexalith.Agents.UI/Components/Shared/AgentReadiness.cs` — `MapState` plus a UI-local `ProviderReadinessState` enum at `:48-70` (`Enabled, NotConfigured, Disabled, HistoricalSelection, Degraded, Failed, Unknown`) whose shape has no `OperationalState`, `Callability`, `ReasonCode`, `CapabilityVersion`, `ObservedAt`, or `ValidUntil`. *Fix:* add one row mapping both to Story 5.5, which is where the replacing contract lands.
- **[low]** Two raw-HTML classes violate `hexalith-ux-instructions.md` and are absent from the table. `LaunchReadiness.razor:100-127` renders a full `<table>/<thead>/<tr>/<th scope>/<td>` metrics grid where `DESIGN.md § launch-readiness-panel` mandates `FluentDataGrid` — squarely against § Reuse over hand-rolling. `AgentConfiguration.razor` carries 11 `<section>` with 10 `<h2>` sibling titled sections and no `FluentAccordion`, against § Page sections and `DESIGN.md § Layout & Spacing`. *Fix:* one row per class, absorbed by 8.7 and 5.2/5.7.
- **[low]** `AgentsFrontComposerRegistration.cs`'s `RegisterDomain` XML doc says "its **eight** ordered, policy-gated nav entries" while nine are registered. The spine's row correctly says nine. *Fix:* note that the code comment is wrong so a dev does not reconcile the spine down to eight.

## Accordion law versus the spine's own table

- **[medium]** Two page-like surfaces with two or more sibling titled sections are not grouped in a `FluentAccordion` and are not in `DESIGN.md § Layout & Spacing`'s four-row accordion table. `DESIGN.md § operational-status-panel` and its EXPERIENCE row group rows under **six** named recovery actions (configure Provider, fix policy, wait for approval, retry posting, inspect audit, start a new call); `DESIGN.md § audit-evidence-panel` and its EXPERIENCE row render **two** evidence classes (support-safe evidence; configuration-and-governance change evidence). `references/Hexalith.AI.Tools/hexalith-ux-instructions.md § Page sections` requires one `FluentAccordion` with one `FluentAccordionItem` per section on exactly these surfaces, and `DESIGN.md § Layout & Spacing` restates the rule — but the table that tells a dev *which item is primary* covers only Agents overview, Provider catalog, Proposal detail, and "Policy surfaces". Launch readiness, Cost controls, Content safety, and Audit governance state their items in their component rows only. *Fix:* extend the table to every multi-section route with its primary region named, or state that the component rows are authoritative and the table is illustrative.
- **[low]** The table's "Policy surfaces" row collapses three routes with different section sets into one generic line ("Draft; Authoring; History"). `conversation-context-policy-panel` is read-only with "no control of any kind", so it has no Draft and no Authoring item. *Fix:* split into the three named routes.

## Contract seams that must grow

- **[high]** The Provider-disable blast-radius query has no home and no interim. `EXPERIENCE.md § Known gaps` assigns `ProviderReadinessResult` "including a by-Provider/model query for the disable blast radius" to Story 5.5, and the `provider-catalog-grid` row says the disable confirmation's in-scope-Agents list is "supplied by the Story 5.5 readiness result **queried by Provider and model**". Story 5.5's acceptance criteria (`epics.md:1374-1391`) specify the record schema, the three valid triples, and an *operation-family* readiness request — no by-Provider/model query. `ARCHITECTURE-SPINE.md:151` fully specifies the result's fields and `ProviderReadinessReasonCode` but no such query. And the disable action ships in **Story 5.3, before 5.5**. Every other 5.5-dependent rendering has an interim (`Unknown`/Subtle for the badge, currency-as-entered before 8.4); this one has none, so a 5.3 dev must either invent the query or ship a confirmation the spine forbids as incomplete. *Fix:* add the interim to the `provider-catalog-grid` row — before 5.5, the confirmation names the platform scope and renders the in-scope-Agents list and cross-tenant count as a `{colors.status-subtle}` deferred metric — or move the query's ownership to 5.3.
- **[medium]** `AgentCallOperationStatus` and `AgentReadinessStatus` growth is specified nowhere but here, and the additive rule is unstated. Shipped: `AgentCallOperationStatus` = `Unknown, Requested, Authorized, Denied, ContextLoading, ContextBlocked, Generating, GenerationFailed, Generated` and `AgentReadinessStatus` = `Unknown, Callable, Checking, InvalidConfiguration, MissingPartyIdentity, ProviderUnavailable, Disabled` (`src/Hexalith.Agents.Contracts/Operations/`). `ARCHITECTURE-SPINE.md` mentions **neither enum**, though it specifies `ProviderReadinessResult` in full including a versioned additive `ProviderReadinessReasonCode` whose "unknown codes fail closed". The eleven tokens § Agent call and § Known gaps require are named with rendering rules for each, so **no Epic 6 story is blocked from starting** — but the enums that must grow carry no counterpart of the reason-code rule. *Fix:* state that these two enums are versioned additive and that an unknown value renders `authoritative pending` (never Success, never a terminal), and point Architecture at § Agent call as the normative token list rather than restating it there.
- **[medium]** The projection nudge binding is correct but incomplete, and Architecture still has nothing. `IProjectionChangeDetailNotifier` exists with the exact member the spine names — `event Func<ProjectionChangedDetail, Task>? ProjectionChangedDetail` at `references/Hexalith.FrontComposer/src/Hexalith.FrontComposer.Contracts/Communication/IProjectionChangeNotifier.cs:49` — and it is registered `TryAddScoped` at `Shell/Extensions/EventStoreServiceExtensions.cs:88`, so § Projection catch-up contract's nudge row is buildable. Two gaps: the spine does not say the registration arrives through the EventStore extension (a dev injecting it into a page whose host skipped that call gets a DI failure at first render), and `ARCHITECTURE-SPINE.md` contains **zero** occurrences of `poll`, `nudge`, `SignalR`, `IProjectionChange`, or `catching up`, so the 250 ms / 8 s contract lives only in this file while the two shipped paths run 250 ms/5 s and 200 ms/8 s. *Fix:* name the registration extension in the Standard row and keep the Architecture deferral visible at the point of use, not only in `reconcile-validation-2026-09-09.md § 4`.
- **[low]** `reconcile-validation-2026-09-09.md § 4` calls the "contracts-that-must-grow table in `EXPERIENCE.md`" the full list of Architecture's items, but that table does not contain `IProjectionChangeDetailNotifier` (nothing must grow — it already carries the member) or `EXT-CONV-UI-1` (which is in the seam table instead). *Fix:* either add both as pointer rows or drop the "full list" claim.

## PRD FR-18 seven states versus the ten-value enum

- **[low]** **No story is blocked by this today.** `ProposedAgentReplyState` ships all ten values plus `Unknown` (`src/Hexalith.Agents.Contracts/AgentInteraction/ProposedAgentReplyState.cs`), `DESIGN.md § proposal-state-badge` and `EXPERIENCE.md § Proposal lifecycle` render all ten with a colour role each, and § Proposal lifecycle states the fold and the `PostFailed` → `PostingFailed` alias explicitly. A story-dev on 7.1–7.6 builds from the enum and reads the spine for the mapping; nothing is ambiguous at the keyboard. *Fix:* say that plainly in the § Proposal lifecycle paragraph ("no story is blocked; the enum governs") so the Product deferral is not read as a gate.

## `IBadgeCountService` dropped for V1

- **[medium]** Suppression is real but it is not a switch, and the spine does not say what it rests on. Verified: `IBadgeCountService` is keyed `IReadOnlyDictionary<Type,int>` over projection runtime types (`Contracts/Badges/IBadgeCountService.cs:29`) with `CountChanged` and `TotalActionableItems` and **no label member**; the nav badges render at `Shell/Components/Layout/FrontComposerNavigation.razor:128-130` and `:166-168` as `<FluentBadge>@count</FluentBadge>` with a `data-testid` and **no `aria-label`, no visually-hidden label, no parameter slot**; `FrontComposerNavigation` exposes **no `[Parameter]` at all**; and the service is registered `AddScoped` (not `TryAdd`) at `Shell/Extensions/ServiceCollectionExtensions.cs:338` with `CapabilityDiscoveryEffects` taking it as a required constructor dependency — so there is **no domain opt-out flag**. The count stays suppressed *only* because Agents declares no `[ProjectionRole(ProjectionRole.ActionQueue)]` projection, which is what `ReflectionActionQueueProjectionCatalog.cs:81` enumerates, leaving the `@if (count > 0)` gate closed. The spine's claim holds — but by construction, not by configuration. *Fix:* state the mechanism and state it as a standing constraint in the `proposal-notification` row: the day any Agents projection is marked `ActionQueue`, an unlabelled number appears in the rail and cannot be turned off.
- **[low]** The domain-rendered string is localizable and its convention holds — whole string with a named placeholder, `Agents.<Surface>.<Item>`, matched by 599/599 EN/FR parity in `AgentsResources.resx`/`.fr.resx` — but no `Agents.Overview.PendingProposals.*` key exists yet, and the spine's plural rule requires per-count keys (`.Zero`, `.One`, `.Other`) while the `proposal-notification` variants say "zero (no badge), count". *Fix:* reconcile — zero renders no badge, so `.One` and `.Other` are the whole inventory; say so, or the parity gate will demand a `.Zero` string nothing can render.

## Story ownership

Every story named anywhere in the pair exists in `epics.md`: 5.1–5.7, 6.1–6.7, 7.1–7.6, 8.1–8.7, plus 4.1 for Omar's reference journey. Every § Information Architecture owning story, every § Key Flows story list, every § Known gaps owner, and every § Shipped-code corrections absorbing story resolves to a real heading. That is a clean result and it was not true two runs ago.

- **[medium]** Story 6.2 owns `/agents/context-policy` in § Information Architecture and its acceptance criteria (`epics.md:1585-1607`) never mention a UI read model, a route, or exposing the tokenizer basis, reserved output allowance, safety margin, or Agent Instructions size that `conversation-context-policy-panel` renders. The route is unbuildable and the owning story does not know it owns it. The same holds for the Operational status extras — per-tenant blocked counts, per-Conversation `safety blocked` history, cost consumption, projection id/version — which have no contract and no AC. Both are correctly deferred to the Epics maintainer in `reconcile-validation-2026-09-09.md § 4` and, to the spine's credit, marked at the point of use inside the `operational-status-panel` row. Still open. *Fix (Epics):* one AC clause each.
- **[low]** `/agents/audit` and `/agents/audit-governance` are the only IA rows without a single named owning story ("7.x, 8.x" and "8.1 to 8.3"). The audit list is now declared an id-entry surface, so it is small — but nobody ships it. *Fix:* name the story that builds the id-entry surface and its routing.

## `sprint-status.yaml`

- **[medium]** Still not regenerated, and now a full epic-set behind. `_bmad-output/implementation-artifacts/sprint-status.yaml` (last modified 2026-09-08; `last_updated: 2026-09-08`, `generated: 2026-06-23T21:28:59+02:00`, 179 lines) tracks the superseded `5-1 … 5-18` slugs — `5-1` done, `5-2` and `5-3` `review`, `5-4` through `5-18` `backlog` — and **stops at `epic-5`. Epics 6, 7, and 8 are absent entirely.** Not one story in Epics 6–8 can be picked up, sequenced, or reported through sprint tracking, and the brownfield-drift review at `architecture/architecture-agents-2026-06-23-2/reviews/review-2026-09-08-brownfield-drift.md:65` already cites "Stories 6.2, 6.4, 7.3" that the file does not contain. Deferred to Sprint planning since 2026-09-08. *Fix:* regenerate against `epics.md` Epics 5–8 before Epic 6 planning; the spine cannot fix this and correctly does not try.

## What checks out

Positive verifications, stated once each because a dev needs to know where *not* to spend a day.

- **`FcFluentIcons` is now exact.** `Components/Icons/FcFluentIcons.cs`: `TryCreate` accepts exactly the twelve names `DESIGN.md § Brand & Style` lists, in that vocabulary and no others (switch at `:134-147`, `null` otherwise); exactly thirteen no-arg Regular 16 px factories exist (`:85-121`) and match the spine's list item for item; **none of the thirteen is reachable through `TryCreate`**, as stated; all seven status roles bind to real methods (`CheckmarkCircle16`, `ArrowSync16`, `Warning16`, `SubtractCircle16`, `DismissCircle16`, `QuestionCircle16`, `InfoCircle16`), so **no status glyph is a FrontComposer request**; `DevMode20` does render `DeveloperBoard` (`:39-40`) and `DeveloperBoard` is not a `TryCreate` name. Two aliases worth knowing but harmless: `DocumentSearch48` delegates to `DocumentSearch32` (`:34`) and `InfoCircle16` maps to `Regular.Size16.Info` (`:94`).
- **The route-heading split is right.** `FcAggregateListPage<TItem>` has `Heading` (`:26`), `HeadingTabIndex` (`:38`), `PageTitle` (`:23`) and `FocusHeadingAsync()` (`:83`). `FcAggregateDetailPage<TItem>` has 18 parameters and **none** heading-related — its own class doc says the domain supplies `FcPageHeader`. `FcPageHeader` carries all three plus `Actions`/`Metadata`, and `FocusHeadingAsync()` throws if `Heading` is blank or `HeadingTabIndex` is null, which the spine's "non-blank localized heading with `HeadingTabIndex=-1`" satisfies by construction. `BackLinkLabel` really is the anchor's only content (`FcAggregateDetailPage.razor:19-24`), so the spine's warning about an unnamed anchor is correct.
- **The `FcAggregateDetailState` mapping matches the component.** `FcAggregateDetailPage.razor:27-45` renders the ready body only for `Ready`/`Stale`/`Degraded` with a caller-supplied banner above it, and `ResolveStateContent()` (`.razor.cs:82-88`) maps `Loading`/`Unauthorized`/`NotFound` to their own slots, failing closed to `UnavailableContent` — exactly "one slot per state", exactly a degraded *banner* over the ready body, and declaring shell `Stale` unused is safe because the domain sets `State`.
- **`FcPageLayoutMode` has exactly `FullWidth` and `Constrained`**, and one-mode-per-route is not a style preference: `FcPageLayoutCoordinator.cs:22-23` documents two measures on one page as unsupported and `Reset()` forces the whole page back to `FullWidth` on dispose.
- **Every Fluent v5 name in the pair exists under that name at the MCP's build.** `FluentButton.DisabledFocusable` ("disabled yet still reachable") and `FluentButton.AutoFocus`; `ButtonAppearance` = `Default, Outline, Primary, Subtle, Transparent` — **`Accent` does not exist and has zero occurrences in either spine or anywhere in `src/`**, so that prior finding is fully closed; `FluentMessageBar.AriaLive` = `Off, Polite, Assertive`; `MessageBarIntent` = `Success, Warning, Error, Info, Custom`, which is why page-level-only is the right restriction; all eight `BadgeColor` roles; `BadgeAppearance` = `Filled, Ghost, Outline, Tint`; `FluentBadge.IconLabel`; `FluentDialogBody.FixedHeaderFooter` (default `true`); `FluentAccordion`/`FluentAccordionItem`; `FluentText`; `FluentStack`; `FluentTextArea`; `FluentSelect<T>`; `FluentRadioGroup<T>`/`FluentRadio<T>`; `FluentNumberInput<T>`; `FluentDataGrid<T>`. The generics (`FcAggregateListPage<TItem>`, `FluentNumberInput<T>`, `FluentRadioGroup<T>`) are unstated in the spine but obvious at the keyboard.
- **Localization is already the shipped practice.** `Resources/AgentsResources.resx` and `AgentsResources.fr.resx` both hold **599 keys at exact parity** (zero EN-only, zero FR-only), follow `Agents.<Surface>.<Item>`, and include enum-keyed whole-string lookups; the parity gate is `test/Hexalith.Agents.UI.Tests/LocalizationResourceTests.cs`, exactly as § Voice and Tone names it with the Story 8.6 rename. FC-L10N's two-localizer split (`IStringLocalizer<FcShellResources>` for chrome, host-owned for domain) matches the spine's shell/domain division.
- **`ProviderReadinessResult` is fully derivable from Architecture even though no code exists.** `ARCHITECTURE-SPINE.md:151` gives `OperationalState`/`Callability`/`ReasonCode`/`CapabilityVersion`/`ObservedAt`/exclusive `ValidUntil`, the fifteen-value `ProviderReadinessReasonCode`, the three valid triples, and the rule that unknown codes fail closed — so `provider-status-badge` needs no invention once the type lands.
- **The 22 component names are identical and identically ordered** in `DESIGN.md`'s `components:` frontmatter, its `###` sections, and `EXPERIENCE.md § Component Patterns` (verified by diff, three-way).
- **FC-A11Y does not conflict with the deterministic focus rule.** `HFC1051` flags `tabindex="-1"` on an *interactive* element; the spine's target is the item's non-interactive status cell, and route headings use FrontComposer's own `HeadingTabIndex` parameter.
- **`role="group"` with an `aria-label` already ships** (`AgentsOverview.razor:83`), so the queue's replacement filter toggles have a working precedent in-repo — though `aria-pressed` has zero hits, so the toggles themselves are new build.

## Regression against 2026-09-09

Prior findings from `.working/validation-2026-09-09-pre-update/review-implementation-readiness.md § Findings` (26 items: 6 high, 14 medium, 6 low), plus the deferred-dependency rows it added.

| # | Prior finding | Class | Basis |
|---|---|---|---|
| 1 | [high] Conversations seam stated only in the spine; epics and register disagree | **partial** | Spine applied in full; `epics.md:1841` still "No new external seam" and register `ConsumingStories` still `TBD`. |
| 2 | [high] `agent-response-marker` has no seam or provenance source | **partial** | Spine widened `EXT-CONV-UI-1` to a decoration slot plus an Agents-side accessor; the register still specifies Conversations rendering from message metadata — an incompatible design, unreconciled. |
| 3 | [high] Status glyphs requested although they exist; curated list wrong | **resolved** | Re-verified against `FcFluentIcons.cs`: 12 `TryCreate` names and 13 Size16 factories match the spine character for character; all seven roles bind to real methods. |
| 4 | [high] `IBadgeCountService` cannot carry the pending count | **resolved** | Dropped for V1; count domain-rendered as a whole string; suppression confirmed achievable (no `ActionQueue` projection → `count > 0` gate closed). |
| 5 | [high] `pending in another session` has no data source | **partial** | Derived from authoritative pending with no local lock and buildable today via the same-session interim; the accepted-by session/actor reference is still absent, correctly listed at owner 5.7. |
| 6 | [high] **Call hexa** must know readiness; no seam supplies it | **partial** | `GetCallabilityAsync(tenant, conversation)` added to the spine's seam table; absent from the register's `RequiredArtifact` and from `IConversationAgentCallGateway`. |
| 7 | [medium] Mandatory FC-TBL components require a `ViewKey` | **partial** | `ViewKey`s assigned per grid; but nobody owns reading/applying `DataGridNavigationState.ViewStates[ViewKey]`, and replacing the chips with Agents-owned toggles left `FcFilterResetButton` clearing state nothing uses. |
| 8 | [medium] `Appearance.Accent` does not exist in v5 | **resolved** | `ButtonAppearance` = Default/Outline/Primary/Subtle/Transparent confirmed at the MCP build; zero `Appearance.Accent` in the spines and zero in `src/`. |
| 9 | [medium] `FcAggregateDetailPage` has no `Heading`/`HeadingTabIndex`/`PageTitle` | **resolved** | Verified: 18 parameters, none heading-related; spine prescribes `FcPageHeader` in every state slot plus a localized `BackLinkLabel`. |
| 10 | [medium] Nearing-expiry threshold not in the spines | **resolved** | In both spines with the 15-minute floor, computed from `ExpiresAt`, the configured window, and server read time. |
| 11 | [medium] Regeneration ceiling default and range undocumented | **partial** | Numbers stated (default 3, range 1–10) and flagged for Product; still no contract field and no Story 5.2/7.3 AC. |
| 12 | [medium] Segregation-of-duties needs the viewer's Party id | **resolved** | `CanCurrentUserApproveSelectedVersion` with its reason, owner 7.4, in the contracts table; the UI is forbidden from comparing raw ids. |
| 13 | [medium] Disclosure category per row versus per policy | **resolved** | `ApproverPolicySource.DisclosureCategory` added to contracts-that-must-grow at owner 5.4. |
| 14 | [medium] Tenant budget currency has no source before 8.4 | **resolved** | Interim rule stated: currency renders as entered with no mismatch state; the tenant budget currency on Cost controls is authoritative thereafter. |
| 15 | [medium] Live-region ownership contradicts shipped components, names no carrier | **partial** | Rule rewritten with `AgentsPageStatusRegion` as the named carrier and an explicit list of which Fc nodes may speak; the correction row still names five badges where 15 `role="status"` sites exist across 13 files. |
| 16 | [medium] SignalR nudge has no seam | **partial** | Bound to `IProjectionChangeDetailNotifier`, which exists and is DI-registered; `ARCHITECTURE-SPINE.md` still has zero polling/nudge/`catching up` text and the two shipped paths still diverge. |
| 17 | [medium] The single `not available` state has no copy key | **partial** | Two keys named and the two-key rule stated; neither key exists in `AgentsResources.resx`/`.fr.resx`, the forbidden per-cause keys remain, and "the resource file wins" contradicts the rule. |
| 18 | [medium] Story 6.2 does not own a UI read model; Operational status extras uncontracted | **unresolved** | `epics.md:1585-1607` unchanged; deferred to Epics with an owner and marked at the point of use, but nothing has landed. |
| 19 | [medium] Audit evidence **list** route unspecified | **resolved** | `/agents/audit` declared an id-entry surface, not a grid; FC-TBL set explicitly excluded; the cost/governance evidence lists moved to the status and audit routes. |
| 20 | [medium] Provider disable blast radius has no query | **partial** | Named ("Story 5.5 readiness result queried by Provider and model") but absent from Story 5.5's AC and from Architecture, and 5.3 ships first with no interim. |
| 21 | [low] `aria-disabled` → `DisabledFocusable` never named | **resolved** | Named and generalised spine-wide; verified as a real `FluentButton` parameter in v5. |
| 22 | [low] Parity gate name drift | **resolved** | Spine now says "`AgentsResourcesParityTests` (today `LocalizationResourceTests` …; rename or add under Story 8.6)"; the file exists at that path. |
| 23 | [low] Shipped drift overridden without naming the absorbing story | **resolved** | § Shipped-code corrections added; all eleven rows verified real today. (Its scope defects are new findings, not a regression of this one.) |
| 24 | [low] Fluent MCP documents a different build than the pin | **resolved** | Spine states re-verification at the pin; every name used here was confirmed present at the MCP build. |
| 25 | [low] UJ-1 step 2 Party identity has no field or command | **resolved** | Read-only status with `HasPartyIdentity`/`MissingPartyIdentity` blocker; platform provisioning named as the resolution path. |
| 26 | [low] `PricingVersion` assignment unspecified | **resolved** | Server-assigned, read-only, pinned column; confirmation renders `next pricing version {n}`. |

**Prior-finding counts: 15 resolved, 10 partial, 0 regressed, 1 unresolved (26 total).**

**This review: 25 findings — 0 critical, 7 high, 11 medium, 7 low.**
