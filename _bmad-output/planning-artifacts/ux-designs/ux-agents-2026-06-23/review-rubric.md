# Spine Pair Review — agents

Reviewer: rubric walker. Run date: 2026-09-09 (third run of the day, after the FR-33 Update, the prose pass, and the `Agents.ComplianceInspector` revert). Inputs: `DESIGN.md` and `EXPERIENCE.md` (both `status: final`, `updated: 2026-09-09`), the archived predecessor (`.working/validation-2026-09-09-post-fr33/review-rubric.md`), `reconcile-validation-2026-09-09-2.md` and its two predecessors, `design-md-spec.md` and the five calibration examples, `hexalith-ux-instructions.md` and `hexalith-llm-instructions.md`, every `sources:` path in both frontmatters (17 in DESIGN, 20 in EXPERIENCE — all resolve on disk), plus `prd.md`, `ARCHITECTURE-SPINE.md` (AD-7, AD-8, AD-10, AD-12, AD-13, AD-15, AD-30), `epics.md`, `launch-readiness-register.md`, `FcFluentIcons.cs`, and `AgentsFrontComposerRegistration.cs` for name and constant verification. `imports/` is empty; `mockups/` and `wireframes/` do not exist, by the recorded decision.

## Overall verdict

The FR-33 reconciliation landed, and it landed well where it looked: § Authorization roles binds all six PRD roles to the five AD-30 policy constants, every `RequiredPolicy` cell is re-derived from an FR-33 row, the cost-cap lower-only rule and the Platform-only override are stated with their validation and copy consequences, per-tenant Provider enablement is a first-class column, the Content Safety split is a two-tier surface with a rendered Security approval stage, and the kill switch and the per-Conversation block have homes, states, and controls. The `Agents.ComplianceInspector` invention is gone and the label-versus-identifier convention is stated against AD-30 with the `ConversationOwner` precedent; the § Shipped-code corrections row now records the one real divergence, `Agents.PlatformOperator` unregistered, which I verified against the shipped file (four tenant-scoped constants declared, no platform one). Mechanically the pair is as clean as its predecessor: 22 component names identical **and identically ordered** across three places, all 19 `{path.to.token}` references resolving, all 37 source paths resolving, every internal and external `§` reference resolving, and the `FcFluentIcons` inventory matching its source.

But the Update repeated its predecessor's failure mode at a smaller radius. It reconciled the **FR-33 table** and stopped there, while the same 2026-09-09 PRD revision it cites carries OQ-24 through OQ-30, a rewritten FR-3, FR-18, FR-28 and FR-30, and a new FR-4 data-handling obligation — none of it read. The result is two operator-facing statements that contradict their source outright (the kill-switch confirmation tells the operator that `Approved` proposals "complete on their own terms" when FR-28 and OQ-27 hold them without posting; export and legal-hold release lose the second-party approval FR-30 requires while deletion gained it), plus the exact stale-artifact pattern this run was sent to hunt: three counts left behind by their own sets ("the four FR-18 exits" against six, "the six-part Provider eligibility set" against seven, "this four-name list" against two names), a family roster of ten that omits the two families the Update itself declared, and a seam bullet still asserting the ownership the Update replaced two paragraphs earlier.

Verdicts: token completeness, visual reference coverage, and shape fit **strong**; flow coverage, component coverage, state coverage, and bloat **adequate**; inheritance discipline **thin**. `status: final` is not supportable at this revision.

## 1. Flow coverage — adequate

Checked: UJ-1 to UJ-4 titles compared character-for-character against `prd.md:47,55,63,71` — verbatim. Each carries a named protagonist, numbered steps, a bolded **Climax**, a Resolution, and at least one failure path; UX-J5 and UX-J6 are labelled UX-derived and grounded in named FRs/NFRs/stories. Every story cited across the six flows exists in `epics.md`. The gap is not missing flows — it is that the Update re-gated the surfaces and left the flows describing the pre-FR-33 world.

### Findings

- **high** **UX-J5 has one protagonist performing three different FR-33 roles, and no step names a role.** Priya opens Launch readiness (`Agents.Operator`), publishes a Content Safety Policy version (step 3 — Platform Operator with Security approval), configures caps and rate limits and applies an audited override (step 4 — Platform or Release Operator sets, Platform Operator only overrides), and inspects Audit governance (step 5 — `Agents.AuditOperator`) (`EXPERIENCE.md:672–681`). Step 3 does not mention the Security approval stage the Update added to `content-safety-policy-editor`; step 4 does not mention that a Tenant Agent Administrator may only lower. A story-dev calibrating from the only end-to-end governance flow will build a single-role surface set, which is precisely what § Authorization roles forbids. *Fix:* name Priya's role in the header, split the steps that are not hers into a named second actor or state the role change inline, and add the Security approval stage and the lower-only refusal as visible beats.
- **medium** **UJ-1 step 3 carries the stale "six-part eligibility set".** "She selects a Provider/model that passes the six-part eligibility set" (`EXPERIENCE.md:606`), mirroring the `agent-config-form` row. `prd.md:209` now lists seven elements, adding *enabled for this tenant* (FR-33) and *data-handling record complete with its current `DataHandlingVersion` accepted for this tenant* (FR-4). Both are new blockers with UI consequences. *Fix:* restate the set from `prd.md:209` in both places and stop numbering it, or number it seven.
- **medium** **No flow reaches the `DataHandlingVersion` acceptance step**, which FR-33 assigns to the Tenant Agent Administrator "before first activation and on each change" with a 30-day grace after which the model cannot be called (`prd.md:199`, `461`, OQ-29 at `999`). It belongs in UJ-1 between Provider selection and activation. See §7 for the surface-level filing.

## 2. Token completeness — strong

Checked: every frontmatter key in `DESIGN.md` (8 colors, 5 typography roles, one `rounded` note, 6 spacing steps, 22 component objects) against every brace reference in both files. 19 distinct `{path.to.token}` references; all resolve by name (script-verified against the parsed YAML). The eleven non-dotted braces (`{count}`, `{n}`, `{mode}`, `{expiresAt}`, `{scope}`, `{resetAt}`, `{dependency}`, `{party}`, `{model}`, `{duration}`, `{AgentInteractionId}`) are localization or route placeholders with no dotted path and cannot collide with a resolver. No legacy v4/FAST token name appears anywhere except as a named prohibition (`DESIGN.md § Do's and Don'ts`) and as the zero-hit inventory statement in § Shipped-code corrections. No theme redefinition.

The predecessor's one finding is closed: `{spacing.1}`–`{spacing.8}` now inherit by name — `--spacingHorizontal`/`--spacingVerticalXS` through `XXL` on the Fluent 2 ramp, with `FluentStack` `HorizontalGap`/`VerticalGap` preferred over a hand-authored `gap`, and the hand-authored-CSS carve-out cited to `hexalith-ux-instructions.md § No theme redefinition` (`DESIGN.md § Layout & Spacing`). Inheritance remains bindable: the package pin, the `BadgeColor` enum member per role, the `BadgeAppearance` binding, the glyph per role, and WCAG 2.2 AA targets in Light, Dark, and forced-colors with a per-route proof lane are all stated together.

No findings.

## 3. Component coverage — adequate

Checked mechanically: the component-name sets from `DESIGN.md` `components:` frontmatter, from the `###` headings in `DESIGN.md § Components`, and from the bold row labels in `EXPERIENCE.md § Component Patterns` are **identical and identically ordered**, 22 each. The three-way parity rule the spines declare holds under test. Every component has a real visual spec and a real behavioral row; no one-word descriptions. Every other backticked name is an inherited Fluent or FrontComposer primitive, an ARIA attribute, a contract type, or a projection name, correctly without a row.

What has slipped is not coverage but agreement: two visual specs were not updated when their behavioral rows were re-gated, so `DESIGN.md` now describes a component the FR-33 rules forbid.

### Findings

- **high** **`provider-catalog-grid`'s visual spec contradicts its own scoped-read rule.** The frontmatter pins `'Provider name, model, enabled state, configured state, readiness, pricing version, CapabilityVersion'` and the `###` section repeats the same pinned set (`DESIGN.md`, `components.provider-catalog-grid` and § Components). § Provider catalog rules now states that a tenant reader "never sees configured state or `ConfigurationReferenceId`" and that a secret-derived reason collapses to one undifferentiated `Blocked`, and that **per-tenant enablement is a first-class column and filter** (`EXPERIENCE.md:270`). So the pinned column list pins a Platform-Operator-only column for every reader and omits the column the Update made first-class. The section's stated visual consequence is still only the `read-only (no mutation policy)` variant, which is the *editor* delta, not the column delta. *Fix:* split the pinned set into the tenant-reader set and the Platform-Operator set, add per-tenant enablement, and add a `tenant-scoped read` variant beside `read-only (no mutation policy)`.
- **medium** **`cost-control-editor`'s two DESIGN statements disagree with each other.** § Layout & Spacing's per-surface table gives Cost controls four accordion items — "Lower-only editor; Operator editor; Overrides; History" — while the `###` section describes a single editor ("`FluentNumberInput` fields with explicit units … The audited override is a separate item") and lists variants `Submitted`, `AuthoritativePending`, `ProjectionConfirmed`, `awaiting projection`, `override active` with no tenant/platform split. The EXPERIENCE row is explicit that there are **two authorization tiers on one surface** and that the override control is *absent*, not disabled, for every role but Platform Operator. *Fix:* make the `###` section carry the two editors and add the absent-override and lower-only-refusal variants.
- **low** `audit-governance-panel`'s visual spec lists the deletion blockers as "a matching legal hold is active" plus the `EXT-SECRETS-1` `authority unresolved` treatment, but not the new A-12 second-person approval, which the EXPERIENCE row makes a third `DisabledFocusable` cause with its own reason string. *Fix:* add it to the variant list.

## 4. State coverage — adequate

Checked: walked the 15 IA rows against empty, cold-load, focus, error, offline, permission-denied, stale, blocked, pending, suspended, and concurrency. The Update did real work here — § Agent call grew from 17 rows to 21 with `no eligible approver`, `kill switch active`, `conversation blocked` and `rate limited` all typed and all listed in § Contracts that must grow; approval-freezes-expiry is carried on four rows; the `PostingFailed` exhaustion row now has exits; `Abandoned` carries four typed reasons; the retry bound is A-7 Architecture-owned; the four pre-post checks are enumerated in the `ConversationPosting` family. Five of the predecessor's six state findings are closed. What is missing is the state vocabulary the *rest* of the 2026-09-09 PRD introduced alongside the kill switch.

### Findings

- **critical** **The kill-switch confirmation copy contradicts FR-28 and OQ-27 on the one point an operator most needs.** § Launch readiness rules has the confirmation state "`Approved` and `PostingPending` proposals complete or fail on their own terms" (`EXPERIENCE.md:317`). `prd.md:698` (FR-28) and OQ-27 (`997`) say the opposite for half of that pair: "`Approved` proposals are held in `Approved` **without starting a post** and without consuming a retry attempt, and a `PostingPending` attempt already in flight completes or fails on its own terms" — OQ-27 records this as a deliberate correction ("safety wins over 'complete on their own terms', a Spine AD-12 correction"), and AD-12 in the Architecture Spine now matches the PRD. The same sentence also drops "edited, or regenerated" from what awaiting proposals may not do, and omits the paused `PostingFailed` retry clock and `ExpiredWhileSuspended`. The identical defect sits in the `agent-config-form` Disable confirmation, which states only that "pending proposals stay resolvable" (`EXPERIENCE.md:206`), while `prd.md:176` says that while the Agent is `Disabled` or the tenant `Suspended` those proposals "accept reject and abandon only". A story-dev renders a confirmation that promises the operator an emergency control will let approved content still post. *Fix:* replace both confirmation bodies with the FR-28/`prd.md:176` semantics verbatim, add the paused retry clock and `ExpiredWhileSuspended` to § Proposal lifecycle, and state that approve, edit, and regenerate are all refused while either condition holds.
- **high** **`Suspended` and `Draft` have no state, no badge role, and no copy.** FR-3 as revised (`prd.md:171`) makes the public lifecycle `Draft` (provisioned, not yet valid for activation), `Active`, `Disabled`, with `Unknown` as the FR-23 sentinel, and makes **`Suspended` an additive FR-25 status reported while the kill switch is pulled "so an operator can tell a suspended tenant from a disabled Agent"**; AD-12 requires `agent-setup` and `launch-readiness` to expose it distinctly. § Agent readiness's nine states carry `disabled` and lifecycle `active` and neither of the other two; `DESIGN.md § Colors` assigns no role to either; § Contracts that must grow lists neither. The spine's own claim that "`hexa` is pre-provisioned once per tenant, so there is no not-yet-created state" is right about creation and wrong about `Draft`, which is exactly the pre-activation state `agent-config-form` always opens in. *Fix:* add `Draft` and `Suspended` to § Agent readiness with role, treatment, and localized label, and add `Suspended` to the `AgentReadinessStatus` growth row.
- **medium** **The launch-readiness blocker vocabulary stops at two of seven.** § Launch readiness rules names `UnretiredAssumption` and `ProhibitedCostControlPosture` and otherwise renders `BlockerCode` generically. FR-30 (`prd.md:621`) requires the surface to record `PayloadProtectionUnavailable`, `ProhibitedCostControlPosture`, `GateOutOfScope`, and `TriggerReviewOverdue`; FR-28's `RQ-1` list adds `OpenDecision` (`prd.md:689`) and OQ-24 adds `DependencyNotAvailable` (`994`). Grep for each across both spines returns zero except `ProhibitedCostControlPosture`. Under the spine's own rule that raw tokens never display, each is a missing resource key on a surface a Release Operator reads during the launch decision. `FR-34` and `EXT-PROTECTION-1` appear once, in the `audit-governance-panel` row, and never on Launch readiness. *Fix:* name the six blocker codes as rendered rows with their recovery owner, and add them to the architecture-token localization row.
- **medium** **`ExpiredWhileSuspended` and the paused retry clock are absent from § Proposal lifecycle.** The `Expired` row says expiry is rendered "on every read and every command, regardless of timer delivery" with no suspension carve-out, and the `PostingFailed` row states the 3-attempts-in-15-minutes bound with no pause. FR-28 (`prd.md:698`) and OQ-27 both make `ExpiresAt` keep running under the switch, record `ExpiredWhileSuspended`, exclude it from the SM-3 and SM-C5 denominators, and pause the retry window (AD-12 carries it as `PausedDuration`, so the *rendered* attempts-used-and-time-remaining figure the `ConversationPosting` confirmation promises is wrong under suspension). *Fix:* add the typed outcome to `Expired` and state the paused-window arithmetic beside the A-7 bound.
- **medium** **The block state machine's UI-visible fields are missing.** OQ-25 (`prd.md:995`) and AD-7 make the block versioned by `BlockVersion`, mirrored by an at-least-once outbox with a **visible `MirrorPending` flag**, and make a clear record `ReadmitPending` "rather than re-joining"; AD-7 adds that a clear is refused while the matching removal is sent but unacknowledged. Grep for `BlockVersion`, `MirrorPending`, `ReadmitPending` across both spines: zero. The Operational status block row therefore renders a set/clear control with no pending-mirror state, no post-clear state, and no refusal reason. *Fix:* add `MirrorPending` and `ReadmitPending` as rendered states beside the per-Conversation history row and give the refused-clear its own typed reason.
- **low** `kill switch active` is a per-tenant blocked-call reason but is absent from § Operational status rules' blocked-count list, which enumerates context policy, safety, cost cap, rate limit, capacity, `NoEligibleApprover`, and `RemovedInConversations`. *Fix:* add it, or state why suspension is counted as `Suspended` rather than as a blocked-call reason.

## 5. Visual reference coverage — strong

Checked: `imports/` exists and is empty; `mockups/` and `wireframes/` do not exist; `.working/` holds four archived validation runs. Grepped both spines for `mockup`, `wireframe`, `import`, `sketch`, `screenshot`, `.html`, `.png`, `.svg`, `figma`, `excalidraw`: **zero dangling references**, no orphans, no unspecific "see the mockup". The recorded no-visual-reference decision is now stated in **both** spines, and `DESIGN.md § Brand & Style` — the file a visual consumer opens first — carries it in full, dated 2026-09-09, against all 15 IA surfaces, with the spines-win-on-conflict rule extended to shipped code. That closes the substance of the predecessor's finding; only the two halves drifting apart remains.

### Findings

- **low** The same decision is dated differently in the two spines: `DESIGN.md § Brand & Style` says "(2026-08-02, reaffirmed 2026-09-09)" over "all 15 Information Architecture surfaces"; `EXPERIENCE.md:43` still says "(2026-08-02, reaffirmed 2026-09-08)" with no count; `.memlog.md:38` still says "all 14 IA surfaces" against a 15-row table. *Fix:* one wording, one date, one denominator, stated in `DESIGN.md` and pointed at from `EXPERIENCE.md`.

## 6. Bloat & overspecification — adequate

Checked: pixel specs against token coverage, source restatement, prose where a table works, sections no consumer would read, decorative narrative. The Update added roughly 1,400 words and every one of them is content — the § Authorization roles table, the two-tier cost and safety rules, four new call states, the four pre-post checks, the FR-18 exits, the compliance-inspection rules. No persona or FR text is restated; non-goals appear once; `DESIGN.md` remains tight and its editorial voice tied to decisions throughout. The user has declined the DESIGN-side behavioural leakage (~460 words), the EXPERIENCE duplication (~1000 words), the Component Patterns cell density, the § Inspiration cut, and the WCAG 2.2.1 argument, and I have not re-filed any of them; they are classified in the regression table only. What is left is one section that still carries no decision.

### Findings

- **low** `## FrontComposer Readiness` remains a pure cross-index: all 13 rows resolve to a rule stated elsewhere, and only four (`FcAggregateDetailPage` parameters, `IBadgeCountService` not used, `EXT-CONV-UI-1` blocking, `EXT-SECRETS-1` fail-closed) carry a constraint of their own. The Update added no row to it and updated one ("Five policy constants"), so it now also has to be maintained in step with § Authorization roles. *Fix (optional, unchanged from the predecessor):* fold the four constraint-bearing rows into their owning sections and drop the table.
- **low** The prose pass corrected the argue-don't-state sentences it was asked to; four survive because they were not on its list — "so the operator sees the consequence before the setting takes effect" (`response-mode-toggle`), "without joining the ten confirmation families" (§ Proposal editor rules), "would reintroduce the focus-loss defect this pattern exists to prevent" and "which is the v5 binding that emits `aria-disabled`" (`pending-command-indicator`). Not re-filed as a new defect; carried from the predecessor for the ledger.

## 7. Inheritance discipline — thin

Checked: all 37 `sources:` paths resolve on disk (17 DESIGN, 20 EXPERIENCE). UJ titles verbatim. Glossary consistent across spines and sources: `ProposedAgentReplyState` 10/10 against the contract; `AuditAvailabilityStatus` 4/4; `FcAggregateDetailState` 7/7; the freshness vocabulary identical in both spines; `Conversation Facilitator` used throughout with zero "Conversation owner" hits outside the sentences forbidding it; all six FR-33 role names used verbatim from `prd.md`. Component names identical across all sections in both files (§3). Every EXPERIENCE token reference resolves to a DESIGN token by name. The `FcFluentIcons` inventory matches `FcFluentIcons.cs` — 12 `TryCreate` names exactly, and all thirteen listed Size16 factories exist with all seven status roles bound. The policy-constant model is now correct against AD-30 and the shipped registration, verified member by member.

The mechanics are strong, and the FR-33 table itself is faithfully inherited. What is thin is the *boundary* of the inheritance: the Update read one section of a revision that rewrote seven, and it left three of its own counts pointing at supersedcd sets.

### Findings

- **critical** **Export and legal-hold release lost the second-party approval FR-30 and FR-33 require, while deletion gained it.** The IA table gates "Legal hold and export" on `Agents.AuditOperator` alone and gives only deletion the two-person rule (`EXPERIENCE.md:88`); § Audit governance rules likewise gates export on justification plus `EXT-SECRETS-1` availability and gates a hold release on nothing but "a hold release names the records it exposes". `prd.md:622` (FR-30): "**Export requires prior second-party approval and hold release a separate approver (FR-33)**", and FR-33's own rows are explicit — release legal hold is "Compliance Inspector, **with the audited approval of a second Compliance Inspector or the Platform Operator**"; request export is "Compliance Inspector, **with prior approval by a second party on the FR-24 terms; never post hoc**". A story-dev implements single-person export of protected Conversation-derived content and single-person hold release, both of which the PRD forbids and both of which are exactly the class of control the Update just added for deletion. *Fix:* give `ExportRequest` and a new `LegalHoldRelease` the same request/approve/execute staging `DeletionRequest` has, add the approver identity and the "never post hoc" rule to their confirmation contents, and correct the IA cell.
- **high** **The compliance-inspection approver contradicts OQ-21 and names the party OQ-21 excludes.** § Audit evidence rules says an inspection "carries a recorded justification and **either second-party approval or a declared post-hoc review by the Tenant Agent Administrator**" (`EXPERIENCE.md:307`). OQ-21 (`prd.md:991`) and the FR-33 row (`479`) say the approver is "a second party who is **neither the Inspector nor a subject of the case** — the Platform Operator or a second Compliance Inspector **where the case concerns the Tenant Agent Administrator**", that **post-hoc review is allowed only for single-proposal or single-Conversation scopes within the A-19 window**, and that "**the Platform Operator approves any inspection wider than one Conversation**". The spine's rule makes the Administrator the default post-hoc reviewer with no scope bound — the one person the PRD removes from the approval path when the case is about them, and the one bound that keeps a compliance inspection from becoming a general read of protected content. *Fix:* state the three-part rule (approver outside the subject set; Platform Operator for any scope wider than one Conversation; post-hoc only for single-proposal or single-Conversation within the A-19 window) and render the scope and approver identity in the inspection's justification-entry confirmation.
- **high** **The two families the Update declared do not exist in the family roster, and "ten" is now a stale count of the Update's own making.** § Authorization roles says the per-tenant kill switch and the removal/block/re-admit control are each "`high-impact-confirmation` families with a required justification" (`EXPERIENCE.md:110`), and § Launch readiness rules and § Operational status rules repeat it. § Confirmation contents by family carries **ten** rows and neither of them; § High-risk pending commands says "the ten families"; § Interaction Primitives and § List, detail and deep-link surfaces both say "the ten"; § Audit governance rules lists the justification-required families as legal hold, export, deletion, `PolicyPublication`, and `TenantBudgetUpdate`, again without either. AD-12 already owns the vocabulary — `TenantKillSwitch` is a lock-bearing family and the block/clear commands sit under `AgentSetupMutation` — so the names exist and the spine simply does not use them. A story-dev implementing the kill switch has a family with no required contents and a lock rule that does not mention it. *Fix:* add `TenantKillSwitch` and the block/clear rows to § Confirmation contents by family with their required contents (the FR-28 semantics, the named trigger condition, the abandon-and-no-rejoin statement, the justification), name them in § High-risk pending commands, and correct every "ten" to twelve.
- **high** **`DataHandlingVersion` acceptance has no surface anywhere in the pair.** Grep for `DataHandling` or "data-handling" across both spines: zero hits. FR-4 (`prd.md:199`), the FR-33 row (`460`), the selection-eligibility set (`209`), the Agent Administrator glossary entry (`83`), and OQ-29 (`999`) together make this a Tenant Agent Administrator action with a rendered record (retention term, training-use status, processing region, contractual reference), a version, an acceptance before first activation and on each change, a **30-day grace after which the model cannot be called for that tenant**, an activation blocker when unaccepted, and an Audit Evidence field naming the record version per attempt. None of it reaches `agent-config-form`, `provider-catalog-grid`, § Agent readiness, § Audit evidence rules, or § Voice and Tone. *Fix:* add the data-handling record as a read-only block on the Provider selection with an accept control and its own confirmation family, add `DataHandlingNotAccepted` (and the grace-window warning) as an activation blocker and readiness reason, and add the record version to the audit field list.
- **high** **§ Accessibility Floor contradicts itself about which routes use `FcAggregateDetailPage`.** One bullet says the domain renders `FcPageHeader` in five state slots "on the **eight Constrained detail routes**"; four bullets later, "the routes bound to no `Fc*` page component — Agents overview, Operational status, Launch readiness, the Audit evidence list, and **the four form surfaces (Conversation context policy, Content safety, Cost controls, Audit governance)** — are hand-authored". Those four form surfaces are four of the eight Constrained routes, so the two sentences assign them to opposite mechanisms. At most four Constrained routes plausibly use `FcAggregateDetailPage` (`hexa` configuration, which `agent-config-form` names explicitly; Approver policy; Proposal detail; Audit evidence detail), and the spine never says which. This is the predecessor's medium reopened in a new shape: the mechanism bullet was added without correcting the count it contradicts. *Fix:* list the routes bound to `FcAggregateDetailPage` by name and drop the "eight Constrained detail routes" phrasing.
- **medium** **"The four FR-18 exits" is short by two, and the no-retry rule for a safety-verdict failure is missing.** § Proposal editor action rail and § Proposal lifecycle both commit to four exits from an exhausted `PostingFailed`. `prd.md:439` gives those four and then adds: "Two further exits come from the table: the `MessageId` lookup finds the post (`Posted`, `LateConfirmed`), or a removal or block is detected (`Abandoned`, `RemovedInConversations`)." The same paragraph states that "a `PostingFailed` proposal whose reason is a safety verdict **is not retryable and accepts abandon only**", which the action rail's `retries remaining` and `retries exhausted` rows do not carry, and OQ-26 (`996`) makes the `MessageId` lookup a precondition of *every* abandon exit. `LateConfirmed` has zero hits in the pair. *Fix:* state six exits, add the `LateConfirmed` display-only outcome, and add a safety-verdict variant to the two `PostingFailed` rail rows in which Retry posting is absent.
- **medium** **Administrative abandon is stated unconditionally where FR-33 conditions it.** The IA cell and the rail row both give the Tenant Agent Administrator an audited abandon of `PostingFailed` needing no Conversation read access, with no precondition. The FR-33 row reads "Abandon a `PostingFailed` proposal **whose Source Conversation is gone or inaccessible** (FR-18)", and the FR-18 transition table (`prd.md:431`) repeats the condition. As written the spine grants a tenant administrator a unilateral abandon of any failed proposal. *Fix:* carry the condition into both places and give the unmet-condition case its typed refusal reason.
- **medium** **The regeneration-ceiling deferral is still stale and still quotes its source wrongly.** Unchanged from the predecessor: `agent-config-form` says the ceiling is "flagged for Product sign-off because FR-32 states only 'a documented default and a valid range'" (`EXPERIENCE.md:206`). `prd.md:717` states "a default of **3** and a valid range of **1 through 10** (FR-16)", `393` repeats it, and A-6 owns the numbers with a retirement condition. The spine's numbers are right; its stated reason for flagging them is false against its own source. This is the one predecessor finding on which nothing moved. *Fix:* cite FR-32 and A-6 and drop the "FR-32 states only" clause.
- **medium** **The seam bullet still asserts the ownership the Update replaced.** § Conversation Integration Seam: "Removal of `hexa` is a **Conversation-owned action**. Agents reacts by blocking further calls in that Conversation and moving every non-terminal proposal to `Abandoned` with reason **`Agent removed from Conversation`**" (`EXPERIENCE.md:104`). Six lines later § Information Architecture makes removal, block, and re-admit an **Agents-owned** control on Operational status (`110`), and § Proposal lifecycle types the reason as `RemovedInConversations`. AD-7 and OQ-16 give a three-part membership step that reads participant state, records the external removal, sets the block, and rejects — none of which the bullet carries. *Fix:* rewrite the bullet to the AD-7/OQ-16 three-part step with the typed reason, and point at the Operational status control for set/clear.
- **medium** **The block-clear authority is stated as symmetric with the set authority; FR-33 makes it asymmetric.** § Operational status rules: "authority is the Tenant Agent Administrator or Conversation Facilitator (FR-2, A-9), each transition is a `high-impact-confirmation`". FR-33 has *two* rows: set is Administrator or Facilitator; **clear** is "the authority that set the block, evaluated at clear time, or the Tenant Agent Administrator; **a Conversation Facilitator cannot clear an Administrator's block**. An `ExternallyRemoved` record is cleared by the Tenant Agent Administrator or any current Conversation Facilitator" (`prd.md:459`, OQ-25 at `995`, AD-7). Three distinct clear authorities collapse to one in the spine, so the re-admit control renders for a Facilitator who may not use it. *Fix:* split set and clear into two rows with the three clear cases and their refusal reasons.
- **medium** **The spine's own statement of what it reconciled to is short by seven open questions.** The header names "the 2026-09-09 revision (FR-33, OQ-20 to OQ-23, A-9 to A-13, A-17, and the in-place amendments to OQ-3, OQ-6, OQ-9, OQ-11, OQ-14, OQ-16 and OQ-18)". That same `prd.md` revision carries **OQ-24 through OQ-30**, all marked Resolved or amended 2026-09-09, and grep returns zero hits for every one of them in both spines. OQ-25 (block state machine), OQ-26 (`PostingFailed` exits and `LateConfirmed`), OQ-27 (kill-switch semantics), and OQ-29 (data handling) each produced a finding above; OQ-24's blocker vocabulary produced another in §4. This is the predecessor's critical recurring one revision later at a smaller radius: a targeted reconciliation of one FR against a PRD that moved as a whole. *Fix:* run the reconciliation over OQ-24 to OQ-30 and FR-3/FR-4/FR-30 as revised, and state the revision by commit or content hash rather than by a hand-listed row set that goes stale the moment the PRD moves.
- **medium** **The architecture-token localization row is nine tokens against a vocabulary the Update roughly tripled.** § Voice and Tone still lists `Ready`, `Callable`, `Degraded`, `Blocked`, `InsufficientEvidence`, `Stale`, `Pass`, `Block`, `Unknown`. The Update introduced or newly surfaced `NoEligibleApprover`, `KillSwitchActive`, `RemovedInConversations`, `SourceConversationUnavailable`, `UnretiredAssumption`, `ProhibitedCostControlPosture`, the five cost-control postures, and (per §4) `Suspended`, `Draft`, `ExpiredWhileSuspended`, `LateConfirmed`, and the six launch-readiness blocker codes — every one of which the spine's own "raw tokens never display" rule turns into a required resource key with EN/FR parity, enforced by a gate that fails on a missing key. This was a low in the predecessor run; the surface area has grown enough to matter. *Fix:* make the row a pointer to a per-enum inventory rather than a hand-maintained list, or list them.
- **low** **"This four-name list" names two components.** § Grid rules: "Required components (Agents-owned set) | `FcFilterEmptyState` and `FcExpandInRowDetail`. **This four-name list** was previously attributed to FC-TBL". § Accessibility Floor's closed speaker list correctly says two. Verified in `git show HEAD` that the phrasing predates the FR-33 Update, so this is an inherited defect the earlier runs missed rather than a regression — but it is the same species, a count left behind by its own set. *Fix:* "This list".
- **low** The header's pointer to "the current reconciliation, dispositions, and deferred items" names `reconcile-validation-2026-09-09.md`, while `reconcile-validation-2026-09-09-2.md` is the current one and is listed in both frontmatters. *Fix:* point at `-2` and demote the other to the predecessor slot.
- **low** § Key Flows opens "UJ-1 to UJ-4 mirror the PRD of 2026-09-08" against a header claiming the 2026-09-09 revision. The titles are in fact verbatim against the current `prd.md`, so the sentence is a stale date rather than a stale claim. *Fix:* drop the date or match the header.
- **low** `DESIGN.md § Brand & Style`'s typed-16px inventory lists thirteen factories; `FcFluentIcons.cs` declares fourteen, the extra being `Play16`. The spine's characterisation is still accurate — it lists the factories "not reachable through `TryCreate`", and `Play16` *is* reachable as `Regular.Size16.Play`, which the spine lists in the string contract. Recorded so the next verifier does not read the mismatch as drift. No fix required.

## 8. Shape fit — strong

Checked against `design-md-spec.md` and the five calibration examples. `DESIGN.md` carries all eight canonical body sections in canonical order with nothing interleaved: Brand & Style → Colors → Typography → Layout & Spacing → Elevation & Depth → Shapes → Components → Do's and Don'ts. Frontmatter carries `name`, `description`, `colors`, `typography`, `rounded`, `spacing`, `components` plus the workflow keys, with the `note`-object convention the spec permits for inherited systems.

`EXPERIENCE.md` carries all eight required defaults — Foundation, Information Architecture, Voice and Tone, Component Patterns, State Patterns, Interaction Primitives, Accessibility Floor, Key Flows — with nothing dropped, and both required-when-applicable sections present and genuinely triggered: **Inspiration & Anti-patterns** (the addendum names four reference products, and the section carries two lifts and three rejects) and **Responsive & Platform** (four breakpoints, the 320 CSS px reflow contract, per-grid surviving column sets).

Four invented sections, all earning their place. **Authorization roles** is the Update's addition and is the strongest of them: it is the only place the six PRD roles, the five AD-30 constants, the two non-role authorities, and the fail-closed and tenant-scoping rules are stated once, and every `RequiredPolicy` cell in the IA table derives from it. Placing it before Information Architecture is right, because the IA table cannot be read without it. **Conversation Integration Seam** remains the only home for `EXT-CONV-UI-1`, the three artifact kinds, and the FR-31 untrusted-data rule. **Known gaps** is the right shape for a spine deliberately ahead of its contracts, and every row names an owning story. **FrontComposer Readiness** is the weakest (see §6) but short and correctly pointed.

Repo law: compliant throughout. FrontComposer plus Fluent UI Blazor v5 only, reuse over hand-rolling asserted against the two shipped violations, no theme redefinition, no legacy v4/FAST token except as a named prohibition and a zero-hit inventory, and the two-or-more-sibling-titled-sections `FluentAccordion` rule carried with a per-surface primary-region table and the "primary content never behind an accordion interaction" clause. One gap in that last rule:

### Findings

- **medium** **The per-surface accordion table has no row for `hexa` configuration or Approver policy**, the two Constrained routes most obviously carrying multiple sibling titled sections. `DESIGN.md § Layout & Spacing` lists ten surfaces; the IA table has 15. The omission matters because § Shipped-code corrections cites this very rule against `AgentConfiguration.razor`, which "renders eleven `<section>` with ten sibling `<h2>` and no `FluentAccordion`", with absorbing stories 5.2 and 5.7 — so the correction points at a rule that never says which of the eleven sections is the primary region and which become accordion items. Proposal queue and the Audit evidence list are also absent, though as a grid and a single-field id-entry form both are defensibly single-region. *Fix:* add rows for `hexa` configuration and Approver policy, and state once that a surface absent from the table has one content region and needs no accordion.

## Regression against the 2026-09-09 post-FR33 run

Predecessor: `.working/validation-2026-09-09-post-fr33/review-rubric.md` — 19 findings (1 critical, 5 high, 8 medium, 5 low). Every one is classified below against the current spine text, not against any disposition claim.

| # | Prior finding (section · severity) | Classification | Basis |
|---|---|---|---|
| 1 | FR-28 per-tenant kill switch has no surface, state, copy, or flow (§1 high) | **resolved** | Carried on Launch readiness with the FR-33 gating (`Agents.PlatformOperator`, or `Agents.Operator` on the recorded trigger conditions), a `kill switch active` call state with `KillSwitchActive` in § Contracts that must grow, an accordion item in the DESIGN layout table, and a confirmation. Its *contents* are wrong against FR-28 — filed fresh as the §4 critical, not as this finding surviving. |
| 2 | OQ-16 inverts the removal ownership the seam pins (§1 high) | **partial** | The control landed: block/re-admit on Operational status beside the safety-history row, a `conversation blocked` call state, `RemovedInConversations` in the contract-growth row, `Abandoned` reason typed. The seam bullet still says removal is Conversation-owned with the untyped reason (§7 medium), the clear asymmetry is missing (§7 medium), and `BlockVersion`/`MirrorPending`/`ReadmitPending` are absent (§4 medium). |
| 3 | FR-7 `NoEligibleApprover` has no state; SoD predicate generic (§1 medium) | **resolved** | `no eligible approver` is a § Agent call row with its pre-Provider timing, recovery, and SM-C4 counting; § Operational status rules counts it; § Approver policy rules carries FR-7's two-part configuration predicate verbatim, the Facilitator-only validity, and the per-half error copy. |
| 4 | `spacing` given as literal px with no Fluent equivalence (§2 low) | **resolved** | § Layout & Spacing maps `{spacing.1}`–`{spacing.8}` onto `--spacingHorizontal`/`--spacingVerticalXS`…`XXL` and prefers `FluentStack` `HorizontalGap`/`VerticalGap`, citing the law's hand-authored-CSS carve-out. |
| 5 | Approval-freezes-expiry not carried; `Expired` rule contradicts FR-18 (§4 high) | **resolved** | `Expired` is scoped to "while `Pending`, `Edited`, or `Regenerated`" and states the freeze; `Approved`, `PostingPending`, and `PostingFailed` each say expiry no longer applies. |
| 6 | Exits from an exhausted `PostingFailed` contradict FR-18 (§4 high) | **partial** | Four exits, administrative abandon and administrative retry, "no Conversation read access required", and the retention-clock statement all landed. FR-18's two further exits, `LateConfirmed`, the safety-verdict no-retry rule, and the "Source Conversation gone" condition on administrative abandon did not (§7 mediums). |
| 7 | Four FullWidth routes have no page-component binding (§4 medium) | **partial** | A mechanism bullet was added naming all eight hand-authored routes and their obligations — but it contradicts the "eight Constrained detail routes" sentence four bullets above, so the binding is now stated twice, incompatibly (§7 high). |
| 8 | `Abandoned` reasons incomplete and untyped (§4 medium) | **resolved** | Four typed values with localized labels, the SM-3 denominator exclusion, and the separate Operational status count all present. |
| 9 | Posting-retry bound specified as a tenant configuration field (§4 medium) | **resolved** | "3 attempts within 15 minutes of the first failure (A-7, Architecture-owned, not tenant-configurable)", rendered read-only in the confirmation with attempts-used and time-remaining, and removed from `agent-config-form`'s inventory. Window expiry is a distinct exhaustion reason. |
| 10 | Pre-post re-validation reduced to its safety third (§4 medium) | **resolved** | All four FR-18 checks are named individually in the `ConversationPosting` confirmation row, each with its own typed `PostingFailed` reason. |
| 11 | No-visual-reference decision stale-dated and absent from DESIGN (§5 low) | **partial** | `DESIGN.md § Brand & Style` now carries it in full, dated 2026-09-09, over 15 surfaces. `EXPERIENCE.md:43` still reads 2026-09-08 and the memlog still says 14 (§5 low). |
| 12 | Eight DESIGN component sections carry EXPERIENCE-owned behavior (§6 medium) | **unchanged by decision** | Declined by the user (~460 words of behavioural leakage in DESIGN component sections). `provider-catalog-grid` was trimmed to a pointer; the rest stand. Not re-filed. |
| 13 | § FrontComposer Readiness is a pure cross-index (§6 low) | **unresolved** | Unchanged except the "Four→Five policy constants" correction. Re-filed at low. |
| 14 | Argue-don't-state sentences survive in EXPERIENCE (§6 low) | **unchanged by decision** | The prose lens fixed its listed set; these four were not on it and the residue was declined. Not re-filed as new. |
| 15 | FR-33 absent; the authorization model contradicts it on six rows (§7 **critical**) | **resolved** | § Authorization roles binds all six roles to the five AD-30 constants with scope and resolver; every one of the six contradicting rows is corrected — cost caps split with the tenant lower-only rule and its typed refusal, the override Platform-only with the control absent for every other role, Content Safety split into platform publication with a rendered Security approval stage and a tenant stricter-only delta, per-tenant Provider enablement as a first-class column with `agent-config-form` restricted to the enabled subset, deletion as Platform Operator submit plus a distinct Compliance Inspector approval, and both administrative `PostingFailed` exits present. The role basis is a rendered confirmation content and an audit field; `UnretiredAssumption` is a rendered `RQ-1` blocker; the constant registration gap is the one § Shipped-code corrections row. |
| 16 | `Caller` retained as a live Approver Policy source (§7 high) | **resolved** | Three buildable values only, `Caller` retired with the FR-23 register cited, a legacy policy rendering the row read-only in `{colors.status-severe}` with the typed reason and publication blocked. |
| 17 | OQ-21's compliance-inspection access level not carried (§7 medium) | **partial** | Two levels, the compliance-inspection path, the scoped-and-justified entry, Story 8.8, and inspectability after Conversation deletion all landed. The approver identity and the post-hoc scope bound contradict OQ-21 (§7 high). |
| 18 | Regeneration-ceiling deferral stale and quotes FR-32 wrongly (§7 medium) | **unresolved** | The sentence is byte-identical to the predecessor's quotation. |
| 19 | Cost-control posture vocabulary missing from the token list and the panel (§7 low) | **partial** | `launch-readiness-panel` now renders the posture as a field, names the three satisfying and two rejected postures, and renders `ProhibitedCostControlPosture` as an additive blocker. The § Voice and Tone architecture-token row is unchanged at nine tokens (§7 medium, re-filed larger). |

**Counts: 9 resolved · 6 partial · 2 unresolved · 0 regressed · 2 unchanged by prior decision.**

**No prior finding regressed**, and the Update closed its own critical. The answer to this run's question is nonetheless *no, it did not land cleanly*: of the 32 findings in this report, 20 are new and 12 are the unclosed remainder of a predecessor finding, and 16 of the new ones trace to one cause — the Update reconciled the FR-33 table and treated it as the whole of the 2026-09-09 PRD revision, when that revision also rewrote FR-3, FR-4, FR-18, FR-28 and FR-30 and added OQ-24 to OQ-30. The hunted pattern is confirmed four times over: a count left behind by its own set ("the four FR-18 exits", "the six-part eligibility set", "the ten families" against twelve declared, "this four-name list" against two), a cross-reference left pointing at the superseded reconcile file, a flow left describing the pre-FR-33 role model, and a seam bullet left asserting the ownership the section above it replaced.

## Mechanical notes

- **Frontmatter.** Both spines carry `name`, `description`, `status: final`, `created`, `updated: 2026-09-09`, `sources`. All 37 source paths resolve on disk (17 DESIGN, up one from the predecessor's 16 with `reconcile-validation-2026-09-09-2.md`; 20 EXPERIENCE, unchanged). `DESIGN.md` additionally carries `colors`, `typography`, `rounded`, `spacing`, `components` per spec. `status: final` should be `draft` until the two criticals are closed.
- **Cross-references.** All 19 distinct `{path.to.token}` references resolve, script-verified against the parsed YAML. Every internal `§ …` reference resolves to a heading in the named file, and all six external ones resolve to real headings in `launch-readiness-register.md`, `hexalith-ux-instructions.md`, and `epics.md`. Every FrontComposer and Fluent type named resolves in the submodule or at the pin.
- **Component parity.** The 22 names are byte-identical **and identically ordered** across the DESIGN frontmatter, the DESIGN `###` sections, and the EXPERIENCE Component Patterns rows. The three-way rule the spines declare holds under test.
- **Policy constants.** The five AD-30 constants are used consistently and no sixth is invented. `AgentsFrontComposerRegistration.cs` declares `Agents.Administrator`, `Agents.Approver`, `Agents.Operator`, and `Agents.AuditOperator`; `Agents.PlatformOperator` is absent, exactly as § Shipped-code corrections records. Nine nav entries are registered against the spine's twelve, also recorded.
- **Name consistency.** `Conversation Facilitator`, `Compliance Inspector`, `PostingFailed`, the `Stale` / `catching up` / `awaiting projection` triple, and the Global Providers Aggregate / `ProviderCatalog` / Provider catalog triple are consistent throughout. Zero "Conversation owner" hits outside the sentences forbidding it; zero `Agents.ComplianceInspector` hits. No casing drift.
- **Icons.** `FcFluentIcons` verified against source: the twelve `TryCreate` names match exactly; the thirteen listed Size16 factories all exist and all seven status roles bind to one. `Play16` exists as a fourteenth factory and is correctly listed under the string contract instead.
- **Tables.** No malformed rows in either file. The single ` ```text ` fence is opened and closed correctly. No Mermaid.
- **Repo UX law.** Compliant. FrontComposer + Fluent v5 only, reuse over hand-rolling asserted against the two shipped violations, no theme redefinition, no legacy v4/FAST token except as a named prohibition and a zero-hit inventory, and the single-`FluentAccordion` grouping rule carried per surface — with the coverage gap filed at §8.
- **Denominators.** The IA table holds 15 rows and `DESIGN.md` now says 15; `.memlog.md:38` still says 14. The DESIGN layout split (6 FullWidth / 8 Constrained) counts Audit evidence twice, which is correct for routes and confusing next to a surface count. One stated denominator would help.

## Finding totals

| Severity | Count |
|---|---|
| critical | 2 |
| high | 7 |
| medium | 14 |
| low | 9 |
| **total** | **32** |

Of the 32, **20 are new to this run** and 12 are the unclosed remainder of a predecessor finding (the 6 partials and 2 unresolved above, several of which split into more than one filing). By category: flow coverage 3, token completeness 0, component coverage 3, state coverage 6, visual reference 1, bloat 2, inheritance discipline 16, shape fit 1.
