# Spine Pair Review — agents

## Overall verdict

This is an unusually strong spine pair on every mechanical axis: 22 components named and ordered identically in all three required places (verified by diff), every `{path.to.token}` in both files resolving, every one of 39 `sources:` paths resolving on disk, every internal and external `§` cross-reference resolving, and DESIGN.md carrying all eight canonical sections in exact canonical order. A story-dev can source-extract from it without asking a question — the 50 `UX-DR` rows already extracted into `epics.md` are evidence that extraction works at scale.

Two things stop it short of clean. First, `EXPERIENCE.md`'s own currency banner (line 33) is wrong in **both** halves — it claims reconciliation to a PRD revision and an architecture assumption index that have each been superseded, while the body already carries the newer decisions, so the one field a consumer uses to judge whether the contract is current actively misleads. Second, `DESIGN.md` has not been re-verified since 2026-09-10 and carries zero verified-closure evidence from the 2026-09-12 round: three visual decisions that round created have no visual binding, and one of them directly contradicts `DESIGN.md § Layout & Spacing`. Size is the remaining cost — at 178 KB the contract is right but expensive to read, and about a third of the bytes are argument and code-audit backlog rather than rule.

## 1. Flow coverage — strong

Extracted from `sources:`: `prd.md` (`updated: 2026-09-12`) declares four User Journeys — UJ-1 Nora, UJ-2 Milan, UJ-3 Anika, UJ-4 Omar — plus FR-1…FR-34, NFR-1…NFR-14, and `epics.md`'s 33-story active backlog. Checked each journey for a named protagonist, numbered steps, a climax beat, a resolution, and a failure path.

All four PRD journeys are mirrored with their titles and protagonists intact (`EXPERIENCE.md:743, 762, 779, 798`), each with numbered steps, an explicit bolded **Climax:**, a Resolution line, and one or more failure paths. Two UX-derived journeys — UX-J5 Priya (`:812`) and UX-J6 Milan (`:829`) — cover launch governance and capacity backpressure, both grounded in named FRs/NFRs and both naming their stories. UX-J5 is the standout: it detects that three of its eight steps belong to a *different role*, names the second actors inline (Raul, Platform Operator; Anders, Compliance Inspector) and says why, rather than letting one persona silently acquire four authorization tiers. UJ-3 has been correctly rewritten to the two-person segregation-of-duties flow the 2026-09-12 Product decision on regeneration-requester eligibility requires.

### Findings

- **high** UJ-3 step 2 offers Anika three discovery paths — "the pending count, queue, or **Conversation status entry**" (`EXPERIENCE.md:784`) — but no section defines a Conversation status entry. § Conversation Integration Seam defines the fourth `EXT-CONV-UI-1` artifact as caller-facing only (validation, `submitted`, `denied`, `generation failed`, `capacity rejected`, `Posted`, `EXPERIENCE.md:121`). `reconcile-prd-2026-09-12.md:137-139` records that AD-31 and the external register made that artifact **dual-purpose** — also carrying pending-proposal state for authorized Approvers under AD-8's three-tier disclosure, with caller/Approver disclosure tests required. Neither the dual purpose nor a fifth artifact is in the spine, so a journey step points at a surface the seam contract does not create, and FR-13's third discovery path has no implementable definition. *Fix:* extend the fourth artifact's row to state both purposes and the three disclosure tiers (full content with current read access; existence-and-state for a previously resolved Approver who lost it; nothing for a Party never resolved), or add a fifth artifact kind and cite it from UJ-3 step 2.
- **medium** The two highest-consequence operator paths the 2026-09-12 round spent the most effort on have no walked flow: **administrative retry / administrative abandon** of a `PostingFailed` proposal (the `MessageId` lookup with its three outcomes, `PausedDuration` arithmetic, the shared three-attempt budget, `LateConfirmed`) and the **governed compliance inspection** (computed subject set, post-hoc window, `UnreviewedInspection`). Both are specified in dense rule prose (`§ Proposal editor action rail`, `§ Audit evidence rules`) but neither is rehearsed end-to-end, so nobody has checked that the rules compose into a path a human can complete. Anders appears only as one line inside UX-J5. *Fix:* add UX-J7 (Tenant Agent Administrator recovers a stranded `PostingFailed` proposal) and UX-J8 (Anders opens a governed inspection); both would exercise rules no current flow touches.

## 2. Token completeness — strong

Extracted every frontmatter token and every `{path.to.token}` reference in the prose of both files. DESIGN.md defines 8 colors, 5 typography roles, 6 spacing steps, 1 `rounded` entry, 22 component objects. References used: 8 distinct `colors.*`, 5 `typography.*`, 6 `spacing.*` across both files. **Every reference resolves.** No orphan definitions except `rounded` (see below).

The color tokens carry `note:` strings rather than hex, which is correct for this spine — it is a Fluent v5 inheritance spec, and each token names a concrete, resolvable construct. Verified against the pinned package API: `BadgeColor` has exactly the eight members the spine binds (Brand, Danger, Important, Informative, Severe, Subtle, Success, Warning); `BadgeAppearance.Tint`/`.Filled`, `ButtonAppearance.Primary`, `FluentBadge.IconLabel`, `FluentDialogBody.FixedHeaderFooter`, `FluentNumberInput<T>`, `FluentSelect<T>`, `FluentTextArea` all exist. Not one named construct is fictional. Contrast targets are stated for the load-bearing case with a verification lane attached (`DESIGN.md:207`: 4.5:1 text, 3:1 non-text and focus, in Light, Dark and forced-colors, inherited pairs assumed AA at the pin and re-verified on pin change, proved per route by the Agents-owned axe lane). The no-color-only rule (`DESIGN.md:209`) is the strongest single passage in either file: one glyph per role from a named factory, all seven roles satisfied today, badge accessible name is the text alone with `IconLabel` unset so AT never reads "checkmark circle, Posted".

### Findings

- **low** `rounded:` is a single bare `note:` with no scale names, so unlike `colors`, `typography` and `spacing` it exposes no addressable token — `{rounded.sm}` cannot be written. Nothing references it, so the pair is internally consistent, but the key is a comment occupying a token slot. *Fix:* either drop the key (Shapes prose already carries the rule) or give it the inherited scale names so the group binds like the other three.

## 3. Component coverage — strong

Extracted every component name from `DESIGN.md` frontmatter `components:`, from the `###` sections under `DESIGN.md § Components`, and from the `EXPERIENCE.md § Component Patterns` table. **All three lists are byte-identical in content and order, 22 for 22** — confirmed by diff, not by eye. The pair states this invariant explicitly (`DESIGN.md:120`) and then holds it, which is rare.

Neither side is a stub. Every DESIGN row binds real tokens and closes with an enumerated Variants list (`provider-catalog-grid`: loading, empty, filtered-empty, error, `catching up`, `superseded by another decision`, `read-only (no mutation policy)`, `tenant-scoped read`). Every EXPERIENCE row carries real behavioral rules with props/events/variants. The visual/behavioral split is policed in both directions — DESIGN rows say "behavior is in the matching EXPERIENCE row", EXPERIENCE rows say "visual specs live in `DESIGN.md § Components`" — and I found no leakage of one into the other.

### Findings

- **high** `DataHandlingAcceptance` became the tenth lock-bearing confirmation family and is the only one carrying **two affirmative actions** — Accept and **Decline**, where Decline "blocks the model for the tenant immediately" (`EXPERIENCE.md:307`, `§ Provider data handling`). `DESIGN.md § high-impact-confirmation` specifies exactly one Confirm (`ButtonAppearance.Primary`, `{colors.brand-accent}`, never the default button) plus Cancel, and "no destructive red button". There is no visual binding for a three-button dialog whose second affirmative commits a blocking side effect — which of Accept/Decline is Primary, how Decline is distinguished from Cancel, whether Decline gets a severity treatment. This is the one confirmation in the roster where getting the button hierarchy wrong blocks a tenant's model by mis-click. *Fix:* add the Accept/Decline/Cancel binding to `DESIGN.md § high-impact-confirmation` as a named variant.
- **high** The audit-evidence **enumerable list** has no component on either side. `EXPERIENCE.md § Information Architecture` gives `/agents/audit` "an enumerable, tenant-scoped list" for configuration-and-governance change evidence and compliance-inspection records, and rules that "The Agents grid rules apply to that list and to it only." But `§ Grid rules` enumerates per-grid values for exactly three grids — the `ViewKey` row names `agents.provider-catalog`, `agents.pending-proposals`, `agents.launch-readiness` and stops — so this fourth grid has no view key, no `DetailPanelAriaLabel`, no 320 CSS px surviving-column set, no default sort and no page size, all of which `§ Grid rules` treats as mandatory per-grid values. On the DESIGN side `audit-evidence-panel` is "Definition list plus `FluentBadge` rows", i.e. the detail only. *Fix:* add the fourth grid's four values to `§ Grid rules` and give DESIGN.md either a variant on `audit-evidence-panel` or a 23rd component.
- **medium** Delegation is inconsistent: 11 of the 22 EXPERIENCE rows delegate ("Full rules in § X rules") while 18 carry inline rules of 1,500–3,500 characters, and several do both — `agent-config-form` carries ~3,300 characters inline with no companion section, while the structurally similar `provider-catalog-grid` is two sentences pointing elsewhere. A consumer cannot predict from the row whether it is complete, so every component costs two lookups. *Fix:* pick one convention — either every row delegates to a `§ <component> rules` section, or none do.

## 4. State coverage — strong

Walked all 15 IA surfaces against the applicable state list (empty, cold-load, focus, error, offline, permission-denied, unauthorized, in-flight approval, filtered-empty, catching-up).

Coverage is achieved by rule rather than by per-surface enumeration, and the rules are genuinely total. `§ List, detail, and deep-link surfaces` binds loading / empty / filtered-empty / error / permission-denied / `catching up` to every grid or list, maps all seven `FcAggregateDetailState` values (including an argued **Unused** for `Stale`, because the shell's meaning would collide with the spine's reserved one), and collapses Unauthorized / NotFound / Unavailable into a single `not available` state with identical copy, status class **and timing** from two keys — with the reason stated (per-cause copy is itself a disclosure channel). `§ Route heading and focus` then closes the gap most spines leave, naming the nine routes bound to no `Fc*` page component and requiring them to carry the same state-slot pattern by hand. Offline is covered (`connection lost`, with the shell named as the announcer and Agents restricted to the domain consequence). In-flight approval is covered in unusual depth by `§ High-risk pending commands` plus the deterministic focus rule, which is correct about `gridcell` being name-from-author and about not overriding a row's owned-element contract. State vocabularies are deep: 13 Agent-readiness states, 20 Agent-call states, 10 proposal states plus 8 display-only outcomes, 5 membership states plus 2 mutually exclusive derived mirror statuses, 4 audit-availability, 4 launch-evidence.

### Findings

- **medium** Two surfaces whose read model ships in a later story have no pre-contract rendering rule, while three structurally identical cases do. `provider-status-badge` has one ("Interim rule before Story 5.5: render `Unknown`…", `:221`), `provider-catalog-grid` currency has one ("Before Story 8.4 ships the budget policy read model there is no comparison value…", `:312`), the disable blast radius has one (`:312`). But `conversation-context-policy-panel` (read model owned by Story 6.2) and `content-safety-policy-editor` (contracts owned by Story 8.4) have none — a dev building either before its contract lands has no instruction, and the established pattern says they should. *Fix:* add the interim state to both rows, or state once that surfaces without a stated interim do not render until their contract ships.
- **medium** No rendering rule for a legacy prohibited cost-control posture on the editor. `launch-readiness-panel` renders `ProhibitedCostControlPosture` for `ReportingOnlyMonitoring` and `AcceptedLaunchRisk` (`§ Launch readiness rules`), but Story 5.9 requires that "no legacy client, **UI option**, default, or compatibility path can convert it into an accepted hard-control posture" — and `cost-control-editor` never mentions the posture at all: not whether it is a field, not how a persisted legacy value renders, not that the two values are unofferable. The spine has an exact precedent it did not apply — retired `Caller` on `approver-policy-builder` renders "the row read-only in `{colors.status-severe}` with the typed retirement reason, and publication is blocked while it is present." *Fix:* apply the `Caller` pattern to the posture in the `cost-control-editor` row, and cite Story 5.9.

## 5. Visual reference coverage — adequate

`mockups/` and `wireframes/` do not exist; `imports/` exists and contains **zero files**. So there are no orphans, no unspecific references, and nothing for the spines to link.

The decision is stated once, in `DESIGN.md § Brand & Style` (`:33`), with its date and reaffirmation (2026-08-02, reaffirmed 2026-09-09), its denominator (15 IA surfaces), its reasoning (FrontComposer and Fluent inheritance plus the per-surface primary-region and accordion tables make layout derivable; a visual reference would restate inherited chrome), and spines-win-on-conflict. `EXPERIENCE.md § Foundation` correctly points at that single statement rather than restating the decision. This is the right shape and I am treating it as a stated decision, not a finding.

On whether any surface genuinely needed one: for the 13 Agents-rendered surfaces the claim holds. The hardest layout cases all resolve in prose — `provider-catalog-grid`'s reader-tier-split pinned columns give an explicit column list per tier plus a 320 px surviving set; `proposal-editor` gets primary region, accordion items and rail from the per-surface table in `DESIGN.md § Layout & Spacing`; `high-impact-confirmation` at 320 px falls out of `FluentDialogBody` parameters. One case does not.

### Findings

- **medium** The Conversation seam is the surface that genuinely needed a visual reference. Agents contributes **three artifacts into a foreign module's layout** — the dialog body, a persistent status region "beside **Call hexa**", and a per-message decoration slot — plus a focus-return contract crossing the boundary. Placement is conveyed entirely by the word "beside", to an audience (the Conversations owners) who must implement it, for a dependency that is `Uncommitted` with owner `TBD`, so there is no shipped surface to point at instead. Every other surface derives its layout from inherited chrome the reader already has; this one does not. *Fix:* one annotated sketch of the Conversation surface showing the three contribution points and the focus path, linked from `§ Conversation Integration Seam` and named as illustrating the `EXT-CONV-UI-1` artifact placement.
- **low** The denominator is inflated. Two of the 15 rows are not Agents-rendered surfaces: "API/client contract reference" is Developer docs and the spine itself says "not a FrontComposer screen", and "Conversation invocation" is Conversation-owned. The real claim is 13 surfaces. *Fix:* state the denominator as 13 rendered surfaces (of 15 IA rows).

## 6. Bloat & overspecification — thin

`EXPERIENCE.md` is 178 KB across 887 lines. Section sizes: **Component Patterns 68.6 KB (38%)**, State Patterns 28.2 KB, Accessibility Floor 15.9 KB, Known gaps 13.7 KB, IA 11.2 KB, Key Flows 10.7 KB, Voice and Tone 9.3 KB. Average Component Patterns table row is ~1,100 characters against ~60 in `experience-example-shadcn.md`. There is no pixel-spec-where-tokens-cover-it problem and almost no source restatement (the V1 non-goals paragraph is the only one, is two lines, and declares itself). The size is not padding — it is three specific habits.

### Findings

- **medium** Rationale is inlined with rule throughout, and a large fraction of it is not the rule's justification but the **history of the previous wording being wrong**. Examples: "the former condition `where its label contract allows` described an empty set and read as permission" (`FcStatusFilterChips`); "which would drop focus to `document.body` on the very two controls added to close that dead end" (action rail); "the set is deliberately unnumbered here because every count this spine has carried for it went stale"; "this spine's own rule is that…, because…". A reader implementing the rule needs the rule and, at most, the constraint that forces it. Re-litigation of superseded drafts belongs in `.memlog.md`, which exists and is 54 KB. Conservatively this is 15–20% of the file. *Fix:* keep one clause of justification where a rule looks arbitrary; move every "the earlier wording said X and that was wrong" clause to the memlog.
- **medium** `§ Shipped-code corrections` (9.0 KB, 24 rows) is a code-audit backlog living in a durable contract. It carries file names, line-level site counts and inventories — "29 sites across 20 `.razor` files", "15 sites across 5 files, with `DisabledFocusable` at **zero** occurrences", "roughly 115 distinct BEM class values", "eight files" — every one of which is invalidated by the first commit that touches those files, with no refresh mechanism and no stated owner for keeping it current. The *rules* it cites are elsewhere in the spine and remain true; only the inventory rots. A story-dev wants this, but wants it in the absorbing story, which each row already names. *Fix:* reduce each row to divergence class → owning rule → absorbing story and move the site inventories into the named stories' acceptance criteria.
- **low** `EXPERIENCE.md` prose carries editorial voice the shape reserves for `DESIGN.md`: "because safety wins", "rather than papered over", "two lines after § Authorization roles forbids falling back to a more permissive row", "the comment will send a dev the other way from the code beneath it". `DESIGN.md`'s own voice is well-judged and in the right place; this is the behavioral file borrowing it. *Fix:* flatten to statement in `EXPERIENCE.md`.
- **low** `§ FrontComposer Readiness` (2.1 KB) is a pointer table — 15 rows, 14 of which say "see § X" and add nothing. It earns a partial pass by giving a reverse index from FrontComposer's own capability ids (FC-LYT, FC-TBL, FC-A11Y, FC-L10N), which no other section provides. But one row smuggles in a normative gap found nowhere else — "the 250 ms / 8 s contract exists only in this spine: the Architecture Spine names no polling, nudge, or catch-up contract, and the two shipped paths run 250 ms / 5 s and 200 ms / 8 s" — which is a Known-gaps item misfiled in a navigation aid. *Fix:* move the `IProjectionChangeDetailNotifier` gap to `§ Known gaps`; keep the rest of the table as the capability index it is.
- **low** `§ Inspiration & Anti-patterns` (618 bytes) restates the PRD addendum's competitive positioning and is placed **second**, ahead of Information Architecture, where both reference examples place it near the end before Key Flows. No downstream consumer extracts from it, and its position costs every reader two screens before the IA table. *Fix:* move it to just before `§ Key Flows`.

## 7. Inheritance discipline — strong

Mechanically verified, not assumed: all 18 `sources:` paths in `DESIGN.md` and all 21 in `EXPERIENCE.md` **resolve on disk**, including the four cross-repo `references/Hexalith.*` paths and the three shipped-source `.cs`/`.razor` paths. Every external `§` target resolves — `launch-readiness-register.md § NFR-14 Browser Monotonic Timing Contract` (line 245), `hexalith-ux-instructions.md § No theme redefinition` / `§ Reuse over hand-rolling` / `§ Page sections` (lines 22, 10, 41). Every internal `§` reference in both files resolves to a real heading; I found no broken cross-ref in either direction. UJ titles mirror the PRD's verbatim. Glossary usage is consistent across both spines and the PRD — I found no term carrying a divergent meaning, and the two contract-identifier-versus-rendered-label rules (`ConversationOwner` → Conversation Facilitator, `Agents.AuditOperator` → Compliance Inspector) are handled identically. Every `EXPERIENCE.md` token reference resolves to a `DESIGN.md` token by name.

### Findings

- **critical** `EXPERIENCE.md:33` is stale in both halves. It declares the spine "reconciled to `prd.md` **as of its 2026-09-09 revision in full**" — `prd.md` is `updated: 2026-09-12`. And it declares "`ARCHITECTURE-SPINE.md` at `architecture_assumption_index_version` **5** (2026-09-10)" — the Architecture Spine is `updated: 2026-09-12` at `architecture_assumption_index_version: **7**`, two increments on, and those increments **retire `ARCH-A-13` and add `ARCH-A-15`**. The body already absorbs the 09-12 decisions (matrix v3 in all three normative places, ten lock-bearing families, four artifact kinds, the six-conjunct predicate, `CurrentMirror`, `ARCH-A-13` retirement at `:852`), so the banner now contradicts the file beneath it. This is the single field a downstream consumer reads to decide whether the contract is current, and a consumer trusting it will believe the spine predates decisions it actually carries — or re-do the reconciliation. It also has a behavioral edge: the spine rules that "every unretired §8.1 `A-n` and `ARCH-A-n` row blocks `RQ-1` whatever its owner", so the `UnretiredAssumption` blocker set `launch-readiness-panel` renders grew with `ARCH-A-15`, against a baseline the spine says is index 5. *Fix:* update the banner to `prd.md` 2026-09-12 and `ARCH-A-INDEX-7`, add the five 2026-09-12 architecture reviews to `sources:`, and state which of their directives are absorbed.
- **high** The `EntryMissing` terminal-history directive is unabsorbed. `reconcile-ux-2026-09-12.md:22` directs: "**Add the terminal-history rendering rule to UX** and remove the section-8 deferral" — AD-2 and AD-10 now permit the `EntryMissing` carve-out only for the Agent's current selection or an in-flight interaction's prior snapshot, with terminal history rendering from the immutable snapshot plus authorized Audit Evidence. `EntryMissing` appears **nowhere in either spine**, and none of `version-history`, `audit-evidence-panel` or `provider-catalog-grid` states how a terminal proposal renders a Provider/model whose catalog entry is gone. *Fix:* add the rule to the `version-history` and `audit-evidence-panel` rows and name the code.
- **medium** `CurrencyMismatch` is never named. The spine renders the condition twice ("a pricing currency mismatch is `{colors.status-important}`", `DESIGN.md:292`; "A per-tenant mismatch renders `{colors.status-important}`", `EXPERIENCE.md:312`) but never uses the token, although the register's `ProviderReadinessReasonCode` list now carries it and every sibling code is named (`Unconfigured`, `SecretUnavailable`, `PlatformNotReady`, `Unpriced`, `DataHandlingAcceptanceLapsed`). The spine's own localization rule requires a per-enum inventory of `Agents.<Enum>.<Value>` labels; an unnamed value cannot enter that inventory. *Fix:* name `CurrencyMismatch` in both places.
- **medium** Seven FRs whose behavior is fully present are never cited by id: **FR-1, FR-6, FR-10, FR-15, FR-22, FR-27, FR-34**. FR-34 is the notable one — the spine renders `PayloadProtectionUnavailable` as a launch blocker *and* as the `DisabledFocusable` cause on export and deletion, and specifies the A-24 two-part attestation in detail, without ever naming the requirement that demands it. `epics.md` maintains an FR Coverage Map, so a consumer doing FR→spine traceability hits seven false gaps. *Fix:* cite each at its point of use; FR-34 at the `audit-governance-panel` row and in `§ Launch readiness rules`.
- **medium** A-25's override bound is not rendered. The spine says the audited override "carries a numeric ceiling and an expiry, both rendered in its confirmation" and that it lapses at the earlier of the two — but A-25 fixes those as **25% of the monthly cap and 7 days**, and adds that a **second override in the same month requires Product approval**, which is a gate with a UI consequence that nothing in `cost-control-editor` or `§ Confirmation contents by family` renders. The spine renders A-6's, A-7's, A-8's and A-19's numbers; this one it leaves to the server. *Fix:* render the bound and add the second-override-in-a-month condition to the `TenantBudgetUpdate` — override confirmation row.
- **medium** Stories 5.9 and 8.5 are never cited, and the spine records their subject matter as unowned. `§ Operational status rules` says the per-tenant blocked counts, per-Conversation safety history, cost consumption and projection id/version "have no contract yet… deferred items for the epics skill, so ownership lands as a story acceptance criterion rather than a spine assertion" — but **Story 8.5** exists in the active backlog and owns exactly those metric contracts, including SM-C4 blocked-call share and SM-C5. A deferral recorded as ownerless against a story that owns it will be read as a real gap. (Story 5.9 is the `ProhibitedCostControlPosture` owner; see finding 4.) *Fix:* cite 8.5 in `§ Operational status rules` and narrow the deferral to what 8.5 genuinely does not cover.
- **low** The two `sources:` lists are asymmetric without a stated reason: `DESIGN.md` lists `reconcile-validation-2026-09-08.md`, `EXPERIENCE.md` does not; `EXPERIENCE.md` lists three shipped-source files and the Tenants `EXPERIENCE.md`, `DESIGN.md` lists the Tenants `DESIGN.md`. The second asymmetry is principled and obvious; the first looks accidental. *Fix:* align or annotate.
- **low** `DESIGN.md § Do's and Don'ts` carries the Conversation Facilitator labeling rule but not its twin. `EXPERIENCE.md` rules that `Agents.AuditOperator` is "labeled Compliance Inspector on every surface, **exactly as** `ApproverPolicySourceKind.ConversationOwner` is labeled Conversation Facilitator" — one of the pair is a visual Don't, the other is not, and `DESIGN.md` never uses the words Compliance Inspector. *Fix:* add the second row.

## 8. Shape fit — strong

`DESIGN.md`: all eight canonical sections present, in exact canonical order — Brand & Style (165), Colors (201), Typography (234), Layout & Spacing (242), Elevation & Depth (272), Shapes (276), Components (280), Do's and Don'ts (374). Nothing omitted, nothing out of order, no invented section. Frontmatter carries `name` and `description` as required. Elevation & Depth and Shapes are three lines each and correctly so — they say "inherit, and here is what that forbids", which is the right length for an inheritance spec.

`EXPERIENCE.md`: all eight required defaults present — Foundation, Information Architecture, Voice and Tone, Component Patterns, State Patterns, Interaction Primitives, Accessibility Floor, Key Flows. No default dropped. Required-when-applicable `Responsive & Platform` is present and genuinely triggered (multi-breakpoint web with a 320 CSS px reflow obligation). Optional `Inspiration & Anti-patterns` present.

Judging the four invented sections:

- **Authorization roles** — earns its place decisively. FR-33 is the authoritative matrix but names no policy constants; AD-30 names constants but not roles. The binding between them has no other home, and every `RequiredPolicy` cell in the IA table derives from it. Downstream extracts constants directly from it. Keep.
- **Conversation Integration Seam** — earns it. `EXT-CONV-UI-1` is an external contract with four artifact kinds, an ownership split, a gateway surface and a focus-return contract; it cannot be compressed into the `conversation-agent-call` component row, and a consuming story is blocked on it. Keep.
- **Known gaps → Contracts that must grow** — earns it. Twelve rows, each naming the missing contract member *and* its owning story, and explicitly framed so "a correction is never unowned". This is the delta list architecture consumes. Keep.
- **Known gaps → Shipped-code corrections** — does not earn a place in a durable spine. See finding 6.
- **FrontComposer Readiness** — marginal. See finding 6.

### Findings

- **high** `DESIGN.md § Layout & Spacing` (`:82`) directly contradicts the current `EXPERIENCE.md`. It rules that "The Audit evidence list is an id-entry surface, **not a grid**", that the required filter component set "therefore applies to the Provider catalog and Proposal queue grids only", and that `FcExpandInRowDetail` "additionally governs the Launch readiness gate grid, which is **a third** `FluentDataGrid`". `EXPERIENCE.md` now gives `/agents/audit` an enumerable tenant-scoped list governed by the Agents grid rules — a fourth grid. On the pair's own rule that the spines are one contract and win on conflict, a consumer has two contradictory instructions and no tiebreak. *Fix:* update `DESIGN.md § Layout & Spacing` to four grids and give the audit list its layout row.
- **high** `DESIGN.md` carries no visual binding for four decisions `EXPERIENCE.md` delegates to it. The 2026-09-12 architecture reviews cite `EXPERIENCE.md` line numbers throughout and **never cite a `DESIGN.md` line**, so `DESIGN.md` has no verified-closure evidence from that round, and its 2026-09-10 stamp precedes every closure. Concretely, `EXPERIENCE.md` makes four visual demands with nothing on the other side: (a) the **nearing-retry-window flag**, required "on the same terms as nearing expiry" with the remaining time "rendered on the proposal detail itself" — `DESIGN.md § proposal-state-badge` specifies the nearing-*expiry* chip to the character (visible text, `Expires in {duration}`, `Warning16` decorative, `{colors.status-warning}`) and says nothing about its twin; (b) a **paused retry clock** that "renders as paused rather than counting down"; (c) an `Approved` proposal waiting under disable-or-suspension rendering a "**waiting, not posting** treatment that is visibly distinct from `PostingPending`" — but `DESIGN.md` gives `Approved` and `PostingPending` the same `{colors.status-informative}` and no distinguishing device; (d) `LateConfirmed` and `ExpiredWhileSuspended`, which `EXPERIENCE.md` records in status, are absent from both the `DESIGN.md` colors role lists and the `proposal-state-badge` display-only-outcome set. `DESIGN.md § proposal-editor`'s Variants list likewise has no `PostingFailed`/administrative-retry variant. *Fix:* re-verify `DESIGN.md` against the 2026-09-12 round and bind these four; bump its `updated:`.

## Mechanical notes

- **Name consistency:** clean. The 22 component names are byte-identical and identically ordered across `DESIGN.md` frontmatter, `DESIGN.md § Components` `###` headings, and `EXPERIENCE.md § Component Patterns` (verified by diff). Shipped-versus-spine name aliases are declared rather than left to collide — `ConversationAgentCallPanel` ≡ `conversation-agent-call`, PRD `PostFailed` ≡ contract `PostingFailed`, "Global Providers Aggregate" ≡ `ProviderCatalog` ≡ "Provider catalog" ("the three names denote one thing"). One near-miss worth noting: `EXPERIENCE.md` invents confirmation family **`EditorDraft`** for the unsaved-changes dialog, a name that is not in `OperationGateMatrixVersion = 3` and is not declared as UX-owned, in a document that elsewhere treats operation-family names as a closed contract vocabulary. It is correctly given no justification and no audit record, but the name should be marked as UI-local.
- **Cross-references:** no broken references found in either direction. All internal `§` targets resolve; all four external `§` targets resolve (`launch-readiness-register.md § NFR-14 Browser Monotonic Timing Contract` line 245; `hexalith-ux-instructions.md` §§ at lines 10, 22, 41). All 39 `sources:` paths resolve on disk. One dangling *concept* reference: "Conversation status entry" in UJ-3 (finding 1).
- **Roster precision:** `§ Confirmation contents by family` and `§ High-risk pending commands` both say "the roster **is** the register's `OperationGateMatrixVersion = 3`". The v3 matrix holds 23 families; the confirmation table covers 15. The 8 uncovered are correctly uncovered — `ProviderInvocation`, `ConversationPosting`, `SystemTimer`, `AgentCallAcceptance`, `ReadinessInspection`, `ReadinessObservation`, `ProposalEdit`, `ProposalRegeneration` are workflow-only or not browser-issued — but "the roster is v3" read literally instructs a consumer to build a `SystemTimer` confirmation dialog. State the subset rule (human-initiated, browser-issued families) rather than equating the roster with the matrix.
- **Frontmatter completeness:** both files carry `name`, `description`, `status: final`, `created`, `updated`, `sources`. `DESIGN.md` additionally carries `colors`, `typography`, `rounded`, `spacing`, `components`. `DESIGN.md updated: 2026-09-10`, `EXPERIENCE.md updated: 2026-09-12` — the two-day gap is the substantive lag documented in findings 8 and 3, not a formatting issue.
- **Mermaid syntax:** no Mermaid diagrams in either file. `EXPERIENCE.md` has one fenced block (```text, lines 419–424) holding the four state-flow arrows; it is plain text, renders fine, and is the right tool for four transitions.
- **Counts that will rot:** the spine is admirably self-aware here — it cites the confirmation roster "by matrix version rather than by a count" and leaves the Provider eligibility set "deliberately unnumbered here because every count this spine has carried for it went stale". It then carries hard counts elsewhere that have the same failure mode: "exactly twelve names" (icon string contract), "all 15 Information Architecture surfaces", "**five** reference kinds", "twelve entries at nav order 0 to 11", "29 sites across 20 `.razor` files", "15 sites across 5 files", "roughly 115 BEM class names". Apply the stated principle consistently.
