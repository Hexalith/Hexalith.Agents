---
topic: Hexalith Agents UX
updated: 2026-09-10T19:12
---

- (event) Source confirmed: _bmad-output/planning-artifacts/briefs/brief-agents-2026-06-23/brief.md
- (decision) Foundation: Hexalith Agents web UI uses Hexalith.FrontComposer. Tenants module implementation is the reference pattern for web UI composition.
- (decision) FrontComposer UX constraints to carry forward: FC-LYT full-width vs constrained page measure, FC-TBL generated/projection DataGrid surface, FC-A11Y skip links/focus/live-region/accessibility diagnostics, FC-L10N shell-vs-domain localization split, and policy-gated nav entries.
- (decision) Composition pattern from Tenants: domain module registers a FrontComposer DomainManifest and localized nav entries; FrontComposerShell owns header, navigation, account controls, theme/settings, command palette, skip links, and shell chrome. Agents owns page bodies and domain copy.
- (assumption) Assumption pending user correction: Hexalith Agents is a desktop-first responsive web admin/workflow surface inside FrontComposer; mobile/narrow widths support review/status/reference flows but not complex high-risk approval editing unless explicitly required.
- (decision) UI system inheritance: Hexalith Agents inherits Hexalith.FrontComposer shell behavior and Microsoft Fluent UI Blazor v5 visual authority; DESIGN.md should specify only Agents-specific semantic deltas, not a bespoke palette or component system.
- (event) Reference files consulted: Hexalith.Tenants/src/Hexalith.Tenants.UI/Composition/TenantsFrontComposerRegistration.cs; Hexalith.Tenants/src/Hexalith.Tenants.UI/Program.cs; Hexalith.Tenants/src/Hexalith.Tenants.UI/Components/Layout/MainLayout.razor; Hexalith.Tenants/_bmad-output/planning-artifacts/ux-designs/ux-tenants-2026-06-02/DESIGN.md and EXPERIENCE.md; Hexalith.FrontComposer/_bmad-output/project-context.md; Hexalith.FrontComposer/docs/reference/components/front-composer-shell.md, navigation.md, datagrid.md.
- (event) Source confirmed: _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md, addendum.md, and reconcile-brief.md. PRD review-rubric.md was not added as product source; keep it available for validation context if needed.
- (decision) Source-derived key journeys: Nora configures hexa for tenant launch; Milan calls hexa from a Conversation in automatic mode; Anika reviews/edits/regenerates/approves a Proposed Agent Reply in confirmation mode; Omar uses API/client contracts for governed operations and status/audit inspection.
- (decision) Source-derived proposal lifecycle: Proposed Agent Reply is outside the Conversation until approved; states include generated, edited, regenerated, approved, rejected, abandoned, and expired. Terminal proposals cannot be posted. Every generated/edited/regenerated version is preserved, and approval identifies the exact version posted.
- (decision) Source-derived IA candidates: Agents admin area; Global Providers administration; hexa configuration/detail and lifecycle readiness; response mode and approver policy configuration; Conversation-originated invocation surface; proposal queue for authorized approvers; proposal detail/editor with version history, regeneration, approval, rejection, abandonment, expiry; operation/status surfaces for readiness, call outcomes, failures, posting outcomes; audit evidence surfaces for configuration, calls, proposal versions, approval decisions, and final messages.
- (assumption) Open UX-affecting PRD questions to resolve or carry as blockers/assumptions: conversation-originated invocation pattern (mention, command, action button, participant membership, mention resolution, or combination); proposal runtime/storage owner; default/configurable proposal expiry; approver notification path; latency target; cost controls; provider capability metadata; audit retention/export/deletion; safety filters/content policies; oversized conversation context behavior.
- (decision) Concern scan: UX carries accessibility, keyboard/focus/live region behavior through FrontComposer; tenant isolation and authorization fail-closed; privacy and provider-secret non-disclosure; audit/version preservation; proposal status clarity; conversation context boundaries; latency/status feedback; cost-control visibility; localization via FrontComposer shell/domain split; desktop-first responsive behavior; notification paths for approvers.
- (decision) Stakes classification: not regulated UX. Treat as internal/governed operational UX: preserve product-required authorization, approval, audit, versioning, privacy, and provider-secret safety, but do not add extra regulated-industry language burden beyond source requirements.
- (event) Draft DESIGN.md and EXPERIENCE.md generated from confirmed brief, PRD, FrontComposer references, Tenants implementation reference, and memlog decisions. Status remains draft pending reviewer gate, mock coverage decision, and resolution/carry-forward of UX-affecting open questions.
- (change) Reconciled final UX spines to the approved 2026-08-02 proposal, updated Architecture Spine, PRD, and launch-readiness register: Success now requires proven Agent callability or posted/projection-confirmed completion; Approved and posting pending remain progress; Provider Degraded is Warning and follows authoritative Callability; failed generation creates only a separate non-approvable failure record; high-risk pending scope is session plus resource plus operation family; WCAG 2.2 AA, whole-string EN/FR parity, and exact NFR-14 evidence are binding.
- (event) spines finalized
- (event) Validate run 2026-09-08: 4 lenses (rubric, accessibility, governance, implementation readiness); 92 findings (4 critical, 23 high, 38 medium, 27 low); prior 2026-06-23 run archived under .working/validation-2026-06-23/
- (event) Update run started 2026-09-08 with three change signals: UX validation 2026-09-08 (92 findings), PRD reconciliation 2026-09-08 (FR-29..FR-32, OQ-14..OQ-19, amended OQ-3/6/9/10, EXT-CONV-UI-1), and Story 5.3 pricing-governance/code drift. Scope mirrors the PRD update run: apply every confident fix including prior-decision revisions; defer only what needs a Product, Architecture, or Conversations decision.
- (change) Proposal lifecycle aligned to PRD FR-18 (2026-09-08) and ProposedAgentReplyState: Pending, Edited, Regenerated, Approved, PostingPending, Posted, PostingFailed, Rejected, Abandoned, Expired. Dropped 'generated' and 'pending approval' as states; PostingFailed is non-terminal with bounded audited retry; Expired renders on read whenever ExpiresAt has passed. Revises the 2026-08-02 lifecycle.
- (change) Approver Policy source 'Conversation owner' replaced by Conversation Facilitator per PRD OQ-14 / AD-8; the ApproverPolicySourceKind.ConversationOwner enum value is labelled Facilitator; no UI, audit, or API copy may imply a distinct Conversation owner.
- (change) Responsive rule revised: reflow to 320 CSS px (400% zoom) is required on every route including proposal detail, version history, and confirmation; narrow width alone never blocks a high-impact action; unavailability only when content cannot render, with a visible reason. Register NFR-13 wording preserved and clarified. Replaces the 2026-08-02 fail-closed viewport lockout.
- (change) Pending high-impact control uses aria-disabled and keeps focus; resolution moves focus deterministically (item status region, next row, page heading). Session for the advisory lock = authenticated user + tab/circuit; reload re-derives pending from the authoritative projection; second tab renders 'pending in another session'. High-risk families extended with ProviderCatalogMutation, AgentSetupMutation, AgentActivation from the register's OperationGateMatrix.
- (decision) Projection catch-up contract: poll every 250 ms up to 8 s after an accepted write; SignalR nudge triggers an immediate poll; exhaustion renders 'awaiting projection' with refresh, never Success. 'Stale' reserved for evidence past ValidUntil; projection lag renders 'catching up'. Every authoritative read exposes projection id and version. Architecture to pin these in the spine.
- (decision) Pricing governance UX (Story 5.3 drift): no default values; ISO 4217 currency must equal tenant budget currency or the row is status-important with reservations blocked; unit prices non-negative per FR-4 but zero must be typed and the confirmation warns it reserves nothing; pricing version pinned column, strictly increasing; pricing edits are ProviderCatalogMutation high-risk with old->new, effective version, future-only copy; CapabilityVersion is the concurrency token and stale/regressed submissions render 'superseded by another decision'. Secret values are never entered in the Agents UI; reference id only, operator-only, masked, never echoed.
- (decision) Surface binding: 12 nav entries with routes, RequiredPolicy (Agents.Administrator/Approver/Operator/AuditOperator), one FcPageLayoutMode per route, owning story, and contract. New routes /agents/context-policy, /agents/content-safety, /agents/cost-controls, /agents/audit-governance. Launch readiness moves to Agents.Operator (Priya). /agents/conversation-call is a pre-integration harness, de-listed from nav, removed before Story 6.7 closes. Conversation invocation has no Agents nav entry; seam is EXT-CONV-UI-1 with exported ConversationAgentCallPanel + IConversationAgentCallGateway.
- (decision) Component set is 22: added high-impact-confirmation (focus-trapped FluentDialog, initial focus on cancel, exact VersionId/kind/author/timestamp/content for approvals, future-only line, authorization basis, old->new pricing, provider-disable blast radius, safety re-check line), pending-command-indicator, and agent-response-marker (FR-11 AI-generated and FR-17 human-edited markers contributed to the Conversation surface).
- (decision) Build bindings: Fluent pin 5.0.0-rc.5-26219.1 via Directory.Packages.props; FcFluentIcons is the only icon source and missing glyphs are FrontComposer requests; BadgeAppearance Tint default, Filled for Danger and Severe; AA 4.5:1 text / 3:1 non-text; all grids are hand-authored FluentDataGrid in FcAggregateListPage with FcFilterEmptyState, FcFilterResetButton, FcExpandInRowDetail, FcStatusFilterChips (needs-my-action slot) and a visible status text column; FluentMessageBar for page notices only; FluentRadioGroup for response mode and version selection (no diff view in V1); Agents truth-flow helper is the pending pattern, FcPendingCommandSummary out; no command palette entries in V1; Agents owns one role=status and one role=alert node per route; localization via IStringLocalizer<AgentsResources>, keys Agents.<Surface>.<Item>, AgentsResourcesParityTests as the gate.
- (decision) Expiry: 'nearing expiry' starts at 10% of the configured window or 1 h before ExpiresAt, whichever is smaller; absolute culture-formatted timestamp plus static relative label refreshed at most once per minute outside live regions; announced once per proposal. Proposal expiry duration (24 h default, 1 h to 30 d, future-only) and the regeneration ceiling are fields on hexa configuration; cost caps and per-Party/per-Conversation rate limits live on Cost controls with no implicit defaults.
- (change) UJ-3 rewritten per PRD FR-7 segregation of duties: Anika requests one regeneration, views versions one at a time, and approves a version she did not author; approval is aria-disabled while the editor is dirty or when she last edited the selected version; approval-time safety re-check and lost-Conversation-access ('existence only') edge case added. Priya's flow retitled UX-J5 (UX-derived); UX-J6 added for Story 6.5 capacity backpressure. Story column added to Key Flows.
- (change) Removed the Browser Performance Evidence section and five NFR-14 restatements; one UX-owned statement remains in launch-readiness-panel with a reference to launch-readiness-register.md § NFR-14. UX Closure Decisions reduced to a pointer. Non-goals stated once in Foundation. Spine-only decision recorded: no mockups or wireframes by decision; spines are the sole reference.
- (assumption) Deferred with owners: Product to sign off the WCAG 2.2.1 essential-exception position on proposal expiry (no in-UI extension); Product + Security own OQ-18 unsafe historical content (UX shows blocked history only); Architecture to add ProviderReadinessResult (Story 5.5) and grow AgentCallOperationStatus (SafetyBlocked, BudgetBlocked, CapacityQueued, CapacityRejected, UnknownOutcome) and AgentReadinessStatus (ActiveNotProvenCallable, Stale, AuthorityUnresolved); Conversations to commit EXT-CONV-UI-1; FrontComposer glyph requests (inbox/queue, launch, shield, budget, policy, audit) and a localizable IBadgeCountService label; sprint-status.yaml regeneration against Epics 5-8.
- (decision) Mock coverage 2026-09-08: all 14 IA surfaces remain spine-only; no mockups or wireframes rendered. Rationale: FrontComposer and Fluent v5 inheritance plus the per-surface accordion/primary-region list in DESIGN.md make layout derivable from the spine tables; a visual reference would restate inherited chrome.
- (event) Reviewer Gate at Finalize skipped for this run: the four-lens validation of 2026-09-08 (rubric, accessibility, governance, implementation readiness) is the input being applied; re-running it is the recommended next step once Architecture and Contracts absorb the deferred items. Doc standards (bmad-review structure + prose) applied instead.
- (event) Doc standards applied 2026-09-08 (bmad-review structure + prose): 28 structure and 30 prose fixes across both spines; DESIGN.md 34.6 KB -> 28.2 KB (behavioral duplication removed, 22 component sections kept as visual bindings), EXPERIENCE.md 59.3 KB -> 58.5 KB. Resolved the proposal-queue sort drift in favour of the EXPERIENCE rule and removed DESIGN's duplicate NFR-14 sentence. French column kept with AgentsResources.fr.resx as the enforced source. Component Patterns column schema left as is by decision.
- (event) spines finalized
- (event) Validate run 2026-09-09 (re-validation after the 2026-09-08 Update): 4 lenses (rubric, accessibility, governance, implementation readiness); 84 findings (0 critical, 17 high, 37 medium, 30 low), down from 92 with 4 critical. Regression: 85 prior findings resolved, 14 partial, 0 unresolved. Rubric verdicts: component coverage, visual reference, shape fit strong; flow, token, state, bloat, inheritance adequate. Convergent items: nearing-expiry threshold missing from both spines (all 4 lenses), FcFluentIcons status glyphs wrongly filed as requests, FcAggregateDetailPage has no Heading params, IBadgeCountService Type-keyed, live-region duplication, Conversations seam gaps (callability query, marker slot, epics/register alignment), Appearance.Accent v4 name. Pre-update run archived under .working/validation-2026-09-08-pre-update/.
- (event) Update run started 2026-09-09 against the 2026-09-09 re-validation (84 findings: 0 critical, 17 high, 37 medium, 30 low). Scope mirrors the 2026-09-08 precedent: apply every confident fix including prior-decision revisions; defer only what needs a Product, Architecture, Security, or Conversations decision, with named owners.
- (change) Icon inventory corrected against FcFluentIcons.cs: two access paths stated (12 TryCreate string names; 13 Size16 factory methods unreachable through TryCreate). All seven status roles now bind to existing factories (CheckmarkCircle16, ArrowSync16, Warning16, SubtractCircle16, DismissCircle16, QuestionCircle16, InfoCircle16), so no status glyph is a FrontComposer request. DevMode/DeveloperBoard named as one glyph. Audit evidence and Audit governance render label-only rather than reusing Operational status's Search. Revises the 2026-09-08 build-bindings decision.
- (change) IBadgeCountService dropped for V1: Type-keyed over projection runtime types, shell-rendered without a label slot, and Agents runs no generated projection lane. The pending count is domain-rendered inside the Agents overview link as a whole string; the shell nav count is suppressed. A localizable shell badge label is a FrontComposer request. Revises the 2026-09-08 build-bindings decision.
- (change) Live-region rule rewritten from 'one role=status and one role=alert node per route' to 'one Agents-owned node of each politeness per route, and every event announced by exactly one node', with AgentsPageStatusRegion as the named carrier, an explicit list of which Fc nodes may speak, FluentMessageBar AriaLive=Off when duplicated, connection-lost shell-announced, and ConversationAgentCallPanel carrying its own pair. Queue expiry announcements aggregated once per poll cycle.
- (change) Route-heading rule split: FcAggregateListPage takes Heading/HeadingTabIndex/PageTitle as parameters; FcAggregateDetailPage has none, so the eight Constrained detail routes render a domain-supplied FcPageHeader in every state slot, and every detail route supplies a localized BackLinkLabel or sets ShowBackLink=false. Detail routes bound to FcAggregateDetailState with shell Stale explicitly unused.
- (change) Provider catalog authorization split: Agents.Administrator reads, and every ProviderCatalogMutation requires the new platform-scoped Agents.PlatformProviderAdministrator; mutation controls are absent, not disabled, without it. Currency circularity resolved: the tenant budget currency on Cost controls is authoritative and the catalog validates ISO 4217 well-formedness only. Pricing version is server-assigned.
- (change) Approved, once projection-confirmed, closes the decision: edit, regenerate, and approve are absent and ApprovedVersionId is pinned read-only; only retry posting, abandon where permitted, and audit remain. retry posting is fully specified as a governed Conversation write (Agents.Approver with current Conversation read access, family ProposalResolution, bounded audited attempts, configured maximum on hexa configuration).
- (change) Response-mode toggle edits a draft; an explicit Apply opens the confirmation. Generalised: a confirmation is never opened by a selection event. Version radios are DisabledFocusable while the editor is dirty. Spine-wide binding rule added: aria-disabled always means FluentButton.DisabledFocusable or a focusable hand-authored equivalent; Disabled and Loading are never used for a blocked-but-explicable state.
- (decision) Nearing expiry threshold carried into both spines: 10% of the configured window or 1 hour before ExpiresAt, whichever is smaller, floored at 15 minutes. Client-computed from ExpiresAt, the configured window, and the server read time; never the browser clock. The 15-minute floor is what the WCAG 2.2.1 essential-exception argument now rests on. Expired is likewise compared against server time.
- (decision) EXT-CONV-UI-1 widened to two artifact kinds: the Call hexa action contribution and a per-message decoration slot keyed by MessageId for agent-response-marker, plus an Agents-side provenance accessor. Seam must add GetCallabilityAsync(tenant, conversation). Conversations owns the trigger button; ConversationAgentCallPanel is the dialog body and is self-contained for live regions and post-submit focus.
- (decision) New EXPERIENCE.md sections: Confirmation contents by family (per-family required contents for all nine high-risk families, the rendered set being what audit carries) and Shipped-code corrections (eleven known src/Hexalith.Agents.UI divergences mapped to absorbing stories 5.3/5.7/6.7/7.1/7.2/7.3/8.6). Audit evidence list declared an id-entry surface, not a grid, so the mandatory FC-TBL set applies to two grids only.
- (decision) Governance writes (LegalHold, ExportRequest, DeletionRequest, PolicyPublication, TenantBudgetUpdate) require a justification typed into the confirmation before Confirm enables. Deletion renders exact scope, hold state, confirming projections, and an irreversibility line, and is blocked while a matching hold is active or EXT-SECRETS-1 is not Available. No type-to-confirm anywhere. Two-person rule deferred to Product + Security.
- (assumption) Deferred with owners (reconcile-validation-2026-09-09.md section 4): Product + Security own the two-person deletion/hold-release rule and OQ-18; Product owns the WCAG 2.2.1 expiry position, the regeneration ceiling numbers (UX proposes default 3, range 1-10), and PRD FR-18 seven-state vs ten-value enum alignment; Conversations owns EXT-CONV-UI-1; the epics/register maintainer owns the Story 6.7 External line and consuming-story contradiction; Epics owns Story 6.2 and the Operational status extras; Architecture owns ProviderReadinessResult with a by-Provider/model query, AgentCallOperationStatus growth including RateLimited and the posting tokens, and the IProjectionChangeDetailNotifier nudge contract; FrontComposer owns six nav glyphs, domain-keyed FcStatusFilterChips slots, and a localizable shell badge label.
- (event) Update run 2026-09-09 applied all 84 findings: 17 high and 37 medium and 30 low dispositioned; 3 low noted-only by decision (frontmatter note-object convention per the Tenants precedent, Component Patterns cell density, Fluent MCP build note). 12 prior decisions revised. Dispositions and deferrals in reconcile-validation-2026-09-09.md.
- (event) Reviewer Gate at Finalize skipped by user decision for this run: the 2026-09-09 four-lens validation is the input being applied, so re-running it would mostly re-read this run's own edits. Re-validation is the recommended next step once Architecture and Contracts absorb the deferred items.
- (event) Doc standards applied 2026-09-09 (bmad-review structure + prose). Structure lens found the 84-finding absorption had grown EXPERIENCE.md by duplication, not content. Applied: new '## Known gaps' section after Key Flows holding the lifted 'Contracts that must grow' table plus Shipped-code corrections with its Rule column reduced to section pointers; new '### Grid rules' and '### Proposal editor action rail' subsections absorbing the shared grid rules and the 357-word proposal-editor cell; nearing-expiry threshold, IBadgeCountService rationale, confirmation contents, the nine-family roster and the seam live-region rules each stated once; Inspiration and Anti-patterns moved after Foundation; DESIGN.md high-impact-confirmation and proposal-notification cut back to visual delta; three-way component parity rule added to the DESIGN Components preamble. Prose lens found a real defect: a three-cell row in a two-column table silently dropped the tenant-scoped search rule and the no-palette-entries rule from rendered output; fixed. Plus sentence splits, Enable/Activate and hand-rendered/domain-rendered reconciled across the peer documents, three British spellings, old-to-new hyphenation. Final: DESIGN.md 3849 words / 31.8 KB, EXPERIENCE.md 12463 words / 87.0 KB.
- (event) spines finalized
- (event) Validate run 2026-09-09 (second run of the day, after the 2026-09-09 Update applied all 84 findings): 4 lenses (rubric, accessibility, governance, implementation readiness) with regression reporting against the pre-Update run. Prior review set archived to .working/validation-2026-09-09-pre-update/.
- (event) Validate run 2026-09-09 (post-Update) complete: 96 findings (7 critical filings / 6 distinct, 35 high, 33 medium, 21 low). Regression vs the 84-finding pre-Update run: 57 resolved, 23 partial, 2 unresolved, 0 regressed, 2 unchanged by decision. Rubric verdicts: token, component, visual-reference and shape fit strong; flow, state and bloat adequate; inheritance discipline THIN. Blocking discovery: prd.md carries a 2026-09-09 reconciliation (FR-33 six-role authorization matrix, OQ-20 to OQ-22, A-9 to A-13, A-17) that the 2026-09-09 Update never read; both spines still say 'reconciled to the PRD of 2026-09-08' and their five RequiredPolicy constants contradict FR-33 on six rows. Found independently by the rubric and governance lenses. Other criticals: retry posting filed under ProposalResolution, which skips the membership and safety gates; Content Safety Policy publishable and relaxable from a tenant-scoped surface; cost caps and the cap override self-set by the party they bound; two-person deletion deferred though prd.md A-12 already decided it; ConversationAgentCallPanel live-region pair destroyed by the Submit it announces. Update-introduced defects also found: no Save/Discard in the proposal editor rail though Approve and the version radios block until saved or discarded; DisabledFocusable does not exist on FluentRadio; no aria-describedby on any blocked control; shipped UI has no CSS behind ~115 BEM classes; the two Agents.Surface.NotAvailable keys do not exist while the forbidden per-cause keys do.
- (change) Architecture update 2026-09-09 (AD-30) binds the Platform Operator policy name Agents.PlatformOperator for every platform-scoped FR-33 row; the three EXPERIENCE.md occurrences of Agents.PlatformProviderAdministrator were renamed to it so UX and spine share one role vocabulary. No other UX text changed.
- (direction) Architecture reconcile 2026-09-09 (reviews/review-2026-09-09-reconcile-ux.md in the architecture run folder) found UX drift against the final PRD and the spine to absorb at the next UX update: approval blocked for the caller unconditionally (FR-13/AD-8); Caller approver source rendered as a rejected legacy value (FR-23); content-safety publication split into tenant restrictions (Agents.Administrator) and platform publication (Agents.PlatformOperator, Security approval, ChangeKind); initial configuration or raise of caps and rate limits gated Agents.Operator or Agents.PlatformOperator, lowering Agents.Administrator; removal of hexa detected lazily at the next membership step; Expired and nearing-expiry apply only to awaiting-decision states; no tokenizer approximation; projection id agent-setup; posting-retry bound is fixed by A-7, not a configuration field; posting retry attempts are attempt rows, not versions; Disable has kill-switch semantics for proposals; missing surfaces for TenantKillSwitch, TenantProviderEnablement, administrative retry/abandon of exhausted PostingFailed, and DeletionRequest by Platform Operator with Compliance Inspector approval; five policy constants including Agents.PlatformOperator; EXT-CONV-UI-1 counted as three artifact kinds.
- (change) Update run 2026-09-09 (post-validation) applied the FR-33 authorization model across both spines: new EXPERIENCE section Authorization roles binding the six PRD roles to constants; RequiredPolicy re-derived row by row (9 of 15 IA rows changed); Agents.AuditOperator retired, Agents.ComplianceInspector added, Agents.PlatformOperator confirmed as the platform constant. Cost controls split into an operator tier and a tenant lower-only tier with a Platform-Operator-only override; Content safety split into platform publication with a Security approval stage and a tenant stricter-only delta; Provider catalog read scoped to tenant-enabled models with secret-derived reason codes collapsed to Blocked, and a zero unit price made a readiness blocker. Eligible Approver predicate carried in full (five conjuncts) plus the FR-7 edit-time check; ApproverPolicySourceKind.Caller retired. FR-18 fixed: Expired scoped to the awaiting-decision states with approval freezing expiry, the four exits from an exhausted PostingFailed including two administrative ones needing no Conversation read access, retry posting moved to family ConversationPosting, the A-7 bound restored as Architecture-owned, four typed Abandoned reasons, and three new pre-Provider rejection states. Kill switch added to Launch readiness and per-Conversation block/re-admit to Operational status; denial evidence added. Accessibility: Save and Discard added to the rail, version radios never disabled, aria-describedby required on every blocked control, EXT-CONV-UI-1 given a third artifact kind (persistent status region) plus a focus-return contract, politeness table completed, speaker list closed, focus follows a state-slot swap, WCAG 2.2.1 re-argued as an essential exception on its own merits. Shipped-code corrections grown from 11 to 17 rows including the no-CSS and missing-resx-key divergences. Dispositions and deferrals in reconcile-validation-2026-09-09-2.md.
- (event) Doc standards 2026-09-09, structure lens only (bmad-review lenses=structure, both spines reviewed together; prose lens not yet run). Model: Reference/Database; diagnosis was that the FR-33 Update deposited Pyramid-genre rationale inside reference tables (35 'because' clauses, 18 'would'-conditionals), and that roughly 1000 of the 1860 recoverable EXPERIENCE words are single rules stated two to four times. 24 recommendations returned; user accepted the two defects plus the two zero-cost wins. Applied: (1) the FcFilterResetButton contradiction the Update introduced - Grid rules said 'not used, Agents owns the reset control' while List/detail/deep-link surfaces still mandated it, so a story-dev entering at either shipped the opposite control; resolved in favour of Grid rules. (2) DESIGN proposal-editor's action-rail inventory, stale at six actions after Save and Discard were added, replaced by a pointer so the list has one home. (3) Accessibility Floor given real substructure: four plain-text pseudo-headings promoted to Live regions, Route heading and focus, Forms, Other criteria; a verbatim doubled LR-UI-CONFORMANCE sentence removed. (4) The eight heaviest Component Patterns rows (2684 words, 57 percent of that section) extracted into descriptive ### subsections - Provider catalog, Proposal queue, Approver policy, Proposal editor, Operational status, Launch readiness, Audit evidence, Audit governance rules - each table row reduced to a one-or-two-sentence binding plus a pointer, following the precedent of Grid rules and Confirmation contents by family. Net EXPERIENCE.md 17625 to 17945 words (+320, +1.8 percent): the extraction cost words rather than the lens's predicted zero, buying navigable substructure over previously unscannable 300-to-500-word table cells. Parity 22/22/22, 19 token refs and every new pointer resolve, no malformed rows. Declined for now and recorded in the structure findings rather than lost: FR-33's three homes and the rest of the ~1000 words of duplication, DESIGN's ~460 words of behavioural leakage, the French column question, the 197-word 2.2.1 argument, and cutting Inspiration and Anti-patterns.
- (event) Doc standards completed 2026-09-09, prose lens (bmad-review lenses=prose, both spines together, on top of the structure pass). 16 recommendations returned, all 16 applied by user decision. Two were stale numerals the FR-33 Update left behind, each contradicting a table in the same file: five sites said 'nine' high-risk/confirmation families while the roster carries ten (the Update added ConversationPosting when it moved retry posting out of ProposalResolution), and FrontComposer Readiness said 'Four policy constants' while Authorization roles names five. Four British spellings regressed in with the Update (colour x2, unlabelled x2; DESIGN's nearing-expiry sentence and the proposal-notification cell each carried both spellings of one word). One dropped-word typo in the FR-13 listing predicate ('resolved as an Approver on under'). One sentence in Provider catalog rules gave the same instruction twice ('the confirmation names the platform scope' ... 'and it names the platform scope of the action') with three drifting 'it's. Plus the Agent-call-permission parallelism break, a gendered pronoun on a hypothetical role-holder, 'an admin revoking an approver' in the one section whose subject is the FR-33 role vocabulary, two sentences opening 'The audited override' in one cell, three sentence splits (compliance-inspection chain, deterministic focus rule, superseded row), an unqualified DESIGN section pointer into EXPERIENCE, en-dash story ranges, straight quotes, and a Component-Patterns pointer sharpened to Provider catalog rules after the structure pass moved the rule. DESIGN.md 4306 to 4308 words; EXPERIENCE.md 17945 to 18007 (+62, the AD-30 note plus the three splits). Parity 22/22/22, 19 token refs and every section pointer resolve, no malformed rows.
- (change) Policy-constant naming corrected 2026-09-09: `Agents.ComplianceInspector` reverted to `Agents.AuditOperator` across EXPERIENCE.md (Authorization roles row, the register-constants bullet, the Audit evidence and Audit governance IA rows, Audit evidence rules, Shipped-code corrections). AD-30 is the naming authority and reads 'FR-33 roles map one-to-one to FrontComposer policies Agents.PlatformOperator (Platform Operator), Agents.Administrator (Tenant Agent Administrator), Agents.Approver, Agents.AuditOperator (Compliance Inspector), and Agents.Operator (Release Operator)' - the role label was renamed, the wire identifier was not, exactly as AD-8 keeps ApproverPolicySourceKind.ConversationOwner labelled Conversation Facilitator. prd.md FR-33 names the six roles and no constants at all, so the 2026-09-09 Update had no source for coining one; `Agents.ComplianceInspector` existed nowhere outside these UX files except as an unadopted recommendation in prd-agents-2026-06-23/review-implementation-drift-2026-09-09-update-gate.md, which also proposed `Agents.ReleaseOperator`, a name neither AD-30 nor the shipped registration uses. This reverses the 'Agents.AuditOperator is retired; FR-33 has no role for it' decision recorded in the Update run and in reconcile-validation-2026-09-09-2.md section 1. The Shipped-code corrections row was inverted by the same error and is corrected: the registration declares four of the five correct constants and is missing only `Agents.PlatformOperator`. UX keeps 'Compliance Inspector' as the rendered role label everywhere. Raised and settled with the concurrent architecture session (agents-3a) against the AD-30 text, not by assertion.
- (assumption) Deferred to Product/PM, raised by the UX side and logged by the architecture session: add a role-to-policy-name column to the PRD FR-33 table. FR-33 currently names roles only, so a PRD-only reader has no way to reach the constant vocabulary AD-30 owns, which is exactly how the ComplianceInspector collision above was reachable.
- (event) Validate run 2026-09-09 (third run of the day, after the FR-33 Update, the prose pass and the AD-30 policy-constant revert): 4 lenses (rubric, accessibility, governance, implementation readiness) in parallel, each with regression duty against its own archived predecessor. Prior review set archived to .working/validation-2026-09-09-post-fr33/ via git mv. Result: 101 findings (11 critical filings / 9 distinct, 35 high, 38 medium, 17 low) against a 96-finding baseline. Rubric verdicts: token completeness, visual reference coverage and shape fit strong; flow, component, state and bloat adequate; inheritance discipline THIN. Regression across all 96 baseline findings: 58 resolved, 24 partial, 5 unresolved, 3 REGRESSED, 5 unchanged by decision, 1 re-filed.
- (event) Answer to the regression question: the FR-33 Update did NOT land cleanly. It landed the authorization model and left the plumbing describing the pre-change world, in the same pattern as the Update before it - a set, rule or name changed and the counts, rosters, cross-references and downstream statements that described the old state left standing. Confirmed a dozen times over: 'the four FR-18 exits' against six, 'the six-part Provider eligibility set' against seven, 'this four-name list' against two (verified pre-Update, an inherited miss), and 'the ten families' against a roster the Update itself grew past - it declared the kill switch, the per-Conversation block, administrative abandon and administrative retry to be high-impact-confirmation families and added a contents row for none, so four justification-gated dialogs have no specified body and, since the rendered set is the audit record, no specified audit record. Plus a seam bullet still asserting Conversation-owned removal two paragraphs after the section above it made removal Agents-owned, UX-J5 walking one protagonist through three FR-33 roles without naming one, and both shipped-drift inventories the Update rewrote to fix a counting error still miscounting and each omitting the file the original finding named (live regions: row says 15 sites in 13 files, tree holds 29 in 20, ProviderCatalog.razor absent; Disabled: row says eleven pages, enumerates four, tree holds 15 sites in 5 files, ProposalRegenerator.razor absent).
- (event) Three baseline findings REGRESSED, all the same failure - a rendering that was missing became a rendering that is wrong. (1) The Abandoned enumeration landed with a fourth typed reason, kill switch, which FR-18 and AD-12 state can never be a system-abandonment reason. (2) The Audit evidence IA cell landed granting posted provenance - Conversation-derived content - to Agents.Administrator, Agents.Operator and Agents.PlatformOperator without membership or read access, contradicting its own rules section, the FR-33 row and AD-22. (3) The version-radio fix, having correctly established and MCP-verified that DisabledFocusable is absent on FluentRadio, replaced it with a rule that opens a modal confirmation on every arrow keypress in a native FluentRadioGroup while the editor is dirty - readmitting as a named exception the exact defect the Update's own 'a confirmation is never opened by a selection event' generalisation had just closed. WCAG 3.2.2, and the dialog carries no family so no focus-trap, Esc or focus-return contract reaches it.
- (event) Two root causes found beneath the individual findings. (1) The Update reconciled the FR-33 table and treated it as the whole 2026-09-09 PRD revision, which also rewrote FR-3, FR-4, FR-18, FR-28 and FR-30 and added OQ-24 through OQ-30; all seven OQs return zero hits in both spines, and 16 of the rubric lens's 20 new findings trace to it. Consequences include the kill-switch confirmation contradicting FR-28/OQ-27, export and legal-hold release losing the FR-30 second-party approval that deletion gained, Suspended and Draft having no state or badge role, and DataHandlingVersion acceptance (FR-4, OQ-29) having no surface anywhere in the pair. (2) The mirror image in the epics: epics.md carries 33 stories and the pair names 28. Stories 5.8, 5.9, 5.10, 6.8 and 7.7 appear nowhere, and three own precisely what the Update added - 6.8 the pre-Provider rejections the spine assigns to 6.3/6.4/6.5, 7.7 the entire PostingFailed recovery lane including the pre-exit MessageId read and LateConfirmed, 5.10 the Caller retirement assigned to 5.4. The per-tenant kill switch and the Security approval stage have no story with an AC that would build them; the Security approval stage additionally contradicts AD-30, where it is a recorded reference on the PolicyPublication command rather than a pending stage.
- (change) The invented-constant failure the AD-30 revert corrected was not the only one of its kind. The Update also invented KillSwitchActive, a token appearing in no source artifact; Architecture's token for the same runtime consequence is Suspended (AD-12), which has no row in either spine, and Story 6.8's closing AC explicitly bars inventing further additive enum members. Same class as Agents.ComplianceInspector, caught twice in one day.
- (assumption) Highest-severity open item for the next Update: Agents.PlatformOperator is registered nowhere and no route is gated on it, so every platform-scoped operation falls back to Agents.Administrator - two lines after section Authorization roles forbids falling back to a more permissive row, on an already-shipped page, with no fail-closed interim (unlike EXT-SECRETS-1, where the interim is stated). Filed critical by governance and medium by implementation readiness, which additionally found that the absorbing stories 5.3 and 5.7 have no AC that would register a constant or regate a surface, and that the row carries no RQ-1 or LR-TENANT-ACCESS linkage. Both spines still say status: final; on the authorization model, the lifecycle vocabulary and story ownership they are not.
- (event) Update run started 2026-09-10 (fast path, autonomous, reviewer gate skipped by user decision) with four confirmed inputs: (1) validation round 3 of 2026-09-09, 101 findings / 11 critical filings / 9 distinct / 3 regressed; (2) root cause 1, the rest of the 2026-09-09 PRD revision beyond FR-33 - FR-3, FR-4, FR-18, FR-28, FR-30 and OQ-24 to OQ-30, all seven OQs at zero hits in both spines; (3) root cause 2, epics.md 33 stories against the pair's 28, with 5.8/5.9/5.10/6.8/7.7 absent; (4) architecture round 4, UPDATE-REPORT-2026-09-09-2.md, AD-7/AD-12/AD-13/AD-22/AD-30 rewrites plus the already-logged reconcile-ux drift list. Convention unchanged from the three prior runs: apply every confident fix including prior-decision revisions, defer only what needs a Product, Architecture, Security, Conversations or Epics decision, with named owners.
- (event) Scope correction discovered during extraction: the architecture spine had moved to round 5 (ARCHITECTURE-SPINE.md updated 2026-09-10, architecture_assumption_index_version 5) while this run was starting, not the round 4 the user scoped. Round 5 rewrote AD-5, AD-12, AD-13, AD-22 and AD-30 again and renamed AgentCallOperationStatus to AgentInteractionStatus everywhere, so anything absorbed verbatim from UPDATE-REPORT-2026-09-09-2.md alone would have been stale on arrival. reviews/review-2026-09-09-reconcile-ux.md is round-3-era and now asserts two things that are no longer true; it was not used as a token source. Second correction: the 33-vs-28 story gap is real but is not a miscount of one list - epics.md holds two tiers, Epics 1 to 4 being 26 completed historical stories the file itself declares non-executable, and the active forward backlog being exactly 33.
- (change) All three 2026-09-09 regressions closed. (1) Abandoned reverted from four typed reasons to the three of Story 6.8 AC-1 plus FR-18's PostingWindowElapsed, with the invented kill-switch reason deleted against Story 7.7 AC-4 and AD-12. (2) The Audit evidence IA cell rewritten into three tiers that never merge, so posted provenance reaches a current Participant with read access only and no Agents role constant reaches Conversation-derived content without membership. (3) The version-radio rule replaced by the response-mode-toggle shape - the radio group edits a draft selection and an explicit View version command control applies it - withdrawing the named exception to 'a confirmation is never opened by a selection event', and the unsaved-changes dialog given an explicit contract under family EditorDraft.
- (change) All six distinct criticals closed. Route gating rule added: a surface carrying two authorization tiers is gated on the disjunction, each control on its own constant, because gating on the tenant constant alone denied the page to the Platform Operator whose control it carried. The Agents.PlatformOperator drift row gained the fail-closed interim it lacked, linked to LR-TENANT-ACCESS and RQ-1. Kill-switch confirmation restated to FR-28/OQ-27/AD-12 in full, with Approved held rather than completing - OQ-27 records that as a deliberate call that safety wins. LegalHoldRelease and ExportRequest given the same request/approve/execute staging DeletionRequest had. Compliance inspection rewritten from prose into four enforced mechanics. The conversation-agent-call panel now owns no live-region nodes at all, in both spines.
- (change) Root cause 1 absorbed - the rest of the 2026-09-09 PRD revision. Three new EXPERIENCE sections: Agent lifecycle and tenant suspension (lifecycle Draft/Active/Disabled and tenant Suspended as two orthogonal axes never collapsed into one badge, with a per-state degradation table for both stop conditions and the one accounting difference between them); Conversation Agent State (five states, BlockVersion, MirrorPending informative and MirrorRefused terminal-and-actionable with exactly two remediations, ReadmitPending meaning a clear never renders as re-admitted); and Provider data handling (four-field record, DataHandlingVersion, DataHandlingAcceptanceLapsed, immediate block by default with a 30-day grace only for a declared tightening change carrying a recorded field-level diff, and a DataHandlingAcceptance family with both Accept and Decline). Plus FR-12's Indeterminate replacing the cost editor's Unknown reservations with all four dispositions including Unreconciled; the full launch-readiness blocker vocabulary in three provenance classes; PostingPending uninterruptible with no renderable countdown; LateConfirmed, PostingWindowElapsed and the pre-exit MessageId lookup with three outcomes; and OQ-28 provisioning with no Create or Delete control for any tenant role.
- (change) Root cause 2 absorbed - story ownership re-derived against the 33-story active backlog. Pre-Provider rejections: 6.8 owns the contract presence and the unknown-value fail-closed rule while 6.2/6.3/6.4/6.5/6.6/7.1/7.5 own the semantics, a split recorded rather than resolved because epics.md dual-attributes it. PostingFailed recovery is 7.2 to 7.7, itself split three ways - 7.4 builds the seam-2 MessageId lookup, 7.5 consumes it for abandon, 7.7 owns retry and the ten-state parity source. Caller retirement is 5.4 and 5.10, not 5.4 alone. Story 6.6 added to Operational status for the membership state vocabulary the pair rendered nowhere.
- (change) Architecture round 5 absorbed. AgentCallOperationStatus renamed to AgentInteractionStatus at three sites. Automatic-path retry reversed: the pair said a new Agent Call was the sole recovery, AD-5 now gives the automatic path the same bounded retry and substitutes any current Conversation Facilitator wherever FR-18 names an Eligible Approver. KillSwitchActive deleted as invented - zero occurrences in the spine, both registers, prd.md and epics.md - and replaced by the tenant state Suspended plus the FR-8 step-2 typed rejection TenantSuspended; this is the second invented constant after Agents.ComplianceInspector, caught by the same check, and AgentInteractionStatus growth is confirmed at six sanctioned members with the reason codes explicitly not enum members. Block-clear authority split into AD-7's three distinct rules. agent-setup-readiness corrected to agent-setup. Justification widened from FR-30's five families to AD-22's all nine with no exemptions. Security approval rebound from the spine's invented two-stage acceptance sequence to AD-30's recorded reference on the PolicyPublication command, since the stage had no acceptance token, no authorization constant and no AC.
- (change) Six claims verified directly against src/Hexalith.Agents.UI rather than taken from the report, three of which the report itself had wrong. Live regions are 29 sites across 20 .razor files, not 15 across 13, with ProviderCatalog.razor absent from the row and three .cs matches identified as XML doc comments and excluded so a later verifier does not re-inflate the count. Disabled is 15 sites across 5 files, not 'eleven pages', with ProposalRegenerator.razor added and the two enum-member matches excluded. The aria-describedby binding - flagged as the most load-bearing new accessibility rule with no verified binding - is resolved empirically: data-testid is already splatted onto FluentButton in eight files, which only compiles because unmatched attributes are captured, so a plain attribute is the binding and no typed parameter is needed. The Constrained viewport lockout is confirmed in source and filed as a new drift row: ProposalApprover.razor renders Approve only under CanApprove and not Constrained and replaces it with a focusable paragraph otherwise, AgentCallStatusFeedback.razor does the same to the Call action, and both carry a self-referential aria-describedby - a WCAG 1.4.10 failure on the product's highest-consequence action.
- (assumption) Deferred with named owners (reconcile-validation-2026-09-10.md section 8). Architecture owns six: whether ConversationPosting is lock-bearing (it appears in neither AD-12's nine-family roster nor its non-lock-bearing trio); the fact that administrative retry has no valid family at all, since ConversationPosting's gate set requires Conversation membership the administrative exit explicitly does not need; CapacityQueued queue position, which AD-24 does not bind; CurrencyMismatch being spine-named but absent from the register enum AD-10 makes binding; the twice-deferred AD-10 EntryMissing versus AD-2 wording question; and ARCH-A-13's fourth Approver exclusion, which has no FR-7 basis and whose copy is marked provisional. Architecture and Conversations own the EXT-CONV-UI-1 amendment to four artifact kinds plus the focus-return contract. Epics owns five, two of them structural: the per-tenant kill switch has no story that builds it, and the Security approval stage on Content Safety Policy publication has none either - 'Security approval' returns zero hits across every AC, OwnedClauses and test artifact in epics.md, which is notable because the file models two-party approval carefully in 8.1, 8.2, 8.3 and 8.8. Product owns the A-13/A-17 threshold confirmation by 2026-09-30 and the FR-33 role-to-policy-name column carried from the previous run.
- (event) Update run 2026-09-10 applied the round-3 validation in full: 3 regressions and 9 distinct criticals closed, plus the highs, mediums and lows reachable without an external decision; everything else deferred with a named owner rather than guessed. Post-edit checks: component parity 22/22/22 across DESIGN frontmatter, DESIGN sections and EXPERIENCE rows; all 8 design-token references resolve; zero malformed table rows in either file; every stale string on the report's grep list now returns zero, and all 18 previously zero-hit contract tokens are present. Both spines remain status: final, updated 2026-09-10 - the rubric lens's condition for that was the three regressions and the criticals, which are closed.
- (event) Doc standards 2026-09-10, structure lens (bmad-review lenses=structure, both spines together). Model: Reference/Database. Diagnosis: the two failure patterns prior passes named are both present and both worse after this round - the three new sections each restate material that already existed without the older statements being reduced to pointers, and rationale prose leaked into cells sized for bindings. 22 recommendations; applied the ones naming defects rather than preferences. (1) The PostingFailed lifecycle row still listed four exits while the action rail rewritten hours earlier listed six - duplication that drifted inside one cycle, which is the argument for giving the exit inventory one home. (2) A verbatim duplicated parity-gate sentence inside one table. (3) Six clauses in which the documents narrated their own revision history, all six written this run; a spine that wins on conflict states what is true now and the history belongs in the reconcile document, and left alone this accumulates every Update run. (4) Three DESIGN component sections had started carrying behavioral vocabulary against their own opening rule that each section is the visual binding only. (5) The disable/suspend degrade matrix stated four times, reduced to the new canonical section plus pointers. Declined and recorded rather than lost: the large relocations - five heavy Component Patterns rows into ### sections, moving Confirmation contents by family and Provider data handling out of that section, DESIGN frontmatter/section column duplication, and a reader map - real but structural reorganizations with cross-reference surface, and the same class was declined on the two prior runs. EXPERIENCE 25512 to 25315 words, DESIGN 4888 to 4837.
- (event) Doc standards completed 2026-09-10, prose lens (bmad-review lenses=prose, on top of the structure pass). 12 findings, all applied, and the lens earned its place: nine were defects this Update introduced. The worst was a broken list - the eligibility-set rewrite stranded 'unconfigured cost cap, and unconfigured rate limit' after an unrelated sentence, leaving a fragment where two activation blockers should be. Three stale numerals against tables edited in the same run: 'the nine states' against thirteen Agent readiness rows, 'two provenance classes' against three, 'the six FR-28 effects' against seven. A dropped predicate left a sentence with no verb. A garbled negation in the kill-switch confirmation - 'never system-abandons and deletes no proposal' - inverted the meaning the same rule states correctly 170 lines later. A dangling 'states that' from the degrade-matrix pointer edit. A broken DESIGN cross-reference to a heading that does not exist, since cost-control-editor is a Component Patterns row and not a section. Plus two British spellings, a forward-pointing 'additionally', a participial clause reading as the rule rather than the failure it avoids, and a six-exits sentence that added a seventh in the next breath. The lens independently re-verified sixteen counts as correct, including the 29-in-20 and 15-in-5 shipped-code figures this run had corrected against the tree. Final: DESIGN 4842 words, EXPERIENCE 25317. Parity 22/22/22, all token refs and section pointers resolve, zero malformed table rows, zero britishisms.
- (event) spines finalized
