---
scope: agents workspace
purpose: build-substrate architecture spine only
altitude: initiative
updated: 2026-09-09T02:36
---

- (constraint by user) Scope is the agents workspace under /home/administrator/projects/hexalith/agents.
- (constraint by user) Deliverable is the architecture spine only; no companion deck, report, C4 set, or human-facing solution design is requested now.
- (direction) Use Coaching path: decisions are elicited and challenged before drafting.
- (assumption) Altitude starts as initiative because the scope is the whole agents workspace and should bind module/feature work without specifying per-story detail.
- (constraint) Brownfield reality: the root agents repository is a workspace/super-repo of Hexalith modules; each module owns its own .slnx, build props, package versions, tests, docs, AppHost/deployment posture, and release rules.
- (constraint) Brownfield reality: root-level submodules and sibling MSBuild path discovery are the workspace dependency mechanism; recursive submodule initialization and casual edits to submodule contents are forbidden by existing module contexts.
- (constraint) Brownfield reality: Hexalith.EventStore is the shared command/query/event substrate; Tenants owns tenant lifecycle and access state; Parties owns party identity and PII; Memories owns knowledge/search backends; FrontComposer owns generated/admin UI contracts; Conversations consumes these as bounded-context dependencies.
- (constraint) Source input: final PRD at _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md defines V1 Hexalith Agents requirements for governed hexa participation in Conversations.
- (constraint) Source input: final product brief at _bmad-output/planning-artifacts/briefs/brief-agents-2026-06-23/brief.md defines hexa as first named Agent, explicit conversation invocation, Party attribution, response policy, approval workflow, and V2 deferrals.
- (constraint) Source input: UX draft at _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/EXPERIENCE.md and DESIGN.md binds FrontComposer inheritance, admin/workflow surfaces, proposal state distinctions, and unresolved conversation invocation pattern.
- (question) Architecture-owned PRD questions include Proposed Agent Reply runtime/storage ownership, latency target, cost controls, provider capability metadata, oversized Conversation Context policy, and audit governance boundaries.
- (decision by user) Hexalith Agents is a full EventStore-backed domain module: durable Agent configuration, provider governance, Agent calls, Proposed Agent Reply lifecycle, version preservation, approval decisions, audit evidence, and status belong to Agents rather than a thin orchestration layer.
- (decision) Use separate EventStore aggregate boundaries: Agent aggregate owns Agent identity link, lifecycle, instructions, provider/model selection, response policy, and approver policy; ProviderCatalog aggregate owns provider/model records, capability metadata, enablement, and secret references; AgentInteraction aggregate owns each Agent call's generation attempt, automatic-post evidence, proposal lifecycle when applicable, version history, approval/rejection/abandonment/expiry, and posting outcome. Binds durable state ownership and prevents one tenant-wide stream or split call/proposal records from creating inconsistent audit and proposal history.
- (decision) Agents aggregates remain pure EventStore aggregates: Handle methods decide from command plus current state and emit domain or rejection events; provider calls, Conversations reads/posts, Parties validation/provisioning, Tenants projection reads, timers, and notifications are application-side orchestration/adapters and never run inside aggregate logic. Binds mutation model and prevents replay/nondeterministic I/O from entering domain state.
- (decision) Agents posts final responses through the supported Hexalith.Conversations.Client boundary, especially AppendMessageAsync with ConversationCommandMetadata and idempotency key, never by writing Conversation events/streams directly or treating Proposed Agent Replies as Conversation Messages. Binds Conversations integration and prevents Agents from bypassing Conversations authorization, idempotency, governance, and typed error contracts.
- (decision) ApproverPolicy is Agents-owned configuration with supported V1 sources: caller PartyId from the AgentInteraction, predefined PartyIds, tenant roles resolved from the local Tenants projection, and conversation authority resolved from Conversations detail. Because the current Conversations public contract exposes ParticipantRole.Facilitator but no owner field, V1 treats product 'conversation owner' authority as Conversation Facilitator unless Conversations adds an explicit owner resolver before implementation. Binds approval-source semantics and prevents per-feature reinterpretation of owner/caller/role authority.
- (assumption) Conversation-owner approver source maps to Conversation ParticipantRole.Facilitator for V1; revisit if Conversations adds a first-class owner field or product rejects facilitator equivalence.
- (decision) Agent identity stores only stable PartyId references owned by Hexalith.Parties; Agent creation/linking validates or provisions the Party through a Parties adapter, command-time posting fails closed when Party state is missing/disabled/ambiguous, and Agents never stores Party PII or display/contact details in durable Agent events. Binds identity ownership and prevents PII duplication.
- (decision) Provider integration is hidden behind Agents-owned generation adapters; public API/client contracts and durable events expose ProviderId, ModelId, safe capability metadata, usage/status, and secret reference state only, never provider SDK types, credentials, raw payloads, or provider-specific error bodies. Binds provider boundary and prevents SDK leakage across contracts/UI/audit.
- (decision) ProviderCatalog V1 capability floor includes ProviderId, ModelId, display label, enabled state, secret reference/configured state, text-generation capability, context-window token limit, max-output token limit, timeout policy, and optional safe provider capability flags; exact provider-specific knobs stay adapter-local unless promoted through a new architecture decision. Binds provider metadata and prevents callers from inventing incompatible capability shapes.
- (decision) Conversation Context V1 is built only from authorized Conversations detail and visible timeline content; if the full Source Conversation cannot be loaded fresh enough or cannot fit the selected model's context budget under the configured max-output reserve, the AgentInteraction records context-blocked failure and no provider call, proposal, or Conversation Message is created. Binds oversized-context behavior and prevents silent truncation or summary substitution in V1.
- (decision) V1 cost/performance guardrails are per-call: selected model context limit, Agent-configured max output tokens, provider timeout, usage capture, and operational counters; tenant-wide quota/budget enforcement is deferred unless product sets launch thresholds before implementation. Binds launch floor and prevents hidden unbounded calls while avoiding a premature billing/quota subsystem.
- (decision) Each AgentInteraction snapshots the Agent configuration version, instructions version, response mode, approver policy version, ProviderId, ModelId, provider capability version, caller PartyId, Source ConversationId, and context-build policy at request time; later Agent or ProviderCatalog changes affect future interactions only. Binds temporal consistency and prevents pending proposals from changing authorization/model behavior underneath approvers.
- (decision) AgentInteraction state machine is append-only and versioned: requested -> authorized/context-building -> generated version recorded -> automatic posting or proposal pending; proposal versions are immutable generated/edited/regenerated records; approval selects exactly one version; posting success/failure is a later state transition. Terminal rejected/abandoned/expired interactions preserve all versions and cannot post. Binds lifecycle semantics and prevents proposal/message conflation.
- (decision) All external side effects are idempotent and causally tied to AgentInteraction ids: generation attempts use deterministic attempt ids; Conversation posting uses deterministic MessageId and idempotency key derived from AgentInteractionId plus approved/generated VersionId; retries record outcomes without creating duplicate Conversation Messages or duplicate proposal versions. Binds retry behavior and prevents duplicate provider/posting side effects.
- (decision) Generated, edited, and prompt/context-derived content in Agents is sensitive conversation-derived content: durable storage uses EventStore payload-protection/redaction conventions where available, logs/telemetry/status/audit summaries never include raw content, and public audit/status surfaces expose content only through authorized detail queries. Binds data-safety posture and prevents proposal content leaking through operational paths.
- (decision) Design paradigm is an event-sourced hexagonal Hexalith domain module: EventStore aggregates and contract events/commands form the domain core; application services/orchestrators coordinate side effects through ports; adapters implement Conversations, Parties, Tenants, provider, secret, notification, and FrontComposer integration. Binds architecture language and prevents layered shortcuts that leak infrastructure into contracts or aggregates.
- (decision) Project topology follows sibling Hexalith module shape: Hexalith.Agents.Contracts for public commands/events/models/queries, Hexalith.Agents.Client for typed adopter API, Hexalith.Agents.Server for aggregates/application/orchestration ports, Hexalith.Agents for host composition, Hexalith.Agents.UI for FrontComposer domain UI, Hexalith.Agents.Aspire/AppHost/ServiceDefaults as needed for local orchestration, and Hexalith.Agents.Testing plus focused tests. Binds source layout and prevents dumping product logic into the root workspace.
- (decision) Dependency direction points inward to Contracts: Contracts has no provider SDK, Dapr runtime, ASP.NET host, UI, EventStore server, or persistence dependencies beyond adopter-safe EventStore contracts; Client depends Contracts; Server depends Contracts plus EventStore client/runtime and adapter abstractions; UI depends Client/Contracts/FrontComposer; provider adapters are infrastructure leaves. Binds references and prevents public contract contamination.
- (decision) Admin UI and API/client surfaces share the same public Agents contracts and authorization outcomes; FrontComposer UI registers an Agents domain/nav like Tenants, uses policy-gated entries, and calls Agents API/BFF/client boundaries rather than EventStore streams, provider SDKs, or aggregate internals. Binds surface parity and prevents UI-only workflows.
- (decision) Authorization gates run before every side effect and fail closed on uncertainty: tenant access comes from Agents' local Tenants projection, conversation access/context comes from Conversations authorized queries, Party identity comes from Parties adapters/projections, provider/model readiness comes from ProviderCatalog projections, and approver rights come from the AgentInteraction policy snapshot plus current dependency availability. Binds access model and prevents JWT-only or UI-only authorization.
- (decision) Agents owns local projections for readiness, proposal queues, interaction status, audit summaries, provider/model catalog, and tenant access/cache views; projections are rebuilt from Agents events and subscribed upstream events, tolerate at-least-once/out-of-order delivery, expose freshness/degraded states, and never pretend stale state is fresh. Binds read model ownership and prevents synchronous dependency calls on every read path.
- (decision) Conversation-originated invocation normalizes into an Agents command carrying SourceConversationId, caller PartyId/principal, AgentId, prompt, correlation/causation/idempotency metadata, and entrypoint metadata; mention, command palette, button, or participant affordance are UX adapters over the same command and remain deferred. Binds invocation contract and prevents UI-specific backends.
- (decision) Before posting any Agent response, Agents ensures the Agent PartyId is valid and present in the Source Conversation as Conversations ParticipantType.AiAgent with an approved role, using Conversations AddParticipant/AppendMessage boundaries and idempotent keys; if membership cannot be validated or established, posting fails closed and AgentInteraction records posting failure. Binds conversation participant semantics and prevents anonymous/generic authoring.
- (decision) Proposal expiry is policy-driven: expiry duration comes from Agent response policy or deployment defaults at interaction creation, is snapshotted on AgentInteraction, and is applied by an idempotent expiry command/timer that records Expired only when the proposal is still non-terminal. Exact default duration is deferred to Product/launch policy. Binds expiry ownership and prevents hardcoded or UI-only expiry.
- (decision) Approver notifications are non-authoritative adapters over AgentInteraction/proposal events and proposal queue projections; missing/delayed notifications do not grant or remove approval rights, and the queue/status API remains the source of truth. Notification channels and templates are deferred. Binds notification boundary and prevents hidden workflow state outside Agents.
- (decision) Operational topology is module-local: Hexalith.Agents owns its own AppHost/local orchestration and deployable workloads; the root agents workspace remains a coordination/super-repo and does not become the authoritative root AppHost. Agents AppHost composes Agents service/UI with existing EventStore, Conversations, Parties, Tenants, and provider adapter dependencies for local/dev/test. Binds deployment ownership and prevents cross-module root topology drift.
- (decision) Production/secrets posture: provider credentials live in configured secret stores or host configuration behind secret references; Agents events, projections, UI, API responses, logs, and audit summaries expose only configured/not-configured state and safe provider/model identifiers. Binds secret ownership and prevents credentials in durable or observable surfaces.
- (decision) Runtime messaging follows existing Hexalith/Dapr/EventStore assumptions: event/projector/subscriber handlers are idempotent and tolerate at-least-once delivery, duplicates, replay, and stale projections; AppHost changes require restart; containers/package/build rules follow module-local Directory.Packages.props and .slnx conventions. Binds operations baseline and prevents exactly-once or root-solution assumptions.
- (decision) Public Agents contracts are versioned and additive-first: command/query/result shapes expose Agents language, not EventStore stream names, aggregate internals, provider SDK details, or projection storage; breaking changes require explicit versioning/baseline updates. Binds adopter surface and prevents internal mechanics from becoming API.
- (decision) Testing floor follows Hexalith module practice with xUnit v3, Shouldly, NSubstitute/Testcontainers where relevant, and focused suites for aggregate transition purity, authorization fail-closed paths, proposal version immutability, idempotent generation/posting retries, tenant isolation, context-too-large blocking, provider-secret non-disclosure, FrontComposer contract/UI conformance, and audit completeness for every posted response. Binds verification scope and prevents launch-critical invariants from being left to manual QA.
- (version) Local Hexalith module baseline reality-checked from sibling Directory.Packages.props/global.json: target net10.0; .NET SDK 10.0.300-10.0.301 across modules; Central Package Management; .slnx solutions.
- (version) Local package baseline reality-checked from sibling modules: Dapr 1.18.4, Aspire Hosting 13.4.6, Keycloak/Kubernetes Aspire preview 13.4.6-preview.1.26319.6, MediatR 14.1.0, FluentValidation 12.1.1, OpenTelemetry 1.15.x-1.16.0, Microsoft.Extensions 10.0.x, xUnit v3 3.2.2, Shouldly 4.3.0, NSubstitute 6.0.0-rc.1 where used, Fluent UI Blazor v5 rc.3 via FrontComposer.
- (version) No provider SDK is selected in the spine; provider adapters remain infrastructure leaves and must be versioned in Directory.Packages.props when implementation chooses a concrete provider package.
- (event) reviewer gate completed: lint clean; rubric/source/current-reality/adversarial reviews written; clear fixes applied for source paths, source-module commit pins, Conversations AddParticipant seam, and content protection requirement.
- (event) spine finalized
- (decision) Agents must use Dapr-native AI agent and workflow building blocks for agentic runtime orchestration: Dapr Workflow owns durable long-running execution, retries, timers, human-in-the-loop waits, and replay-safe coordination for AgentInteraction processing; Dapr AI / Dapr Agents integrations are adapter/runtime leaves for model interaction and agent execution where they fit the .NET host boundary. EventStore aggregates remain the domain source of truth and are mutated only through commands/events; custom in-memory background workers, direct provider SDK orchestration, or non-Dapr workflow engines are not allowed for V1 agent workflow execution. Binds agent runtime/workflow ownership and prevents incompatible orchestration substrates.
- (version) Dapr runtime fit verified 2026-06-23 against official Dapr docs: v1.18 is the latest docs stream; Dapr Agents is v1.0 GA and documented as a Python framework for LLM-powered autonomous applications using Dapr workflows/state/messaging/observability; Dapr Agents DurableAgent uses Dapr Workflows for long-running durable execution; .NET SDK docs expose Dapr Workflow and Dapr AI packages, with local Hexalith baseline already carrying Dapr.Workflow, Dapr.AI, and Dapr.AI.Microsoft.Extensions 1.18.4 in sibling package props where used.
- (event) spine updated with AD-18 Dapr Agent Workflow Runtime; reviewer gate amendment completed with lint clean and dated review addenda
- (decision) Add protocol/tool boundary invariant: in-process same-owner tools use Microsoft Agent Framework function tools; reusable cross-process tools use MCP over Dapr service invocation by default; Dapr MCPServer is reserved for argument-aware RBAC, audit, redaction, durable retry, or per-tool observability; A2A is only for independently deployed remote agents; Dapr pub/sub CloudEvents carry asynchronous coordination; domain mutations remain domain commands. Binds interoperability and prevents incompatible tool, remote-agent, and service-call seams.
- (version) Runtime fit verified 2026-06-23 against primary sources: Microsoft Agent Framework overview documents .NET/Python agents, MCP servers, workflows, sessions, middleware, telemetry, checkpointing, and human-in-the-loop; NuGet lists Microsoft.Agents.AI and Microsoft.Agents.AI.Workflows at 1.10.0 with net10.0 compatibility; Dapr Agents docs identify Dapr Agents v1.0 as GA/production-ready and Python-based; Dapr MCPServer docs state service invocation is the default MCP path and MCPServer is workflow-centric for argument-level RBAC, audit, redaction, durable retries, and per-tool observability.
- (constraint) Source input: technical research at _bmad-output/planning-artifacts/research/technical-dapr-ai-agents-research-2026-06-23.md recommends a hybrid agent runtime: Microsoft Agent Framework as primary .NET agent/workflow layer, Dapr as distributed runtime substrate, optional Python Dapr Agents DurableAgent workers for bounded durable autonomous workloads, MCP for tool boundary, A2A for remote agents, and EventStore/domain services as business truth.
- (decision) Amend AD-18 from Dapr-only workflow runtime to hybrid runtime ownership: V1 uses a .NET AgentHost with Microsoft Agent Framework for typed agents, sessions, model/provider integration, MCP/A2A clients, tool approval, and graph workflows; Dapr supplies sidecars, service invocation, state, pub/sub, workflow, secrets, resiliency, security, and observability; Dapr Workflow owns deterministic long-running business/process orchestration when selected; Python Dapr Agents DurableAgent workers are optional adapter services for autonomous durable workloads that justify that runtime. Every task has exactly one durable owner and all owners mutate Agents domain state only through commands/events. Binds runtime ownership and prevents double orchestration, in-memory background workers, or framework-specific types leaking into public contracts or aggregates.
- (decision) Tighten AD-17 test gate for the hybrid runtime amendment: implementation tests must cover selected durable-owner replay/idempotency, Agent Framework workflow/session restore, and MCP/A2A/tool schema contracts in addition to existing aggregate, authorization, idempotency, tenant isolation, secret-safety, UI, and audit gates. Binds verification and prevents the hybrid runtime/protocol rules from relying on manual validation.
- (event) spine updated from technical Dapr AI Agents/Microsoft Agent Framework research: AD-18 amended to hybrid runtime ownership, AD-19 added for tool/protocol boundaries, AD-17 test gate tightened; reviewer gate addenda completed with lint clean
- (decision) AD-4/AD-10 capability-version reconciliation: the request-time ProviderCapabilityVersion is the minimum trusted catalog revision, not an equality pin. At context build, the live catalog entry for the snapshotted ProviderId/ModelId must have CapabilityVersion greater than or equal to the snapshot version; a missing or lower version is stale dependency state and fails closed as ModelBudgetUnavailable. When the floor is satisfied, current catalog readiness and safe limits govern the build and may tighten or block the in-flight interaction but may not retarget it. Binds temporal consistency and runtime provider safety; prevents stale catalog projections from supplying an older model budget while avoiding invalidation after benign metadata updates.
- (constraint) Brownfield gap found 2026-08-01: AgentInteractionContextOrchestrator currently accepts any positive live catalog CapabilityVersion and does not compare it with request.ProviderCapabilityVersion. Story 2.3 needs an implementation follow-up and focused tests before the context path conforms to amended AD-10.
- (decision) Reviewer-gate tightening of AD-10: every provider-dependent runtime step independently checks current enabled/configured/text-generation readiness and valid step-required limits because enable/disable does not bump CapabilityVersion. The step requires the live version to be at least both the request snapshot version and the latest prior effective step version. Each step's evidence version means the effective live catalog version consumed, while the request snapshot version remains provenance. A higher version requires revalidation of capability-dependent inputs before a side effect; deterministic retries cannot change effective version or inputs under the same attempt identity and must fail closed or start an explicit new attempt when they differ. Binds context-to-generation handoff, evidence semantics, and retry safety; prevents version/readiness conflation and idempotency-key reuse with changed provider inputs.
- (constraint) Additional brownfield gaps confirmed by reviewer gate 2026-08-01: context build does not independently reject disabled/unconfigured live entries; generation/regeneration use current catalog limits but carry the snapshot ProviderCapabilityVersion as provider/evidence provenance and do not bind effective capability inputs across retries. Expand the implementation follow-up before any live provider binding.
- (decision) Final adversarial tightening: initialize a durable capability high-water mark from the request snapshot and advance it with every identified live catalog version, including reads that later fail readiness/limit checks; every subsequent provider-dependent read must be trust-bearing fresh and meet or exceed that mark. ContextReady authorizes the generation step but does not freeze provider input; generation/regeneration repeats the authorized fresh content read, token measurement, and full capability-budget validation immediately before preparing a provider attempt. Binds freshness and context-to-provider safety; prevents later stale reads and stale ContextReady evidence from authorizing changed or oversized input.
- (decision) Final adversarial tightening of AD-13: the durable owner checkpoints one prepared provider-attempt descriptor before invocation, containing the effective capability version, validated limits/timeout, and a canonical fingerprint of the provider request. Only transient provider transport/timeout failures may retry before a terminal domain outcome, and every retry uses that exact descriptor while rechecking current readiness. Any capability version, readiness, or input fingerprint change fails closed under that attempt id; there is no replacement-attempt alternative inside the same interaction/regeneration action. Binds retry scope and prepared-input durability; prevents one idempotency identity from naming multiple requests.
- (constraint) Implementation follow-up must add the capability high-water/effective-version contract, pre-invocation context+budget revalidation, and durable prepared-attempt checkpoint/fingerprint. Existing Story 2.3/2.4/3.4 code does not implement these invariants; live provider binding remains non-conformant until it does.
- (event) capability-version reconciliation finalized 2026-08-01: AD-4/AD-10/AD-11/AD-13 updated; duplicate decision actions closed; runtime implementation follow-up opened; lint clean and rubric/current-reality/adversarial reviewer rechecks pass with no critical or high findings
- (constraint) Approved 2026-08-02 authority adds external-dependency-register.md and launch-readiness-register.md as binding inputs; unknown commitments remain TBD and Uncommitted, and unevidenced gates remain non-passing.
- (decision) Amend AD-10 so the public Provider readiness result fixes OperationalState, Callability, safe ReasonCode, CapabilityVersion, ObservedAt, and ValidUntil; Degraded can be Callable only when every hard gate passes, while unknown, stale, unconfigured, unpriced, invalid-limit, failed, or disabled state is Blocked. This binds API/UI/runtime meaning and prevents warning state from bypassing readiness.
- (decision) Amend AD-12 and AD-13 to allow one pending high-risk command per user session, resource, and operation family while treating UI/BFF locking as advisory and deterministic identity, EventStore optimistic concurrency, and idempotency as authoritative. This permits unrelated work without weakening cross-session correctness.
- (decision) Amend AD-17 to bind the twelve-field readiness record, explicit freshness and invalidation rules, the LR-* minimum gate inventory, and the named projection inventory from launch-readiness-register.md. This prevents host-only configuration, implicit freshness, and unnamed affected projections from becoming readiness authority.
- (decision) Add AD-23 for NFR-11: production-like failure injection proves EventStore RPO 0, recovery within 15 minutes, no duplicate Provider attempts, proposal versions, timers, reservations, or Conversation posts, and no changed terminal decisions.
- (decision) Add AD-24 for NFR-12: a versioned capacity profile supplies numeric tenant/system concurrency and queue limits, queue-or-reject behavior, and measurable fairness; atomic admission keyed by deterministic attempt identity occurs before Provider invocation and preserves cost caps.
- (decision) Add AD-25 and AD-26 for NFR-13 and NFR-14: browser/component conformance proves WCAG 2.2 AA, EN/FR whole-string parity, and restrictive viewport safety; one browser-monotonic clock binds navigation-to-usable, submit-to-authoritative-pending-render, and terminal-receipt-to-render/live-region-announcement evidence with at least 30 production-like samples.
- (decision) Bind every EXT dependency to its governing decision: Conversations membership/posting to AD-6/7, Provider adapter to AD-9/10/21, tokenizer to AD-11, secrets to AD-14/16/21, host to AD-16, topology fixture to AD-17, and safety adapter to AD-20. This prevents stories from consuming an uncommitted seam by implication.
- (version) Local workspace catalog verified 2026-08-02: global.json pins SDK 10.0.301 with latestPatch; imported and root package catalogs select Aspire 13.4.6, Dapr and Workflow 1.18.5, CommunityToolkit Aspire Dapr 13.4.1-beta.687, Fluent UI 5.0.0-rc.4-26180.1, MediatR 14.2.0, FluentValidation 12.1.1, OpenTelemetry 1.17.0, xUnit v3 3.2.2, Shouldly 4.3.0, and root NSubstitute override 5.3.0. Provider and Agent Framework SDKs remain Unselected pending EXT-PROVIDER-1.
- (constraint) Brownfield implementation gap verified 2026-08-02: the solution still contains module-owned Hexalith.Agents.AppHost, Hexalith.Agents.Aspire, and Hexalith.Agents.ServiceDefaults projects. Their presence is non-conformant with AD-16; replacement Story 5.1 owns boundary removal and Story 5.6 owns consumption of EXT-HOST-1 platform composition.
- (decision) Reviewer reconciliation tightens AD-17 with separate QualificationExecutionGateSet and ReleaseQualificationGateSet semantics: controlled production-like calls re-evaluate only the gates applicable to each side effect and can collect evidence when dependencies are Committed or Available, while all 18 current Pass gates plus Available dependencies are exclusive to RQ-1 READY and production enablement. This prevents circularly requiring recovery/performance/metric evidence before the executions that produce it.
- (decision by codex) Reviewer hardening makes Provider invocation an ordered durable protocol across AgentInteraction, budget ledger, shared capacity allocator, and the idempotent Provider adapter; it also fixes WeightedRoundRobinV1 fairness, frozen-cohort monotonic recovery evidence, and discriminated idempotent browser timing samples.
- (decision by codex) Reviewer hardening makes Provider invocation an ordered durable protocol across AgentInteraction, budget ledger, shared capacity allocator, and the idempotent Provider adapter; it also fixes WeightedRoundRobinV1 fairness, frozen-cohort monotonic recovery evidence, and discriminated idempotent browser timing samples.
- (decision by codex) Adversarial closure adds ProviderCatalogMutation, AgentSetupMutation, and AgentActivation to OperationGateMatrixVersion 1, and fences Provider invocation with a monotonic AdmissionFence plus linearizable BeginInvocation so reclaimed admission can never reach Provider transport.
- (event by codex) Spine finalized after the 2026-08-02 register/readiness/NFR change: architecture lint reports zero findings, contract parity checks pass, and rubric, verified-current, and adversarial delta reviews have no remaining critical or high findings.
- (event) Validate gate run 2026-09-08 against main@d7e9cda: lint 0 findings; five lenses (rubric, verified-current, adversarial, brownfield drift, security/data integrity) returned 3 critical (2 themes: sensitive content in Dapr Workflow/Agent Framework state outside AD-14/AD-22; TenantId absent from identities and ProviderCatalog scope contradictory), 21 high, 33 medium, 25 low. Decisions AD-1..AD-26 hold; ratification stale since Story 5.1 (host projects removed) and EXT-HOST-1 Committed 2026-08-09. Report: VALIDATION-REPORT-2026-09-08.md; reviews/review-2026-09-08-*.md. Spine not modified.
- (event) Update run started 2026-09-09: change signals are the 2026-09-08 validate gate (3 critical, 21 high, 33 medium, 25 low), the PRD update of 2026-09-09 (FR-2, FR-7, FR-8, FR-18, FR-24, FR-28, FR-32, FR-33, section 8.1 assumptions, OQ-20..OQ-23), and the UX reconcile of 2026-09-09 (platform-scoped catalog policy, EXT-CONV-UI-1 widening, contracts-that-must-grow). Path: autonomous fast application; product-level calls carry [ASSUMPTION] tags. Deliverable remains the spine only.
- (constraint) Source inputs added 2026-09-09: prds/prd-agents-2026-06-23/update-report-2026-09-09.md, ux-designs/ux-agents-2026-06-23/reconcile-validation-2026-09-09.md, references/Hexalith.AI.Tools/hexalith-llm-instructions.md (platform layout the spine deviates from), sprint-change-proposal-2026-08-04-live-integration-tier.md (AD-17 bind-and-test atomicity).
- (decision) C-1 closed by new AD-27 Execution-State Content Boundary: workflow inputs, activity inputs/outputs, custom status, timer/external-event payloads, and any Agent Framework session/checkpoint carry only identities, versions, revisions, digests, safe codes, and protected-content references; content is materialized inside an activity from the protected EventStore payload and discarded; Agent Framework session persistence is disabled in V1; terminal workflow instances are purged within terminal handling plus a 7-day operational window; workflow-execution-state is a named purge scope item in the register. Binds every Dapr Workflow contract; prevents content persisting where AD-14 protection and AD-22 erasure cannot reach.
- (decision) C-2 closed: ProviderCatalog is platform-scoped, one aggregate per (ProviderId, ModelId) stored under the reserved EventStore tenant id 'system' (the fixed tenant EventStore RestTenantSource and AggregateIdentity already reserve), mutated only by the Platform Operator principal; a new TenantProviderEnablement aggregate per TenantId, also Platform-Operator-mutated, records which (ProviderId, ModelId) a tenant may see and select (PRD FR-33 row, assumption A-10). CapabilityVersion is monotonic per platform key; tenant-facing readiness is the platform entry joined with the tenant enablement and discloses platform-only blockers as the additive code PlatformNotReady; a key not enabled for the caller's tenant returns exactly the absent-key response. TenantId is a mandatory component of every Agents aggregate identity, deterministic id, idempotency tuple, projection key, cursor scope, allocator and ledger call. Overrides the 2026-08-02 'global catalog key' wording and the tenant-scoped Story 5.3 implementation. Binds tenant isolation; prevents cross-tenant catalog disclosure and per-tenant version streams under one global key.
- (constraint) Brownfield gap 2026-09-09: the shipped ProviderCatalogAggregate, ProviderCatalogReadModelAddresses, and Story 5.3 tests are tenant-scoped (aggregate id = tenant id). They are non-conformant with the amended AD-2/AD-10 platform scope and need a migration to the system-tenant catalog plus TenantProviderEnablement before Story 5.5 publishes readiness contracts. [ASSUMPTION] Story 5.5 owns the migration.
- (decision) H-1 closed: AD-2 now names the complete V1 aggregate inventory with identity key and tenant scope: Agent (tenant, AgentId; hexa is one Agent per tenant per OQ-20), ProviderCatalog (system, ProviderId+ModelId), TenantProviderEnablement (tenant, TenantId), AgentInteraction (tenant, AgentInteractionId), BudgetLedger (tenant, TenantId+BudgetPeriod; one reservation event per attempt bounds the hot stream), TenantGovernancePolicy (tenant, TenantId; cost caps, rate limits, regeneration ceiling, expiry defaults, tenant safety restrictions, calling restriction), ContentSafetyPolicy (system; published policy versions with RestrictivenessRank), LaunchReadinessGate (per logical gate key), LegalHold (tenant, HoldId), AuditExport (tenant, ExportId), ProtectedDeletion (tenant, DeletionRequestId). The Agent aggregate stores no safety policy; the snapshot's ContentSafetyPolicyVersion is the platform ContentSafetyPolicy version plus the TenantGovernancePolicy version. Unrecoverable decisions (deletion under hold, DEK destruction, export scope) read the owning aggregate at an expected revision, never a projection. Binds ownership; prevents two owners for one entity and hold-versus-deletion projection races.
- (decision) H-2 closed by new AD-30 Principals And Trusted Envelope: every command envelope carries exactly one principal kind - User (TenantId, PartyId, resolved FR-33 roles), Administrator (reserved actor:* extension populated only by the Agents API ingress after a fresh Tenants-projection role check), Workflow (AgentInteractionId as workflow instance id, activity name, CorrelationId, OnBehalfOfPartyId = snapshot caller), or Platform (system-tenant operator). Ingress strips every client-supplied reserved extension; a reserved extension from a non-ingress path is a rejection and an audited security event. Workflow principals may dispatch only AgentInteraction lifecycle-result commands, BudgetLedger reserve/reconcile, and readiness evidence for their own interaction. FR-33 roles map to FrontComposer policies Agents.PlatformOperator, Agents.Administrator, Agents.Approver, Agents.AuditOperator (Compliance Inspector), Agents.Operator (Release Operator); the caller PartyId is resolved at ingress from the authenticated subject through the Parties adapter and Tenants membership and fails closed when not unique and active [ASSUMPTION pending platform identity confirmation]. Ratifies the shipped actor:agentsAdmin / actor:agentsProviderAdmin pattern as Administrator extensions. Binds authorization carriers; prevents workflow activities borrowing admin authority.
- (decision) H-3 closed by new AD-28 Time Authorities: DomainInstant = EventStore event-metadata commit timestamp of the creating event, stored once in the payload for derived deadlines (ExpiresAt, retention base); EvaluationInstant = one injected TimeProvider supplied to the orchestrator and stamped as EvaluatedAt into the trusted command, aggregates compare supplied instants only; SchedulingInstant = Dapr Workflow CurrentUtcDateTime used only to schedule, timer activities re-read the stored deadline and re-arm for the remainder at most three times before recording a TimerDrift blocker; monotonic fixture/browser clocks measure durations only; the UI never re-evaluates deadlines against the browser clock. NFR-9 runtime latency and SM product metrics derive only from EventStore commit timestamps of named events; runtime-metrics and product-metrics are the sole calculators. Binds every stored instant; prevents aggregate, workflow, projection, and UI computing one deadline from different clocks.
- (decision) H-4 closed: AD-17 defines Freshness as a versioned discriminated value {Basis: ValidityWindow | RevisionLag; EvaluatedAt; ExpectedRevision?; ObservedRevision?; ValidUntil?} evaluated once on the server at EvaluatedAt and carried unchanged to API, BFF, and UI; readiness records and Provider readiness use ValidityWindow, read-your-writes queries use RevisionLag, a surface combining both reports Stale if either is stale; ProviderReadinessReasonCode.Stale maps to LaunchReadinessRecord State Stale on LR-PROVIDER and Blocked callability. Ratifies the shipped ProviderCatalogViewFactory revision-lag basis. Binds freshness; prevents two stale definitions.
- (decision) H-5 closed by new AD-29 Deterministic Identity Derivation: one shared AgentsIdentity canonicalizer (UTF-8, length-prefixed, U+001F separators, SHA-256, lowercase hex, purpose tag first) ratified from the shipped AgentInteractionIdentity; AgentInteractionId = H(interaction, TenantId, AgentId, SourceConversationId, CallerPartyId, ClientIdempotencyKey); AttemptId = H(attempt, TenantId, AgentInteractionId, AttemptOrdinal) with the aggregate-assigned 1-based ordinal of prepared descriptors (transport retries never increment); ProviderIdempotencyKey = AttemptId verbatim; ReservationId = H(reservation, AttemptId); AdmissionId = H(admission, AttemptId); QueueId = AdmissionId; ProposalVersionId = H(version, AgentInteractionId, VersionOrdinal, Kind); MessageId = H(message-id, AgentInteractionId, VersionId) and posting IdempotencyKey = H(idempotency-key, AgentInteractionId, VersionId) ratified from AgentResponsePostingIdentity; SafetyDecisionId = H(safety, AttemptId, Stage); ObservationId per AD-17; SampleId per AD-26. API idempotency is keyed by (TenantId, principal, operation family, client key). Binds every id; prevents two derivations of one identity.
- (constraint) Brownfield gap 2026-09-09: AgentInteractionGenerationOrchestrator derives AttemptId as 'attempt-' + interaction id and AgentProposalRegenerationIdentity hashes a different tuple; both are non-conformant with AD-29 and are corrected by the story that first binds a live Provider attempt (6.4), with regeneration (7.3) following.
- (decision) H-6 closed in AD-17: TenantScope is the closed grammar tenant:<TenantId> or platform; each GateId declares ScopeKind in the register (Platform: LR-TOPOLOGY, LR-EVENTSTORE, LR-SECRETS, LR-RECOVERY, LR-CAPACITY-FAIRNESS, LR-UI-CONFORMANCE, LR-UI-PERFORMANCE, LR-RUNTIME-PERFORMANCE; Tenant: all others) [ASSUMPTION for the three LR-UI/runtime gates]; evaluation for a tenant reads platform records for Platform gates and tenant records for Tenant gates; EnvironmentProfile is <name>@<ProfileVersion> and a version change invalidates the old profile's records; ObservationId = H(observation, GateId, TenantScope, EnvironmentProfile, SourceVersion, EvidenceReference, ObservedAt) computed by the submitting orchestrator and recomputed by the aggregate, re-observation with unchanged SourceVersion is legal, exact duplicate is a no-op, same id with different payload is a conflict; each GateId binds an AuthorizedProducer role checked before append; OperationGateMatrixVersion 2 adds ProposalEdit, ProposalRegeneration, SystemTimer, LegalHoldRelease, ExportDownload, AuditInspection, TenantKillSwitch, and TenantProviderEnablement families, and every public command and workflow activity declares exactly one family in its contract. Binds registry keys; prevents platform-wide evidence never satisfying tenant evaluation and families invented locally.
- (decision) H-7, S-9, S-11 closed in AD-20: each published ContentSafetyPolicy version carries an aggregate-assigned non-decreasing RestrictivenessRank and a publisher-declared ChangeKind (Tightens increments, Loosens and Neutral keep); a retry evaluates under the current version only when its rank is at least the snapshot rank, otherwise under the snapshot version exactly; every safety decision is an AgentInteraction event (SafetyDecisionId, stage, policy versions, adapter version, outcome, category codes, content fingerprint, never content); Conversation Context is untrusted input (FR-31) so the prepared request separates instructions from context using the Provider's role/message structure and ControlBypass includes instruction-override attempts; the FR-27 verdict cache is keyed by (TenantId, ConversationId, Agents content hash, policy versions) and invalidated on policy publication. Binds safety ordering; prevents version comparison weakening a retry.
- (decision) H-8, S-4, S-8, A-11, A-12 closed in AD-21: per-caller and per-Conversation rate limits, the regeneration ceiling, and a maximum of concurrent nonterminal interactions per caller from TenantGovernancePolicy are enforced before reservation with typed rejection; reservation follows context measurement (FR-8 step 6) and is attributed to the BudgetPeriod current at reservation time; a later pre-Provider failure releases it immediately as NotInvoked; an Indeterminate outcome holds the reservation for a configured period (default 24 h, range 1-72 h, PRD A-8) then settles ChargedAtMaximum; a reservation past its deadline with no authoritative outcome becomes Unreconciled, still counted, settleable only by an audited TenantBudgetUpdate operator command or at period close as ChargedAtMaximum; orphan reservations found by recovery settle the same way; the owner that terminalizes an attempt before invocation releases reservation and admission in the same trusted step; reservations never migrate between periods. Binds reservation lifecycle; prevents outage-wedged budgets and unowned releases.
- (decision) H-9 closed in AD-22: protected payloads use a per-AgentInteraction data-encryption key wrapped by a per-tenant key-encryption key custodied through EXT-SECRETS-1; no deployment-wide key protects content; cryptographic erasure destroys the DEK, redaction rewrites projection copies only; restrictive completion means every protected event unprotects as unreadable, every named projection and the workflow-execution-state scope report purged; legal hold pins the DEK and DEK destruction under an active hold is rejected; snapshots and replay caches inherit the DEK so restore cannot revive erased content. Binds erasure semantics; prevents undecidable deletion completeness.
- (decision) H-10, S-6, S-15 closed: new Audit envelope convention - every Agents event carries the AD-30 principal, OnBehalfOfPartyId where applicable, CorrelationId (= AgentInteractionId for every interaction-lifecycle command, else the ingress request id), CausationId, TenantId, and the W3C trace id as the safe trace reference; logs, spans, and metric exemplars carry only TenantId, AgentInteractionId, AttemptId, and the trace id as dimensions; AD-22 export manifests list every item with TenantId, stream or projection id, revision range, SHA-256 of exported bytes, key versions, requester principal, expiry, and an HMAC issued through EXT-SECRETS-1, with the manifest hash recorded in the export projection. FR-24 two-level inspection (posted provenance for current Participants; unposted content for resolved Eligible Approvers or scoped, justified, second-party-approved or post-hoc-reviewed compliance inspection that survives Conversation loss) is bound in AD-22. Binds audit completeness; prevents split chains and unverifiable exports.
- (decision) H-11, S-7 closed in AD-4 and AD-12: Agent lifecycle, tenant Provider enablement, the Agents-owned per-Conversation block, and the per-tenant kill switch are not part of the frozen snapshot; every side-effecting step (Provider invocation, proposal creation, edit, regeneration, approval, posting) re-reads them and fails closed with a durable safe result; the kill switch (FR-28, pulled by Platform Operator or Release Operator, family TenantKillSwitch) disables Agent Calls tenant-wide, lets awaiting proposals be rejected or abandoned but never approved, lets Approved and PostingPending complete or fail on their own terms, cancels queued admissions, and deletes nothing; agent-setup and launch-readiness projections expose the stop state. Binds emergency stop; prevents pending proposals posting under a disabled Agent.
- (decision) H-12, B-11 closed by new AD-31 Conversation Surface Contribution: Conversations never references Agents packages; the Call hexa action and the per-message provenance decoration are Agents-owned UI contributions rendered through the Conversations-owned extension seam committed as EXT-CONV-UI-1 (action contribution with typed registration failure, per-message decoration slot keyed by MessageId, GetCallabilityAsync(tenant, conversation) gateway); Conversations owns the trigger, Agents owns the self-contained ConversationAgentCallPanel dialog body, which submits only the public Agents call command with the caller principal, ConversationId, and idempotency metadata; the /agents/conversation-call harness is removed before Story 6.7 closes; an Uncommitted record blocks 6.7. Binds the reverse seam; prevents module dependency cycles and deep-link entry points.
- (decision) H-13 closed: AD-10, AD-13, AD-17, AD-23, AD-24, AD-25, and AD-26 keep their invariant sentences (owner, ordering, fail-closed precedence, unknown fails closed, one matrix version per decision, fence validated immediately before transport, browser ticks never mixed with wall clock) and point to launch-readiness-register.md sections for field sets, enum vocabularies, GateId and projection inventories, thresholds, profile fields, and sample kinds; AD-13 steps 3-5 collapse to a reference to AD-24. The register is amended in the same run so the two documents never both claim the same text. Binds altitude; prevents two normative sources drifting.
- (decision) H-14 closed by ratifying code: the shipped projection ids agent-setup and provider-catalog are the authoritative setup-detail projections; the register inventory renames agent-setup-readiness to agent-setup and provider-capability-pricing to provider-catalog, adds tenant-provider-enablement, and adds workflow-execution-state as a purge scope item; the remaining ids are unbuilt and stay authoritative for their stories. Binds projection naming; prevents AD-22 deletion completeness being defined over ids that do not exist.
- (decision) H-15, B-4 closed: EXT-PROVIDER-1 consumers narrow to the stories that execute the adapter (5.5, 6.4, 7.3); AD-9 states that catalog-truth publication is adapter-independent; the register is updated in this run.
- (decision) H-16, B-1, B-2, R-4, V-1, V-2 closed: every dated status note is removed from AD rules and the seed; the prerequisites section states that commitment status is read from the register at evaluation time and lists governing decisions and consumers only; AD titles lose change-log tags; history stays in the memlog.
- (decision) B-7 closed: AD-17 adopts the approved 2026-08-04 bind-and-test atomicity rule - a versioned live-seam matrix maps every Story 4.5 section 6 row to its authority and BindingStatus; the story that flips a seam to Live must in the same change add and pass the corresponding Hexalith.Agents.IntegrationTests coverage asserting persisted state-store or read-model end state; Story 5.6 creates that project at the first live projection binding; Stories 5.2 and 5.3 predate it and owe coverage through 5.6. Binds verification; prevents live seams shipping with fake-store evidence only.
- (decision) R-5, R-6, B-10, V-13 closed: Structural Seed redrawn to the tree - Hexalith.Agents is the domain assembly (aggregates, states, internal twin-policies under Agent, AgentInteraction, ProviderCatalog and the new aggregate folders), Hexalith.Agents.Server holds Api, Application/{Agents,AgentInteractions,Queries,Workflows,Activities}, Composition, Ports, Projections; Server/Aggregates and Application/Tools are dropped (Tools contradicts AD-19; StructuralSeedConformanceTests must stop requiring it); test/Hexalith.Agents.Tests is listed and IntegrationTests is marked as created by Story 5.6. New Package layout convention row: sibling Hexalith module shape (Contracts/Client/Server/UI/Testing plus domain assembly) is an accepted deviation from the per-layer reference layout in hexalith-llm-instructions.md; the domain-module boundary rule and aggregate-organised test layout apply unchanged; Hexalith.Agents.Testing holds Agents-specific fixtures only. Aggregates and policies live only in the domain assembly.
- (decision) R-8 closed: the runtime sequence diagram releases the capacity lease only after the outcome event is appended and ensures membership before every Conversation append, on both automatic and approval paths.
- (decision) R-10 and PRD FR-7 absorbed in AD-8: the conditional owner-resolver clause is removed; V1 resolves conversation authority to ParticipantRole.Facilitator, carried on the stable legacy wire identifier ApproverPolicySourceKind.ConversationOwner and labelled Conversation Facilitator everywhere; adopting a Conversations owner field requires an AD-8 amendment and a new ApproverPolicyVersion; the caller source is retired via the FR-23 register; the Eligible Approver predicate (resolved now, Conversation-scoped, not the caller, not the last editor, holding current Conversation read access) is applied at configuration, call, edit, and approval time with typed NoEligibleApprover rejection before Provider work. Revises the 2026-06-23 caller-source decision.
- (decision) R-11, R-12, R-13, A-18 closed: frontmatter binds lists PRD FR-1..FR-33, NFR-1..NFR-14, OQ-1..OQ-23; AD titles carry no tags; AD-19 retitled No Tool Or Remote-Agent Surface In V1; the paradigm is one sentence plus the diagram; AD-12 names the lock-bearing subset (ProposalResolution, PolicyPublication, TenantBudgetUpdate, LegalHold, ExportRequest, DeletionRequest, ProviderCatalogMutation, AgentSetupMutation, AgentActivation) and the family vocabulary is owned by the register matrix version.
- (decision) R-14, A-13, A-15 closed by convention rows: public HTTP routes are versioned under /api/v1/agents/... matching the sibling Conversations prefix, Contracts package major is the breaking-change unit, additive-first rules follow PRD FR-23 (Unknown = 0 sentinel, no removal or rename, unknown values fail closed); errors are one typed AgentsProblem shape with safe ReasonCode, absent and cross-tenant keys return the identical not-found response, typed rejections map to 422, conflicts to 409, accepted-pending to 202 with the authoritative identity and projection version.
- (constraint) Brownfield gap 2026-09-09: AgentsOperationEndpoints maps /api/agents/operations without the v1 segment; non-conformant with the route convention; corrected when Story 5.5 publishes the readiness and provider-state contracts [ASSUMPTION owner].
- (decision) R-16 closed: Notifications convention row - in-product only (queue, count, Conversation status entry); any notification is a non-authoritative adapter over projections and never grants or removes approval rights.
- (decision) B-13 ratified as a Trusted verdicts convention row: authorization and dependency verdicts reach aggregates only as server-populated reserved actor:* and *:validation envelope extensions under AD-30; live EventStore dispatch is one IAgentCommandDispatcher seam, fail-closed Deferred* when no EventStore base URL is configured; accepted writes return Submitted identities, never callability; the UI truth flow submitted, authoritative pending, projection confirmed is the AD-12 lock pattern.
- (decision) S-10 closed in AD-9: SecretReference is an opaque Agents-issued handle (ULID) with no infrastructure meaning, bound to a secret only inside EXT-SECRETS-1; rotation keeps the handle; SecretConfigured is a durable catalog fact appended from a server-side resolvability probe result, never computed in a query path; secrets are resolved only inside the generation activity immediately before transport and never enter events, workflow state, exports, or logs; secret state is Platform-Operator-only.
- (decision) S-13 closed in AD-23: EventStore is the only backed-up source of truth; workflow state, allocator state, and read models are rebuildable and never restored later than the EventStore restore point; after any restore all admission fences are invalidated, InvocationActive leases reclaimed through outcome lookup, open reservations follow AD-21, and every nonterminal interaction re-enters recovery; EXT-HOST-1 owns the procedure and LR-RECOVERY includes one restore-from-backup exercise.
- (decision) S-14 deferred with an explicit row: regional data residency and per-tenant retention overrides inherit from the platform host and the catalog entry's optional ProcessingRegion capability flag in V1; tenant-selectable residency is out of V1.
- (decision) A-16 closed in AD-13: the canonical fingerprint covers TenantId, ProviderId, ModelId, EffectiveProviderCapabilityVersion, InstructionsVersion, the structured role/provenance message sequence with per-message content digests, generation parameters, reserved output tokens, and the evaluated safety policy versions; anything outside that set never invalidates a retry.
- (decision) A-17 closed: Conversations owns the MessageId namespace and uniqueness; Agents supplies the deterministic value under AD-29 and Conversations must accept a caller-supplied id idempotently through EXT-CONV-AI-1.
- (decision) A-19 closed in AD-4: Agent ConfigurationVersion increments on every accepted Agent configuration or lifecycle event (DW-4 is non-conformant); the snapshot's ProviderCapabilityVersion is read from the tenant enablement view of the platform catalog entry at request time.
- (decision) PRD 2026-09-09 absorbed: AD-5 adopts the ten ProposedAgentReplyState values with Pending, Edited, Regenerated awaiting decision, Approved freezing expiry, PostingFailed bounded retry of 3 attempts over 15 minutes (A-7) then persisting until an Approver, the Tenant Agent Administrator, an audited administrative retry, or system abandonment resolves it, and system abandonment reasons NoEligibleApprover, SourceConversationUnavailable, RemovedInConversations, kill switch; AD-6/AD-7 adopt the six EXT-CONV-AI-1 seams, the three-part membership step as the last pre-Provider acceptance step, the Agents-owned per-Conversation block set by the Tenant Agent Administrator or Conversation Facilitator (A-9), and the Conversation deletion signal; AD-11 adopts the Safe Context Budget (limit minus reserved output, instructions, prompt and framing, and a margin defaulting to 10 percent, range 5-25, A-5) with the additive Blocked context mode; AD-13 adopts the FR-8 nine-step acceptance order; AD-17 adopts UnretiredAssumption as an RQ-1 blocker and the pre-enablement versus launch-health metric split; AD-2 adopts per-tenant hexa with tenant-wide response mode (OQ-20).
- (decision) UX 2026-09-09 absorbed: AD-31 carries the widened EXT-CONV-UI-1; AD-15 binds the contracts-that-must-grow list (ProviderReadinessResult with a by-Provider/model query, AgentCallOperationStatus growth, AgentReadinessStatus growth, IProjectionChangeDetailNotifier nudge) as Story 5.5/5.7 obligations; the Platform Operator policy is named Agents.PlatformOperator and EXPERIENCE.md's Agents.PlatformProviderAdministrator is renamed to it in this run so UX and spine agree; the two-person rule on deletion and hold release stays deferred to Product and Security.
- (version) Stack re-verified 2026-09-09 from the checked-in files: global.json 10.0.301 latestPatch (siblings on 10.0.400); root overrides Fluent UI 5.0.0-rc.5-26219.1, xunit.v3 3.2.2, NSubstitute 5.3.0, Shouldly 4.3.0, bunit 2.9.0; imported Hexalith.Builds catalog Aspire.Hosting 13.5.3, Dapr.Workflow 1.18.5, CommunityToolkit.Aspire.Hosting.Dapr 13.5.0-preview.1, MediatR 14.2.0, FluentValidation 12.1.1, OpenTelemetry 1.18.0, xunit.v3 4.0.0, NSubstitute 6.2.0; parent gitlinks EventStore 1b6f08d4, Conversations 73bcee6f, Parties fa423985, Tenants 54fc4040, FrontComposer 053b2008; Microsoft Agent Framework 1.20.0 GA and its DurableTask/Azure Functions hosting named as excluded alternate durable owners (2026-09-08 verified-current review); Dapr 1.18 workflow instance-id reuse rule noted in AD-18.
- (decision) V-3, V-4, V-5, V-7, V-10 closed: Aspire and CommunityToolkit rows are dropped from the Stack because hosting is platform-owned under AD-16; root test-stack overrides (xunit.v3 3.2.2, NSubstitute 5.3.0, SDK 10.0.301) are recorded as explicit accepted deviations from the workspace catalog kept for bunit and FrontComposer.Testing alignment; [ASSUMPTION] Story 5.6 aligns them with the catalog (xunit.v3 4.0.0, NSubstitute 6.2.0, SDK 10.0.400) when it consumes EXT-HOST-1; sibling rows record parent gitlinks at the spine's updated date.
- (assumption) Open assumptions carried into the spine with [ASSUMPTION] tags: Story 5.5 owns the catalog platform-scope migration; LR-UI-CONFORMANCE, LR-UI-PERFORMANCE, LR-RUNTIME-PERFORMANCE are Platform-scoped gates; caller PartyId ingress resolution through Parties plus Tenants membership; Story 5.6 aligns the test stack; Story 5.5 corrects the API route prefix; workflow purge window 7 days; Unreconciled period-close settlement ChargedAtMaximum. Revisit each at the named story's readiness check or when Product/Platform confirm.
- (event) Distill 2026-09-09 complete: spine rewritten (AD-1..AD-26 amended in place, AD-27..AD-31 added, 76 KB), lint_spine.py 0 findings, all four mermaid diagrams parsed with mermaid 11 under jsdom (sequence-diagram semicolons in message text replaced by commas); launch-readiness-register.md and external-dependency-register.md amended to match; EXPERIENCE.md platform policy renamed to Agents.PlatformOperator. Reconcile (PRD, UX, closure matrix) and reviewer gate (rubric, verified-current, adversarial, brownfield, security) dispatched as eight parallel subagents.
- (decision) PRD reconcile 2026-09-09 applied: deletion requires Platform Operator plus Compliance Inspector approval (A-12) and only legal-hold release stays deferred; Provider retry authorized only after outcome lookup confirms no usage within the per-model retry budget, timeout without confirmation is Indeterminate and never retried; Tenant Agent Administrator abandon of PostingFailed limited to gone/inaccessible Conversations; AgentCallAccepted event starts NFR-9 clocks and counts SM-2, fast-rejection sample excludes classifier-dependent reasons; kill-switch triggers and A-17 bound in AD-12; approval-time safety decision added as the third of four points; AD-8 gains the decidable configuration rule, read-access re-check moments, durable resolved-Approver event with three-tier discovery, and FR-7 disclosure vocabulary pointer; AD-20 gains mode-specific tighten-only tenant restrictions, Security approval reference on publication, reused-verdict provenance, and the OQ-18 HistoryFailsSafetyPolicy block; Conversation Context Policy owned by Agent with the Full/Bounded/Blocked enum and Bounded rejection; AD-15 names agent-setup and product-metrics as the FR-25 counter sources and the caller-sees-own-calls rule; AD-7 pre-post re-validation re-runs the three-part step with MembershipUnavailable/MembershipRejected scoped to it; AD-21 names the cap setters, rolling window, no retroactive settlement, and TAA-owned ceiling/expiry; AD-10 gains the retry budget field, pricing version rules, concurrency-token rejections, and disabled-entry migration state; GenerationFailureRecord bound in AD-5; PRD A-n keys cited inline; new Architecture Assumptions table ARCH-A-1..ARCH-A-6 with owners and retirement conditions; sequence diagram reordered so rate limits precede the Conversation read.
- (event) Reconcile and reviewer gate 2026-09-09 completed: reconcile-prd (5 contradictions, 22 gaps), reconcile-ux (16 gaps, 16 contradictions, 12 of them UX-side drift returned to the UX memlog), closure-matrix (52 closed, 14 partial, 7 not addressed, 2 diverged of the 75 prior findings), rubric PASS WITH FINDINGS (0C/2H/5M/6L), verified-current PASS WITH FINDINGS (0C/1H/3M/8L), adversarial FAIL (1C/8H/16M/6L), brownfield FAILS TO RATIFY (0C/4H/7M/7L), security FAIL (2C/6H/9M/5L). All critical and high findings and the actionable mediums applied in three edit batches; reviews in reviews/review-2026-09-09-*.md.
- (decision) UX reconcile applied: SafetyDecisionId for approval and pre-post stages keyed by ProposalVersionId, Stage, and DecisionOrdinal; accepted writes return the FR-29 authoritative pending identity (202) through the *CommandAcceptance contracts and Submitted is client-local; polling is the only proof of projection confirmation, IProjectionChangeDetailNotifier over the FrontComposer projection-change channel is a nudge that never replaces it, Agents owns no hub, cadence UX-owned; RegistryRevision stamped on every readiness evaluation; abandon legal from awaiting states and PostingFailed only; automatic-mode posting shares the A-7 bound with no human retry; Disable has kill-switch semantics for its proposals; proposal expiry duration and regeneration ceiling are Agent configuration written through AgentSetupMutation and snapshotted; the PostingFailed retry bound stays fixed; catalog writes serialized by EntryRevision with CapabilityVersion as the capability-write token; SecretReference is the shipped operator-supplied ConfigurationReferenceId; tenant sees configuration problems only as PlatformNotReady; CurrencyMismatch tenant-scoped blocker; budget basis settled plus outstanding plus Unreconciled; override carries ceiling and expiry; change-evidence event per lock-bearing command with the confirmation-rendered set; justification-required families named; lock-bearing mapping of v2 families; session defined as principal plus browser circuit; shipped link/replace Party commands ratified; harness denied in production-like profiles; Conversation status entry through the decoration slot.
- (decision) Verified-current applied: xUnit deviation rationale corrected to the Microsoft Testing Platform v2 test.runner requirement; Aspire and CommunityToolkit rows dropped; EXT-HOST-1 described as a requirement on the platform host, whose a66cdf34 target is an empty AppHost; Dapr terminal-id re-creation clause added to AD-18; new EXT-PROTECTION-1 register record because the EventStore checkout ships only protection hooks and a no-op default, so the AD-22 key hierarchy needed an owner.
- (decision) Rubric applied: AD-17 UnretiredAssumption scan covers the spine's Architecture Assumptions index; Workflow principals never submit readiness evidence; new Schema and stream evolution convention (additive events, MigratedFrom replay into new streams, frozen old streams, UnrecognizedValue folds); prerequisites table drops the consumer column in favour of the register; sequence diagram reordered to the FR-8 order with provider eligibility.
- (decision) Adversarial applied: RestrictivenessRank replaced by a lineage test (ChangeKind plus LastLoosensVersion, superset validation, component-wise on the platform/tenant pair) and transport retries never re-evaluate safety; deadlines an aggregate stores derive from the creating command's EvaluatedAt while commit timestamps serve measurement and retention; new ConversationAgentState aggregate per (TenantId, ConversationId) owns membership-established, the block, and the non-terminal proposal index, with Workflow allowlist extended for external removal, timer drift, ledger release, retention expiry, and purge under SystemTimer; the prepared descriptor is appended immediately after context measurement with one reservation per descriptor and AttemptCancelled(NotInvoked) on later failure; the Platform principal carries ActorTenantId system, targets the envelope TenantId, and has an explicit cross-tenant family allowlist; principal kind selected by family; AuthorizedProducer values are principal kinds; holds pin each in-scope interaction's DEK two-phase and erasure reads the interaction's own pins; erasure granularity is the interaction and class-scoped deletion rejects in full when any is pinned; orphan reservations without an authorized invocation release NotInvoked; BudgetPeriod is the UTC month of the reserving command's EvaluatedAt; kill-switch semantics made precise per state with release audited; exact replay defined by tuple plus payload fingerprint with the record on the target stream; automatic-mode output is a ProposalVersion; SampleId carries TenantScope; platform gate streams live in tenant system; TimerDrift is an interaction event with a 2-second skew tolerance (ARCH-A-7) and the SystemTimer sweep terminalizes; live-seam matrix now lives in the register; ExpectedRevision is caller-supplied; tenant:system invalid for Platform gates; not-found for non-enabled by-key queries.
- (decision) Security applied: content lives only in a field-level ProtectedContent envelope sealed by the Agents orchestrator, other fields plaintext, destroyed-key fields unprotect as typed Erased so streams stay replayable, envelope stays sealed on the broker, in read models, and in adapter caches; EXT-SECRETS-1 custodian operations WrapDek/UnwrapDek/PinDek/UnpinDek/DestroyDek with an irreversible receipt; reserved envelope extensions carry an HMAC tag issued to the Agents Server principal and verified before the aggregate with a forged-extension test in LR-TENANT-ACCESS; denials and security events durable in a SecurityEventLog aggregate per (TenantId, UtcDay); rate limits and the per-Party open-interaction set enforced atomically by the BudgetLedger AdmitCall command at FR-8 step 4; compliance inspections owned by AuditInspection with pre-approval by a distinct Tenant Agent Administrator principal or post-hoc review, surviving erasure; export envelope key delivered by the custodian and ManifestSealed as an AuditExport event; generation, output safety, and version dispatch are one activity; content digests outside envelopes are keyed HMACs under a per-tenant DigestKey; verdict cache key includes adapter and tenant-restriction versions; Agents.Approver is navigation-only; OnBehalfOfPartyId is the initiating Party of the step; retention legal basis deferred with a Governance owner.
- (constraint) Brownfield gaps recorded 2026-09-09 from the drift review (owners are assumptions until sprint planning assigns them): AgentAggregate stores AgentContentSafetyConfiguration and a single ContentSafetyPolicyVersion, superseded by the ContentSafetyPolicy plus TenantGovernancePolicy pair (Story 8.4); the Epic 4 RecordAgentLaunchReadiness value on Agent is a historical predecessor of LaunchReadinessGate (Story 5.5); HttpAgentAdministrationContextProvider authorizes from JWT roles alone and stamps actor:agentsProviderAdmin for tenant administrators, non-conformant with AD-30 (Story 5.4, deferred-work DW-2); generated and edited version ids and ProposalId derive outside AD-29 (Stories 7.1, 7.2); Application/Tools and Server/Aggregates folders and StructuralSeedConformanceTests contradict the seed (Story 5.6); epics.md and sprint-status.yaml carry none of the obligations this update assigns to Stories 5.4 to 7.3, so a correct-course or epics update is the recommended next step.
- (event) Superseded memlog entries as of 2026-09-09 (append-only log, markers recorded here): the 2026-06-23 caller approver source decision is retired by the FR-23 register; the 2026-06-23 global catalog key wording and the 2026-08-01 note keeping it are replaced by the system-tenant platform catalog; the 2026-06-23 secret-reference wording is replaced by the shipped ConfigurationReferenceId handle; the 2026-08-02 constraint about module-owned AppHost projects is resolved by Story 5.1; the RestrictivenessRank decision earlier today is replaced by the lineage test; the Agent-config placement of expiry and ceiling on TenantGovernancePolicy earlier today is replaced by Agent ownership.
- (decision) Delta-consistency pass 2026-09-09 applied 24 items: sequence diagram places PrepareAttempt and reservation right after context measurement with an AdmitCall ledger line and Server-orchestrated approval safety check; kill switch dropped from AD-5 system-abandonment reasons and named a TenantGovernancePolicy fact read at expected revision; in-flight outcome command permitted under the switch; AD-4 Prevents reworded; block set by three parties and cleared by two; hexa provisioning is a create-only AgentSetupMutation in the Platform allowlist; actor:agentsProviderAdmin is the Platform extension; new ReadinessObservation family with AuthorizedProducer values as principal kinds in spine and register; approval evaluates under the lineage-selected pair; membership is the last acceptance step; seed, class diagram, and Naming row carry ConversationAgentState, AuditInspection, SecurityEventLog, LaunchReadinessGate, ProviderAttempt, GenerationFailureRecord; SecretReference in the diagram; audit-evidence projection named for inspection rate; register by-key not-found and SampleId derivation aligned; BindingStatus lists three values.
- (event) spine finalized
