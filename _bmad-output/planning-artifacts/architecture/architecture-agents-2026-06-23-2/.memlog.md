---
scope: agents workspace
purpose: build-substrate architecture spine only
altitude: initiative
updated: 2026-09-13T02:10
---

- (constraint by user) Scope is the agents workspace under /home/administrator/projects/hexalith/agents.
- (constraint by user) Deliverable is the architecture spine only; no companion deck, report, C4 set, or human-facing solution design is requested now.
- (direction) Use Coaching path: decisions are elicited and challenged before drafting.
- (assumption) Altitude starts as initiative because the scope is the whole agents workspace and should bind module/feature work without specifying per-story detail.
- (constraint) Brownfield reality: the root agents repository is a workspace/super-repo of Hexalith modules; each module owns its own .slnx, build props, package versions, tests, docs, AppHost/deployment posture, and release rules.
- (constraint) Brownfield reality: root-level submodules and sibling MSBuild path discovery are the workspace dependency mechanism; recursive submodule initialization and casual edits to submodule contents are forbidden by existing module contexts.
- (constraint) Brownfield reality: Hexalith.EventStore is the shared command/query/event substrate; Tenants owns tenant lifecycle and access state; Parties owns party identity and PII; Memories owns knowledge/search backends; FrontComposer owns generated/admin UI contracts; Conversations consumes these as bounded-context dependencies.
- (constraint) Source input: final PRD at _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md defines V1 Hexalith Agents requirements for governed hexa participation in Conversations.
- (constraint) Source input: final product brief at _bmad-output/planning-artifacts/briefs/brief-agents-2026-06-23/brief.md defines hexa as first named Agent, explicit conversation invocation, Party attribution, response policy, approval workflow, and V2 deferrals.
- (constraint) Source input: UX draft at _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/EXPERIENCE.md and DESIGN.md binds FrontComposer inheritance, admin/workflow surfaces, proposal state distinctions, and unresolved conversation invocation pattern.
- (question) Architecture-owned PRD questions include Proposed Agent Reply runtime/storage ownership, latency target, cost controls, provider capability metadata, oversized Conversation Context policy, and audit governance boundaries.
- (decision by user) Hexalith Agents is a full EventStore-backed domain module: durable Agent configuration, provider governance, Agent calls, Proposed Agent Reply lifecycle, version preservation, approval decisions, audit evidence, and status belong to Agents rather than a thin orchestration layer.
- (decision) Use separate EventStore aggregate boundaries: Agent aggregate owns Agent identity link, lifecycle, instructions, provider/model selection, response policy, and approver policy; ProviderCatalog aggregate owns provider/model records, capability metadata, enablement, and secret references; AgentInteraction aggregate owns each Agent call's generation attempt, automatic-post evidence, proposal lifecycle when applicable, version history, approval/rejection/abandonment/expiry, and posting outcome. Binds durable state ownership and prevents one tenant-wide stream or split call/proposal records from creating inconsistent audit and proposal history.
- (decision) Agents aggregates remain pure EventStore aggregates: Handle methods decide from command plus current state and emit domain or rejection events; provider calls, Conversations reads/posts, Parties validation/provisioning, Tenants projection reads, timers, and notifications are application-side orchestration/adapters and never run inside aggregate logic. Binds mutation model and prevents replay/nondeterministic I/O from entering domain state.
- (decision) Agents posts final responses through the supported Hexalith.Conversations.Client boundary, especially AppendMessageAsync with ConversationCommandMetadata and idempotency key, never by writing Conversation events/streams directly or treating Proposed Agent Replies as Conversation Messages. Binds Conversations integration and prevents Agents from bypassing Conversations authorization, idempotency, governance, and typed error contracts.
- (decision) ApproverPolicy is Agents-owned configuration with supported V1 sources: caller PartyId from the AgentInteraction, predefined PartyIds, tenant roles resolved from the local Tenants projection, and conversation authority resolved from Conversations detail. Because the current Conversations public contract exposes ParticipantRole.Facilitator but no owner field, V1 treats product 'conversation owner' authority as Conversation Facilitator unless Conversations adds an explicit owner resolver before implementation. Binds approval-source semantics and prevents per-feature reinterpretation of owner/caller/role authority.
- (assumption) Conversation-owner approver source maps to Conversation ParticipantRole.Facilitator for V1; revisit if Conversations adds a first-class owner field or product rejects facilitator equivalence.
- (decision) Agent identity stores only stable PartyId references owned by Hexalith.Parties; Agent creation/linking validates or provisions the Party through a Parties adapter, command-time posting fails closed when Party state is missing/disabled/ambiguous, and Agents never stores Party PII or display/contact details in durable Agent events. Binds identity ownership and prevents PII duplication.
- (decision) Provider integration is hidden behind Agents-owned generation adapters; public API/client contracts and durable events expose ProviderId, ModelId, safe capability metadata, usage/status, and secret reference state only, never provider SDK types, credentials, raw payloads, or provider-specific error bodies. Binds provider boundary and prevents SDK leakage across contracts/UI/audit.
- (decision) ProviderCatalog V1 capability floor includes ProviderId, ModelId, display label, enabled state, secret reference/configured state, text-generation capability, context-window token limit, max-output token limit, timeout policy, and optional safe provider capability flags; exact provider-specific knobs stay adapter-local unless promoted through a new architecture decision. Binds provider metadata and prevents callers from inventing incompatible capability shapes.
- (decision) Conversation Context V1 is built only from authorized Conversations detail and visible timeline content; if the full Source Conversation cannot be loaded fresh enough or cannot fit the selected model's context budget under the configured max-output reserve, the AgentInteraction records context-blocked failure and no provider call, proposal, or Conversation Message is created. Binds oversized-context behavior and prevents silent truncation or summary substitution in V1.
- (decision) V1 cost/performance guardrails are per-call: selected model context limit, Agent-configured max output tokens, provider timeout, usage capture, and operational counters; tenant-wide quota/budget enforcement is deferred unless product sets launch thresholds before implementation. Binds launch floor and prevents hidden unbounded calls while avoiding a premature billing/quota subsystem.
- (decision) Each AgentInteraction snapshots the Agent configuration version, instructions version, response mode, approver policy version, ProviderId, ModelId, provider capability version, caller PartyId, Source ConversationId, and context-build policy at request time; later Agent or ProviderCatalog changes affect future interactions only. Binds temporal consistency and prevents pending proposals from changing authorization/model behavior underneath approvers.
- (decision) AgentInteraction state machine is append-only and versioned: requested -> authorized/context-building -> generated version recorded -> automatic posting or proposal pending; proposal versions are immutable generated/edited/regenerated records; approval selects exactly one version; posting success/failure is a later state transition. Terminal rejected/abandoned/expired interactions preserve all versions and cannot post. Binds lifecycle semantics and prevents proposal/message conflation.
- (decision) All external side effects are idempotent and causally tied to AgentInteraction ids: generation attempts use deterministic attempt ids; Conversation posting uses deterministic MessageId and idempotency key derived from AgentInteractionId plus approved/generated VersionId; retries record outcomes without creating duplicate Conversation Messages or duplicate proposal versions. Binds retry behavior and prevents duplicate provider/posting side effects.
- (decision) Generated, edited, and prompt/context-derived content in Agents is sensitive conversation-derived content: durable storage uses EventStore payload-protection/redaction conventions where available, logs/telemetry/status/audit summaries never include raw content, and public audit/status surfaces expose content only through authorized detail queries. Binds data-safety posture and prevents proposal content leaking through operational paths.
- (decision) Design paradigm is an event-sourced hexagonal Hexalith domain module: EventStore aggregates and contract events/commands form the domain core; application services/orchestrators coordinate side effects through ports; adapters implement Conversations, Parties, Tenants, provider, secret, notification, and FrontComposer integration. Binds architecture language and prevents layered shortcuts that leak infrastructure into contracts or aggregates.
- (decision) Project topology follows sibling Hexalith module shape: Hexalith.Agents.Contracts for public commands/events/models/queries, Hexalith.Agents.Client for typed adopter API, Hexalith.Agents.Server for aggregates/application/orchestration ports, Hexalith.Agents for host composition, Hexalith.Agents.UI for FrontComposer domain UI, Hexalith.Agents.Aspire/AppHost/ServiceDefaults as needed for local orchestration, and Hexalith.Agents.Testing plus focused tests. Binds source layout and prevents dumping product logic into the root workspace.
- (decision) Dependency direction points inward to Contracts: Contracts has no provider SDK, Dapr runtime, ASP.NET host, UI, EventStore server, or persistence dependencies beyond adopter-safe EventStore contracts; Client depends Contracts; Server depends Contracts plus EventStore client/runtime and adapter abstractions; UI depends Client/Contracts/FrontComposer; provider adapters are infrastructure leaves. Binds references and prevents public contract contamination.
- (decision) Admin UI and API/client surfaces share the same public Agents contracts and authorization outcomes; FrontComposer UI registers an Agents domain/nav like Tenants, uses policy-gated entries, and calls Agents API/BFF/client boundaries rather than EventStore streams, provider SDKs, or aggregate internals. Binds surface parity and prevents UI-only workflows.
- (decision) Authorization gates run before every side effect and fail closed on uncertainty: tenant access comes from Agents' local Tenants projection, conversation access/context comes from Conversations authorized queries, Party identity comes from Parties adapters/projections, provider/model readiness comes from ProviderCatalog projections, and approver rights come from the AgentInteraction policy snapshot plus current dependency availability. Binds access model and prevents JWT-only or UI-only authorization.
- (decision) Agents owns local projections for readiness, proposal queues, interaction status, audit summaries, provider/model catalog, and tenant access/cache views; projections are rebuilt from Agents events and subscribed upstream events, tolerate at-least-once/out-of-order delivery, expose freshness/degraded states, and never pretend stale state is fresh. Binds read model ownership and prevents synchronous dependency calls on every read path.
- (decision) Conversation-originated invocation normalizes into an Agents command carrying SourceConversationId, caller PartyId/principal, AgentId, prompt, correlation/causation/idempotency metadata, and entrypoint metadata; mention, command palette, button, or participant affordance are UX adapters over the same command and remain deferred. Binds invocation contract and prevents UI-specific backends.
- (decision) Before posting any Agent response, Agents ensures the Agent PartyId is valid and present in the Source Conversation as Conversations ParticipantType.AiAgent with an approved role, using Conversations AddParticipant/AppendMessage boundaries and idempotent keys; if membership cannot be validated or established, posting fails closed and AgentInteraction records posting failure. Binds conversation participant semantics and prevents anonymous/generic authoring.
- (decision) Proposal expiry is policy-driven: expiry duration comes from Agent response policy or deployment defaults at interaction creation, is snapshotted on AgentInteraction, and is applied by an idempotent expiry command/timer that records Expired only when the proposal is still non-terminal. Exact default duration is deferred to Product/launch policy. Binds expiry ownership and prevents hardcoded or UI-only expiry.
- (decision) Approver notifications are non-authoritative adapters over AgentInteraction/proposal events and proposal queue projections; missing/delayed notifications do not grant or remove approval rights, and the queue/status API remains the source of truth. Notification channels and templates are deferred. Binds notification boundary and prevents hidden workflow state outside Agents.
- (decision) Operational topology is module-local: Hexalith.Agents owns its own AppHost/local orchestration and deployable workloads; the root agents workspace remains a coordination/super-repo and does not become the authoritative root AppHost. Agents AppHost composes Agents service/UI with existing EventStore, Conversations, Parties, Tenants, and provider adapter dependencies for local/dev/test. Binds deployment ownership and prevents cross-module root topology drift.
- (decision) Production/secrets posture: provider credentials live in configured secret stores or host configuration behind secret references; Agents events, projections, UI, API responses, logs, and audit summaries expose only configured/not-configured state and safe provider/model identifiers. Binds secret ownership and prevents credentials in durable or observable surfaces.
- (decision) Runtime messaging follows existing Hexalith/Dapr/EventStore assumptions: event/projector/subscriber handlers are idempotent and tolerate at-least-once delivery, duplicates, replay, and stale projections; AppHost changes require restart; containers/package/build rules follow module-local Directory.Packages.props and .slnx conventions. Binds operations baseline and prevents exactly-once or root-solution assumptions.
- (decision) Public Agents contracts are versioned and additive-first: command/query/result shapes expose Agents language, not EventStore stream names, aggregate internals, provider SDK details, or projection storage; breaking changes require explicit versioning/baseline updates. Binds adopter surface and prevents internal mechanics from becoming API.
- (decision) Testing floor follows Hexalith module practice with xUnit v3, Shouldly, NSubstitute/Testcontainers where relevant, and focused suites for aggregate transition purity, authorization fail-closed paths, proposal version immutability, idempotent generation/posting retries, tenant isolation, context-too-large blocking, provider-secret non-disclosure, FrontComposer contract/UI conformance, and audit completeness for every posted response. Binds verification scope and prevents launch-critical invariants from being left to manual QA.
- (version) Local Hexalith module baseline reality-checked from sibling Directory.Packages.props/global.json: target net10.0; .NET SDK 10.0.300-10.0.301 across modules; Central Package Management; .slnx solutions.
- (version) Local package baseline reality-checked from sibling modules: Dapr 1.18.4, Aspire Hosting 13.4.6, Keycloak/Kubernetes Aspire preview 13.4.6-preview.1.26319.6, MediatR 14.1.0, FluentValidation 12.1.1, OpenTelemetry 1.15.x-1.16.0, Microsoft.Extensions 10.0.x, xUnit v3 3.2.2, Shouldly 4.3.0, NSubstitute 6.0.0-rc.1 where used, Fluent UI Blazor v5 rc.3 via FrontComposer.
- (version) No provider SDK is selected in the spine; provider adapters remain infrastructure leaves and must be versioned in Directory.Packages.props when implementation chooses a concrete provider package.
- (event) reviewer gate completed: lint clean; rubric/source/current-reality/adversarial reviews written; clear fixes applied for source paths, source-module commit pins, Conversations AddParticipant seam, and content protection requirement.
- (event) spine finalized
- (decision) Agents must use Dapr-native AI agent and workflow building blocks for agentic runtime orchestration: Dapr Workflow owns durable long-running execution, retries, timers, human-in-the-loop waits, and replay-safe coordination for AgentInteraction processing; Dapr AI / Dapr Agents integrations are adapter/runtime leaves for model interaction and agent execution where they fit the .NET host boundary. EventStore aggregates remain the domain source of truth and are mutated only through commands/events; custom in-memory background workers, direct provider SDK orchestration, or non-Dapr workflow engines are not allowed for V1 agent workflow execution. Binds agent runtime/workflow ownership and prevents incompatible orchestration substrates.
- (version) Dapr runtime fit verified 2026-06-23 against official Dapr docs: v1.18 is the latest docs stream; Dapr Agents is v1.0 GA and documented as a Python framework for LLM-powered autonomous applications using Dapr workflows/state/messaging/observability; Dapr Agents DurableAgent uses Dapr Workflows for long-running durable execution; .NET SDK docs expose Dapr Workflow and Dapr AI packages, with local Hexalith baseline already carrying Dapr.Workflow, Dapr.AI, and Dapr.AI.Microsoft.Extensions 1.18.4 in sibling package props where used.
- (event) spine updated with AD-18 Dapr Agent Workflow Runtime; reviewer gate amendment completed with lint clean and dated review addenda
- (decision) Add protocol/tool boundary invariant: in-process same-owner tools use Microsoft Agent Framework function tools; reusable cross-process tools use MCP over Dapr service invocation by default; Dapr MCPServer is reserved for argument-aware RBAC, audit, redaction, durable retry, or per-tool observability; A2A is only for independently deployed remote agents; Dapr pub/sub CloudEvents carry asynchronous coordination; domain mutations remain domain commands. Binds interoperability and prevents incompatible tool, remote-agent, and service-call seams.
- (version) Runtime fit verified 2026-06-23 against primary sources: Microsoft Agent Framework overview documents .NET/Python agents, MCP servers, workflows, sessions, middleware, telemetry, checkpointing, and human-in-the-loop; NuGet lists Microsoft.Agents.AI and Microsoft.Agents.AI.Workflows at 1.10.0 with net10.0 compatibility; Dapr Agents docs identify Dapr Agents v1.0 as GA/production-ready and Python-based; Dapr MCPServer docs state service invocation is the default MCP path and MCPServer is workflow-centric for argument-level RBAC, audit, redaction, durable retries, and per-tool observability.
- (constraint) Source input: technical research at _bmad-output/planning-artifacts/research/technical-dapr-ai-agents-research-2026-06-23.md recommends a hybrid agent runtime: Microsoft Agent Framework as primary .NET agent/workflow layer, Dapr as distributed runtime substrate, optional Python Dapr Agents DurableAgent workers for bounded durable autonomous workloads, MCP for tool boundary, A2A for remote agents, and EventStore/domain services as business truth.
- (decision) Amend AD-18 from Dapr-only workflow runtime to hybrid runtime ownership: V1 uses a .NET AgentHost with Microsoft Agent Framework for typed agents, sessions, model/provider integration, MCP/A2A clients, tool approval, and graph workflows; Dapr supplies sidecars, service invocation, state, pub/sub, workflow, secrets, resiliency, security, and observability; Dapr Workflow owns deterministic long-running business/process orchestration when selected; Python Dapr Agents DurableAgent workers are optional adapter services for autonomous durable workloads that justify that runtime. Every task has exactly one durable owner and all owners mutate Agents domain state only through commands/events. Binds runtime ownership and prevents double orchestration, in-memory background workers, or framework-specific types leaking into public contracts or aggregates.
- (decision) Tighten AD-17 test gate for the hybrid runtime amendment: implementation tests must cover selected durable-owner replay/idempotency, Agent Framework workflow/session restore, and MCP/A2A/tool schema contracts in addition to existing aggregate, authorization, idempotency, tenant isolation, secret-safety, UI, and audit gates. Binds verification and prevents the hybrid runtime/protocol rules from relying on manual validation.
- (event) spine updated from technical Dapr AI Agents/Microsoft Agent Framework research: AD-18 amended to hybrid runtime ownership, AD-19 added for tool/protocol boundaries, AD-17 test gate tightened; reviewer gate addenda completed with lint clean
- (decision) AD-4/AD-10 capability-version reconciliation: the request-time ProviderCapabilityVersion is the minimum trusted catalog revision, not an equality pin. At context build, the live catalog entry for the snapshotted ProviderId/ModelId must have CapabilityVersion greater than or equal to the snapshot version; a missing or lower version is stale dependency state and fails closed as ModelBudgetUnavailable. When the floor is satisfied, current catalog readiness and safe limits govern the build and may tighten or block the in-flight interaction but may not retarget it. Binds temporal consistency and runtime provider safety; prevents stale catalog projections from supplying an older model budget while avoiding invalidation after benign metadata updates.
- (constraint) Brownfield gap found 2026-08-01: AgentInteractionContextOrchestrator currently accepts any positive live catalog CapabilityVersion and does not compare it with request.ProviderCapabilityVersion. Story 2.3 needs an implementation follow-up and focused tests before the context path conforms to amended AD-10.
- (decision) Reviewer-gate tightening of AD-10: every provider-dependent runtime step independently checks current enabled/configured/text-generation readiness and valid step-required limits because enable/disable does not bump CapabilityVersion. The step requires the live version to be at least both the request snapshot version and the latest prior effective step version. Each step's evidence version means the effective live catalog version consumed, while the request snapshot version remains provenance. A higher version requires revalidation of capability-dependent inputs before a side effect; deterministic retries cannot change effective version or inputs under the same attempt identity and must fail closed or start an explicit new attempt when they differ. Binds context-to-generation handoff, evidence semantics, and retry safety; prevents version/readiness conflation and idempotency-key reuse with changed provider inputs.
- (constraint) Additional brownfield gaps confirmed by reviewer gate 2026-08-01: context build does not independently reject disabled/unconfigured live entries; generation/regeneration use current catalog limits but carry the snapshot ProviderCapabilityVersion as provider/evidence provenance and do not bind effective capability inputs across retries. Expand the implementation follow-up before any live provider binding.
- (decision) Final adversarial tightening: initialize a durable capability high-water mark from the request snapshot and advance it with every identified live catalog version, including reads that later fail readiness/limit checks; every subsequent provider-dependent read must be trust-bearing fresh and meet or exceed that mark. ContextReady authorizes the generation step but does not freeze provider input; generation/regeneration repeats the authorized fresh content read, token measurement, and full capability-budget validation immediately before preparing a provider attempt. Binds freshness and context-to-provider safety; prevents later stale reads and stale ContextReady evidence from authorizing changed or oversized input.
- (decision) Final adversarial tightening of AD-13: the durable owner checkpoints one prepared provider-attempt descriptor before invocation, containing the effective capability version, validated limits/timeout, and a canonical fingerprint of the provider request. Only transient provider transport/timeout failures may retry before a terminal domain outcome, and every retry uses that exact descriptor while rechecking current readiness. Any capability version, readiness, or input fingerprint change fails closed under that attempt id; there is no replacement-attempt alternative inside the same interaction/regeneration action. Binds retry scope and prepared-input durability; prevents one idempotency identity from naming multiple requests.
- (constraint) Implementation follow-up must add the capability high-water/effective-version contract, pre-invocation context+budget revalidation, and durable prepared-attempt checkpoint/fingerprint. Existing Story 2.3/2.4/3.4 code does not implement these invariants; live provider binding remains non-conformant until it does.
- (event) capability-version reconciliation finalized 2026-08-01: AD-4/AD-10/AD-11/AD-13 updated; duplicate decision actions closed; runtime implementation follow-up opened; lint clean and rubric/current-reality/adversarial reviewer rechecks pass with no critical or high findings
- (constraint) Approved 2026-08-02 authority adds external-dependency-register.md and launch-readiness-register.md as binding inputs; unknown commitments remain TBD and Uncommitted, and unevidenced gates remain non-passing.
- (decision) Amend AD-10 so the public Provider readiness result fixes OperationalState, Callability, safe ReasonCode, CapabilityVersion, ObservedAt, and ValidUntil; Degraded can be Callable only when every hard gate passes, while unknown, stale, unconfigured, unpriced, invalid-limit, failed, or disabled state is Blocked. This binds API/UI/runtime meaning and prevents warning state from bypassing readiness.
- (decision) Amend AD-12 and AD-13 to allow one pending high-risk command per user session, resource, and operation family while treating UI/BFF locking as advisory and deterministic identity, EventStore optimistic concurrency, and idempotency as authoritative. This permits unrelated work without weakening cross-session correctness.
- (decision) Amend AD-17 to bind the twelve-field readiness record, explicit freshness and invalidation rules, the LR-* minimum gate inventory, and the named projection inventory from launch-readiness-register.md. This prevents host-only configuration, implicit freshness, and unnamed affected projections from becoming readiness authority.
- (decision) Add AD-23 for NFR-11: production-like failure injection proves EventStore RPO 0, recovery within 15 minutes, no duplicate Provider attempts, proposal versions, timers, reservations, or Conversation posts, and no changed terminal decisions.
- (decision) Add AD-24 for NFR-12: a versioned capacity profile supplies numeric tenant/system concurrency and queue limits, queue-or-reject behavior, and measurable fairness; atomic admission keyed by deterministic attempt identity occurs before Provider invocation and preserves cost caps.
- (decision) Add AD-25 and AD-26 for NFR-13 and NFR-14: browser/component conformance proves WCAG 2.2 AA, EN/FR whole-string parity, and restrictive viewport safety; one browser-monotonic clock binds navigation-to-usable, submit-to-authoritative-pending-render, and terminal-receipt-to-render/live-region-announcement evidence with at least 30 production-like samples.
- (decision) Bind every EXT dependency to its governing decision: Conversations membership/posting to AD-6/7, Provider adapter to AD-9/10/21, tokenizer to AD-11, secrets to AD-14/16/21, host to AD-16, topology fixture to AD-17, and safety adapter to AD-20. This prevents stories from consuming an uncommitted seam by implication.
- (version) Local workspace catalog verified 2026-08-02: global.json pins SDK 10.0.301 with latestPatch; imported and root package catalogs select Aspire 13.4.6, Dapr and Workflow 1.18.5, CommunityToolkit Aspire Dapr 13.4.1-beta.687, Fluent UI 5.0.0-rc.4-26180.1, MediatR 14.2.0, FluentValidation 12.1.1, OpenTelemetry 1.17.0, xUnit v3 3.2.2, Shouldly 4.3.0, and root NSubstitute override 5.3.0. Provider and Agent Framework SDKs remain Unselected pending EXT-PROVIDER-1.
- (constraint) Brownfield implementation gap verified 2026-08-02: the solution still contains module-owned Hexalith.Agents.AppHost, Hexalith.Agents.Aspire, and Hexalith.Agents.ServiceDefaults projects. Their presence is non-conformant with AD-16; replacement Story 5.1 owns boundary removal and Story 5.6 owns consumption of EXT-HOST-1 platform composition.
- (decision) Reviewer reconciliation tightens AD-17 with separate QualificationExecutionGateSet and ReleaseQualificationGateSet semantics: controlled production-like calls re-evaluate only the gates applicable to each side effect and can collect evidence when dependencies are Committed or Available, while all 18 current Pass gates plus Available dependencies are exclusive to RQ-1 READY and production enablement. This prevents circularly requiring recovery/performance/metric evidence before the executions that produce it.
- (decision by codex) Reviewer hardening makes Provider invocation an ordered durable protocol across AgentInteraction, budget ledger, shared capacity allocator, and the idempotent Provider adapter; it also fixes WeightedRoundRobinV1 fairness, frozen-cohort monotonic recovery evidence, and discriminated idempotent browser timing samples.
- (decision by codex) Reviewer hardening makes Provider invocation an ordered durable protocol across AgentInteraction, budget ledger, shared capacity allocator, and the idempotent Provider adapter; it also fixes WeightedRoundRobinV1 fairness, frozen-cohort monotonic recovery evidence, and discriminated idempotent browser timing samples.
- (decision by codex) Adversarial closure adds ProviderCatalogMutation, AgentSetupMutation, and AgentActivation to OperationGateMatrixVersion 1, and fences Provider invocation with a monotonic AdmissionFence plus linearizable BeginInvocation so reclaimed admission can never reach Provider transport.
- (event by codex) Spine finalized after the 2026-08-02 register/readiness/NFR change: architecture lint reports zero findings, contract parity checks pass, and rubric, verified-current, and adversarial delta reviews have no remaining critical or high findings.
- (event) Validate gate run 2026-09-08 against main@d7e9cda: lint 0 findings; five lenses (rubric, verified-current, adversarial, brownfield drift, security/data integrity) returned 3 critical (2 themes: sensitive content in Dapr Workflow/Agent Framework state outside AD-14/AD-22; TenantId absent from identities and ProviderCatalog scope contradictory), 21 high, 33 medium, 25 low. Decisions AD-1..AD-26 hold; ratification stale since Story 5.1 (host projects removed) and EXT-HOST-1 Committed 2026-08-09. Report: VALIDATION-REPORT-2026-09-08.md; reviews/review-2026-09-08-*.md. Spine not modified.
- (event) Update run started 2026-09-09: change signals are the 2026-09-08 validate gate (3 critical, 21 high, 33 medium, 25 low), the PRD update of 2026-09-09 (FR-2, FR-7, FR-8, FR-18, FR-24, FR-28, FR-32, FR-33, section 8.1 assumptions, OQ-20..OQ-23), and the UX reconcile of 2026-09-09 (platform-scoped catalog policy, EXT-CONV-UI-1 widening, contracts-that-must-grow). Path: autonomous fast application; product-level calls carry [ASSUMPTION] tags. Deliverable remains the spine only.
- (constraint) Source inputs added 2026-09-09: prds/prd-agents-2026-06-23/update-report-2026-09-09.md, ux-designs/ux-agents-2026-06-23/reconcile-validation-2026-09-09.md, references/Hexalith.AI.Tools/hexalith-llm-instructions.md (platform layout the spine deviates from), sprint-change-proposal-2026-08-04-live-integration-tier.md (AD-17 bind-and-test atomicity).
- (decision) C-1 closed by new AD-27 Execution-State Content Boundary: workflow inputs, activity inputs/outputs, custom status, timer/external-event payloads, and any Agent Framework session/checkpoint carry only identities, versions, revisions, digests, safe codes, and protected-content references; content is materialized inside an activity from the protected EventStore payload and discarded; Agent Framework session persistence is disabled in V1; terminal workflow instances are purged within terminal handling plus a 7-day operational window; workflow-execution-state is a named purge scope item in the register. Binds every Dapr Workflow contract; prevents content persisting where AD-14 protection and AD-22 erasure cannot reach.
- (decision) C-2 closed: ProviderCatalog is platform-scoped, one aggregate per (ProviderId, ModelId) stored under the reserved EventStore tenant id 'system' (the fixed tenant EventStore RestTenantSource and AggregateIdentity already reserve), mutated only by the Platform Operator principal; a new TenantProviderEnablement aggregate per TenantId, also Platform-Operator-mutated, records which (ProviderId, ModelId) a tenant may see and select (PRD FR-33 row, assumption A-10). CapabilityVersion is monotonic per platform key; tenant-facing readiness is the platform entry joined with the tenant enablement and discloses platform-only blockers as the additive code PlatformNotReady; a key not enabled for the caller's tenant returns exactly the absent-key response. TenantId is a mandatory component of every Agents aggregate identity, deterministic id, idempotency tuple, projection key, cursor scope, allocator and ledger call. Overrides the 2026-08-02 'global catalog key' wording and the tenant-scoped Story 5.3 implementation. Binds tenant isolation; prevents cross-tenant catalog disclosure and per-tenant version streams under one global key.
- (constraint) Brownfield gap 2026-09-09: the shipped ProviderCatalogAggregate, ProviderCatalogReadModelAddresses, and Story 5.3 tests are tenant-scoped (aggregate id = tenant id). They are non-conformant with the amended AD-2/AD-10 platform scope and need a migration to the system-tenant catalog plus TenantProviderEnablement before Story 5.5 publishes readiness contracts. [ASSUMPTION] Story 5.5 owns the migration.
- (decision) H-1 closed: AD-2 now names the complete V1 aggregate inventory with identity key and tenant scope: Agent (tenant, AgentId; hexa is one Agent per tenant per OQ-20), ProviderCatalog (system, ProviderId+ModelId), TenantProviderEnablement (tenant, TenantId), AgentInteraction (tenant, AgentInteractionId), BudgetLedger (tenant, TenantId+BudgetPeriod; one reservation event per attempt bounds the hot stream), TenantGovernancePolicy (tenant, TenantId; cost caps, rate limits, regeneration ceiling, expiry defaults, tenant safety restrictions, calling restriction), ContentSafetyPolicy (system; published policy versions with RestrictivenessRank), LaunchReadinessGate (per logical gate key), LegalHold (tenant, HoldId), AuditExport (tenant, ExportId), ProtectedDeletion (tenant, DeletionRequestId). The Agent aggregate stores no safety policy; the snapshot's ContentSafetyPolicyVersion is the platform ContentSafetyPolicy version plus the TenantGovernancePolicy version. Unrecoverable decisions (deletion under hold, DEK destruction, export scope) read the owning aggregate at an expected revision, never a projection. Binds ownership; prevents two owners for one entity and hold-versus-deletion projection races.
- (decision) H-2 closed by new AD-30 Principals And Trusted Envelope: every command envelope carries exactly one principal kind - User (TenantId, PartyId, resolved FR-33 roles), Administrator (reserved actor:* extension populated only by the Agents API ingress after a fresh Tenants-projection role check), Workflow (AgentInteractionId as workflow instance id, activity name, CorrelationId, OnBehalfOfPartyId = snapshot caller), or Platform (system-tenant operator). Ingress strips every client-supplied reserved extension; a reserved extension from a non-ingress path is a rejection and an audited security event. Workflow principals may dispatch only AgentInteraction lifecycle-result commands, BudgetLedger reserve/reconcile, and readiness evidence for their own interaction. FR-33 roles map to FrontComposer policies Agents.PlatformOperator, Agents.Administrator, Agents.Approver, Agents.AuditOperator (Compliance Inspector), Agents.Operator (Release Operator); the caller PartyId is resolved at ingress from the authenticated subject through the Parties adapter and Tenants membership and fails closed when not unique and active [ASSUMPTION pending platform identity confirmation]. Ratifies the shipped actor:agentsAdmin / actor:agentsProviderAdmin pattern as Administrator extensions. Binds authorization carriers; prevents workflow activities borrowing admin authority.
- (decision) H-3 closed by new AD-28 Time Authorities: DomainInstant = EventStore event-metadata commit timestamp of the creating event, stored once in the payload for derived deadlines (ExpiresAt, retention base); EvaluationInstant = one injected TimeProvider supplied to the orchestrator and stamped as EvaluatedAt into the trusted command, aggregates compare supplied instants only; SchedulingInstant = Dapr Workflow CurrentUtcDateTime used only to schedule, timer activities re-read the stored deadline and re-arm for the remainder at most three times before recording a TimerDrift blocker; monotonic fixture/browser clocks measure durations only; the UI never re-evaluates deadlines against the browser clock. NFR-9 runtime latency and SM product metrics derive only from EventStore commit timestamps of named events; runtime-metrics and product-metrics are the sole calculators. Binds every stored instant; prevents aggregate, workflow, projection, and UI computing one deadline from different clocks.
- (decision) H-4 closed: AD-17 defines Freshness as a versioned discriminated value {Basis: ValidityWindow | RevisionLag; EvaluatedAt; ExpectedRevision?; ObservedRevision?; ValidUntil?} evaluated once on the server at EvaluatedAt and carried unchanged to API, BFF, and UI; readiness records and Provider readiness use ValidityWindow, read-your-writes queries use RevisionLag, a surface combining both reports Stale if either is stale; ProviderReadinessReasonCode.Stale maps to LaunchReadinessRecord State Stale on LR-PROVIDER and Blocked callability. Ratifies the shipped ProviderCatalogViewFactory revision-lag basis. Binds freshness; prevents two stale definitions.
- (decision) H-5 closed by new AD-29 Deterministic Identity Derivation: one shared AgentsIdentity canonicalizer (UTF-8, length-prefixed, U+001F separators, SHA-256, lowercase hex, purpose tag first) ratified from the shipped AgentInteractionIdentity; AgentInteractionId = H(interaction, TenantId, AgentId, SourceConversationId, CallerPartyId, ClientIdempotencyKey); AttemptId = H(attempt, TenantId, AgentInteractionId, AttemptOrdinal) with the aggregate-assigned 1-based ordinal of prepared descriptors (transport retries never increment); ProviderIdempotencyKey = AttemptId verbatim; ReservationId = H(reservation, AttemptId); AdmissionId = H(admission, AttemptId); QueueId = AdmissionId; ProposalVersionId = H(version, AgentInteractionId, VersionOrdinal, Kind); MessageId = H(message-id, AgentInteractionId, VersionId) and posting IdempotencyKey = H(idempotency-key, AgentInteractionId, VersionId) ratified from AgentResponsePostingIdentity; SafetyDecisionId = H(safety, AttemptId, Stage); ObservationId per AD-17; SampleId per AD-26. API idempotency is keyed by (TenantId, principal, operation family, client key). Binds every id; prevents two derivations of one identity.
- (constraint) Brownfield gap 2026-09-09: AgentInteractionGenerationOrchestrator derives AttemptId as 'attempt-' + interaction id and AgentProposalRegenerationIdentity hashes a different tuple; both are non-conformant with AD-29 and are corrected by the story that first binds a live Provider attempt (6.4), with regeneration (7.3) following.
- (decision) H-6 closed in AD-17: TenantScope is the closed grammar tenant:<TenantId> or platform; each GateId declares ScopeKind in the register (Platform: LR-TOPOLOGY, LR-EVENTSTORE, LR-SECRETS, LR-RECOVERY, LR-CAPACITY-FAIRNESS, LR-UI-CONFORMANCE, LR-UI-PERFORMANCE, LR-RUNTIME-PERFORMANCE; Tenant: all others) [ASSUMPTION for the three LR-UI/runtime gates]; evaluation for a tenant reads platform records for Platform gates and tenant records for Tenant gates; EnvironmentProfile is <name>@<ProfileVersion> and a version change invalidates the old profile's records; ObservationId = H(observation, GateId, TenantScope, EnvironmentProfile, SourceVersion, EvidenceReference, ObservedAt) computed by the submitting orchestrator and recomputed by the aggregate, re-observation with unchanged SourceVersion is legal, exact duplicate is a no-op, same id with different payload is a conflict; each GateId binds an AuthorizedProducer role checked before append; OperationGateMatrixVersion 2 adds ProposalEdit, ProposalRegeneration, SystemTimer, LegalHoldRelease, ExportDownload, AuditInspection, TenantKillSwitch, and TenantProviderEnablement families, and every public command and workflow activity declares exactly one family in its contract. Binds registry keys; prevents platform-wide evidence never satisfying tenant evaluation and families invented locally.
- (decision) H-7, S-9, S-11 closed in AD-20: each published ContentSafetyPolicy version carries an aggregate-assigned non-decreasing RestrictivenessRank and a publisher-declared ChangeKind (Tightens increments, Loosens and Neutral keep); a retry evaluates under the current version only when its rank is at least the snapshot rank, otherwise under the snapshot version exactly; every safety decision is an AgentInteraction event (SafetyDecisionId, stage, policy versions, adapter version, outcome, category codes, content fingerprint, never content); Conversation Context is untrusted input (FR-31) so the prepared request separates instructions from context using the Provider's role/message structure and ControlBypass includes instruction-override attempts; the FR-27 verdict cache is keyed by (TenantId, ConversationId, Agents content hash, policy versions) and invalidated on policy publication. Binds safety ordering; prevents version comparison weakening a retry.
- (decision) H-8, S-4, S-8, A-11, A-12 closed in AD-21: per-caller and per-Conversation rate limits, the regeneration ceiling, and a maximum of concurrent nonterminal interactions per caller from TenantGovernancePolicy are enforced before reservation with typed rejection; reservation follows context measurement (FR-8 step 6) and is attributed to the BudgetPeriod current at reservation time; a later pre-Provider failure releases it immediately as NotInvoked; an Indeterminate outcome holds the reservation for a configured period (default 24 h, range 1-72 h, PRD A-8) then settles ChargedAtMaximum; a reservation past its deadline with no authoritative outcome becomes Unreconciled, still counted, settleable only by an audited TenantBudgetUpdate operator command or at period close as ChargedAtMaximum; orphan reservations found by recovery settle the same way; the owner that terminalizes an attempt before invocation releases reservation and admission in the same trusted step; reservations never migrate between periods. Binds reservation lifecycle; prevents outage-wedged budgets and unowned releases.
- (decision) H-9 closed in AD-22: protected payloads use a per-AgentInteraction data-encryption key wrapped by a per-tenant key-encryption key custodied through EXT-SECRETS-1; no deployment-wide key protects content; cryptographic erasure destroys the DEK, redaction rewrites projection copies only; restrictive completion means every protected event unprotects as unreadable, every named projection and the workflow-execution-state scope report purged; legal hold pins the DEK and DEK destruction under an active hold is rejected; snapshots and replay caches inherit the DEK so restore cannot revive erased content. Binds erasure semantics; prevents undecidable deletion completeness.
- (decision) H-10, S-6, S-15 closed: new Audit envelope convention - every Agents event carries the AD-30 principal, OnBehalfOfPartyId where applicable, CorrelationId (= AgentInteractionId for every interaction-lifecycle command, else the ingress request id), CausationId, TenantId, and the W3C trace id as the safe trace reference; logs, spans, and metric exemplars carry only TenantId, AgentInteractionId, AttemptId, and the trace id as dimensions; AD-22 export manifests list every item with TenantId, stream or projection id, revision range, SHA-256 of exported bytes, key versions, requester principal, expiry, and an HMAC issued through EXT-SECRETS-1, with the manifest hash recorded in the export projection. FR-24 two-level inspection (posted provenance for current Participants; unposted content for resolved Eligible Approvers or scoped, justified, second-party-approved or post-hoc-reviewed compliance inspection that survives Conversation loss) is bound in AD-22. Binds audit completeness; prevents split chains and unverifiable exports.
- (decision) H-11, S-7 closed in AD-4 and AD-12: Agent lifecycle, tenant Provider enablement, the Agents-owned per-Conversation block, and the per-tenant kill switch are not part of the frozen snapshot; every side-effecting step (Provider invocation, proposal creation, edit, regeneration, approval, posting) re-reads them and fails closed with a durable safe result; the kill switch (FR-28, pulled by Platform Operator or Release Operator, family TenantKillSwitch) disables Agent Calls tenant-wide, lets awaiting proposals be rejected or abandoned but never approved, lets Approved and PostingPending complete or fail on their own terms, cancels queued admissions, and deletes nothing; agent-setup and launch-readiness projections expose the stop state. Binds emergency stop; prevents pending proposals posting under a disabled Agent.
- (decision) H-12, B-11 closed by new AD-31 Conversation Surface Contribution: Conversations never references Agents packages; the Call hexa action and the per-message provenance decoration are Agents-owned UI contributions rendered through the Conversations-owned extension seam committed as EXT-CONV-UI-1 (action contribution with typed registration failure, per-message decoration slot keyed by MessageId, GetCallabilityAsync(tenant, conversation) gateway); Conversations owns the trigger, Agents owns the self-contained ConversationAgentCallPanel dialog body, which submits only the public Agents call command with the caller principal, ConversationId, and idempotency metadata; the /agents/conversation-call harness is removed before Story 6.7 closes; an Uncommitted record blocks 6.7. Binds the reverse seam; prevents module dependency cycles and deep-link entry points.
- (decision) H-13 closed: AD-10, AD-13, AD-17, AD-23, AD-24, AD-25, and AD-26 keep their invariant sentences (owner, ordering, fail-closed precedence, unknown fails closed, one matrix version per decision, fence validated immediately before transport, browser ticks never mixed with wall clock) and point to launch-readiness-register.md sections for field sets, enum vocabularies, GateId and projection inventories, thresholds, profile fields, and sample kinds; AD-13 steps 3-5 collapse to a reference to AD-24. The register is amended in the same run so the two documents never both claim the same text. Binds altitude; prevents two normative sources drifting.
- (decision) H-14 closed by ratifying code: the shipped projection ids agent-setup and provider-catalog are the authoritative setup-detail projections; the register inventory renames agent-setup-readiness to agent-setup and provider-capability-pricing to provider-catalog, adds tenant-provider-enablement, and adds workflow-execution-state as a purge scope item; the remaining ids are unbuilt and stay authoritative for their stories. Binds projection naming; prevents AD-22 deletion completeness being defined over ids that do not exist.
- (decision) H-15, B-4 closed: EXT-PROVIDER-1 consumers narrow to the stories that execute the adapter (5.5, 6.4, 7.3); AD-9 states that catalog-truth publication is adapter-independent; the register is updated in this run.
- (decision) H-16, B-1, B-2, R-4, V-1, V-2 closed: every dated status note is removed from AD rules and the seed; the prerequisites section states that commitment status is read from the register at evaluation time and lists governing decisions and consumers only; AD titles lose change-log tags; history stays in the memlog.
- (decision) B-7 closed: AD-17 adopts the approved 2026-08-04 bind-and-test atomicity rule - a versioned live-seam matrix maps every Story 4.5 section 6 row to its authority and BindingStatus; the story that flips a seam to Live must in the same change add and pass the corresponding Hexalith.Agents.IntegrationTests coverage asserting persisted state-store or read-model end state; Story 5.6 creates that project at the first live projection binding; Stories 5.2 and 5.3 predate it and owe coverage through 5.6. Binds verification; prevents live seams shipping with fake-store evidence only.
- (decision) R-5, R-6, B-10, V-13 closed: Structural Seed redrawn to the tree - Hexalith.Agents is the domain assembly (aggregates, states, internal twin-policies under Agent, AgentInteraction, ProviderCatalog and the new aggregate folders), Hexalith.Agents.Server holds Api, Application/{Agents,AgentInteractions,Queries,Workflows,Activities}, Composition, Ports, Projections; Server/Aggregates and Application/Tools are dropped (Tools contradicts AD-19; StructuralSeedConformanceTests must stop requiring it); test/Hexalith.Agents.Tests is listed and IntegrationTests is marked as created by Story 5.6. New Package layout convention row: sibling Hexalith module shape (Contracts/Client/Server/UI/Testing plus domain assembly) is an accepted deviation from the per-layer reference layout in hexalith-llm-instructions.md; the domain-module boundary rule and aggregate-organised test layout apply unchanged; Hexalith.Agents.Testing holds Agents-specific fixtures only. Aggregates and policies live only in the domain assembly.
- (decision) R-8 closed: the runtime sequence diagram releases the capacity lease only after the outcome event is appended and ensures membership before every Conversation append, on both automatic and approval paths.
- (decision) R-10 and PRD FR-7 absorbed in AD-8: the conditional owner-resolver clause is removed; V1 resolves conversation authority to ParticipantRole.Facilitator, carried on the stable legacy wire identifier ApproverPolicySourceKind.ConversationOwner and labelled Conversation Facilitator everywhere; adopting a Conversations owner field requires an AD-8 amendment and a new ApproverPolicyVersion; the caller source is retired via the FR-23 register; the Eligible Approver predicate (resolved now, Conversation-scoped, not the caller, not the last editor, holding current Conversation read access) is applied at configuration, call, edit, and approval time with typed NoEligibleApprover rejection before Provider work. Revises the 2026-06-23 caller-source decision.
- (decision) R-11, R-12, R-13, A-18 closed: frontmatter binds lists PRD FR-1..FR-33, NFR-1..NFR-14, OQ-1..OQ-23; AD titles carry no tags; AD-19 retitled No Tool Or Remote-Agent Surface In V1; the paradigm is one sentence plus the diagram; AD-12 names the lock-bearing subset (ProposalResolution, PolicyPublication, TenantBudgetUpdate, LegalHold, ExportRequest, DeletionRequest, ProviderCatalogMutation, AgentSetupMutation, AgentActivation) and the family vocabulary is owned by the register matrix version.
- (decision) R-14, A-13, A-15 closed by convention rows: public HTTP routes are versioned under /api/v1/agents/... matching the sibling Conversations prefix, Contracts package major is the breaking-change unit, additive-first rules follow PRD FR-23 (Unknown = 0 sentinel, no removal or rename, unknown values fail closed); errors are one typed AgentsProblem shape with safe ReasonCode, absent and cross-tenant keys return the identical not-found response, typed rejections map to 422, conflicts to 409, accepted-pending to 202 with the authoritative identity and projection version.
- (constraint) Brownfield gap 2026-09-09: AgentsOperationEndpoints maps /api/agents/operations without the v1 segment; non-conformant with the route convention; corrected when Story 5.5 publishes the readiness and provider-state contracts [ASSUMPTION owner].
- (decision) R-16 closed: Notifications convention row - in-product only (queue, count, Conversation status entry); any notification is a non-authoritative adapter over projections and never grants or removes approval rights.
- (decision) B-13 ratified as a Trusted verdicts convention row: authorization and dependency verdicts reach aggregates only as server-populated reserved actor:* and *:validation envelope extensions under AD-30; live EventStore dispatch is one IAgentCommandDispatcher seam, fail-closed Deferred* when no EventStore base URL is configured; accepted writes return Submitted identities, never callability; the UI truth flow submitted, authoritative pending, projection confirmed is the AD-12 lock pattern.
- (decision) S-10 closed in AD-9: SecretReference is an opaque Agents-issued handle (ULID) with no infrastructure meaning, bound to a secret only inside EXT-SECRETS-1; rotation keeps the handle; SecretConfigured is a durable catalog fact appended from a server-side resolvability probe result, never computed in a query path; secrets are resolved only inside the generation activity immediately before transport and never enter events, workflow state, exports, or logs; secret state is Platform-Operator-only.
- (decision) S-13 closed in AD-23: EventStore is the only backed-up source of truth; workflow state, allocator state, and read models are rebuildable and never restored later than the EventStore restore point; after any restore all admission fences are invalidated, InvocationActive leases reclaimed through outcome lookup, open reservations follow AD-21, and every nonterminal interaction re-enters recovery; EXT-HOST-1 owns the procedure and LR-RECOVERY includes one restore-from-backup exercise.
- (decision) S-14 deferred with an explicit row: regional data residency and per-tenant retention overrides inherit from the platform host and the catalog entry's optional ProcessingRegion capability flag in V1; tenant-selectable residency is out of V1.
- (decision) A-16 closed in AD-13: the canonical fingerprint covers TenantId, ProviderId, ModelId, EffectiveProviderCapabilityVersion, InstructionsVersion, the structured role/provenance message sequence with per-message content digests, generation parameters, reserved output tokens, and the evaluated safety policy versions; anything outside that set never invalidates a retry.
- (decision) A-17 closed: Conversations owns the MessageId namespace and uniqueness; Agents supplies the deterministic value under AD-29 and Conversations must accept a caller-supplied id idempotently through EXT-CONV-AI-1.
- (decision) A-19 closed in AD-4: Agent ConfigurationVersion increments on every accepted Agent configuration or lifecycle event (DW-4 is non-conformant); the snapshot's ProviderCapabilityVersion is read from the tenant enablement view of the platform catalog entry at request time.
- (decision) PRD 2026-09-09 absorbed: AD-5 adopts the ten ProposedAgentReplyState values with Pending, Edited, Regenerated awaiting decision, Approved freezing expiry, PostingFailed bounded retry of 3 attempts over 15 minutes (A-7) then persisting until an Approver, the Tenant Agent Administrator, an audited administrative retry, or system abandonment resolves it, and system abandonment reasons NoEligibleApprover, SourceConversationUnavailable, RemovedInConversations, kill switch; AD-6/AD-7 adopt the six EXT-CONV-AI-1 seams, the three-part membership step as the last pre-Provider acceptance step, the Agents-owned per-Conversation block set by the Tenant Agent Administrator or Conversation Facilitator (A-9), and the Conversation deletion signal; AD-11 adopts the Safe Context Budget (limit minus reserved output, instructions, prompt and framing, and a margin defaulting to 10 percent, range 5-25, A-5) with the additive Blocked context mode; AD-13 adopts the FR-8 nine-step acceptance order; AD-17 adopts UnretiredAssumption as an RQ-1 blocker and the pre-enablement versus launch-health metric split; AD-2 adopts per-tenant hexa with tenant-wide response mode (OQ-20).
- (decision) UX 2026-09-09 absorbed: AD-31 carries the widened EXT-CONV-UI-1; AD-15 binds the contracts-that-must-grow list (ProviderReadinessResult with a by-Provider/model query, AgentCallOperationStatus growth, AgentReadinessStatus growth, IProjectionChangeDetailNotifier nudge) as Story 5.5/5.7 obligations; the Platform Operator policy is named Agents.PlatformOperator and EXPERIENCE.md's Agents.PlatformProviderAdministrator is renamed to it in this run so UX and spine agree; the two-person rule on deletion and hold release stays deferred to Product and Security.
- (version) Stack re-verified 2026-09-09 from the checked-in files: global.json 10.0.301 latestPatch (siblings on 10.0.400); root overrides Fluent UI 5.0.0-rc.5-26219.1, xunit.v3 3.2.2, NSubstitute 5.3.0, Shouldly 4.3.0, bunit 2.9.0; imported Hexalith.Builds catalog Aspire.Hosting 13.5.3, Dapr.Workflow 1.18.5, CommunityToolkit.Aspire.Hosting.Dapr 13.5.0-preview.1, MediatR 14.2.0, FluentValidation 12.1.1, OpenTelemetry 1.18.0, xunit.v3 4.0.0, NSubstitute 6.2.0; parent gitlinks EventStore 1b6f08d4, Conversations 73bcee6f, Parties fa423985, Tenants 54fc4040, FrontComposer 053b2008; Microsoft Agent Framework 1.20.0 GA and its DurableTask/Azure Functions hosting named as excluded alternate durable owners (2026-09-08 verified-current review); Dapr 1.18 workflow instance-id reuse rule noted in AD-18.
- (decision) V-3, V-4, V-5, V-7, V-10 closed: Aspire and CommunityToolkit rows are dropped from the Stack because hosting is platform-owned under AD-16; root test-stack overrides (xunit.v3 3.2.2, NSubstitute 5.3.0, SDK 10.0.301) are recorded as explicit accepted deviations from the workspace catalog kept for bunit and FrontComposer.Testing alignment; [ASSUMPTION] Story 5.6 aligns them with the catalog (xunit.v3 4.0.0, NSubstitute 6.2.0, SDK 10.0.400) when it consumes EXT-HOST-1; sibling rows record parent gitlinks at the spine's updated date.
- (assumption) Open assumptions carried into the spine with [ASSUMPTION] tags: Story 5.5 owns the catalog platform-scope migration; LR-UI-CONFORMANCE, LR-UI-PERFORMANCE, LR-RUNTIME-PERFORMANCE are Platform-scoped gates; caller PartyId ingress resolution through Parties plus Tenants membership; Story 5.6 aligns the test stack; Story 5.5 corrects the API route prefix; workflow purge window 7 days; Unreconciled period-close settlement ChargedAtMaximum. Revisit each at the named story's readiness check or when Product/Platform confirm.
- (event) Distill 2026-09-09 complete: spine rewritten (AD-1..AD-26 amended in place, AD-27..AD-31 added, 76 KB), lint_spine.py 0 findings, all four mermaid diagrams parsed with mermaid 11 under jsdom (sequence-diagram semicolons in message text replaced by commas); launch-readiness-register.md and external-dependency-register.md amended to match; EXPERIENCE.md platform policy renamed to Agents.PlatformOperator. Reconcile (PRD, UX, closure matrix) and reviewer gate (rubric, verified-current, adversarial, brownfield, security) dispatched as eight parallel subagents.
- (decision) PRD reconcile 2026-09-09 applied: deletion requires Platform Operator plus Compliance Inspector approval (A-12) and only legal-hold release stays deferred; Provider retry authorized only after outcome lookup confirms no usage within the per-model retry budget, timeout without confirmation is Indeterminate and never retried; Tenant Agent Administrator abandon of PostingFailed limited to gone/inaccessible Conversations; AgentCallAccepted event starts NFR-9 clocks and counts SM-2, fast-rejection sample excludes classifier-dependent reasons; kill-switch triggers and A-17 bound in AD-12; approval-time safety decision added as the third of four points; AD-8 gains the decidable configuration rule, read-access re-check moments, durable resolved-Approver event with three-tier discovery, and FR-7 disclosure vocabulary pointer; AD-20 gains mode-specific tighten-only tenant restrictions, Security approval reference on publication, reused-verdict provenance, and the OQ-18 HistoryFailsSafetyPolicy block; Conversation Context Policy owned by Agent with the Full/Bounded/Blocked enum and Bounded rejection; AD-15 names agent-setup and product-metrics as the FR-25 counter sources and the caller-sees-own-calls rule; AD-7 pre-post re-validation re-runs the three-part step with MembershipUnavailable/MembershipRejected scoped to it; AD-21 names the cap setters, rolling window, no retroactive settlement, and TAA-owned ceiling/expiry; AD-10 gains the retry budget field, pricing version rules, concurrency-token rejections, and disabled-entry migration state; GenerationFailureRecord bound in AD-5; PRD A-n keys cited inline; new Architecture Assumptions table ARCH-A-1..ARCH-A-6 with owners and retirement conditions; sequence diagram reordered so rate limits precede the Conversation read.
- (event) Reconcile and reviewer gate 2026-09-09 completed: reconcile-prd (5 contradictions, 22 gaps), reconcile-ux (16 gaps, 16 contradictions, 12 of them UX-side drift returned to the UX memlog), closure-matrix (52 closed, 14 partial, 7 not addressed, 2 diverged of the 75 prior findings), rubric PASS WITH FINDINGS (0C/2H/5M/6L), verified-current PASS WITH FINDINGS (0C/1H/3M/8L), adversarial FAIL (1C/8H/16M/6L), brownfield FAILS TO RATIFY (0C/4H/7M/7L), security FAIL (2C/6H/9M/5L). All critical and high findings and the actionable mediums applied in three edit batches; reviews in reviews/review-2026-09-09-*.md.
- (decision) UX reconcile applied: SafetyDecisionId for approval and pre-post stages keyed by ProposalVersionId, Stage, and DecisionOrdinal; accepted writes return the FR-29 authoritative pending identity (202) through the *CommandAcceptance contracts and Submitted is client-local; polling is the only proof of projection confirmation, IProjectionChangeDetailNotifier over the FrontComposer projection-change channel is a nudge that never replaces it, Agents owns no hub, cadence UX-owned; RegistryRevision stamped on every readiness evaluation; abandon legal from awaiting states and PostingFailed only; automatic-mode posting shares the A-7 bound with no human retry; Disable has kill-switch semantics for its proposals; proposal expiry duration and regeneration ceiling are Agent configuration written through AgentSetupMutation and snapshotted; the PostingFailed retry bound stays fixed; catalog writes serialized by EntryRevision with CapabilityVersion as the capability-write token; SecretReference is the shipped operator-supplied ConfigurationReferenceId; tenant sees configuration problems only as PlatformNotReady; CurrencyMismatch tenant-scoped blocker; budget basis settled plus outstanding plus Unreconciled; override carries ceiling and expiry; change-evidence event per lock-bearing command with the confirmation-rendered set; justification-required families named; lock-bearing mapping of v2 families; session defined as principal plus browser circuit; shipped link/replace Party commands ratified; harness denied in production-like profiles; Conversation status entry through the decoration slot.
- (decision) Verified-current applied: xUnit deviation rationale corrected to the Microsoft Testing Platform v2 test.runner requirement; Aspire and CommunityToolkit rows dropped; EXT-HOST-1 described as a requirement on the platform host, whose a66cdf34 target is an empty AppHost; Dapr terminal-id re-creation clause added to AD-18; new EXT-PROTECTION-1 register record because the EventStore checkout ships only protection hooks and a no-op default, so the AD-22 key hierarchy needed an owner.
- (decision) Rubric applied: AD-17 UnretiredAssumption scan covers the spine's Architecture Assumptions index; Workflow principals never submit readiness evidence; new Schema and stream evolution convention (additive events, MigratedFrom replay into new streams, frozen old streams, UnrecognizedValue folds); prerequisites table drops the consumer column in favour of the register; sequence diagram reordered to the FR-8 order with provider eligibility.
- (decision) Adversarial applied: RestrictivenessRank replaced by a lineage test (ChangeKind plus LastLoosensVersion, superset validation, component-wise on the platform/tenant pair) and transport retries never re-evaluate safety; deadlines an aggregate stores derive from the creating command's EvaluatedAt while commit timestamps serve measurement and retention; new ConversationAgentState aggregate per (TenantId, ConversationId) owns membership-established, the block, and the non-terminal proposal index, with Workflow allowlist extended for external removal, timer drift, ledger release, retention expiry, and purge under SystemTimer; the prepared descriptor is appended immediately after context measurement with one reservation per descriptor and AttemptCancelled(NotInvoked) on later failure; the Platform principal carries ActorTenantId system, targets the envelope TenantId, and has an explicit cross-tenant family allowlist; principal kind selected by family; AuthorizedProducer values are principal kinds; holds pin each in-scope interaction's DEK two-phase and erasure reads the interaction's own pins; erasure granularity is the interaction and class-scoped deletion rejects in full when any is pinned; orphan reservations without an authorized invocation release NotInvoked; BudgetPeriod is the UTC month of the reserving command's EvaluatedAt; kill-switch semantics made precise per state with release audited; exact replay defined by tuple plus payload fingerprint with the record on the target stream; automatic-mode output is a ProposalVersion; SampleId carries TenantScope; platform gate streams live in tenant system; TimerDrift is an interaction event with a 2-second skew tolerance (ARCH-A-7) and the SystemTimer sweep terminalizes; live-seam matrix now lives in the register; ExpectedRevision is caller-supplied; tenant:system invalid for Platform gates; not-found for non-enabled by-key queries.
- (decision) Security applied: content lives only in a field-level ProtectedContent envelope sealed by the Agents orchestrator, other fields plaintext, destroyed-key fields unprotect as typed Erased so streams stay replayable, envelope stays sealed on the broker, in read models, and in adapter caches; EXT-SECRETS-1 custodian operations WrapDek/UnwrapDek/PinDek/UnpinDek/DestroyDek with an irreversible receipt; reserved envelope extensions carry an HMAC tag issued to the Agents Server principal and verified before the aggregate with a forged-extension test in LR-TENANT-ACCESS; denials and security events durable in a SecurityEventLog aggregate per (TenantId, UtcDay); rate limits and the per-Party open-interaction set enforced atomically by the BudgetLedger AdmitCall command at FR-8 step 4; compliance inspections owned by AuditInspection with pre-approval by a distinct Tenant Agent Administrator principal or post-hoc review, surviving erasure; export envelope key delivered by the custodian and ManifestSealed as an AuditExport event; generation, output safety, and version dispatch are one activity; content digests outside envelopes are keyed HMACs under a per-tenant DigestKey; verdict cache key includes adapter and tenant-restriction versions; Agents.Approver is navigation-only; OnBehalfOfPartyId is the initiating Party of the step; retention legal basis deferred with a Governance owner.
- (constraint) Brownfield gaps recorded 2026-09-09 from the drift review (owners are assumptions until sprint planning assigns them): AgentAggregate stores AgentContentSafetyConfiguration and a single ContentSafetyPolicyVersion, superseded by the ContentSafetyPolicy plus TenantGovernancePolicy pair (Story 8.4); the Epic 4 RecordAgentLaunchReadiness value on Agent is a historical predecessor of LaunchReadinessGate (Story 5.5); HttpAgentAdministrationContextProvider authorizes from JWT roles alone and stamps actor:agentsProviderAdmin for tenant administrators, non-conformant with AD-30 (Story 5.4, deferred-work DW-2); generated and edited version ids and ProposalId derive outside AD-29 (Stories 7.1, 7.2); Application/Tools and Server/Aggregates folders and StructuralSeedConformanceTests contradict the seed (Story 5.6); epics.md and sprint-status.yaml carry none of the obligations this update assigns to Stories 5.4 to 7.3, so a correct-course or epics update is the recommended next step.
- (event) Superseded memlog entries as of 2026-09-09 (append-only log, markers recorded here): the 2026-06-23 caller approver source decision is retired by the FR-23 register; the 2026-06-23 global catalog key wording and the 2026-08-01 note keeping it are replaced by the system-tenant platform catalog; the 2026-06-23 secret-reference wording is replaced by the shipped ConfigurationReferenceId handle; the 2026-08-02 constraint about module-owned AppHost projects is resolved by Story 5.1; the RestrictivenessRank decision earlier today is replaced by the lineage test; the Agent-config placement of expiry and ceiling on TenantGovernancePolicy earlier today is replaced by Agent ownership.
- (decision) Delta-consistency pass 2026-09-09 applied 24 items: sequence diagram places PrepareAttempt and reservation right after context measurement with an AdmitCall ledger line and Server-orchestrated approval safety check; kill switch dropped from AD-5 system-abandonment reasons and named a TenantGovernancePolicy fact read at expected revision; in-flight outcome command permitted under the switch; AD-4 Prevents reworded; block set by three parties and cleared by two; hexa provisioning is a create-only AgentSetupMutation in the Platform allowlist; actor:agentsProviderAdmin is the Platform extension; new ReadinessObservation family with AuthorizedProducer values as principal kinds in spine and register; approval evaluates under the lineage-selected pair; membership is the last acceptance step; seed, class diagram, and Naming row carry ConversationAgentState, AuditInspection, SecurityEventLog, LaunchReadinessGate, ProviderAttempt, GenerationFailureRecord; SecretReference in the diagram; audit-evidence projection named for inspection rate; register by-key not-found and SampleId derivation aligned; BindingStatus lists three values.
- (event) spine finalized
- (event) Update pass 2026-09-09 (post-validation) started: user chose full-sweep scope over the 2026-09-09 VALIDATION-REPORT findings (2 critical, 6 high, 8 medium, 5 low across rubric-v2, verified-current-v2, adversarial-divergence-v2, brownfield-drift-v2, security-data-integrity-v2). Brownfield/implementation-debt findings (H-4, H-5, H-6, M-8) are re-confirmed already-tracked debt per memlog line 137 and are excluded from this spine-text pass.
- (decision) C-1 closed: AD-2's absent-key rule gains an explicit exception naming AD-10's EntryMissing/reason-coded-Blocked carve-out for an Agent's own previously-snapshotted or currently-selected ProviderId/ModelId, so AD-2 and AD-10 read as one rule instead of two competing ones.
- (decision) C-2 closed: AD-29 gains a general command-payload fingerprint canonicalization rule distinct from the identity canonicalizer and the AD-13 Provider-attempt fingerprint - every public command DTO's fingerprint hashes its contract-declared properties in contract-declared order through the shared canonicalizer, with a leading presence byte (0x00 absent, 0x01 present) distinguishing an absent optional field from an explicit default/null, nested objects and collections flattening depth-first in declared/index order. This is the one command-fingerprint function every command-handler story must use.
- (decision) H-2 (Adversarial-3) closed: AD-4 states ConfigurationVersion, InstructionsVersion, and ApproverPolicyVersion are three independent monotonic counters, not one counter under three names - ConfigurationVersion increments on every accepted Agent configuration or lifecycle event (including edits that also bump the other two), InstructionsVersion increments only on an accepted Agent-Instructions-text edit, ApproverPolicyVersion increments only on an accepted ApproverPolicy source-list edit under AD-8.
- (decision) Adversarial-4 closed: AD-21's AdmitCall per-Party window and MaxConcurrentNonterminalInteractionsPerParty are keyed by OnBehalfOfPartyId per attempt (AD-30) - the caller for the first generation, the requesting Approver for each regeneration - never by CallerPartyId for the interaction's whole lifetime; each attempt's charge releases independently when that attempt's ProviderAttempt reaches a terminal outcome under AD-13.
- (decision) Adversarial-5 closed: AD-12's kill-switch pause accumulates a PausedDuration on the affected PostingFailed proposal, recorded at pull and updated at release; AD-5's 15-minute retry-window deadline becomes origin + window + PausedDuration, evaluated under AD-28, so paused time is never counted against the audited retry bound.
- (decision) Adversarial-6 closed: AD-5 stops calling automatic-mode PostingFailed terminal (it stays non-terminal per the general state list, remains indexed, and automatic mode simply attempts no further automatic retry); AD-7 gains the ConversationAgentState non-terminal-proposal index synchronization rule - the confirmation/proposal workflow orchestration idempotently adds/removes every AgentInteraction proposal-state transition including automatic-mode PostingFailed, keyed by AgentInteractionId, and the index never mirrors Posted/Rejected/Abandoned/Expired.
- (decision) Security-3 closed: AD-30's User principal now re-reads resolved FR-33 roles from the Tenants-projection role assignment at every command ingress, mirroring the Administrator freshness rule, never trusting a cached or token-embedded role claim alone.
- (decision) Security-4/M-4 closed: AD-7 gains one binding sentence making PartyId-reference-only a global cross-referenced invariant over every Party-adjacent field in the Agents domain and its projections (AD-2's Party identity link, AD-8's predefined Parties, AD-30's principal PartyIds), forbidding any cached Party attribute without an AD-7 amendment.
- (decision) Security-5 closed: AD-14 gains a rule requiring HTTP-client/SDK instrumentation wrapping the generation activity's outbound Provider call to exclude request/response headers and bodies from span, metric, and log capture, folded into the AD-17 execution-state content sweep.
- (decision) Security-2 closed: AD-22's justification-reject enforcement clause is widened from 6 to all 9 lock-bearing families (adds ProposalResolution, ProviderCatalogMutation/TenantProviderEnablement, AgentSetupMutation/TenantKillSwitch, AgentActivation), matching AD-12's lock-bearing family list exactly.
- (decision) Security-1 closed as a named residual risk, not a new cross-module seam: AD-22 states explicitly that a posted Conversation Message's Conversations-owned retention is independent of an Agents-side LegalHold, and the gap is tracked as LR-LEGAL-HOLD-PROPAGATION in launch-readiness-register.md plus new spine assumption ARCH-A-10, owned by Governance/Compliance, retired when a Conversations-owned preservation seam is committed or Legal confirms the residual risk is accepted.
- (decision) Rubric-M (SecurityEventLog) closed: AD-2 drops the unsubstantiated 'rate-bounded' qualifier on SecurityEventLog and states the (TenantId, UtcDay) daily partition as the only V1 mitigation; the absence of a numeric burst bound is tracked as new spine assumption ARCH-A-9, owned by Security, retired when Security specifies a limit or a register section.
- (decision) Verified-current H-3 closed: new spine assumption ARCH-A-8 tracks Fluent UI Blazor v5 (pinned 5.0.0-rc.5-26219.1) as a release-candidate risk, owned by Architecture + FrontComposer Maintainer, retired when v5 reaches GA or the pin is confirmed as an accepted production risk before the story that first ships UI.
- (decision) Verified-current M-7 addressed: AD-9 gains a one-line clarification that Provider/Agent Framework SDKs being 'Unselected' means deliberately deferred to the EXT-PROVIDER-1 decision record, not a reflection of ecosystem immaturity - Microsoft Agent Framework (GA 2026-04-03) and Dapr Agents are both already GA.
- (decision) Security secondary observation closed: AD-17 states the AD-2 tenant-ownership check always strictly precedes any Freshness/RevisionLag computation for a keyed read, closing the cross-tenant existence-oracle risk.
- (decision) H-1 (Rubric) closed as an artifact fix, not a spine change: epics.md Story 5.4 (line 1360) is corrected from the four-way User/Platform/Service/System list to the AD-30 principal kinds User/Administrator/Platform/Workflow.
- (event) Distill (update pass) 2026-09-09 complete: ARCHITECTURE-SPINE.md amended in place (AD-2, AD-4, AD-5, AD-7, AD-9, AD-12, AD-14, AD-17, AD-21, AD-22, AD-29, AD-30 text amended; ARCH-A-8, ARCH-A-9, ARCH-A-10 added to Architecture Assumptions; no AD renumbered), launch-readiness-register.md LR-AUDIT-PROTECTION-DELETION description extended, epics.md Story 5.4 principal-kind list corrected to match AD-30. lint_spine.py: 0 findings. Dispatching reviewer gate (rubric + verified-current + adversarial + security, v3) against the amended spine before close.
- (event) Reviewer gate v3 (delta, post-amendment) completed via 4 parallel subagents: rubric-v3 PASS WITH FINDINGS (0C/0H/0M/2L/2I, both prior HIGH/MEDIUM confirmed CLOSED), verified-current-v3 both findings CLOSED (ARCH-A-8 confirmed well-formed and RQ-1-wired; AD-9's GA claims re-verified live, still accurate; Fluent UI v5 still RC, framing still accurate), adversarial-divergence-v3 (3 of 6 original findings CLOSED, 3 partially closed with narrower residual gaps, 2 new findings), security-data-integrity-v3 (4 of 5 CLOSED, 1 closed-as-scoped-residual-risk, 1 new MEDIUM: AD-29's new command-fingerprint rule used unkeyed SHA-256 on payloads that can carry sensitive content). Reviews in reviews/review-2026-09-09-*-v3.md.
- (decision) Round-2 fixes applied from the v3 reviewer gate: AD-10's EntryMissing carve-out widened to match AD-2 exactly (adds an in-flight interaction's previously-snapshotted entry, not only currently-selected); AD-17's tenant-check-before-freshness rule gains an explicit exception for the AD-2/AD-10 carve-out so it doesn't contradict AD-10's unconditional Freshness requirement; AD-21's per-attempt AdmitCall release now names Indeterminate as a terminal attempt outcome for release purposes, decoupled from the separate, possibly-still-outstanding budget-reservation settlement; AD-22's ARCH-A-10 reference gains the standard [ASSUMPTION] bracket; AD-7's PartyId-global-invariant sentence corrected to not overclaim that Administrator/Platform carry a PartyId; Capability-To-Architecture Map's Confirmation/proposal workflow row gains AD-7, matching the index-ownership rule AD-7 now states; AD-29's command-payload fingerprint corrected from unkeyed SHA-256 to HMAC-SHA-256 under the per-tenant DigestKey (matching AD-29's own sensitive-content-digest rule, since command payloads like ProposalEdit can carry sensitive text) and gains a clause on DTO-level presence-marker semantics. lint_spine.py: 0 findings after round 2.
- (event) Update pass 2026-09-09 (post-validation, full-sweep) closed. Spine remains status: final, updated: 2026-09-09 (same day). lint_spine.py 0 findings; reviewer gate v3 confirms all critical/high findings from VALIDATION-REPORT-2026-09-09.md closed, plus the actionable mediums, with two rounds of independent re-verification catching and closing 2 self-introduced issues (AD-29 unkeyed-vs-HMAC digest; AD-2/AD-10 carve-out scope mismatch) before close. Excluded from this pass, as previously decided: H-4/H-5/H-6/M-8 implementation debt (already tracked, code-level, awaiting the stories that build them).
- (event) Validate gate run 2026-09-09 (round 2) against main@611bc5a: lint 0 findings; five lenses (rubric-v4, verified-current-v4, adversarial-v4, brownfield-drift-v3, security-v4) returned 8 critical, 11 high, 9 medium, 7 low (35 total). Gate verdict: FAIL. Root cause of the rubric criticals: prd.md was updated again at 10:08:58, 23 minutes after the spine's 09:45:59 finalize, and that update's own section 8.1 names AD-5/AD-12/AD-22 as unresolved 'Spine defects' with a fix list already in update-report-2026-09-09-2.md (F-SPINE-1..4). Adversarial found two new self-contradictions (AD-8/AD-13 vs sequence diagram on Eligible Approver resolution mode-scoping; AD-21 reusing 'authorization' for a term AD-12/AD-13 already fixed to an earlier step). Security found AD-22's erasure completeness claim never addresses the posted copy in Conversations, untracked unlike the LegalHold case. Brownfield re-confirmed all previously-tracked implementation debt (AD-2, AD-4, AD-29, AD-30, structural seed) still open, unchanged since a191d24, plus a new tracking-integrity finding: sprint-status.yaml marks Story 5.2 done despite unmet amended acceptance criteria. Report: VALIDATION-REPORT-2026-09-09-2.md; reviews/review-2026-09-09-*-v4.md, review-2026-09-09-brownfield-drift-v3.md. Spine not modified (Validate intent).
- (event) Update pass started 2026-09-09, rolling VALIDATION-REPORT-2026-09-09-2.md (round 2) into an Update; source: 10:08 PRD update (update-report-2026-09-09-2.md, F-SPINE-1..4), adversarial-divergence-v4, brownfield-drift-v3, security-data-integrity-v4.
- (decision) AD-5 amended (F-SPINE-2, PRD FR-18): system abandonment is now legal from Approved and PostingFailed on a detected removal (RemovedInConversations), matching AD-7's membership re-check, not human-abandonment-only from PostingFailed. Before any exit from PostingFailed -- retry, human abandon, administrative abandon, or the new system-abandon path -- a MessageId lookup decides whether the post landed: found -> Posted with LateConfirmed; a typed absent/deleted/removed answer counts as no message for an abandon exit; unavailable refuses the exit and the proposal stays PostingFailed. PostingPending remains uninterruptible: nothing but the posting attempt's own conclusion leaves it, so a removal or block detected while an attempt is in flight is applied on exit, never mid-flight. Binds the FR-18 transition table exactly; prevents a removal or block silently starving a PostingFailed proposal that already has an exit, and prevents PostingPending being torn down mid-attempt.
- (decision) AD-12 amended (F-SPINE-4, PRD FR-28): under the pulled kill switch, Approved proposals now wait unposted -- they do not start posting and consume no retry attempt, same as an awaiting-decision proposal -- and only a PostingPending attempt already in flight completes or fails on its own terms; ExpiresAt keeps running and a proposal that expires while suspended is recorded ExpiredWhileSuspended, excluded from SM-3/SM-C5. The Release-Operator trigger is replaced with a mandatory review, not a raw threshold: over a rolling seven-day window, blocked-call share above 50%, unavailable-call share above 20%, Provider-or-generation-failure share above 20%, or posting-failure rate above 10% forces the Release Operator to convene and record a review within one business day (else TriggerReviewOverdue on FR-30); minimum sample is 5 distinct calling Parties and 50 Agent Calls, or for tenants below 5 Parties, every calling Party and 20 calls; below the minimum the trigger evaluates to InsufficientEvidence and is reported, not acted on. The Release Operator pulls the switch only as that review's recorded decision. Binds FR-28 kill-switch semantics and trigger governance exactly; prevents a gameable raw-threshold trigger and prevents Approved proposals posting stale content under a pulled switch.
- (decision) AD-22 amended (F-SPINE-3, PRD FR-24/FR-33): the sole-second-party (Tenant Agent Administrator) inspection model is replaced with FR-24's computed subject-set model. The compliance-inspection second party is computed, not declared: every Party recorded as caller, editor, Approver, decision actor, or Conversation Facilitator on any proposal/Conversation in scope, plus the Tenant Agent Administrator whose ConfigurationVersion was in force for any in-scope call, form the subject set; the second party is the Tenant Agent Administrator when outside that set, else the Platform Operator or a second Compliance Inspector; a second party inside the set, or the Inspector themself, is a typed rejection. Two Compliance Inspectors cannot approve each other's inspections within a rolling 30-day window (anti-collusion). Any inspection wider than one Conversation requires Platform Operator approval regardless of the computed second party. ExportRequest now requires the same prior second-party approval before any content read, never post hoc; LegalHoldRelease requires a second Compliance Inspector or the Platform Operator's audited approval (already true for release, now aligned to the same subject-set/anti-collusion terms as inspection). Binds FR-24 and FR-33's second-party rows exactly; prevents a single administrator being both subject and sole gatekeeper of their own inspection, and prevents export or hold-release proceeding without a genuine second party.
- (decision) AD-13/sequence-diagram (adversarial C-4): struck the diagram's '(confirmation mode)' qualifier on the EligibleApprover-resolution step. Resolution stays universal in the FR-8 acceptance order -- resolved before Provider work in both Automatic and Confirmation mode, per AD-8's unconditional call-time application and AD-5's implication that automatic-mode proposals need a resolved Approver too. An Automatic-mode Agent with zero configured Approvers is rejected up front with NoEligibleApprover before any Provider call, not left to fail later into PostingFailed. Prevents two independently-built acceptance pipelines diverging on whether Automatic-mode calls skip Approver resolution.
- (decision) AD-21 amended (adversarial C-5): replaced the ambiguous phrase 'before safety, admission, and authorization' with the exact AD-13 event name ProviderInvocationAuthorized -- budget reservation happens before safety validation, capacity admission, and the ProviderInvocationAuthorized authorization event (AD-13 step 4), not before AD-13 step 1's authorization gate, which AD-12 and AD-13 already fix as the first acceptance step. Prevents 'authorization' being read as two different steps by two independently-built BudgetLedger handlers, and closes whether an unauthorized caller could trigger a durable reservation before being authorized at all (it cannot: AD-13 step 1 authorization still gates acceptance before reservation runs).
- (decision) AD-22 erasure/Conversations gap closed (security C-8): AD-22's 'Restrictive completion' bar now explicitly discloses the posted-copy exception -- a posted Conversation Message carrying erased content remains readable in Conversations after Agents-side erasure, because AD-6 forbids Agents from writing to or purging Conversations streams, so erasure completeness is defined over Agents-owned state only and never claims to reach a posted copy. ARCH-A-10 and LR-AUDIT-PROTECTION-DELETION are extended to cover DeletionRequest symmetrically with LegalHold: the residual-risk tracking that previously named only a hold outliving Conversations retention now also names an erasure whose posted copy survives in Conversations, same owner (Governance + Compliance), same retirement condition (a Conversations-owned preservation/purge seam is committed, or Legal confirms the residual risk is accepted). Prevents a builder or auditor satisfying AD-22's completeness bar to the letter while erased content remains permanently plaintext-readable as a posted Message, with no tracked residual risk.
- (decision) Frontmatter binds corrected (H-1): PRD now runs FR-1..FR-34/OQ-1..OQ-30; spine frontmatter binds updated to match. FR-34 (payload-protection gating) gets explicit AD coverage: AD-14 and AD-17 already implied the fail-closed behavior (content-bearing workflows stay disabled while protection is unavailable) but never cited FR-34 or PayloadProtectionUnavailable by name -- both ADs now cite FR-34 and the typed blocker explicitly.
- (decision) Sources list corrected (H-1/Medium): superseded update-report-2026-09-09.md is marked superseded-by update-report-2026-09-09-2.md inline rather than cited bare; VALIDATION-REPORT-2026-09-09-2.md and the round-2 review files (reviews/*-v2/v3/v4.md) added to sources.
- (decision) AD-2 amended (rubric H-2, PRD OQ-16/OQ-25): ConversationAgentState gains BlockVersion (increments on every set/clear so a stale clear cannot silently no-op against a newer block) and MirrorPending (set when a block/clear/removal record is written and cleared only when the Conversations-side mirror -- the removal or re-admit seam -- confirms, so FR-25 can expose it per PRD FR-25). The state machine is stated explicitly rather than left implicit: NeverJoined -> Joined -> (ExternallyRemoved | Blocked) -> ReadmitPending -> Joined, where ExternallyRemoved and Blocked are rejecting states on the same terms (AD-7), ReadmitPending is entered on clear and holds until the mirror confirms re-admission, and a clear while MirrorPending re-clears idempotently rather than double-mirroring.
- (decision) AD-7 amended (rubric H-3, PRD OQ-25/OQ-28): the Party link and replace commands are retired from live identity-provisioning paths onto the FR-23 deprecate-and-reject register, matching AD-8's ApproverPolicy caller-source retirement pattern. hexa's Party identity is create-only, provisioned once at tenant enablement by the Platform Operator through AgentSetupMutation (AD-30); a missing identity remains the MissingPartyIdentity activation blocker, but there is no live command to link or replace it post-provisioning in V1. Prevents the spine presenting a live re-provisioning path the PRD no longer allows.
- (decision) AD-15 amended (rubric H-4, PRD FR-25): the FR-25 blocked-call counter list gains rate-limit-blocked calls (RateLimited, the AD-21 per-Party/per-Conversation/per-tenant-concurrency rejection) alongside context policy, safety, cost cap, NoEligibleApprover, and RemovedInConversations/block -- AD-21 already defines RateLimited as a distinct rejection reason and the counter list was silently missing it.
- (decision) ARCH-A-4 clarified (verified-current H-5): the pinned .NET SDK 10.0.301 feature band fell out of active servicing (last 10.0.3xx monthly bundle shipped 2026-09-08, one day before the spine's own updated stamp, with no successor). ARCH-A-4's framing is split: the xUnit v3/NSubstitute/Microsoft Testing Platform v2 deviations remain low-urgency test-stack debt targeted at Story 5.6, but the SDK feature-band lapse is now called out separately as a build-supportability risk (an out-of-support SDK band receives no further security patches), targeted for resolution no later than Story 5.6 as well, escalated sooner if a security advisory lands against 10.0.3xx.
- (decision) AgentReadinessStatus shape reconciled (adversarial H-6): fixed as a composite wrapper -- not a bare enum -- carrying a per-selected-entry ProviderReadinessResult (AD-10's OperationalState/Callability/ReasonCode/CapabilityVersion/ObservedAt/ValidUntil/Freshness triple) plus the Agent-level growth states ActiveNotProvenCallable, Stale, and AuthorityUnresolved (AD-15) as siblings alongside it, not as alternate values of the same enum. AD-10, AD-15, and AD-17 now describe the same shape; a team reading any one AD arrives at the identical contract.
- (decision) CurrencyMismatch cross-reference fixed (adversarial H-7): AD-10's Blocked-state enumeration now names currency-mismatched pricing explicitly (code CurrencyMismatch, cross-referencing AD-21 for the rule), so the readiness join AD-21 already pointed to actually carries the code. Currency is evaluated at readiness time (surfaced Blocked in ProviderReadinessResult, so the UI never offers a mismatched entry) and re-enforced at AD-21's reservation time as a redundant fail-closed check, not decided differently by the two ADs.
- (decision) AD-30 amended (security H-11): the Platform principal now carries the same role-freshness re-check discipline as User and Administrator -- every command dispatch under the actor:agentsProviderAdmin extension re-verifies the Platform Operator assignment against the Tenants-projection role at ingress, not from a cached or token-embedded claim, mirroring the User/Administrator freshness rule already stated. Closes the one principal kind that previously had no stated freshness re-check.
- (decision) Architecture Assumptions table gains a Target Retirement column (rubric medium): every Architecture-owned row (ARCH-A-1, 2, 3, 4, 6, 7, 8, 10) carries a milestone-bound target -- most tie to the same story already named in the row (Story 5.6 for ARCH-A-4, EXT-HOST-1 commitment for ARCH-A-1, RQ-1 evaluation for ARCH-A-3/6/7, GA or first UI-shipping story for ARCH-A-8, a committed preservation seam or Legal sign-off for ARCH-A-10); ARCH-A-2 targets EXT-HOST-1's identity contract. Security-owned ARCH-A-9 and ARCH-A-5 (already retired) are left without a target, matching the PRD rule that only Architecture-owned rows require one.
- (decision) Stack table deviation tracking completed (verified-current medium): Microsoft.NET.Test.Sdk and xunit.runner.visualstudio rows added alongside the existing xUnit v3/NSubstitute deviation entries, same ARCH-A-4 override reason and Story 5.6 target -- they were diverging from the catalog without being named.
- (decision) AD-12 lock-bearing-family list gains the block/clear command (adversarial medium): setting or clearing hexa's Conversation block now explicitly belongs to the AgentSetupMutation family for AD-12 locking and AD-22 justification-gate purposes -- it was Agents/tenant-scoped setup-adjacent state with no declared family, an ambiguity two teams could resolve differently.
- (decision) AD-22 AuditInspection post-hoc consequence stated (security medium): a compliance inspection not reviewed within the FR-24 post-hoc window is recorded as unreviewed on the audit-evidence surface, same as an unmirrored block -- already-granted read access is not retroactively revoked (the inspection already happened), but the unreviewed flag persists as a visible governance signal to the Tenant Agent Administrator and Platform Operator until a review is recorded; no automatic escalation beyond that visibility is defined for V1.
- (decision) C-6/C-7 (brownfield, already tracked) confirmed as implementation debt requiring no spine amendment: AD-2's ProviderCatalog/TenantProviderEnablement platform scoping (Story 5.3) and AD-4's ConfigurationVersion lifecycle bump (DW-4) remain correctly described in the spine; the code has not caught up. New this round: sprint-status.yaml:88 marks Story 5.2 done against unmet amended acceptance criteria (the ConfigurationVersion bump and AgentLifecycleConfigurationVersionTests it requires) -- corrected to in-progress in the same change, so the tracker no longer contradicts DW-4 and the story's own AC.
- (event) Critical/High tier of VALIDATION-REPORT-2026-09-09-2.md applied: C-1..C-8 and H-1..H-11 resolved via spine amendment or confirmed as already-tracked implementation debt (H-8/H-9/H-10 -- no spine change). Medium tier folded in where the fix was a direct spine-text addition (ARCH-A retirement dates, stack table completeness, AD-12 lock-family, AD-22 post-hoc consequence); remaining Medium/Low items (organization-Party Approvers, staleness bound on Approved, undated-assumption informational carryovers, bunit/MediatR/Aspire pin drift) deferred, unchanged from the report's own Low/informational classification -- no new spine defect.
- (event) Discovered on starting the edit pass: AD-2 (hexa create-only ProvisionHexa provisioning), AD-12 (kill-switch trigger-review machinery), AD-30 (ProvisionHexa/AgentSetupMutation scoping), and the Architecture Assumption Index versioning scaffold were already uncommitted in the working tree from an earlier same-day pass, not from this session. Verified no live concurrent writer via a peer-session check (agents-81, confirmed scoped to the UX folder only, zero spine edits) before continuing; built this pass's edits on top of that prior content rather than reapplying it. Result: C-2/F-SPINE-4 (AD-12) and part of H-3/OQ-28 (AD-30 provisioning) were already resolved by that prior pass; this pass still had to fix AD-7's stale link/replace text, which contradicted the already-fixed AD-30, to close H-3 for real.
- (direction) Deferred (raised by agents-81, the concurrent UX session): add a role -> FrontComposer-policy-name column to the PRD's FR-33 table so the AD-30 mapping isn't reachable-wrong by a PRD-only reader (UX shipped Agents.ComplianceInspector, sourced from an unadopted review recommendation, before checking AD-30). PM/PRD-owner call, not an Architecture edit; no spine change made for this.
- (event) Reviewer Gate v5 dispatched (rubric walker, verified-current, adversarial-divergence) against the amended spine. Rubric: FAIL -- new Critical (AD-7's pre-post re-check still routed a detected removal to PostingFailed instead of AD-5's new Approved/PostingFailed->Abandoned(RemovedInConversations) path, contradicting the just-amended AD-5 and FR-18/FR-2 almost verbatim), Medium (AD-15's AgentReadinessStatus phrasing still read as bare-enum styling despite AD-10's composite-wrapper fix), Low (redundant AD-5 phrasing); independently re-confirmed C-2/C-3/C-4/C-5/C-8, H-1..H-5/H-7/H-11, the medium items, and the sprint-status.yaml correction as genuinely closed. Verified-current: PASS WITH FINDINGS -- the new .NET SDK servicing-risk claim's specific date was wrong (last 10.0.3xx patch was the 2026-08-11 bundle, not 2026-09-08; 2026-09-08 is when the *next* bundle shipped without a 10.0.3xx member), and ARCH-A-4's table row didn't mirror the Stack table's advisory-escalation language; reconfirmed the Microsoft.NET.Test.Sdk/xunit.runner.visualstudio addition and all other Stack entries unchanged. Adversarial: FAIL -- new Critical (AD-5's MessageId-unavailable clause said 'stays PostingFailed' even when invoked for the new Approved-origin abandon check, which would force an illegal Approved->PostingFailed transition), new High (AD-22's computed-subject-set text let the historical TAA-in-subject-set clause and the current-TAA-as-second-party clause collide, making the TAA-second-party branch permanently unreachable on a same-referent reading; AD-2/AD-7/AD-30 left hexa's own Party deactivation/replacement post-provisioning fully unaddressed), new Low (AuditInspection's 'case' scope undefined); reconfirmed C-4/C-5, AD-8/AD-13 mode-scoping, and AD-7/AD-2/AD-30 ProvisionHexa agreement as genuinely closed.
- (decision) Reviewer Gate v5 findings fixed in place (no re-dispatch; applied directly per Finalize's 'apply the clear fixes yourself'): AD-7's pre-post re-check now moves the proposal being posted to Abandoned(RemovedInConversations) via AD-5's MessageId-lookup gate, never to PostingFailed, on a detected removal. AD-5's MessageId-lookup paragraph now explicitly covers both the PostingFailed-origin exits and the new Approved-origin RemovedInConversations abandon check, and an unavailable lookup now leaves the proposal in its own pre-check state (PostingFailed or Approved) rather than unconditionally 'stays PostingFailed' -- closing the illegal-transition hole. AD-5's redundant 'system abandons it, or the system system-abandons it' phrasing simplified. AD-15's AgentReadinessStatus reference reworded to match AD-10's composite-wrapper framing (sibling growth fields, not enum values). AD-22's second-party rule now distinguishes the historical TAA principal(s) recorded in the computed subject set from the TAA currently holding the role at inspection time, so the TAA-as-second-party branch is reachable exactly on TAA turnover. AD-22's 'case' scope now has a one-clause definition (Inspector-assigned grouping of Conversations, always Platform-Operator-gated). AD-7 gained an explicit clause: hexa's own Party deactivation/replacement post-provisioning fails every side-effecting step closed tenant-wide via the AD-12 Party-state gate (MissingPartyIdentity), with no live V1 repair command since ProvisionHexa is create-only -- named as an open item, not a promised path. The Stack table's .NET SDK servicing-risk claim corrected to the accurate dates (last 10.0.3xx patch in the 2026-08-11 bundle, band absent as of the 2026-09-08 bundle) with a dotnet/core release-notes source added; ARCH-A-4's table row now mirrors the advisory-escalation target. Re-ran lint_spine.py clean (0 findings) after all fixes.
- (event) spine finalized (Update pass closed 2026-09-09): frontmatter status remains final, updated stays 2026-09-09 (same-day edit). Follow-up not actioned in this pass, owner PRD/bmad-prd: prd.md section 8.1 still names AD-5, AD-12, and AD-22 as open 'Spine defects' -- all three are now corrected in the spine and the PRD callout is stale; recommend a bmad-prd update to reconcile section 8.1 so upstream and the spine don't silently diverge, per the skill's Update-intent guidance.
- (event) Validate gate run 2026-09-09 (round 3) against the working-tree spine (main @ 4ec626d, plus uncommitted round-2 Update pass edits): lint 0 findings; five lenses (rubric-v6, verified-current-v6, adversarial-v6, brownfield-drift-v6, security-v6) returned 3 critical, 8 high, 7 medium, 9 low (27 total). Gate verdict: FAIL. Root cause pattern this round: two of round 5's fixes only closed one level up while the rule/register they cite one level down still doesn't deliver what the fixed text now claims -- AD-7's Party-deactivation re-check cites an AD-12 enumeration that never lists Party state, and AD-22/ARCH-A-10's erasure-completeness claim cites launch-readiness-register.md's LR-AUDIT-PROTECTION-DELETION evidence contract, which was never edited to cover DeletionRequest symmetrically with LegalHold. Plus two independent fresh contradictions: the Deferred Beyond V1 table still describes LegalHoldRelease as single-approver, directly contradicted by AD-22's own current second-party rule; and AD-7's block-clear rule is under-restrictive against FR-2/FR-33/OQ-25 (a Facilitator can clear a TAA-set block under AD-7's current wording, which none of the three PRD sources allow). Adversarial also found a new AD-12/AD-5 gap: disabled-Agent semantics for PostingFailed retries are unstated. Brownfield reconfirmed the sprint-status.yaml Story 5.2 tracking-integrity fix held and all previously-tracked implementation debt (AD-2, AD-4, AD-29, AD-30, structural seed) remains open exactly as expected, no new drift. Report: VALIDATION-REPORT-2026-09-09-3.md and .html; reviews/review-2026-09-09-*-v6.md. Spine not modified (Validate intent).
- (constraint) Update pass 2026-09-09 (round 4): rolls VALIDATION-REPORT-2026-09-09-3.md (round-3 gate, v6 lenses) into an Update per its own Recommendation. Priority order: C-1/C-2/C-3 (spine-text/register only), H-1/H-2/H-3 (citation-not-defect pattern), H-4..H-8 (brownfield debt, no spine amendment), then Medium tier.
- (event) Round-3 C-1 (Deferred-table 'Two-person rule on legal-hold release' row contradicting AD-22) verified already closed: the current Deferred Beyond V1 table carries no such row. No action needed.
- (decision) Round-4 fix for round-3 C-2: AD-7's block-clear clause amended so clearing authority is bound to the specific Party who set the block (re-evaluated as currently held at clear time; a Facilitator who lost the role cannot clear, and a Facilitator can never clear a TAA-set block), or the Tenant Agent Administrator; a system-set ExternallyRemoved block, having no setting Party, stays clearable by the TAA or any current Facilitator. Brings the Rule paragraph into line with the second-update reconciliation paragraph, which already stated the narrower rule. Binds AD-7's clearing authority; prevents a Facilitator undoing a TAA-set block, matching FR-2/FR-33/OQ-25.
- (decision) Round-4 fix for round-3 C-3: launch-readiness-register.md LR-AUDIT-PROTECTION-DELETION evidence text (line 108) amended to name DeletionRequest/erasure symmetrically with LegalHold, closing the gap where AD-22/ARCH-A-10 claimed register symmetry the register's own text did not yet state. Binds the register's evidence contract to AD-22's actual claim.
- (decision) Round-4 fix for round-3 H-1: AD-12's side-effecting-step re-read enumeration extended to explicitly name Party state (hexa's own linked-identity liveness per AD-7) alongside Agent lifecycle, tenant enablement, per-Conversation block, and kill switch, so a literal compliant implementation actually detects a deactivated hexa identity as AD-7 already claims. Binds AD-12's re-read set; prevents the fix-the-citation-not-the-defect gap where AD-7 pointed at an AD-12 enumeration that never named Party state.
- (decision) Round-4 fix for round-3 H-2 [ASSUMPTION]: AD-12's disabled-Agent consequence clause extended to state explicitly that PostingFailed automatic/administrative retries are likewise suspended with their retry-window clock paused while the Agent is disabled, on the same terms as the kill switch; AD-5's deadline formula and PausedDuration wording generalized to accrue from either the AD-12 kill-switch suspension or an AD-12 disabled-Agent suspension, whichever applies, rather than naming only the kill switch. Product-level call (no PRD text specifies disabled-Agent PostingFailed-retry behavior); tagged ARCH-A-11 [ASSUMPTION] pending Product confirmation. Prevents two implementations diverging on whether a disabled Agent's retry budget silently exhausts or keeps posting.
- (decision) Round-4 fix for round-3 H-3: AD-30's Platform principal freshness re-check reworded to name its authoritative source unambiguously as the system tenant's own Tenants-projection Platform Operator role assignment (not the target/envelope tenant's projection, unlike User/Administrator), since FR-19 defines Platform Operator as the one non-tenant-scoped role. Closes the reopened ambiguity a prior round's fix was meant to settle.
- (decision) Round-4 Medium fixes applied: (1) Stack table Hexalith.EventStore gitlink corrected from stale 1b6f08d4 to the actual committed submodule pin e302432c (verified via git ls-tree); the other four gitlink rows remain exact matches, unchanged. (2) Consistency Conventions 'Provider attempt fingerprint' row reworded from plain SHA-256 to HMAC-SHA-256 under the per-tenant DigestKey, matching AD-29's blanket sensitive-content-digest rule and closing the textual ambiguity over whether per-message digests are separately keyed. (3) AD-22 given an explicit DigestKey rotation/lifecycle bound: DigestKey is never rotated for a tenant with any unerased protected content, and rotation (only after full-tenant erasure or an accepted-risk waiver) is itself a justified, audited lock-bearing operation [ASSUMPTION ARCH-A-12]. (4) Structural-seed classDiagram AuditInspection.Mode renamed to Scope to match AD-22's current 'case' prose (named Conversation or case), removing the orphaned attribute the rubric lens flagged.
- (event) Round-4 reviewer gate (v7): rubric, verified-current, adversarial-divergence, security/data-integrity dispatched as parallel subagents against the round-4 spine. Verdicts: rubric PASS WITH FINDINGS (0C/1H/1M/3L), verified-current PASS WITH FINDINGS (0C/1H/1M/1L), adversarial FAIL (2C/3H/1M), security FAIL (1C/1M). All prior round-3 Critical/High fixes independently re-confirmed genuinely closed by at least one v7 lens before any new finding was applied.
- (decision) Round-4-gate fix (adversarial CRITICAL): AD-30's Platform-principal freshness source corrected from the invented 'system tenant's own Tenants-projection' (round-4's own first-pass wording, itself wrong) to Hexalith.Tenants' actual global-administrators domain/projection, per references/Hexalith.Tenants/_bmad-output/project-context.md ('The two aggregate domains are tenants and global-administrators'). Closes a genuine domain-model conflation a builder reading AD-30 literally would have wired to the wrong, tenant-scoped source.
- (decision) Round-4-gate fix (adversarial CRITICAL): AD-7's block-clearing clause rewritten to disambiguate identity-bound vs role-bound authority explicitly: a TAA-set block is cleared by whichever Party currently holds TAA regardless of who set it (role-bound); a Facilitator-set block is cleared only by that same Party while still currently holding the Facilitator role, or by the current TAA at any time (identity-bound for the Facilitator path). Closes the ambiguity where 'the specific authority that set it' could be read either way by two compliant builders.
- (decision) Round-4-gate fix (security CRITICAL + rubric/adversarial HIGH, same root cause): redesigned the DigestKey rotation rule from a 'never rotate while unerased content exists' bar (practically unsatisfiable for any live tenant, and miscategorized as one of AD-12's closed nine lock-bearing families) to a versioned-key model: EXT-SECRETS-1 custodies every DigestKey version indefinitely while any digest under it survives; rotation mints a new version for future digests without invalidating history; short-lived digests carry no rotation risk since never compared across a boundary; rotation is Platform Operator-initiated, audited, content-free, and explicitly NOT lock-bearing. ARCH-A-12 reworded to match. Matches the versioned-key discipline already used for ContentSafetyPolicy/pricing elsewhere in the spine.
- (decision) Round-4-gate fix (adversarial HIGH): AD-13's Provider-attempt fingerprint given an explicit algorithm statement (HMAC-SHA-256 under the per-tenant DigestKey, matching AD-29's blanket sensitive-content-digest rule and the per-message digests it embeds), closing the contradiction where the Consistency Conventions summary row asserted HMAC-SHA-256 but AD-13's own text — the fingerprint's actual defining AD — never stated an algorithm.
- (decision) Round-4-gate fix (security + rubric MEDIUM, same finding): AD-12's dual-cause PausedDuration (kill-switch-pulled + Agent-disabled) given an explicit merge rule — union of wall-clock time either cause was active, no double-counting on overlap, clock resumes only once both causes clear.
- (decision) Round-4-gate fix (verified-current HIGH): ARCH-A-4's own stated escalation condition ('sooner on any security advisory against 10.0.3xx') has already fired — the 2026-09-08 10.0.12 bundle fixed disclosed CVEs including CVE-2026-69522 (CVSS 8.8 RCE) unreachable by the repo's capped 10.0.303 pin. Stack table and ARCH-A-4 row reworded from hypothetical/future framing to an explicit live-exposure statement with an immediate (not Story-5.6) target for the SDK pin specifically; test-stack catalog alignment keeps its Story 5.6 target.
- (decision) Round-4-gate fix (verified-current MEDIUM): Stack table's MediatR row corrected — 'not referenced by Agents projects' was true at the direct-reference level but misleading in effect, since AD-16's platform-owned EventStore SDK host Agents runs on is itself built on MediatR's IMediator, making MediatR's commercial license already operationally live for Agents.
- (decision) Round-4-gate fix (rubric LOW x3): AD-22 and AD-7 Prevents lines extended to name the divergences their newly-tightened rules guard against (unrotatable digest key; Facilitator overriding a TAA-set block); Consistency Conventions Identity row extended to list InspectionId among identities Agents owns (already defined on AuditInspection in AD-2, previously omitted from this summary row).
- (event) Adversarial v7's HIGH finding that AD-5 cites 'the AD-13 seam-2 existence read' was checked against the live text and not reproduced: the actual current text reads 'the AD-6 EXT-CONV-AI-1 seam-2 existence read', confirmed by a single grep match in the whole file. Not applied; recorded as a false positive, not carried forward as an open item.
- (question) Two items surfaced this round remain open, deliberately not applied to control scope: (1) round-3's carried Medium — AD-10's EntryMissing carve-out adds an 'in-flight interaction's' qualifier AD-2's unqualified 'previously-snapshotted' wording doesn't have; (2) rubric v7's residual Medium on the same AD-10/AD-2 pair, same root cause. Both need a single reconciling edit to AD-2 or AD-10 in a future round; revisit before Story 5.3/5.5 finalize their ProviderCatalog/readiness work.
- (event) spine finalized (round 4, 2026-09-09): lint_spine.py 11 findings, all pre-existing literal-TBD flags in the Architecture Assumptions table's TargetRetirementDate column (confirmed present even in the last-committed HEAD version before this round's edits, contradicting every prior round's claimed 'lint: 0 findings' -- a genuine tool/reporting discrepancy, not a regression from this round; not resolved here since fabricating dates for legitimately open items would violate the no-invented-content rule). Reviewer gate v7 (rubric, verified-current, adversarial, security) ran as parallel subagents; all round-3 fixes independently re-confirmed; 2 new Criticals, 2 new/carried Highs (one High found false-positive on inspection), 2 Mediums, 3 Lows found and fixed in this same pass. Frontmatter status remains final, updated 2026-09-09.
- (event) Validate round 5 (2026-09-09, v8 lenses) completed: lint 10 findings (pre-existing TBD placeholders, not a regression). Rubric FAIL (3C/4H/4M) - independently re-verified all seven PRD Section-8.1-listed Spine divergences are still present after four prior rubric rounds, most severely AD-5 vs FR-18 (forbids a human abandon from Approved that FR-18 grants; wrong MessageId-lookup ordering; missing PostingWindowElapsed/PostingPending deadline), AD-12's kill-switch trigger formula (uses the flat-20/tuple rule the PRD addendum records as explicitly rejected, not FR-28/A-17's distinct-Party-ratio/3-Party floor), and AD-13 (misorders FR-8's acceptance steps, wrongly requires Eligible Approver resolution in Automatic mode against FR-11). Adversarial PASS WITH FINDINGS (2C/2H/1M/1L) found two fresh internal self-contradictions: AD-30's Workflow-principal OnBehalfOfPartyId is stated as both a lifecycle constant and per-attempt-varying in the same paragraph (contradicting AD-21's citation of it); ConversationContextPolicy (AD-11's ContextPolicyReference) has no owning aggregate in AD-2's closed enumeration. Security PASS WITH FINDINGS (1C/0H/0M/1L): the round-4 DigestKey rotation redesign never binds a stored digest to the DigestKeyVersion that produced it, breaking idempotency-conflict correctness and long-horizon audit-evidence re-verifiability after a rotation - a new, foreseeable consequence of the round-4 fix, not a residual of the bug it replaced. Verified-current PASS WITH FINDINGS (0C/0H/2M/2L): CVE-2026-69522/10.0.3xx-abandonment claim independently reconfirmed true; Hexalith.EventStore gitlink already 2h11m stale (e302432c vs actual 0994c378); ARCH-A-4's 'immediate' SDK remediation not yet applied to global.json (still 10.0.301). Brownfield FAILS TO RATIFY, FIXABLE (0C/7H/2M/2L), unchanged from v6 (src/test byte-identical since a191d24); all 7 Highs are pre-existing already-tracked debt (Stories 5.2/5.3/5.6/5.7, DW-4); one new minor M1 (sprint-status.yaml Story 5.3 lacks an inline annotation for its superseded prior implementation). Report: VALIDATION-REPORT-2026-09-09-4.md/.html. Spine and memlog not amended by this run (Validate intent) - recommendation is to roll findings into an Update pass, C-1..C-3 first.
- (decision) C-1 (round-5 validation) closed: AD-5 rewritten to match FR-18 verbatim - a human abandon (Eligible Approver; Tenant Agent Administrator, audited, no Conversation read access) is now legal from Approved, the MessageId lookup is always skipped for any exit from Approved since no post was ever attempted, and the PostingWindowElapsed system-abandonment bound plus the PostingPending stored attempt deadline are now first-class AD-5 rule text.
- (decision) C-2 (round-5 validation) closed: AD-12's Second-update trigger-calculation paragraph (flat-20/(Party,Conversation,reason)-tuple rule, rejected by the PRD addendum) is superseded by a Third-update paragraph adopting FR-28/A-17's distinct-calling-Party ratio, 3-Party convening floor, and the 5-Party/50-call (else every-Party/10-per-Party, 20-50 bounded) minimum sample verbatim.
- (decision) C-3 (round-5 validation) closed: AD-13's acceptance-order sentence rewritten to the current FR-8 ten-step order (restoring the step-3 local block check) and Eligible Approver resolution rescoped to Confirmation Response Mode only, matching FR-8/FR-11; Automatic mode's zero-Approver substitution (Conversation Facilitator) is now stated explicitly.
- (decision) C-4 (round-5 validation) closed: AD-30's Workflow-principal envelope clause no longer fixes OnBehalfOfPartyId to 'the snapshot caller'; it now points at the per-attempt-varying rule stated later in the same AD-30 paragraph (and cited by AD-21), removing the two-unit charge-attribution contradiction the adversarial lens found.
- (decision) C-5 (round-5 validation) closed by design choice, not new design: ConversationContextPolicy stays an inline, self-versioned Agent field (not a 15th AD-2 aggregate) - AD-11 now states this plainly, strikes the 'publishes... version[s]' framing that read as a ContentSafetyPolicy analogy, names the Tenant Agent Administrator as the field's sole mutator, and puts its version increment on the same independent-counter terms AD-4 already gives ConfigurationVersion/InstructionsVersion/ApproverPolicyVersion. Chosen over adding a 15th aggregate because AD-2's aggregate list is closed ('exactly') and AD-4 already treats ContextPolicyReference as a snapshotted config field, not an independently-versioned published-policy reference like ContentSafetyPolicy - the inline-field reading requires no reopening of AD-2's closed enumeration. Flagging this choice for user review even though no blocking ambiguity remained.
- (decision) C-6 (round-5 validation) closed: every persisted digest under the AD-29 versioned-DigestKey model (the AD-29 command-idempotency-fingerprint record, the AD-13 ProviderAttempt fingerprint, and every per-message digest inside audit-evidence) now carries the DigestKeyVersion it was computed under alongside the digest value; AD-29, AD-13, and AD-22 all state that re-verification and idempotency-conflict comparison recompute against the recorded DigestKeyVersion, never the tenant's current one, closing the false-409-on-retry-after-rotation and unbounded-audit-reverification gaps the security lens found.
- (decision) H-1 (round-5 validation) closed: AD-6 now names seven EXT-CONV-AI-1 seams including seam 7 (message retraction signal, A-28); the Deferred Beyond V1 table's automatic-mode-retraction row updated to reflect the seam is now named but not yet Committed.
- (decision) H-2 (round-5 validation) closed: renamed AgentCallOperationStatus to AgentInteractionStatus everywhere in the Spine (AD-5, AD-15), matching FR-8's actual public write-path contract name; no second enum was intended.
- (decision) H-3 and H-5 (round-5 validation) closed together: AD-7's Second-update membership-reconciliation paragraph now gates ExternallyRemoved re-admission behind A-22 (presence alone never re-admits while A-22 stands; only the FR-33 clear row does, via ReadmitPending), adds the roster-freshness condition FR-2 requires before recording ExternallyRemoved, and states the outbound re-admission mirror call a clear issues. This also removes AD-7's direct ExternallyRemoved -> Joined-on-presence transition, so it now matches AD-2's state-machine graph exactly - closing both the FR-2 divergence (H-3) and the AD-2/AD-7 internal contradiction (H-5) with one text fix.
- (decision) H-4 (round-5 validation) closed: AD-14's Second-update protection-attestation paragraph now states the FR-34 signed-identity pin explicitly (Security-qualified build identity sourced through EXT-SECRETS-1, verified and pinned on every check, every seal/unseal call verified against the pin) alongside the pre-existing canary, and states the canary is liveness-only, never proof of protection - closing the pass-through-wrapper gap the omission left open.
- (decision) H-6 (round-5 validation) closed: AD-8's Eligible Approver predicate gains a fourth explicit exclusion - not the Approver who requested a regeneration of the version under decision - closing the self-approval path for Regenerated (not Edited) versions the adversarial lens found; the PRD section-3 Eligible Approver definition does not yet state this fourth exclusion, so this is flagged as a PRD-text gap worth carrying back, not only a Spine fix.
- (decision) Medium tier (round-5 validation) closed: frontmatter binds line updated PRD OQ-1..OQ-30 -> OQ-1..OQ-31 (matching the PRD's current OQ count).
- (decision) Medium tier (round-5 validation) closed: AD-17's Second-update blocker-emission paragraph (now Third-update) adds SuspensionReviewOverdue (FR-28/FR-30 overdue containment or trigger review) and DeferredAssumption (FR-28 item 9, a Release-PM-deferred late ARCH-A row) to the authoritative-producer blocker vocabulary, closing the gap against FR-28/FR-30's defined blocker set.
- (decision) Medium tier (round-5 validation) closed: AD-21's AdmitCall citation corrected from the stale 'FR-8 step 4' to the current 'FR-8 step 5' (step 4 is now Provider/model eligibility, step 5 is rate limits, per the third PRD update's FR-8 renumbering).
- (decision) Medium tier (round-5 validation) closed: AD-22 gains the FR-24/A-23 post-hoc compliance-inspection aggregation guardrail - more than 5 distinct Conversations touched by one Inspector's post-hoc inspections in a rolling 30-day window is one wide inspection requiring prior Platform Operator approval from the sixth Conversation onward, and an unreviewed inspection blocks the Inspector's next one - closing the previously-absent governance bound.
- (decision) Medium tier (round-5 validation) closed: Stack table's Hexalith.EventStore gitlink corrected from the stale e302432c to the actual committed pin 0994c378, re-pulled fresh via git submodule status immediately before this Update pass closes, per the round-5 report's own caution that this spine churns same-day.
- (decision) Medium tier (round-5 validation) closed: ARCH-A-4's SDK escalation is resolved, not just tracked - global.json bumped from 10.0.301 (capped 10.0.303, CVE-2026-69522-exposed) to 10.0.400, matching every sibling repo's pin; the Stack table's .NET SDK row and the ARCH-A-4 assumption row (Architecture Assumptions table) both updated to record the SDK sub-item as retired 2026-09-09, while the test-stack catalog alignment sub-item (Microsoft Testing Platform v2 test.runner, xUnit v3/NSubstitute/Test.Sdk catalog versions) remains open, targeting Story 5.6. architecture_assumption_index_version bumped 3 -> 4 (ARCH-A-INDEX-4) since ARCH-A-4's assumption text and retirement condition changed.
- (decision) Medium tier (round-5 validation, brownfield M1) closed outside the spine: sprint-status.yaml Story 5.3's backlog line gets a one-line inline annotation noting the completed, fully-tested prior implementation (2569/2569 tests passing) underneath it, and why it still isn't promoted (AD-2's platform/tenant ProviderCatalog split, NC-5.3-PLATFORM-CATALOG-SCOPE, unreconciled in code) - matching the annotation style already used on Story 5.2's line.
- (decision) Deliberately deferred again this round (low-confidence, recurring since round 3): AD-10's EntryMissing carve-out wording ('the Agent's own currently-selected entry, or an in-flight interaction's previously-snapshotted entry') vs AD-2's unqualified 'previously-snapshotted or currently-selected' carve-out wording - AD-10 adds an 'in-flight' qualifier AD-2 doesn't have. Not applied this round because it is a wording-precision question, not a behavioral contradiction found in three successive reviewer passes, and the round-4 Update already logged the same open question with the same revisit condition (before Story 5.3/5.5 finalize), which still stands.
- (decision) Round-9 verified-current gate F1 (Critical) fixed same-round: the initial 10.0.400 global.json pin escaped the abandoned 10.0.3xx band but did not actually reach the CVE-2026-69522 fix - dotnet/core release notes confirm the 2026-08-11 bundle (10.0.11) shipped SDK 10.0.400 without the fix, and only the 2026-09-08 bundle (10.0.12) shipping SDK 10.0.401 contains it; rollForward:latestPatch only rolls to an installed patch, never to whatever is latest upstream, so the pin itself had to name the fixed patch. global.json corrected to pin 10.0.401 directly; Stack table and ARCH-A-4 reworded to state this precisely and re-dated retired 2026-09-10. This diverges from every sibling repo's 10.0.400 pin by one patch, which is expected and temporary (siblings will catch up on their own schedule) - not logged as a new ARCH-A row since it is self-explanatory and non-blocking.
- (decision) Round-9 security-data-integrity gate Critical fixed: AD-22's short-lived-digest sentence used to list the ProviderAttempt fingerprint as an example of a digest that 'carries no rotation risk' - directly contradicting AD-13/AD-29's new DigestKeyVersion persistence for that exact fingerprint. Reworded: only an ephemeral, never-persisted verdict-cache key is rotation-risk-free; the ProviderAttempt fingerprint is explicitly named as persisted and DigestKeyVersion-carrying, for the life of its interaction, so a retry can be re-verified after rotation.
- (decision) Round-9 security-data-integrity gate Medium fixed: AD-29's digest rule reworded to separate 'is HMAC-SHA-256' (applies to all five digest kinds) from 'carries DigestKeyVersion' (applies only to the three persisted kinds - idempotency record, ProviderAttempt fingerprint, per-message audit-evidence digests); verdict-cache keys explicitly carry no DigestKeyVersion since they are ephemeral and never persisted, closing the reviewer's over-broad-sweep ambiguity.
- (decision) Round-9 security-data-integrity gate Medium fixed: the ProviderAttempt mermaid class diagram gains the DigestKeyVersion field, and the Consistency Conventions table's 'Provider attempt fingerprint' row now states descriptors store the DigestKeyVersion alongside the digest and safe scalar inputs - closing the diagram/table lag behind AD-13's prose.
- (decision) Round-9 rubric gate Critical fixed: AD-5's automatic-mode posting paragraph used to claim 'automatic mode itself attempts no further automatic retry; the sole automatic-mode recovery is a new Agent Call' - directly contradicting FR-11's own text ('PostingFailed with the same retry bound... Spine AD-5 states that automatic mode retries nothing; FR-11 governs') and PRD section 8.1's explicit divergence listing. Rewritten to state the automatic posting record uses the identical FR-18 bounded retry (automatic and administrative) as a human-decided proposal, with Facilitator substituting for Eligible Approver and no accessibility condition on the TAA's abandon (FR-33) - adopting FR-11's own text, not new design.
- (decision) Round-9 rubric gate High fixed: AD-14's protection-attestation paragraph gains the FR-34/A-24 trigger list (startup, every readiness evaluation, a bounded cadence - hourly by default - and any host composition change), closing the gap where only startup and readiness-evaluation triggers were stated.
- (decision) Round-9 rubric+adversarial gates High fixed together: AD-6 no longer calls all seven EXT-CONV-AI-1 seams 'committed' - only the original six are stated as committed; the seventh (message retraction, A-28) is explicitly named but not yet Committed and not a usable path, matching the Deferred Beyond V1 table's own wording exactly instead of contradicting it. This was a self-inflicted contradiction from the round-5 H-1 fix (adding seam 7 to the same committed-list clause it was drawn from) surfaced by both v9 lenses independently.
- (decision) Round-9 adversarial gate Critical fixed: AD-13's sequence diagram still labelled the EligibleApprover-resolution step 'EligibleApprover resolution (both modes)' at its old pipeline position, contradicting the round-5 text fix scoping it to Confirmation mode only. Diagram step relabelled 'Confirmation mode only; skipped in Automatic mode' and moved to right after the rate-limit AdmitCall step and before the Conversation context read, matching the corrected FR-8 order.
- (decision) Round-9 adversarial gate High fixed: AD-2's literal membership state-machine graph string was missing a NeverJoined -> Blocked edge, even though AD-7's own 'a block may be set from any state' rule (and FR-2) require it - a block can be set before hexa ever joins a Conversation. Graph string amended to add Blocked as directly reachable from NeverJoined.
- (decision) Round-9 adversarial gate Medium and rubric gate Critical, closed together with new ARCH-A rows rather than silent spine edits: (1) ARCH-A-13 flags that AD-8's new fourth Eligible-Approver exclusion (not the regeneration-requesting Approver) has no textual basis in FR-7's canonical two-exclusion definition - it closes a real self-approval gap the round-5 adversarial lens found, but is Architecture-only tightening pending Product confirming FR-7 should state it; AD-8's text now cites [ASSUMPTION ARCH-A-13] rather than silently presenting the exclusion as already PRD-aligned. (2) ARCH-A-14 flags that the PostingPending stored attempt-timeout deadline has no concrete duration fixed by the PRD ('no shorter than the seam's posting timeout' is a floor, not a value) or by this spine; AD-5's deadline sentence now cites [ASSUMPTION ARCH-A-14]. architecture_assumption_index_version bumped 4 -> 5 (ARCH-A-INDEX-5).
- (decision) Round-9 adversarial gate Low fixed: frontmatter sources list was missing VALIDATION-REPORT-2026-09-09-4.md, the round-4 UPDATE-REPORT files, the v8 review family (this round's actual trigger), and now the v9 review family and this round's own update report; all added.
- (event) Round-5 Update pass closed 2026-09-10: pass 1 applied VALIDATION-REPORT-2026-09-09-4.md's C-1..C-6, H-1..H-6, and Medium-tier findings per the user's stated priority order; pass 2 ran a fresh v9 Reviewer Gate (rubric, verified-current, adversarial-divergence, brownfield-drift, security-data-integrity) and fixed everything it found, including a same-round correction to the global.json SDK pin (10.0.400 was insufficient; corrected to 10.0.401). Two genuine design/authority gaps were closed by flagging rather than silent invention: ARCH-A-13 (AD-8's fourth Approver exclusion exceeds FR-7's current text) and ARCH-A-14 (no concrete PostingPending timeout duration exists anywhere). architecture_assumption_index_version 3 -> 5. Spine frontmatter updated: updated 2026-09-10, status remains final. UPDATE-REPORT-2026-09-10.md documents both passes.
- (constraint) Update input: the final PRD at _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md (updated 2026-09-09) and final UX pair at _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/EXPERIENCE.md and DESIGN.md plus reconcile-validation-2026-09-10.md (updated 2026-09-10) are the load-bearing sources for this update.
- (decision) User confirmed the recommended ARCH-A-13 disposition: an Approver who requested regeneration remains ineligible to approve the resulting regenerated version. Binds the AD-8 Eligible Approver predicate and PRD FR-7, prevents request-and-approve self-dealing, and retires ARCH-A-13 once FR-7 is synchronized.
- (decision) Clarify operation-family ownership without adding a family: the Tenant Agent Administrator administrative-retry request is a lock-bearing ProposalResolution public command requiring no Conversation read access; after acceptance, the workflow's MessageId lookup, full pre-post revalidation, and posting attempt each declare ConversationPosting. ConversationPosting is workflow-only and outside the browser-session advisory-lock classification, relying on AD-5/AD-13/AD-29 deterministic idempotency. Binds command/activity classification and prevents either over-gating the administrator request on Conversation membership or under-gating the actual post.
- (decision) CapacityQueued exposes durable QueueId and safe queued state but no ordinal queue position in V1. WeightedRoundRobinV1 and concurrent cross-tenant queue mutation make a displayed position unstable and potentially disclose other-tenant activity; UX must render status without a position unless a future AD adds a privacy-safe position contract. Binds capacity result shape and prevents independently-built allocator and UI units from inventing incompatible queue-position semantics.
- (decision) CurrencyMismatch is a required additive ProviderReadinessReasonCode and must be added to the launch-readiness register vocabulary already made authoritative by AD-10. Binds provider readiness across Server/API/BFF/UI and prevents AD-21 currency rejection from degrading to Unknown or PlatformNotReady.
- (decision) Reconcile the AD-2/AD-10 EntryMissing carve-out in favor of AD-10's narrower rule: only the Agent's currently-selected entry or an in-flight interaction's previously-snapshotted entry may report EntryMissing; terminal historical interaction views use their immutable snapshot/audit evidence and receive no catalog existence carve-out. Binds tenant-safe reads and prevents terminal history from becoming a catalog-existence oracle.
- (decision) EXT-CONV-UI-1 comprises four artifact kinds plus a focus-return contract: Conversation-owned Call hexa action contribution, MessageId-keyed decoration slot whose sole provenance source is the Agents accessor, GetCallabilityAsync, and an Agents-owned persistent status region beside the trigger; the seam also returns focus to the Conversations-owned trigger or a deterministic successor. Binds AD-31 and the external dependency record and prevents outcomes being announced into the destroyed dialog or provenance being sourced twice.
- (decision) Lint-gate cleanup: replace legacy literal TBD retirement-date cells with explicit Unscheduled revisit milestones, preserving each open assumption and its RQ-1 blocking effect without inventing dates. Binds assumption-index mechanics and prevents placeholders from masquerading as decisions.
- (event) Update draft distilled 2026-09-12 from PRD/UX reconciliation: AD IDs preserved; ARCH-A-INDEX-5 -> ARCH-A-INDEX-6; ARCH-A-13 retired; external dependency and readiness registers synchronized; deterministic lint clean (0 findings).
- (decision) PRD/UX reconciliation: ProviderCatalog owns the four-field data-handling record, retained contractual reference, monotonic DataHandlingVersion, tightening declaration, and field-level diff; TenantProviderEnablement owns the tenant's accepted version and explicit accept/decline decision. DataHandlingAcceptance is a lock-bearing public operation family; Provider readiness blocks with DataHandlingAcceptanceLapsed unless the current version is accepted or a declared-tightening 30-day grace applies. Each ProviderAttempt audit record carries both current and in-force versions. Binds FR-4/FR-5/OQ-29 and prevents platform enablement from being mistaken for tenant acceptance.
- (decision) PRD/UX reconciliation: MirrorRefused is a durable terminal mirror-outcome flag on ConversationAgentState after a typed permanent Conversations refusal ends retries; the authoritative block remains in force. Tenant Agent Administrator remediation is exactly re-set (new block mirror) or clear (re-admission mirror), and product-metrics/status count Conversations carrying the flag. Binds FR-2/FR-25/SM-C4 and prevents permanent mirror failure from being treated as transient or invisible.
- (decision) PRD/UX reconciliation: AD-31's dialog owns no post-submit live-region node. The persistent contributed status region owns submitted, authoritative-pending, terminal, and authorized-Approver pending-proposal announcements after submit; the dialog may announce only outcomes that occur while it remains open. Binds live-region ownership and prevents announcements targeting destroyed DOM.
- (decision) PRD/UX reconciliation: AD-15 public parity includes all FR-25 and SM-C4 status classes: blocked, failed, unavailable, acceptance-lapsed, and separately-reported authorization, lifecycle, suspension, membership rejection, capacity, dependency, and protection outcomes, plus proposal markers and Conversation mirror flags. Binds status contracts and prevents API/UI projections from silently dropping required counters.
- (decision) PRD reconciliation consequence of the user-approved regeneration-requester exclusion: regeneration is rejected before Provider work unless at least one Eligible Approver would remain after excluding caller, last editor, and regeneration requester; UJ-3 becomes a two-person regeneration-and-approval journey. Binds FR-7 and prevents a permitted regeneration from stranding the proposal.
- (constraint) PRD reconciliation: External V1 prerequisites must list EXT-PARTIES-1, and EXT-CONV-AI-1 seam 7 remains explicitly requested but uncommitted and unusable until its register record changes; neither may disappear from the spine's dependency view.
- (decision by assistant) Launch-readiness OperationGateMatrixVersion 3 appends DataHandlingAcceptance as a lock-bearing public operation family; ProviderInvocation, ConversationPosting, and SystemTimer remain workflow-only families outside the browser advisory-lock contract.
- (decision by assistant) ProviderReadinessReasonCode and the readiness register now include DataHandlingAcceptanceLapsed as a hard blocker; ProcessingRegion is owned only as a mandatory data-handling-record field, not duplicated as an optional capability flag.
- (decision by assistant) EXT-PARTIES-1 is now an authoritative Uncommitted dependency record, and EXT-CONV-AI-1 explicitly carries requested seam 7 while remaining Uncommitted and unusable.
- (decision by assistant) ConversationAgentCallPanel owns no live-region node; validation, submitted, and all later announcements route through the persistent EXT-CONV-UI-1 status region.
- (decision by assistant) Reviewer-gate fix: DataHandlingAcceptance omits LR-PROVIDER to avoid self-blocking its own acceptance-lapsed remediation; matrix v3, audit evidence, and restrictive-viewport coverage treat it as the tenth lock-bearing family.
- (decision by assistant) Reviewer-gate fix: EXT-PARTIES-1 now requires an owner-selected V1 branch—AI-type identity or Product/Parties-approved Organization identity-by-id fallback—and blocks RQ-1 while uncommitted without blocking transitional story development.
- (decision by assistant) Reviewer-gate fix: CurrentMirror is a versioned direction/outcome record; data-handling grace is cumulative, exclusive, and non-extendable; regeneration authorization and requester attribution append atomically before Provider work.
- (assumption by assistant) ARCH-A-15 added: imported Dapr 1.18.5 is superseded by security-motivated upstream 1.18.7; atomic package-family upgrade or Security-approved bounded exception is due 2026-09-30 before adoption.
- (correction by assistant) Reviewer-gate reconciliation supersedes the earlier block-only MirrorRefused note: ConversationAgentState owns one direction-aware CurrentMirror(BlockVersion, Direction, Outcome, AttemptId); refused Remove leaves Blocked, refused Readmit leaves ReadmitPending, successor attempts supersede lower versions, late outcomes are audit-only, and SM-C4 counts only the current Refused outcome.
- (decision by assistant) Final cross-document reconciliation binds EXT-PARTIES-1 to an owner-selected Branch A AI-type or Branch B Product/Parties-approved Organization identity-by-id contract; Uncommitted Branch-B-compatible work is development-only. Administrative retry may consume any remaining shared attempt through server CanAdministrativeRetry and serializes with automatic retry at one expected revision. Epics now own all affected behavior and all ten matrix-v3 lock-bearing families.
- (event) Reviewer gate passed on the final current tree: rubric, verified-current, and adversarial-divergence reviews each reported zero Critical, High, Medium, or Low findings; architecture linter and diff checks passed.
- (event) The 2026-09-12 validation report is the authoritative update input and supersedes the preceding clean-gate claim: resolve its three Critical findings before its twelve High findings, then run a fresh complete gate.
- (decision) C-1 / AD-20 amended: every retry, regeneration, approval, and pre-post check must pass both the immutable snapshot policy and every then-current applicable policy; one-pass dominance is allowed only when formally equivalent and contract-tested. Policy evolution never mutates transport input under an existing AttemptId, and AD-13 retry evidence records both policy outcomes fail closed.
- (decision) C-2 / AD-22 preserves the existing all-or-nothing class-deletion contract. Legal hold and protected deletion share a tenant-and-frozen-resource-set durable prepare fence; no DestroyDek may run until every resolved interaction and export copy is reserved, holds contend on the same fence, and completion requires recoverable acknowledgements plus failure-injection evidence.
- (decision) C-3 / AD-17 adopts target-aware OperationGateMatrix v4: every family declares Platform or Tenant evaluation scope, and a bootstrap or repair command omits only the gate it is authorized to establish while all unrelated gates remain fail closed. Global catalog and policy mutation never evaluates a tenant-only gate; empty-state and broken-state fixtures are mandatory.
- (decision) H-1 / AD-8 binds Agents as owner of one durable single-flight approver recheck per tenant and proposal. Cadence remains ARCH-A-20; authoritative data freshness must be within that cadence. Awaiting proposals require two consecutive authoritative empty rosters at least one cadence apart; PostingFailed receives marker-only handling; unavailable evidence records ResolutionUnavailable and retries; typed source deletion or removal abandons with SourceConversationUnavailable.
- (decision) H-2 / AD-20 binds an EventStore-authoritative tenant invalidation epoch, durable conversation scan status, and tenant-keyed lease coordinator. Policy publication or HMAC rotation makes the epoch pending before activation, never serves an earlier-epoch verdict, uses readiness-profile scan bounds, and makes calls wait within that bound or fail exactly ContextReadUnavailable(RescanPending). Missing bounds fail closed; coarse current contracts and worker implementation are delivery debt.
- (decision) H-3 / AD-8 requires Parties-authoritative human type and active liveness validation both when predefined approvers are configured and on every runtime resolution. Non-human, unavailable, stale, or ambiguous evidence contributes no eligible approver and produces one typed fail-closed rejection family; the external Parties dependency must contract this seam.
- (decision) H-4 / AD-2 and AD-21 separate three records and lifetimes: immutable rolling RateLimitLedger consumption expires only by its window; OpenInteractionLedger holds one lease for the original caller interaction until overall interaction or automatic posting terminality; monthly BudgetLedger reservations settle and reconcile in the original UTC budget period. Each has an explicit tenant partition, acquisition key, and release or expiry event.
- (decision) H-5 / AD-7, AD-22, and AD-30 add stable AuthenticatedHumanActorId to every human-originated User, Administrator, and Platform principal independent of role, PartyId, and target tenant; authorization, audit, separation-of-duties comparisons, and trusted-envelope authentication bind it. Workflow automation is a distinct non-human principal and cannot satisfy a human second-party requirement unless Product, Governance, and Security later approve a changed contract.
- (decision) H-6 / AD-7 makes AgentInteraction events the proposal truth. The same stream transition emits an outbox record with source revision; ConversationAgentState applies it idempotently with a high-water mark. Removal freezes the checkpoint, blocks new proposals, reconciles the materialized index against authoritative interaction truth, abandons matches, and rechecks to none before completion; both crash boundaries require failure-injection evidence.
- (decision) H-7 / AD-6 separates the optional automatic-retraction seam as EXT-CONV-RETRACTION-1. EXT-CONV-AI-1 now contains only core V1 seams 1 through 6; the new dependency remains Uncommitted and has no consumer unless OQ-23 selects the automatic branch.
- (decision) H-8 / AD-30 authenticates trusted extensions over AD-29 length-prefixed purpose-tagged canonical bytes containing schema, principal kind and human actor, PartyId and role basis when present, command and operation family, target, correlation and causation, idempotency tuple with payload fingerprint and digest-key version, audience, issued and expiry instants, nonce, and key version. EXT-SECRETS-1 owns bounded lifetime, skew, overlap, rotation, and emergency revocation; replay is permitted only as exact idempotent redispatch, verification is constant-time and fail closed.
- (decision) H-9 / AD-22 binds an Agents-owned export-artifact interface with immutable tenant/export key, authenticated encryption, content-free interaction index, signed canonical manifest, and physical purge receipt included in deletion completion. Exact expiry, later-hold treatment, restore behavior, and provider technology remain an explicit Product/Governance/Security blocking decision; export creation and RQ-1 fail closed until its versioned policy is approved.
- (decision) H-10 current-reality correction: Agents already consumes Dapr.Client and Dapr.AspNetCore transitively through EventStore at the parent-authoritative Builds gitlink catalog version 1.18.5; only Dapr.Workflow adoption is future. The dirty Builds checkout exposes 1.18.7 but is not root authority. ARCH-A-15 remains a ship blocker until the parent gitlink and package family advance to 1.18.7 or later with compatibility evidence, or Security records a bounded exception; implementation and integration are delivery debt.
- (decision) H-11 / AD-15 renames current public-contract parity to required completion parity and assigns missing vocabulary to Stories 5.6, 6.2, 6.6, 7.1, 7.4 through 7.6, and 8.5. Existing legacy contracts are implementation debt and are not described as shipped.
- (question) H-12 is tracking and evidence debt, not an architecture change: EXT-HOST-1 is Uncommitted, Stories 5.1 and 5.2 say backlog or not run, and the required verifier is absent while sprint status says done and in progress. Owner discussion must choose rollback to current authority or document historical completion with a changed-dependency exception and replacement evidence; the spine does not rewrite history.
- (decision) Final-spine distillation removes superseded correction layers from AD-2, AD-7, AD-12, AD-14, AD-17, and AD-22 while preserving their stable IDs; history remains only in this memlog. ReadmitPending ownership and recovery are explicit, ARCH-A-14 remains a posting blocker, legacy conformance tests and the bUnit lag remain delivery debt, and architecture_assumption_index_version advances 7 to 8 for the corrected ARCH-A-15 scope.
- (event) Reviewer Gate v3 first batch failed with 2 Critical and 9 deduplicated High findings after rubric, verified-current, and adversarial review; apply Critical fixes before High fixes and rerun the complete gate.
- (question) C1 hold-versus-deletion product policy remains unresolved: the PRD requires DeletionDeferredByHold and hold precedence, while the prior spine invented rejection and a post-arm hold cutoff. Preserve PRD deferral and disable every irreversible destruction until Product and Governance approve a versioned linearization policy; represent this as a normative OpenDecision blocker.
- (decision) C2 matrix v4 readiness observations gain versioned typed ConditionResults with closed per-GateId condition inventories. Overall state is a deterministic fold; bootstrap evaluation recomputes that fold after excluding only exact registered condition ids. Missing, duplicate, unknown, stale, or fold-inconsistent results block, and fixtures combine an omitted failure with an unrelated failure.
- (decision) Gate H-R3-1 fixes safety-decision identity: attempt-bound SafetyDecisionId includes an aggregate-assigned SafetyEvaluationOrdinal; exact replay reuses it, while each retry evaluation increments it without changing AttemptId or transport inputs.
- (decision) Gate H1 binds safety outcomes: any pre-transport snapshot-or-current failure terminalizes that ProviderAttempt as SafetyFailed and it can never revive. Original generation terminalizes the interaction; regeneration preserves the existing proposal. Rate consumption ages out normally, monetary reservation and capacity are released NotInvoked, and the original-caller lease releases only if the overall interaction terminalizes; approval rejection and pre-post PostingFailed retain their existing stage-specific states.
- (decision) Gate H2 binds comparable human identity: EXT-PARTIES-1 returns a non-PII versioned Party-to-AuthenticatedHumanActorId binding with historical validity. Each subject-set event persists actor id and binding version at action/resolution time; later separation-of-duties compares those recorded actor ids. Missing or ambiguous binding fails compliance operations closed.
- (decision) Gate H3 separates LogicalCommandId from per-delivery DeliveryNonce. Redispatch retains logical identity/idempotency/payload but mints nonce, issued/expiry, key version, and tag. Exact duplicate delivery is idempotent only when canonical bytes match; nonce reuse with changed bytes rejects. Replay-record retention is a mandatory bounded EXT-SECRETS profile value covering tag and retry horizons; emergency-revoked deliveries redispatch under a current key.
- (decision) Gate H-R3-2/H4 binds rolling-rate admission: AgentInteraction first appends RateAdmissionAuthorized with an aggregate ordinal; RateAdmissionId is purpose-tagged over tenant, interaction, ordinal, on-behalf-of Party, and Conversation. Live preparations reserve scope capacity. AgentInteraction owns RateAdmissionCommitDecided after both prepare acknowledgements; before it recovery may abort both, after it recovery must complete both and preparations cannot expire. Provider progression waits for both commit acknowledgements.
- (decision) Gate H5 reconciles the bound PRD without deciding OQ-23: optional message retraction is EXT-CONV-RETRACTION-1 throughout the dependency, A-28, and OQ-23 text; the six core EXT-CONV-AI-1 seams remain independently committable.
- (decision) Gate H6 corrects export runtime threshold to EXT-EXPORT-STORE-1 Available with its compatibility command passing against the deployed target; Committed permits readiness and contract work only.
- (decision) Gate H7 makes spine OD records normative launch-readiness inputs with version, owner, approval roles/evidence, state, and affected gates. Open or malformed records emit OpenDecision and block. EXT-EXPORT-STORE-1 availability evidence must bind the approved export-lifecycle policy version.
- (decision) Gate Medium cleanup removes the stale nine-family count, registers SuspensionReviewOverdue and DeferredAssumption, and removes the transient claim that SDK 10.0.401 is unavailable; no build or test success is inferred.
- (decision) AD-8/AD-22/AD-30 now use a versioned historical Party-to-AuthenticatedHumanActorId binding: action events persist PartyId, actor id, binding version, and validity evidence; later separation-of-duties checks compare recorded stable actor identities and fail closed on gaps or mismatch.
- (decision) AD-21 and AD-29 now give rolling-rate admission an aggregate-assigned ordinal and deterministic RateAdmissionId, count live prepares against capacity, and make AgentInteraction.RateAdmissionCommitDecided the durable two-ledger commit/recovery authority before downstream execution.
- (decision) AD-29/AD-30 split stable LogicalCommandId from per-delivery DeliveryNonce. Redispatch mints nonce, time bounds, key version, and tag; replay records survive the greater of cryptographic acceptance and command recovery horizons; changed-field nonce reuse and revoked-key envelopes fail closed.
- (decision) launch-readiness-register.md now owns typed direct preconditions for bootstrap variants and normative versioned OD records. Open hold/deletion precedence and export lifecycle decisions emit OpenDecision and block their affected operations and RQ-1; no product choice was inferred.
- (direction) The bound PRD now treats EXT-CONV-AI-1 as six core V1 seams and EXT-CONV-RETRACTION-1 as the optional OQ-23 dependency, matching the architecture and external register without deciding OQ-23.
- (event) Applied the first complete reviewer round's clear Critical/High fixes, removed stale SDK-environment prose, reran deterministic spine lint clean, and dispatched independent v4 rubric, verified-current, and adversarial-divergence reviews.
- (decision) Matrix v4 now has a gate-free, directly evidenced LR-EVENTSTORE observation bootstrap/repair variant; all other observations still require LR-EVENTSTORE, and gate-free variants record RegistryCheckpoint.NotRead rather than a fabricated revision.
- (decision) TenantKillSwitch pull and release are separate variants: containment pull bypasses readiness records that may be failing and attempts the authoritative EventStore append after direct authority/target/revision checks; release retains normal readiness and review evidence.
- (decision) OpenInteractionLedger now follows an AgentInteraction-owned authorize/prepare/commit-or-abort/ack protocol. Preparations count against concurrency until an authoritative decision, never expire autonomously, and recovery reuses the original lease.
- (decision) TrustedEnvelopeReplay is the EventStore owner of DeliveryNonce first-seen state. It binds logical id, key version, canonical tag digest, and retention deadline before target dispatch; exact replay is idempotent and changed reuse fails closed.
- (decision) AuditExport now participates in the tenant ProtectionFence before content read. Export/deletion races serialize through prepare/abort/commit tokens and a committed export-index high-water so no artifact escapes a later deletion snapshot.
- (decision) AD-15 now preserves the PRD AgentInteractionStatus vocabulary exactly; rate, budget, capacity, membership, dependency, suspension, and proposal lifecycle remain typed reason/progress or ProposedAgentReplyState data rather than new interaction-status members.
- (event) The v4 rubric/adversarial pass found two Critical and six High issues across bootstrap, containment, export fencing, provisioning replay, gate scope, open leases, trusted-envelope replay, and status parity. Applied the clear architecture fixes and kept unresolved product policy in OD records.
- (decision by architecture-update) Reviewer v5 C-1 resolved: export abort retains the tenant protection fence in ExportCleanupPending until every frozen partial artifact, manifest, index entry, wrapped key, and lifecycle-covered copy is proven never written or physically purged or key-destroyed with durable receipts.
- (decision by architecture-update) Reviewer v5 C-2 and H-R5-3/H-R5-4 resolved: trusted-envelope first-seen registration is one non-public pre-command EventStore primitive limited by capability and ACL to the reserved-system issuer-and-nonce replay stream; the record binds target tenant and rejects cross-tenant nonce reuse.
- (decision by architecture-update) Reviewer v5 H-1 resolved: every terminal pre-acceptance outcome, explicitly including SafetyFailed, authorizes prepared open-lease abort by exact event revision; committed-lease terminal release stays distinct.
- (decision by architecture-update) Reviewer v5 H-R5-2 resolved: AgentInteraction records one mutually exclusive budget disposition; absence of ProviderInvocationAuthorized never releases a reservation, and invocation disposition is batched atomically with provider authorization.
- (decision by architecture-update) Reviewer v5 H-2 resolved: emergency Platform containment pull requires a target-bound confirmed SM-4 incident fact and revision; Release Operator pull continues to require its recorded trigger-review decision.
- (decision by architecture-update) Reviewer v5 H-3 resolved: OperationGateMatrix v4 now closes replay-admission and governance-protection prepare, abort-cleanup, commit, destruction, purge, completion, and recovery variants with exact decision and owner evidence.
- (decision by architecture-update) Reviewer v5 H-4 resolved: rate and open-lease preparation deadlines derive once from EvaluatedAt plus the positive versioned AdmissionPreparationTimeout in the capacity-fairness profile, and every ledger must receive the exact deadline and profile version.
- (decision by architecture-update) Reviewer v5 H-5 resolved: the launch register now materializes Dapr security and sprint-tracking open decisions, with the sprint item scoped to delivery authorization rather than runtime or RQ-1.
- (decision by architecture-update) Verified-current v5 H1/H2 resolved in planning authority: epics retain story IDs while adopting the complete owner, readiness, projection, matrix-v4, protected-export, fence, decision, and all-copies deletion contracts; implementation remains delivery debt.
- (open by architecture-update) Architecture-owned assumption retirement dates and the PostingPending timeout remain unresolved and fail closed as existing assumption debt; no owner date or duration was invented.
- (decision by architecture-update) Recorded governance branches use dedicated gate-free recovery variants: mutable readiness gates authorize initial/reversible work but cannot strand cleanup or an irreversible branch after its immutable decision; recovery uses only frozen decision/policy/revision facts, least-privilege capability, and directly required dependency outcomes.
- (decision by architecture-update) Trusted-envelope replay equality compares only immutable authenticated delivery fields and reuses the first record's stored FirstSeenAt and RetainUntil; replay registration precedes target-command construction and AD-29 target idempotency.
- (decision by architecture-update) Pre-command denial evidence uses a second non-public content-free security recorder with caller-independent actor/workflow/system routing, deterministic SecurityObservationId, an EXT-HOST-1 replicated security-audit spool, exact acknowledgement recovery, and least-privilege SecurityEventLog drain ACLs.
- (decision by architecture-update) A retry-time safety failure after ProviderInvocationAuthorized preserves the attempt's immutable InvocationSettlement budget disposition and may release money only from the confirmed-no-use outcome revision; it never creates NotInvokedRelease.
- (open by architecture-update) OD-RATE-CONCURRENCY-CONSUMPTION-1 remains Open for Product + Architecture: decide whether a concurrency-rejected original call consumes Party/Conversation rolling rate, the single common owner order, and FR-25 attribution. Until approval, AgentCallAcceptance and Story 6.4 stop before either ledger; no default is inferred.
- (decision by architecture-update) RateAdmissionAuthorized stores AdmissionEvaluatedAt plus scope-specific PartyResetAt and ConversationResetAt derived once from frozen windows; ledgers and projections reuse those instants and never derive expiry from prepare/commit receipt time.
- (decision by architecture-update) The open hold/deletion precedence decision blocks deletion-directed destruction/purge only; mandatory cleanup of an uncommitted export under its existing fence token remains authorized and must converge before fence release.
- (decision by architecture-update) ArchitectureDecisionRecord in reserved tenant system is the durable append-only owner for OD contracts, approvals, supersession, and affected evaluations. Platform records but does not approve; signed EXT-SECRETS-1 authority manifests bind exact decision versions and approver evidence, and runtime never parses planning documents.
- (decision by architecture-update) Stories 5.4, 5.5, 6.1, 6.3, 6.4, 8.1, 8.2, 8.4, 8.6, and 8.8 now own the delivery evidence for replay/security recording, decision materialization, Dapr decision gating, safety-rescan recovery, joint rate/open admission, human actor binding, and public-versus-internal matrix-v4 conformance; these remain implementation debt, not claims of shipped behavior.
- (decision) ArchitectureDecisionRecord successors are pending until root or effective-predecessor governance authorizes the exact contract; effective and pending affected evaluations remain unioned, so a version cannot weaken its own quorum or blockers.
- (decision) PRD FR-33 is authoritative: only a freshly authorized Release Operator records externally signed architecture or planning-decision packages; the recorder and platform custodian cannot mint or substitute approver evidence.
- (decision) Reversible hold, export, and deletion preparation records one mutually exclusive PrepareRecoveryDispositionDecided Resume or Abort before recovery; only the matching dedicated row can advance.
- (decision) The only pre-command mutations are TrustedEnvelopeReplay first-seen registration and the content-free durable security recorder, each with its own confined capability.
- (decision) Legal hold freezes and pins overlapping committed export artifacts as well as interaction DEKs; Active and release require exact store acknowledgements and the approved export-lifecycle version where artifacts overlap.
- (decision) Human separation of duty compares stable AuthenticatedHumanActorId: Party-bearing Users use EXT-PARTIES, Administrators use tenant role plus durable actor evidence, and Platform approvers use Tenants global-administrator evidence without an invented tenant Party.
- (decision) EXT-PARTIES direct story consumers are 5.2, 5.4, 6.6, 7.1 through 7.5, 8.1, 8.2, 8.3, and 8.8.
- (decision) Deferred PRD OQ-18, OQ-23, and OQ-31 are materialized as OD-PRD-OQ18-HISTORICAL-SAFETY-1, OD-PRD-OQ23-AUTOMATIC-RETRACTION-1, and OD-PRD-OQ31-INSTRUCTION-PROTECTION-1 without selecting outcomes.
- (decision) AdmissionPreparationTimeout is positive and strictly shorter than both frozen rolling windows; PreparationDeadline precedes both reset instants, and either reset before both acknowledgements drives the same abort.
- (decision) Safety epoch activation freezes an EventStore position, enumerates active Agents tenants and caught-up safety-verdict-directory Conversation keys with count/hash/high-water evidence, and requires concurrent tenant and unindexed Conversation initialization.
- (decision) OD-HOLD-DELETION-PRECEDENCE-1 applies to hold acceptance and recovery when DeletionArmed overlaps; while open neither competing branch advances and architecture selects no outcome.
- (decision) Repository reality has no shipped RateLimitLedger, OpenInteractionLedger, or BudgetLedger; all three remain implementation debt, not current architecture.
- (decision by architecture) AD-5 and Story 7.4 require durable Approved, then durable PostingPending, before any Conversations append; current post-before-dispatch behavior remains delivery debt.
- (decision by architecture) AD-22 and EXT-EXPORT-STORE-1 require linearizable fence or store authorization for every expiry, purge, key destruction, provider TTL, backup, and restore cleanup; missing control evidence retains data.
- (decision by architecture) AD-17 recovery is branch-specific: hold prepare failure never authorizes Abort, export and deletion Abort require already-recorded closed facts, and recorded recovery is phase-pinned against later decision successors.
- (decision by architecture) OD-HOLD-PREPARE-CANCELLATION-1 surfaces the unresolved Product, Governance, and Security choice for cancelling a reversible preparing hold; no cancellation outcome was invented.
- (decision by architecture) ArchitectureDecisionCatalog is the runtime required-decision authority with stable literal DecisionId values, minimum contract versions, and effective plus pending affected-evaluation union.
- (decision by architecture) OD-RELEASE-RECORDER-SCOPE-1 and PRD OQ-33 surface the unresolved authority source for a tenant Release Operator recording Platform-scoped decision streams; all principals remain denied until Product resolves it.
- (decision by architecture) Decision recording always separates the recorder stable actor from every approval-evidence actor, including a dual-hatted human.
- (decision by architecture) AD-22 second-party evidence is tagged by principal kind: Party-bearing User history uses HumanActorBindingVersion; Administrator and Platform history use their authoritative role-source revisions without invented Parties.
- (decision by architecture) PRD FR-28, OQ-22, OQ-24, A-26, and A-28 now scope the optional automatic-retraction decision only to AutomaticModeEnablementEligibility and derive qualification dependencies from actual consumers.
- (decision by architecture) Safety policy activation waits on finite checkpoint manifests only; post-checkpoint tenants and Conversations perform local version-pinned initialization and cannot extend the global barrier.
- (decision by architecture) Deletion under an active or preparing pre-arm hold is durably DeletionDeferredByHold and re-evaluated after release; only post-arm contention remains governed by the unresolved precedence decision.
- (decision by architecture) TenantGovernancePolicy is the sole durable owner of SM-4 confirmations, trigger-review decisions, and kill-switch transitions; Story 8.4 and matrix v4 bind exact expected-revision recovery.
- (decision by architecture) Proposal reject and abandon revalidate ProposalResolution authority; membership or safety failure supports a no-post terminal result and cannot become a gate that strands abandonment.
- (decision by architecture) v9 correction: ProtectionFence owns the sole ExportCommitDecided append and committed-export high-water; AuditExport acknowledgement is secondary and only it enables key delivery, while deletion already honors the fence decision.
- (decision by architecture) v9 correction: ArchitectureDecision catalog publication and activation are separate commands; ActivateCatalog alone records the required-record revision/digest manifest and makes PendingCatalog effective.
- (decision by architecture) v9 correction: missing or invalid catalog authorization emits the existing OpenDecision blocker with safe detail CatalogAbsentOrInvalid; no peer blocker code was added.
- (decision by architecture) v9 correction: NFR11 recovery is branch-specific and phase-pinned after a recorded prepare disposition, human hold release, export commit decision, or DestructionStarted; later decision versions cannot strand recovery.
- (decision by architecture) v9 correction: no-hold deletion may append DestructionStarted at the exact fence revision; only an armed-deletion/later-hold contention requires the approved precedence decision.
- (decision by architecture) v9 correction: hold-prepare cancellation decision affects only the Abort/unwind and Story 8.1 cancellation branch and is not an RQ-1 input.
- (decision by architecture) v9 correction: the approved PRD OQ-31 decision unconditionally gates all Story 8.3 authorization and deletion-signal handling; export-lifecycle dependencies remain conditional only on export-bearing frozen sets.
- (decision by architecture) v9 correction: human approval appends Approved only; full current pre-post validation precedes a separate BeginPosting append, which alone authorizes the Conversations effect.
- (decision by architecture) v9 correction: the call-admission diagram applies the exact approved rate/concurrency joint disposition and does not preselect consumption for a rejected call.
- (decision by architecture) v9 correction: Story 8.3 owns EXT-CONV-AI-1 Conversation-deletion propagation through an authenticated source envelope, signal-derived DeletionRequestId, closed ConversationDeletionPropagation workflow, and dedicated intake matrix variant.
- (decision by architecture) v9 correction: a recorded human hold-release decision phase-pins recovery to its decision revision, frozen set/token, effective export-lifecycle version, and exact store target when artifacts overlap.
- (note by architecture) v9 metadata reconciliation: spine binds PRD OQ-1 through OQ-34; blocking-decision summary includes recorder scope and catalog activation; v9 reviewer reports are authoritative sources for this pass.
- (decision by architecture) v10 correction: ConversationAgentState is the authoritative content-free interaction-permit directory and owns an orthogonal permanent deletion barrier; permit registration and barrier installation serialize at one expected revision.
- (decision by architecture) v10 correction: final call acceptance appends one permit plus protected target-limited creation outbox; the interaction first append emits directory acknowledgement and the sole workflow-start outbox, closing crash and projection-lag gaps.
- (decision by architecture) v10 correction: Conversation deletion installs the barrier, freezes a finite permit count/hash/high-water manifest, converges every queued/in-flight/posting member to authoritative outcomes, acknowledges the directory, then freezes the deletion set.
- (decision by architecture) v10 correction: EXT-CONV-AI-1 seam 6 is a source-atomic durable outbox/feed with stable logical signal identity, ordered backfill, retry until authenticated Agents acknowledgement, target rollover, and poison-entry blocking.
- (decision by architecture) v10 correction: deletion origin is a closed union; operator origin carries Platform requester plus Inspector approval, while Conversation origin carries authenticated source evidence and no fabricated human fields or local Abort authority.
- (decision by architecture) v10 correction: matrix v4 now has exact Conversation deletion intake, barrier, convergence, directory acknowledgement, set-freeze, and pre-arm DeletionDeferredByHold variants with origin-compatible recovery.
- (decision by architecture) v10 correction: BootstrapRecorderScope is the sole catalog-independent target-limited path for recording a future independently signed recorder-scope choice; it does not select a source or authorize another decision operation.
- (decision by architecture) v10 correction: ExportPreparing phase-pins lifecycle version and store target; successors govern only new prepares, and export-free deletion completes without reading or fabricating the export-lifecycle decision.
- (decision by architecture-update) Replaced the Conversation deletion barrier re-read with a ConversationAgentState-owned effect-lease cutover: Closing serializes new permits and effect leases, manifests all earlier outboxes and active leases, and Effective requires exact owner, external-outcome, Budget/capacity/posting settlement, and directory acknowledgements before deletion freeze. This is architecture protocol, not implementation-complete status and not a Product choice.
- (decision by architecture-update) Assigned Story 6.1 an idempotent legacy interaction-directory migration: quiesced topology cohort, finite EventStore checkpoint/count/hash/high-water inventory, content-free MigratedFrom backfill, direct-create freeze, two identical high-water scans or independently signed exact-empty proof, and late-write invalidation. Current direct AgentInteraction streams remain implementation debt until this cutover passes.
- (decision by architecture-update) Added durable export-key authorize/effect/result/recovery: AuditExport records a deterministic principal/purpose/expiry/version-bound DeliveryId after commit acknowledgement; EXT-SECRETS-1 must deliver idempotently directly to the principal and expose exact outcome lookup; Agents records safe results only and never key material.
- (decision by architecture-update) Propagated conditional export lifecycle/store authority to the first export-bearing deletion prepare and phase-pinned destruction/purge/recovery, while preserving the recorded no-copy proof path for export-free deletion without reading or inventing OD-EXPORT-LIFECYCLE-1.
- (decision by architecture-update) Strengthened EXT-PROTECTION-1 with an explicit target interaction protection-key alias for the Conversation directory creation outbox and dual-stream typed-Erased replay proof after one interaction-DEK destruction; the dependency remains Uncommitted and current EventStore no-op protection remains implementation debt.
- (decision by architecture-update) Added a fail-closed legacy protection precondition to the directory cutover: a finite inventory must be exactly empty or prove every sensitive field already protected under the accepted engine/key contract. Legacy plaintext is not retroactively erasable and remains PayloadProtectionUnavailable pending a separately authorized migration or eradication procedure with all-copy receipts; no Product/Governance retention outcome was invented.
- (correction by architecture-update) Reconciled specialist and verified-current v11 findings: Architecture Assumption Index is singular at ARCH-A-INDEX-9; AD-7 and AD-13 now share one directory-first intake/acceptance order; Story 8.2 reserves success for ExportCommitted; present-state wording and Builds checkout status are corrected; the partial Conversations adapter binding is recorded as implementation debt.
- (decision by Codex) Replaced revocable active Conversation effect leases with same-owner Reserved, CommittedToEffect, Settled, and CancelledBeforeCommit phases. Closing races with acquire and commit on ConversationAgentState; only the committed revision authorizes a deterministic target step, and committed work must settle before Effective.
- (decision by Codex) Restored the Product-fixed FR-8 order. Directory intake may be registered earlier, but Conversation membership is leased step 10 after rate/open, Confirmation-only approver resolution, Context, Budget reservation, and safety; prior failures cause no membership effect.
- (decision by Codex) Made InteractionDirectoryMigration an explicitly Platform-Operator-started target-limited Workflow. Its Begin variant omits only the circular audit/protection/deletion gate, and readiness requires an authoritative EventStore namespace migration epoch guard with legacy direct-write capability revocation.
- (decision by Codex) Surfaced OD-LEGACY-PLAINTEXT-DISPOSITION-1 without choosing an outcome. Legacy plaintext remains PayloadProtectionUnavailable and blocks affected directory completion, deletion, stories, and qualification until Product, Governance, Security, and the EventStore Maintainer approve an outcome-specific all-copy procedure.
- (decision by Codex) Moved immutable deletion-inventory ownership to ProtectionFence. ProtectedDeletion first records a reversible candidate; fence acceptance serializes it against holds and export prepare, commit, cleanup, copy, and migration-write-fence revisions, with same-request refreeze and acknowledgement recovery.
- (decision by Codex) Added exact manifest-bound deletion Workflow authority for settling an existing Provider Budget reservation and releasing existing capacity only, and added a separate committed ProposalMutation lease for generated output persistence.
- (decision by Codex) Made the recorder bootstrap initial catalog version and digest authoritative: the first PublishCatalog must match and consume that exact commitment; successors retain ordinary predecessor governance.
- (decision by Codex) Made DeletionDestructionStarted a gate-free continuation of the recorded Resume, prepare, and armed branch. Export-bearing destruction and later recovery use the prepare-pinned lifecycle and store authority plus direct availability, never a later mutable decision union.
- (decision by Codex) Reconciled Story 6.1 and external dependency consumers with EXT-HOST-1, EXT-SECRETS-1, and EXT-PARTIES-1, and bound its acceptance evidence to FR-8 order, effect commit, generated-output leasing, migration authorization, write fencing, and legacy plaintext rejection.
- (decision by architecture-update) Resolved v13 effect-cut sequencing: before reserve/commit only closed content-free owner-local checks are legal; ProposalMutation and ConversationPosting must commit before approval safety, pre-post safety, protected version access, or Conversations roster/access reads, and typed negative results settle the committed lease.
- (decision by architecture-update) Resolved generated-failure variants: Provider errors, timeouts, and output-safety negative results append content-free GenerationFailed under the committed ProviderInvocation lease with proposal/version/content/failure-record identities absent; only allowed complete output uses a separate ProposalMutation(RecordGeneratedVersion) lease. Failed bytes are not retained because no current Product requirement authorizes retention; any future retention needs an approved separate record protocol.
- (decision by architecture-update) Resolved operator-origin deletion authority: the target-limited GovernanceProtection Workflow may build/supersede the reversible candidate, accept the fence inventory, acknowledge it, defer for holds, and execute the recorded deletion branch only when bound to the exact requester, distinct Inspector approval, tenant, stable request, approved scope, candidate ordinal, and revisions; refreeze may discover resources inside but never expand approved scope.
- (decision by architecture-update) Resolved migration repair epoch transition: DirectoryInvalidated blocks new permit/acquire/commit work; repair freezes all current directory high-waters, installs an old-epoch bridge limited to exact pending outboxes and committed leases, cancels reserved leases before effect, drains every frozen member, then atomically activates a successor epoch and revokes old epoch plus bridge before complete reconciliation can restore DirectoryReady.
- (note by architecture-update) Reconciled handoff metadata: Story 5.5 now declares EXT-PROVIDER-1; Stories 6.1, 7.4, and 8.3 plus matrix v4, implementation conventions, dependency register, sequence, class model, and delivery-debt ledger carry the corrected contracts and failure-injection obligations. Product-owned assumption dates and posting timeout remain surfaced and blocked, not invented.
- (event by architecture-update) Re-distilled the build substrate after v13 closure: removed amendment-shaped capability wording and compressed historical SDK/correction narration into current authoritative rules while preserving all AD-1 through AD-31 and all open-decision identifiers.
- (decision by architecture-update) Closed migration-repair workflow-start handling: the old-epoch bridge starts only a workflow whose WorkflowStart lease was already committed; otherwise repair cancels any reservation and consumes the outbox as StartSuppressedByMigrationRepair, while creation outboxes still materialize their deterministic owner.
- (decision) Validation v14 reconciliation: initial generated-output safety denial/unavailability public status remains an unresolved Product + Security choice, tracked as OD-INITIAL-OUTPUT-SAFETY-STATUS-1; output is always discarded with no proposal/message/failure-content record, and live Provider work plus Stories 6.1/6.3/RQ-1 fail closed until approval.
- (decision) FR-8 Confirmation step 6 now uses its own same-owner ApproverResolution effect lease before any Conversations/Parties Eligible Approver read; call-time and scheduled results bind and settle before Context step 7, and Closing races reservation/commit at the directory owner.
- (decision) Every User edit/regeneration/approval/rejection/abandon action atomically reserves ProposalMutation with an immutable protected action-intent outbox; commit precedes target/dependency work, and the original request or exact Interaction Workflow recovery must record the same intent-bound result, acknowledge the outbox, and settle without impersonation.
- (decision) Conversation-deletion Effective now requires the complete permit, creation/workflow-start/User-action-intent, lease, rate, open-interaction, pre-Provider Budget, Provider Budget/capacity/posting decision-and-acknowledgement manifest; the deletion Workflow may recover existing owner decisions only and cannot invent new reservations or outcomes.
- (decision) Both deletion origins bind inventory acceptance to the current InteractionDirectoryMigration Ready epoch/guard and EventStore namespace high-water, then require deletion-scope write-fence authorize/effect/result before preparation and direct exact verification immediately before DestructionStarted; pre-start drift refreezes under the same request/scope.
- (decision) An installed deletion-scope fence persists across migration epochs and exposes a complete violation ledger; after DestructionStarted an authoritative in-approved-scope violation enters an append-only containment manifest with full destruction/purge receipts, while outside-scope or unresolvable violation is a security incident that blocks completion and requires new human authority.
- (decision by codex) Preserved AD-1 through AD-31 and added canonical GovernanceScopeV1 with deterministic predicate digests. ExactInteractions and ExactConversation are executable from EventStore-owned membership facts; ClassUtcRange is blocked by OD-GOVERNANCE-CLASS-RANGE-SCOPE-1 until Product, Governance, and Security choose the class vocabulary, owner, UTC instant, boundaries, and public meaning.
- (decision by codex) Required both OperatorApprovedDeletion and ConversationApprovedDeletion to install the same EventStore scope admission fence, converge every request-scoped ConversationAgentState owner cut to Effective, and record a global effect-cut receipt before either origin can build an inventory candidate. The later content write fence remains a separate authorize/effect/result boundary before preparation.
- (decision by codex) Removed architecture authority for operator-origin deletion Abort. OD-OPERATOR-DELETION-CANCELLATION-1 now owns that Product/Governance/Security choice; while Open only Resume/retry is legal. A future approved Abort must prove cleanup complete, remove both exact EventStore fences, release every request-scoped owner cut, and only then record DeletionAborted.
- (decision by codex) Made migration repair race-free by recording the epoch plan, then installing and recording an atomic EventStore directory-repair fence/checkpoint before freezing the repair cohort. The cohort now includes permits, creation/workflow-start/User-action-intent outboxes, leases, and active deletion fences; successor activation compares all high-waters and revokes the old epoch, repair fence, and bridge.
- (decision by codex) Added the explicit PreProviderCapacityDispositionDecided(CancelNoInvocation) recovery path for capacity queued or admitted before Provider authorization when deletion Closing wins. Exact allocator lookup and proof of no authorization/begin/active invocation are mandatory before release and acknowledgement; absence cannot invent no-use.
- (decision by codex) Moved OD-INITIAL-OUTPUT-SAFETY-STATUS-1 consumption to Provider authorization: the committed Provider lease phase-pins the effective decision revision, contract digest, selected status/reason/metric/open-lease mapping, and catalog activation before Provider work. Result and recovery use that pin; current shipped SafetyFailed mapping is recorded as implementation debt, not Product authority.
- (decision by architecture-update) Closed the admission-fence serialization gap by making Rate/Open, Budget reservation, and capacity admission interaction-owned phase authorizations whose exact revisions are sole external capabilities. Both deletion and migration-repair guards block those phase starts; Closing and repair manifests include every authorization, result, disposition, and acknowledgement.
- (decision by architecture-update) Added a separate accepted admission-fence violation ledger. A pre-destruction acceptance invalidates the global cut and derived candidate/token while retaining restrictive fences, and requires a higher-ordinal fence plus complete owner recut; a post-destruction acceptance records DeletionIntegrityCompromised and permanently blocks successful completion.
- (decision by architecture-update) Resolved the Provider decision-read ownership cycle: commit a generic Provider lease first, then record exactly one interaction-owned ProviderInvocationAuthorized or ProviderInvocationNotAuthorized branch. The negative branch settles existing Budget, capacity, target, and lease obligations without Provider work; the authorized branch alone phase-pins the approved output-status mapping.
- (open-decision by architecture-update) Surfaced, without choosing, OD-HUMAN-EXACT-CONVERSATION-SCOPE-1 for human-origin temporal/fence semantics and OD-OPERATOR-DELETION-NONTERMINAL-DISPOSITION-1 for operator-deletion terminal treatment. Exact-interaction and source-approved Conversation deletion remain executable; affected human/operator branches remain restrictive while Open.
- (decision by architecture-update) Deletion recut integrity is ordinal and append-only: each (DeletionRequestId, ScopePredicateDigest, AdmissionFenceOrdinal) owner cut is an immutable separately keyed Closing-to-Effective cycle, successor cycles carry every prior still-restrictive obligation plus new or violating winners without reopening an earlier Effective fact, and only the current ordinal's ZeroAcceptedViolationsSinceInstall(InstallCheckpoint, VerifiedHighWater) proof may bind its global cut while prior violations remain visible.
- (decision by architecture-update) Deletion destruction starts only at the EventStore guard's atomic CommitDeletionDestructionStartBarrierEffect: it serializes with every matching admission append, verifies the current ordinal and both separate ledgers, commits immutable DestructionSealed, and returns the one-shot sole irreversible capability; ProtectionFence authorization and result recording cannot replace that barrier, and direct read-then-append authorization is forbidden.
- (decision by architecture-update) Use one tenant GovernanceScopeGuard as the EventStore linearization owner for legal-hold contender registration and deletion DestructionSealed. Hold registration that wins after deletion authorization makes that authorization stale; an unresolved armed contention advances neither branch, so Architecture defines ordering without choosing the Product outcome.
- (decision by architecture-update) Contain an accepted deletion-admission violation through an explicit pre-first-global-cut branch or exact-existing-artifact branch. Both preserve restrictive evidence and idempotently assign one successor ordinal; retries, concurrent detection, and lost acknowledgement resolve to that same ordinal and receipt.
- (decision by architecture-update) Keep one continuously installed content guard keyed by DeletionRequestId and ScopePredicateDigest. After a pre-seal admission recut, append an authorized successor ordinal and token binding to the existing guard; never silently reuse, remove, or reinstall it.
- (decision by architecture-update) Bind deletion destruction to a deterministic single-use DestroyDekManifest batch for the sorted accepted key-target manifest. The protection owner consumes the whole batch atomically or none, returns an ordered per-target receipt vector, gives each containment ordinal a distinct batch, and linearizes revocation against any unconsumed batch after integrity compromise.
- (decision by architecture-update) Bind every deletion start-barrier authorization to the exact observed GovernanceScopeGuard revision and authorization ordinal. Seal commits conditionally at that revision; any intervening registration, release, or guard mutation returns an authenticated stale result that must be recorded before reauthorization.
- (decision by architecture-update) Permit continuous content-guard successor binding to advance from the latest installed binding across a complete gap-free ordered chain of invalidated or tokenless ordinal receipts. An authorization overtaken by another admission violation terminates as ObsoleteBindingAuthorization without binding an invalid token.
- (decision by architecture-update) Allocate one monotonic ProtectionFence containment ordinal per authoritative post-seal content target, freeze its singleton manifest, and require an explicit revision-bound authorize, GovernanceScopeGuard issue-or-stale effect, and recorded result before protection consumption.
- (decision by architecture-update) Make accepted and containment DestroyDekManifest capabilities non-expiring but revocable, single-use, target-bound, and versioned. Their capability-key version remains verifiable until consumption or revocation; outage, migration, and restore reuse the same batch identity and no renewal branch exists.
- (decision by architecture-update) Retain each non-expiring destruction capability signing-key version for the full retention of its batch identity and terminal outcome, including verification needed for exact retry and lookup after consumption or revocation; terminal batch state ends destructive eligibility but does not erase verification evidence.
- (note by architecture-update) v20 complete gate failed C1/H3 plus rubric completion race: post-start holds were not durable blockers for new containment; final completion was not guard-linearized; destruction capability trust lifecycle, acceptance-time ordinal attribution, and same-DEK alias coverage were incomplete. v21 resolves the mechanical contracts while leaving Product precedence Open.
- (decision by architecture) The EventStore append guard assigns AcceptedAtAdmissionFenceOrdinal and AcceptedAtGuardHighWater at acceptance linearization, atomically contaminates that current partition, and treats caller ordinal as diagnostic only; missing acceptance evidence is integrity-unattributable and blocks deletion.
- (decision by architecture) A post-seal hold appends PostStartHoldContenderObserved with DestructionAlreadySealed on GovernanceScopeGuard. While OD-HOLD-DELETION-PRECEDENCE-1 lacks an exact deletion-allowed post-start outcome, every still-preventable batch dispatch, containment issue, purge, and completion blocks; already-consumed vectors remain immutable.
- (decision by architecture) Every accepted or containment batch requires a guard-revision-conditional DeletionBatchDispatchAuthorized receipt after issue and before protection. It serializes earlier holds/compromise before the effect; a later hold is ordered after the batch-specific uninterruptible authorization and restricts all remaining work.
- (decision by architecture) DeletionBatchCapabilityV1 is an EXT-SECRETS-1-custodied ES256 detached JWS over closed RFC8785 fields and committed protection audience. Routine rotation retains verifiers; emergency compromise blocks unconsumed old attestations and permits only same-batch same-manifest one-active-attestation replacement plus successor dispatch.
- (decision by architecture) Post-seal containment deduplicates by complete tenant/interaction/protection-key alias. Every violation resource maps through an append-only ProtectionFence coverage receipt to one original batch/per-target result; only an uncovered key target receives a containment ordinal and capability.
- (decision by architecture) Deletion completion is a ProtectionFence authorization followed by GovernanceScopeGuard conditional completion seal and exact result mirror. Any intervening guard mutation makes the authorization stale; DeletionCompletionSealed rejects later matching writes and alone may be mirrored as complete.
- (note by architecture-update) OperationGateMatrixVersion 5, conventions, PRD, epics, dependency contracts, model, migration preservation, and implementation-debt ledger now carry the v21 deletion refinements. Missing assumption retirement dates and PostingPending duration remain surfaced owner decisions, not invented values.
- (note by architecture-update) ARCH-A-4 wording now accurately separates SDK 10.0.401 from the Microsoft.DiaSymReader.Native advisory floor; because the assumption text changed, the architecture assumption index advances from 9 to 10. No retirement owner/date or readiness outcome changed.
- (decision by codex) 2026-09-13 v22: Preserve AD-1 through AD-31. After DeletionBatchDispatchAuthorized, a later legal hold remains ordered at dispatch, while admission-integrity and capability-key compromise cancel-versus-consume outcomes linearize only at EXT-PROTECTION-1. BlockDeletionBatchConsumption and ReserveAndConsumeDeletionBatch conditionally change one local batch state; ConsumptionReserved is the irreversible instant for those cancellation reasons. Exact blocked, reserved, consumed, or revoked outcomes survive crash, migration, and restore. Re-attestation requires a protection-owned ConsumptionBlocked(CapabilityKeyCompromise) receipt, remains blocked through guard credential replacement and successor dispatch, and reopens only through protection-owner ActivateReattestedDeletionBatch. Signed capability requests bind SigningAttemptOrdinal and IntendedIssuedGuardRevision; a stale signed but never issued attempt requires exact no-issue proof and terminal SignedAttestationObsoleteUnissued before a deterministic successor attempt. Emergency per-tenant key compromise enters through a third non-public AD-3 primitive, IDeletionCapabilityCompromiseRegistrar, never through a human Platform or Workflow principal. OperationGateMatrixVersion 6 owns these variants. The runtime affected-evaluation baseline for OD-HOLD-DELETION-PRECEDENCE-1 now includes both pre-seal contention and every post-start containment, dispatch, protection-reservation, purge, completion, and StoryAuthorization 8.1/8.3 branch that consumes it. No Product outcome was selected. Current code lacks all v22 mechanics; that absence is delivery debt.
- (decision by architecture) v23 closes the v22 Critical/High reviewer findings without choosing a Product outcome: OperationGateMatrixVersion 7 retains v6 and assigns one ProtectionFence-owned DestructionSealId from the accepted deletion token, scope-predicate digest, and accepted-manifest digest before first barrier authorization; accepted and containment batch ids, JWS bytes, and signing-request ids use that stable seal id while SigningAttemptOrdinal and IntendedIssuedGuardRevision may change across proven-obsolete pre-issue attempts, and actual CommittedDestructionSealGuardRevision/CommittedIssuedGuardRevision remains separate result evidence. Re-attestation activation now has deterministic DeletionBatchReattestationActivationId inputs and atomically arbitrates the replacement tenant-key block at the protection owner: absence permits Unconsumed, while an existing/racing compromise returns ActivationBlockedByReplacementKeyCompromise and retains/installs the exact new-key ConsumptionBlocked state for another same-batch re-attestation; activation never clears a global block. The AD-3 conformance inventory is exactly three target-limited pre-command primitives, Story 5.4 owns only replay and security-recorder delivery, Story 8.3 owns compromise registration, and Story 8.3 hold precedence covers both post-arm/pre-seal contention and every post-start branch through completion while ordinary no-contender deletion remains evaluable. Historical missing v21 and unrun v22 specialist citations were removed; current v23 reviewer sources are declared. These are architecture/handoff corrections; absent code and live seams remain explicit implementation debt.
