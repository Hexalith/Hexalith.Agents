---
title: Closure Matrix - 2026-09-08 Validation Findings Against The 2026-09-09 Spine
lens: closure-verification
target: ../ARCHITECTURE-SPINE.md
target_updated: 2026-09-09
applies: ../VALIDATION-REPORT-2026-09-08.md
reviewed: 2026-09-09
reviewer: independent closure reviewer (validation gate)
---

# Closure Matrix — 2026-09-09

## Method

Every finding in `VALIDATION-REPORT-2026-09-08.md` (C-1, C-2, H-1..H-16) and every medium/low item in the five detailed reviews (A-11..A-20, S-8..S-15, R-4..R-17, B-5..B-18, V-3..V-13) was re-read against the re-distilled `ARCHITECTURE-SPINE.md` (`updated: 2026-09-09`), the amended `launch-readiness-register.md` and `external-dependency-register.md` (working-tree diff), the current `git ls-tree HEAD references/` gitlinks, and `.memlog.md`. A finding is **Closed** only when the spine's Rule text (or a Consistency Convention row, which the spine treats as binding) is enforceable for the exact divergence the finding described. Where the memlog logs a closure whose sentence did not reach the spine text, the item is scored on the spine text and the memlog gap is named. No document other than this file was modified.

Statuses: **Closed** — the proposed invariant or an equivalent is bound. **Partial** — bound in part; the "Gap remaining" column says exactly what is missing. **Not addressed** — no spine text bears on it. **Deliberately diverged** — the spine chose differently; the column says whether the choice is stated.

## Counts

| Group | Items | Closed | Partial | Not addressed | Deliberately diverged |
| --- | --- | --- | --- | --- | --- |
| Report C-1, C-2, H-1..H-16 | 18 | 15 | 2 (H-4, H-8) | 0 | 1 (H-11) |
| Adversarial A-11..A-20 | 10 | 1 | 7 | 1 (A-20) | 1 (A-16) |
| Security S-8..S-15 | 8 | 8 | 0 | 0 | 0 |
| Rubric R-4..R-17 | 14 | 13 | 1 (R-11) | 0 | 0 |
| Brownfield B-5..B-18 | 14 | 5 | 3 (B-5, B-7, B-9) | 6 | 0 |
| Verified-current V-3..V-13 | 11 | 10 | 1 (V-5) | 0 | 0 |
| **Total** | **75** | **52** | **14** | **7** | **2** |

## Report findings (critical and high)

| Finding | Status | Where in spine | Gap remaining |
| --- | --- | --- | --- |
| C-1 Sensitive content in workflow history / Agent Framework session state (A-1, S-2) | Closed | New AD-27 (Binds every workflow input, activity input/output, custom status, timer/external-event payload, AF session/checkpoint; Rule: identities, versions, revisions, digests, safe codes, `ProtectedContentReference` only; content materialized and discarded inside the activity; AF session persistence disabled; terminal instances purged within terminal handling + 7 days [ASSUMPTION]; `workflow-execution-state` is a purge scope item; content-bearing activity contract fails the AD-17 sweep). AD-14 "Execution state carries content only as references under AD-27"; AD-18 "workflow history is execution state bound by AD-27 and purged at terminal handling"; Data planes and Runtime orchestration rows; sequence diagram "purge instance at terminal handling (AD-27)"; register projection inventory adds `workflow-execution-state` to deletion completion. | None. |
| C-2 `TenantId` absent from identities; catalog scope contradictory (S-1, A-2, R-9) | Closed | AD-2: `ProviderCatalog` under reserved EventStore tenant `system`, one stream per `ProviderId`+`ModelId`; new `TenantProviderEnablement` (`TenantId`); "`TenantId` is a mandatory component of every aggregate identity, deterministic id, idempotency tuple, projection key, query cursor scope, allocator call, and ledger call; a query for a key outside the caller's tenant, or for a platform entry not enabled for it, returns exactly the absent-key response". AD-10: `CapabilityVersion` per (`ProviderId`, `ModelId`) in the `system` catalog, cross-key comparison forbidden, tenant readiness = platform entry joined with enablement, additive `PlatformNotReady`. AD-30 `Platform` principal (`system`-tenant Platform Operator). Identity row. Register Provider Readiness Contract amended to match. | None for the divergence described; the platform-scoped option (rubric's) was taken and is stated. Nit: AD-29 `SampleId = H(sample, QualificationSessionId, ExecutionId, SampleKind)` carries no `TenantId` while AD-2 says every deterministic id does; acceptable only if the spine states that platform-scoped samples are exempt. |
| H-1 Aggregate inventory incomplete (R-1, A-10) | Closed | AD-2 names eleven aggregates with identity key and tenant scope (`Agent`, `ProviderCatalog`, `TenantProviderEnablement`, `AgentInteraction`, `BudgetLedger` (`TenantId`, `BudgetPeriod`; one reservation event per attempt bounds the stream), `TenantGovernancePolicy` (absorbs budget policy), `ContentSafetyPolicy` (`system`, owns `RestrictivenessRank`), `LaunchReadinessGate`, `LegalHold` with closed scope grammar, `AuditExport`, `ProtectedDeletion`); `Agent` stores no safety policy; snapshot safety version is the pair; unrecoverable decisions read the owning aggregate at an expected revision, never a projection. Seed folders, class diagram, Naming row. | None. |
| H-2 No principal model (S-3, R-15) | Closed | New AD-30: exactly one principal per envelope (`User`, `Administrator`, `Platform`, `Workflow`); ingress removes every client-supplied reserved extension; reserved extension from a non-ingress path is a rejection and an audited security event; each extension bound to an allowlisted command set and target scope verified by aggregates; `Workflow` allowlist excludes `Agent`, catalog, enablement, policy, hold, export, deletion commands; FR-33 role to FrontComposer policy map; caller `PartyId` resolved at ingress [ASSUMPTION]. Trusted verdicts row; AD-18 "at most one deterministic trusted command under AD-30". | None. |
| H-3 Three time authorities (A-3, R-7, S-6) | Closed | New AD-28: `DomainInstant` = commit timestamp, derived deadline stored once in the payload; `EvaluationInstant` = injected `TimeProvider` stamped as `EvaluatedAt`, aggregates compare supplied instants only; `SchedulingInstant` = `CurrentUtcDateTime` schedules only, timer re-reads the deadline and re-arms at most three times then `TimerDrift`; monotonic clocks durations only; NFR-9/SM from commit timestamps of named events by `runtime-metrics`/`product-metrics`; UI never re-evaluates. AD-22 retention base, AD-25 expiry rendering, Time row, sequence diagram "re-arm under AD-28", capability-map row. | None. |
| H-4 Two definitions of fresh/stale (A-4) | Partial | AD-17: "`Freshness` is one versioned discriminated value (`Basis` `ValidityWindow` or `RevisionLag`, `EvaluatedAt`, expected and observed revision, `ValidUntil`) evaluated once on the server under AD-28 and carried unchanged to API, BFF, and UI, which never recompute it; a surface combining both bases is stale if either is." AD-10 result carries `Freshness`; Freshness and Provider readiness rows; class diagram. | The memlog decision has two sentences the spine text lacks: (1) which basis each surface uses (readiness records and Provider readiness = `ValidityWindow`; read-your-writes queries = `RevisionLag`), and (2) the mapping `ProviderReadinessReasonCode.Stale` → `LaunchReadinessRecord.State = Stale` on `LR-PROVIDER` → `Blocked` callability, "no other mapping exists". Without (2) `agent-setup` can still compose the two `Stale` enums two ways, which was part of A-4's stated failure. |
| H-5 Deterministic ids named, not derived (A-8, S-12) | Closed | New AD-29: one `AgentsIdentity` canonicalizer; `AgentInteractionId`, `AttemptId` with aggregate-assigned `AttemptOrdinal` (transport retry never increments), `ProviderIdempotencyKey = AttemptId` verbatim, `ReservationId`, `AdmissionId`, `QueueId = AdmissionId`, `ProposalVersionId`, `MessageId`, posting `IdempotencyKey`, `SafetyDecisionId`, `ObservationId`, `SampleId`; ids never embed raw tenant text; API idempotency tuple. Idempotency row. Memlog records the two shipped `AttemptId` derivations as a brownfield gap owned by 6.4/7.3. | None. |
| H-6 Readiness registry keys under-specified (A-5, A-6, A-7, B-8, A-18, R-13) | Closed | AD-17: `TenantScope` closed grammar `tenant:<TenantId>` or `platform`; evaluation reads `platform` records for Platform-scoped gates and `tenant:<id>` for Tenant-scoped; `EnvironmentProfile` `<name>@<ProfileVersion>` with old-profile invalidation; AD-29 `ObservationId` (includes `EvidenceReference`, `ObservedAt`), re-observation with unchanged `SourceVersion` legal, exact duplicate no-op, same id different payload conflict; `AuthorizedProducer` rejection before append; every public command and workflow activity declares exactly one family. AD-12 lock-bearing subset with vocabulary owned by the matrix version. Register: Gate Scope Kinds And Authorized Producers table, matrix v2 rows (`ProposalEdit`, `ProposalRegeneration`, `SystemTimer`, `LegalHoldRelease`, `ExportDownload`, `AuditInspection`, `TenantKillSwitch`, `TenantProviderEnablement`), `GateId` outside inventory rejected at submission, missing family fails contract tests. | None. The `Platform` assignment of the three LR-UI/runtime gates is carried as an assumption in the register, as A-6 allowed. The residual register contradiction on stricter gates is scored under A-18. |
| H-7 "At least as restrictive" has no order (A-9, S-11) | Closed | AD-20: aggregate-assigned non-decreasing `RestrictivenessRank`, publisher-declared `ChangeKind` (`Tightens` increments, `Loosens`/`Neutral` keep); retry evaluates under the current version only when its rank is at least the snapshot rank, otherwise under the snapshot version exactly; decision event carries `SafetyDecisionId`, stage, policy versions, adapter version, outcome, category codes, content fingerprint, never content. AD-2 `ContentSafetyPolicy` owns the rank; class diagram. | None for the divergence. Nit: `ChangeKind` is publisher-declared with no "validated against the category matrix" clause, so a mis-declared `Tightens` is a governance error the spine does not catch. |
| H-8 Budget reservations have no bounded reclamation (S-4, A-11, A-12) | Partial | AD-21: caps in `TenantGovernancePolicy` with no implicit defaults; rate limits, regeneration ceiling, per-caller concurrency before reservation; reservation attributed to the current `BudgetPeriod`; pre-Provider failure releases `NotInvoked`; `Indeterminate` holds 24 h (1–72) then `ChargedAtMaximum`; past-deadline reservation becomes `Unreconciled`, stays counted, settled only by audited `TenantBudgetUpdate` or at period close as `ChargedAtMaximum`; orphans settle the same way; never migrate between periods. AD-2 `BudgetLedger` (`TenantId`, `BudgetPeriod`); Cost row "every reservation has a bounded exit"; AD-23 restore. | Two residuals, detailed under A-11 and A-12: `BudgetPeriod` granularity is not defined (UTC calendar month of the reservation's `DomainInstant`) and mid-period cap changes have no applicability rule; and no owner is named for releasing the reservation and cancelling the queue entry when the interaction terminalizes (expiry, reject, abandon) while an attempt sits at AD-13 steps 1–5. The memlog logs "the owner that terminalizes an attempt before invocation releases reservation and admission in the same trusted step", but that sentence is not in AD-13 or AD-21, so the phantom reservation is bounded only by the deadline → `Unreconciled` → period-close path. |
| H-9 Payload-protection key ownership (S-5) | Closed | AD-22: per-`AgentInteraction` DEK wrapped by per-tenant KEK custodied through `EXT-SECRETS-1`; no deployment-wide key; erasure destroys the DEK; redaction rewrites projection copies only; hold pins the DEK and destruction under hold is rejected; snapshots and replay caches inherit the DEK; restrictive completion = every protected event unreadable + every named projection + `workflow-execution-state` purged. AD-14 references the AD-22 key hierarchy. | None. |
| H-10 Audit envelope silent (S-6, S-15) | Closed | Audit envelope row (AD-30 principal, `OnBehalfOfPartyId`, `CorrelationId` = `AgentInteractionId` for interaction-lifecycle commands else ingress request id, `CausationId`, `TenantId`, W3C trace id); Observability row; AD-28 commit-timestamp authority for domain instants; AD-22 manifest (every item with `TenantId`, stream/projection id, revision range, SHA-256, key versions, requester principal, expiry; HMAC via `EXT-SECRETS-1`; hash recorded in `export`). | None. Bound as convention rows plus AD-22/AD-28 rather than one AD; enforceable. |
| H-11 Agent disable does not stop in-flight work; no emergency stop (S-7) | Deliberately diverged | AD-4: Agent lifecycle, tenant enablement, per-Conversation block, and tenant kill switch are never frozen; AD-12: every side-effecting step (Provider invocation, proposal creation, edit, regeneration, approval, posting) re-reads them and fails closed; per-tenant kill switch (`TenantKillSwitch`, Platform Operator or Release Operator) disables calls tenant-wide, lets awaiting proposals be rejected or abandoned but never approved, lets `Approved`/`PostingPending` finish, cancels queued admissions, deletes nothing; `agent-setup`/`launch-readiness` expose the stop state; AD-5 system abandonment reason "kill switch". | The Agent-disable half is closed. For the emergency stop the spine follows PRD FR-28 (a per-tenant kill switch) instead of S-7's platform-level `AgentsEmergencyStop`. The choice is bound but not stated as a choice: nothing says a platform-wide single-action stop is out of V1 or is defined as catalog-entry disable (halts invocation) plus one `TenantKillSwitch` per tenant (halts posting), so halting every tenant's posting still needs one command per tenant. |
| H-12 Sole V1 entry point has no AD (R-2, B-11) | Closed | New AD-31: Conversations never references Agents packages; **Call hexa** action and per-message provenance decoration are Agents-owned contributions through `EXT-CONV-UI-1` (action contribution with typed registration failure, `MessageId`-keyed decoration slot with Agents provenance accessor, `GetCallabilityAsync`); Agents owns `ConversationAgentCallPanel`; harness removed before 6.7 closes; `Uncommitted` blocks 6.7. Prerequisites row `EXT-CONV-UI-1` → 6.7; register record widened with consumer 6.7; capability-map row; design flowchart. | None. |
| H-13 Spine at spec altitude (R-3) | Closed | AD-10 vocabulary and valid triples "bound by `launch-readiness-register.md` section Provider Readiness Contract"; AD-17 "the normative readiness authority for the record schema, state vocabulary, minimum GateId inventory ..., gate sets, `OperationGateMatrix` versions, projection inventory, and measurement contracts; the spine binds only the invariants below" with no GateId/projection enumerations; AD-13 steps 3–5 collapsed to "per AD-24"; AD-23/24/25/26 each end with a pointer to their register section. Register amended in the same run. | None. AD-17 still carries the descriptive "Tests cover ..." scope list, which the rubric accepted as a test-scope floor. |
| H-14 Live projection ids outside the authoritative list (B-3) | Closed | AD-17: "`agent-setup` and `provider-catalog` are the shipped setup-detail projections; every other projection id in the register inventory is authoritative for the story that builds it"; AD-12 and capability map name `agent-setup`/`provider-catalog`; register inventory renames both ids, adds `tenant-provider-enablement` and `workflow-execution-state`, and rewrites the deletion-completion list. | None. |
| H-15 Story 5.3 executed against an `Uncommitted` dependency (B-4) | Closed | Prerequisites table `EXT-PROVIDER-1` consumers 5.5, 6.4, 7.3; AD-9 "catalog-truth publication is adapter-independent"; register consumer list narrowed with a dated note. | None. |
| H-16 Stale reality inside rules (B-1, B-2, B-5, R-4, V-1, V-2) | Closed | AD-16 has no gap note ("ships no module-owned AppHost, Aspire, or ServiceDefaults project; guard tests enforce the boundary"; "Hosting package versions are the platform's, not the spine's"); AD-10 and AD-4 notes removed; prerequisites paragraph "commitment status ... is read from [the register] at evaluation time; the spine does not track it"; Stack refreshed; `updated: 2026-09-09`. | None. |

## Adversarial divergence (A-11..A-20)

| Finding | Status | Where in spine | Gap remaining |
| --- | --- | --- | --- |
| A-11 Budget period identity and month boundary | Partial | AD-2 `BudgetLedger` (`TenantId`, `BudgetPeriod`); AD-21 "attributes it to the current `BudgetPeriod`", "reservations never migrate between periods", "numeric monthly ... bounds". | `BudgetPeriod` is not defined as the UTC calendar month of the reservation's `DomainInstant`, so a rolling window or tenant-local month is still compliant; no rule says a budget policy version applies to periods starting at or after its effective instant with only tightening applying immediately. |
| A-12 No owner releases reservation / cancels queue when an attempt terminalizes before step 6 | Partial | AD-21 "A later pre-Provider failure releases the reservation immediately as `NotInvoked`"; AD-24 "cancellation, expiry, kill switch, or terminalization invalidates the fence"; sequence diagram `release NotInvoked` branches; deadline → `Unreconciled` fallback. | No text assigns the release and the allocator cancel to the terminalizing path (an `AttemptCancelled`-style rule making the ledger release the authoritative no-usage result for steps 1–5, plus recovery inventory of leaked reservations). The memlog claims it; AD-13/AD-21 do not. The leak is bounded by period close, not prevented. |
| A-13 Public error contract and HTTP mapping unbound | Partial | Errors row: one typed `AgentsProblem` with a safe `ReasonCode`; absent and cross-tenant keys return the identical not-found response; 422 for typed rejections, 409 for conflicts, 202 for accepted-pending with identity and projection version. | Missing: the status vocabulary beyond rejected/conflict/accepted (`Blocked`, `Unavailable` and their codes), a `Retryable` flag, a correlation reference, `Freshness`/`Blockers[]` on the envelope, and the rule that UI gateways render `AgentsProblem` and may not define their own status enums — so the 20+ shipped per-operation enums remain compliant and BFF retry policy is still per story. |
| A-14 Command idempotency semantics backed by no AD | Partial | AD-29: API idempotency keyed by (`TenantId`, principal, operation family, client key); different tuple is a new command; exact replay returns the original identity and projection version. Idempotency row. | Same-tuple replay with a different payload has no rule (silent apply of the first payload vs `Conflict`), no payload-fingerprint storage requirement, and key retention (aggregate lifetime vs window) is unbound. |
| A-15 "Versioned and additive-first" has no applicable rule | Partial | API and contract versioning row: `/api/v1/agents/...`, `Hexalith.Agents.Contracts` package major is the breaking unit, FR-23 additive rules, `Unknown = 0`, "unknown values fail closed". | The row universalizes "unknown fails closed", which is exactly the behaviour A-15 showed breaks export and metrics on an additive enum member; no decide-vs-record consumer split (`UnrecognizedValue` pass-through for audit/export/metrics), and no event/fold evolution rule (`V<n>` types, fold version, rebuild trigger). |
| A-16 Fingerprint field inventory unspecified | Deliberately diverged | AD-13: fingerprint covers `TenantId`, `ProviderId`, `ModelId`, `EffectiveProviderCapabilityVersion`, `InstructionsVersion`, the structured role/provenance message sequence with per-message content digests, generation parameters, reserved output tokens, and the evaluated safety policy versions; "nothing else invalidates a retry". AD-29 canonicalizer; fingerprint row. | The choice is stated as an explicit inventory, but it includes the policy and capability versions A-16 proposed to exclude, and the AD-13/AD-20 contradiction the finding tabled is not resolved: a transport retry that AD-20 lets evaluate under a tighter current policy changes "evaluated safety policy versions" and therefore fails closed under AD-13. Text normalization (NFC, line endings) and field order are also unbound. |
| A-17 Identity table vs AD-13 on `MessageId` ownership | Partial | Identity row: "Conversations owns `ConversationId` and the `MessageId` namespace and uniqueness while Agents supplies the deterministic value"; AD-13 "Conversation posting uses the AD-29 `MessageId` and idempotency key". | Neither AD-6/AD-13 nor the register's `EXT-CONV-AI-1` seam (2) requires Conversations to persist the supplied `MessageId` verbatim or reject the append, that a differing returned id is a typed incompatibility that blocks posting, or that recovery looks up by the idempotency key. The memlog says "must accept a caller-supplied id idempotently"; spine and register do not. |
| A-18 Two operation-family vocabularies; stricter-gate contradiction | Partial | AD-12: lock-bearing subset (`ProposalResolution`, `PolicyPublication`, `TenantBudgetUpdate`, `LegalHold`, `ExportRequest`, `DeletionRequest`, `ProviderCatalogMutation`, `AgentSetupMutation`, `AgentActivation`) and "the family vocabulary is owned by the register matrix version"; AD-17 "gate outside the inventory blocks". | The single family vocabulary is closed. The register's Minimum Gate Inventory still says "Implementations may add stricter gates" while the same document rejects any `GateId` outside the inventory at submission; the sentence A-18 asked to remove is still there. |
| A-19 Agent version semantics and snapshot source of `ProviderCapabilityVersion` | Closed | AD-4: "`ConfigurationVersion` increments on every accepted `Agent` configuration or lifecycle event"; snapshot `ProviderCapabilityVersion` "read from the tenant enablement view of the platform catalog entry" at request time; memlog flags DW-4 as non-conformant. | None for the divergence. Nit: `InstructionsVersion`/`ApproverPolicyVersion` counter semantics and "independent of stream revision" are implied, not stated. |
| A-20 Naming drift for the version identity | Not addressed | Identity row and AD-29 use `ProposalVersionId`; AD-5 uses `ApprovedVersionId`; AD-29 `MessageId = H(message-id, AgentInteractionId, VersionId)`; class diagram `ProposalVersion.VersionId`, `ProposedAgentReply.ApprovedVersionId`. | Drift persists inside the spine (`ProposalVersionId` vs `VersionId` vs `ApprovedVersionId`); `SelectedProposalVersionId` not adopted. |

## Security / data integrity (S-8..S-15)

| Finding | Status | Where in spine | Gap remaining |
| --- | --- | --- | --- |
| S-8 No per-caller / per-Conversation quota before reservation | Closed | AD-21: numeric monthly, per-call, per-Party, per-Conversation bounds in `TenantGovernancePolicy` with no implicit defaults; rate limits, regeneration ceiling (3, range 1–10), and a maximum of concurrent nonterminal interactions per caller yield typed rejections with no reservation, admission, or Provider call. AD-2 `TenantGovernancePolicy`. | None (defaults are policy-configured rather than "versioned in the capacity profile"; equivalent). |
| S-9 Prompt injection not a named safety class | Closed | AD-20: prepared request separates Agent Instructions from context using the Provider's role/message structure, never one concatenated string; `ControlBypass` includes instruction-override attempts found in context; AD-11 context "treated as untrusted data"; AD-13 fingerprint covers the structured sequence. | None. |
| S-10 Secret reference shape and rotation | Closed | AD-9: `SecretReference` is an opaque Agents-issued ULID bound only inside `EXT-SECRETS-1`; rotation keeps the handle; `SecretConfigured` is a durable probe fact (`Resolvable`, `Unresolvable`, `Denied`, `ObservedAt`), never computed in a query path; secrets resolved only inside the generation activity and never enter events, workflow state, exports, or logs; secret state Platform-Operator-only. | None. |
| S-11 Safety decision record shape and comparability | Closed | AD-20 decision event (`SafetyDecisionId`, stage, policy versions, adapter version, outcome, category codes, content fingerprint, never content) plus the `RestrictivenessRank` retry rule; AD-29 `SafetyDecisionId = H(safety, AttemptId, Stage)`. | None (aggregate-assigned rank replaces the "adapter attests AtLeastAsRestrictiveAs" mechanism; equivalent). |
| S-12 API idempotency keys not tenant/principal bound | Closed | AD-29 last sentence. | None. |
| S-13 Backup/restore consistency | Closed | AD-23: EventStore is the only backed-up truth; workflow state, allocator state, read models rebuildable and never restored later than the EventStore restore point; after restore all fences invalidated, `InvocationActive` leases reclaimed through outcome lookup, open reservations follow AD-21, every nonterminal interaction re-enters recovery; `EXT-HOST-1` owns the procedure; `LR-RECOVERY` includes one restore exercise. | None. |
| S-14 Data residency and retention legal basis | Closed | Deferred row "Regional data residency and per-tenant retention overrides"; AD-10 optional safe capability flag `ProcessingRegion`. | None. |
| S-15 Observability correlation contract | Closed | Audit envelope row (W3C trace id as the safe trace reference); Observability row (only `TenantId`, `AgentInteractionId`, `AttemptId`, trace id as dimensions). | None. |

## Rubric (R-4..R-17)

| Finding | Status | Where in spine | Gap remaining |
| --- | --- | --- | --- |
| R-4 Stale status notes inside rules | Closed | Dated notes removed from AD-4, AD-10, AD-16 and the seed; prerequisites paragraph defers status to the register; `updated: 2026-09-09`; tags removed from AD-16/17/18/19. | None here; the surviving `[ADOPTED]` tag on AD-1 is scored under R-11. |
| R-5 Seed contradicts conventions on where the domain lives | Closed | Seed: `Hexalith.Agents/` is the domain assembly with per-aggregate folders; no `Server/Aggregates`; `test/Hexalith.Agents.Tests/` listed; AD-3 "live ... solely in the `Hexalith.Agents` domain assembly"; Package layout row. | None. |
| R-6 Deviation from platform reference layout unstated | Closed | Package layout row (accepted deviation; domain-module boundary rule and aggregate-organised test layout apply unchanged; `Hexalith.Agents.Testing` holds Agents-specific fixtures only); `hexalith-llm-instructions.md` added to `sources`. | None. |
| R-7 NFR-9 and SM metrics have no bound clock | Closed | AD-28 NFR-9/SM clause; capability-map row "Runtime latency and product metrics". | None. |
| R-8 Sequence diagram releases lease before outcome; no membership on approval path | Closed | Diagram: `RecordGeneratedVersion or GenerationFailed` → `settle reservation` → `release AttemptId lease`; "re-validate membership + Append approved version" on the confirmation branch. | None. |
| R-9 `ProviderCatalog` tenant key, mutation authority, read scope | Closed | AD-2 (`system` tenant, one stream per entry, `TenantProviderEnablement`); AD-10; AD-30 `Platform` principal is the `system`-tenant Platform Operator; AD-2 `TenantId`-in-every-identity makes the `system` stream unaddressable by tenant principals. | None (the "mutated only by the Platform Operator" sentence lives in the memlog rather than AD-2, but AD-2 plus AD-30 are enforceable). |
| R-10 AD-8 escape hatch | Closed | AD-8 rewritten: Facilitator on the stable `ConversationOwner` wire identifier; adopting an owner field requires an AD-8 amendment and new `ApproverPolicyVersion`; in-flight interactions keep their snapshot. Deferred row 1 reworded. | None. |
| R-11 Frontmatter `binds`; AD title tags | Partial | `binds` is PRD FR/NFR/OQ ranges; AD-19 retitled "No Tool Or Remote-Agent Surface In V1"; tags removed from AD-16/17/18/19. | AD-1's title still reads "Agents Is A Full EventStore Domain Module [ADOPTED]". |
| R-12 Design Paradigm duplicates AD-18/19 | Closed | One sentence plus the flowchart. | None. |
| R-13 AD-12 "normative families" readable as complete list | Closed | AD-12 "the lock-bearing subset is ...; the family vocabulary is owned by the register matrix version". | None. |
| R-14 Versioning mechanism | Closed | API and contract versioning row (path prefix, Contracts package major). | None (evolution semantics gap is A-15). |
| R-15 Principal → caller `PartyId` seam | Closed | AD-30: "The caller `PartyId` is resolved at ingress from the authenticated subject through the Parties adapter and Tenants membership and fails closed when no unique active Party exists [ASSUMPTION pending platform identity confirmation]". | None; bound as an assumption to be confirmed with the platform. |
| R-16 OQ-4 notification posture | Closed | Notifications row. | None. |
| R-17 Notes for other lenses | Closed | Stack re-verified (V-*); seed drops `Application/Tools`; AD-19 "the source tree reserves no folder for them"; memlog notes `StructuralSeedConformanceTests` must stop requiring `Tools`. | None in the spine; code follow-up remains (the conformance test still requires `Application/Tools`). |

## Brownfield drift (B-5..B-18)

| Finding | Status | Where in spine | Gap remaining |
| --- | --- | --- | --- |
| B-5 AD-10 gap note partially stale | Partial | AD-10 gap note removed; catalog invariants (monotonic `CapabilityVersion`, non-bump on enable/disable, regression blocker, versioned pricing, eligibility floor) stated as rules. | Minor: AD-10 still says pricing "with currency and effective version" rather than the shipped `PricingVersion` name; the runtime-side gap (no `EffectiveProviderCapabilityVersion`/high-water in code until 6.2/6.4/7.3) is recorded nowhere — the spine now carries no gap notes and the memlog has no entry for it. |
| B-6 `LaunchReadinessGate` not built; Epic-4 Agent-aggregate shape has no gap note | Not addressed | AD-17 sole-writer rule and AD-2 "exactly" inventory make the `Agent`-aggregate readiness value non-conformant by rule. | No spine or memlog text names the shipped `RecordAgentLaunchReadiness`/`AgentLaunchReadiness` shape as a superseded predecessor that Story 5.5 retires; it stays silently non-conformant. Consistent with the R-4 no-gap-notes policy only if the memlog carries it, which it does not. |
| B-7 SCP 2026-08-04 AD-17 amendment unapplied; no IntegrationTests project | Partial | AD-17: versioned live-seam matrix with `BindingStatus`; "the story that flips a seam to `Live` adds and passes the matching `Hexalith.Agents.IntegrationTests` coverage asserting persisted state-store or read-model end state in the same change, and a live seam without its named passing test blocks completion"; seed `IntegrationTests/ # created by Story 5.6 at the first live seam binding`. | The SCP rule is applied, but the spine does not record that Stories 5.2/5.3 already bound live seams before the project existed and that 5.6 owes that coverage (the memlog says so); the seed comment "at the first live seam binding" is factually past. |
| B-8 AD-12 six families vs matrix thirteen | Closed | AD-12 lock-bearing subset plus vocabulary ownership. | None. |
| B-9 Stack table drift | Partial | Five sibling rows now "parent gitlink `<sha>` at the spine `updated` date" and match `git ls-tree HEAD` (`1b6f08d4`, `73bcee6f`, `fa423985`, `54fc4040`, `053b2008`); Aspire/CommunityToolkit rows replaced by the platform-owned Hosting row; OpenTelemetry and Fluent UI refreshed. | MediatR, FluentValidation, OpenTelemetry, and Dapr rows are still plain catalog pins without the requested "catalog-pinned, not referenced by the module" annotation (or removal). |
| B-10 Seed no longer matches the tree | Closed | Seed lists `Api/`, `Composition/`, `Application/{Agents,AgentInteractions,Queries,Workflows,Activities}`, `test/Hexalith.Agents.Tests/`; `Tools` and `Server/Aggregates` dropped. | None in the spine (code follow-up: `StructuralSeedConformanceTests` still requires `Application/Tools`). |
| B-11 SCP 2026-08-03 obligations (`EXT-CONV-UI-1`) never carried out | Closed | AD-31; prerequisites row; register `EXT-CONV-UI-1` with consumer 6.7. | None. |
| B-12 Three story-numbering views | Not addressed | Prerequisites table uses `epics.md` numbering. | No provenance line ("Story numbers follow `epics.md`; `sprint-status.yaml` slugs are archived numbering") under the table. |
| B-13 Unratified 5.2/5.3 conventions | Closed | Trusted verdicts row; AD-30 ratifies `actor:*` extensions as `Administrator`; AD-15 "Accepted writes return `Submitted` identities and the UI renders submitted, authoritative pending, then projection confirmed". | None. |
| B-14 AD-4 snapshot wording | Not addressed | AD-4 still lists caller `PartyId` and source `ConversationId` inside the snapshot; class diagram places them on `AgentInteraction`. | Review disposition was "ignore"; the wording nit stands and is harmless. |
| B-15 `ReadModelWritePolicy` / `IQueryCursorCodec` unused | Not addressed | AD-2 mentions "query cursor scope" (implies `QueryCursorScope`); nothing on `ReadModelWritePolicy`. | Whether the fold-with-checkpoint pattern replaces `ReadModelWritePolicy` is undecided; disposition was "discuss with the EventStore owner". |
| B-16 Flat layout ratified only in memlog | Closed | Package layout row. | None. |
| B-17 Deferred-work ledger entries touching AD semantics | Not addressed | — | Deferred by disposition; DW-2 (deferred `ITenantAccessReader` vs role checks) is not cited in AD-12; DW-4 appears only in the memlog (A-19). |
| B-18 Memlog retains superseded decisions without markers | Not addressed | `.memlog.md` L44/L46/L60/L61/L80 unchanged; 2026-09-09 entries appended without supersession markers on the older lines. | Superseded decisions (module-local AppHost, hybrid MAF/MCP/A2A runtime, AD-17 hybrid test gate, AppHost gap constraint) still stand un-marked. |

## Verified-current (V-3..V-13)

| Finding | Status | Where in spine | Gap remaining |
| --- | --- | --- | --- |
| V-3 Aspire row cites removed AppHost declarations | Closed | Stack "Hosting (Aspire, Dapr hosting integrations)" → "platform-owned through `EXT-HOST-1` (`Hexalith.Platform@a66cdf34`); not pinned by Agents"; AD-16 "Hosting package versions are the platform's, not the spine's". | None. |
| V-4 xUnit root override diverges from catalog 4.0.0 | Closed | Stack xUnit row: `3.2.2` root override; catalog `4.0.0`; accepted deviation for bunit/FrontComposer.Testing alignment until Story 5.6 aligns [ASSUMPTION]. | None. |
| V-5 .NET SDK pin behind the workspace | Partial | Stack .NET SDK row: `10.0.301` with `latestPatch`; "siblings are on `10.0.400`". | The drift is visible, but the row records no reason or decision for staying on the 3xx band; the memlog's "accepted deviation ... Story 5.6 aligns ... SDK 10.0.400" did not reach the spine text, unlike the xUnit and NSubstitute rows. |
| V-6 Sibling commit rows stale | Closed | Stack rows "parent gitlink `<sha>` at the spine `updated` date"; verified equal to `git ls-tree HEAD references/`. | None. |
| V-7 CommunityToolkit Aspire Dapr version stale | Closed | Row folded into the platform-owned Hosting row. | None. |
| V-8 OpenTelemetry 1.18.0 | Closed | Stack `1.18.0`. | None. |
| V-9 Fluent UI rc.5 | Closed | Stack `5.0.0-rc.5-26219.1`; AD-25 keeps "Fluent UI Blazor V5". | None. |
| V-10 NSubstitute override vs catalog 6.2.0 | Closed | Stack: `5.3.0` root override; catalog `6.2.0`; same deviation as xUnit. | None. |
| V-11 Dapr 1.18.x instance-id reuse | Closed | AD-18 "because Dapr 1.18 never re-creates a non-terminal instance id, recovery resumes or terminalizes the existing instance and never re-creates it". | None. |
| V-12 Agent Framework 1.20.0; Durable Task hosting as excluded owner | Closed | AD-18 names "its Durable Task and Azure Functions hosting, Python Dapr Agents `DurableAgent`, and any other workflow owner" as excluded; memlog `(version)` line records MAF 1.20.0. | None. |
| V-13 Seed names `IntegrationTests`, which does not exist | Closed | Seed lists `test/Hexalith.Agents.Tests/`; `IntegrationTests/` annotated "created by Story 5.6 at the first live seam binding". | None (see B-7 for the annotation's accuracy). |

## Cross-cutting observation

Six of the fourteen Partial items (H-4, H-8/A-12, A-17, A-18, R-11, B-7) and one Not-addressed item (B-6) are cases where `.memlog.md` logs a decision sentence that the distilled spine does not contain. They are one-sentence distill omissions rather than open design questions, and the memlog can be treated as the intended text for each.
