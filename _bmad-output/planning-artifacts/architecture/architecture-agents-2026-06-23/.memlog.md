---
scope: agents workspace
purpose: build-substrate architecture spine only
altitude: initiative
updated: 2026-06-23T16:17
---

- (constraint) Scope is the agents workspace: the Hexalith module ecosystem checked out under /home/administrator/projects/hexalith/agents, including existing module conventions from project-context files.
- (constraint) Deliverable scope is the architecture spine only; no companion deck, report, C4 set, or human-facing solution design unless requested later.
- (decision) Set altitude to initiative: the spine binds cross-module workspace invariants so downstream module and feature work does not diverge; it will not specify per-story or per-module implementation detail.
- (constraint) Scope is the agents workspace: the Hexalith module ecosystem checked out under /home/administrator/projects/hexalith/agents, including existing module conventions from project-context files.
- (constraint) Brownfield reality: the root agents repository is a workspace/super-repo of checked-out Hexalith modules, not a root .NET solution; each module owns its own .slnx, build props, package versions, tests, docs, and release posture.
- (constraint) Brownfield reality: cross-module source references use root/sibling MSBuild path discovery properties such as HexalithEventStoreRoot, HexalithTenantsRoot, HexalithPartiesRoot, HexalithMemoriesRoot, HexalithCommonsRoot, and HexalithTenantsBasePath.
- (constraint) Brownfield reality: Hexalith.EventStore is the shared command/query/event substrate and gateway pattern for domain modules; Tenants owns tenant lifecycle, membership, roles, and configuration; Parties owns party identity and PII; Memories owns knowledge/search backends; Conversations consumes these as bounded-context dependencies.
- (constraint) Brownfield reality: modules target net10.0 and use Central Package Management, warnings as errors, file-scoped namespaces, xUnit v3, Shouldly, Dapr, Aspire, OpenTelemetry, and module-local AppHost/deployment/test conventions.
- (constraint) Brownfield reality: root-level submodules are the workspace dependency mechanism; recursive submodule initialization and casual edits to submodule contents are forbidden by multiple module contexts.
- (question) What does the agents workspace own above the checked-out Hexalith modules: a deployable Agents product/topology, a coordination workspace for module development, or a new agent orchestration layer that consumes the existing domain services?
- (question) Should new agent-facing capabilities persist as EventStore-backed domain modules with durable commands/events, or stay in a thinner orchestration/application layer that calls existing modules and records only coordination state?
- (question) Should the root agents workspace eventually own a root AppHost/deployment topology, or should each module AppHost remain authoritative and the root spine only define dependency and boundary rules?
