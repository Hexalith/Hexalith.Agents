---
topic: Hexalith Agents governed AI participant V1
updated: 2026-09-09T07:55
---

- (direction by user) Refresh the Hexalith Agents spec against the final 2026-09-09 PRD, UX DESIGN and EXPERIENCE spines, architecture spine AD-1..AD-31, and its update report; cite AD-27..AD-31 and amended AD-2, AD-17, AD-20, and AD-22 wherever dependent.
- (direction by bmad-spec) Headless express derivation uses slug agents inherited from prd-agents-2026-06-23; no prior spec memlog exists and no elicitation is needed.
- (decision by bmad-spec) Adopt the final PRD, UX DESIGN and EXPERIENCE spines, architecture spine, architecture implementation conventions, external dependency register, and launch-readiness register as read-only companions because downstream needs their load-bearing detail; the architecture update report is process metadata and is absorbed only as the change signal.
- (capability by bmad-spec) CAP-1 intent: tenant administrators can configure, activate, disable, and inspect the one tenant-scoped hexa with stable Party identity, instructions, Provider/model selection, tenant-wide response mode, Approver Policy, context policy, expiry, and regeneration bounds (FR-1..FR-7; AD-2, AD-4, AD-7, AD-30); success: activation and calls remain blocked until current identity, configuration, role, Provider, and readiness gates pass, accepted changes are versioned and audited, and later changes affect only future interactions.
- (capability by bmad-spec) CAP-2 intent: the Platform Operator can govern one platform-scoped Provider/model catalog and explicit per-tenant enablement while tenant administrators select only enabled entries (FR-4, FR-5, FR-33; amended AD-2, AD-9, AD-10, amended AD-17, AD-30); success: the full system catalog and TenantProviderEnablement inventory enforce monotonic capability/pricing versions, safe secret visibility, tenant-filtered readiness, and fail-closed selection and invocation.
- (capability by bmad-spec) CAP-3 intent: an authorized Conversation Participant can explicitly call hexa only from the Conversation-owned Call hexa action using the complete authorized Source Conversation when it fits the safe context budget (FR-8..FR-10, FR-31; AD-6, AD-11, AD-12, amended AD-20, AD-29, AD-30, AD-31); success: an unavailable, unauthorized, stale, oversized, unsafe, or blocked request creates no Provider work, proposal, or Conversation Message, while an accepted request has one tenant-bound interaction identity and visible authoritative status.
- (capability by bmad-spec) CAP-4 intent: an accepted call can generate a governed response and Automatic Response Mode can post it once as the hexa Party (FR-10..FR-12; AD-5..AD-7, AD-13, AD-18, amended AD-20, AD-21, AD-24, AD-27, AD-28, AD-29, AD-30, AD-31); success: retries, replay, restart, timeout, and unknown Provider outcomes never duplicate attempts, versions, reservations, or messages, content never persists in execution state, time and identities are deterministic, and only confirmed posting reaches Posted.
- (capability by bmad-spec) CAP-5 intent: authorized Eligible Approvers can discover, edit, regenerate, approve, reject, abandon, and resolve immutable Proposed Agent Reply versions outside the Conversation (FR-13..FR-18; AD-4, AD-5, AD-8, AD-13, amended AD-20, amended AD-22, AD-27, AD-28, AD-29, AD-30); success: the canonical proposal state machine enforces expiry, segregation of duties, regeneration and retry bounds, exact-version safety and posting, immutable history, and safe terminal recovery without stranding proposals.
- (capability by bmad-spec) CAP-6 intent: every administration, call, proposal, posting, governance, status, and audit operation enforces tenant, Party, Conversation, policy, and FR-33 role authority before side effects (FR-19..FR-21, FR-33; amended AD-2, AD-8, AD-12, amended AD-22, AD-29, AD-30); success: each command carries exactly one verified principal, forged reserved extensions and stale authority fail closed and are audited content-free, and cross-tenant or undisclosed keys are indistinguishable from absence.
- (capability by bmad-spec) CAP-7 intent: the Platform Operator with Security approval can publish versioned Content Safety Policy and a tenant can only tighten it, with Conversation content always treated as untrusted data (FR-26, FR-27, FR-31; AD-12, AD-14, amended AD-20, amended AD-22, AD-27, AD-28, AD-29, AD-30); success: fresh durable decisions cover prompt plus context before Provider use, generated output before proposal or post, the exact approved version, and pre-post, with lineage proving no retry or re-check used a weaker policy and no Approver override.
- (capability by bmad-spec) CAP-8 intent: authorized operators can configure hard tenant cost caps, rate limits, regeneration bounds, and production capacity controls that govern every call and regeneration (FR-28, FR-32; amended AD-2, AD-13, AD-21, AD-24, AD-28, AD-29, AD-30); success: atomic admission and reservation prevent overspend and starvation, every reservation reaches a bounded settlement, queues are bounded and fair across replicas, and rejection occurs before Provider cost.
- (capability by bmad-spec) CAP-9 intent: administrators, approvers, operators, inspectors, participants, and API clients can use the same governed capabilities and authoritative states through FrontComposer UI and versioned public contracts (FR-22, FR-23, FR-29; AD-15, amended AD-17, AD-25, AD-26, AD-28, AD-30, AD-31); success: all binding UX routes and the Conversation contribution expose matching authorization and pending/projection states, WCAG 2.2 AA behavior, English/French parity, responsive safety, browser-monotonic timing, and no alternate production invocation route.
- (capability by bmad-spec) CAP-10 intent: authorized users can trace calls and responses and authorized governance roles can inspect evidence, apply or release holds, request export, and request approved deletion (FR-24, FR-25, FR-30; amended AD-2, AD-14, amended AD-17, amended AD-22, AD-27, AD-28, AD-29, AD-30); success: field-level protected content obeys the 365-day terminal retention clock, sealed export and two-phase hold rules, restrictive erasure including named projections and workflow state, while immutable streams remain replayable as erased tombstones.
- (capability by bmad-spec) CAP-11 intent: release operators can inspect operational status and evaluate launch readiness, recovery, capacity, UI conformance, performance, and product metrics from one versioned evidence authority (FR-25, FR-28; NFR-9..NFR-14; amended AD-17, AD-23..AD-26, AD-28, AD-29, AD-30); success: RQ-1 records READY only at one registry checkpoint when every required gate and consumed dependency qualifies at Levels 4 and 5 and every Product, Architecture, or Governance assumption is retired; otherwise it names NOT READY blockers.
- (constraint by bmad-spec) Hexalith Agents is a full EventStore domain module with the exact fourteen-aggregate inventory and platform-scoped ProviderCatalog plus TenantProviderEnablement defined by AD-1 and amended AD-2; projections and workflow state never become business truth.
- (constraint by bmad-spec) Aggregates stay pure, each durable orchestration step dispatches at most one trusted command using the twin-policy convention, and Dapr Workflow is the sole V1 durable execution owner (AD-3, AD-18 and IMPLEMENTATION-CONVENTIONS.md).
- (constraint by bmad-spec) Execution state is reference-only and content-bearing activity boundaries are forbidden; terminal workflow state is purged and deletion completion includes workflow-execution-state (AD-27, amended AD-22).
- (constraint by bmad-spec) All durable instants, comparisons, scheduling, deadlines, and duration evidence use the distinct authorities and server-evaluated rules of AD-28; browser clocks never decide domain state.
- (constraint by bmad-spec) All deterministic identities, idempotency tuples, sensitive digests, and replay behavior follow the one canonical derivation in AD-29; tenant scope is included and conflicting payload reuse is rejected.
- (constraint by bmad-spec) Every command carries exactly one AD-30 principal in a stripped, allowlisted, HMAC-tagged trusted envelope; workflow and platform principals cannot borrow tenant administrator authority.
- (constraint by bmad-spec) Safety-policy comparison uses the amended AD-20 category-set and LastLoosensVersion lineage rule; a rank or version number alone never proves a policy is at least as restrictive.
- (constraint by bmad-spec) Sensitive content uses the amended AD-22 field-level ProtectedContent envelope, per-interaction data keys, two-phase hold pinning, sealed transport and export, and restrictive deletion confirmation; raw content and secrets never enter logs, telemetry, status, or execution state.
- (constraint by bmad-spec) The amended AD-17 registry grammar, AuthorizedProducer rules, operation-gate matrix, Freshness value, projection inventory, live-seam tests, and Level 4/5 evidence rules are normative; current RQ-1 remains NOT READY while required evidence or consumed dependencies are insufficient or uncommitted.
- (constraint by bmad-spec) Conversations never references Agents packages; the Call hexa trigger, self-contained panel, callability query, and MessageId-keyed provenance decoration use only EXT-CONV-UI-1, and the pre-integration harness is removed before the consuming story closes (AD-31).
- (constraint by bmad-spec) V1 uses only the complete authorized Source Conversation when it fits and otherwise blocks; it has no approved bounded-context behavior and never truncates, summarizes, or windows silently (AD-11).
- (decision by bmad-spec) Non-goals are long-term memory; tools, MCP, A2A, remote agents or alternate workflow owners; project, folder, file or external retrieval; ambient or non-Conversation triggers; external-channel bots; business mutations beyond Conversation replies; multiple product-visible Agents; and customer billing.
- (constraint by bmad-spec) All PRD A-1..A-17 and architecture ARCH-A-1..ARCH-A-7 assumptions remain explicitly provisional; every unretired Product, Architecture, or Governance assumption blocks RQ-1 and this spec introduces no replacement assumption.
- (question by bmad-spec) OQ-15 remains deferred: whether Conversations adds a first-class owner contract and a true owner resolver after V1.
- (question by bmad-spec) OQ-18 remains deferred: per-category handling of unsafe historical Conversation content; V1 blocks the Conversation with no redaction or exclusion path until Product and Security decide.
- (question by bmad-spec) OQ-23 remains deferred: an Automatic Response Mode retraction metric awaits a committed Conversations retraction seam before the first Automatic-mode tenant is enabled.
- (decision by bmad-spec) Success signal: a production-like tenant demonstrates configuration, explicit Conversation invocation, automatic and confirmation posting, governance and audit flows with no unauthorized or duplicate effect, and RQ-1 records READY from one current checkpoint with complete qualifying evidence.
- (decision by bmad-spec) CAP-3 citation refinement: the accepted interaction also depends on amended AD-22 protection and AD-27 reference-only execution state; all prior CAP-3 intent and success text remains unchanged.
- (decision by bmad-spec) CAP-4 citation refinement: runtime gate evaluation and generated-content protection add amended AD-17 and amended AD-22; all prior CAP-4 intent and success text remains unchanged.
- (decision by bmad-spec) CAP-8 citation refinement: cost and capacity operation gates add amended AD-17; all prior CAP-8 intent and success text remains unchanged.
- (question by bmad-spec) OQ-18 carries the provisional 2026-10-15 decision date from PRD assumption A-14.
- (constraint by bmad-spec) Content-bearing workflows remain disabled until EXT-PROTECTION-1 is Available; this makes the amended AD-22 field-level protection boundary operational rather than documentary.
- (constraint by bmad-spec) A consuming story cannot become ready-for-dev until each consumed dependency is Committed or Available with a complete accepted record; runtime and qualification execute a consumed seam only when it is Available and its verification command passes (FR-21, amended AD-17).
- (note by bmad-spec) Wrapper-only content excluded from the kernel: upstream workflow chronology, source frontmatter, reviewer finding counts, implementation follow-up numbering, and update-report next-step ceremony remain in their owning artifacts; every load-bearing product, UX, architecture, dependency, readiness, and command-convention claim remains reachable through the adopted companions.
- (event by bmad-spec) Self-validation pass 1 coherence PASS: CAP-1..CAP-11 are stable and unique, every capability has intent and testable success, constraints bend implementation, five non-goals bound V1 scope, the success signal is demonstrable, three inherited deferred questions remain explicit, and prose is lean.
- (event by bmad-spec) Self-validation pass 2 preservation PASS: all seven adopted companion paths resolve; FR-1..FR-33 are covered by the capability set; PRD NFR-1..NFR-14, UX behavior and visuals, architecture AD-1..AD-31, dependency commitments, readiness grammar, projections, evidence rules, and command conventions remain authoritative in companions; AD-27..AD-31 and amended AD-2, AD-17, AD-20, and AD-22 are cited in dependent capabilities and explicit constraints.
- (event by bmad-spec) Hexalith Agents spec finalized from the 2026-09-09 authorities; current RQ-1 remains NOT READY and no consuming seam may bypass the dependency and evidence gates.
