---
title: '5.3 Migrate Provider Governance to Platform Catalog and Tenant Enablement'
type: 'feature'
created: '2026-09-23'
status: 'done'
route: 'dispatch'
review_loop_iteration: 5
baseline_commit: '1d69f3f349b76716fca20ef126ae35ed4a6c4d40'
context:
  - '_bmad-output/implementation-artifacts/epic-5-context.md'
  - '_bmad-output/planning-artifacts/epics.md'
  - 'references/Hexalith.AI.Tools/hexalith-state-instructions.md'
  - 'references/Hexalith.AI.Tools/hexalith-ux-instructions.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** The shipped tenant-scoped catalog violates AD-2 and leaves `NC-5.3-PLATFORM-CATALOG-SCOPE` open. Tenant enablement and data-handling decisions are absent.

**Approach:** Migrate to one reserved-`system` ProviderCatalog stream per provider/model and tenant-scoped TenantProviderEnablement. Publish joined reads and versioned decisions through EventStore, API/client, and UI.

## Boundaries & Constraints

**Always:** Persist pure aggregate decisions through EventStore; freeze legacy writes without rewriting history. Platform Operators alone mutate catalog/enablement; tenant administrators make lock-bearing decisions on their own current terms. Check revisions, positive limits, explicit nonnegative prices, ISO currency, and non-reusable capability/pricing/data-handling versions. DataHandlingVersion changes only with retention, training use, processing regions, or retained terms reference; pricing version only with price/currency. Missing terms, zero price, missing acceptance, stale or unknown evidence block. Trusted server time starts one exclusive 30-day grace at the first unaccepted cumulatively tightening version; tightening means non-lengthening retention, movement toward opt-out, narrowed regions, and unchanged terms reference. Decline, loosening, incomparable terms, or expiry block. Authorize before disclosure; tenant reads show only enabled entries, without secret reference/configured state; platform reads may show these safe fields. Keep submitted/pending/confirmed truth and English/French parity.

**Never:** Do not choose among divergent legacy entries, delete history, infer callability, expose secrets, invoke a Provider, add its SDK adapter, or claim launch readiness. Story 5.4 owns signed-envelope ingress; 5.5–5.7 own readiness, host, and activation.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
| --- | --- | --- | --- |
| Migration | Legacy tenant entries | Deterministic `MigratedFrom` targets; repeat no-op; legacy frozen | Divergent same-key metadata conflicts |
| Governance | Platform mutation or tenant enablement | Expected-revision events/projections; terms required to enable | Wrong principal, stale or invalid input rejects |
| Terms | Named version, justification | Durable accept/decline, actor/role, exact confirmation, current/in-force versions | Superseded, blank, divergent duplicate, cross-tenant rejects |
| Tightening | Successive versions | Cumulative comparison; one exclusive deadline | Decline, loosening, missing diff, expiry blocks |

</frozen-after-approval>

## Code Map

- `src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs` — reuse pure validation; replace tenant-wide state with `system` entry ownership.
- `src/Hexalith.Agents.Server/Application/Agents/ProviderCatalogAdministrationOrchestrator.cs` — replace tenant target/`IsProviderAdmin` with Platform scope and entry ID; reuse dispatcher.
- `src/Hexalith.Agents.Server/Projections/ProviderCatalogReadModelAddresses.cs`, `src/Hexalith.Agents.Server/Application/Queries/ProviderCatalogQueryHandlerBase.cs`, `src/Hexalith.Agents.Server/Ports/ProjectedProviderCatalogReader.cs` — replace tenant catalog slots with authorized platform/tenant join.
- `src/Hexalith.Agents.Server/Application/Agents/ProviderSelectionVerdict.cs`, `src/Hexalith.Agents.UI/Components/Pages/ProviderCatalog.razor`, and `TenantProviderCatalog.razor` — honor tenant terms eligibility and confirm the exact submitted command, including no-op and rejection outcomes.

## Tasks & Acceptance

**Execution:**
- [x] `src/Hexalith.Agents.Contracts/ProviderCatalog/ProviderDataHandlingRecord.cs`, `TenantProviderCatalogEntryView.cs`, and `Commands/CreateProviderModelEntry.cs` — add terms, tenant-safe view, versions, revisions, provenance, and rejections.
- [x] `src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs` and `src/Hexalith.Agents/TenantProviderEnablement/TenantProviderEnablementAggregate.cs` — implement platform entry and tenant enablement/decision folds, validation, replay, and nonextending grace.
- [x] `src/Hexalith.Agents.Server/Application/Agents/ProviderCatalogMigrationService.cs` and `ProviderCatalogAdministrationOrchestrator.cs` — migrate deterministic targets, freeze legacy writes, enforce authority before dispatch.
- [x] `src/Hexalith.Agents.Server/Projections/TenantProviderEnablementProjectionHandler.cs`, `src/Hexalith.Agents.Server/Application/Queries/ProviderCatalogQueryHandlerBase.cs`, and `src/Hexalith.Agents.Server/Ports/ProjectedProviderCatalogReader.cs` — persist/join read models; hide absent and nonenabled entries alike.
- [x] `src/Hexalith.Agents.Server/Application/Agents/EventStoreProviderCatalogOperations.cs`, `src/Hexalith.Agents.Client/IProviderCatalogOperations.cs`, `src/Hexalith.Agents.UI/Components/Pages/ProviderCatalog.razor`, and `src/Hexalith.Agents.UI/Resources/AgentsResources.fr.resx` — expose scoped writes/reads, acceptance, grace, and truth stages with English/French resources.
- [x] `test/Hexalith.Agents.Tests/ProviderCatalogAggregateTests.cs`, `test/Hexalith.Agents.Server.Tests/ProviderCatalogEventStoreIntegrationTests.cs`, `test/Hexalith.Agents.UI.Tests/ProviderCatalogUiTests.cs`, and `eng/verify-story-5.3.ps1` — prove migration/replay, persisted end state, isolation, grace, UI, and secret sweep; close `NC-5.3-PLATFORM-CATALOG-SCOPE` in `_bmad-output/planning-artifacts/external-dependency-register.md` only after evidence is accepted.
- [x] `src/Hexalith.Agents.Server/Ports/ProjectedProviderCatalogReader.cs`, `src/Hexalith.Agents.Server/Application/Agents/ProviderSelectionVerdict.cs`, and `src/Hexalith.Agents.Server/Projections/TenantProviderCatalogViewFactory.cs` — fail selection/activation closed when tenant terms are unaccepted or expired or the authoritative platform stream is ahead of its projection; preserve pending truth for an absent projection. Add live-reader and interaction/activation tests in `test/Hexalith.Agents.Server.Tests`.
- [x] `src/Hexalith.Agents.Server/Application/Agents/EventStoreProviderCatalogOperations.cs`, `src/Hexalith.Agents.Server/Projections/ProviderCatalogProjectionFold.cs`, `src/Hexalith.Agents.UI/Components/Pages/ProviderCatalog.razor`, and `TenantProviderCatalog.razor` — correlate platform and tenant writes to their exact EventStore command outcomes; show a terminal safe rejection, confirm the submitted message only, and confirm an authoritative no-op without waiting for a new event. Cover concurrent writers, no-op retries, and rejection in server and UI tests.
- [x] `src/Hexalith.Agents.UI/Components/Pages/TenantProviderCatalog.razor`, `ProviderCatalog.razor`, and `src/Hexalith.Agents.UI/Resources/AgentsResources*.resx` — render the effective time and any tightening declaration with its field-level diff for tenant review, use a localized boolean control for training use, and clear prior decision truth on selecting another entry. Cover English/French rendering and the Fluent switch accessible role/change behavior in UI tests; native browser key handling awaits the platform host.
- [x] `src/Hexalith.Agents.Server/Projections/ProviderCatalogProjectionHandler.cs` and `src/Hexalith.Agents.Server/Application/Agents/ProviderCatalogMigrationService.cs` — allow already-committed legacy deliveries to finish projecting and refuse migration while any inventoried legacy stream is behind its authoritative checkpoint. Test a delayed legacy delivery before migration and an exact repeat after catch-up.
- [x] `src/Hexalith.Agents.Server/Ports/ProjectedProviderCatalogReader.cs` — compare the tenant enablement stream head as well as the platform entry stream head before returning a selectable entry. A committed tenant disable or decline awaiting projection must block selection and activation; test both races.
- [x] `src/Hexalith.Agents.Server/Application/Agents/ProviderCatalogMigrationService.cs` and `src/Hexalith.Agents.Server/Projections/ProviderCatalogProjectionHandler.cs` — reject an existing enabled platform target when all inventoried legacy sources for that key are disabled; preserve the prior disabled-target to enabled-source upgrade for a later tenant. Use the legacy tenant stream checkpoint when an older read model has no per-stream checkpoint. Test both status directions and delivery at sequence two after a pre-upgrade projection.
- [x] `src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs`, `src/Hexalith.Agents.Server/Projections/ProviderCatalogProjectionFold.cs`, `ProviderCatalogViewFactory.cs`, and `src/Hexalith.Agents.UI/Components/Pages/ProviderCatalog.razor` — require expected capability version on public platform updates, reject corrupt projection payloads without acknowledging them, and keep a missing platform projection pending in both its read result and operator UI state. Test stale/no-version update, corrupt delivery, and missing model truth/state.
- [x] `src/Hexalith.Agents.Server/Application/Agents/EventStoreProviderCatalogOperations.cs` — distinguish an unavailable platform read from a provider/model that is genuinely absent before tenant enablement, with focused tests.
- [x] `src/Hexalith.Agents.UI/Components/Pages/TenantProviderCatalog.razor` and `ProviderCatalog.razor` — preserve per-entry pending locks for DataHandlingAcceptance and ProviderCatalogMutation after polling times out until each exact command has terminal truth, while unrelated entries remain usable; expose editable text-generation and safe capability flags; let an operator explicitly clear a configuration reference without rendering its stored value. Test pending resubmission for both families, capability edits, and clearing.
- [x] `references/Hexalith.EventStore/tests/Hexalith.EventStore.Server.Tests/Commands/CommandStatusControllerTests.cs`, `references/Hexalith.EventStore/tests/Hexalith.EventStore.Client.Tests/Gateway/EventStoreGatewayClientTests.cs`, `eng/verify-story-5.3.ps1`, and `test/Hexalith.Agents.UI.Tests/ProviderCatalogGovernanceUiTests.cs` — assert the status API returns and the gateway reads the owning tenant, include both focused status tests in the story gate, and assert both exact formatted tenant-review times in English and French.
- [x] `src/Hexalith.Agents.Server/Projections/TenantProviderCatalogViewFactory.cs` and `src/Hexalith.Agents.Server/Application/Agents/EventStoreProviderCatalogOperations.cs` — preserve pending detail truth when a projection is absent and compare tenant and known platform stream heads before public tenant list/detail calls claim `Current` or `ProjectionConfirmed`; if completeness cannot be proven, return pending truth. Test absent projection and delayed terms/disable projections through the public query path.
- [x] `src/Hexalith.Agents.Server/Ports/ProjectedProviderCatalogReader.cs` and `src/Hexalith.Agents.UI/Components/Pages/TenantProviderCatalog.razor` — reevaluate the exclusive grace deadline after awaited stream reads before returning a selection result, and refresh a tenant page when its displayed grace deadline arrives. Test the in-flight deadline crossing and an open page crossing the deadline.
- [x] `src/Hexalith.Agents.UI/Components/Pages/ProviderCatalog.razor` — permit disabling tenant enablement without platform terms, and retain exact pending outcome evidence for a tenant/model enablement write after polling times out. Test disable-without-terms and prevent same-target resubmission while unrelated targets remain usable.
- [x] `src/Hexalith.Agents.Server/Application/Agents/ProviderCatalogMigrationService.cs` — compare existing platform and tenant target projections to their authoritative stream heads before preflight/repeat results; correlate dispatched migration commands to their exact terminal status so a rejection is not reported as pending or confirmed by another command. Test stale-target no-op/conflict and rejected dispatch.
- [x] `src/Hexalith.Agents.Server/Application/Agents/EventStoreProviderCatalogOperations.cs` and `references/Hexalith.EventStore/tests/Hexalith.EventStore.Client.Tests/Gateway/EventStoreGatewayClientTests.cs` — report a missing command status as unverifiable unless submission evidence exists, and assert a completed zero-event HTTP status deserializes `EventCount = 0` for no-op confirmation.
- [x] `src/Hexalith.Agents.Server/Application/Agents/EventStoreProviderCatalogOperations.cs` and `src/Hexalith.Agents.Server/Application/Queries/ProviderCatalogQueryHandlerBase.cs` — apply authoritative platform and tenant stream-head checks to every operator list/detail, operator enablement inspection, and tenant EventStore `/query` result before labeling it current or confirmed; keep absent or unverifiable data pending. Test both read surfaces with delayed platform and tenant projection deliveries.
- [x] `src/Hexalith.Agents.Server/Application/Agents/EventStoreProviderCatalogOperations.cs` — when an operator enables a tenant immediately after a platform create, distinguish a committed platform stream with lagging projection from an absent entry, returning unavailable/pending rather than `NotFound`. Test that race.
- [x] `src/Hexalith.Agents.UI/Components/Pages/ProviderCatalog.razor`, `TenantProviderCatalog.razor`, and a session-scoped pending-command store — retain per-resource pending command identities and locks across component navigation and browser refresh for `ProviderCatalogMutation` and `DataHandlingAcceptance`; restore exact pending truth, and release only after the same command's terminal result. Test navigation and the persisted store boundary without exposing secret data.
- [x] `src/Hexalith.Agents.Server/Application/Agents/ProviderCatalogMigrationService.cs` — before reporting `ProjectionConfirmed`, recheck all migrated platform and tenant targets against their authoritative stream heads and expected metadata/status so a concurrent mutation earlier in a multi-entry run is detected. Test a mutation after an earlier target is confirmed but before the final result.
- [x] `src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs` and `references/Hexalith.EventStore/tests/Hexalith.EventStore.Server.Tests/Actors/CoordinatedCommandActorTests.cs` — reject undefined `SafeCapabilityFlags` bits on create/update and test a fenced stale-source command after target reconciliation misses, proving it cannot mutate tenant state. Include the focused EventStore case in the story gate.
- [x] `src/Hexalith.Agents.Server/Application/Agents/ProviderCatalogMigrationService.cs` — retain initial legacy source and confirmed target checkpoints, recheck source stream heads and target checkpoints at the end, and refuse confirmation after an intervening source event or a target disable/re-enable even when final metadata/status matches. Test both multi-entry races without losing prior no-op and conflict behavior.
- [x] `src/Hexalith.Agents.Server/Application/Queries/ProviderCatalogQueryHandlerBase.cs` and tenant query handlers — evaluate grace eligibility after authoritative stream reads, using an injectable clock; a deadline crossed during a delayed query must not return selectable Grace truth.
- [x] `src/Hexalith.Agents.Server/Ports/ProjectedProviderCatalogReader.cs` — when a tenant-enabled key has no platform projection, check its named authoritative platform stream and return unavailable/pending rather than absence if committed; preserve hidden/absent indistinguishability before any platform-specific read. Test delayed create, hidden, and absent keys.
- [x] `src/Hexalith.Agents.Contracts/ProviderCatalog/Commands/EnableProviderModelEntry.cs`, `DisableProviderModelEntry.cs`, and `src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs` — require a caller-observed lifecycle revision or stream sequence on enable/disable, reject stale intent, and carry the value through API/client/UI and migration. Test two operators with stale intent while retaining exact retry behavior.
- [x] `src/Hexalith.Agents.UI/Components/Pages/ProviderCatalog.razor` — key tenant enablement pending state by tenant and provider/model so tenant A cannot block tenant B, and offer a disabled create option that can stage an entry before terms are complete. Test both UI cases.
- [x] `test/Hexalith.Agents.Server.Tests/ProviderCatalogCoordinationIntegrationTests.cs` and `test/Hexalith.Agents.Server.Tests/TenantProviderCatalogIntegrationTests.cs` — prove a fenced provider command reaches the real source-terms coordinator and a skipped tenant event leaves its persisted checkpoint unchanged and tenant reads pending; include both in the story gate.

**Acceptance Criteria:**
- Given a Platform Operator, when a valid catalog command commits, then only the `system` entry changes and replay preserves terms, pricing, versions, and enablement.
- Given a Platform Operator, when tenant enablement changes, then only that tenant's EventStore state changes; incomplete terms and tenant-admin mutation reject.
- Given legacy entries, when migration repeats, then targets match, the repeat is a no-op, and divergent entries conflict without rewriting history.
- Given an authorized tenant reader, when projections join, then only enabled entries appear, hidden keys resemble absent keys, and no configuration state leaks.
- Given current terms, when a tenant administrator accepts or declines, then durable evidence records exact rendered fields/justification; stale or cross-tenant decisions preserve state.
- Given successive terms, when eligibility is evaluated at the deadline, then only declared cumulative tightening grants nonextending grace until that instant.
- Given unaccepted or expired tenant terms or a platform projection behind the committed stream, when selection or activation reads the catalog, then the verdict blocks and no `Valid` selection is recorded.
- Given a no-op, rejected, or concurrently superseded governance command, when its caller checks write truth, then only that command's authoritative result determines confirmation and another writer's projection cannot confirm it.
- Given an accepted legacy event awaiting projection, when migration begins, then it waits or returns pending without copying a stale legacy entry; after the delivery it migrates once and repeats as a no-op.
- Given a committed tenant disable or decline awaiting projection, when selection or activation reads the joined catalog, then it blocks until the tenant projection catches up.
- Given a pre-upgrade legacy projection or an existing platform target with mismatched enablement, when migration resumes, then sequence-two legacy delivery can catch up, an enabled target with disabled sources conflicts before dispatch, and a disabled target can upgrade for a later enabled source.
- Given a pending tenant decision whose poll times out, when the administrator tries to submit another decision for that entry, then the prior command remains locked until exact terminal truth is obtained.
- Given a pending platform catalog mutation whose poll times out, when the operator tries another mutation for the same provider/model, then the prior command remains locked while unrelated entries stay usable.
- Given a Platform Operator editing a model, when text generation, safe capability flags, or the configuration reference need changing, then the UI submits those explicit choices without disclosing a stored configuration reference.
- Given a public tenant catalog read while a platform or tenant stream is ahead of its projection, when truth is presented, then it remains pending and does not assert current terms or absence.
- Given an exclusive grace deadline crossed during selection or while the terms page is open, when eligibility is used or shown, then the post-deadline verdict is blocked and the page refreshes its status.
- Given a migration repeat against a stale target projection or a dispatched command with a terminal rejection, when migration reports its result, then it does not claim a no-op or confirmation from that stale or unrelated projection.
- Given an operator or EventStore query read while a committed catalog, tenant enablement, or terms change awaits projection, when the result is shown, then it remains pending rather than claiming current truth.
- Given a pending catalog mutation or terms decision, when the user navigates away and returns or refreshes the browser session, then the same resource remains locked under its retained message ID until exact terminal truth is checked.
- Given a concurrent target mutation during multi-entry migration, when its final result is formed, then every target is revalidated before confirmation.
- Given an admitted legacy event or a target disable/re-enable during migration, when final confirmation is formed, then changed source or target checkpoints prevent a false confirmation even if final metadata/status looks equal.
- Given a tenant query whose stream-head check crosses an exclusive grace deadline, when its result is serialized, then eligibility reflects the later instant and cannot be selectable under expired grace.
- Given an enabled tenant key whose platform create has committed but is unprojected, when selection reads it, then it remains unavailable rather than absent; a hidden tenant key still resembles an absent key.
- Given stale operator lifecycle intent, when a later enable or disable command executes, then it rejects against the observed revision; exact retries retain their prior outcome.
- Given pending enablement for tenant A and the same model in tenant B, when the operator edits tenant B, then only tenant A stays locked; a disabled model can be created through the operator UI without completed terms.

### Review Findings

Code review 2026-09-24, chunk (a): contracts and domain aggregates (`src/Hexalith.Agents.Contracts`, `src/Hexalith.Agents`), diff `1d69f3f..bd181c0`. Chunks (b) server/EventStore coordinator, (c) UI/client, and (d) tests/verifier remain to be reviewed.

- [x] [Review][Patch] Block selection when either unit price is zero [src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs:712] — Decision 2026-09-24: keep storing explicit nonnegative prices, but `HasValidPricing` (and the mirrored server `ProviderSelectionVerdict`) must fail with `Unpriced` when the input or output unit price is 0, so zero-priced entries are never selectable and cost accounting never reads 0.
- [x] [Review][Patch] Latest terms decision wins; decline blocks only the declined current version [src/Hexalith.Agents/TenantProviderEnablement/TenantProviderEnablementAggregate.cs:131] — Decision 2026-09-24: a different decision (or a different administrator) on the same current version records a new `ProviderDataHandlingDecided` at the next revision; an identical decision by the same actor stays a no-op. `TenantProviderEligibility.Evaluate` returns `Declined` only when `DeclinedVersion` equals the current version, and `AcceptanceRequired` (blocked, no grace) when a newer version exists. Rationale: `DataHandlingVersion` advances only on field change, so a final decline could never be recovered.
- [x] [Review][Patch] Public create/enablement accept migration-only fields [src/Hexalith.Agents.Contracts/ProviderCatalog/Commands/CreateProviderModelEntry.cs:12] — `MigratedFrom` and `InitialCapabilityVersion` bind straight from the public request body; any non-null (even blank) `MigratedFrom` disables version-1 creation rules and records fabricated provenance. The same holds for `SetTenantProviderModelEnablement.MigratedFrom`. Reject these fields on the public write path so only `ProviderCatalogMigrationService` can supply them.
- [x] [Review][Patch] Exact-duplicate create with terms is rejected, not a no-op [src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs:681] — `SameTerms` compares `EffectiveAt`, which `EventStoreProviderCatalogOperations.Stamp` rewrites with server time on every submission, so a resubmitted identical create returns `ProviderModelEntryAlreadyExistsRejection`. The existing no-op test uses a fixed fixture timestamp and cannot see it; add a test whose duplicate differs only in `EffectiveAt`.
- [x] [Review][Patch] ISO currency set depends on host ICU and throws under invariant globalization [src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs:541] — The static initializer throws `ArgumentException` under `DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=1` (verified by probe), which becomes a `TypeInitializationException` for the whole aggregate; with ICU the accepted set varies by ICU version and omits valid ISO 4217 codes. Use a static ISO 4217 list.
- [x] [Review][Patch] Tenant enablement no-op ignores a null `MigratedFrom` [src/Hexalith.Agents/TenantProviderEnablement/TenantProviderEnablementAggregate.cs:83] — `Apply` keeps the first provenance (`??=`), but the no-op check requires equality, so re-sending the same enabled state without provenance emits an event, bumps the tenant revision, and staleness-rejects concurrent decisions without changing state.
- [x] [Review][Patch] Test the terms-history fold that drives tenant eligibility [src/Hexalith.Agents/ProviderCatalog/ProviderCatalogState.cs:89] — No test folds create plus terms-changing updates through `Apply` and checks `DataHandlingHistory` and the resulting eligibility; dropping the history append would leave a tenant `Current` under loosened terms with every test green.
- [x] [Review][Patch] Test the "terms required to enable" rejections [src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs:694] — Neither the create (`Enabled` with null terms) nor the enable (`DataHandling: null`) rejection is asserted anywhere.
- [x] [Review][Patch] Test platform selectability without terms [src/Hexalith.Agents/ProviderCatalog/ProviderCatalogInspection.cs:95] — No sibling not-selectable test covers an enabled entry with null or version-0 terms.
- [x] [Review][Patch] Test the decline decision round trip [src/Hexalith.Agents/TenantProviderEnablement/TenantProviderEnablementState.cs:47] — Every decision test uses `Accepted = true`; no test handles, applies, and evaluates a decline.
- [x] [Review][Patch] Test tightening-declaration rejections [src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs:632] — Client-supplied declarations on create/update and `DeclareDataHandlingTightening` with loosened or unchanged terms are rejected without any test; removing the check would let a caller forge the declaration that grants grace.
- [x] [Review][Patch] Complete XML docs for new public contracts [src/Hexalith.Agents.Contracts/ProviderCatalog/ProviderDataHandlingRecord.cs:5] — `ProviderDataHandlingRecord` lacks `<param>` for `EffectiveAt` and `TighteningDeclaration`; new command/event records and `TenantProviderEnablementAggregate.Handle` overloads lack param/returns docs required by the repository standard.
- [x] [Review][Defer] Pricing-version and initial-capability-version validation is untested [src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs:585] — deferred: today's UI always sends `PricingVersion: 0`; add theory rows when this area is next touched.
- [x] [Review][Defer] Raw EventStore command submission may bypass server stamping of `EffectiveAt`/`DecidedAt` [src/Hexalith.Agents/ProviderCatalog/ProviderCatalogAggregate.cs:647] — deferred (maybe-false, medium if true): the aggregate trusts payload times and only `EventStoreProviderCatalogOperations` stamps them; settle by confirming whether the external EventStore gateway host (DW-21) exposes generic command submission for `provider-catalog`/`tenant-provider-enablement` to operators or tenant administrators.

**Rejected (chunk a):**

- rejected by decision 2026-09-24 — Tenant views disclose the Platform Operator's `TighteningDeclaration.ActorUserId`: PRD FR-4 (prd.md:207) makes the declaration and diff tenant-visible Audit Evidence, and the coordinator's exact rendered-terms confirmation depends on tenants receiving the full record.
- false — Retried enablement/decision returns `StaleRevision` instead of no-op: exact retries are deduplicated by EventStore message-ID idempotency (coordinator digest includes `MessageId`); revision-before-value ordering is the deliberate B7/E5 fix.
- false — `ConfirmedTerms` / enablement `CurrentTerms` unvalidated or fabricable: `AgentsProviderCatalogCoordinationPolicy.Validate` requires an exact rendered-snapshot match with the authoritative platform terms before tenant mutation.
- false — Operator controls `EffectiveAt`, future-dated terms treated as current, `AddDays` overflow near `MaxValue`: the public write path stamps `EffectiveAt` and `DecidedAt` from the server clock (raw-gateway residual deferred above).
- false — `Assign` NRE on null regions: `Validate` rejects null regions before `Assign`/`SameTerms` on create and update.
- false — Legacy streams can no longer be updated/disabled: freezing legacy writes is required by the spec.
- false — `EntryKey` format change breaks snapshots: legacy streams deny every write before `FindEntry`, new `system` streams post-date the change, and migration reads projections rather than aggregate state.
- false — Migration imports only current terms: legacy entries carried no data-handling record (B5).
- false — `EffectiveAt`-only update silently dropped: `EffectiveAt` is server-stamped; no caller can express such a correction.
- false — View exposes mutable `DataHandlingHistory`: views are mapped from freshly replayed state that is not mutated afterward.
- false — `IsPlatformEntry` bypass on blank IDs: blank IDs are rejected by metadata validation before any event.
- false — `Apply(ProviderDataHandlingDecided)` revision drift: the handler emits only for existing enabled entries.
- false — Decision no-op uses `SameFields` rather than snapshot: the coordinator enforces the exact snapshot before the aggregate runs.
- low — Magic-string statuses/role names: no present divergence; converting to enums is more than a direct fix.
- low — No upper bounds on retention, regions, justification, `MigratedFrom`: adding guards; not met in normal use.
- low — Projection message IDs on domain state, `_ = Revision` no-op, redundant checks, `SameSnapshot` byte comparison: cosmetic, no named harm.
- low — Disabling a never-enabled tenant entry records a disabled entry: indistinguishable from absent for tenant reads.

## Implementation Notes

The staged implementation adds the reserved `system` catalog, tenant enablement and terms decisions, a Platform Operator migration endpoint, safe tenant joins, recorded tightening declarations, and command-message-specific UI confirmation. Migration reads the operator-supplied legacy tenant inventory from persisted projections and rejects divergent or mismatched records before dispatch. The supplied inventory cannot itself prove that every legacy tenant was named.

`TMPDIR=/home/administrator/tmp-story-5-3 pwsh ./eng/verify-story-5.3.ps1` passed after review fixes. Both builds had zero warnings. Focused EventStore/domain/server/UI suites passed (5/68/60/16 tests), as did all five full owning test projects (689/6/828/688/1104 tests). The review findings were triaged below and every accepted patch was verified. Both repositories passed `git diff --check`. The broader EventStore Server.Tests run had three failures in unchanged security/query paths; a clean baseline was not run, so their provenance is unconfirmed.

On 2026-09-24 the user chose to extend Hexalith.EventStore for the Terms matrix row. A provider/model keyed coordinator now serializes platform catalog mutations with tenant enablement and terms decisions, reads the authoritative platform events, and rejects superseded terms before tenant mutation. Focused tests cover both command orderings, persisted tenant revision, exact retry, cross-tenant message-ID isolation, and altered rendered terms. The ordering test uses the real router/coordinator/policy with simulated actors; a live Dapr sidecar concurrency test has not run. Keep `NC-5.3-PLATFORM-CATALOG-SCOPE` open until its evidence is accepted, the operator supplies a complete legacy tenant inventory, and the external EventStore gateway host registers `AddAgentsEventStore` (DW-21).

## Spec Change Log

- 2026-09-24 review loop 1: BH3, BH4–BH7, BH10, and EH2 exposed missing authoritative freshness, command-specific confirmation, legacy projection catch-up, and complete tenant-visible terms evidence. Added explicit code paths, tests, and acceptance above; the known-bad state is a stale or unrelated projection being treated as current truth. KEEP: reserved `system` streams, tenant-safe join, pure aggregates, EventStore coordination, server-stamped terms, explicit submitted/pending/confirmed stages, prior accepted review patches, and zero-warning test gates. Existing committed feature code is retained under repository Git instructions; this loop derives corrective changes forward without reverting commits.
- 2026-09-24 review loop 2: LB1–LB2, LB4–LB6, LB8–LB11, EH2, and VG1–VG2 exposed tenant-stream lag, migration upgrade/target conflicts, optional update evidence, projection truth gaps, a lost pending lock, incomplete operator controls, and missing boundary assertions. Added the corrective tasks and acceptance above. Known-bad state avoided: a committed tenant revocation remains selectable or a pending decision can be resubmitted. KEEP: the already verified platform checkpoint, exact command outcome path, bounded projected command IDs, localized terms view, legacy catch-up design, and prior review fixes. Existing committed feature code remains under repository Git instructions; derive corrections forward without reverting commits.
- 2026-09-24 review loop 3: LC1–LC8, EC3, and VG5 exposed public tenant truth based on lagging projections, deadline drift while reads/pages remain open, tenant enablement UI gates, migration target/status races, and a missing zero-event wire assertion. Added the corrective tasks and acceptance above. Known-bad state avoided: a stale projection presented as current or a different command's target state confirming migration. KEEP: both authoritative selection checkpoints, exact command ID UI confirmation, migration's one-way disabled-to-enabled upgrade, localized operator controls and pending locks, zero-warning gates, and all prior accepted fixes. Existing committed code remains under repository Git instructions; derive corrections forward.
- 2026-09-24 review loop 4: LD4–LD5, LD8, LD10, LD12, EC7, and VG6–VG7 exposed remaining projection-only query/operation surfaces, page-local pending identity, a final migration race, undefined capability bits, and an untested fenced source guard. Added shared freshness, session retention, final revalidation, validation, and tests above. Known-bad state avoided: a public read claiming confirmed stale terms or a refreshed page permitting a duplicate unresolved decision. KEEP: exact command outcome and projection identity checks, operator/tenant local locks, grace refresh, source/target migration checkpoints, localization, and warning-free gates. Existing committed feature code remains under repository Git instructions; derive corrections forward.
- 2026-09-24 review loop 5: L5E2, L5B1–L5B2, L5B5, L5B9–L5B10, and the verified patch findings exposed checkpoint-equivalent migration races, a query deadline race, missing-row selection truth, stale lifecycle intent, a model-wide enablement lock, and absent route/sequence tests. Added explicit checkpoint, clock, lifecycle, UI, and test requirements above. Known-bad state avoided: a migration or query claiming current truth after a source/target or grace change, and one tenant's pending enablement blocking another. KEEP: all previously verified exact command outcomes, authoritative named-head checks, tenant-safe reads, session locks, grace refresh, zero-warning gate, and the operator list's honest pending completeness truth. Existing committed feature code remains under repository Git instructions; derive corrections forward without reverting commits.

## Review Triage Log

| Finding | Verdict and evidence | Route |
| --- | --- | --- |
| B1 | medium — `EntryKey` accepts its own separator in IDs, and the new tenant fold also uses it; distinct pairs can alias. | patch |
| B2 | medium — migration fingerprints `Status`, so tenant-specific disabled state falsely conflicts with an enabled peer. | patch |
| B3 | false — `MigrateAsync` explicitly takes a complete operator inventory, reports only work for those IDs, and NC closure remains pending independent inventory acceptance; it does not attest global completeness. | reject |
| B4 | medium — platform `MigratedFrom` depends on the supplied tenant set, preventing a later omitted tenant from joining an existing exact platform target. | patch |
| B5 | false — the pre-migration legacy contract had no data-handling record or history; no historical terms exist to copy from those streams. | reject |
| B6 | medium — public tenant disable checks for terms before dispatch even though disable needs no current terms. | patch |
| B7 | medium — enablement's value-based no-op precedes `ExpectedRevision`, allowing a distinct stale command to succeed. | patch |
| B8 | medium — public decision precheck ignores effective time and declaration, then records the server snapshot as caller confirmation. | patch |
| B9 | medium — a committed decision retry after a terms update fails the projected-current check before EventStore can reconcile its message ID. | patch |
| B10 | medium — governance HTTP writes omit the available idempotency and correlation headers, preventing callers from controlling retry identity. | patch |
| B11 | medium — an absent projection returns pending truth but the initial tenant page renders Empty because it checks entry count only. | patch |
| B12 | false — the review artifact includes a separate prefixed EventStore submodule diff, so its changes can be reviewed or applied; repository instructions prohibit creating a submodule commit solely for this build. | reject |
| V1 | medium — authorized tenant list/get query handlers have no executed disclosure test, leaving a meaningful regression gap. | patch |
| V2 | medium — the migration test's fixture-based persistence bypasses label, limits, capabilities, configuration, pricing, and version. | patch |
| V3 | medium — the public authorized tenant decision dispatch is not exercised by current tests. | patch |
| E1 | medium — `HasValidPricing` applies a version-one creation rule to a valid migrated or updated version above one, blocking selection. | patch |
| E2 | medium — a non-null confirmed terms record with null regions reaches `SameFields` and throws rather than returning a safe rejection. | patch |
| E3 | medium — same full-snapshot mismatch as B8; the public precheck is weaker than the coordinator. | patch (B8) |
| E4 | medium — same stale enablement no-op as B7. | patch (B7) |
| E5 | medium — a distinct stale decision with the same value returns no-op before revision validation. | patch |
| E6 | low — imported data-handling version `int.MaxValue` can overflow on the next terms change. | patch |
| E7 | low — imported capability version `int.MaxValue` can overflow on update. | patch |
| E8 | false — `ProjectedProviderCatalogReader` is an internal server selection/activation port; its callers use only readiness and capability version and never serialize the returned platform view to a tenant. Public tenant queries use `TenantProviderCatalogEntryView`. | reject |
| A1 | low — final audit found the same `int.MaxValue` overflow in migrated pricing on a changed price; the guard and focused aggregate test now reject it before arithmetic. | patch |
| BH1 | medium — `CreateEntry` converts a missing projection's `AuthoritativePending` result into `EntryNotFound`; a detail read asserts absence before the join is known. | patch |
| BH2 | false — `PlatformNotReady` is the required platform blocker when the platform entry is disabled or invalid; the terms record remains on the tenant view. | reject |
| BH3 | medium — a committed platform terms update can precede its projection, leaving the old accepted terms selectable through the projected reader. The implementation plan lacks an authoritative freshness gate for selection and activation. | bad_spec |
| BH4 | medium — the catalog write path reports `Submitted` for a dispatched receipt without resolving a synchronous rejection or later terminal status; the UI can remain pending after a rejection. The plan lacks command-outcome correlation. | bad_spec |
| BH5 | medium — an unchanged tenant enablement is an aggregate no-op, so the UI's required new message ID and revision never arrive and polling stays pending. | bad_spec (BH4) |
| BH6 | medium — an identical tenant terms decision is also a no-op, leaving its UI confirmation poll pending for the same reason. | bad_spec (BH4) |
| BH7 | high — platform confirmation checks only capability version or status; another operator's successful update can satisfy it after this command was rejected. Command-specific outcome evidence is missing. | bad_spec (BH4) |
| BH8 | medium — the French catalog editor accepts only `true`/`false` text for training use, so the localized UI offers an English-only input format. | patch |
| BH9 | false — legacy create/update paths already validated label, limits, configuration reference, flags, pricing, and versions; their persisted projections cannot contain the described invalid source fields through normal writes. | reject |
| BH10 | medium — the new projection handler acknowledges undelivered legacy events without folding them, so migration can consume a permanently stale legacy model. Catch-up before migration is absent from the plan. | bad_spec |
| BH11 | low — selecting another tenant entry leaves the previous truth or failure beside its decision form; clearing those two local fields is a direct correction. | patch |
| BH12 | maybe-false — simulated coordinator actors passed, but a live Dapr sidecar ordering test has not run. The external gateway host and sidecar test are needed to establish whether runtime serialization fails. | defer (unverified medium) |
| EH1 | medium — the tenant reader passes an eligibility-derived selectability flag, but `ProviderSelectionVerdict` ignores it, permitting a `Valid` selection with unaccepted terms. | patch |
| EH2 | medium — the tenant terms page does not render the effective time or tightening declaration and diff that the confirmed terms snapshot and PRD make tenant-visible. | bad_spec |
| EH3 | low — tenant revision can overflow after 2,147,483,647 mutations; that scale is implausible for a tenant stream and a fix adds branches to both write paths. | reject |
| VG1 | medium — no live reader test proves that unaccepted or expired terms make a projected entry nonselectable and block the interaction gate; the current tests isolate the join and gate. | patch (EH1) |
| VG2 | medium — `ProviderSelectionVerdict` never checks the tenant selectability flag, so selection and activation can record `Valid` even when the join marked terms ineligible. | patch (EH1) |
| LB1 | high — the reader checks only the platform stream head; a committed tenant disable or decline can remain selectable until its projection catches up. | bad_spec |
| LB2 | medium — migration ignores target status when matching metadata, and only repairs disabled-to-enabled; an enabled target with disabled legacy sources passes as success. | bad_spec |
| LB3 | carried false — same inventory-completeness claim as B3; migration accepts the operator-supplied inventory and NC closure remains separately gated by complete inventory evidence. | reject |
| LB4 | medium — a public update with null expected capability version bypasses the only update concurrency check and can overwrite newer metadata; enable/disable operate on current status and do not overwrite those fields. | bad_spec |
| LB5 | medium — the delivery preflight resolves event types but does not deserialize payloads; a corrupt event makes `Fold` stop at checkpoint zero while the handler can acknowledge a partial model. | patch (moot under loopback) |
| LB6 | medium — `CreateList` with a missing model and no expected version marks the empty platform catalog current and confirmed. | patch (moot under loopback) |
| LB7 | carried false — same platform-blocker/status claim as BH2; `PlatformNotReady` is the required blocker and the terms record remains visible. | reject |
| LB8 | medium — `ReadPlatformModelAsync` catches a store failure as null, so tenant enablement reports `NotFound` for a store outage. | bad_spec |
| LB9 | medium — tenant decision polling times out to `AuthoritativePending` but releases `_busy`, allowing another decision while the earlier one has no terminal outcome; UX-DR31 requires the pending lock. | bad_spec |
| LB10 | medium — the operator editor hardcodes text generation on creation and preserves safe flags on edit, so the UI cannot govern those recorded fields. | bad_spec |
| LB11 | medium — blank configuration-reference input means retain, so an operator cannot explicitly clear the existing reference through the UI. | bad_spec |
| EH4 | high — same tenant stream freshness gap as LB1; a delayed disable or decline remains selectable. | bad_spec (LB1) |
| EH5 | medium — older legacy read models have only `LastSequenceNumber`; a delayed event at sequence two fails the new per-stream gap check forever because its checkpoint defaults to zero. | bad_spec |
| EH6 | false — `ConcurrencyConflict` is an infrastructure failure without a domain rejection event; `CommandStatusQueryResponse.IsRejected` deliberately excludes it, and `Unavailable` is the safe terminal result. | reject |
| VG3 | medium — the owning tenant is returned by the status controller but not asserted at the API boundary or by the story gate; removing it would cause the outcome reader to reject valid status. | patch (moot under loopback) |
| VG4 | medium — tenant UI tests check timestamp labels but not the supplied effective and declaration values; a wrong date would pass. | patch (moot under loopback) |
| LC1 | carried medium — same missing-projection detail claim as BH1: `CreateEntry` still sets `EntryNotFound` while the list truth is pending. | patch (rederive under loopback) |
| LC2 | high — public tenant list/detail marks existing projections current without reading either committed stream head, so a delayed terms change or revocation is displayed as current. | bad_spec |
| LC3 | medium — the open tenant page holds a grace status computed at load time past its exclusive deadline until another local refresh. | bad_spec |
| LC4 | medium — the operator page disables tenant enablement editing with no platform terms and its submit guard prevents a disable that the public path now permits. | patch (moot under loopback) |
| LC5 | medium — the operator page releases `_busy` after tenant enablement polling times out, allowing another same-target decision while the first outcome is unknown. | bad_spec |
| LC6 | medium — migration waits for matching target projections without checking the submitted command outcome, so a rejected command can be reported pending or another migration's matching projection can count as its confirmation. | bad_spec |
| LC7 | high — migration checks only legacy source freshness; an already committed change ahead of an existing platform or tenant target projection can produce a false `NoOp` or conflict. | bad_spec |
| LC8 | low — an arbitrary missing command-status record maps to `Submitted` indefinitely; the caller has no receipt evidence for that ID, so `UnableToVerify` states the uncertainty more accurately. | patch (moot under loopback) |
| LC9 | low — full platform replay adds linear validation cost as a model's update history grows, but normal catalog histories are small and a bounded state design adds substantial complexity without an observed latency problem. | reject |
| LC10 | low — each distinct source conflict adds a coordinator rejection key; retaining retry identity is required, and ordinary operator/tenant use adds few such keys relative to the durable status ledger. A bounded retention policy would add complexity without a demonstrated storage pressure. | reject |
| EC1 | low — a command identity may leave the 64-item projection window after 64 later same-stream commands before the caller checks; this volume during a governance poll is unlikely and a persisted-stream fallback adds significant complexity. | reject |
| EC2 | carried low — same `int.MaxValue` tenant revision overflow as EH3; that mutation count is implausible and the added guards would be disproportionate. | reject |
| EC3 | medium — tenant eligibility is computed before awaited authoritative checkpoint reads; the exclusive grace deadline can pass while those reads are in flight and leave a `Valid` result. | bad_spec |
| EC4 | false — production terms effective times are stamped from server time, legacy entries had no terms, and the near-`MaxValue` input is unreachable through the governed command path; raw gateway exposure remains separately deferred. | reject |
| EC5 | false — the projection consumes domain-produced catalog events; aggregate validation rejects semantically invalid values before emission, while the changed preflight handles unreadable bytes and unresolved event types. No normal producer of the proposed syntactically valid but invalid event was shown. | reject |
| VG5 | medium — no gateway client test deserializes a completed HTTP status with `EventCount = 0`; losing that nullable field would leave authoritative no-ops unconfirmed. | patch (moot under loopback) |
| LD1 | carried false — same operator-inventory completeness claim as B3/LB3; migration accepts a supplied inventory while NC closure still requires independent completeness evidence. | reject |
| LD2 | false — legacy aggregate pricing assignment already uppercases currency before projection, and legacy entries had no data-handling regions; normal persisted legacy sources cannot carry the proposed raw values. | reject |
| LD3 | false — the pre-migration legacy aggregate validates pricing and positive limits before emitting events; the proposed invalid later group cannot arise through the authoritative legacy write path. | reject |
| LD4 | high — operator list/detail operations read the platform projection without comparing its entry stream heads, so a committed disable or terms update can be labeled confirmed while the projection lags. | bad_spec |
| LD5 | medium — operator tenant-enablement inspection returns the projected revision/status without checking the committed tenant stream head, inviting an obsolete revision submission. | bad_spec |
| LD6 | false — a platform command committed during an already-running selection read is concurrent with that read; its platform-head check is a valid linearization point, and activation performs its own validation read. The specified stale-at-read case remains blocked. | reject |
| LD7 | carried low — same 64-identity eviction case as EC1; more than 64 same-stream governance events before a poll is unlikely, and fallback replay would add substantial complexity. | reject |
| LD8 | medium — component-local pending IDs disappear on navigation/refresh even though UX-DR31 scopes advisory locks to the user session; another command can then be sent before exact truth. | bad_spec |
| LD9 | low — a deadline refresh failure leaves the page visibly pending and blocked until manual reload; transient failure is uncommon and an automatic retry loop adds lifecycle complexity. | reject |
| LD10 | medium — create/update accept numeric capability bits outside the four declared safe flags, violating the allow-listed contract for direct API callers. | patch (moot under loopback) |
| LD11 | carried low — same full-history coordinator replay cost as LC9; ordinary per-model update histories are small and no latency breach is demonstrated. | reject |
| LD12 | medium — multi-entry migration checks each target as it goes but can return confirmed after an earlier target is concurrently changed; final target revalidation is missing. | bad_spec |
| EC6 | carried false — same semantically invalid direct event claim as EC5; normal projections consume validated domain events, and no reachable producer was shown. | reject |
| EC7 | high — EventStore tenant `/query` joins projections directly and labels them confirmed while a committed terms or enablement event may await projection. | bad_spec (VG7) |
| EC8 | false — an enabled tenant entry absent from the platform projection is explicitly detected by `IsTenantCatalogCurrentAsync`; a new entry not enabled for that tenant is correctly omitted. | reject |
| EC9 | medium — tenant enablement after a committed platform create but before its projection currently reports `NotFound` even though the authoritative entry stream exists. | bad_spec |
| VG6 | medium — fenced coordinator stale-source validation has no executed test after a reconciliation miss; a fenced-only bypass could persist superseded terms unnoticed. | patch (moot under loopback) |
| VG7 | high — the registered EventStore `/query` path lacks the committed-head check already added to client operations, so it can return stale tenant eligibility as confirmed. | bad_spec |
| L5V1 | medium — no test routes a fenced provider command through the real coordinator policy, so a fenced-only bypass could commit a stale tenant decision while actor-only and unfenced tests pass. | patch (moot under loopback) |
| L5V2 | medium — no test skips a tenant event before a later delivery; a removed sequence guard could advance the checkpoint to the head while leaving an old enabled state. | patch (moot under loopback) |
| L5E1 | medium — `BeginEnablement` marks an entire model pending when any tenant has an outstanding enablement, and the submit guard blocks a different tenant despite its distinct key. | patch (moot under loopback) |
| L5E2 | medium — final migration compares current target heads with current projections and expected metadata/status, but a concurrent disable/re-enable can advance a target checkpoint and restore the expected state unnoticed. | bad_spec |
| L5E3 | carried low — same deadline-refresh failure as LD9; the page becomes stale and blocked until manual reload, while retry scheduling adds lifecycle complexity for an uncommon transient fault. | reject |
| L5B1 | medium — tenant `/query` constructs eligibility before awaited stream-head reads; grace can expire during those reads and a serialized result can still report selectable Grace. | bad_spec |
| L5B2 | medium — the selection reader skips the platform head when its row is absent; an enabled tenant key whose platform create committed before projection can be reported as missing rather than pending. | bad_spec |
| L5B3 | false — the cited reader is an internal selection port and its public `SelectProviderModelAsync` route is still unavailable; no 5.3 tenant-facing read discloses this status. The tenant query path filters hidden keys before platform-specific checks. | reject |
| L5B4 | false — `AgentProviderSelectionOrchestrator` is internal and the public 5.3 selection operation returns `Unavailable` before invoking it, so the `NotConfigured` verdict is not disclosed to a tenant by this story's public surface. | reject |
| L5B5 | medium — platform enable/disable commands have no caller-observed version, so a stale operator lifecycle command can reverse a more recent status decision while the aggregate checks only present status. | bad_spec |
| L5B6 | false — catalog projection events are produced by the aggregate from a scope-validated envelope and command identity; no reachable producer of the proposed misaddressed payload was shown. Direct raw gateway exposure is tracked separately. | reject |
| L5B7 | false — tenant enablement/decision events are produced from the validated envelope tenant, and the aggregate rejects a mismatched command tenant before emission; the proposed cross-tenant event cannot arise through the governed write path. | reject |
| L5B8 | carried low — same 64-ID projection-window eviction claim as EC1/LD7; normal governance command volumes make it unlikely, and a persisted-stream fallback adds substantial complexity. | reject |
| L5B9 | medium — migration checks legacy source heads before dispatch but not at final confirmation; a previously admitted legacy event can commit during migration after preflight and leave copied targets stale. | bad_spec |
| L5B10 | medium — the operator editor hardcodes `Enabled: true` on create, so a model cannot be staged disabled through the UI before terms are complete even though the domain supports it. | bad_spec |
| L5B11 | false — a governed acceptance is coordinated against the exact current platform snapshot, which is then retained in immutable platform history; migration does not create accepted tenant terms. No reachable missing-history acceptance was shown. | reject |
| L5B12 | carried maybe-false — same live-Dapr ordering evidence gap as BH12; the external gateway host and sidecar remain unavailable, with no new observed runtime failure. | defer (unverified medium; already recorded) |
| FVG1 | high — a real EventStore no-op returns `EventCount = 0` but `AggregateActor.CompleteTerminalAsync` stores null for zero; the new status API and outcome reader therefore cannot confirm an authoritative no-op. The actor-to-status boundary lacks a test. | patch |
| FB1 | high — the stream controller returns 404 `missing-stream` for an absent target and the gateway throws; migration's sequence-zero preflight catches it as false, so a first migration remains pending before creating any target. | patch |
| FB2 | high — public tenant reads clear stale entries but retain projected command IDs; UI polling can use that ID to claim `ProjectionConfirmed` while either authoritative head is ahead. | patch |
| FB3 | high — catalog, enablement, and decision polling accept a projected ID with a nonterminal outcome when the receipt said pending, allowing confirmation before the exact command reaches successful terminal status. | patch |
| FB4 | carried low — same 64-ID eviction case as EC1/LD7/L5B8; normal governance volume makes it unlikely and persisted replay adds significant complexity. | reject |
| FB5 | medium — disabled entries created without terms cannot be edited because `_editing` alone forces terms validation, preventing label/pricing/capability staging until terms exist. | patch |
| FB6 | high — tenant disablement unnecessarily reads and validates platform terms first; an unavailable or lagging platform projection can block a safety revocation that needs no current terms. | patch |
| FB7 | medium — legacy full replay rebuild removes rows by hashed entry ID while the legacy stream ID is the tenant ID, so an orphaned projected legacy row survives a rebuild. | patch |
| FB8 | low — a transient post-submission failure leaves `_storeUnavailable` latched until navigation or refresh; the page stays fail-closed and session locks recover on reload, while safe in-place recovery needs separate storage/network fault state. | reject |
| FB9 | low — a malformed detail payload can be shaped as pending rather than a validation failure; such payloads are uncommon and a distinct handler/base validation path adds branching without a data leak or accepted write. | reject |
| FB10 | low — a pending grace refresh leaves the open page stale until manual reload; it remains blocked and the transient retry timer would add lifecycle complexity. | reject |
| FE1 | medium — a rejected create can advance a fully projected stream without a catalog row; comparing that stream with zero reports `Unavailable` forever instead of a confirmed missing entry. | patch |
| FE2 | medium — blank provider/model input reaches `ProviderCatalogIdentity.EntryId` in tenant enablement and throws instead of returning a shaped validation result. | patch |
| FE3 | carried low — same `int.MaxValue` tenant revision overflow as EH3/EC2; more than two billion successful mutations is implausible and guards would add complexity. | reject |
| FE4 | low — lifecycle revision can overflow after more than two billion status toggles; this is implausible and a guard adds a new rejection path without a realistic caller. | reject |
| FE5 | carried false — same misaddressed platform event claim as L5B6; governed events use the validated envelope and command identity, with no reachable alternate producer shown. | reject |
| FE6 | carried false — same cross-tenant event claim as L5B7; the aggregate emits the validated envelope tenant, not an arbitrary payload tenant. | reject |
| FE7 | false — the tenant aggregate emits a decision only after an enabled entry exists in the same stream, and the projection's contiguous-sequence guard prevents skipping its enablement event. | reject |
| FE8 | medium — same legacy rebuild orphaned-row defect as FB7; a tenant-wide legacy replay must replace rather than retain rows for that stream. | patch (FB7) |
| FE9 | low — slash-delimited grid keys can collide for otherwise valid provider/model pairs because identifiers have no slash restriction; using `ProviderCatalogIdentity.EntryId` is a direct correction in both grids. | patch |

## Design Notes

PRD FR-33 and UX restrict configured state/reference to Platform Operators despite broad Story 5.3 query wording. Use distinct views. PRD assumption A-10 remains a launch gate.

## Verification

**Commands:**
- `pwsh ./eng/verify-story-5.3.ps1` — warning-free owning build and focused/full test projects, including persisted end state and negative cases.

**2026-09-24 result:** The command passed with zero build warnings and no skipped tests. Focused suites passed 5/80/64/16 cases; the five full owning projects passed 689/6/843/694/1104 cases. The accepted chunk (a) patches are implemented and covered. Review chunks (b)–(d) remain pending. `NC-5.3-PLATFORM-CATALOG-SCOPE` remains open until the stated external evidence, complete legacy inventory, and gateway registration are accepted.

**Review loop 1 result:** `pwsh -NoProfile -File eng/verify-story-5.3.ps1` passed after the freshness, outcome, UI, and migration corrections: zero build warnings; focused 5/80/79/22; full 689/6/843/709/1112; no skipped tests. Exact command ID windows are capped at 64 per stream. bUnit covers the Fluent switch role and boolean change event; native Space/Enter behavior remains for platform-host browser evidence. The EventStore submodule has three required uncommitted status-contract edits; its parent pointer remains unchanged.

**Review loop 2 result:** `pwsh -NoProfile -File eng/verify-story-5.3.ps1` passed after the tenant checkpoint, migration, projection, operator controls, lock, and boundary-test corrections: three builds with zero warnings; focused EventStore server/client, domain, Agents server/UI 30/1/81/86/26; full owning projects 689/6/844/716/1116; composition 9/7; no skipped tests. Root and EventStore worktrees pass `git diff --check`.

**Review loop 3 result:** `pwsh -NoProfile -File eng/verify-story-5.3.ps1` passed after the public read, grace, migration outcome, and enablement UI corrections: three builds with zero warnings; focused EventStore server/client, domain, Agents server/UI 30/2/81/94/28; full owning projects 689/6/844/724/1118; composition 9/7; no skipped tests. Returning to a pending enablement shows localized pending truth and keeps the target locked. Both worktrees pass `git diff --check`.

**Review loop 4 result:** `pwsh -NoProfile -File eng/verify-story-5.3.ps1` passed after the authoritative read, session lock, final migration, and safe flag corrections: three builds with zero warnings; focused EventStore server/client, domain, Agents server/UI 31/2/83/104/33; full owning projects 689/6/846/734/1125; composition 9/7; no skipped tests. Root and EventStore worktrees pass `git diff --check`. Operator catalog lists remain `AuthoritativePending` because EventStore exposes no global provider/model stream inventory; named detail reads and tenant lists can confirm from their authoritative stream heads. The external `NC-5.3-PLATFORM-CATALOG-SCOPE` evidence gate remains open.

**Review loop 5 result:** `pwsh -NoProfile -File eng/verify-story-5.3.ps1` passed after the checkpoint, query-time grace, missing-row, lifecycle revision, operator UI, and integration corrections: three builds with zero warnings; focused EventStore server/client, domain, Agents server/UI 31/2/84/114/35; full owning projects 689/6/847/744/1127; composition 9/7; no skipped tests. The external `NC-5.3-PLATFORM-CATALOG-SCOPE` evidence gate remains open.

**Final review patch result:** `pwsh -NoProfile -File eng/verify-story-5.3.ps1` passed after the zero-event status, exact missing-stream, read freshness, safe revocation, legacy rebuild, validation, and UI truth corrections: three builds with zero warnings; focused EventStore server/client, domain, Agents server/UI 32/2/84/123/40; full owning projects 689/6/847/753/1132; composition 9/7; no skipped tests. Root and EventStore staged and unstaged diffs passed `git diff --check`. All final review patch findings were fixed and covered by focused tests. Rejected low-probability and unsupported findings remain documented in the triage log. `NC-5.3-PLATFORM-CATALOG-SCOPE` remains open pending accepted complete legacy inventory, external gateway registration (DW-21), and live Dapr sidecar evidence.
