---
baseline_commit: 7a26d52de81278a59a084ded64111b29f1fc502e
---

# Story 3.2: Discover Pending Proposals In Product

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As an Approver,
I want to find Proposed Agent Replies that need my action,
so that confirmation-mode responses do not stall silently.

## Acceptance Criteria

**AC1 - The proposal queue lists pending + authorized historical proposals with the safe queue fields; unapproved content is never rendered as a Conversation Message**
**Given** pending proposals exist for an authorized Approver
**When** the Approver opens the proposal queue
**Then** the queue lists pending and authorized historical proposals with proposal state, Source Conversation, caller, Agent, current responsibility, expiry, and age
**And** unapproved generated content is not rendered as a Conversation Message.

**AC2 - Filters narrow the queue; the grid distinguishes all six non-data states; filtered-empty offers a clear reset**
**Given** an Approver filters the queue
**When** filters for "needs my action", state, Agent, Source Conversation, caller, or expiry are applied
**Then** the grid distinguishes loading, empty, filtered-empty, error, permission-denied, and stale/degraded states
**And** filtered-empty offers a clear filter reset.

**AC3 - In-product visibility is the V1 mechanism (notifications deferred); a proposal count/status indication is available to authorized Approvers**
**Given** active notifications are not included in V1 launch
**When** launch readiness or product status is inspected
**Then** the system records that Approvers rely on the in-product pending-proposal visibility surface
**And** proposal count or status indication is available to authorized Approvers.

**AC4 - Unauthorized access discloses nothing through counts, empty states, filters, accessible names, or error details; the denial is safe and auditable**
**Given** an unauthorized caller opens the proposal queue
**When** proposal records exist in another tenant or outside their policy
**Then** those records are not disclosed by counts, empty states, filters, accessible names, or error details
**And** the authorization denial is safe and auditable.

[Source: _bmad-output/planning-artifacts/epics.md#Story-3.2-Discover-Pending-Proposals-In-Product; prds/prd-agents-2026-06-23/prd.md#4-6-proposed-agent-reply-workflow (FR-13, FR-14); prd.md#4-7-authorization-tenant-isolation-and-governance (FR-19, FR-20, FR-21); prd.md#4-8-admin-ui-and-api-client-contracts (FR-22); prd.md#4-9-audit-evidence-and-operational-visibility (FR-24, FR-25); architecture/architecture-agents-2026-06-23-2/ARCHITECTURE-SPINE.md#AD-5-Proposal-Lifecycle; #AD-8-Approver-Policy-Resolution; #AD-12-Authorization-And-Dependency-Uncertainty; #AD-14-Sensitive-Content-And-Secret-Safety; #AD-15-Public-Surface-And-UI-Parity]

## Tasks / Subtasks

- [x] **Task 1 - Add the proposal-queue public READ contracts to `Hexalith.Agents.Contracts/AgentInteraction/`** (AC: #1, #2, #3, #4)
  - [x] Add the safe row view `AgentInteraction/PendingProposalView.cs` (bare `record`) — **mirror `AgentInteractionStatusView.cs`** (safe references only; never content — AD-14). Fields: `string AgentInteractionId` (the aggregate-id row handle), `string ProposalId` (the Story 3.1 deterministic proposal id), `ProposedAgentReplyState State` (the proposal sub-state — `Pending` in V1; reserved 3.3–3.6 states render through a total default), `AgentInteractionStatus InteractionStatus` (the coarse durable status — `ProposalCreated` in V1; gives the broader lifecycle signal), `string SourceConversationId` (opaque reference — AD-6), `string CallerPartyId` (a reference, not PII — AD-7), `string AgentId`, `bool NeedsCurrentUserAction` (the server-computed "needs my action" flag — drives the AC2 filter and the AC1 "current responsibility" column; see Task 1 note below), `string ProposedVersionId` (the proposed version *id*, never its content), `int ApproverPolicyVersion`, `int ContentSafetyPolicyVersion`, `string? ExpiresAt` (optional ISO-8601 expiry — null when no expiry policy is configured, AC1), `string? CreatedAt` (optional ISO-8601 creation timestamp sourced from EventStore event metadata — the UI computes "age" from it; null when unavailable). Copy `AgentInteractionStatusView`'s XML class remark verbatim in spirit: it carries **NEVER** the generated/edited content, a raw provider/Conversations payload, an EventStore stream name, a stack trace, or a secret (AD-14). All member names are clear of the forbidden secret tokens (`Secret`/`ApiKey`/`Credential`/`Password`/`ConnectionString`).
  - [x] Add the inspection-status enum `AgentInteraction/PendingProposalsInspectionStatus.cs` — **mirror the `AgentInteractionInspectionStatus.cs` *convention*** (`[JsonConverter(typeof(JsonStringEnumConverter))]`, `Unknown = 0` fail-safe sentinel). Values: `Unknown = 0, Success, NotAuthorized, Unavailable, Stale`. **Note this is an extension, not a copy:** it drops `NotFound` (a list read never targets a single record) and adds the two freshness states `Unavailable`/`Stale` — both are **net-new to the Agents contracts** (no prior inspection-status enum defines them; they back the UI `Error`/`Stale` surfaces). Document each: `Success` = an authorized fresh read (the list may still be empty); `NotAuthorized` = fail-closed denial, no records disclosed (AD-12, AC4); `Unavailable` = the read faulted / a dependency is unreachable (the UI `Error` surface); `Stale` = the projection reports degraded/stale data and must not be rendered as fresh (the UI `Stale` surface — architecture "Read models expose freshness/degraded/blocked states").
  - [x] Add the list-result envelope `AgentInteraction/PendingProposalsResult.cs` (bare `record`) — **mirror `ProviderCatalog/ProviderCatalogInspectionResult.cs` exactly** (a failed/empty result carries an empty list + zero count so it never fingerprints other tenants' records — AC4). Shape: `PendingProposalsResult(PendingProposalsInspectionStatus Status, IReadOnlyList<PendingProposalView> Proposals, int PendingCount)`. Static factories: `Success(IReadOnlyList<PendingProposalView> proposals, int pendingCount)`; `NotAuthorized()` → `new(NotAuthorized, [], 0)`; `Unavailable()` → `new(Unavailable, [], 0)`; `Stale(IReadOnlyList<PendingProposalView> proposals, int pendingCount)` (degraded data may still carry rows the UI renders behind a stale notice — or pass `[]`/`0` when none are trustworthy). `PendingCount` is the AC3 count indication (the authorized count of proposals **needing the current Approver's action**); it is `0` on every non-`Success`/`Stale` outcome.
  - [x] Add the read query `AgentInteraction/Queries/ListPendingProposalsQuery.cs` (bare `record`) — **mirror `ProviderCatalog/Queries/ListProviderCatalogEntriesQuery.cs` AND the deferred `AgentInteraction/Queries/GetAgentInteractionStatusQuery.cs`**: `ListPendingProposalsQuery(bool IncludeHistorical)` (`IncludeHistorical = false` → only proposals awaiting action; `true` → also authorized historical/terminal proposals once Stories 3.5/3.6 add them). The query record carries **NO tenantId** (tenant scope is supplied by the request/envelope at dispatch — mirror `ListProviderCatalogEntriesQuery`). Copy the deferred-binding XML remark from `GetAgentInteractionStatusQuery.cs` verbatim in spirit: binding this query to the EventStore SDK `IDomainQueryHandler`/`IReadModelStore` DAPR read path (and the proposal read-model **projection** that computes `NeedsCurrentUserAction`/age/freshness) is **deferred to the dedicated Agents read-model story (Epic 4, mirroring Story 1.2)**; the stable query/view/result contracts land here.
  - [x] **Task 1 note — "current responsibility" + "needs my action":** the view carries the single server-computed `NeedsCurrentUserAction` flag. The AC1 **"current responsibility"** column is *derived in UI presentation* from `State`/`InteractionStatus` (V1: `Pending` → "awaiting approval"; the flag refines it to "awaiting your approval"), NOT a separate contract field — this avoids a premature responsibility enum for states Stories 3.3–3.6 do not ship yet. Document this in the view's XML. The live computation of `NeedsCurrentUserAction` (resolving the snapshotted `ApproverPolicyVersion` + current user identity + current dependency availability against the read path) is part of the **deferred** Epic-4 read-model binding (AD-8/AD-12); the contract field lands now.
  - [x] Keep `Hexalith.Agents.Contracts` inward-only and content-free: no provider SDK, no `Microsoft.Agents.AI`, no Dapr, no server infra, no sibling-module types, no `Hexalith.Conversations`/`Hexalith.Parties` types. The assembly-wide `ContractsSecretNonDisclosureTests` + `ContractsBoundaryTests` auto-cover the new types — confirm they stay green and unweakened.

- [x] **Task 2 - Add the UI proposal-queue read gateway seam in `Hexalith.Agents.UI/Services/Gateways/`** (AC: #1, #3, #4)
  - [x] Add the interface `IProposalQueueGateway.cs` — **mirror `IConversationAgentCallGateway.cs`** (the UI depends only on this abstraction + the public display contracts, never on `Hexalith.Agents.Server`, EventStore streams, provider SDKs, or aggregate internals — AD-15). Method: `Task<PendingProposalsResult> ListPendingProposalsAsync(bool includeHistorical, CancellationToken cancellationToken)`. Copy the deferred-binding XML remark: the live implementation calls `Hexalith.Agents.Client` → BFF/API → the Agents read-model path and is **deferred to Epic 4 (4.1/4.3)**; bUnit tests substitute this seam with NSubstitute so the `Deferred*` placeholder is never exercised in tests but keeps the DI graph complete; the method returns a fail-closed result wrapper so a non-success outcome carries no record identity (AD-12, AD-14).
  - [x] Add the fail-closed placeholder `DeferredProposalQueueGateway.cs` — **mirror `DeferredConversationAgentCallGateway.cs` / `DeferredProviderCatalogGateway.cs`**: returns `Task.FromResult(PendingProposalsResult.NotAuthorized())` (fail closed — deny, disclose nothing — so the default graph renders the `PermissionDenied` surface until the live binding is wired, exactly as the deferred provider-catalog gateway does).
  - [x] Register it in `Services/Gateways/AgentsUiServiceCollectionExtensions.cs` inside `AddAgentsUi(...)` via `services.TryAddScoped<IProposalQueueGateway, DeferredProposalQueueGateway>();` (TryAdd so a live registration wins; scoped = circuit/request scope — mirror the existing gateway registrations).

- [x] **Task 3 - Add the proposal-state presentation mapping + badge in `Hexalith.Agents.UI/Components/Shared/`** (AC: #1)
  - [x] Add `ProposedAgentReplyStatePresentation.cs` (`public static class`) — **mirror `AgentCallStatusPresentation.cs`** (pure, dependency-free, unit-testable without bUnit; bind status to a Fluent **role**, never a hex value — DESIGN Colors). Methods over `ProposedAgentReplyState`: `BadgeColor ColorFor(ProposedAgentReplyState state)` — `Pending → BadgeColor.Informative` (pending/awaiting; DESIGN `status-informative`), `Unknown → BadgeColor.Subtle`, `_ => BadgeColor.Subtle` (**total** default — the reserved 3.3–3.6 states map through it until those stories add explicit roles, mirroring `AgentCallStatusPresentation.MapStatus`'s `_ => Unknown` totality); `Icon IconFor(ProposedAgentReplyState state)` composed ONLY from the curated `FcFluentIcons` factory (e.g. `Pending → FcFluentIcons.ArrowSync16()` or `Clock`-style from the curated set; `_ => FcFluentIcons.QuestionCircle16()`); `string LabelKeyFor(ProposedAgentReplyState state)` → `$"Agents.ProposalState.Label.{state}"` (whole-string key — UX-DR14). Add a pure age helper `string AgeLabelKeyOrText(string? createdAtIso, DateTimeOffset now)` (or a coarse bucket helper) that is **deterministic given `now`** so bUnit tests pass a fixed `now` (no `DateTimeOffset.UtcNow` inside the component path — avoids flaky tests; never derives a secret/id/PII).
  - [x] Add `ProposedAgentReplyStateBadge.razor` — **mirror `AgentCallStatusBadge.razor`**: renders a `FluentBadge` with `Color="@ProposedAgentReplyStatePresentation.ColorFor(State)"`, the curated icon, and the localized whole-string label as **visible text** (color is never the sole signal — DESIGN "no-color-only"; `BadgeConformanceTests` enforces color + icon + text and a no-raw-hex regex). The accessible name is the same safe localized label — never an id, content, or PII (AD-14).

- [x] **Task 4 - Add the proposal-queue page + filters in `Hexalith.Agents.UI/Components/Pages/`** (AC: #1, #2, #3, #4)
  - [x] Add `ProposalQueue.razor` at route `@page "/agents/proposals"` — **mirror `ProviderCatalog.razor` exactly** (the canonical filterable-`FluentDataGrid`-with-surface-states page). Wrap in `<FcAggregateListPage @ref="_listPage" TItem="PendingProposalView" LayoutMode="FcPageLayoutMode.FullWidth" RootTestId="agents-proposal-queue" ...>` with `PageTitle`/`Heading`/`Eyebrow`/`Description`/`HeadingId="agents-proposal-queue-heading"`/`HeadingTabIndex="-1"`, and `<FcContentLabel LabelledBy="@HeadingId" />`. Inject `@inject IStringLocalizer<AgentsResources> Localizer` and `@inject IProposalQueueGateway Gateway`. Load in `OnInitializedAsync`: `_result = await Gateway.ListPendingProposalsAsync(includeHistorical: _includeHistorical, CancellationToken.None).ConfigureAwait(false);` (CA2007 `.ConfigureAwait(false)` is a build error if omitted). Focus the heading in `OnAfterRenderAsync` via `_listPage.FocusHeadingAsync()` (mirror ProviderCatalog).
  - [x] **Pinned `FluentDataGrid` columns** (DESIGN proposal-queue-grid; `Pin="DataGridColumnPin.Start"` on the lead columns, `ResizableColumns="true"`, `GenerateHeader="DataGridGeneratedHeaderType.Sticky"`, `ItemKey="@(p => p.AgentInteractionId)"`, `data-testid="agents-proposal-queue-grid"`): **proposal state** (`<ProposedAgentReplyStateBadge State="@context.State" />`), **Source Conversation** (`<code>@context.SourceConversationId</code>`), **caller** (`<code>@context.CallerPartyId</code>`), **Agent** (`<code>@context.AgentId</code>`), **current responsibility** (derived label — see Task 1 note; `@(context.NeedsCurrentUserAction ? Localizer["Agents.ProposalQueue.Responsibility.You"] : Localizer["Agents.ProposalQueue.Responsibility.Approver"])`), **expiry** (`@(context.ExpiresAt is null ? Localizer["Agents.ProposalQueue.Expiry.None"] : <localized timestamp>)`), **age** (from `ProposedAgentReplyStatePresentation` age helper). Add a `GridSort<PendingProposalView>` static field (e.g. sort by `CreatedAt` descending, then `AgentInteractionId`) like ProviderCatalog's `_sortByProvider`. Every cell is a safe id/enum/timestamp/whole-string — **never content**, and the grid is a queue table, never styled as a posted Conversation Message (AC1; DESIGN "a proposed reply is never styled like an already-posted Conversation Message"; UX forbidden copy: never call unapproved content a "message").
  - [x] **`<Filters>` slot — Fluent v5 components only (never raw `<select>`/`<input>` — FrontComposer Fluent-only rule):** (a) **"needs my action"** → `FluentSwitch @bind-Value="_needsMyActionOnly"`; (b) **state** → `FluentSelect` over `ProposedAgentReplyState` (+ an "all" sentinel); (c) **Agent** → `FluentSelect` over the distinct `AgentId`s present; (d) **Source Conversation** and (e) **caller** → `FluentSearch`/`FluentTextField` contains-match (or `FluentSelect` of distinct values); (f) **expiry** → `FluentSelect` (`Any` / `Expiring soon` / `Expired` / `No expiry`). All inside a `<section aria-label="@Localizer[...]">`. Filtering is **in-memory** over the authorized rows via a computed `VisibleRows` property (mirror ProviderCatalog's `VisibleRows`/`_selectableOnly`) — never a second gateway round-trip. `NeedsCurrentUserAction` is server-computed; the switch filters on it. (This is the first `FluentSelect`/`FluentSearch` usage in the Agents UI — net-new but the same Fluent v5 family.)
  - [x] **`<States>` slot — the six AgentSurfaceKind states, using `<AgentSurfaceState Kind="..." TestId="agents-proposal-queue-state" .../>`:** `_result is null` → `Loading`; `Status is PendingProposalsInspectionStatus.NotAuthorized` → `PermissionDenied`; `Status is PendingProposalsInspectionStatus.Unavailable` → `Error`; `Status is PendingProposalsInspectionStatus.Stale` → `Stale` with `OnRefresh="ReloadAsync"`; `(Status is Success or Stale) && Proposals.Count == 0` → `Empty`; `VisibleRows.Count == 0` (filters active) → `FilteredEmpty` with `OnReset="ResetFilters"`. **Extend (don't blindly copy) the ProviderCatalog `<States>` pattern:** ProviderCatalog only wires four branches (`Loading`/`PermissionDenied`/`Empty`/`FilteredEmpty`) and no Agents page has yet wired `Error` or `Stale`. `AgentSurfaceState` already supports `Error`, `Stale`, `OnReset`, and `OnRefresh` (params + `-reset`/`-refresh` test ids exist), so the `Error`/`Stale`+refresh branches are **net-new wiring over an existing component** — `AgentSurfaceKind` already defines exactly these six (`Loading, Empty, FilteredEmpty, Error, PermissionDenied, Stale`) with the correct `role`/`aria-live` politeness, so no new surface component is needed. `ResetFilters()` clears every filter field; `ReloadAsync()` re-invokes the gateway.
  - [x] **`<Body>` slot:** render the `FluentDataGrid` only when `ShowGrid => VisibleRows.Count > 0` (mirror ProviderCatalog).
  - [x] **Deterministic "age":** the age column derives from `CreatedAt` relative to "now". Inject `TimeProvider` into the page (`@inject TimeProvider TimeProvider`, default `TimeProvider.System` from the host) and pass `TimeProvider.GetUtcNow()` into the pure `ProposedAgentReplyStatePresentation` age helper — do NOT call `DateTimeOffset.UtcNow` inline (untestable / flaky). bUnit tests register a `FakeTimeProvider` (or substitute) so the age renders deterministically. When `CreatedAt` is null, show a safe "unknown age" whole string.
  - [x] **AC3 count/status indication:** surface `_result.PendingCount` as a visible localized indication in the page `Eyebrow`/`Heading` area (e.g. `Localizer["Agents.ProposalQueue.PendingCount", _result?.PendingCount ?? 0]`) with a stable `data-testid="agents-proposal-queue-count"`, shown only on `Success`/`Stale`. This is the "proposal count or status indication available to authorized Approvers" — the in-product V1 mechanism. (Surfacing the same count as a nav badge on the Story 1.8 overview is an additive future enhancement, **out of scope here**; the queue's own visible count satisfies AC3.)
  - [x] Defense-in-depth page authorization: `@attribute [Authorize(Policy = AgentsFrontComposerRegistration.AgentsApproverPolicy)]` (the new approver policy const added in Task 5). Nav hiding alone is not authorization; the real decision is server-side and shared with the API (AD-15 parity) — the UI gateway already fails closed.

- [x] **Task 5 - Register the approver-facing nav entry + policy constant in `Hexalith.Agents.UI/Composition/AgentsFrontComposerRegistration.cs`** (AC: #1, #4)
  - [x] Add the constant `public const string AgentsApproverPolicy = "Agents.Approver";` with XML doc **mirroring the existing `AgentsAdministratorPolicy` doc**: this gates the approver-facing proposal-queue nav entry + page; nav hiding alone is not authorization (the page also carries `[Authorize(Policy = …)]` and the real decision is server-side, shared with the API — AD-12/AD-15); registering the actual `AddAuthorizationCore(... AddPolicy(...))` lives in the **deferred host `Program.cs`** — the constant + entry/page wiring is in scope. This is the first **Approver** (not Administrator) surface — the queue's audience is Approvers, not only Agent Administrators (PRD glossary: Approver ≠ Administrator).
  - [x] In `RegisterDomain(...)` append a sixth nav entry (Order 5) — **mirror the existing `AddNavEntry` calls**: `new FrontComposerNavEntry("agents", "Pending proposals", "/agents/proposals", Icon: "Regular.Size20.<curated glyph e.g. MailInbox/Inbox/TaskListSquare>", Order: 5, RequiredPolicy: AgentsApproverPolicy, TitleKey: "Agents.Navigation.ProposalQueue", Resource: typeof(AgentsResources))`. Use a glyph that exists in the curated `FcFluentIcons` vocabulary (verify against the factory as the existing entries did — reuse an existing `Regular.Size20.*` name if the ideal one is absent, exactly as the "Conversation call" entry reused `ChevronRight`). Also update the `RegisterDomain` XML doc comment that currently says "five ordered ... nav entries" to "six" so the doc stays accurate.

- [x] **Task 6 - Localization (en + fr) in `Hexalith.Agents.UI/Resources/`** (AC: #1, #2, #3)
  - [x] Add **whole-string** keys to `AgentsResources.resx` (en) AND `AgentsResources.fr.resx` (fr) — en/fr parity is enforced by `LocalizationResourceTests`; never assemble sentences from runtime fragments (UX-DR14). Keys: `Agents.Navigation.ProposalQueue`; page chrome `Agents.ProposalQueue.{Title,Eyebrow,Description,ControlsLabel}`; columns `Agents.ProposalQueue.Column.{State,SourceConversation,Caller,Agent,Responsibility,Expiry,Age}`; responsibility `Agents.ProposalQueue.Responsibility.{You,Approver}`; expiry `Agents.ProposalQueue.Expiry.{None,ExpiringSoon,Expired,Label}`; age `Agents.ProposalQueue.Age.*`; filters `Agents.ProposalQueue.Filter.{NeedsMyAction,State,Agent,SourceConversation,Caller,Expiry}` (+ option labels, incl. an "all"/"any" sentinel); count `Agents.ProposalQueue.PendingCount` (with a named `{0}` placeholder); state labels `Agents.ProposalState.Label.{Unknown,Pending}` (only the shipped values; reserved states are added by their owning stories). Reuse the existing shared `Agents.Surface.{Loading,Empty,FilteredEmpty,Error,PermissionDenied,Stale}.{Title,Message}` keys for the surface states (already present — confirm they exist; do not duplicate). Keep all copy free of the word "message" for unapproved content (UX forbidden-copy rule).

- [x] **Task 7 - Tests + narrow verification** (AC: #1, #2, #3, #4)
  - [x] **Contracts (`tests/Hexalith.Agents.Contracts.Tests`):** new `PendingProposalsContractsTests` (xUnit v3 + Shouldly; no NSubstitute): System.Text.Json round-trip for `PendingProposalView`, `PendingProposalsResult` (incl. nested view list), and `ListPendingProposalsQuery`; `PendingProposalsInspectionStatus` serializes **by name** and unknown input → `Unknown` sentinel; factory invariants — `NotAuthorized()`/`Unavailable()` carry an **empty** `Proposals` list AND `PendingCount == 0` (AC4 — a failed/denied result never fingerprints records), `Success(rows, n)` round-trips the rows + count; AD-14 no-content-leak: serialize a `PendingProposalView` and assert the generated-content sample (`AgentInteractionTestData.GeneratedContentText`) and no secret token appears (mirror the 3.1 contract no-leak assertions). The assembly-wide `ContractsSecretNonDisclosureTests` auto-covers the new view — confirm it stays green and unweakened.
  - [x] **UI (`tests/Hexalith.Agents.UI.Tests`, bUnit + xUnit v3 + Shouldly + NSubstitute):** new `ProposalQueueTests` extending `AgentsTestContext` (substitutes `IProposalQueueGateway`; `StubAgentsLocalizer` returns keys so assertions compare keys) — **mirror `ProviderCatalogTests`**: (a) authorized non-empty result → grid renders one row per proposal with the safe columns + state badge; (b) the dispatched read uses the expected args (`Received().ListPendingProposalsAsync(false, Arg.Any<CancellationToken>())`); (c) **each surface state** — `null`/before-load → `Loading`; `NotAuthorized` → `PermissionDenied`; `Unavailable` → `Error`; `Stale` → `Stale` (refresh button present, `data-testid …-refresh`); `Success`+empty → `Empty`; (d) **filter → filtered-empty → reset** flow for the "needs my action" switch and the state `FluentSelect` (filtered to zero rows → `FilteredEmpty` with `…-reset`, reset restores rows); (e) **AC3 count** — count indication renders the pending count (`data-testid="agents-proposal-queue-count"`) only on `Success`/`Stale`; (f) **AC4 no-leak** — on `NotAuthorized`/`Empty`, assert the rendered output `ShouldNotContain` any sample proposal id / caller / conversation id / content sample and shows no count/rows (mirror ProviderCatalog's secret-absent assertions). Add proposal-queue cases to `AccessibilityTests` (the page exposes `role="main"` heading with `tabindex="-1"`, the grid has table semantics, the `[Theory]` over `AgentSurfaceKind` role/aria-live already covers politeness — assert the queue surfaces use it) and `BadgeConformanceTests` (the new `ProposedAgentReplyStateBadge` renders color + icon + text, no raw hex). Add the new keys to `LocalizationResourceTests` coverage (en/fr parity is auto-scanned). Extend `AgentUiTestData` with a `PendingProposal(...)`/`PendingProposalsResult(...)` factory (defaults + `with` overrides) mirroring its `Entry(...)`/`Status(...)` factories.
  - [x] **Presentation unit tests (`tests/Hexalith.Agents.UI.Tests`, no bUnit):** `ProposedAgentReplyStatePresentationTests` — `ColorFor`/`IconFor`/`LabelKeyFor` are **total** over every `ProposedAgentReplyState` value (incl. reserved → subtle/question-mark default, mirroring the `MapStatus_is_total` precedent so the reserved 3.3–3.6 states never throw); the age helper is deterministic given a fixed `now`.
  - [x] **Regression sweep (additive-enum safety):** grep the UI/contracts tests for any exhaustive enumeration over `ProposedAgentReplyState`, `AgentInteractionStatus`, or `AgentSurfaceKind` and confirm the new presentation/contracts paths keep them total/green. No write-side aggregate/contract changes in this story, so the 2.x/3.1 aggregate, contract, and server tests stay green unchanged — confirm, don't modify.
  - [x] xUnit v3 + Shouldly; PascalCase BDD-style names (`{Subject}_{scenario}_{expectation}`); no raw `Assert.*`; bUnit for components only (NSubstitute the gateway, never the presentation/contracts). Build/test commands (run from `Hexalith.Agents/`): `dotnet restore Hexalith.Agents.slnx`; `dotnet build Hexalith.Agents.slnx --configuration Release -m:1` (**0 warnings / 0 errors**, warnings-as-errors); then each touched test project individually: `dotnet test tests/Hexalith.Agents.Contracts.Tests/Hexalith.Agents.Contracts.Tests.csproj --configuration Release` and `dotnet test tests/Hexalith.Agents.UI.Tests/Hexalith.Agents.UI.Tests.csproj --configuration Release` (and the Contracts/Server guard suites if touched). **Before marking the story for review:** regenerate the Debug Log test counts from the actual run output and diff the File List against `git status` (Epic 2 retro Action Item 1 — the recurring stale-count / File-List-omission finding).

## Dev Notes

### Critical Guardrails

- This story is **the read-side + UI discovery surface for Proposed Agent Replies — the in-product "proposal queue" — ONLY.** Do NOT implement: edit (Story 3.3), regenerate (3.4), approve-then-post (3.5), reject/abandon/expire enforcement (3.6), the proposal-detail/version-history/accessibility workspace (3.7), active notifications (deferred, PRD OQ-4), or any write-path command. The queue is **read-only**: it lists, filters, and counts proposals; row-level actions land in 3.3–3.7. [Source: epics.md#Story-3.2; #Story-3.3..3.7]
- **Follow the established read+UI story shape — this is the exact twin of Story 2.6's call-status read.** Story 2.6 added a safe display contract + a UI gateway seam + a `Deferred*` placeholder + a Fluent surface + tests, and **deferred the entire server read-model/projection/query-handler/BFF binding** to Epic 4. Story 3.2 does the same for the proposal queue: stable `ListPendingProposalsQuery`/`PendingProposalView`/`PendingProposalsResult` contracts + `IProposalQueueGateway`(+`Deferred*`) + `ProposalQueue.razor` + presentation/badge + tests. **No server read code, no projection, no query handler, no Client/BFF wiring in this story** — the deferred gateway fails closed to `NotAuthorized` so the surface renders `PermissionDenied` until Epic 4 wires the live read path. [Source: 2-6-conversation-invocation-ux-and-call-status-feedback.md; ARCHITECTURE-SPINE.md#AD-15; #Consistency-Conventions (Projections deferred); GetAgentInteractionStatusQuery.cs (deferred-binding remark)]
- **A Proposed Agent Reply is NEVER a Conversation Message, and the queue must never render it as one (AC1, AD-6).** The queue is a `FluentDataGrid` table of safe ids/enums/timestamps — visually and structurally distinct from the conversation thread; it carries no content. Microcopy must never call unapproved generated content a "message" (UX forbidden copy). The generated content's sole durable home stays the Story 2.4 `AgentOutputGenerated` event/state; the queue references only the proposal/version *ids*. AC1 holds by construction — the view carries no content field at all. [Source: ARCHITECTURE-SPINE.md#AD-6; DESIGN.md#components ("a proposed reply is never styled like an already-posted Conversation Message"); DESIGN.md#do-s-and-don-ts; EXPERIENCE.md#voice-and-tone (forbidden copy)]
- **Sensitive-content & secret safety on a READ surface (AC1, AC4, AD-14).** The `PendingProposalView` transports ONLY safe references: proposal/interaction/version ids, Source-Conversation reference, caller Party reference (not PII — AD-7), Agent id, policy-version numbers, optional ISO-8601 expiry/created timestamps, and the coarse `State`/`InteractionStatus`/`NeedsCurrentUserAction` flags. It must NEVER carry generated/edited content, a raw provider/Conversations payload, an EventStore stream name, a stack trace, or a secret — in fields, badge text, accessible names, tooltips, error details, or the count. The assembly-wide `ContractsSecretNonDisclosureTests` scans the new view automatically; keep member names clear of `Secret`/`ApiKey`/`Credential`/`Password`/`ConnectionString`. [Source: ARCHITECTURE-SPINE.md#AD-14; EXPERIENCE.md#accessibility-floor ("Provider secrets, raw payloads, and unrelated tenant data must not appear in accessible names, tooltips, copied text, diagnostics, or announcements"); ContractsSecretNonDisclosureTests.cs]
- **Fail closed; disclose nothing on denial (AC4, AD-12, FR-19, FR-20, FR-21).** The result envelope mirrors `ProviderCatalogInspectionResult`: `NotAuthorized()`/`Unavailable()` carry an **empty list + zero count**, so a denial/fault never fingerprints another tenant's records via counts, empty states, filters, accessible names, or error details. The deferred gateway returns `NotAuthorized` by default (deny). The "needs my action" flag, the count, and the row set are all computed server-side from tenant scope + the snapshotted `ApproverPolicyVersion` + current dependency availability — the read **is** an authorization gate (FR-20 lists "proposal discovery" as a governed action). The denial must be **auditable** (the deferred state cannot audit yet; record in dev notes that the live read-model story owns the auditable read-authorization decision, shared with the API per AD-15). [Source: ARCHITECTURE-SPINE.md#AD-12; #AD-15; prd.md#4-7 (FR-19/FR-20/FR-21); ProviderCatalogInspectionResult.cs]
- **In-product visibility is the V1 mechanism; notifications are deferred (AC3, FR-13, OQ-4).** PRD FR-13 mandates "an in-product pending-proposal visibility surface for authorized Approvers, including proposal count or status indication," and explicitly accepts that if active notifications are not in V1, Approvers rely on this surface (OQ-4 leaves the notification path unresolved). This story delivers that surface + the visible count. The **formal launch-readiness gate recording** ("the system records that Approvers rely on…") is owned by Epic 4 Story 4.4 (launch-readiness gates) — here, satisfy AC3 by (a) shipping the count/status indication and (b) documenting the notification deferral in code comments + this story; do NOT build a notification path. [Source: prd.md#4-6 (FR-13); prd.md#12 (OQ-4); EXPERIENCE.md#information-architecture ("pending proposal count"); EXPERIENCE.md#open-questions (OQ-4); epics.md#Story-4.4]
- **Approver ≠ Administrator — this is the first approver-facing surface.** All five existing Agents nav entries are gated by `AgentsAdministratorPolicy`. The proposal queue's audience is **Approvers** (PRD glossary: a Party authorized by the Agent's Approver Policy). Add a distinct `AgentsApproverPolicy = "Agents.Approver"` const and gate the queue nav entry + page with it. The actual `AddPolicy(...)` registration is in the deferred host `Program.cs` (as the admin-policy doc already notes); the const + nav/page wiring is in scope. Do NOT invent server-side approver-resolution semantics here — that is the deferred read-model binding (AD-8). [Source: AgentsFrontComposerRegistration.cs (AgentsAdministratorPolicy precedent); prd.md#3 (Approver, Approver Policy); prd.md#4-3 (FR-7); ARCHITECTURE-SPINE.md#AD-8]
- **Read-model freshness is first-class (AD-12 / architecture Projections convention).** The `Stale` inspection status + the `Stale` `AgentSurfaceKind` exist so degraded/stale projection data is never rendered as fresh. The live projection computes freshness; in the deferred state the path is `NotAuthorized`, but the contract + UI handle `Stale`/`Unavailable` now (tested via the substituted gateway). [Source: ARCHITECTURE-SPINE.md#AD-12; #Consistency-Conventions ("Read models expose freshness/degraded/blocked states. Stale state must not be rendered or treated as fresh"); AgentSurfaceKind.cs]

### Design: Where Each Responsibility Lives (the AD-15 UI/read parity, deferred read path)

```
Approver opens /agents/proposals  (nav + page gated by Agents.Approver; AuthorizeView + [Authorize])
  ProposalQueue.razor
    ├─ OnInitializedAsync → IProposalQueueGateway.ListPendingProposalsAsync(includeHistorical:false)
    │        (live: → Hexalith.Agents.Client → BFF/API → Agents read-model)   [DEFERRED → Epic 4]
    │        (now:  DeferredProposalQueueGateway → PendingProposalsResult.NotAuthorized())  ← fail closed
    ├─ <States> maps result → AgentSurfaceKind  (Loading | PermissionDenied | Error | Stale | Empty | FilteredEmpty)
    ├─ <Filters> (Fluent v5: switch + selects/search) → in-memory VisibleRows  (needs-my-action, state, agent, conv, caller, expiry)
    ├─ count indication ← PendingProposalsResult.PendingCount   (AC3; Success/Stale only)
    └─ <Body> FluentDataGrid<PendingProposalView>  (pinned: state badge, source conversation, caller, agent, responsibility, expiry, age)
                 each cell = safe id/enum/timestamp/whole-string — NEVER content (AD-14); never styled as a Conversation Message (AD-6)

Server read-model / projection / query-handler / Client-BFF binding that computes
  rows + NeedsCurrentUserAction + age + freshness from EventStore  ───────────────  [DEFERRED → Epic 4 read-model story, mirroring Story 1.2 / 2.6]
```

[Source: ARCHITECTURE-SPINE.md#AD-15; #AD-12; ProviderCatalog.razor; FcAggregateListPage; AgentSurfaceState.razor; 2-6-…md]

### Current Code State To Preserve

Read these completely before editing — they are the exact patterns to mirror (do NOT reshape them):

- `Hexalith.Agents/src/Hexalith.Agents.Contracts/ProviderCatalog/ProviderCatalogInspectionResult.cs` + `Queries/ListProviderCatalogEntriesQuery.cs` + `ProviderCatalogEntryView.cs` — **the exact list-read contract trio**: a `record Query(bool IncludeX)` (no tenantId), a safe `record EntryView(...)` of references, and a `record Result(Status, IReadOnlyList<View>)` whose `NotAuthorized()`/`NotFound()` factories return an **empty** list. Mirror for `ListPendingProposalsQuery`/`PendingProposalView`/`PendingProposalsResult` (+ the `PendingCount`).
- `Hexalith.Agents/src/Hexalith.Agents.Contracts/AgentInteraction/AgentInteractionStatusView.cs` + `AgentInteractionInspectionResult.cs` + `AgentInteractionInspectionStatus.cs` — the safe per-interaction view (the field-shape template: ids/enums/version-numbers, **no content**), the fail-closed `Success/NotAuthorized/NotFound` envelope (null view on failure), and the `Unknown=0 …` by-name status enum. Mirror the view's content-exclusion XML and the status-enum convention.
- `Hexalith.Agents/src/Hexalith.Agents.Contracts/AgentInteraction/Queries/GetAgentInteractionStatusQuery.cs` (and `GetAgentInteractionGateEvidenceQuery.cs`, `GetAgentInteractionContextEvidenceQuery.cs`) — the **deferred read-query** precedent: a stable query contract whose EventStore `IDomainQueryHandler`/`IReadModelStore` binding is explicitly deferred to the read-model story. Copy that remark style for `ListPendingProposalsQuery`.
- `Hexalith.Agents/src/Hexalith.Agents.Contracts/AgentInteraction/{ProposedAgentReplyState,AgentProposedReplyEvidence}.cs` + `AgentInteractionStatus.cs` — the Story 3.1 proposal types the view surfaces: `ProposedAgentReplyState` (`Unknown=0, Pending` — reserved 3.3–3.6 values), the safe `AgentProposedReplyEvidence(ProposalId, SourceConversationId, ProposedVersionId, ApproverPolicyVersion, ContentSafetyPolicyVersion, ExpiresAt)` (ids only, the source of the view's safe fields), and `AgentInteractionStatus.ProposalCreated=12`. The view ADDS caller/Agent/needs-action/created-at that the evidence does not carry.
- `Hexalith.Agents/src/Hexalith.Agents.UI/Components/Pages/ProviderCatalog.razor` — **the canonical filterable-grid page**: `FcAggregateListPage` slots (`Filters`/`States`/`Body`), the `<States>` branch order, `VisibleRows`/`ShowGrid`/`ResetFilters`, the `GridSort<T>` static, heading focus, `@inject IStringLocalizer<AgentsResources>` + gateway, `.ConfigureAwait(false)`. Mirror exactly for `ProposalQueue.razor`.
- `Hexalith.Agents/src/Hexalith.Agents.UI/Components/Shared/AgentSurfaceState.razor` + `AgentSurfaceKind.cs` — the shared six-state surface component + enum (`Loading, Empty, FilteredEmpty, Error, PermissionDenied, Stale`) with `role`/`aria-live` politeness and the `OnReset`/`OnRefresh` callbacks. **Reuse as-is** (no new surface component); reuse the existing `Agents.Surface.*` localization keys.
- `Hexalith.Agents/src/Hexalith.Agents.UI/Components/Shared/AgentCallStatusPresentation.cs` + `AgentCallStatusBadge.razor` + `AgentReadinessBadge.razor` — the pure status→`BadgeColor`/`Icon`/label-key mapping (total switch, role-not-hex, curated `FcFluentIcons`) + the badge component (color + icon + visible text). Mirror for `ProposedAgentReplyStatePresentation` + `ProposedAgentReplyStateBadge`.
- `Hexalith.Agents/src/Hexalith.Agents.UI/Services/Gateways/IConversationAgentCallGateway.cs` + `DeferredConversationAgentCallGateway.cs` + `AgentsUiServiceCollectionExtensions.cs` — the UI read-gateway seam (returns the public result wrapper, deferred-binding remark), the fail-closed `Deferred*` placeholder, and the `TryAddScoped` registration. Mirror for `IProposalQueueGateway`.
- `Hexalith.Agents/src/Hexalith.Agents.UI/Composition/AgentsFrontComposerRegistration.cs` — the policy const + ordered `AddNavEntry` calls (Order 0–4). Add `AgentsApproverPolicy` + the Order-5 entry.
- `Hexalith.Agents/src/Hexalith.Agents.UI/Resources/{AgentsResources.cs,AgentsResources.resx,AgentsResources.fr.resx}` — the `IStringLocalizer<AgentsResources>` whole-string convention (dotted keys, enum-keyed dynamic lookup allowed, en/fr parity). Add the new keys to both resx.
- `Hexalith.Agents/tests/Hexalith.Agents.UI.Tests/{AgentsTestContext,StubAgentsLocalizer,AgentUiTestData,ProviderCatalogTests,AccessibilityTests,BadgeConformanceTests,LocalizationResourceTests}.cs` — the bUnit base (`FrontComposerTestBase`, NSubstituted gateways, `RenderPage<T>`/`RenderInShell<T>`), the key-returning localizer stub, the `with`-override data factory, and the per-concern test templates. Mirror `ProviderCatalogTests` for `ProposalQueueTests`.

What must be preserved: `.slnx` only, `net10.0`, nullable, implicit usings, **warnings-as-errors** (CA2007 `ConfigureAwait(false)` on every await), Central Package Management (no inline versions), **Fluent v5 components only** (never raw `<button>/<input>/<select>/<textarea>` — they strip the a11y floor; raw `<a>` ok), bind status to a Fluent **role** not a hex, whole-string localization with en/fr parity, content-free + secret-free + Party-PII-free public contracts, and the deferred read-path/gateway binding convention (fail closed). [Source: Hexalith.EventStore/_bmad-output/project-context.md; Hexalith.FrontComposer/_bmad-output/project-context.md (Fluent-only, no-theme-redefinition, ULID-not-GUID); ARCHITECTURE-SPINE.md#Stack; #Consistency-Conventions; 2-6-…md]

### Naming Map (architecture / PRD term → this story's code name)

| Architecture / PRD term | This story's code name | Notes |
|---|---|---|
| In-product pending-proposal visibility surface | `ProposalQueue.razor` (route `/agents/proposals`) | The FR-13 mandated V1 surface. |
| Proposal queue read query | `ListPendingProposalsQuery(bool IncludeHistorical)` | Mirror `ListProviderCatalogEntriesQuery(bool IncludeDisabled)`; no tenantId on the record. |
| Safe proposal-queue row | `PendingProposalView` | Mirror `AgentInteractionStatusView`; references only, no content (AD-14). |
| Proposal-queue read result | `PendingProposalsResult(Status, Proposals, PendingCount)` | Mirror `ProviderCatalogInspectionResult`; empty+0 on failure (AC4). |
| Read outcome | `PendingProposalsInspectionStatus` (`Unknown=0, Success, NotAuthorized, Unavailable, Stale`) | Mirror `AgentInteractionInspectionStatus` + the freshness states. |
| "Needs my action" / current responsibility | `bool NeedsCurrentUserAction` (+ derived responsibility label) | Server-computed flag (deferred); UI derives the responsibility column. |
| Proposal count / status indication | `PendingProposalsResult.PendingCount` (queue heading) | AC3 in-product count. |
| UI read seam | `IProposalQueueGateway` / `DeferredProposalQueueGateway` | Mirror `IConversationAgentCallGateway`; deferred → `NotAuthorized`. |
| Proposal state badge | `ProposedAgentReplyStateBadge` + `ProposedAgentReplyStatePresentation` | Mirror `AgentCallStatusBadge` + `AgentCallStatusPresentation`; total switch. |
| Approver authorization policy (UI gate) | `AgentsApproverPolicy = "Agents.Approver"` | First Approver (not Administrator) surface. |

[Source: ARCHITECTURE-SPINE.md#Consistency-Conventions (Naming); prd.md#3 (glossary); epics.md#Story-3.2]

### Sensitive-Data Handling (AD-9 / AD-14 / FR-19 / FR-24)

- The `PendingProposalView` is a **read projection of safe references** — it must never carry generated/edited content, prompts, context, raw provider/Conversations payloads, EventStore stream names, stack traces, or secrets, in any field, badge label, accessible name, tooltip, count, or error detail. Content's sole durable home stays Story 2.4's `AgentOutputGenerated` event/state; the queue references the proposal/version *ids* only. The deferred gateway returns no rows at all. [Source: ARCHITECTURE-SPINE.md#AD-14; AgentInteractionStatusView.cs (content-exclusion precedent); EXPERIENCE.md#accessibility-floor]
- Tenant isolation & non-disclosure (AC4, FR-19): a `NotAuthorized`/`Unavailable` result carries an empty list + zero count, so counts/empty-states/filters/accessible-names/error-details cannot reveal cross-tenant existence. Tenant scope is threaded server-side (the deferred binding); the query record carries no tenantId. [Source: prd.md#4-7 (FR-19); ProviderCatalogInspectionResult.cs]
- The Story 1.1 secret-non-disclosure guard (forbidden member tokens `Secret`/`ApiKey`/`Credential`/`Password`/`ConnectionString`) still applies — keep new member names clear of them and do not weaken `ContractsSecretNonDisclosureTests`. [Source: ContractsSecretNonDisclosureTests.cs; 3-1-…md#Sensitive-Data-Handling]

### Accessibility (UX accessibility floor + review-accessibility findings)

- **Surface states** reuse `AgentSurfaceState` which already maps `Error`/`PermissionDenied` → `role="alert"`/assertive and the rest → `role="status"`/polite. Empty/permission-denied never leak or fingerprint records. [Source: EXPERIENCE.md#state-patterns; AgentSurfaceState.razor]
- **Grid semantics:** the `FluentDataGrid` exposes table semantics, header relationships, sort/filter state, and safe row/column names; every status badge is color **+ icon + visible text** (never color-only). Accessible names use the same safe localized whole strings — never ids, content, or PII. [Source: EXPERIENCE.md#accessibility-floor; DESIGN.md#colors ("no-color-only"); review-accessibility.md]
- **Heading focus** on load (`HeadingId` + `tabindex="-1"` + `FocusHeadingAsync()`), matching every Agents page. Skip-links/landmarks (`role="main"` `#fc-main-content`) are owned by `FrontComposerShell` — do not re-add. [Source: AccessibilityTests.cs; ProviderCatalog.razor]
- **Live-region politeness** for any future row-removal/refresh announcement should be polite, not assertive, for ordinary queue updates (review-accessibility transition-matrix finding). Row-level focus-return-after-action and `Esc` semantics belong to Stories 3.3–3.7 (no row actions in 3.2). [Source: EXPERIENCE.md#accessibility-floor; review-accessibility.md (lines 7–9)]

### Latest Technical Information

- **No new references or NuGet packages this story.** Reuse the registered FluentUI (`Microsoft.FluentUI.AspNetCore.Components` `5.0.0-rc.3-26138.1`), the FrontComposer `Contracts`/`Shell`, and the in-module Contracts/Client refs. Do NOT add a provider SDK, Agent Framework, Dapr-runtime, Conversations/Parties/Tenants clients, or `Hexalith.Agents.Server` (the UI must not reference Server — `ProjectReferenceDirectionTests` enforces UI → Contracts/Client only). `FluentSelect`/`FluentSearch` are net-new *usages* of the already-referenced Fluent v5 package, not new dependencies. [Source: Hexalith.Agents.UI.csproj; ProjectReferenceDirectionTests.cs; Hexalith.FrontComposer/_bmad-output/project-context.md]
- Stack baseline unchanged: .NET `10.0.300`+, `net10.0`, `.slnx`, Central Package Management, xUnit v3 `3.2.2`, bUnit, Shouldly `4.3.0`, NSubstitute `5.3.0`, warnings-as-errors, `ConfigureAwait(false)` (CA2007). Build serialized with `-m:1`; run test projects individually (never solution-level `dotnet test`). [Source: Hexalith.EventStore/_bmad-output/project-context.md; 3-1-…md#Latest-Technical-Information]

### Testing Requirements

- **Contracts:** JSON round-trip (view, result, query); `PendingProposalsInspectionStatus` by-name + unknown→`Unknown`; factory invariants (`NotAuthorized()`/`Unavailable()` → empty list + zero count); AD-14 no-content-leak; secret guard green.
- **UI (bUnit):** grid renders rows with safe columns + state badge; gateway called with expected args; **all six surface states** (Loading/PermissionDenied/Error/Stale/Empty/FilteredEmpty) via the substituted gateway; filter→filtered-empty→reset; AC3 count indication (Success/Stale only); AC4 no-leak on NotAuthorized/Empty (no ids/caller/conversation/content/count rendered); accessibility (heading focus, table semantics, surface role/aria-live); badge conformance (color+icon+text, no hex); en/fr localization parity.
- **Presentation (no bUnit):** `ProposedAgentReplyStatePresentation` total over every state; deterministic age helper.
- Build/test commands (from `Hexalith.Agents/`):
  - `dotnet restore Hexalith.Agents.slnx`
  - `dotnet build Hexalith.Agents.slnx --configuration Release -m:1` (0 warnings / 0 errors)
  - `dotnet test tests/Hexalith.Agents.Contracts.Tests/Hexalith.Agents.Contracts.Tests.csproj --configuration Release`
  - `dotnet test tests/Hexalith.Agents.UI.Tests/Hexalith.Agents.UI.Tests.csproj --configuration Release`
- **Dev Agent Record accuracy (Epic 2 retro Action Item 1):** regenerate the Debug Log test counts from the latest run and diff the File List against `git status` before moving to review.

### Project Structure Notes

- New/changed code:
  - `Hexalith.Agents.Contracts/AgentInteraction/`: `PendingProposalView.cs` (new); `PendingProposalsInspectionStatus.cs` (new); `PendingProposalsResult.cs` (new); `Queries/ListPendingProposalsQuery.cs` (new). No edits to existing contracts (the Story 3.1 `ProposedAgentReplyState`/evidence/status are consumed, not changed).
  - `Hexalith.Agents.UI/`: `Services/Gateways/IProposalQueueGateway.cs` (new), `Services/Gateways/DeferredProposalQueueGateway.cs` (new), edit to `Services/Gateways/AgentsUiServiceCollectionExtensions.cs` (register the gateway); `Components/Shared/ProposedAgentReplyStatePresentation.cs` (new), `Components/Shared/ProposedAgentReplyStateBadge.razor` (new); `Components/Pages/ProposalQueue.razor` (new); edit to `Composition/AgentsFrontComposerRegistration.cs` (+`AgentsApproverPolicy` const, +Order-5 nav entry); edits to `Resources/AgentsResources.resx` + `AgentsResources.fr.resx` (new keys).
  - Tests: `tests/Hexalith.Agents.Contracts.Tests/PendingProposalsContractsTests.cs` (new); `tests/Hexalith.Agents.UI.Tests/ProposalQueueTests.cs` (new), `tests/Hexalith.Agents.UI.Tests/ProposedAgentReplyStatePresentationTests.cs` (new), edits to `AgentUiTestData.cs` (+proposal factory), and additive cases in `AccessibilityTests.cs`/`BadgeConformanceTests.cs`/`LocalizationResourceTests.cs` (auto-scan parity).
- **Deferred (explicitly NOT in this story):** the server read-model **projection** under `Hexalith.Agents.Server/Projections/` (still `.gitkeep`-only), the EventStore `IDomainQueryHandler`/`IReadModelStore` query-handler binding, the live `NeedsCurrentUserAction`/age/freshness computation, the `Hexalith.Agents.Client`→BFF/API read wiring, the read-authorization audit record, active notifications, and any nav-badge on the Story 1.8 overview. All belong to the Epic 4 read-model/operational-topology stories (mirroring how every prior read query and Story 2.6 deferred their bindings). [Source: 2-6-…md; GetAgentInteractionStatusQuery.cs; epics.md#Epic-4]
- Discovery loaded: root `epics.md` (Epic 3 + Story 3.2 + cross-stories 3.1/3.3–3.7), PRD (FR-7/FR-13/FR-14/FR-18/FR-19/FR-20/FR-21/FR-22/FR-24/FR-25 + NFRs §7 + glossary + OQ-3/OQ-4), architecture spine (AD-2/AD-3/AD-4/AD-5/AD-6/AD-8/AD-12/AD-14/AD-15 + Structural Seed + Consistency Conventions/Projections + Deferred), UX DESIGN/EXPERIENCE/review-accessibility (proposal-queue-grid columns/filters/six-state pattern/count/notifications-deferred/never-a-Conversation-Message/accessibility floor), and the as-built Story 2.6 + Story 1.8 + Story 3.1 code (Contracts views/queries/results + UI pages/gateways/surface-state/presentation/badge/nav/resources + UI/Contracts test fixtures). No root `project-context.md` exists for `agents`; sibling-module `project-context.md` files (EventStore + FrontComposer) supply carry-forward rules (deferred read-path bindings, fail-closed, Fluent-only UI, role-not-hex, no-theme-redefinition, ULID-not-GUID, `ConfigureAwait(false)`, `.slnx`-only, run test projects individually).

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-3.2-Discover-Pending-Proposals-In-Product]
- [Source: _bmad-output/planning-artifacts/epics.md#Epic-3-Proposal-Review-And-Approval-Workflow]
- [Source: _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md#4-6-proposed-agent-reply-workflow (FR-13, FR-14)]
- [Source: _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md#4-7-authorization-tenant-isolation-and-governance (FR-19, FR-20, FR-21)]
- [Source: _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md#4-8-admin-ui-and-api-client-contracts (FR-22)]
- [Source: _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md#4-9-audit-evidence-and-operational-visibility (FR-24, FR-25)]
- [Source: _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md#4-3-response-policy-and-approver-configuration (FR-7)]
- [Source: _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md#3-definitions (Proposed Agent Reply, Approver, Approver Policy, Confirmation Response Mode, Source Conversation, Versioned Proposal Content, Audit Evidence)]
- [Source: _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md#7-cross-cutting-non-functional-requirements (tenant isolation, privacy, observability, auditability)]
- [Source: _bmad-output/planning-artifacts/prds/prd-agents-2026-06-23/prd.md#12-open-questions-and-deferred-decisions (OQ-3 expiry duration, OQ-4 notification path)]
- [Source: _bmad-output/planning-artifacts/architecture/architecture-agents-2026-06-23-2/ARCHITECTURE-SPINE.md#AD-2-Aggregate-Boundaries]
- [Source: _bmad-output/planning-artifacts/architecture/architecture-agents-2026-06-23-2/ARCHITECTURE-SPINE.md#AD-5-Proposal-Lifecycle]
- [Source: _bmad-output/planning-artifacts/architecture/architecture-agents-2026-06-23-2/ARCHITECTURE-SPINE.md#AD-6-Conversations-Boundary]
- [Source: _bmad-output/planning-artifacts/architecture/architecture-agents-2026-06-23-2/ARCHITECTURE-SPINE.md#AD-8-Approver-Policy-Resolution]
- [Source: _bmad-output/planning-artifacts/architecture/architecture-agents-2026-06-23-2/ARCHITECTURE-SPINE.md#AD-12-Authorization-And-Dependency-Uncertainty]
- [Source: _bmad-output/planning-artifacts/architecture/architecture-agents-2026-06-23-2/ARCHITECTURE-SPINE.md#AD-14-Sensitive-Content-And-Secret-Safety]
- [Source: _bmad-output/planning-artifacts/architecture/architecture-agents-2026-06-23-2/ARCHITECTURE-SPINE.md#AD-15-Public-Surface-And-UI-Parity]
- [Source: _bmad-output/planning-artifacts/architecture/architecture-agents-2026-06-23-2/ARCHITECTURE-SPINE.md#Consistency-Conventions (Projections: read models expose freshness/degraded/blocked states)]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/DESIGN.md#components (proposal-queue-grid: FC-TBL/FluentDataGrid, pinned columns, state badge)]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/DESIGN.md#colors (no-color-only; proposal-state colors)]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/DESIGN.md#do-s-and-don-ts (never style a proposed reply like a posted Conversation Message)]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/EXPERIENCE.md#component-patterns (proposal-queue filters)]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/EXPERIENCE.md#state-patterns (loading/empty/filtered-empty/error/permission-denied/stale; filtered-empty reset)]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/EXPERIENCE.md#information-architecture (pending proposal count)]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/EXPERIENCE.md#accessibility-floor (table semantics, accessible names never leak, live-region politeness)]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/EXPERIENCE.md#voice-and-tone (forbidden copy: never call unapproved content a "message")]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-agents-2026-06-23/review-accessibility.md (focus-return fallback, live-region transition matrix, grid responsive semantics)]
- [Source: _bmad-output/implementation-artifacts/2-6-conversation-invocation-ux-and-call-status-feedback.md]
- [Source: _bmad-output/implementation-artifacts/3-1-create-proposed-agent-replies-in-confirmation-mode.md]
- [Source: _bmad-output/implementation-artifacts/1-8-admin-setup-ui-and-readiness-overview.md]
- [Source: Hexalith.Agents/src/Hexalith.Agents.Contracts/ProviderCatalog/ProviderCatalogInspectionResult.cs]
- [Source: Hexalith.Agents/src/Hexalith.Agents.Contracts/ProviderCatalog/Queries/ListProviderCatalogEntriesQuery.cs]
- [Source: Hexalith.Agents/src/Hexalith.Agents.Contracts/AgentInteraction/AgentInteractionStatusView.cs]
- [Source: Hexalith.Agents/src/Hexalith.Agents.Contracts/AgentInteraction/AgentInteractionInspectionResult.cs]
- [Source: Hexalith.Agents/src/Hexalith.Agents.Contracts/AgentInteraction/Queries/GetAgentInteractionStatusQuery.cs]
- [Source: Hexalith.Agents/src/Hexalith.Agents.Contracts/AgentInteraction/ProposedAgentReplyState.cs]
- [Source: Hexalith.Agents/src/Hexalith.Agents.Contracts/AgentInteraction/AgentProposedReplyEvidence.cs]
- [Source: Hexalith.Agents/src/Hexalith.Agents.UI/Components/Pages/ProviderCatalog.razor]
- [Source: Hexalith.Agents/src/Hexalith.Agents.UI/Components/Shared/AgentSurfaceState.razor]
- [Source: Hexalith.Agents/src/Hexalith.Agents.UI/Components/Shared/AgentSurfaceKind.cs]
- [Source: Hexalith.Agents/src/Hexalith.Agents.UI/Components/Shared/AgentCallStatusPresentation.cs]
- [Source: Hexalith.Agents/src/Hexalith.Agents.UI/Components/Shared/AgentCallStatusBadge.razor]
- [Source: Hexalith.Agents/src/Hexalith.Agents.UI/Services/Gateways/IConversationAgentCallGateway.cs]
- [Source: Hexalith.Agents/src/Hexalith.Agents.UI/Services/Gateways/AgentsUiServiceCollectionExtensions.cs]
- [Source: Hexalith.Agents/src/Hexalith.Agents.UI/Composition/AgentsFrontComposerRegistration.cs]
- [Source: Hexalith.Agents/tests/Hexalith.Agents.UI.Tests/ProviderCatalogTests.cs]
- [Source: Hexalith.Agents/tests/Hexalith.Agents.UI.Tests/AgentsTestContext.cs]
- [Source: Hexalith.EventStore/_bmad-output/project-context.md]
- [Source: Hexalith.FrontComposer/_bmad-output/project-context.md]

## Dev Agent Record

### Agent Model Used

Claude Opus 4.8 (1M context) — `claude-opus-4-8[1m]`

### Debug Log References

Build/test commands run from `Hexalith.Agents/` (.NET 10, Release, warnings-as-errors, `-m:1`):

- `dotnet build Hexalith.Agents.slnx --configuration Release -m:1` → **0 warnings / 0 errors**.
- `dotnet test tests/Hexalith.Agents.Contracts.Tests/...` → **217 passed, 0 failed, 0 skipped** (incl. the new `PendingProposalsContractsTests`).
- `dotnet test tests/Hexalith.Agents.UI.Tests/...` → **428 passed, 0 failed, 0 skipped** (incl. `ProposalQueueTests`, `ProposedAgentReplyStatePresentationTests`, and the additive badge/accessibility/localization/nav/composition/deferred-gateway cases).
- Regression confirm (untouched write-side): `dotnet test tests/Hexalith.Agents.Tests/...` → **557 passed**; `dotnet test tests/Hexalith.Agents.Server.Tests/...` → **253 passed**.

Counts regenerated from the actual run output; File List diffed against `git status` (Epic 2 retro Action Item 1). (2026-06-24 review correction: the Contracts/UI counts were re-verified against a fresh `--no-build` run — 217/428 — and corrected from the earlier stale 215/415.)

Two RC-version corrections made during the build loop (Fluent v5 `5.0.0-rc.3-26138.1`):

- `FluentSearch`/`FluentTextField` do not exist in the pinned RC — the contains-match filters use `FluentTextInput` (`TextInputType.Search`), the canonical v5 text input.
- Pinned `FluentDataGrid` columns require an explicit `Width` in this RC — the two `Pin="DataGridColumnPin.Start"` lead columns (state, source conversation) set `Width`.

### Completion Notes List

Implemented the read-side + UI discovery surface for Proposed Agent Replies — the in-product "proposal queue" — ONLY; no server read code, projection, query handler, or Client/BFF wiring (the live read-model binding is deferred to Epic 4, the deferred gateway fails closed to `NotAuthorized` so the surface renders `PermissionDenied` until then). This mirrors the Story 2.6 call-status read shape exactly.

- **Task 1 — Contracts:** `PendingProposalView` (content-free safe references only; AD-14), `PendingProposalsInspectionStatus` (`Unknown/Success/NotAuthorized/Unavailable/Stale` — by-name, `Unknown=0` sentinel; the two freshness states are net-new), `PendingProposalsResult` (mirror `ProviderCatalogInspectionResult`; `NotAuthorized()`/`Unavailable()` → empty list + zero count; `Success`/`Stale` carry rows + count), and `ListPendingProposalsQuery(bool IncludeHistorical)` (no tenantId; deferred-binding remark). The assembly-wide `ContractsSecretNonDisclosureTests` auto-covers the new view and stays green/unweakened.
- **Task 2 — UI gateway seam:** `IProposalQueueGateway` + fail-closed `DeferredProposalQueueGateway` (returns `NotAuthorized`) + `TryAddScoped` registration in `AddAgentsUi`.
- **Task 3 — Presentation + badge:** `ProposedAgentReplyStatePresentation` (total switches over `ProposedAgentReplyState`: `Pending → Informative`, reserved/`Unknown → Subtle`; curated `FcFluentIcons`; whole-string label keys; a deterministic coarse "age" bucket helper) + `ProposedAgentReplyStateBadge` (color + icon + visible text).
- **Task 4 — Page:** `ProposalQueue.razor` at `/agents/proposals` mirroring `ProviderCatalog.razor` — `FcAggregateListPage` slots, pinned `FluentDataGrid` of safe ids/enums/timestamps (never content, never styled as a Conversation Message), Fluent-v5-only filters (switch + selects + search inputs) over an in-memory `VisibleRows`, all six `AgentSurfaceKind` states (the `Error`/`Stale`+refresh branches are net-new wiring over the existing `AgentSurfaceState`), the AC3 `PendingCount` indication (Success/Stale only), deterministic age via injected `TimeProvider`, and `[Authorize(Policy = AgentsApproverPolicy)]`.
- **Task 5 — Nav + policy:** new `AgentsApproverPolicy = "Agents.Approver"` const (first Approver-gated surface) + Order-5 nav entry (reused the curated `Regular.Size20.Navigation` glyph since no inbox glyph exists in the vocabulary); `RegisterDomain` doc updated five→six.
- **Task 6 — Localization:** en + fr whole-string keys for nav, page chrome, columns, responsibility, expiry, age buckets, filters (+ all/any sentinels), the `{0}` count, and the shipped proposal-state labels; reused the existing `Agents.Surface.*` keys. en/fr parity auto-enforced by `LocalizationResourceTests`.
- **Task 7 — Tests:** contracts round-trip/factory/serialization/no-leak; presentation totality + deterministic age; bUnit grid/dispatch-args/six-surface-states/filter→filtered-empty→reset (switch and state select)/AC3-count/AC4-no-leak; additive badge-conformance, accessibility heading-focus, localization parity, nav-gating (admin entries vs approver-only queue), composition, and deferred-gateway cases.

AC coverage: **AC1** queue lists safe fields, no content rendered (view has no content field; grid is a table, never a Conversation Message). **AC2** filters narrow the queue; all six surface states distinguished; filtered-empty offers reset. **AC3** in-product visibility is the V1 mechanism (notifications deferred, documented in code + story); `PendingCount` indication shown to authorized Approvers. **AC4** denied/faulted reads carry empty list + zero count; nothing disclosed via counts/empty states/filters/accessible names; nav + page both fail closed (deferred binding owns the auditable decision per AD-15).

### File List

New — Contracts (`Hexalith.Agents/src/Hexalith.Agents.Contracts/`):
- `AgentInteraction/PendingProposalView.cs`
- `AgentInteraction/PendingProposalsInspectionStatus.cs`
- `AgentInteraction/PendingProposalsResult.cs`
- `AgentInteraction/Queries/ListPendingProposalsQuery.cs`

New — UI (`Hexalith.Agents/src/Hexalith.Agents.UI/`):
- `Services/Gateways/IProposalQueueGateway.cs`
- `Services/Gateways/DeferredProposalQueueGateway.cs`
- `Components/Shared/ProposedAgentReplyStatePresentation.cs`
- `Components/Shared/ProposedAgentReplyStateBadge.razor`
- `Components/Pages/ProposalQueue.razor`

Modified — UI (`Hexalith.Agents/src/Hexalith.Agents.UI/`):
- `Services/Gateways/AgentsUiServiceCollectionExtensions.cs` (register the proposal-queue gateway)
- `Composition/AgentsFrontComposerRegistration.cs` (`AgentsApproverPolicy` const + Order-5 nav entry + doc five→six)
- `Resources/AgentsResources.resx` (en keys)
- `Resources/AgentsResources.fr.resx` (fr keys)

New — Tests:
- `tests/Hexalith.Agents.Contracts.Tests/PendingProposalsContractsTests.cs`
- `tests/Hexalith.Agents.UI.Tests/ProposalQueueTests.cs`
- `tests/Hexalith.Agents.UI.Tests/ProposedAgentReplyStatePresentationTests.cs`
- `tests/Hexalith.Agents.UI.Tests/FixedTimeProvider.cs`

Modified — Tests (`tests/Hexalith.Agents.UI.Tests/`):
- `AgentsTestContext.cs` (proposal-gateway substitute + deterministic `TimeProvider`)
- `AgentUiTestData.cs` (`PendingProposal(...)` + `ProposalsResult(...)` factories)
- `BadgeConformanceTests.cs` (proposal-state badge cases)
- `AccessibilityTests.cs` (proposal-queue heading-focus)
- `LocalizationResourceTests.cs` (Story 3.2 keys in the parity scan)
- `AgentsNavigationTests.cs` (six entries; approver-gated queue vs admin entries)
- `AgentsUiCompositionTests.cs` (proposal-queue gateway registration)
- `DeferredGatewayTests.cs` (deferred proposal-queue gateway fails closed)

### Change Log

- 2026-06-24: Implemented Story 3.2 — proposal-queue read contracts + UI discovery surface (page, gateway seam, presentation/badge, approver nav/policy, en/fr localization) and tests. Build 0/0; Contracts 217, UI 428, aggregate 557, Server 253 tests passing. Server read-model/projection/query-handler/Client-BFF binding deferred to Epic 4. Status → review.
- 2026-06-24: Adversarial code review (story-automator). Re-ran build (0/0) and all four touched/regression suites; validated AC1–AC4 against the implementation and every `[x]` task against its files; confirmed File List matches `git status`. Single finding (MEDIUM, auto-fixed): stale Debug Log counts (Contracts 215→217, UI 415→428). No CRITICAL findings. Status → done.

## Senior Developer Review (AI)

- **Reviewer:** Administrator — 2026-06-24
- **Outcome:** Approved (auto-fix mode). 0 CRITICAL, 1 MEDIUM (fixed), 0 unresolved.
- **Scope reviewed:** All 9 new + 12 modified source/test files in the File List, cross-referenced against `git status` (0 discrepancies; only `_bmad-output/` tracking files changed outside the list, correctly excluded).
- **Verification (fresh `--no-build` runs):** `dotnet build Hexalith.Agents.slnx -c Release -m:1` → 0 warnings / 0 errors; Contracts 217, UI 428, aggregate 557, Server 253 — all passing.
- **AC validation:** AC1 IMPLEMENTED (safe columns; view has no content field, so "never a Conversation Message" holds by construction). AC2 IMPLEMENTED (six surface states wired incl. net-new Error/Stale; six filters; filtered-empty offers reset). AC3 IMPLEMENTED (`PendingCount` rendered on Success/Stale with a `{0}` placeholder verified in en + fr resx). AC4 IMPLEMENTED (fail-closed empty list + zero count; bUnit no-leak tests on NotAuthorized/Unavailable; nav + page both policy-gated).
- **Task audit:** all 7 tasks marked `[x]` verified done against their files. Contracts mirror `ProviderCatalogInspectionResult`/`AgentInteractionStatusView`; gateway + `Deferred*` mirror the call-status seam; presentation/badge are total switches with role-not-hex; page mirrors `ProviderCatalog.razor`.
- **Finding (MEDIUM, fixed):** Debug Log test counts were stale (Contracts 215, UI 415) versus the actual run (217, 428) — the recurring Epic 2 retro Action Item 1. Corrected the Debug Log and Change Log counts.
- **Low-severity notes (no change — would deviate from the story spec):** (1) an `Unknown`-status result falls through to the `Empty` surface rather than a fail-closed surface, but it discloses no rows/count and matches the `ProviderCatalog` precedent the story mandated; (2) `Agents.ProposalQueue.Expiry.{Label,Expired,ExpiringSoon}` resx keys are unused by the current cells but were explicitly requested by Task 6.
