<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <PackageId>Hexalith.Agents.Testing</PackageId>
    <Description>Shared test helpers for consumers of the Hexalith Agents module. Fixtures and builders are added by the stories that introduce testable behavior.</Description>
    <PackageTags>hexalith;agents;testing</PackageTags>
  </PropertyGroup>

  <!-- Public testing helpers depend only on the public contracts package graph. -->
  <ItemGroup>
    <ProjectReference Include="..\Hexalith.Agents.Contracts\Hexalith.Agents.Contracts.csproj" />
  </ItemGroup>

</Project>
