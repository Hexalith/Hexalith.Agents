namespace Hexalith.Agents.Contracts;

/// <summary>
/// Identifies the <c>Hexalith.Agents.Contracts</c> assembly for reflection-based composition and the
/// architecture boundary-guard tests. Contract types (Agent / ProviderCatalog / AgentInteraction
/// commands, events, and DTOs) are added by the Agent-setup stories (1.2-1.8); this marker is the only
/// type the buildable shell ships.
/// </summary>
public sealed class AgentsContractsAssemblyMarker
{
}
