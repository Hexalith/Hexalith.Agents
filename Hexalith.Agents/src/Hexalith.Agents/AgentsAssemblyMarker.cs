namespace Hexalith.Agents;

/// <summary>
/// Identifies the <c>Hexalith.Agents</c> domain assembly for SDK assembly-scan discovery (the canonical
/// EventStore domain-service host targets this assembly). Aggregate roots, events, and state are added by
/// the Agent-setup stories (1.2-1.8); this marker is the buildable-shell placeholder.
/// </summary>
public sealed class AgentsAssemblyMarker
{
}
